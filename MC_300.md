***
### QUESTION 300 解説

- 問題文:
Refer to the exhibit.  
An OSPF neighbor relationship between R2 and R3 is stuck in EXCHANGE/EXSTART state. The neighbor is established between R1 and R2. The network engineer can ping from R2 to R3 and vice versa, but the neighbor is still down. Which action resolves the issue?

- 問題文要約（日本語）:  
R2 と R3 の間で OSPF ネイバーを張ろうとしているが、`show ip ospf neighbor` では  
- R2 側: neighbor 192.168.99.2 state **EXCHANGE/**  
- R3 側: neighbor 192.168.99.1 state **EXSTART/**  
の状態で止まっており、Full まで上がらない。[file:61]  
ICMP では相互に ping は成功するため L3 到達性はあるが、OSPF の隣接関係だけが確立しない。どの対応がこの問題を解決するかを問う。

- 選択肢:
A. Restore the Layer 2/Layer 3 connectivity issue in the ISP network.  
B. Match MTU on both router interfaces or ignore MTU.  
C. Administrative “shut then no shut” both router interfaces.  
D. Enable OSPF on the interface, which is required.  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - OSPF 隣接が **EXSTART / EXCHANGE** 状態で止まり、ping は通る、という典型的な原因は「両端インターフェイスの MTU 不一致」。  
      OSPF は DBD（Database Description）パケットのやり取りの際に MTU をチェックし、値が異なるとネイバーを Full まで上げずに EXSTART/EXCHANGE で固まる挙動を取る。[file:24]
    - したがって、この問題は  
      - インターフェイスの `ip mtu` / `mtu` 値を両端で一致させる  
      - もしくはインターフェイス側に `ip ospf mtu-ignore` を設定して、OSPF による MTU チェックを無効化する  
      のいずれかで解決できる。  
      選択肢 B の「Match MTU on both router interfaces or ignore MTU」は上記の対策をそのまま表しているため正解。

- 他の選択肢がダメな理由
    - A: ping が成功していることから、L2/L3 の基本的な到達性には問題がない。ISP 側の接続を「復旧」するのは根本原因ではない。
    - C: インターフェイスを shut/no shut すると一時的に状態はリセットされるが、MTU 不一致が残っていれば再び EXSTART/EXCHANGE で止まるため、恒久的な解決にはならない。
    - D: 出力には既に `show ip ospf neighbor` でネイバーが見えているので、そのインターフェイスで OSPF は有効化されている。  
      有効化されていないならそもそも INIT/2-WAY すら表示されないため、この回答は状況と矛盾する。

***
### QUESTION 301 解説

- 問題文:
Refer to the exhibit.  
R1 and R2 have been configured with the following settings for forming a BGP neighbor relationship over 172.16.1.0/24:

R1 (AS 100)  
router bgp 100  
 neighbor 172.16.1.2 remote-as 200  
 neighbor 172.16.1.1 password cisco123  

R2 (AS 200)  
router bgp 200  
 neighbor 172.16.1.1 remote-as 100  
 neighbor 172.16.1.1 password cisco  

The neighbor relationship is not coming up. Which configuration resolves the issue?

- 問題文要約（日本語）:  
R1（AS100）と R2（AS200）は eBGP でピアを張ろうとしている。  
物理リンクは 172.16.1.1（R1 e0/0）– 172.16.1.2（R2 e0/0）。[file:62]  
しかし、BGP 設定を見ると、R1 側の `neighbor` と `password` 行の IP が一致していなかったり、両者で設定しているパスワード文字列も異なっており、その結果 BGP ネイバーが up しない。  
どの設定に変更すれば、この隣接関係を確立できるか。

- 選択肢:
A. R1  
   router bgp 100  
    neighbor 172.16.1.2 password cisco  

B. R1  
   router bgp 100  
    neighbor 172.16.1.2 password MD5 cisco  
   R2  
   router bgp 200  
    neighbor 172.16.1.1 password MD5 cisco  

C. R2  
   router bgp 200  
    neighbor 172.16.1.1 password cisco123  

D. R1  
   router bgp 100  
    neighbor 172.16.1.2 password MD5 cisco123  
   R2  
   router bgp 200  
    neighbor 172.16.1.1 password MD5 cisco123  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 現状の問題点は 2 つ:
        1. R1 でパスワードを設定している neighbor IP が誤り  
           - R1 は実際には `neighbor 172.16.1.2 remote-as 200` で R2 とピアを張ろうとしているのに、パスワード行は `neighbor 172.16.1.1 password ...` となっており、**自分自身の IP を指定してしまっている**。  
        2. R1 と R2 のパスワード文字列が不一致  
           - R1: `cisco123`、R2: `cisco`。BGP MD5 認証では両端で **完全一致したパスワード** が必要。  
    - 選択肢 A は R1 側で  
      `neighbor 172.16.1.2 password cisco`  
      と設定し、  
      - neighbor IP を正しく相手 (172.16.1.2) に修正  
      - パスワード文字列を R2 側の `cisco` と一致させる  
      ため、この一行で両方の問題が解消される。[file:24]
    - BGP のパスワードは明示的に `MD5` と書かなくても、`neighbor … password` でデフォルトが MD5 認証になるため、A のような設定で十分。[file:24]

- 他の選択肢がダメな理由
    - B: パスワード文字列は R1・R2 とも `cisco` で一致しているが、**R1 の元々の誤った neighbor IP（172.16.1.1）を修正する記述がない**。  
      問題文の設定のままに `MD5` を追加しただけなら、依然として R1 は自分自身に対してパスワードを設定していることになり、ピアは上がらない。
    - C: R2 側のパスワードを `cisco123` に変えるだけで、**R1 側の neighbor IP の誤りが残ったまま**。  
      そのため、R1 が本当にピアを結びたい 172.16.1.2 にはパスワードが設定されず、認証ミスマッチでネイバーは確立されない。
    - D: 両端でパスワード文字列は一致するが、B と同様に「R1 の neighbor IP の誤りを直す記述がない」点は変わらない。  
      `MD5` を明示しても、対象 neighbor の IP が正しくなければ意味がない。

***
### QUESTION 301 解説

- 問題文:
Refer to the exhibit.  
R1 and R2 have been configured with the following settings for forming a BGP neighbor relationship over 172.16.1.0/24:

R1 (AS 100)  
router bgp 100  
 neighbor 172.16.1.2 remote-as 200  
 neighbor 172.16.1.1 password cisco123  

R2 (AS 200)  
router bgp 200  
 neighbor 172.16.1.1 remote-as 100  
 neighbor 172.16.1.1 password cisco  

The neighbor relationship is not coming up. Which configuration resolves the issue?

- 問題文要約（日本語）:  
R1（AS100）と R2（AS200）は eBGP でピアを張ろうとしている。  
物理リンクは 172.16.1.1（R1 e0/0）– 172.16.1.2（R2 e0/0）。[file:62]  
しかし、BGP 設定を見ると、R1 側の `neighbor` と `password` 行の IP が一致していなかったり、両者で設定しているパスワード文字列も異なっており、その結果 BGP ネイバーが up しない。  
どの設定に変更すれば、この隣接関係を確立できるか。

- 選択肢:
A. R1  
   router bgp 100  
    neighbor 172.16.1.2 password cisco  

B. R1  
   router bgp 100  
    neighbor 172.16.1.2 password MD5 cisco  
   R2  
   router bgp 200  
    neighbor 172.16.1.1 password MD5 cisco  

C. R2  
   router bgp 200  
    neighbor 172.16.1.1 password cisco123  

D. R1  
   router bgp 100  
    neighbor 172.16.1.2 password MD5 cisco123  
   R2  
   router bgp 200  
    neighbor 172.16.1.1 password MD5 cisco123  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 現状の問題点は 2 つ:
        1. R1 でパスワードを設定している neighbor IP が誤り  
           - R1 は実際には `neighbor 172.16.1.2 remote-as 200` で R2 とピアを張ろうとしているのに、パスワード行は `neighbor 172.16.1.1 password ...` となっており、**自分自身の IP を指定してしまっている**。  
        2. R1 と R2 のパスワード文字列が不一致  
           - R1: `cisco123`、R2: `cisco`。BGP MD5 認証では両端で **完全一致したパスワード** が必要。  
    - 選択肢 A は R1 側で  
      `neighbor 172.16.1.2 password cisco`  
      と設定し、  
      - neighbor IP を正しく相手 (172.16.1.2) に修正  
      - パスワード文字列を R2 側の `cisco` と一致させる  
      ため、この一行で両方の問題が解消される。[file:24]
    - BGP のパスワードは明示的に `MD5` と書かなくても、`neighbor … password` でデフォルトが MD5 認証になるため、A のような設定で十分。[file:24]

- 他の選択肢がダメな理由
    - B: パスワード文字列は R1・R2 とも `cisco` で一致しているが、**R1 の元々の誤った neighbor IP（172.16.1.1）を修正する記述がない**。  
      問題文の設定のままに `MD5` を追加しただけなら、依然として R1 は自分自身に対してパスワードを設定していることになり、ピアは上がらない。
    - C: R2 側のパスワードを `cisco123` に変えるだけで、**R1 側の neighbor IP の誤りが残ったまま**。  
      そのため、R1 が本当にピアを結びたい 172.16.1.2 にはパスワードが設定されず、認証ミスマッチでネイバーは確立されない。
    - D: 両端でパスワード文字列は一致するが、B と同様に「R1 の neighbor IP の誤りを直す記述がない」点は変わらない。  
      `MD5` を明示しても、対象 neighbor の IP が正しくなければ意味がない。

***
### QUESTION XXX 解説

- 問題文:
Refer to the exhibit.  
The diagram shows that R1 has the OSPF router ID 10.255.255.1, and a message similar to the following is seen:  
`%OSPF-4-FLOOD_WAR: ... type-5 adv-rtr 10.255.255.1 ...`  
After configuring OSPF on R1, some external destinations in the network became unreachable. Which action resolves the issue?

- 問題文要約（日本語）:  
R1 に OSPF を設定したところ、ネットワーク上のいくつかの外部宛先が到達不能になった。  
ログには「`%OSPF-4-FLOOD_WAR` type-5 adv-rtr 10.255.255.1」といったメッセージが出ており、R1 の OSPF Router ID は 10.255.255.1 になっている。  
これは OSPF の外部 LSA（Type 5）に関するフラッディング警告であり、どの対処を行えば問題を解消できるかを問う問題。[file:24]

- 選択肢:
A. Change the R1 router ID from 10.255.255.1 to a unique value and clear the process.  
B. Increase the SPF delay interval on R1 to synchronize routes.  
C. Disconnect the router with the OSPF router ID 0.0.0.0 from the network.  
D. Clear the OSPF process on R1 to flush stale LSAs sent by other routers.  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - `%OSPF-4-FLOOD_WAR` は、「ルータが**自分自身が生成した LSA を、別ルータ経由で再度受け取り続けている**」ときに出る警告で、  
      よくある原因は「同じ OSPF Router ID を持つルータが複数存在すること」。特に Type-5 LSA（外部経路）については、**異なるエリアで同じ Router ID を使って ASBR が存在**すると、このような状況が起こる。[file:24]
    - 今回、ログの `adv-rtr 10.255.255.1` は「LSA を広告したルータ ID」が 10.255.255.1 であることを示しており、R1 も Router ID 10.255.255.1 を使っている。  
      つまり **他の ASBR も同じ Router ID を使っている** 可能性が高く、外部 LSA が衝突して一部の外部宛先が不達になっている。
    - この問題を根本的に解決するには、**R1 の Router ID をネットワーク内で一意な値に変更し、その後 `clear ip ospf process` などで OSPF プロセスを再起動して新しい LSAs を再配布させる**必要がある。  
      選択肢 A はこの手順をそのまま表しているため正解。

- 他の選択肢がダメな理由
    - B: SPF delay（SPF 計算のディレイタイマ）を増やしても、**Router ID の重複という根本原因は解決しない**。LSA のループ／フラッディング自体は続き、外部経路の不整合も残る。
    - C: Router ID 0.0.0.0 は通常一時的な状態で、ここでの問題は明示的に 10.255.255.1 の Type-5 LSA が繰り返し見えていること。  
      0.0.0.0 のルータを切断しても、この FLOOD_WAR の原因には関係しない。
    - D: OSPF プロセスをクリアすると、一時的には LSDB がリセットされて状況が改善したように見えるかもしれないが、**Router ID が重複したままなら、再び同じ問題が発生する**。  
      したがって、単純なプロセスクリアだけでは恒久的な解決にはならない。

***
### QUESTION 302 解説

- 問題文:
Refer to the exhibit.  
The diagram shows that R1 has the OSPF router ID 10.255.255.1, and a message similar to the following is seen:  
`%OSPF-4-FLOOD_WAR: ... type-5 adv-rtr 10.255.255.1 ...`  
After configuring OSPF on R1, some external destinations in the network became unreachable. Which action resolves the issue?

- 問題文要約（日本語）:  
R1 に OSPF を設定したところ、ネットワーク上のいくつかの外部宛先が到達不能になった。  
ログには「`%OSPF-4-FLOOD_WAR` type-5 adv-rtr 10.255.255.1」といったメッセージが出ており、R1 の OSPF Router ID は 10.255.255.1 になっている。  
これは OSPF の外部 LSA（Type 5）に関するフラッディング警告であり、どの対処を行えば問題を解消できるかを問う問題。[file:24]

- 選択肢:
A. Change the R1 router ID from 10.255.255.1 to a unique value and clear the process.  
B. Increase the SPF delay interval on R1 to synchronize routes.  
C. Disconnect the router with the OSPF router ID 0.0.0.0 from the network.  
D. Clear the OSPF process on R1 to flush stale LSAs sent by other routers.  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - `%OSPF-4-FLOOD_WAR` は、「ルータが**自分自身が生成した LSA を、別ルータ経由で再度受け取り続けている**」ときに出る警告で、  
      よくある原因は「同じ OSPF Router ID を持つルータが複数存在すること」。特に Type-5 LSA（外部経路）については、**異なるエリアで同じ Router ID を使って ASBR が存在**すると、このような状況が起こる。[file:24]
    - 今回、ログの `adv-rtr 10.255.255.1` は「LSA を広告したルータ ID」が 10.255.255.1 であることを示しており、R1 も Router ID 10.255.255.1 を使っている。  
      つまり **他の ASBR も同じ Router ID を使っている** 可能性が高く、外部 LSA が衝突して一部の外部宛先が不達になっている。
    - この問題を根本的に解決するには、**R1 の Router ID をネットワーク内で一意な値に変更し、その後 `clear ip ospf process` などで OSPF プロセスを再起動して新しい LSAs を再配布させる**必要がある。  
      選択肢 A はこの手順をそのまま表しているため正解。

- 他の選択肢がダメな理由
    - B: SPF delay（SPF 計算のディレイタイマ）を増やしても、**Router ID の重複という根本原因は解決しない**。LSA のループ／フラッディング自体は続き、外部経路の不整合も残る。
    - C: Router ID 0.0.0.0 は通常一時的な状態で、ここでの問題は明示的に 10.255.255.1 の Type-5 LSA が繰り返し見えていること。  
      0.0.0.0 のルータを切断しても、この FLOOD_WAR の原因には関係しない。
    - D: OSPF プロセスをクリアすると、一時的には LSDB がリセットされて状況が改善したように見えるかもしれないが、**Router ID が重複したままなら、再び同じ問題が発生する**。  
      したがって、単純なプロセスクリアだけでは恒久的な解決にはならない。

***
### QUESTION 303 解説

- 問題文:
Which two solutions are used to overcome a flapping link that causes a frequent label binding exchange between MPLS routers? (Choose two)

- 問題文要約（日本語）:  
MPLS ネットワークでリンクがフラップすると、LDP セッションが頻繁に張り直され、そのたびにラベルバインディングの再交換が発生して不安定になる。  
この「リンクの一時的なダウン・フラップ」に対して、LDP セッション／ラベル情報を保護し、頻繁な再ネゴシエーションを防ぐ手段を 2 つ選ぶ問題。[file:24]

- 選択肢:
A. create targeted hellos to protect the session  
B. increase a hold-timer to protect the session  
C. increase input queue on links to protect the session  
D. create link dampening on links to protect the session  
E. increase a session delay to protect the session.  

- 正解: **A, B**

- 解説（なぜ A・B が正しいか）
    - **A. create targeted hellos to protect the session**  
      - MPLS LDP Session Protection 機能では、**LDP Targeted Hellos** を用いて、物理リンクがダウンしても LDP セッションを別インターフェイス経由で維持する。  
      - これにより、リンクフラップがあっても LDP セッションやラベルバインディングを再確立する必要が減り、頻繁なラベル交換を防げる。[file:24]
    - **B. increase a hold-timer to protect the session**  
      - `mpls ldp session holdtime` を長めに設定すると、**一定時間 LDP メッセージが途絶えてもセッションをすぐには落とさない**。  
      - 瞬間的なリンクフラップや短時間のパケットロスでセッションがダウンしなくなり、ラベル再配布の頻度を抑えられる。[file:24]

- 他の選択肢がダメな理由
    - C: 入力キューを増やすと輻輳時のドロップは多少減るかもしれないが、**リンクの物理的なフラップによるセッション切断**には直接効かない。LDP セッション保護の手段としては不適切。
    - D: 「リンクダンピング（dampening）」は一般的に BGP ルートフラップ抑制などで使う概念であり、ここでは **LDP セッションそのものを保護するメカニズムではない**。  
    - E: セッションディレイは「セッションの確立を遅らせる」機能であり、問題文のような **既存セッションの頻繁な切断・再確立** を防ぐものではない。[file:24]

***
### QUESTION 304 解説

- 問題文:
Refer to the exhibit.  
A command `aaa authentication login default local` is entered.  
A network engineer must configure a login authentication method at line console 0 on a Cisco ISR router. During configuration, the engineer got a call and the router console session timed out. When the call was completed, the engineer pressed Enter on the console. The engineer was asked for a username to log in to the router even though no username and password were previously configured on the router. What action resolves the issue?

- 問題文要約（日本語）:  
エンジニアが ISR ルータのコンソールで作業中に、`aaa authentication login default local` を入力した直後に電話対応で離席し、その間にコンソールがタイムアウトした。  
戻って Enter を押すと、コンソールログインに「username」が要求されるが、ルータには **まだローカルユーザ名／パスワードを一切設定していない**。  
どうすれば再度ルータにアクセスして設定をやり直せるか、という問題。

- 選択肢:
A. Telnet to the router to log in  
B. Power cycle to reload the router and reconfigure it  
C. Configure the user and password in the RADIUS server  
D. Use CTRL+C to bypass authentication in the router.  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - `aaa authentication login default local` を入れると、「すべてのログイン（コンソール・VTY など）は **ローカルユーザデータベース** で認証する」という意味になる。[file:24]
    - ところが、この時点で `username xxx secret yyy` などのローカルユーザがまだ一切設定されていないため、**認証に使えるアカウントが存在しない**。  
      結果として、コンソールにユーザ名を入れても認証できず、完全にロックアウトされる。
    - さらに、コンソールでの設定変更は running-config 上には反映されているが、まだ `write` していない前提は問題文から明確ではないものの、  
      多くの試験文脈では「保存前なら電源再投入すれば元の設定に戻る」ことを意図している。[file:24]
    - よって、**電源再投入（Power cycle）でルータを再起動し、startup-config（AAA 設定を入れる前の状態）からやり直す**のが現実的な解決策となるため、B が正解。

- 他の選択肢がダメな理由
    - A: Telnet も `aaa authentication login default local` の対象であり、**コンソール同様にローカルユーザ認証を要求**される。  
      ローカルユーザが存在しない限り、Telnet でもログインできないため解決にならない。
    - C: すでに `default local` としているため、認証は RADIUS ではなく「ルータ上のローカルデータベース」を参照する。  
      RADIUS サーバにユーザを作っても、AAA メソッドリストを `group radius` に変更しない限り使われない。
    - D: Cisco IOS のログインプロンプトで **Ctrl+C で認証をバイパスする方法は存在しない**。  
      Ctrl+C はコンフィグモード内でのコマンド中断などに使われるキーであり、ログイン要求そのものをスキップすることはできない。

***
### QUESTION 305 解説

- 問題文:
An engineer creates a default static route on a router with a next hop of 10.1.1.1. On inspection, the engineer finds the router has two VRFs, Red and Blue. The next hop is valid for both VRFs and exists in each assigned VRF. Which configuration achieves connectivity?

- 問題文要約（日本語）:  
ルータには VRF Red と VRF Blue の 2 つがあり、どちらの VRF からも next-hop 10.1.1.1 に到達できる（各 VRF 内に 10.1.1.1 への経路がある）。  
それぞれの VRF からインターネットなど外部へのデフォルトルートを、next-hop 10.1.1.1 で設定したい。  
どの `ip route` 設定を入れれば、Red / Blue 両方の VRF でデフォルトルートを正しく持てるか、という問題。[file:24]

- 選択肢:
A.  
   ip route vrf Red 0.0.0.0 255.255.255.255 10.1.1.1  
   ip route vrf Blue 0.0.0.0 255.255.255.255 10.1.1.1  

B.  
   ip route 0.0.0.0 0.0.0.0 10.1.1.1  

C.  
   ip route vrf Red 0.0.0.0 0.0.0.0 10.1.1.1  
   ip route vrf Blue 0.0.0.0 0.0.0.0 10.1.1.1  

D.  
   ip route vrf Blue 0.0.0.0 255.255.255.255 10.1.1.1  
   ip route vrf Red 0.0.0.0 255.255.255.255 10.1.1.1  

- 正解: **C**

- 解説（なぜ C が正しいか）
    - VRF ごとに別々のルーティングテーブルがあり、**各 VRF 内でデフォルトルートを定義する必要がある**。  
    - デフォルトルートはプレフィックス `/0` なので、Cisco IOS では  
      `ip route vrf <VRF名> 0.0.0.0 0.0.0.0 <next-hop>`  
      の形式を使う。[file:24]
    - 選択肢 C は、Red・Blue の両方に対して  
      - `ip route vrf Red  0.0.0.0 0.0.0.0 10.1.1.1`  
      - `ip route vrf Blue 0.0.0.0 0.0.0.0 10.1.1.1`  
      を設定しており、**両 VRF のルーティングテーブルにデフォルトルートを正しく追加**しているため、要件を満たす。

- 他の選択肢がダメな理由
    - A / D: マスクに `255.255.255.255` を指定しており、これは「/32 のホストルート」であってデフォルトルートではない。  
      宛先 0.0.0.0/32 へのみ有効で、通常の「どこ宛でも 10.1.1.1 へ」というデフォルト動作にならない。
    - B: VRF を指定しない `ip route 0.0.0.0 0.0.0.0 10.1.1.1` は **グローバルルーティングテーブル** のデフォルトルートを作るだけであり、VRF Red/Blue のテーブルには反映されない。  
      そのため、Red / Blue VRF 内のトラフィックはこのデフォルトルートを利用できない。

***
### QUESTION 306 解説

- 問題文:
What are two characteristics of IPv6 Source Guard? (Choose two)

- 問題文要約（日本語）:  
IPv6 Source Guard 機能の性質について正しいものを 2 つ選ぶ問題。  
Source Guard は、IPv6 Snooping などで作られた「バインディングテーブル」に基づいて、ポート上の送信元 IPv6 アドレスを検証し、不正なトラフィックを遮断する Layer2 セキュリティ機能。[file:24]

- 選択肢:
A. requires the user to configure a static binding  
B. recovers missing binding table entries  
C. used in service provider deployments to protect DDoS attacks  
D. requires that validate prefix be enabled  
E. requires IPv6 snooping on Layer 2 access or trunk ports  

- 正解: **C, E**

- 解説（なぜ C・E が正しいか）
    - **E. requires IPv6 snooping on Layer 2 access or trunk ports**  
      - IPv6 Source Guard は、**IPv6 Snooping / ND inspection / DHCPv6 gleaning** などで作成される「IPv6 ファーストホップ・セキュリティのバインディングテーブル」に依存して動作する。[file:24]  
      - このテーブルは、MAC・VLAN・ポート・IPv6 アドレス・リンクローカルアドレスなどを L2 ポート上で学習するため、**L2 アクセス／トランクポートで IPv6 snooping 系機能が有効であることが前提条件**となる。
    - **C. used in service provider deployments to protect DDoS attacks**  
      - IPv6 Source Guard 自体は「既知ホストのみ許可」という性質があり、エンタープライズ側のアクセスエッジ向けが主用途だが、  
        その内部機能である **IPv6 Prefix Guard** を用いて、サービスプロバイダ環境で「許可されたプレフィックス以外からの不正トラフィック（DDoS など）」を遮断する使い方ができる。[file:24]  
      - 問題集の想定解では、Source Guard 機能セットの一部として Prefix Guard を含めた全体を見ており、「SP で DDoS 攻撃から保護するのに用いられる」という説明に合致するため C を正解としている。

- 他の選択肢がダメな理由
    - **A: requires the user to configure a static binding**  
      - Source Guard は「既に存在するバインディングテーブルを参照してフィルタする」機能であって、静的バインディングの設定を必須とはしていない。  
        バインディングテーブルは通常 ND inspection や IPv6 Snooping、DHCP gleaning などで自動生成される。[file:24]
    - **B: recovers missing binding table entries**  
      - 欠落しているエントリを ND や DHCPv6 から再学習するのは、**Binding Table Recovery** 機能の役割であり、Source Guard 単体の動作ではない。  
        Source Guard はあくまで「テーブルにない送信元アドレスのトラフィックを拒否する」側。[file:24]
    - **D: requires that validate prefix be enabled**  
      - `validate prefix` は IPv6 Prefix Guard 機能のためのオプションであり、Source Guard を基本的に動かすだけなら必須ではない。  
        Prefix Guard を使わないなら、このコマンドは不要なので「必須」という表現は誤り。

***
### QUESTION 307 解説

- 問題文:
An engineer is creating a policy that overrides normal routing behavior. If the route to a destination of 10.100.100.0/24 is withdrawn from the routing table, the policy must direct traffic to a next-hop of 10.1.1.1. If the route is present in the routing table, then normal forwarding must occur. Which configuration meets the requirements?

- 問題文要約（日本語）:  
宛先 10.100.100.0/24 へのパケットに対して、  
- ルーティングテーブルに 10.100.100.0/24 の経路が **存在する間は通常ルーティング** を行う。  
- その経路が **消えたときだけ**、PBR で next-hop 10.1.1.1 に転送させたい。  
この条件付き PBR を実現する `route-map` / ACL の組み合わせを選ぶ問題。

- 選択肢（要旨）:
A. ACL 100: `permit ip any any`、route-map で `set ip next-hop recursive 10.1.1.1`  
B. ACL 100: 宛先 10.100.100.0/24、route-map で `set ip next-hop recursive 10.1.1.1`  
C. ACL 100: 宛先 10.100.100.0/24、route-map で `set ip next-hop 10.1.1.1`  
D. ACL 100: 宛先 10.100.100.0/24、route-map で `set ip default next-hop 10.1.1.1`  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - PBR の `set` 動作:
        - `set ip next-hop <IP>`  
          → 該当パケットに対し **常に** 指定 next-hop を使う（ルーティングテーブルに経路があっても上書き）。  
        - `set ip default next-hop <IP>`  
          → **宛先へのルートがルーティングテーブルに無いときだけ** 指定 next-hop を使い、  
            経路が存在するときは通常のルーティングテーブルに従う。[file:24]
    - 問題の要件は「10.100.100.0/24 のルートがある間は通常ルーティング／無くなったら 10.1.1.1 を使う」なので、  
      - 該当宛先をマッチさせる ACL（10.100.100.0/24）  
      - ルートが無いときだけ next-hop を指定する `set ip default next-hop 10.1.1.1`  
      の組み合わせが必要。  
      これを満たすのが選択肢 D であり、要件通りの「条件付き PBR」を実現できる。

- 他の選択肢がダメな理由
    - A: ACL が `any any` なので、**すべてのトラフィック** に対して常に `set ip next-hop recursive 10.1.1.1` を適用してしまい、  
      - 宛先が 10.100.100.0/24 以外も含めて通常ルーティングが上書きされる。  
      - さらに `set ip next-hop` 系はルートが存在しても常に適用されるため、要件と逆。
    - B: 宛先マッチは正しいが、`set ip next-hop recursive` を使っており、**経路の有無に関係なく** 常に 10.1.1.1 を next-hop として使う。  
      経路が存在するときも PBR が優先されるため、通常ルーティングにならない。
    - C: B と同様に `set ip next-hop` を使っているため、ルートがある場合も必ず 10.1.1.1 へ転送してしまう。  
      「ルートがあるときは通常転送」という条件を満たさない。

***
### QUESTION 308 解説

- 問題文:
Refer to the exhibit.  
A diagram shows that R1 has three paths to reach network 5.5.5.0/24. EIGRP is used as the routing protocol, but only two EIGRP routes for 5.5.5.0/24 are installed in the routing table. An engineer finds that traffic destined for 5.5.5.0/24 does not use all available paths. Which action resolves the issue?

- 問題文要約（日本語）:  
R1 から 5.5.5.0/24 へ到達する 3 本の EIGRP パスが存在するが、`show ip route` ではそのうち 2 本しかルーティングテーブルに載っておらず、  
実際のトラフィックも 2 本のパスでしかロードバランシングされていない。  
3 本すべてを EIGRP の等価 / 不等価ロードバランシングの対象に含めたい。どの設定変更が必要か、という問題。[file:24]

- 選択肢:
A. Decrease the variance value in EIGRP.  
B. Stop advertising 192.168.13.0/24 in EIGRP.  
C. Remove the adjacency of R3 from EIGRP.  
D. Increase the variance value in EIGRP.  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - EIGRP では、**FD（Feasible Distance）が最小のパスを SFD とし、他の候補パスのメトリックが「SFD × variance 以下」なら、  
      そのパスもルーティングテーブルに載せてロードバランシングに参加**させる。[file:24]
    - 現状、3 本のうち 2 本だけが「SFD × 現在の variance」以内に収まっており、3 本目のパスはメトリックが高すぎて条件を満たしていないため除外されている。  
      → **variance を大きくすれば、メトリックの高い 3 本目も許容範囲に入り、ルーティングテーブルにインストールされる**。
    - したがって、「Increase the variance value in EIGRP.」である D を選ぶと、3 本すべてのパスを EIGRP が利用できるようになる。

- 他の選択肢がダメな理由
    - A: variance を減らすと、「許容されるメトリック差」が小さくなり、**さらに多くのパスが除外される** 可能性がある。3 本すべてを使いたい要件とは逆。
    - B: 192.168.13.0/24 の広告を止めると、そもそもその経路を通るパスが消えるため、  
      「すべてのパスを利用したい」という目的に反して経路数を減らしてしまうだけ。
    - C: R3 の隣接を削除すると、R3 経由のパスが完全に消えるため、パスを増やすどころか減らしてしまい、問題の趣旨と逆。

***
### QUESTION 309 解説

- 問題文:
Refer to the exhibit.  
A network administrator has developed a Python script on the local Linux machine and is trying to transfer it to the router using SCP, but the transfer fails with:

Password:  
Administratively disabled.  
Connection to 198.51.100.64 closed by remote host. [file:63]

Which action resolves this issue?

- 問題文要約（日本語）:  
Linux 端末から `scp script.py admin@198.51.100.64:script.py` を実行すると、パスワード入力後に「Administratively disabled.」と表示されて接続が切断される。[file:63]  
SCP で IOS ルータにファイルをコピーしたいが、どの設定を行えばよいかを問う問題。

- 選択肢:
A. The SSH service must be enabled with the crypto key generate rsa command.  
B. The SCP service must be enabled with the ip scp server enable command.  
C. The Python interpreter must first be enabled with the guestshell enable command.  
D. The SSH access must be allowed on the VTY lines using the transport input ssh command.  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - メッセージ「Administratively disabled」は、**SSH 接続自体はできるが、SCP サーバ機能が無効化されている** IOS デバイスに対して SCP を試みたときに表示される典型的な出力。[file:24]
    - Cisco IOS で SCP サーバ機能を有効にするには、グローバルコンフィグで  
      `ip scp server enable`  
      を設定する必要がある。  
      これにより、既存の SSH サービス上で SCP 転送が許可され、Linux クライアントからの `scp` が成功するようになる。

- 他の選択肢がダメな理由
    - A: もし RSA キーが生成されておらず SSH 自体が無効なら、**そもそもパスワードプロンプトすら出ず** 接続ができない。  
      今回はパスワード入力後に「Administratively disabled」と出ているため、SSH はすでに有効。
    - C: guestshell（Python インタプリタ）はルータ上でスクリプトを実行するための機能であり、**SCP 転送の成否には関係しない**。
    - D: `transport input ssh` が未設定で SSH が禁止されていれば、A と同様に接続自体に失敗し、「Administratively disabled」ではなく接続拒否やタイムアウトになる。  
      したがって、本ケースの症状とは一致しない。

### QUESTION 310 解説

- 問題文:
Refer to the exhibit.  
A junior engineer updated a branch router configuration. Immediately after the change, the help desk reports that branch users cannot reach any network destinations. Which configuration restores service and continues to block 10.1.1.100/32?

- 問題文要約（日本語）:  
BRANCH-RTR では EIGRP 100 を動かしており、FastEthernet0/0 に対して受信ルートをフィルタするための distribute-list が設定されている。

現在の設定（抜粋）:  
router eigrp 100  
 network 10.4.31.0 0.0.0.7  
 network 10.100.100.1 0.0.0.0  
 distribute-list route-map FILTER-IN in FastEthernet0/0  
 eigrp router-id 10.100.100.1  

ip prefix-list 102 seq 10 permit 10.1.1.100/32  

route-map FILTER-IN deny 10  
 match ip address prefix-list 102  [file:64]

この変更後、ブランチからどの宛先にも到達できなくなった。10.1.1.100/32 だけをブロックしつつ、他の経路は受け入れて通信を復旧させるには、どの設定を追加／修正すべきか。

- 選択肢:
A. route-map FILTER-IN deny 5  
B. ip prefix-list 102 seq 15 permit 0.0.0.0/32 le 32  
C. ip prefix-list 102 seq 5 permit 0.0.0.0/32 le 32  
D. route-map FILTER-IN permit 20  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - 現状の route-map `FILTER-IN` は、シーケンス 10 で  
      `deny` + `match ip address prefix-list 102`  
      のみが定義されている。[file:64]
    - route-map には **暗黙の deny** があり、どのシーケンスにもマッチしなかったルートはすべて拒否される。  
      つまり今の設定だと  
      - 10.1.1.100/32 にマッチ → seq 10 の `deny` で拒否（これは狙い通り）  
      - それ以外のプレフィックス → どのシーケンスにもマッチせず暗黙の `deny` → **全部拒否される**  
      ため、ブランチからどこにも行けなくなっている。
    - 解決には、「10.1.1.100/32 以外はすべて許可」するための **permit 行** を route-map の後ろに追加する必要がある。  
      選択肢 D の `route-map FILTER-IN permit 20` を追加すれば、  
      - seq 10: 10.1.1.100/32 を `deny`  
      - seq 20: その他のプレフィックスは条件無しで `permit`  
      となり、10.1.1.100 だけをブロックしつつ、その他の EIGRP ルートは通常通り受け入れられる。

- 他の選択肢がダメな理由
    - A: `deny 5` を追加しても、結局 seq 5 と seq 10 のどちらかで `deny` されるだけであり、「permit するシーケンス」が存在しない点は変わらない。
    - B / C: prefix-list 102 に「0.0.0.0/0〜/32 を permit」するエントリを足すと、  
      そのプレフィックスも seq 10 の `deny` にマッチしてしまい、**むしろ全プレフィックスが deny 対象になりかねない**。  
      いずれにせよ route-map に `permit` シーケンスが無い限り、暗黙の deny によりルートは受け入れられない。

***
### QUESTION 311 解説

- 問題文:
Refer to the exhibit.  
An engineer cannot copy the IOS.bin image from an FTP server to the switch. Which action resolves the issue?

- 問題文要約（日本語）:  
FTP サーバ (10.0.0.2/24) に IOS.bin ファイルがあり、ユーザ名／パスワードは `cisco / cisco` と表示されている。[file:65]  
スイッチ側では VLAN2 に 10.0.0.1/24 を設定し、`ip ftp source-interface vlan 2` も設定済み。  
PC から 10.0.0.1 への ping は成功しているので、L3 接続は問題ない。  
しかしスイッチで  
`copy ftp://cisco:cisco@10.0.0.2/IOS.bin flash:/`  
を実行すると、  
`%Error opening ftp://cisco:cisco@10.0.0.2/IOS.bin (No such file or directory)`  
と表示されて失敗する。[file:65]  
どの対応を行えば IOS.bin を正常にコピーできるか。

- 選択肢:
A. Allow file permissions to download the file from the FTP server.  
B. Add the IOS.bin file, which does not exist on FTP server.  
C. Make memory space on the switch flash or USB drive to download the file.  
D. Use the copy flash:/ ftp://cisco@10.0.0.2/IOS.bin command.  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 図中の FTP サーバのメモには「File to download: IOS.bin」と記載されており、**IOS.bin ファイル自体はサーバ上に既に存在する前提** になっている。[file:65]
    - にもかかわらず `No such file or directory` が表示されるのは、  
      - FTP サーバ側で、そのファイル（またはディレクトリ）への **読み取り権限が付与されていない**  
      - FTP のホームディレクトリ / パス設定の関係で、匿名または `cisco` ユーザからそのファイルにアクセスできない  
      といった **ファイル権限・アクセス権の問題** の可能性が高い。
    - したがって、FTP サーバ側で `IOS.bin` に対し「ダウンロード（読み取り）を許可するファイル権限」を設定し直せば、同じコマンドでコピーが成功する。  
      これが選択肢 A の内容であり、最も妥当な解決策。

- 他の選択肢がダメな理由
    - B: 図の説明から、`IOS.bin` は「ダウンロードすべきファイル」として既に存在している前提なので、「ファイルを追加する」が正解ではない。  
      もし存在しなければ、その旨の記載があるはず。
    - C: フラッシュ容量不足であれば、エラーは「No space left on device」など容量関連のメッセージになり、「No such file or directory」にはならない。
    - D: `copy flash:/ ftp://...` は「スイッチから FTP サーバへアップロード」するコマンドであり、問題は「サーバからスイッチへダウンロード」なので方向が逆。  
      また現在のエラーはダウンロード時のファイル参照問題であり、コピー方向を変えても根本原因は解決しない。

***
### QUESTION 312 解説

- 問題文:
What is an advantage of implementing BFD?

- 問題文要約（日本語）:  
BFD（Bidirectional Forwarding Detection）をネットワークに導入したときのメリットとして、正しいものを 1 つ選ぶ問題。

- 選択肢:
A. BFD provides faster updates for any flapping route.  
B. BFD provides millisecond failure detection  
C. BFD is deployed without the need to run any routing protocol  
D. BFD provides better capabilities to maintain the routing table  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - BFD は、2 台のルータ間で**非常に短い間隔（ミリ秒オーダー）で Hello メッセージをやり取りしてリンク障害を検出する軽量プロトコル**。[file:24]
    - 通常の OSPF / EIGRP / BGP などのデッドタイマよりもはるかに短い時間で障害検出ができ、**ミリ秒単位の高速フェイルオーバー**を実現するのが最大の利点なので、B が正解。

- 他の選択肢がダメな理由
    - A: BFD 自体は「ルートアップデート」を送るわけではなく、**リンク障害の検出**だけを担当する。ルーティング更新は各プロトコル（OSPF, BGP など）が行う。
    - C: BFD はしばしば OSPF / EIGRP / BGP など「既存のルーティングプロトコルと連携して」使われるものであり、それら無しに単独で運用するのが目的ではない。
    - D: ルーティングテーブルの管理そのものを強化する機能ではなく、あくまで障害検出を速くして、**上位プロトコルが早く経路切り替えできるよう補助する**だけ。

***
### QUESTION 313 解説

- 問題文:
What is the purpose of the DHCPv6 Guard?

- 問題文要約（日本語）:  
DHCPv6 Guard 機能の役割について正しい説明を 1 つ選ぶ問題。  
DHCPv6 Guard は、スイッチのポートで「どのポートが正規の DHCPv6 サーバ／リレーからの応答を流せるか」を制御し、不正（rogue）サーバからの配布を防ぐ仕組み。[file:24]

- 選択肢:
A. It filters messages between a DHCPv6 server and a DHCPv6 client (or relay agent).  
B. It shows that clients of a DHCPv5 server are affected.  
C. It block DHCPv6 messages from relay agents to a DHCPv6 server.  
D. It allows DHCPv6 reply and advertisements from (rouge) DHCPv6 servers.  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - DHCPv6 Guard は、**スイッチが「トラストポート」と「アンテラストポート」を区別し、不正なポートから送られてくる DHCPv6 サーバメッセージ（Reply, Advertise など）をブロックする機能**。[file:24]
    - つまり「DHCPv6 サーバとクライアント（またはリレー）の間を通過する DHCPv6 メッセージをフィルタし、許可されていない方向の応答を遮断する」機能なので、  
      選択肢 A の説明が最も近い。

- 他の選択肢がダメな理由
    - B: DHCPv5 というバージョンは存在せず、DHCPv6 Guard の動作とも無関係。
    - C: DHCPv6 Guard は主に **サーバからクライアントへの Reply / Advertise などをブロック**する機能であり、リレーからサーバへのメッセージをブロックするものではない。[file:24]
    - D: 不正サーバからの応答を「許可する」のではなく、**防ぐ（防止する）** のが目的なので、方向が逆。  
      説明文にも「blocks DHCP reply and advertisement messages that originate from unauthorized DHCP servers」とあり、D の内容とは反対である。[file:24]

***
### QUESTION 314 解説

- 問題文:
Which function does LDP provide in an MPLS topology?

- 問題文要約（日本語）:  
MPLS トポロジにおいて、LDP（Label Distribution Protocol）が果たす役割として正しいものを 1 つ選ぶ問題。

- 選択肢:
A. It enables a MPLS topology to connect multiple VPNs to P routers.  
B. It provides hop-by-hop forwarding in an MPLS topology for LSRs.  
C. It exchanges routes for MPLS VPNs across different VRFs.  
D. It provides a means for LSRs to exchange IP routes.  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - LDP は、**IGP が決めた IP 経路に従って、ラベルスイッチングルータ（LSR）同士がラベルを割り当てて配布するプロトコル**。[file:24]
    - こうして各 LSR が隣接ノードとラベル情報を交換することで、MPLS ネットワーク内で **ホップ・バイ・ホップのラベル転送パス（LSP）** が構築される。  
      したがって、「hop-by-hop forwarding in an MPLS topology for LSRs」を提供するという B の説明が正しい。

- 他の選択肢がダメな理由
    - A: 複数 VPN を P ルータに接続するのは主に **MP-BGP と VRF 設定** の役割であり、LDP は単にラベル配送を行うだけで VPN 間接続を直接制御しない。
    - C: MPLS VPN のルート交換は **MP-BGP** で行われる。LDP は VRF 間のルート交換機能を持たない。
    - D: LDP が交換するのは **IP ルートそのものではなく、それに対応するラベル**。IP ルートは IGP（OSPF、IS-IS など）によって配布される。[file:24]

***
### QUESTION 315 解説

- 問題文:
Refer to the exhibit.  
The administrator noticed that the connection was flapping between the two ISPs instead of switching to ISP2 when ISP1 failed. Which action resolves the issue?

- 問題文要約（日本語）:  
IP SLA で 8.8.8.8 への ICMP Echo を監視し、その結果を track 1 で監視している。

設定抜粋:  
ip sla 1  
 icmp-echo 8.8.8.8  
 threshold 1000  
 timeout 2000  
 frequency 5  
ip sla schedule 1 life forever start-time now  
track 1 ip sla 1  

ip route 0.0.0.0 0.0.0.0 203.0.113.1  name ISP1 track 1  
ip route 0.0.0.0 0.0.0.0 198.51.100.1 2 name ISP2  [file:66]

ISP1 を優先し、IP SLA (track 1) がダウンしたら ISP2 のデフォルトルートに切り替える設計だが、実際には ISP1 と ISP2 の間でデフォルトルートがフラップしてしまう。  
どの対処を行えば安定して ISP2 へ切り替わるか。

- 選択肢:
A. Include a valid source-interface keyword in the icmp-echo statement.  
B. Reference the track object 1 on the default route through ISP2 instead of ISP1.  
C. Modify the static routes to refer both to the next hop and the outgoing interface.  
D. Modify the threshold to match the administrative distance of the ISP2 route.  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 現状の `icmp-echo 8.8.8.8` では **送信元インターフェイスが指定されておらず、ルーティングテーブルに基づいて送信元が決まる**。[file:24]
    - ISP1 がダウンすると、デフォルトルートが ISP2（198.51.100.1）に切り替わり、その回線から 8.8.8.8 への ping が成功する。  
      すると IP SLA は「成功」と判断して track 1 を再び up にし、ISP1 へのデフォルトルートが復活 → しかし ISP1 は実際にはまだ不通 → 再び SLA 失敗 → ISP2 に切り替え…  
      というループが発生し、**ISP1/ISP2 間でフラップ**する。
    - 解決するには、IP SLA の ICMP Echo が **常に ISP1 側のインターフェイス（例: `GigabitEthernet0/0`）を送信元として使うよう指定**する必要がある。  
      具体的には:  
      `ip sla 1`  
      ` icmp-echo 8.8.8.8 source-interface GigabitEthernet0/0`  
      のように `source-interface` を追加することで、ISP1 経由で通信できなくなった時点で SLA が確実に down となり、その後は ISP2 経由に切り替わったまま安定する。[file:24]

- 他の選択肢がダメな理由
    - B: track 1 を ISP2 のルートに付け替えると、「SLA が up のとき ISP2、down のとき ISP1」という逆の動作になり、優先ルートが変わってしまうだけでフラップの根本原因は解決しない。
    - C: 次ホップと出口インターフェイスの両方を指定しても、IP SLA の送信元が ISP2 側に変わってしまう問題には関係しない。
    - D: `threshold` は RTT などの応答時間のしきい値であり、**AD（管理距離）とは無関係**。しきい値を AD に合わせても、SLA の送信元問題によるフラップは続く。

***
### QUESTION 316 解説

- 問題文:
Refer to the exhibit.  
An engineer must establish a point-to-point GRE VPN between R1 and the remote site. Which configuration accomplishes the task for the remote site?

- 問題文要約（日本語）:  
R1 側のトンネル情報には、次のように表示されている。

- Tunnel source 199.1.1.1  
- Tunnel destination 200.1.1.3  
- Tunnel protocol GRE/IP  
- Tunnel transport MTU 1476 bytes など [file:67]

R1 のトンネルインターフェイスの IP アドレスは 192.168.1.1/24（図の欠落部分）だと仮定される。  
この R1 と GRE P2P トンネルを張る「リモートサイト側」の Tunnel1 設定として正しいものを選ぶ問題。

- 選択肢（要旨）:
A. Tunnel1: source 199.1.1.1, destination 200.1.1.3, ip 192.168.1.3/24  
B. Tunnel1: source 200.1.1.3, destination 199.1.1.1, ip 192.168.1.1/24  
C. Tunnel1: source 200.1.1.3, destination 199.1.1.1, ip 192.168.1.3/24  
D. Tunnel1: source 199.1.1.1, destination 200.1.1.3, ip 192.168.1.1/24  

- 正解: **C**

- 解説（なぜ C が正しいか）
    - P2P GRE トンネルでは、両端で次の条件を満たす必要がある。
        1. 一方の `tunnel source` が、もう一方の `tunnel destination` になる（＝互いに向き合う）。  
        2. トンネルインターフェイスの IP は同一サブネット上で、**異なるホストアドレス** を割り当てる。[file:24]
    - R1 側が  
      - `tunnel source 199.1.1.1`  
      - `tunnel destination 200.1.1.3`  
      と設定されているので、リモート側は **逆向き** に  
      - `tunnel source 200.1.1.3`  
      - `tunnel destination 199.1.1.1`  
      とする必要がある。
    - また、R1 のトンネル IP を 192.168.1.1/24 とすると、リモート側は同じ /24 サブネット内で別アドレス、例: 192.168.1.3/24 を使うのが自然。  
      選択肢 C はこの条件を満たしているため正解。

- 他の選択肢がダメな理由
    - A / D: どちらも `tunnel source 199.1.1.1` / `destination 200.1.1.3` と、**R1 と同じ向き** に設定しており、エンド間で向き合う構成にならない。
    - B: source/destination の向きは正しいが、トンネル IP に 192.168.1.1/24 を使っており、R1 と同じアドレスとなってしまう。  
      P2P トンネル両端で同一 IP を設定することはできないため不正。

***
### QUESTION 317 解説

- 問題文:
How is a VPN forwarding table used in an MPLS network?

- 問題文要約（日本語）:  
MPLS L3 VPN で使われる「VPN forwarding table」（＝VRF のルーティング＋ラベル情報を持つテーブル）が、どのような役割を持つのかを問う問題。[file:24]

- 選択肢:
A. learns VPN labels through LDP or RSVP  
B. learns PE route next hops through eBGP  
C. learns customer routes through MP-BGP from the connected CE  
D. stores VPN routes with associated labels  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - MPLS L3 VPN では、PE ルータ上に VRF ごとの **VPN forwarding table** が作られ、そこに  
      - CE から学習した各 VPN のルート情報  
      - それに対応する MPLS ラベル情報（仮想的な「出力インターフェイス＋ラベル」）  
      が紐付けられて保存される。[file:24]
    - このテーブルを使って、PE は VPN ごとに適切なラベルを付け替えつつパケットを転送するため、「VPN routes とそれに対応するラベルを格納する」という D の説明が最も正確。

- 他の選択肢がダメな理由
    - A: LDP や RSVP は主に **コア（P/PE 間）のラベル配布や LSP 設定** を行うプロトコルであり、「VPN forwarding table 自体がラベルを学習する」のではない。
    - B: PE 間の VPNv4 ルート交換には **MP-BGP (iBGP)** が使われ、eBGP ではない。[file:24]
    - C: CE からのルートは通常 CE–PE 間の eBGP や IGP（OSPF/EIGRP など）で学習し、**MP-BGP は PE 間で VPNv4 ルートを運ぶ** 役割。  
      「MP-BGP が CE から直接ルートを学習する」という記述は誤り。

***
### QUESTION 318 解説

- 問題文:
Drag and drop the IPv6 first hop security device roles from the left onto the corresponding descriptions on the right.

- 問題文要約（日本語）:  
IPv6 First-Hop Security では、ポートごとに「router / host / monitor / switch」といったロールを設定して、  
RA や RS などのメッセージをどのように扱うか制御する。  
それぞれのロールを、右側の説明文と正しく対応させるドラッグ＆ドロップ問題。[file:24][file:68][file:69]

- 対応付け（答え）:
1. **host**  
   → 「Receives router advertisements from valid routers, and no router solicitation are received.」  
   （有効なルータからの RA だけを受信し、自身からは RS を送らないホストポート）

2. **router**  
   → 「Receives router solicitation and sends router advertisements.」  
   （ホストからの RS を受け取り、RA を送信するルータポート）

3. **monitor**  
   → 「Receives valid and rogue router advertisements and all router solicitation.」  
   （正規・不正どちらの RA も RS もすべて受信して監視するポート）

4. **switch**  
   → 「Received router advertisements are trusted and are flooded to synchronize states.」  
   （受信した RA を信頼し、スイッチ内で状態同期のためにフラッディングするポート）

***
### QUESTION 319 解説

- 問題文:
Refer to the exhibit.  
PC2 is directly connected to R1. A user at PC2 cannot Telnet to 2001:db8:a:b::10. The user can ping 2001:db8:a:b::10 and receive DHCP-related information from the DHCP server. Which action resolves the issue?

- ACL 設定（抜粋）:
R1# sh ipv6 access-list GUARD  

deny tcp any host 2001:DB8:A:B::10 eq telnet       sequence 10  
permit tcp host 2001:DB8:A:A::20 host 2001:DB8:A:B::10 eq telnet  sequence 20  
permit tcp host 2001:DB8:A:A::2  host 2001:DB8:A:D::1 eq www      sequence 30  
permit ipv6 2001:DB8:A:A::/64 any                               sequence 40  [file:70]

- 問題文要約（日本語）:  
PC2 は R1 に L2 直結しており、IPv6 的には 2001:db8:a:a::/64 のホスト。  
PC2 から 2001:db8:a:b::10 へ ping や DHCP は通るが、Telnet だけができない。  
上記 IPv6 ACL「GUARD」がどこかのインターフェイスに適用されている状態で、どのシーケンス番号のエントリをどこに挿入し直せば Telnet が通るかを問う問題。

- 選択肢:
A. Remove sequence 10 and put it back as sequence 25.  
B. Remove sequence 20 and put it back as sequence 45.  
C. Remove sequence 30 and put it back as sequence 5.  
D. Remove sequence 40 and put it back as sequence 15.  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - ACL の評価は **上から順に** 行われ、最初にマッチしたエントリの動作が適用される。  
    - 現状:
        - seq 10: `deny tcp any host 2001:DB8:A:B::10 eq telnet`  
          → 宛先が 2001:db8:a:b::10 で宛先ポートが telnet のすべての TCP を拒否する。  
          PC2 の送信元も `any` に含まれるため、**Telnet はここでブロックされる**。  
        - seq 20 では PC2（2001:db8:a:a::20）からの Telnet を許可しているが、seq 10 で既に deny されているので到達しない。[file:24]
    - したがって、PC2 の Telnet を許可するには、
        1. まず PC2 からの Telnet を permit でマッチさせる（seq 20）。  
        2. その後に「その他の any からの Telnet を deny」する（seq 10 相当）  
      という順序にする必要がある。
    - 選択肢 A のように、**seq 10（deny）を一度削除し、seq 25 として seq 20 より後ろに再作成**すれば、  
      - 先に seq 20 で PC2 からの Telnet を許可  
      - その後、他の any からの Telnet を seq 25 の deny で拒否  
      となり、要件を満たす。

- 他の選択肢がダメな理由
    - B: seq 20（PC2 からの permit）を最後尾に移すと、先に seq 10 の deny にマッチしてしまい、依然として Telnet は拒否される。
    - C: seq 30（www トラフィック）を先頭に動かしても、Telnet に関する条件には影響しない。
    - D: seq 40（`permit ipv6 2001:DB8:A:A::/64 any`）を seq 15 にすると、Telnet を含むすべてのトラフィックが seq 15 で許可され、  
      その下の telnet deny/permit ロジックが無意味になる。問題は「PC2 以外からの Telnet は拒否したままにする」ことなので、D は意図と合わない。

***
### QUESTION 320 解説

- 問題文:
Refer to the exhibit.  
The remote server is failing to receive the NetFlow data. Which action resolves the issue?

- 設定抜粋:
flow record v4_r1  
 match ipv4 tos  
 match ipv4 protocol  
 match ipv4 source address  
 match ipv4 destination address  
 match transport source-port  
 match transport destination-port  
 collect counter bytes long  
 collect counter packets long  

flow exporter EXPORTER-1  
 destination 172.16.10.2  
 transport udp 2055  

flow monitor FLOW-MONITOR-1  
 exporter EXPORTER-1  
 record v4_r1  

!  

flow monitor v4_r1  

interface Ethernet0/0.1  
 ip address 172.16.6.2 255.255.255.0  
 ip flow monitor v4_r1 input  [file:71]

- 問題文要約（日本語）:  
NetFlow v9/Flexible NetFlow を設定し、`flow record v4_r1` と `flow exporter EXPORTER-1` を使ったモニタ `FLOW-MONITOR-1` を定義している。  
しかし、インターフェイス Ethernet0/0.1 には `ip flow monitor v4_r1 input` が適用されており、リモートのコレクタ (172.16.10.2) はフローを受信していない。  
どの修正を行えば NetFlow データがコレクタに送信されるか。

- 選択肢:
A. Modify the flow transport command `transport udp 2055` to move under flow monitor profile.  
B. Modify the interface command to `ip flow monitor FLOW-MONITOR-1 input`.  
C. Modify the udp port under flow exporter profile to `ip transport udp 4739`.  
D. Modify the flow record command `record v4_r1` to move under flow exporter profile.  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - Flexible NetFlow では、フローの生成・送信には 3 つの要素が必要。
        1. **flow record**: どのフィールドをマッチ／コレクトするか  
        2. **flow exporter**: どの宛先 IP・UDP ポートへ送るか  
        3. **flow monitor**: record と exporter を紐付けたプロファイル  
      そしてインターフェイスには「monitor」を適用する必要がある。[file:24]
    - 設定を見ると、
        - `FLOW-MONITOR-1` は `record v4_r1` と `exporter EXPORTER-1` を正しく参照している。  
        - しかし、インターフェイスには **別名の `flow monitor v4_r1`** が適用されており、こちらにはレコードもエクスポータも定義されていない（空のモニタ）。  
          そのため、コレクタへは何も送られない。
    - 解決策は、インターフェイス側で **正しいモニタ名 `FLOW-MONITOR-1` を指定すること**。  
      つまり `ip flow monitor FLOW-MONITOR-1 input` に修正すれば、レコードとエクスポータが紐付いたモニタが有効になり、リモートサーバへ NetFlow データが送信される。

- 他の選択肢がダメな理由
    - A / D: `transport udp` や `record` の設定位置は既に正しく、エクスポータ／レコードの文法も問題ない。モニタ名のミスマッチが根本原因であり、これらを動かしても状況は変わらない。
    - C: UDP 4739 は IPFIX でよく用いられるポートだが、問題文からはコレクタ側が 4739 を使うという情報はない。  
      現在の 2055 も一般的な NetFlow ポートであり、ポート変更は不要。

***
### QUESTION 321 解説

- 問題文:
What is a function of an end device configured with DHCPv6 guard?

- 問題文要約（日本語）:  
DHCPv6 Guard 機能で「エンドデバイス（クライアント側）」としてロールを設定したポートは、どのような動作をするかを問う問題。[file:24]

- 選択肢:
A. If it is configured as a server, only prefix assignments are permitted.  
B. If it is configured as a relay agent, only prefix assignments are permitted.  
C. If it is configured as a client, messages are switched regardless of the assigned role.  
D. If it is configured as a client, only DHCP requests are permitted.  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - DHCPv6 Guard は、スイッチポートのロール（server / relay / client）に応じて、**DHCPv6 メッセージの方向をフィルタする機能**。[file:24]
    - ポートが **client ロール** の場合、そのポートは「エンドホストが接続されている」とみなされるため、  
      - ホストから上流への **Solicit / Request などのクライアントメッセージは許可**  
      - しかし、そのポートから **Server/Relay 由来の Reply / Advertise などが出ていくことは想定されない** ためブロックされる。  
      → つまり「クライアントからの DHCP 要求だけを通す」動作になり、D の説明と一致する。

- 他の選択肢がダメな理由
    - A / B: server や relay ロールのときに「prefix assignment のみ許可」というような限定は行わない。サーバ／リレー用ポートでは、通常のサーバ／リレーメッセージ（Reply, Advertise など）を許可する。
    - C: client ロールではすべての DHCPv6 メッセージをスイッチするのではなく、**不正なサーバ／リレーメッセージを遮断する** のが目的なので誤り。

***
### QUESTION 323 解説

- 問題文:
Refer to the exhibit.  
An engineer must configure a LAN-to-LAN IPsec VPN between R1 and the remote router. Assume that the remote router is using SHA. Which IPsec Phase 1 configuration must the engineer use on the local router (R1)?

- 問題文要約（日本語）:  
R1（左側）は SP 経由でリモートルータ（R3）と IPsec site-to-site VPN を張る。  
- R1 の外側インターフェイス: 199.1.1.1/24（toward SP）  
- R3 の外側インターフェイス: 200.1.1.3/24（toward SP）[file:72]  
リモート側は ISAKMP Phase 1 でハッシュに SHA を使用している前提。  
ローカル側 R1 の `crypto isakmp policy` / `crypto isakmp key` をどう書くべきかを問う問題。

- 選択肢（要旨）:
A. hash sha, key for address 200.1.1.3  
B. hash md5, key for address 200.1.1.3  
C. hash md5, key for address 199.1.1.1  
D. hash md5, key for address 199.1.1.1（末尾に余分な !）  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - IKE Phase 1 の双方で一致させる必要がある主なパラメータ:
        - 暗号化アルゴリズム (encryption)  
        - ハッシュアルゴリズム (hash)  
        - 認証方式 (authentication)  
        - DH グループ (group)  
        - ライフタイム (未指定ならデフォルト同士で一致)  
      ここでは「リモートは SHA を使用」と明記されているため、ローカルも `hash sha` にする必要がある。[file:24]
    - また、`crypto isakmp key cisco123 address <peer-IP>` の `<peer-IP>` には **対向ピア（リモートルータ）の外側アドレス** を指定する。  
      R1 から見たピアは 200.1.1.3 なので、この IP を指定しなければならない。[file:72]
    - 選択肢 A は  
      - `hash sha`  
      - `crypto isakmp key cisco123 address 200.1.1.3`  
      と、ハッシュとピア IP の両方が正しく設定されているため、要求を満たす。

- 他の選択肢がダメな理由
    - B: ピアアドレスは正しいが `hash md5` となっており、リモートが SHA を使っているため Phase 1 が確立しない。
    - C / D: いずれも `hash md5` で誤りなうえ、`address 199.1.1.1` とローカル自身の IP を指定してしまっている。  
      ISAKMP キーの address に自分の IP を指定しても、リモートとのピア関係には使用されない。

***
### QUESTION 324 解説

- 問題文:
A CoPP policy is applied for receiving SSH traffic from the WAN interface on a Cisco ISR4321 router. However, the SSH response from the router is abnormal and stuck during high link utilization. The problem is identified as SSH traffic does not match in the ACL. Which action resolves the issue?

- 問題文要約（日本語）:  
ISR4321 で、WAN から入ってくる SSH を守るために CoPP（Control Plane Policing）を設定している。  
ところがリンク使用率が高いとき、ルータからの SSH 応答が異常に遅くなり、原因を調べると「SSH トラフィックが ACL にマッチせず、CoPP で保護されていない」ことが分かった。  
どの対応でこの問題を解決できるかを問う。

- 選択肢:
A. Rate-limit SSH traffic to ensure dedicated bandwidth.  
B. Apply CoPP on the control plane interface.  
C. Increase the IP precedence value of SSH traffic to 6.  
D. Apply CoPP on the WAN interface inbound direction.  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - CoPP は名前の通り **「コントロールプレーン（ルータ自身が終端するトラフィック）」に対するポリシング** であり、  
      物理インターフェイスではなく `control-plane` コンテキストに `service-policy` を適用するのが正しい。[file:24]
    - 問題文では「WAN インターフェイスで受信する SSH 用に CoPP を適用した」とあり、  
      おそらくインターフェイス側にポリシーを適用しているため、**実際に CPU へ届く SSH パケットは CoPP ポリシーの対象外** になっている。  
      その結果、高負荷時に他のトラフィックに押し流され、SSH 応答が詰まっている。
    - `control-plane` に CoPP を適用すれば、**すべての入力経路からコントロールプレーンへ届く SSH トラフィックが ACL でマッチし、適切に保護・優先制御される** ため、B が正解。

- 他の選択肢がダメな理由
    - A: レートリミット（帯域制限）をかけるだけでは、「SSH が CoPP にマッチしていない」という根本問題は解決しない。
    - C: IP precedence を 6 に上げても、CoPP の ACL マッチ条件や適用場所が誤っていれば効果はない。
    - D: すでに「WAN インターフェイスで受信する SSH 用に CoPP を適用した」とあり、そのやり方が問題の原因。  
      CoPP はインターフェイスではなく control-plane に適用すべきなので、D は誤り。

***
### QUESTION 325 解説

- 問題文:
How are MPLS Layer 3 VPN services deployed?

- 問題文要約（日本語）:  
MPLS L3 VPN サービスを成立させるために、サービスプロバイダ側で必須となる要件はどれかを問う問題。[file:24]

- 選択肢:
A. The RD and RT values must match under the VRF  
B. The RD and RT values under a VRF must match on the remote PE router  
C. The import and export RT values under a VRF must always be the same.  
D. The label switch path must be available between the local and remote PE routers.  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - MPLS Layer 3 VPN では、**VPNv4 ルートのやり取り (MP-BGP)** と、それを実際に転送するための **MPLS ラベルスイッチパス (LSP)** の両方が必要になる。[file:24]
    - CE 間のユーザトラフィックは、PE からコアの P ルータ群を MPLS ラベル付きで通過し、対向 PE まで届く必要があるため、**ローカル PE とリモート PE の間に LSP が張られていること**が前提条件となる。  
      これを満たすのが D の記述。

- 他の選択肢がダメな理由
    - A: RD と RT は役割が異なり、**一致させる必要はない**。  
      - RD（Route Distinguisher）は VPNv4 アドレスを一意にするための識別子。  
      - RT（Route Target）は「どの VRF にルートをインポート／エクスポートするか」を決めるタグ。
    - B: RD/RT の値がリモート PE と完全一致している必要はない。  
      同一 VPN に属する VRF では「インポート／エクスポート RT の関係」が重要であり、RD の一致は必須条件ではない。
    - C: 一つの VRF 内でも **import RT と export RT をあえて変える**ことで、ハブ＆スポーク型などさまざまなトポロジを実現できる。[file:24]  
      常に同じでなければならないわけではない。

***
### QUESTION 326 解説

- 問題文:
Which feature minimizes DoS attacks on an IPv6 network?

- 問題文要約（日本語）:  
IPv6 ネットワークに対する DoS（Denial of Service）攻撃を最小化するのに役立つ IPv6 First-Hop Security 機能はどれかを問う問題。[file:24]

- 選択肢:
A. IPv6 Binding Security Table  
B. IPv6 Router Advertisement Guard  
C. IPv6 Prefix Guard  
D. IPv6 Destination Guard  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - **IPv6 Destination Guard** は、IPv6 FHS バインディングテーブルを利用して「有効な宛先アドレス」だけに対して NDP 解決を行い、それ以外の宛先あてトラフィックをブロックする機能。[file:24]
    - これにより、存在しない多量の宛先アドレスへのトラフィックや、不正な宛先に対する Neighbor Solicitation を抑制できるため、**NDP を悪用した DoS 攻撃を大幅に軽減**できる。
    - Destination Guard は「宛先アドレスベース」でフィルタするため、攻撃トラフィックがスイッチやルータの制御プレーンに到達する前に落とせる点で、DoS 緩和に特化している。

- 他の選択肢がダメな理由
    - A: Binding Table 自体は FHS 機能が参照するデータベースであり、それ単体が DoS 対策機能というわけではない。
    - B: **RA Guard** は不正な Router Advertisement をブロックして、偽ルータによる攻撃を防ぐ機能であり、NDP 宛先全般への DoS を最小化する主目的ではない。
    - C: **Prefix Guard** は不正・誤ったプレフィックスからのトラフィックを防ぐ機能で、主にトポロジ的に正しくないアドレスの利用を防止する。[file:24]  
      DoS の緩和には寄与するが、Destination Guard ほど「DoS を最小化する機能」として直接的に説明されていない。

***
### QUESTION 328 解説

- 問題文:
Refer to the exhibit. Mutual redistribution is enabled between RIP and EIGRP on R2 and R5. Which configuration resolves the routing loop for the 192.168.1.0/24 network?

- トポロジ要約:
- R1 の LAN: 192.168.1.0/24（R1–R2 間は 178.1.10.4/30）  
- R2–R5 間: 178.1.10.0/30（RIP）  
- R2–R3–R4–R5 間: 181.16.x.x/30（EIGRP）[file:74]  
R2 と R5 は、RIP と EIGRP を相互再配布している。

- ループが起きる理由（日本語要約）:
    - 192.168.1.0/24 は本来 R1 側の RIP ドメインに属するネットワーク。  
    - R2 は 192.168.1.0/24 を RIP から受け取り、EIGRP に再配布する。  
    - R5 はその EIGRP ルートを再び RIP に再配布し、R2 側から 192.168.1.0/24 を「R5 経由」として学習してしまう。  
    - 結果として、R2 と R5 間で 192.168.1.0/24 をお互いに再配布し合い、ルーティングループや不安定さが発生する。[file:24]

- 正解: **B**

- 解説（なぜ B が正しいか）
    - ループを防ぐには、「R1 側の 192.168.1.0/24 ネットワークが EIGRP ドメインに“戻ってくる”のを防ぐ」必要がある。  
    - 選択肢 B では、R2 と R5 の両方で  
      - EIGRP からの学習ルートに対して、インターフェイス s1 入口で  
      - `access-list 1 deny 192.168.1.0` を用いた `distribute-list 1 in`  
      を設定している。[file:24]
    - s1 はそれぞれ EIGRP 側（R3/R4 方面）につながるインターフェイスなので、  
      **EIGRP から 192.168.1.0/24 を受け取らない** ようにすることで、ルートが RIP ドメインに逆流せず、ループが解消される。
    - また、問題文では EIGRP の AS 番号について特に制約が出ていないため、R2/R5 が同じ AS を使っていれば AS 番号 7 である点も問題にならない。

- 他の選択肢がダメな理由
    - A / C / D: distribute-list の適用インターフェイスが不適切だったり、一方のルータだけ／別方向でフィルタしているため、  
      いずれかの経路で 192.168.1.0/24 が EIGRP から RIP に再流入し、ループの可能性が残る。

***
### QUESTION 329 解説

- 問題文:
access-list 1 permit 219.65.100.15  
access-list 2 permit 219.65.100.16  

interface Ethernet1  
 ip policy route-map Texas  

route-map Texas permit 10  
 match ip address 1  
 set ip precedence priority  
 set ip next-hop 219.65.100.17  

route-map Texas permit 20  
 match ip address 2  
 set ip next-hop 219.65.100.18  

Packets arriving from source 219.65.100.15 must be sent with precedence bit 1, and packets from 219.65.100.16 with precedence bit 5. Which action resolves the issue?

- 問題文要約（日本語）:  
PBR（Policy-Based Routing）で、  
- ACL 1: 送信元 219.65.100.15  
- ACL 2: 送信元 219.65.100.16  
をマッチさせ、  
- 219.65.100.15 のパケットは Precedence=1  
- 219.65.100.16 のパケットは Precedence=5  
になるようにしたい。  
既存設定では、両方の `set ip precedence` が 1（priority）になっていないかを確認し、どこを直すかを問う問題。[file:24]

- IP Precedence 名称と値:
  - routine = 0  
  - priority = 1  
  - immediate = 2  
  - flash = 3  
  - flash-override = 4  
  - critical = 5  
  - internet = 6  
  - network = 7  [file:24]

- 現状の設定と要件のギャップ
    - route-map Texas permit 10（ACL1: 219.65.100.15）  
      `set ip precedence priority` → priority=1 なので要件どおり。  
    - route-map Texas permit 20（ACL2: 219.65.100.16）  
      Precedence の設定がなく、デフォルトの 0（routine）のまま。  
      本来は 5（critical）にしたい。

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 219.65.100.15 については既に `set ip precedence priority` で 1 をセットできているので変更不要。  
    - 219.65.100.16 については、permit 20 に  
      `set ip precedence critical`  
      を追加すれば Precedence=5（critical）に設定できる。[file:24]
    - 選択肢 A の  
      > set ip precedence critical in route-map Texas permit 20  
      がちょうどこの修正に該当するため正解。

- 他の選択肢がダメな理由
    - B: permit 10 を immediate(2) に変えてしまい、219.65.100.15 の要件「1」を満たさなくなる。
    - C: permit 10 を critical(5) に変えてしまい、こちらも 219.65.100.15 の要件「1」と不一致。
    - D: permit 20 を priority(1) にすると、219.65.100.16 も Precedence=1 となり、必要な「5」にならない。

***
### QUESTION 330 解説

- 問題文:
R2 のルーティングエントリ（一部）が次のように表示されている:

C 192.168.10.0/24 is directly connected, Serial1/1e  
172.16.0.0/16 is variably subnetted, 5 subnets, 2 masks  
C 172.16.160.0/19 is directly connected, Loopback1  
C 172.16.128.0/19 is directly connected, Loopback2  
C 172.16.224.0/19 is directly connected, Loopback3  
C 172.16.192.0/19 is directly connected, Loopback4  
D 172.16.0.0/16 is a summary, 00:01:23, Null0  

R1 と R2 の間で EIGRP を構成する際、「要約ルート 172.16.0.0/16 を広告させたくない」。  
どの設定を行えばよいか。[file:24]

- 状況のポイント（日本語）:
    - R2 には 172.16.128.0/19 ～ 172.16.224.0/19 など複数のサブネットとともに、  
      `D 172.16.0.0/16 is a summary, Null0` という **EIGRP の自動サマリールート** が入っている。  
    - このサマリは「EIGRP auto-summary」が有効なときに生成される典型的な形で、  
      R2 から R1 へ 172.16.0.0/16 が広報されてしまう。[file:24]
    - 要件は「R1 と R2 の間の EIGRP で、このサマリを使わないこと」。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - 自動サマリは、**そのルートを持っているルータ側の EIGRP プロセス** で制御される。  
      今回サマリ 172.16.0.0/16 を持っているのは R2 なので、R2 側で `no auto-summary` を入れる必要がある。
    - 選択肢 B の  
      - `R2(config)# router eigrp 1`  
      - `R2(config-router)# no auto-summary`  
      により、R2 は 172.16.0.0/16 の自動サマリを生成・広告しなくなり、R1 には個別の /19 ルートだけが配布される。

- 他の選択肢がダメな理由
    - A: R1 に `no auto-summary` を入れても、R2 から送られてくる 172.16.0.0/16 のサマリルートの生成源は R2 のままなので解決しない。
    - C / D: `auto-summary` を有効化する設定であり、逆にサマリルートの生成を促進してしまう。

***
### QUESTION 331 解説

- 問題文:
Refer to the exhibit.  
R1 と R2 は EIGRP ピアを結ぶ際に認証を使うよう設定されているが、ネイバー関係が不安定で down/up を繰り返している。  
どの対応で問題を解決できるか。

- 設定要約:
R1:

key chain cisco  
 key 2  
  key-string abc  

interface Serial1/0  
 ip address 192.168.10.1 255.255.255.0  
 ip authentication mode eigrp 100 md5  
 ip authentication key-chain eigrp 100 cisco  

router eigrp 100  
 network 10.10.1.0 0.0.0.255  
 network 192.168.10.0  
 no auto-summary  

R2:

key chain cisco  
 key 1  
  key-string 123  
 key 2  
  key-string abc  

interface Serial1/0  
 ip address 192.168.10.2 255.255.255.0  
 ip authentication mode eigrp 100 md5  
 ip authentication key-chain eigrp 100 cisco  [file:75]

`show ip eigrp neighbors` では、R2 から見ると隣接が一度 up した後すぐ retry limit exceeded で down になり、再度 up する、を繰り返している。[file:76]

- 問題文要約（日本語）:  
両ルータとも key chain 名は「cisco」、EIGRP 認証モードは MD5 で一致しているのに、EIGRP 隣接が安定しない。  
どの設定変更が必要か。

- 正解: **D**

- 解説（なぜ D が正しいか）
    - EIGRP の MD5 認証では、**key-chain 内の「最も小さい key-id」を送信時に使用**する。[file:24]
    - R1 の key chain:
        - key 2 のみ（string "abc"）  
        → R1 は送信時に key-id 2 を使う。
    - R2 の key chain:
        - key 1 ("123") と key 2 ("abc")  
        → R2 は送信時に **key-id 1** を使う。
    - パケット受信時の挙動:
        - R2 が R1 から受信: R1 は key 2 / "abc" を使う。  
          R2 も key 2 を持っているため検証に成功し、この方向の認証は問題ない。
        - R1 が R2 から受信: R2 は key 1 / "123" を使う。  
          しかし R1 側には key-id 1 が存在しないため、R1 はそのパケットを正しく検証できず拒否する。  
          これが隣接 flapping の原因。
    - 従って、**両ルータで「使用される最小 key-id を揃える」**必要がある。  
      例えば、R1 側にも key 1 を追加するか、R2 側で key 1 を削除して key 2 のみを残すなど。  
      この考え方を述べているのが選択肢 D の  
      > Configure a matching lowest key-id on both routers.  
      であり、これが正解となる。

- 他の選択肢がダメな理由
    - A: 認証タイプ（MD5）は既に `ip authentication mode eigrp 100 md5` で一致している。
    - B: key-chain 名も両方とも `cisco` で一致している。
    - C: 「どれか 1 つの key-id を合わせればよい」ではなく、「送信に使われる最小 key-id」が一致していないことが問題なので不十分。

***
### QUESTION 332 解説

- 問題文:
Which method provides failure detection in BFD?

- 問題文要約（日本語）:  
BFD がフォワーディングパスの障害検出を行うとき、その特徴を最もよく表している組み合わせはどれか。[file:24]

- 選択肢:
A. short duration, low overhead  
B. long duration, low overhead  
C. long duration, high overhead  
D. short duration, high overhead  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - BFD（Bidirectional Forwarding Detection）は、隣接ルータ間のフォワーディングパス（インターフェイス、リンク、転送プレーン）の障害を、**非常に短い間隔で監視する軽量な仕組み**。[file:24]
    - 特徴は:
        - **short duration**: ミリ秒単位の短い検出タイマで素早くダウンを検出できる。  
        - **low overhead**: 単純な制御パケットを周期的に送るだけなので、CPU や帯域への負荷が小さい。  
    - この 2 点の組み合わせは選択肢 A の  
      > short duration, low overhead  
      と一致する。

- 他の選択肢がダメな理由
    - B / C: long duration は BFD の「高速検出」という目的と矛盾する。
    - D: high overhead では BFD の「軽量」という設計方針と合わない。

***
### QUESTION 333 解説

- 問題文:
Refer to the exhibit.  
TCP トラフィックは、ホスト 10.10.10.10/24 に対して左側の R2 経由で到達させたい。  
どの操作を行えばよいか。

- 設定要約（R1）:
ip access-list extended UDP-ACL
 permit udp any any
!
route-map VIA-R2 permit 10
 match ip address UDP-ACL
 set ip next-hop 10.10.11.2
!
interface Gi0/1
 ip policy route-map VIA-R2  [file:77]

- 問題文要約（日本語）:  
R1 の PBR では UDP-ACL にマッチしたトラフィックだけを next-hop 10.10.11.2（R2 側）に送るよう設定されている。  
しかし、TCP トラフィックも R2 経由にしたい。

- 選択肢:
A. 変更なしでも TCP は R2 経由になる。  
B. route-map permit 10 の set ip next-hop を 10.10.12.2 に変更する。  
C. ACL に TCP を許可するエントリを追加し、route-map はそのままにする。  
D. route-map に permit 20 ステートメントを追加して TCP を許可する。  

- 正解: **C**

- 解説（なぜ C が正しいか）
    - 現在の拡張 ACL `UDP-ACL` は `permit udp any any` のみであり、**UDP トラフィックだけ**が PBR にマッチして R2（10.10.11.2）に送られる。[file:77]
    - TCP トラフィックは ACL にマッチしないため、PBR の対象外となり、通常のルーティング（＝右側 R4 経由など）で転送される。
    - したがって、**ACL に TCP を含めるだけでよい**。  
      例:  
      - `permit udp any any`  
      - `permit tcp any any`  
      とすれば、UDP と TCP の両方が同じ route-map エントリ（permit 10）にマッチし、`set ip next-hop 10.10.11.2` により R2 経由となる。
    - この操作内容を示しているのが選択肢 C の  
      > Allow TCP in the access list with no changes to the route map.  
      であり、正解となる。

- 他の選択肢がダメな理由
    - A: 現状 ACL が UDP のみ許可のため、TCP は PBR 適用外で R2 経由にはならない。
    - B: next-hop アドレスを変えても、そもそも TCP が route-map にマッチしていないので意味がない。
    - D: permit 20 を追加しても、そこにマッチさせるための ACL や条件がないと TCP は依然として PBR されない。

***
### QUESTION 334 解説

- 問題文:
Refer to the exhibit.  
R1, R2, R3, R4 が EIGRP AS 100 で接続されている。  
R1–R2–R3 の三角形部分で、poison reverse が有効になっているにもかかわらずループが発生している。  
R1, R2, R3 間のループを防ぐにはどの対応を行うべきか。[file:78]

- 選択肢:
A. Configure route filtering  
B. Configure route tagging  
C. Configure R3 as stub receive-only.  
D. Enable split horizon  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 図では R1–R2–R3 の 3 台がフルメッシュではなく、「ある宛先に対して複数の経路情報がグルグル回ってしまう」状況が想定されている。[file:24]
    - EIGRP では、poison reverse や split horizon で多くのループを防げるが、  
      特定のトポロジでは依然として「あるルータから学習した経路を別方向で再度広告してしまう」ことでループが生じうる。
    - このようなときに有効なのが **route filtering** で、  
      - 例えば R2 が R3 経由で学習した特定プレフィックスを R1 に広告しないようにする  
      - または R3 が R1 から学習した経路を R2 へ再広告しないようにする  
      といった制御をかけることで、ループの経路そのものを遮断できる。[file:24]
    - 選択肢 A はその対策を表しており、正解となる。

- 他の選択肢がダメな理由
    - B: route tagging は主に「プロトコル間の再配送（redistribution）」でループを防ぐために使われる。  
      今回は EIGRP 単一ドメイン内の話であり、前提に redistribution がないので不適切。
    - C: R3 を stub receive-only にすると、R3 はルートを他へ広告しなくなるため、R3 の下流ネットワークを他ルータが学習できなくなるなど、要件を満たさない。
    - D: split horizon は「受信したインターフェイスから同じ経路を送り返さない」機能で、主にハブ&スポークのような部分メッシュ環境で有効。  
      問題文では既に poison reverse が有効であり、split horizon だけでこの三角形内のループは解消できない。

***
### QUESTION 335 解説

- 問題文:
A network administrator added a new spoke site with dynamic IP on the DMVPN network.  
Which configuration command passes traffic on the DMVPN tunnel from the spoke router?

- 問題文要約（日本語）:  
DMVPN ネットワークに「動的 IP（DHCP など）」を使う新しいスポークを追加した。  
スポーク側の物理（NBMA）アドレスが変わっても、ハブ側 NHRP サーバがそのスポークからのトラフィックを受け付けられるようにするには、どのコマンドが必要か。[file:79]

- 選択肢:
A. ip nhrp registration no-unique  
B. ip nhrp registration no-registration  
C. ip nhrp registration ignore  
D. ip nhrp registration dynamic  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - DMVPN では、スポークは NHRP を使って「トンネル IP ↔ NBMA（物理）IP」をハブに登録する。[file:79]
    - 通常は、NHS（ハブ）は「同じトンネル IP には 1 つの NBMA アドレスだけ」を許可する（ユニーク制約）。  
      しかし、スポークが動的 IP を使う場合、再接続のたびに NBMA アドレスが変わる。
    - `ip nhrp registration no-unique` をスポークで設定すると、  
      **ハブ側 NHS が同じトンネル IP からの新しい NBMA アドレスの登録を受け付ける** ようになり、  
      IP 変更後もトンネル経由でトラフィックが通るようになる。[file:79]
    - したがって、動的 IP スポークでトラフィックを通すために必要なコマンドは選択肢 A となる。

- 他の選択肢がダメな理由
    - B: `no-registration` のようなキーワードは NHRP で使用されない。
    - C: `registration ignore` は、ハブ側が特定の登録要求を無視する文脈で使われるもので、スポーク側で動的 IP を許可する目的とは合致しない。
    - D: `registration dynamic` というコマンドは存在せず、動的 IP 許可の機能とも一致しない。

***
### QUESTION 336 解説

- 問題文:
Refer to the exhibit.  
R4 から、R2 の向こう側にある 172.16.2.7 へトラフィックを送るとパケットドロップが発生している。  
どの対応を行えば問題を解決できるか。[file:124]

- トポロジ概要:
    - R1 側 LAN: 172.16.1.0/24  
    - R3 側 LAN: 172.16.2.0/24  
    - R1–R2 間: 10.1.1.0/24  
    - R2–R3 間: 10.1.2.0/24  
    - 3 台とも EIGRP AS 100 を実行。  
    - R1, R3 は auto-summary 有効のため、R2 へ 172.16.0.0/16 として広告している。[file:79]

- 選択肢:
A. Enable auto summarization on all three routers R1, R2, and R3.  
B. Insert a /16 floating static route on R2 toward R3 with metric 254.  
C. Disable auto summarization on R2.  
D. Insert a /24 floating static route on R2 toward R3 with metric 254.  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - R1 と R3 から見た 172.16.0.0/16 の EIGRP ルートが、R2 上で **左右の両インターフェイス（Fa0/0, Fa0/1）経由の等コストルート** として存在するため、R2 は 172.16.2.7 へのパケットをどちら側にも転送しうる。[file:79]
    - 172.16.2.7 へ到達すべき正しい経路は **R3 側（10.1.2.0/24）** だが、要約ルートだけでは R2 が 172.16.1.0/24 と 172.16.2.0/24 を区別できないため、R1 側へ誤転送し、結果としてループやドロップが発生する。
    - これを避けるには、R2 に「172.16.2.0/24 は必ず R3 側へ」という **より具体的なスタティックルート** を入れておけばよい。  
      - 例: `ip route 172.16.2.0 255.255.255.0 10.1.2.x 254`  
      - /24 ルートは /16 要約よりも「長いプレフィックス（より具体的）」なので、常に EIGRP の 172.16.0.0/16 より優先される。
    - この考え方を示しているのが選択肢 D の  
      > Insert a /24 floating static route on R2 toward R3 with metric 254.  
      であり、正解となる。

- 他の選択肢がダメな理由
    - A: R2 でも auto-summary を有効にしても、すでに要約ルートの問題が起きているので根本解決にならない。
    - B: /16 のスタティックルートは EIGRP の /16 と同じプレフィックス長であり、AD を 254 にしても優先順位の制御が難しく、誤転送を防げない。
    - C: R2 の auto-summary を無効にしても、R1 と R3 から 172.16.0.0/16 が送られてきている限り、R2 のルーティングテーブルに 2 本の /16 が残りうるため解決しない。

***
### QUESTION 337 解説

- 問題文:
Refer to the exhibit.  
DHCPv6 を用いて展開されたネットワークで、サーバ間の到達性が不安定になっている。  
DHCPv6 が正常動作するようにするには、どのコマンドを設定から削除する必要があるか。[file:125]

- 設定要約:
ipv6 dhcp pool DHCPPOOL  
 address prefix 2001:0:1:4::/64 lifetime infinite infinite  

interface FastEthernet0/0  
 ip address 10.0.0.1 255.255.255.240  
 ipv6 address 2001:0:1:4::1/64  
 ipv6 enable  
 ipv6 nd ra suppress  
 ipv6 ospf 1 area 1  
 ipv6 dhcp server DHCPPOOL  [file:125]

- 選択肢:
A. ipv6 address 2001:0:1:4::1/64  
B. ipv6 dhcp server DHCPPOOL  
C. ipv6 nd ra suppress  
D. address prefix 2001:0:1:4::/64 lifetime infinite infinite  

- 正解: **C**

- 解説（なぜ C が正しいか）
    - DHCPv6 でアドレスを配布する場合、多くのホストは **RA（Router Advertisement）の M フラグ** を見て、「DHCPv6 を使うかどうか」を判断する。[file:79]
    - インターフェイスに `ipv6 nd ra suppress` を設定すると、そのインターフェイスでは **RA 自体が送信されなくなる**。  
      するとホストは M フラグを受け取れず、DHCPv6 を使うか SLAAC にするかを適切に判断できないため、アドレス取得やデフォルトゲートウェイ学習が不安定になる。
    - したがって、DHCPv6 を安定動作させるには **RA を抑止している `ipv6 nd ra suppress` を削除**すべきである。  
      RA を送信しつつ M フラグを 1 に設定すれば、ホストは「アドレス情報は DHCPv6 から取得すべき」と判断できる。
    - 他の行（DHCP プール定義、インターフェイスの IPv6 アドレス、DHCPv6 サーバ指定）は DHCPv6 に必要な設定であり、削除すると逆に機能しなくなる。

***
### QUESTION 338 解説

- 問題文:
An engineer failed to run diagnostic commands on devices using Cisco DNA Center.  
Which action in Cisco DNA Center resolves the issue?[file:79]

- 選択肢:
A. Enable APIs  
B. Enable Secure Shell  
C. Enable CDP.  
D. Enable Command Runner.  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - Cisco DNA Center には、管理対象デバイスに対して CLI ベースの診断コマンドを一括実行し、その出力を収集する機能 **Command Runner** が用意されている。[file:79]
    - エンジニアが Cisco DNA Center から診断コマンドを実行できない場合、対象デバイスに対してこの Command Runner 機能が有効になっていない（もしくはワークフローで使用していない）ことが原因となる。  
    - Command Runner を有効化・使用することで、選択した複数デバイスに対し show 系などの診断 CLI を実行し、その結果を画面やレポートで確認できるようになるため、設問の問題が解消される。  

- 他の選択肢がダメな理由
    - A: API の有効化は DNA Center と外部システム連携用であり、デバイスへ CLI 診断を投げる機能とは直接関係しない。
    - B: SSH はデバイス側での管理アクセスプロトコルであり、DNA Center 側で「Enable Secure Shell」という操作ではない。
    - C: CDP は隣接情報の検出に使うプロトコルで、診断コマンド実行可否には影響しない。

***

### QUESTION 339 解説

- 問題文:
Refer to the exhibit.  
エンジニアは、ルータのコンフィグ上に平文で見えないように、暗号化されたユーザパスワードを設定しようとしている。  
どの 2 つのコマンドを使えばよいか。（2 つ選択）[file:126]

- 状況:
R1(config)# username Admin password 7 Cisco@123  
Invalid encrypted password: Cisco@123  

`password 7` は「既に暗号化された文字列」を入力する形式であり、`Cisco@123` のような平文を直接指定するとエラーになる。[file:79]

- 選択肢:
A. username Admin password 5 Cisco@123  
B. username Admin password Cisco@123  
C. password encryption aes  
D. username Admin secret Cisco@123  
E. no service password-encryption  
F. service password-encryption  

- 正解: **D, F**

- 解説（なぜ D, F が正しいか）
    - `username Admin secret Cisco@123` を使うと、パスワードは **Type 5/Type 9 のハッシュ（MD5 など）** として保存され、コンフィグ上に平文で表示されない。[file:79]  
      → これが選択肢 **D**。
    - さらに `service password-encryption` を有効化すると、`password` 形式で保存される他のパスワード（line vty, console, enable password など）も **Type 7 で暗号化されて表示**される。[file:79]  
      → これが選択肢 **F**。
    - この 2 つを組み合わせることで、「ユーザのパスワードも含め、コンフィグ上に平文パスワードを残さない」という要件を満たせる。

- 他の選択肢がダメな理由
    - A: `password 5` も「暗号化済み文字列」を期待する形式で、平文 `Cisco@123` を直接入れると無効。
    - B: `password` だけだと平文で保存され、`service password-encryption` がなければ丸見えになる。
    - C: `password encryption aes` というグローバルコマンドは存在しない（ASA など別機器の文法と混同しやすい）。
    - E: `no service password-encryption` は逆に平文表示を許してしまう。

***
### QUESTION 340 解説

- 問題文:
Refer to the exhibit.  
R6 から R1 へのトラフィックは、R6 → R5 → R2 → R1 のパスを通るべきである。  
どの操作を行えばよいか。[file:127]

- トポロジ概要:
    - 3 つのエリア:
        - Area 1: R1–R2–R3  
        - Area 0: R2–R3–R4–R5  
        - Area 2: R4–R5–R6  
    - 各リンクには OSPF コストが表示されている（20, 40, 80 など）。[file:127]

- 目的の経路:
    - R6 から R1 への希望パスは  
      R6 → R5 → R2 → R1

- なぜ調整が必要か
    - 現状では、R6 から見た R1 への最短コストが  
      R6 → R5 → R2 → R3 → R1  
      のように R3 経由になってしまっている。  
    - つまり、R2–R3 間が相対的に安く、R2–R1 への直接パスより R3 を経由したパスの合計コストが小さい状態。

- 正解: **C**

- 解説（なぜ C が正しいか）
    - R2–R3 間のコストを **61** に上げると、R2 → R3 → R1 方向のパス全体のコストが大きくなり、  
      R2 → R1 への直接パス（80 コスト）を経由する方が相対的に安くなる。
    - その結果、Area 0 内・全体トポロジで計算される SPF の結果として、  
      R6 から R1 への最短経路が **R6 → R5 → R2 → R1** となる。
    - 選択肢 C の  
      > Increase the cost to 61 between R2 and R3  
      はまさにこの調整を指しており、希望するパス選択を実現する。

- 他の選択肢がダメな理由
    - A: R6–R5–R2 間のコストを下げても、R2 から先で R3 経由が依然として安ければ、R3 経由が選ばれる可能性が残る。
    - B: 「R2–R3–R1 間すべてを 61 にする」という表現はあいまいで、実際に必要なのは R2–R3 リンク単体のコスト調整である。
    - D: R2–R1 間のコストを 41 に下げるとパスは改善される可能性はあるが、問題の意図は「R2–R3 間を高くして R3 経由パスを不利にすること」であり、公式解としては C が選ばれている。

***
### QUESTION 341 解説

- 問題文（英語）:
Refer to the exhibit. A network engineer applied a filter for LSA traffic on OSPFv3 interarea routes on the area 5 ABR to protect advertising the internal routes of area 5 to the business partner network. All other areas should receive the area 5 internal routes. After the respective route filtering configuration is applied on the ABR, area 5 routes are not visible on any of the areas. How must the filter list be applied on the ABR to resolve this issue? [file:130][file:131]

- 問題文要約（日本語）:  
OSPFv3 ネットワークで、Area 5 の ABR に「interarea ルート用の LSA フィルタ」を設定し、  
Area 20（ビジネスパートナ）には Area 5 の内部ルートを広告しないようにしたい。  
しかし適用後、Area 5 のルートがどのエリアからも見えなくなってしまった。  
この要件を満たしつつ、他のエリアからは Area 5 ルートが見えるようにするには、フィルタリストを ABR にどう適用すべきか。

- 選択肢:
A. in the "out" direction for area 20 on router R2  
B. in the "out" direction for area 5 on router R1  
C. in the "in" direction for area 20 on router R2  
D. in the "in" direction for area 5 on router R1  

- 正解: **C**

- 解説（なぜ C が正しいか）
    - 要件は「Area 5 のルートを、Area 20（ビジネスパートナ）にだけ見せない」ことであり、Area 0 や Area 10 には普通に広告したい。[file:131]
    - Area 5 と Area 0 を接続する ABR は R1。R1 で Area 5 の LSA を **out 方向でフィルタ**すると、Area 0 自体が Area 5 ルートを学習できず、その先の Area 10/20 にも一切届かなくなる（今起きている症状）。
    - そこで、Area 5 → Area 0 の広告はそのまま通し、**Area 0 → Area 20 に入る時だけフィルタ**すればよい。  
      その位置にいるのが、Area 0・10・20 の ABR である R2。
    - R2 で Area 20 に対する LSAs を **in 方向でフィルタ**すると、  
        - Area 0・Area 10 は Area 5 ルートを受け取る  
        - Area 20 のみ Area 5 ルートが入ってこない  
      という動作になり、要件を満たす。
    - これを表しているのが選択肢 C の  
      > in the "in" direction for area 20 on router R2  
      なので、正解となる。

- 他の選択肢がダメな理由
    - A: Area 20 へ出ていく「out 方向」でフィルタすると、一見よさそうに見えるが、ABR の実装上 interarea LSA の扱いが意図通りにならず、エリア間での汎用的な解決策としては不適切とされている（公式解は C）。  
    - B / D: R1 に対して in/out 方向でフィルタをかけると、Area 0 自体に Area 5 ルートが流れず、結果としてどのエリアからも Area 5 ルートが見えなくなる。

***
### QUESTION 342 解説

- 問題文（英語）:
ip sla 10  
 icmp-echo 10.1.1.10  
 timeout 2000  
 threshold 2000  
 frequency 40  
ip sla schedule 10 life forever start-time now  
!  
track 1 ip sla 10 reachability  

Refer to the exhibit. An engineer configured IP SLA to monitor a next hop on a router for reachability. When the next hop is unreachable, the router is executing tracking and failing over another route, but packet loss is experienced because the reachability is flapping. Which action resolves the issue?

- 問題文要約（日本語）:  
上記のとおり IP SLA（ICMP echo）で次ホップ 10.1.1.10 を監視し、`track 1 ip sla 10 reachability` で到達不能になったら別ルートへフェイルオーバーする設定になっている。  
しかし、到達性が「上がったり下がったり」を頻繁に繰り返し、そのたびに経路が切り替わるためパケットロスが発生している。  
このフラッピングを抑えて安定させるにはどうすればよいか。

- 選択肢:
A. Increase the frequency of the SLA probe to 60.  
B. Increase the timeout of the SLA probe to 6000.  
C. Append `delay up 0 down 0` to the track command.  
D. Append `delay up 60 down 60` to the track command.  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - `track 1 ip sla 10 reachability` だけだと、IP SLA の成功/失敗が起きるたびに即座にトラッキング状態が変わり、経路もすぐ切り替わるため、軽い瞬断でもルートがフラップしてしまう。[file:79]
    - `delay up <sec> down <sec>` を付与すると、  
        - `up`：SLA が成功し続けてからトラックを Up にするまで待つ時間  
        - `down`：SLA が失敗し続けてからトラックを Down にするまで待つ時間  
      を指定でき、**ヒステリシス** を持たせてフラッピングを防げる。
    - `delay up 60 down 60` を付けると、「連続 60 秒間成功 or 失敗が続いた場合のみ状態を切り替える」ようになり、短時間の到達性ブレではルート切り替えが起きなくなる。
    - したがって、選択肢 D  
      > Append delay up 60 down 60 to the track command.  
      が、フラッピングによるパケットロスを抑える正しい対処となる。

- 他の選択肢がダメな理由
    - A: frequency を 40→60 にしても、依然として「1 回の失敗で即 Down」なのでフラッピングは防げない。
    - B: timeout を 2000→6000 にしても、応答が遅い場合の扱いが変わるだけで、連続状態を見ているわけではない。
    - C: `delay up 0 down 0` はデフォルトと同じで、全くヒステリシスが付かないため問題は解決しない。

***
### QUESTION 343 解説

- 問題文（英語）:
Refer to the exhibit. The R2 loopback interface is advertised with RIP and EIGRP using default values. Which configuration changes make R1 reach the R2 loopback using RIP? [file:132]

- 問題文要約（日本語）:  
R2 のループバック 192.168.1.100/32 は、RIP と EIGRP の両方で R1 に広告されている（どちらもデフォルト設定）。  
現在 R1 は、より優先度の高い EIGRP 経路を使って R2 ループバックへ到達している。  
このループバックへ **RIP 経路を使って** 到達させるには、R1 でどの設定変更を行うべきか。

- 選択肢:
A.  
   R1(config)# router eigrp 1  
   R1(config-router)# distance eigrp 120 120  

B.  
   R1(config)# router rip  
   R1(config-router)# distance 100  

C.  
   R1(config)# router eigrp 1  
   R1(config-router)# distance eigrp 130 120  

D.  
   R1(config)# router rip  
   R1(config-router)# distance 90  

- 正解: **C**

- 解説（なぜ C が正しいか）
    - デフォルトの管理距離 (AD):
        - EIGRP 内部: 90  
        - RIP: 120  
      そのため、同じ宛先プレフィックスが RIP と EIGRP の両方から来た場合、**AD が小さい EIGRP (90)** が選ばれる。[file:79]
    - 目的は「RIP を使わせること」なので、R1 では **EIGRP の AD を RIP より大きく** しなければならない。
    - 選択肢 C は  
      `distance eigrp 130 120`  
      と設定し、  
        - EIGRP 内部ルートの AD = 130  
        - EIGRP 外部ルートの AD = 120  
      としている。  
      これにより、RIP の AD 120 より **EIGRP 内部 (130) が大きくなる** ため、同一宛先に対して RIP 経路が優先されるようになる。
    - こうして R1 は、R2 ループバックへの到達経路として RIP を選択する。

- 他の選択肢がダメな理由
    - A: EIGRP の AD を 120/120 にすると、RIP 120 と同値になり、どちらが選ばれるかは実装依存で RIP を確実に優先できない。
    - B: RIP の AD を 100 に下げれば、EIGRP 90 より依然として大きくはないが、**本来は 100 > 90 なので EIGRP が優先**されたまま。
    - D: RIP の AD を 90 に下げると EIGRP 内部 (90) と同値になり、やはり RIP を必ずしも優先できない（しかも問題の意図は EIGRP 側の AD を上げること）。

***
### QUESTION 344 解説

- 問題文（英語）:
Refer to the exhibit. An engineer must advertise routes into IPv6 MP-BGP and failed. Which configuration resolves the issue on R1? [file:133]

- 問題文要約（日本語）:  
図では、R1 が左側 LAN プレフィックス `2001:DB8::/64` を持ち、R1–R2 間も IPv6 で接続されている。  
R1 からこの `2001:DB8::/64` を **IPv6 MP-BGP（IPv6 unicast アドレスファミリ）** に広告したいが、うまく広告されていない。  
どの BGP 設定が R1 上で必要か。

- 選択肢:
A.  
   router bgp 64900  
    no bgp default ipv4-unicast  
    address-family ipv6 unicast  
     redistribute ospf  
     network 2001:DB9::/64  

B.  
   router bgp 65000  
    no bgp default ipv4-unicast  
    address-family ipv6 multicast  
     network 2001:DB8::/64  

C.  
   router bgp 65000  
    no bgp default ipv4-unicast  
    address-family ipv6 unicast  
     network 2001:DB8::/64  

D.  
   router bgp 64900  
    no bgp default ipv4-unicast  
    address-family ipv6 multicast  
     neighbor 2001:DB8:7000::2 translate-update ipv6 multicast  

- 正解: **C**

- 解説（なぜ C が正しいか）
    - IPv6 MP-BGP で通常のユニキャストルートを広告するには、  
      - 正しい AS 番号で `router bgp <ASN>` を定義  
      - `no bgp default ipv4-unicast` で IPv4 AF のデフォルト有効を無効化（IOS XE などの動作）  
      - `address-family ipv6 unicast` を有効にし、その中で `network` や `redistribute` を設定  
      する必要がある。[file:79]
    - トポロジ上、R1/R2 は AS 65000 内の iBGP であるため、ASN は 65000 であるべき。  
      また、広告したいのはユニキャストプレフィックス `2001:DB8::/64`。
    - 選択肢 C は  
      - BGP AS を 65000 に設定  
      - IPv6 **unicast** アドレスファミリを使用  
      - 正しいプレフィックス `2001:DB8::/64` を `network` で指定  
      しており、要件に一致する。

- 他の選択肢がダメな理由
    - A: ASN が 64900 と誤っており、さらに network が `2001:DB9::/64` と違うプレフィックスになっている。
    - B: `address-family ipv6 multicast` になっており、ユニキャストルートを広告する用途に合わない。
    - D: こちらも ASN が誤り、かつ IPv6 multicast AF と `translate-update` でマルチキャスト関連の設定になっており、要求と異なる。

***
### QUESTION 345 解説

- 問題文（英語）:
Refer to the exhibit. A network administrator must configure DMVPN tunnels between the hub and spoke with dynamic spoke-to-spoke tunnel capabilities using EIGRP. Which tunnel interface command must the network administrator configure to establish an EIGRP peer? [file:79]

- 問題文要約（日本語）:  
DMVPN（mGRE＋NHRP）でハブ＆スポーク構成を作成しており、スポーク同士も動的にトンネル（spoke-to-spoke）を張れるようにしたい。  
ルーティングには EIGRP を使用しているが、トンネル上で EIGRP ピアが正しく張れず、特に spoke-to-spoke の経路で問題が出ている。  
EIGRP ピア確立と spoke-to-spoke 通信を成立させるには、トンネルインターフェイス上でどのコマンドを設定すべきか。

- 選択肢:
A. `no ip next-hop-self eigrp 1`  
B. `no ip nhrp next-hop-self`  
C. `ip nhrp next-hop-self`  
D. `ip next-hop-self eigrp 1`  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - DMVPN フェーズ 3 などで EIGRP を使う場合、ハブがスポーク宛てルートの **next-hop を自分自身に書き換えてしまう** と、スポーク同士が直接トンネルを張るための「本来の次ホップ（相手スポーク）」情報が失われる。[file:79]
    - その結果、spoke-to-spoke トンネルではなく、常に hub 経由の転送になりやすく、EIGRP のネイバー関係や経路選択に問題が出る。
    - `no ip next-hop-self eigrp 1` をトンネルインターフェイスで指定すると、ハブは EIGRP で再広告する際に next-hop を自分に書き換えなくなり、スポークは互いのルートの next-hop に **相手スポークのトンネル IP** を保持できる。  
      これにより、NHRP と組み合わせて spoke-to-spoke トンネルが確立し、EIGRP ピアリングとトラフィックの直接転送が可能になる。

- 他の選択肢がダメな理由
    - B/C: `ip nhrp next-hop-self`／`no ip nhrp next-hop-self` は NHRP のマッピング挙動に関係するが、問題のポイントは **EIGRP の next-hop-self** の抑制であり、NHRP 側ではない。
    - D: `ip next-hop-self eigrp 1` はその逆で、ハブが積極的に next-hop を自分に書き換えてしまうため、spoke-to-spoke トンネルをさらに阻害してしまう。

***
### QUESTION 346 解説

- 問題文（英語）:
access-list 120 permit tcp any gt 1024 eq bgp log  
access-list 120 permit tcp any eq bgp gt 1024 established  
access-list 120 permit tcp any gt 1024 eq 639  
access-list 120 permit tcp any eq 639 gt 1024 established  
access-list 120 permit tcp any eq 646  
access-list 120 permit udp any eq 646  
access-list 120 permit ospf any  
access-list 120 permit ospf any host 224.0.0.5  
access-list 120 permit ospf any host 224.0.0.6  
access-list 120 permit eigrp any  
access-list 120 permit eigrp any host 224.0.0.10  
access-list 120 permit udp any any eq pim-auto-rp  

Refer to the exhibit. The control plane is heavily impacted after the above CoPP configuration is applied to the router. Which command removal lessens the impact on the control plane? [file:79]

- 問題文要約（日本語）:  
上記の ACL 120 を CoPP（Control Plane Policing）で使用したところ、コントロールプレーンへのトラフィック負荷が大きくなってしまった。  
ACL のどのエントリを削除すれば、コントロールプレーンへの影響をもっとも軽減できるか。

- 選択肢:
A. `access-list 120 permit udp any any eq pim-auto-rp`  
B. `access-list 120 permit tcp any gt 1024 eq bgp log`  
C. `access-list 120 permit ospf any`  
D. `access-list 120 permit eigrp any host 224.0.0.10`  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - 前提として、ACL 中で宛先が省略されている行は `any` 宛てとみなされる。したがって、  
      - `access-list 120 permit ospf any` は「OSPF パケットを任意宛先に対して許可」  
      - `access-list 120 permit eigrp any` は「EIGRP パケットを任意宛先に対して許可」  
      となる。[file:79]
    - そのうえで  
      - `access-list 120 permit ospf any host 224.0.0.5`  
      - `access-list 120 permit ospf any host 224.0.0.6`  
      - `access-list 120 permit eigrp any host 224.0.0.10`  
      は、すでにより広い条件で許可されているトラフィックの**サブセット**であり、完全に冗長なエントリとなる。
    - CoPP では ACL のエントリが多いほどマッチ処理のオーバーヘッドが増え、コントロールプレーンに余計な負荷がかかる。  
      冗長なエントリを削除することで、マッチ処理を減らし影響を軽減できる。
    - 選択肢の中で削除候補として挙がっている冗長エントリは  
      `access-list 120 permit eigrp any host 224.0.0.10`  
      のみなので、D が正解となる。

- 他の選択肢がダメな理由
    - A: PIM Auto-RP 用の UDP トラフィックはマルチキャスト制御に必要で、冗長ではない。
    - B: BGP トラフィックをログ付きで許可する重要なエントリであり、これを消すと BGP の制御トラフィックが制限されてしまう。
    - C: `access-list 120 permit ospf any` を消すと、OSPF の制御トラフィック自体が CoPP で落とされる可能性があり、ルーティングに影響する。

***
### QUESTION 347 解説

- 問題文（英語）:
Which two components are required for MPLS Layer 3 VPN configuration? (Choose two.) [file:79]

A. Use a unique RD per customer VRF  
B. Use OSPF between PE and CE  
C. Use MP-BGP for customer routes  
D. Use pseudowire for Layer 2 routes  
E. Use LDP for customer routes  

- 問題文要約（日本語）:  
MPLS L3 VPN を構成するうえで、「必須」となるコンポーネントを 2 つ選びなさい、という問題。

- 正解: **A, C**

- 解説（なぜ A と C が正しいか）
    - MPLS L3 VPN では、**各顧客ごとに VRF を分離**し、その VRF 内の経路を MPLS コアで運ぶために **VPNv4 アドレス（32bit IPv4 + 64bit RD）** を使う。  
      そのため、通常は「顧客ごと（あるいは VRF ごと）に一意な RD (Route Distinguisher)」を設定する必要がある。[file:79]
    - PE 間で各 VRF の経路をやり取りする際には、**MP-BGP (Multiprotocol BGP)** の VPNv4 アドレスファミリを使って VRF ルートを広告する。  
      これにより、顧客同士で重複した IPv4 アドレスを持っていても、RD により区別され、正しく PE 間で配送される。[file:79]
    - よって  
      - A: 「Use a unique RD per customer VRF」  
      - C: 「Use MP-BGP for customer routes」  
      の 2 つが、MPLS L3 VPN を成り立たせるための必須要素となる。

- 他の選択肢がダメな理由
    - B: PE–CE 間で使う IGP/EGP は EIGRP/OSPF/BGP/静的ルートなど自由に選べるため、OSPF 固定ではない。
    - D: Pseudowire は L2VPN（VPWS, VPLS など）の技術であり、L3 VPN の必須要素ではない。
    - E: LDP は「ラベル配布」で MPLS コアの LSP を作る用途に使うが、「customer routes」を運ぶためのプロトコルではなく、MP-BGP が担当する。

***
### QUESTION 348 解説

- 問題文（英語）:
A customer reports that traffic is not passing on an EIGRP enabled multipoint interface on a router configured as below:

interface Serial0/0/0  
 no ip address  
interface Serial0/0/0.9 multipoint  
 ip address 10.1.1.1 255.255.255.248  
 ip split-horizon eigrp 1  

Which action resolves the issue?

A. Disable poison reverse.  
B. Enable split horizon  
C. Enable poison reverse  
D. Disable split horizon  

- 問題文要約（日本語）:  
上記のように EIGRP を有効にしたマルチポイントサブインターフェイス上で、トラフィックが通らないと報告されている。  
`ip split-horizon eigrp 1` が設定されている状態で、どの操作をすればトラフィックが流れるようになるか。

- 正解: **D**

- 解説（なぜ D が正しいか）
    - フレームリレーや NBMA 環境で「マルチポイント」インターフェイスを使い、そこに複数の EIGRP ネイバー（疑似ハブ＆スポーク）をぶら下げる場合、**split horizon を有効にしていると、学習したルートを同じインターフェイスから再広告できない。**  
      そのため、スポーク間の到達性が失われ、結果として「トラフィックが通らない」状態になる。
    - このときの対処は、マルチポイントインターフェイスで **split horizon を無効化** すること。  
      コマンドとしてはインターフェイス上で  
      `no ip split-horizon eigrp 1`  
      を設定する。
    - 選択肢 D「Disable split horizon」がこれを意味しており、EIGRP が同じマルチポイントインターフェイス上の他の隣接へルートを再広告できるようになり、トラフィックが通るようになる。

- 他の選択肢がダメな理由
    - A / C: poison reverse の有効・無効は、このようなマルチポイント上の再広告制限の問題とは関係がない。
    - B: すでに `ip split-horizon eigrp 1` が設定されているので、有効化ではなく **無効化** が必要。

***
### QUESTION 349 解説

- 問題文（英語）:
Refer to the exhibit. A network administrator notices these console messages from host 10.11.110.12 originating from interface E1/0. The administrator considers this an unauthorized attempt to access SNMP on R1. Which action prevents the attempts to reach R1 E1/0?

A. Configure IOS control plane protection using ACL 90 on interface E1/0  
B. Add a permit statement including the host 10.11.110.12 into ACL 90  
C. Configure IOS management plane protection using ACL 90 on interface E1/0.  
D. Create an inbound ACL on interface E1/0 to deny SNMP from host 10.11.110.12  

- 問題文要約（日本語）:  
ホスト 10.11.110.12 から、R1 のインターフェイス E1/0 宛てに SNMP アクセスの試行があり、コンソールにメッセージが出ている。  
これを「不正な SNMP アクセス」とみなし、E1/0 宛てのその試行を止めたい。どの設定を行うべきか。

- 正解: **D**

- 解説（なぜ D が正しいか）
    - 目的は「特定ホスト 10.11.110.12 からの SNMP（UDP 161/162）アクセスを R1 の E1/0 で止める」こと。  
    - もっともシンプルで確実な方法は、**インターフェイス E1/0 に着信 ACL（inbound ACL）を適用し、そのホストからの SNMP を deny する**こと。  
      例:  
      `access-list 101 deny udp host 10.11.110.12 any eq snmp`  
      `access-list 101 permit ip any any`  
      `interface E1/0`  
      ` ip access-group 101 in`
    - これにより、E1/0 に入ってくる 10.11.110.12 からの SNMP パケットはドロップされ、R1 の SNMP エージェントやコントロールプレーンに届かなくなる。
    - 選択肢 D の  
      > Create an inbound ACL on interface E1/0 to deny SNMP from host 10.11.110.12  
      はこの方針をそのまま表しており、要件に合致する。

- 他の選択肢がダメな理由
    - A / C: コントロールプレーン／マネジメントプレーン保護 (CoPP/MPP) は `control-plane` コンテキストで設定するものであり、単に「ACL 90 をインターフェイスに適用」ではないため、表現として不正確。
    - B: 「permit 10.11.110.12」を ACL に追加すると、むしろそのホストからの SNMP アクセスを許可してしまうので、要件と逆になる。

***
### QUESTION 350 解説

- 問題文（英語）:
Refer to the exhibit. During ISP router maintenance, the network produced many alerts because of the flapping interface. Which configuration on R1 resolves the issue? [file:79]

A. snmp trap ip verify drop-rate  
B. ip verify drop-rate notify hold-down 60  
C. no snmp trap link-status  
D. snmp trap link-status down  

- 問題文要約（日本語）:  
ISP 側ルータのメンテナンス中、R1 から接続されているインターフェイスが頻繁に up/down を繰り返し、そのたびに多数の SNMP アラートが発生した。  
この「インターフェイスのフラッピングによる大量のリンクアップ／ダウン Trap」を抑えたい。R1 ではどの設定を行うべきか。

- 正解: **C**

- 解説（なぜ C が正しいか）
    - SNMP でリンクアップ／ダウンの Trap を送信する動作は、`snmp trap link-status` によって有効になっている。[file:79]
    - これを抑制するには、その逆のコマンドである  
      `no snmp trap link-status`  
      を設定し、「インターフェイスのリンク状態変化に関する SNMP Trap の送信自体を無効化」すればよい。
    - これにより、ISP 側でインターフェイスが何度フラップしても、R1 は SNMP Trap を送信せず、監視システム側のアラートが激減する。

- 他の選択肢がダメな理由
    - A / B: `ip verify drop-rate` は IP Source Guard/ACL drop-rate など別の機能に関連しており、インターフェイスのリンク状態 Trap には関係しない。
    - D: `snmp trap link-status down` は「ダウン時の Trap 送信を明示的に有効」にする方向であり、アラートを減らしたいという目的と逆になる。

***
### QUESTION 351 解説

- 問題文（英語）:
Refer to the exhibit. The administrator is trying to overwrite an existing file on the TFTP server that was previously uploaded by another router. However, the attempt to update the file fails. Which action resolves this issue? [file:134]

A. Make the package.conf file executable by all on the TFTP server.  
B. Make sure to run the TFTP service on the TFTP server.  
C. Make the TFTP folder writable by all on the TFTP server.  
D. Make the package.conf file writable by all on the TFTP server.  

- 問題文要約（日本語）:  
すでに別のルータが TFTP サーバにアップロードした `packages.conf` ファイルを、CPE ルータから上書きしようとしたところ失敗した。  
この既存ファイルを正常に上書きできるようにするには、TFTP サーバ側でどの対応が必要か。

- 正解: **D**

- 解説（なぜ D が正しいか）
    - 端末からは `ping 10.0.2.4` が成功しており、TFTP サーバ自体への IP 到達性には問題がない。[file:134]
    - `copy flash:/packages.conf tftp://10.0.2.4/` を実行すると `Error opening tftp://10.0.2.4/packages.conf (Undefined error)` となっているが、問題文では「すでに存在するファイルを**上書き**しようとしている」と明示されている。[file:79]
    - 既存ファイルを上書きできない典型的な原因は、そのファイル自身に書き込み権限がないこと。  
      この場合、TFTP デーモンはフォルダには書き込めても、該当ファイルを開いて書き換えることができない。
    - 従って、TFTP サーバ側で `packages.conf` を「すべてのユーザが書き込み可能 (writable by all)」とすることで、ルータが TFTP を通じてファイルを上書きできるようになる。
    - 選択肢 D の  
      > Make the package.conf file writable by all on the TFTP server.  
      が、この対処をそのまま表している。

- 他の選択肢がダメな理由
    - A: 実行権（executable）は TFTP の読み書きには不要であり、上書き失敗の原因ではない。
    - B: TFTP サービスが動作していなければ「タイムアウト」エラーになるのが一般的で、ここでの「Undefined error」とは異なる。
    - C: 新規ファイルを作る場合はフォルダの書き込み権限が必要だが、「既に存在するファイルの上書き」の場合、フォルダではなく**ファイル自身の書き込み権限**が問題になる。

***
### QUESTION 352 解説

- 問題文（英語）:
A newly installed spoke router is configured for DMVPN with the `ip mtu 1400` command. Which configuration allows the spoke to use fragmentation with the maximum negotiated TCP MTU over GRE?

A. `ip tcp payload-mtu 1360`  
   `crypto ipsec fragmentation after-encryption`  

B. `ip tcp adjust-mtu 1360`  
   `crypto ipsec fragmentation mtu-discovery`  

C. `ip tcp payload-mtu 1360`  
   `crypto ipsec fragmentation mtu-discovery`  

D. `ip tcp adjust-mtu 1360`  
   `crypto ipsec fragmentation after-encryption`  

- 問題文要約（日本語）:  
新しく導入した DMVPN スポークルータで `ip mtu 1400` を設定している。  
GRE＋IPsec のオーバーヘッドを考慮しつつ、**TCP の最大 MTU を調整し、フラグメンテーションを適切に行いたい**。  
どの組み合わせの設定が正しいか。

- 正解: **D**

- 解説（なぜ D が正しいか）
    - GRE＋IPsec ではヘッダ分だけ実効 MTU が下がるため、その分を見越して **TCP の MSS/MTU を調整**しないとフラグメントやブラックホールが起こる。[file:79]
    - `ip tcp adjust-mtu <value>` は、インターフェイスで通過する TCP セッションに対し、パス MTU を考慮した MSS 調整を行うための一般的なコマンドで、DMVPN 構成でよく使われる。
    - DMVPN で IPsec フラグメントを扱う際は、`crypto ipsec fragmentation after-encryption` により **暗号化後フラグメント**を許可しておくと、トンネル MTU を超えるパケットも正しく送出できる。
    - 選択肢 D の  
      - `ip tcp adjust-mtu 1360`（MSS 調整）  
      - `crypto ipsec fragmentation after-encryption`（暗号化後のフラグメント許可）  
      の組み合わせが、GRE 上で交渉された最大 TCP MTU を保ちつつ、必要時にはフラグメントを許容する正しい設定となる。

- 他の選択肢がダメな理由
    - A / C: `ip tcp payload-mtu` は有効な IOS コマンドではなく、実際の設定として不適切。
    - B: `crypto ipsec fragmentation mtu-discovery` という組み合わせは一般的な IPsec フラグメント制御のコマンドとして正しくなく、意図通りに動作しない。

***
### QUESTION 353 解説

- 問題文（英語）:
Refer to the exhibit. Which configuration allows Router1 to reach `2005:DEAD:BEEF:DB2::/64`? [file:79]

A. Router1  
   route-map Router1V6 permit 10  
    set ipv6 next-hop 2006:DEAD:BEEF:DB1::1  
    exit  
   !  
   router bgp 65001  
    address-family ipv6  
     network 2005:DEAD:BEEF:DB1::/64  
     neighbor 10.12.12.2 activate  
     neighbor 10.12.12.2 route-map Router1V6 out  
    exit-address-family  

B. Router2  
   route-map Router2V6 permit 10  
    set ipv6 next-hop 2006:DEAD:BEEF:DB8::2  
    exit  
   !  
   router bgp 65002  
    address-family ipv6  
     network 2005:DEAD:BEEF:DB2::/64  
     neighbor 10.12.12.2 activate  
     neighbor 10.12.12.2 route-map Router2V6 out  
    exit-address-family  

C. Router2  
   route-map Router2V6 permit 10  
    set ipv6 next-hop 2006:DEAD:BEEF:DB8::2  
    exit  
   !  
   router bgp 65002  
    address-family ipv6  
     network 2005:DEAD:BEEF:DB2::/64  
     neighbor 10.12.12.2 activate  
     neighbor 10.12.12.2 route-map Router out  
    exit-address-family  

D. Router1  
   route-map Router1V6 permit 10  
    set ipv6 next-hop 2006:DEAD:BEEF:DB8::2  
    exit  
   !  
   router bgp 65001  
    address-family ipv6  
     network 2005:DEAD:BEEF:DB1::/64  
     neighbor 10.12.12.2 activate  
     neighbor 10.12.12.2 route-map Router1V6 out  
    exit-address-family  

- 問題文要約（日本語）:  
図では、Router1 (AS 65001) と Router2 (AS 65002) が IPv4 アドレス 10.12.12.0/30 で eBGP ピアになっており、  
- Router1: `2005:DEAD:BEEF:DB1::/64`（自ルータの IPv6 プレフィックス）  
- Router2: `2005:DEAD:BEEF:DB2::/64`（R2 の Loopback0）  
を持っている。[file:79]  
Router1 から `2005:DEAD:BEEF:DB2::/64` へ到達できるようにするには、どの設定が必要か。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - `2005:DEAD:BEEF:DB2::/64` は Router2 のループバックに属するアドレスであり、このプレフィックスを BGP で広告するのは Router2 の役割。[file:79]
    - eBGP では、デフォルトで **next-hop は自ピア（広告元）** になるが、IPv6 では特に next-hop の扱いに注意が必要で、図の構成では R1 が受け取る経路の next-hop を正しく解決できていない前提になっている。
    - そのため、Router2 側で `route-map` を用いて、R1 へ送る IPv6 プレフィックス `2005:DEAD:BEEF:DB2::/64` の **next-hop を Router2 自身の到達可能な IPv6 アドレス（2006:DEAD:BEEF:DB8::2）に書き換えて** 広告する必要がある。
    - 選択肢 B は Router2 において  
      - `route-map Router2V6` で `set ipv6 next-hop 2006:DEAD:BEEF:DB8::2` を指定  
      - その route-map を `neighbor 10.12.12.2 route-map Router2V6 out` で eBGP 隣接に適用  
      - かつ `network 2005:DEAD:BEEF:DB2::/64` を広告  
      しており、R1 から見て「到達可能な next-hop つきで DB2 プレフィックスを受け取れる」状態となる。

- 他の選択肢がダメな理由
    - A / D: Router1 側で route-map を out 方向に適用しているが、R1 は `2005:DEAD:BEEF:DB2::/64` の起点ではないため、ここで next-hop をいじっても R2 から DB2 プレフィックスが正しく来なければ意味がない。
    - C: ルートマップ名として `Router2V6` を定義しているのに、隣接には `route-map Router out` を適用しており、名前が一致していないため設定ミスとなる。[file:79]

***
### QUESTION 354 解説

- 問題文（英語）:
What are the two goals of micro BFD sessions? (Choose two.) [file:79]

A. Each member link of a link aggregation group must run BFD  
B. Any member link on a link aggregation group must run BFD  
C. The high bandwidth member link of a link aggregation group must run BFD  
D. Continuity for each member link of a link aggregation group must be verified  
E. Run the BFD session with 3x3 ms hello timer.  

- 問題文要約（日本語）:  
ポートチャネル（LAG）で使われる **micro BFD セッション** の目的として正しいものを 2 つ選べ、という問題。

- 正解: **A, D**

- 解説（なぜ A と D が正しいか）
    - micro BFD は、ポートチャネル単位ではなく **各メンバリンク単位で BFD セッションを張る** 仕組みで、LACP/非LACP いずれの port-channel でも利用できる。[file:79]
    - 各メンバリンクごとに独立した BFD セッションを持ち、リンク故障を検出したらそのメンバを LAG の転送テーブルから外すことで、より高速かつきめ細かい障害検出を実現する。[file:79]
    - したがって目的は:
        - A: 「各メンバリンクごとに BFD セッションを動作させる」  
        - D: 「各メンバリンクの連続性（リンク状態）を検証する」  
      の 2 つが正しい。

- 他の選択肢がダメな理由
    - B: 「任意のメンバリンクだけで BFD を動かす」では、micro BFD の“全メンバ監視”という目的を満たさない。
    - C: 帯域の高いリンクだけを対象にする、という仕様はない。
    - E: 3×3ms という特定のタイマー値は問題文にはなく、micro BFD の一般的な“目的”ではなく単なる設定例に過ぎない。

***
### QUESTION 355 解説

- 問題文（英語）:
What action is performed for untagged outgoing labels in an MPLS router? [file:79]

A. Convert the incoming MPLS packet to an untagged packet and then do a FIB lookup.  
B. Convert the untagged packet to a labeled packet and forward it to the next router.  
C. Convert the incoming MPLS packet to an IP packet and forward it to the next router.  
D. Convert the incoming MPLS packet to an untagged packet and then do a RIB lookup.  

- 問題文要約（日本語）:  
MPLS ルータで「outgoing label が Untagged の場合」、受信した MPLS パケットに対してどのような処理が行われるか。

- 正解: **C**

- 解説（なぜ C が正しいか）
    - MPLS の LFIB で「outgoing label = Untagged」となっているエントリは、**MPLS ドメインから IP ドメインへの境界**で使われる。[file:79]
    - この場合、ルータは「受信した MPLS パケットからラベルスタックをすべて外し、純粋な IP パケットとして次ホップへ転送」する。  
      つまり「MPLS → IP 変換をして、そのままフォワーディング」する動作であり、選択肢 C の  
      > Convert the incoming MPLS packet to an IP packet and forward it to the next router.  
      に相当する。

- 他の選択肢がダメな理由
    - A/D: 「untagged packet」という表現と FIB/RIB ルックアップを分けて書いているが、Untagged の定義上、**MPLS を剥がして IP として転送する**ことがポイントで、余計な区別をしていない C が正しい。
    - B: ここで扱うのは「incoming MPLS → outgoing untagged」のケースであり、「untagged → labeled」ではない。

***
### QUESTION 356 解説

- 問題文（英語）:
What is a characteristic of an MPLS LSP tunnel? [file:79]

A. bidirectional tunnel  
B. unidirectional tunnel  
C. hop-by-hop tunnel  
D. secured bidirectional tunnel  

- 問題文要約（日本語）:  
MPLS の LSP (Label Switched Path) トンネルの特徴として正しいものを 1 つ選べ、という問題。

- 正解: **C**

- 解説（なぜ C が正しいか）
    - MPLS の LSP は、**各ホップのラベルスイッチルータ (LSR) が、LFIB に基づいて「ラベルを見て次のホップへ転送」する**ことで成り立っている。[file:79]
    - このため、パケットは IP のように宛先アドレスに基づくルックアップではなく、「各ホップごと (hop-by-hop) にラベルに基づき転送されるトンネル」のように振る舞う。  
      この性質から、LSP はしばしば「MPLS トンネル」とも呼ばれる。
    - 以上より、  
      > hop-by-hop tunnel  
      を選んだ C が正解となる。

- 他の選択肢がダメな理由
    - A/B/D: LSP 自体は **方向性**としては片方向のパス（unidirectional）だが、選択肢 C ほど正確に特徴を表していないうえ、D のような「secured」などの性質は LSP に必須ではない（IPsec など別機能が必要）。

***
### QUESTION 357 解説

- 問題文（英語）:
Refer to the exhibit. Routers R1, R2, R3, and R4 use EIGRP. However, traffic always prefers R1–R5 backup links in non‑failure scenarios. Which configuration resolves the issue? [file:79]

A.  
   R1(config)# interface GigabitEthernet0/0/0  
   R1(config-if)# bandwidth 10000000  

B.  
   R1(config)# no ip route 10.40.10.0 255.255.255.252 10.10.20.2  
   R1(config)# ip route 10.40.10.0 255.255.255.252 10.10.20.2 115  

C.  
   R1(config)# no ip route 10.40.10.0 255.255.255.252 10.10.20.2  
   R1(config)# ip route 0.0.0.0 0.0.0.0 10.10.10.2  

D.  
   R1(config)# interface GigabitEthernet0/0/0  
   R1(config-if)# bandwidth 10000  

- 問題文要約（日本語）:  
R1〜R4 は EIGRP を実行しているが、本来は R1–R2–R3–R4 側がメイン、R1–R5 側がバックアップであるべきところ、障害がない平常時でもトラフィックが R1–R5 側のバックアップリンクを優先してしまっている。  
この挙動を修正して、R5 経由は「バックアップ専用」にするには、どの設定を R1 に入れるべきか。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - ルーティングテーブル上、R1 にはネットワーク `10.40.10.0/30`（R4-R5 セグメント）向けの **スタティックルート** が  
      `ip route 10.40.10.0 255.255.255.252 10.10.20.2`  
      のように設定されており、AD はデフォルト 1 となる。[file:79]
    - 一方、EIGRP から学習する同一宛先の経路は AD 90（外部なら 170）のため、**AD 1 のスタティックルートが常に優先**され、結果として平常時でも R5 経由が使われてしまう。
    - このスタティックルートを「バックアップ用」にするには、EIGRP（90）や OSPF（110）より **大きな AD** を設定して「フローティングスタティック」にすればよい。  
      例として AD 115 を指定すると、  
      - EIGRP/OSPF 経路があるとき: それらが優先され、R5 へのスタティックはバックアップに退く  
      - EIGRP/OSPF 経路が落ちたとき: AD 115 のスタティックが有効になり、R5 経由にフェイルオーバー  
      という動作になる。
    - 選択肢 B は  
      - 既存スタティックを削除し (`no ip route ...`)  
      - 同じ宛先・ネクストホップで AD 115 を設定し直す  
      構成になっており、まさに意図通りの「フローティングスタティック」化を行っている。

- 他の選択肢がダメな理由
    - A / D: `bandwidth` コマンドでインターフェイス帯域を変えても、ここで優先されているのは「スタティックルート vs EIGRP」の AD 差であり、EIGRP のメトリックではないため根本原因を解決しない。
    - C: もとのスタティックを消してデフォルトルートを作っているが、バックアップリンクとして `10.40.10.0/30` 向けの明示的な経路を残していないため、設計意図と異なる。

***
### QUESTION 358 解説

- 問題文（英語）:
Refer to the exhibit. An engineer cannot determine the time of the problem on R1 due to a mismatch between the router local clock and logs. Which command synchronizes the time between new log entries and the local clock on R1? [file:79]

A. service timestamps log datetime msec show-timezone  
B. service timestamps debug datetime localtime msec  
C. service timestamps debug datetime msec show-timezone  
D. service timestamps log datetime localtime msec  

- 問題文要約（日本語）:  
R1 のローカルクロックの時刻と、ログに表示される時刻が一致しておらず、いつ問題が起きたのか判断できない。  
exhibit に表示されているのは「debug コマンドの出力」であり、そのタイムスタンプをローカルクロックに合わせたい。  
どのコマンドを入れれば、以後のログの時刻とローカルクロックが同期するか。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - 画面には syslog レベル付きの通常ログではなく、`debug ...` の出力だけが表示されているため、「調整したいのは debug メッセージのタイムスタンプ」である。[file:79]
    - `service timestamps debug datetime localtime msec` は、  
        - debug メッセージに対して  
        - ルータのローカルタイム（タイムゾーン・夏時間などを反映）を使い  
        - ミリ秒単位の datetime 形式で  
      タイムスタンプを付与するコマンド。
    - これにより、今後出力される debug ログの時刻と、`show clock` などで確認できるローカルクロックの表示が一致するようになるため、問題の発生時間を正確に突き合わせられる。

- 他の選択肢がダメな理由
    - A / D: `service timestamps log ...` は **syslog メッセージ**用であり、debug 出力には効かない。
    - C: debug 用ではあるが `localtime` を付けていないため、タイムゾーンを考慮したローカル時間と完全には一致しない。

***
### QUESTION 359 解説

- 問題文（英語）:
Refer to the exhibit. Which action installs routes into the routing table of R1 and R3? [file:79]

A. BGP Synchronization has been enabled. Disable synchronization.  
B. BGP weight is set to 0. Configure a higher weight for these routes.  
C. IGP does not have a route to R4. Rectify route reachability.  
D. BGP has not been redistributed into the IGP. Redistribute BGP.  

- 問題文要約（日本語）:  
R1 と R3 では BGP の経路は受信できているが、その経路がルーティングテーブルにインストールされていない。  
この状況を修正し、R1/R3 のルーティングテーブルに BGP ルートを載せるには、どの対応が必要か。

- 正解: **C**

- 解説（なぜ C が正しいか）
    - exhibit では、R3 の BGP テーブルにはプレフィックスが見えているものの、その next-hop が **10.11.11.1（R4 側アドレス）** になっている。[file:79]
    - ところが、R3 のルーティングテーブルには 10.11.11.1 への到達経路が存在せず、**next-hop 未到達のため BGP ルートが RIB にインストールされない** 状態になっている。
    - BGP ルートが RIB に入る条件の 1 つは「next-hop が IGP などで到達可能であること」なので、まず IGP などで R3 から R4（10.11.11.1）までの経路を学習させる必要がある。
    - よって、選択肢 C の  
      > IGP does not have a route to R4. Rectify route reachability.  
      を行い、IGP に R4 へのルートを追加すれば、BGP ルートが R1/R3 のルーティングテーブルにインストールされる。

- 他の選択肢がダメな理由
    - A: 近年の IOS では BGP synchronization はデフォルト無効であり、このケースで「BGP テーブルにはあるが RIB に載らない」主因ではない。
    - B: weight は「複数 BGP 経路間の優先度」を決める属性であり、ルートが RIB に入る／入らない条件とは無関係。
    - D: BGP を IGP に再配送していないことが問題なのではなく、「BGP next-hop の到達性」が欠けていることが原因。

***
### QUESTION 360 解説

- 問題文（英語）:
Refer to the exhibit. Router R2 should be learning the route for 10.123.187.0/24 via EIGRP. Which action resolves the issue without introducing more issues? [file:79]

A. Use distribute-list to filter the external routes in OSPF.  
B. Redistribute the route in EIGRP with metric, delay, and reliability.  
C. Remove route redistribution in R2 for this route in OSPF.  
D. Use distribute-list to modify the route as an internal EIGRP route.  

- 問題文要約（日本語）:  
R2 はネットワーク 10.123.187.0/24 を **EIGRP 経由で学習してほしい** が、実際には OSPF の経路がルーティングテーブルに入っており、EIGRP ルートはトポロジテーブルにだけ存在して RIB にインストールされていない。  
他に悪影響を出さずに、この経路を EIGRP から学習させるにはどうすべきか。

- 状況整理:
    - R2 の **ルーティングテーブル** には 10.123.187.0/24 の **OSPF external ルート** が AD 110 でインストールされている。  
    - **EIGRP トポロジテーブル** には同じプレフィックスがあるが、`0 successors, FD is Inaccessible` となっており、ルーティングテーブルには採用されていない。[file:79]
    - EIGRP external の AD は 170 のため、OSPF external (110) に負けているのが原因と考えられる。

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 求めたい状態は「R2 では 10.123.187.0/24 を EIGRP から学習し、OSPF 版はローカル RIB に入れないこと」。  
    - ルーティングテーブルに入る経路は「同一プレフィックスに対して AD が最も小さいもの」なので、OSPF external を **R2 の RIB に入らないようにする**必要がある。
    - ここで有効なのが、**OSPF プロセス側で distribute-list を使って特定ルートをフィルタし、ローカルルータの RIB へのインストールのみ止める**方法。  
      distribute-list は LSA の生成・受信自体には影響を与えず、そのルータのルーティングテーブルへの反映だけを制御できる。[file:79]
    - 選択肢 A の  
      > Use distribute-list to filter the external routes in OSPF.  
      により、R2 は OSPF 外部ルート 10.123.187.0/24 を RIB に持たなくなり、代わりに EIGRP external（AD 170）がルーティングテーブルへインストールされる。

- 他の選択肢がダメな理由
    - B: EIGRP のメトリック値を変えても、AD 170 という事実は変わらないため、OSPF external (AD 110) に勝てない。
    - C: 「R2 での OSPF への再配送をやめる」だけでは、すでに他ルータから来ている OSPF external 10.123.187.0/24 を止められない。さらに他プレフィックスへの影響も読みにくい。
    - D: distribute-list で「EIGRP external を internal に書き換える」ような動作はできず、AD の違いという根本問題も解決しない。

***
### QUESTION 361 解説

- 問題文（英語）:
While examining log messages from a router, an administrator sees this error message:  

`%DUAL-5-NBRCHANGE: IP-EIGRP(0) 1: Neighbor 10.4.1.5 (Ethernet1/0) is down: K-value mismatch`  

Both routers are using default values. Which action resolves this issue?

A. Configure metric weights between the peers to calculate the valid K-value.  
B. Resolve metric differences between the peers to calculate the valid K-value.  
C. Upgrade the peer device IOS to support the goodbye message that establishes adjacency.  
D. Match authentication parameters to support the goodbye message that establishes adjacency.  

- 問題文要約（日本語）:  
ログに「Neighbor is down: K-value mismatch」と出ているが、両ルータとも EIGRP の K 値はデフォルトのままのはず。  
この場合、どの対応を行えば問題が解決するか。

- 正解: **C**

- 解説（なぜ C が正しいか）
    - 通常「K-value mismatch」が出るのは、EIGRP メトリック計算に使う K 値 (K1〜K5) が両ルータで異なるとき。[file:79]
    - しかし **両方デフォルト** にもかかわらずこのメッセージが出る場合、原因としてよくあるのが **EIGRP graceful shutdown（Goodbye メッセージ）機能**。  
      この機能では、ルータがシャットダウンする際に「K 値を 255 にセットした Hello」を送り、隣接ルータに「意図的に K‑value mismatch を起こして adjacency を落とす」ことで、きれいに終了を知らせる。[file:79]
    - 新しい IOS では受信側もこの Goodbye メッセージを理解し、  
      `... is down: Interface Goodbye received`  
      のようなログが出るが、**古い IOS ではこれを単なる K 値不一致と誤解**し、  
      `K-value mismatch`  
      として隣接を落としてしまう。
    - したがって、問題を根本的に解消するには、古い側のルータをアップグレードして Goodbye メッセージをサポートさせるのが正解となる。  
      これを述べているのが C の  
      > Upgrade the peer device IOS to support the goodbye message that establishes adjacency.  

- 他の選択肢がダメな理由
    - A / B: 実際には K 値は両者ともデフォルトで一致しており、メトリック設定をいじる必要はない。  
    - D: 問題は認証パラメータではなく、Goodbye メッセージを理解できない古い IOS に起因している。

***
### QUESTION 362 解説

- 問題文（英語）:
Which IPv6 security feature blocks all traffic from an IPv6 host when initially connecting to a switch port except for traffic to gain an IPv6 address and source IPv6 neighbors? [file:79]

A. IPv6 RA Guard  
B. IPv6 Source Guard  
C. IPv6 Destination Guard  
D. IPv6 DHCP Guard  

- 問題文要約（日本語）:  
ホストがスイッチポートに最初に接続したとき、IPv6 アドレス取得や近隣情報取得に必要なトラフィック（DHCPv6 / NDP）以外の IPv6 トラフィックをすべてブロックする機能はどれか。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - **IPv6 Source Guard** は、スイッチポートに流入する IPv6 パケットを、IPv6 スヌーピング（DHCPv6・NDP）で学習した **IPv6 バインディングテーブル** と照合してチェックする「入力方向のフィルタ機能」。[file:79]
    - ホストが接続した直後は、まだその MAC / IP / ポート情報がバインディングテーブルに登録されていないため、Source Guard は  
      - DHCPv6 や NDP など、バインディングテーブル作成に必要なパケットのみ許可し  
      - それ以外の IPv6 トラフィックはすべてブロックする。  
      これが設問にある動作と一致する。

- 他の選択肢がダメな理由
    - A: IPv6 RA Guard は不正な Router Advertisement をブロックする機能で、ホストからの全トラフィックを初期に遮断するものではない。
    - C: IPv6 Destination Guard は「宛先 IPv6 アドレスがバインディングテーブルに存在しない場合の NDP 解決をブロック」する機能で、送信元側の初期トラフィック制御とは異なる。
    - D: IPv6 DHCP Guard は不正な DHCPv6 サーバからの応答をブロックする機能であり、ホストからの一般トラフィックを全面ブロックするものではない。

***
### QUESTION 363 解説

- 問題文（英語）:
ip prefix-list DMZ-STATIC seq 5 permit 10.1.1.0/24  
!  
route-map DMZ permit 10  
 match ip address prefix-list DMZ-STATIC  
!  
router ospf 1  
 network 0.0.0.0 0.0.0.0 area 0  
 redistribute static route-map DMZ  
!  
ip route 10.1.1.0 255.255.255.0 10.20.20.1  

Refer to the exhibit. The static route is not present in the routing table of an adjacent OSPF neighbor router. Which action resolves the issue? [file:79]

A. Configure the subnets keyword in the redistribution command.  
B. Configure a permit 20 statement to the route map to redistribute the static route.  
C. Configure the next-hop interface at the end of the static route for it to get redistributed.  
D. Configure the next hop of 10.20.20.1 in the prefix list DMZ-STATIC.  

- 問題文要約（日本語）:  
上記のように、プレフィックスリストとルートマップを使って静的ルート `10.1.1.0/24` を OSPF に再配布しているが、隣接の OSPF ルータのルーティングテーブルにこの経路が現れない。  
どの設定を追加すれば、この静的ルートが OSPF へ正常に再配布されるか。

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 静的ルート:  
      `ip route 10.1.1.0 255.255.255.0 10.20.20.1` はクラスフル的には 10.0.0.0/8 の「サブネット」にあたる。  
    - 古い IOS では `redistribute static` だけだと「クラスフルネットワークのみ」がデフォルトで再配布され、**クラスフル範囲内のサブネット（/24 など）は除外される**。[file:79]
    - これを明示的に含めるには、OSPF の `redistribute` に `subnets` を付けて  
      `redistribute static subnets route-map DMZ`  
      のように指定する必要がある。
    - したがって、選択肢 A の  
      > Configure the subnets keyword in the redistribution command.  
      が、静的ルート `10.1.1.0/24` を OSPF に広告させる正しい修正となる。

- 他の選択肢がダメな理由
    - B: すでに `route-map DMZ permit 10` で対象プレフィックスを許可しており、追加の seq 20 は不要。
    - C: 再配布の可否は「スタティックであること」と「ルートマップでマッチすること」に依存し、次ホップが IP かインターフェイスかは関係ない。
    - D: プレフィックスリストは宛先プレフィックスをマッチするものであり、ネクストホップアドレス 10.20.20.1 を書いても意味がない。

***
### QUESTION 364 解説

- 問題文（英語）:
R1#
 router ospf 1
  redistribute rip subnets
  network 132.108.1.0 0.0.0.255 area 2
  network 132.108.2.0 0.0.0.255 area 2
  distribute-list 1 out
 !
 access-list 1 permit 132.108.4.0 0.0.0.255

Refer to the exhibit. The R1 OSPF neighbor is not receiving Type 5 external LSAs for 132.108.2.0/24 and 132.108.3.0/24 networks. Which configuration command resolves the issue? [file:79]

A. access-list 1 permit 132.108.0.0 0.0.3.255  
B. access-list 1 permit 132.108.0.0 0.0.1.255  
C. access-list 1 permit 132.108.4.0 0.0.3.255  
D. access-list 1 permit 132.108.2.0 0.0.0.255  

- 問題文要約（日本語）:  
R1 は RIP を OSPF に `redistribute rip subnets` で再配布しており、`distribute-list 1 out` によって外部ルートの再配布を ACL 1 で制御している。  
しかし OSPF の隣接ルータには、RIP ネットワーク 132.108.2.0/24 と 132.108.3.0/24 の Type 5 LSA が届いていない。  
どの ACL 設定に変えれば、この 2 つのネットワークも再配布されるか。

- 正解: **A**

- 解説（なぜ A が正しいか）
    - `distribute-list 1 out` は「OSPF に再配布する外部ルートを、ACL 1 でフィルタする」ために使われる。[file:79]
    - 現在の ACL は  
      `access-list 1 permit 132.108.4.0 0.0.0.255`  
      だけなので、132.108.4.0/24 以外の RIP ルート（132.108.2.0/24 と 132.108.3.0/24）は **再配布時に deny されている**。
    - 132.108.0.0〜132.108.3.255 をまとめて許可すれば、/24 サブネット 4 つ
        - 132.108.0.0/24  
        - 132.108.1.0/24  
        - 132.108.2.0/24  
        - 132.108.3.0/24  
      をすべてマッチできる。
    - それを実現するワイルドカードが  
      `132.108.0.0 0.0.3.255`  
      であり、選択肢 A がこれを指定している。

- 他の選択肢がダメな理由
    - B: `0.0.1.255` では 132.108.0.0/24 と 132.108.1.0/24 しか含められず、問題の 2.0/24 と 3.0/24 が依然として除外される。
    - C: 132.108.4.0〜7.255 を許可するだけで、やはり 2.0/24 と 3.0/24 は含まれない。
    - D: 132.108.2.0/24 だけを許可しており、132.108.3.0/24 が依然として再配布されない。

***
### QUESTION 365 解説

- 問題文（英語）:
Which two functions are the functions of LDP? (Choose two.) [file:79]

A. uses TCP for targeted LDP session  
B. uses P2P VPWS services  
C. uses pseudowire signaling  
D. uses MP2MP VPLS services  
E. used for MPLS-TE Fast Reroute  

- 問題文要約（日本語）:  
LDP (Label Distribution Protocol) の機能として正しいものを 2 つ選べ。

- 正解: **A, C**

- 解説（なぜ A と C が正しいか）
    - LDP は MPLS ネットワークで **ラベルを配布するためのコントロールプレーン・プロトコル** で、TCP/UDP 646 を使用する。[file:79]
        - 通常の隣接 LSR 間の LDP セッションに加え、ルータ同士が IP で離れている場合に **targeted LDP セッション** を張ることができ、このときも TCP を使用する。  
          → A「uses TCP for targeted LDP session」は正しい。
    - また、**MPLS Layer 2 VPN（pseudowire）** では、VC ラベルや PW ステータスをやり取りする「pseudowire signaling」に LDP (tLDP) が使われる。  
      → C「uses pseudowire signaling」も正しい。[file:79]

- 他の選択肢がダメな理由
    - B: P2P VPWS（point-to-point L2VPN）というサービス自体は LDP ベースの pseudowire で実現されることが多いが、「LDP の機能」というよりサービス種別であり、設問の文脈では C の方が正確。
    - D: MP2MP VPLS など VPLS の制御には BGP や LDP など複数方式があるが、「LDP の主な機能」として問われているのは label/pseudowire signaling で、C が代表。
    - E: MPLS-TE Fast Reroute のシグナリングには RSVP-TE が使われ、LDP は直接関与しない。

***
### QUESTION 366 解説

- 問題文（英語）:
A customer requested a GRE tunnel through the provider network between two customer sites using loopback to hide internal networks. Which configuration on R2 establishes the tunnel with R1? [file:79]

A.  
 R2(config)# interface Tunnel1  
 R2(config-if)# ip address 172.20.1.2 255.255.255.0  
 R2(config-if)# ip mtu 1500  
 R2(config-if)# ip tcp adjust-mss 1360  
 R2(config-if)# tunnel source 192.168.20.1  
 R2(config-if)# tunnel destination 10.10.1.1  

B.  
 R2(config)# interface Tunnel1  
 R2(config-if)# ip address 172.20.1.2 255.255.255.0  
 R2(config-if)# ip mtu 1400  
 R2(config-if)# ip tcp adjust-mss 1360  
 R2(config-if)# tunnel source 192.168.20.1  
 R2(config-if)# tunnel destination 192.168.10.1  

C.  
 R2(config)# interface Tunnel1  
 R2(config-if)# ip address 172.20.1.2 255.255.255.0  
 R2(config-if)# ip mtu 1400  
 R2(config-if)# ip tcp adjust-mss 1360  
 R2(config-if)# tunnel source 10.10.2.2  
 R2(config-if)# tunnel destination 10.10.1.1  

D.  
 R2(config)# interface Tunnel1  
 R2(config-if)# ip address 172.20.1.2 255.255.255.0  
 R2(config-if)# ip mtu 1500  
 R2(config-if)# ip tcp adjust-mss 1360  
 R2(config-if)# tunnel source 10.10.2.2  
 R2(config-if)# tunnel destination 10.10.1.1  

- 問題文要約（日本語）:  
プロバイダ網をまたいで、2 拠点間に GRE トンネルを張りたい。内部ネットワークを隠すために loopback アドレスをトンネルの終端に使う設計になっている。  
R1 側はループバック `192.168.10.1` を、R2 側は `192.168.20.1` をトンネルの source/destination として使う想定。  
R1 とトンネルを張るために、R2 ではどの設定を入れるべきか。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - GRE トンネルは  
      - `tunnel source` と `tunnel destination` に **到達可能な IP アドレス**（物理 IF や loopback）を指定する必要がある。
    - 要件では「loopback を使って内部ネットワークを隠す」とあるため、トンネル終端は  
      - R1: `192.168.10.1`（Loopback）  
      - R2: `192.168.20.1`（Loopback）  
      の組み合わせが自然となる。[file:79]
    - 選択肢を見比べると:
        - A: destination が 10.10.1.1（おそらく物理アドレス）で、要件の「loopback 同士で張る」に反する。
        - C / D: source/destination とも 10.10.x.x で、これも loopback を使っていない。
        - B: `tunnel source 192.168.20.1` と `tunnel destination 192.168.10.1` の組み合わせで、要件通り loopback を使用。
    - さらに、GRE＋IPsec などを想定すると、カプセル化のオーバーヘッドを考慮して **トンネル側 MTU を 1400** にしておくのが一般的で、`ip tcp adjust-mss 1360` との組み合わせも妥当。[file:79]
    - 以上から、B が唯一、設計要件と運用上のベストプラクティスの両方を満たす構成になる。

- 他の選択肢がダメな理由
    - A: destination が 10.10.1.1 で、R1 の loopback を使っていないため要件不一致。
    - C / D: 双方とも物理 IP（10.10.2.2, 10.10.1.1）を使っており、「loopback を使って内部アドレスを隠す」という要件を満たさない。さらに D は MTU 1500 のままで、GRE/IPsec のオーバーヘッドを考慮していない。

***
### QUESTION 367 解説

- 問題文（英語）:
In a DMVPN network, the Spoke1 user observed that the voice traffic is coming to Spoke2 users via the hub router. Which command is required on both spoke routers to communicate directly to one another? [file:79]

A. ip nhrp shortcut  
B. ip nhrp redirect  
C. ip nhrp nhs multicast  
D. ip nhrp map dynamic  

- 問題文要約（日本語）:  
DMVPN 環境で、本来は Spoke1 ↔ Spoke2 間をダイレクトに通信させたいのに、音声トラフィックが常に Hub 経由になっている。  
スポーク同士が直接トンネルを張って通信できるようにするには、スポーク両方にどのコマンドを入れる必要があるか。

- 正解: **A**

- 解説（なぜ A が正しいか）
    - DMVPN Phase 3 では、次の 2 つのコマンドを組み合わせて **spoke-to-spoke ダイレクトトンネル**を実現する。[file:79]
        - Hub: `ip nhrp redirect`  
          → ハブが「よりよいネクストホップ（別のスポーク）」を示す NHRP Redirect メッセージをスポークに送れるようにする。
        - Spoke: `ip nhrp shortcut`  
          → スポークが、その Redirect 情報を基に「宛先スポークへのショートカットルート＋ダイレクトトンネル」を動的に作成できるようにする。
    - 問題文では「どのコマンドを両スポークに必要か」と聞いているため、**Spoke 側に入れるべき `ip nhrp shortcut`** が正解となる。

- 他の選択肢がダメな理由
    - B: `ip nhrp redirect` は **ハブ側** に入れるコマンドであり、両スポークには不要。
    - C: `ip nhrp nhs multicast`（正しくは `ip nhrp map multicast` など）はマルチキャストの NHRP マッピング関連で、spoke-to-spoke ショートカットとは別の目的。
    - D: `ip nhrp map dynamic` は動的マッピング関連だが、Phase 3 のショートカットルートを有効にする本質的なコマンドではない。

***
### QUESTION 368 解説

- 問題文（英語）:
Refer to the exhibit. An engineer configured NetFlow on R1, but the flows do not reach the NMS server from R1. Which configuration resolves this issue? [file:79]

A.  
 R1(config)# interface Ethernet0/1  
 R1(config-if)# ip flow monitor FlowMonitor1 input  
 R1(config-if)# ip flow monitor FlowMonitor1 output  

B.  
 R1(config)# interface Ethernet0/0  
 R1(config-if)# ip flow monitor FlowMonitor1 input  
 R1(config-if)# ip flow monitor FlowMonitor1 output  

C.  
 R1(config)# flow monitor FlowMonitor1  
 R1(config-flow-monitor)# destination 10.66.66.66  

D.  
 R1(config)# flow exporter FlowExporter1  
 R1(config-flow-exporter)# destination 10.66.66.66  

- 問題文要約（日本語）:  
R1 に NetFlow（Flexible NetFlow）が設定されているが、NMS サーバにフローが届いていない。  
exhibit の `show flow exporter` の出力を見ると、エクスポータの送信先 IP が誤っている。  
どの設定を追加・修正すれば、R1 から NMS サーバ 10.66.66.66 にフローが届くようになるか。

- 正解: **D**

- 解説（なぜ D が正しいか）
    - Flexible NetFlow では  
        - `flow record` で項目定義  
        - `flow monitor` で record＋exporter を関連付け  
        - `flow exporter` で **NMS サーバの宛先 IP/ポート** を設定  
      する。[file:79]
    - exhibit では `show flow exporter` の出力に、destination が **10.60.66.66** のように誤って設定されているため、パケットが正しい NMS (10.66.66.66) に届いていない。[file:79]
    - 直すべきは exporter の宛先 IPであり、正しいコマンドは  
      `flow exporter FlowExporter1` のサブモードで  
      `destination 10.66.66.66`  
      を設定し直すこと。
    - 選択肢 D はその修正を明示しているため、フローが正しい NMS サーバに送出されるようになり、問題が解決する。

- 他の選択肢がダメな理由
    - A / B: すでにフローモニタはインターフェイスに適用されており、問題は「どの IF に適用しているか」ではなく「exporter の宛先 IP の誤り」。
    - C: `destination` を設定できるのは `flow exporter` であり、`flow monitor` コンテキストではない（構文として不正）。

***
### QUESTION 369 解説

- 問題文（英語）:
What is an MPLS LDP targeted session?

A. LDP session established by exchanging multicast hello packets  
B. label distribution session between non-directly connected neighbors  
C. session between neighbors that are connected no more than one hop away  
D. LDP session established between LSRs by exchanging TCP hello packets  

- 問題文要約（日本語）:  
MPLS の LDP「ターゲットセッション (targeted LDP session)」とはどのようなものか。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - 通常の LDP セッションは、直結インターフェイス上で **マルチキャスト Hello（UDP 646）** をやり取りして自動検出した隣接 LSR 間で確立される。
    - 一方 **targeted LDP セッション** は、直結していないルータ同士でラベルをやり取りしたいときに、特定のリモート IP を「ターゲット」として指定して確立する **非直結ノード間の LDP セッション** である。[file:79]
    - よって  
      > label distribution session between non-directly connected neighbors  
      を述べる B が正解。

- 他の選択肢がダメな理由
    - A: これは通常の「リンクローカル LDP セッション」の説明で、targeted ではない。
    - C: 「1 ホップ以内」に限定されており、「非直結でもよい」という targeted の定義と矛盾する。
    - D: LDP の制御セッション自体は TCP 646 を使うが、「TCP Hello を交換して確立」という表現は不正確で、targeted セッションの特徴を表していない。

***
### QUESTION 370 解説

- 問題文（英語）:
What is the purpose of an OSPF sham-link?

A. to correct OSPF backdoor routing when OSPF is used as the PE-PE connection protocol in an MPLS VPN network  
B. to correct OSPF backdoor routing when OSPF is used as the PE-CE connection protocol in an MPLS VPN network  
C. to allow inter-area routing when OSPF is used as the PE-CE connection protocol in a MPLS VPN network  
D. to allow intra-area routing when OSPF is used as the PE-CE connection protocol in an MPLS VPN network  

- 問題文要約（日本語）:  
MPLS L3 VPN 環境で OSPF を使うとき、「OSPF sham-link」の目的は何か。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - MPLS L3 VPN で PE–CE 間に OSPF を使う場合、同一サイト間に  
        - MPLS VPN 経路（PE 経由）  
        - 直接のバックドアリンク（CE 間など）  
      の両方が存在すると、OSPF のコスト計算の都合で **バックドアリンクの方が優先され、MPLS VPN を迂回する** ことがある。
    - **OSPF sham-link** は、PE 間を仮想的に同一 OSPF エリア内の「プライベートリンク」として見せることで、VPN 経路のコストをバックドア経路より有利にし、**“backdoor 逆転現象”を修正するための仕組み**。[file:79]
    - これはあくまで **PE–CE 間で OSPF を使っている MPLS VPN** における話なので、選択肢 B が定義通りとなる。

- 他の選択肢がダメな理由
    - A: sham-link は PE–CE 間 OSPF 用であり、PE–PE 間プロトコル（通常は BGP/IGP）を対象としない。
    - C / D: inter-area / intra-area ルーティングを可能にする一般機能ではなく、「backdoor を修正して、VPN 経路を同一エリア内の優先パスに見せる」ことが目的。

***
### QUESTION 371 解説

- 問題文（英語）:
The network administrator configured CoPP so that all routing protocol traffic toward the router CPU is limited to 1 Mbps. All traffic that exceeds this limit must be dropped. The router is running BGP and OSPF. Management traffic for Telnet and SSH must be limited to 500 kbps.


No traffic is filtering through CoPP, which is resulting in high CPU utilization. Which configuration resolves the issue? [file:79]

A. （ACL を分割して CM-MGMT に ACL101 を使う）  
B. （service-policy を input に適用）  
C. A の修正＋B の修正を両方反映  
D. ACL100 に Telnet/SSH/OSPF すべてを追加  

- 問題文要約（日本語）:  
CoPP で  
- ルーティングプロトコル（BGP 179, OSPF）を 1 Mbps  
- 管理トラフィック（Telnet/SSH）を 500 kbps  
に制限したいが、現在どのトラフィックも CoPP で制御されておらず CPU が高負荷になっている。  
どの構成変更で問題が解決するか。

- 正解: **C**

- 解説（なぜ C が正しいか）

  現状の問題は 2 つある。[file:79]

  1. **同じ ACL100 を 2 つの class-map でマッチしている**  
     - CM-ROUTING も CM-MGMT も ACL100 を参照しているため、ルーティングと管理トラフィックを区別できない。  
     - Telnet/SSH を別 ACL（例: 101）に分け、CM-MGMT では ACL101 をマッチさせる必要がある。
  
  2. **CoPP ポリシーを control-plane の output に適用している**  
     - CoPP は「CPU へ入ってくるトラフィック」を制御するので、`control-plane service-policy input PM-COPP` と **input 方向**に適用しなければならない。

  選択肢 C はこの 2 点を両方まとめて修正している。

  - ACL を分割:
    ```text
    no access-list 100
    access-list 100 permit tcp any any eq 179
    access-list 100 permit ospf any any
    access-list 101 permit tcp any any range 22 23
    !
    class-map CM-MGMT
     no match access-group 100
     match access-group 101
    ```
  - CoPP を input に適用:
    ```text
    control-plane
     no service-policy output PM-COPP
     service-policy input PM-COPP
    ```

  これにより、  
  - BGP/OSPF は CM-ROUTING で 1 Mbps にポリシング  
  - Telnet/SSH は CM-MGMT で 500 kbps にポリシング  
  - いずれも CPU に到達する **入力トラフィック** として正しく制御されるようになる。

- 他の選択肢がダメな理由
  - A: ACL 分割だけを修正しており、CoPP を依然として `output` に適用しているため CPU へ入るトラフィックは制御されない。
  - B: ポリシーを `input` に変えるだけで、ルーティングと管理トラフィックを同じ ACL100 でマッチしたままなのでクラスが分離されず、意図通りの帯域制御にならない。
  - D: 単に ACL100 に条件を追加するだけで、2 つの class-map が同じ ACL を共有するという問題が解消されない。

***
### QUESTION 372 解説

- 問題文（英語）:
Refer to the exhibit. A network administrator is troubleshooting an OSPF adjacency issue by going through the console logs in the router, but due to an overwhelming log message stream, it is impossible to capture the problem. Which two commands reduce console log messages to relevant OSPF neighbor problem details so that the issue can be resolved? (Choose two.) [file:79]

A. debug condition ip  
B. debug condition all  
C. debug condition ospf neighbor  
D. debug condition interface  
E. debug condition session-id ADJCHG  

- 問題文要約（日本語）:  
OSPF 隣接問題を debug で調べたいが、コンソールに大量のログが流れてしまい、肝心の情報が追えない。  
`debug condition` を使って、OSPF 隣接に関係するメッセージだけに絞り込みたい。どの 2 つのコマンドを使うべきか。

- 正解: **A, D**

- 解説（なぜ A と D が正しいか）
    - `debug condition` で指定できる項目は限られており、実際に IOS で `debug condition ?` とすると次のような候補が出る（抜粋）。[file:79]
      - `interface`  
      - `ip`  
      - `mac-address`  
      - `vrf`  
      など。
    - 選択肢の中で **実在する条件指定** は  
      - A: `debug condition ip`（特定 IP に関係する debug に絞る）  
      - D: `debug condition interface`（特定インターフェイス関連の debug に絞る）  
      の 2 つのみ。
    - 例えば  
      ```text
      debug condition interface Gi0/0
      debug condition ip <neighbor-IP>
      debug ip ospf adj
      ```  
      のように組み合わせれば、「特定インターフェイス・特定ネイバー IP に関係する OSPF debug」だけが出力され、膨大なログから隣接問題に関わるメッセージだけを見られるようになる。

- 他の選択肢がダメな理由
    - B: `debug condition all` という条件は存在せず、実際の IOS の候補にも出てこない。
    - C: `debug condition ospf neighbor` という条件も存在しない（OSPF neighbor は debug コマンドの種類であって、condition ではない）。
    - E: `debug condition session-id ADJCHG` という構文もサポートされていない。

***
### QUESTION 373 解説

- 問題文（英語）:
An engineer noticed that the router log messages do not have any information about when the event occurred. Which action should the engineer take when enabling service time stamps to improve the logging functionality at a granular level?

A. Configure the debug uptime option.  
B. Configure the msec option.  
C. Configure the timezone option.  
D. Configure the log uptime option.  

- 問題文要約（日本語）:  
ログに「いつ起きたか」の情報が出ていない。`service timestamps` を有効化する際に、より細かい（粒度の細かい）時刻情報をログに付けたい。どのオプションを付けるべきか。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - `service timestamps log|debug datetime` に対して **`msec` オプション** を付けると、ログのタイムスタンプに「ミリ秒」まで表示されるようになり、非常に細かい時間解像度でイベントを追跡できる。[file:79]
      例:  
      `service timestamps log datetime localtime msec`  
    - これにより、短時間に多発するイベントや、複数ログの前後関係をより正確に確認できるため、「granular level」での解析が可能になる。

- 他の選択肢がダメな理由
    - A/D: `log uptime` や `debug uptime` は「装置起動からの経過時間」を表示するオプションで、粒度を上げる主目的には合わない。
    - C: `timezone` を設定するとローカルタイムゾーンが反映されるが、ミリ秒レベルの“細かさ”を追加するわけではない。

***
### QUESTION 374 解説

- 問題文（英語）:
interface GigabitEthernet2  
 no ip address  
 ip helper-address 192.168.255.3  
 no shutdown  
!  
interface GigabitEthernet2.10  
 encapsulation dot1Q 210  
 ip address 192.168.210.1 255.255.255.0  
 ip ospf 1 area 0  
 no shutdown  

Refer to the exhibit. With the partial configuration of a router-on-a-stick, clients in VLAN 10 on Gi2 cannot obtain IP configuration from the central DHCP server. The DHCP server is reachable by a successful ping from the router. Which action resolves the issue?

A. Configure a valid IP address on the Gi2 interface so that DHCP requests can be forwarded.  
B. Configure the `ip dhcp pool 1` and `network 192.168.210.0 255.255.255.0` commands.  
C. Configure the `ip helper-address 192.168.255.3` command on the Gi2.10 subinterface.  
D. Configure the `ip dhcp excluded-address 192.168.255.3` command on the Gi2.10 subinterface.  

- 問題文要約（日本語）:  
Router-on-a-stick 構成で VLAN10（サブIF Gi2.10, 192.168.210.0/24）のクライアントが、中央 DHCP サーバ 192.168.255.3 から IP を取得できない。  
ルータからは DHCP サーバへ ping が通る。  
どの設定を入れれば VLAN10 クライアントが IP を取得できるようになるか。

- 正解: **C**

- 解説（なぜ C が正しいか）
    - DHCP ブロードキャスト (DISCOVER) はクライアント VLAN 内で送信され、**デフォルトゲートウェイの L3 インターフェイス** で `ip helper-address` が設定されていると、そのアドレス宛ての **ユニキャスト DHCP リレー** に変換される。
    - クライアントが属するのは VLAN10 (192.168.210.0/24) であり、その L3 インターフェイスは `GigabitEthernet2.10`。  
      ここには `ip helper-address` が設定されていないため、DHCP ブロードキャストがリレーされていない。
    - 逆に、物理 IF Gi2 に設定されている `ip helper-address` は、L2 トランクポートであって SVI ではないので、クライアントのブロードキャストを受け取る L3 インターフェイスとして機能しない。
    - よって、サブインターフェイス `Gi2.10` 上に  
      `ip helper-address 192.168.255.3`  
      を設定する必要があり、これを述べる選択肢 C が正解となる。

- 他の選択肢がダメな理由
    - A: Router-on-a-stick ではサブIF に IP を持たせるのが正しく、物理 IF Gi2 に IP を付けるべきではない。
    - B: 問題の DHCP は「中央サーバ」で提供されており、ルータ自身を DHCP サーバにする必要はない。
    - D: `ip dhcp excluded-address` はローカル DHCP プールからの配布除外に使うコマンドであり、リレー動作には無関係。

***
### QUESTION 375 解説

- 問題文（英語）:
An engineer configured routing between multiple OSPF domains and introduced a routing loop that caused network instability. Which action resolves the problem?

A. Set a tag using the redistribute command toward a domain and deny inbound in the other domain by a matching tag  
B. Set a tag using the redistribute command toward a different domain and deny the matching tag when exiting from that domain  
C. Set a tag using the network command in a domain and use the route-map command to deny the matching tag when exiting toward a different domain.  
D. Set a tag using the network command in a domain and use the route-map command to deny the matching tag when entering into a different domain.  

- 問題文要約（日本語）:  
複数の OSPF ドメイン間で相互再配布をした結果、ループが発生してネットワークが不安定になっている。  
どの方法でループを防げるか。

- 正解: **A**

- 解説（なぜ A が正しいか）
    - OSPF ドメイン間で相互に `redistribute ospf x` を行うと、「ドメイン 1 → ドメイン 2 → 再びドメイン 1」というように、同じ経路が行き来して **ルーティングループ** を起こす危険がある。[file:79]
    - これを防ぐ代表的な方法が **ルートタグ (tag)** の利用。  
      手順のイメージ:
        1. ドメイン 1 からドメイン 2 へ再配布するとき、`redistribute ospf 1 subnets tag 1` のように **特定のタグ (1)** を付与する。
        2. ドメイン 2 側では、OSPF 再配布の「受信（in）」時にルートマップ／distribute-list で **tag 1 を持つルートを deny** し、自分側から再配布された経路が戻ってこないようにする。
        3. 逆方向（ドメイン 2 → 1）も tag 2 を付与し、ドメイン 1 側で tag 2 を持つルートを in で deny する。
    - 選択肢 A の  
      > Set a tag using the redistribute command toward a domain and deny inbound in the other domain by a matching tag  
      はこのやり方を正しく表現しているため、ループ防止策として適切。

- 他の選択肢がダメな理由
    - B: 「exit（出ていく）」方向で deny してしまうと、そのドメイン内でループした経路を防ぎきれないうえ、設計上も一般的ではない。
    - C / D: OSPF の `network` コマンドではタグを付けられず、タグ付与は `redistribute` で行うのが正しい。したがって前提から誤り。

***
### QUESTION 376 解説

- 問題文（英語）:
Refer to the exhibit. An administrator must configure the router with OSPF for IPv4 and IPv6 networks under a single process. The OSPF adjacencies are not established and did not meet the requirement. Which action resolves the issue?

A. Replace OSPF process 10 on the interfaces with OSPF process 1 for the IPv4 address, and remove process 10 from the global configuration.  
B. Replace OSPF process 10 on the interfaces with OSPF process 1, and remove process 10 from the global configuration.  
C. Replace OSPF process 10 on the interfaces with OSPF process 1 for the IPv6 address, and remove process 10 from the global configuration.  
D. Replace OSPF process 10 on the interfaces with OSPF process 1, and configure an additional router ID with IPv6 address.  

- 設定抜粋:
  - グローバル:
    ```text
    router ospfv3 1
     router-id 10.1.1.1
     address-family ipv4 unicast
      passive-interface Loopback0
     exit-address-family
     address-family ipv6 unicast
      passive-interface Loopback0
     exit-address-family
    ```
  - インターフェイス側:
    ```text
    interface Loopback0
     ospfv3 10 ipv4 area 10
     ospfv3 10 ipv6 area 0
    interface GigabitEthernet2
     ospfv3 10 ipv4 area 10
     ospfv3 10 ipv6 area 0
    ``` [file:79]

- 問題文要約（日本語）:  
1 つの OSPFv3 プロセスで IPv4 と IPv6 の両方を動かしたいが、グローバルでは process 1、インターフェイスでは process 10 を使っていて、隣接が張れていない。  
要件どおり「単一プロセス」で動作させるには、どう修正すべきか。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - OSPFv3 では「プロセス IDごとに address-family ipv4/ipv6 をぶら下げる」形で、**1 つの OSPFv3 プロセス内で IPv4/IPv6 を扱う**ことができる。[file:79]
    - 現在の設定では:
        - `router ospfv3 1` には IPv4/IPv6 の address-family と router-id が定義されている。
        - しかし、インターフェイスでは `ospfv3 10 ...` が指定されており、process 10 の下にぶら下がっている。
        - その結果、実際に動作しているのは「プロセス 10」であり、「単一プロセスに統一する」という要件に反している。
    - 対応すべきこと:
        1. すべてのインターフェイスの `ospfv3 10 ...` を **`ospfv3 1 ...` に変更**し、IPv4/IPv6 両方とも process 1 配下に参加させる。
        2. もう使わない `router ospfv3 10` のグローバル設定を `no router ospfv3 10` で削除する。
    - 選択肢 B は「インターフェイスの OSPF process を 1 に置き換え、さらにグローバルから process 10 を削除する」と述べており、上記 2 点を正しく行う構成になっている。

- 他の選択肢がダメな理由
    - A / C: IPv4 だけ、あるいは IPv6 だけを process 1 に変えると、IPv4/IPv6 で別プロセスになってしまい「単一プロセス」という要件を満たさない。
    - D: 既に router-id 10.1.1.1 が設定されており、IPv6 アドレスを用いた router-id は不要。問題は router-id ではなく「プロセス ID の不一致」。

***
### QUESTION 377 解説

- 問題文（英語）:
Which failure detection mechanism is used for BFD?

A. consistent rate  
B. Layer 2 protocol failure  
C. variable rate  
D. routing protocol failure  

- 問題文要約（日本語）:  
BFD が提供する障害検出の仕組みとして、どの特徴が正しいか。

- 正解: **A**

- 解説（なぜ A が正しいか）
    - BFD は、リンクやフォワーディングパスの障害を **一定・均一な間隔（uniform / consistent rate）** の高速 Hello によって検出するプロトコル。[file:79]
    - これにより、OSPF/EIGRP/BGP など各ルーティングプロトコル固有の Hello タイマに依存せず、**常に同じ検出時間** で障害検出・再収束が行えるため、動作が予測しやすい。
    - 選択肢 A の「consistent rate」は、この BFD の“均一なレート”での故障検出を指しており、説明と一致する。

- 他の選択肢がダメな理由
    - B: BFD は L2 プロトコル自体の失敗検出ではなく、「L2/L3 を含むフォワーディングパス全体」を監視する。
    - C: “variable rate” は、プロトコルごとに異なる Hello 間隔を使う従来のやり方であり、BFD はそれを解消するために「一定レート」で動作する。
    - D: BFD 自体は汎用のフォワーディングパス監視であり、特定の「ルーティングプロトコルの失敗」を見ているわけではない（ルーティングプロトコルは BFD 結果をトリガとして利用する）。

***
### QUESTION 378 解説

- 問題文（英語）:
The IPv6 network is under attack by an unknown source that is neither in the binding table nor learned through neighbor discovery. Which feature helps prevent the attack?

A. IPv6 Prefix Guard  
B. IPv6 Snooping  
C. IPv6 Router Advertisement Guard  
D. IPv6 Destination Guard  

- 問題文要約（日本語）:  
IPv6 ネットワークが、バインディングテーブルにも載っておらず、NDP でも学習されていない“不明なアドレス”から攻撃を受けている。  
このような「トポロジ的に正しくない IPv6 アドレス」からのトラフィックを防ぐのに役立つ機能はどれか。

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 基本となるのは **IPv6 Source Guard** で、これはバインディングテーブル（IPv6 Snooping で学習した IPv6/MAC/ポート情報）にない送信元アドレスのトラフィックをブロックする機能。[file:79]
    - **IPv6 Prefix Guard** は Source Guard の中で動作し、さらに一歩進んで  
      - トポロジ的に正しくないプレフィックス  
      - DHCPv6 PD などで委譲されていないプレフィックス  
      からのトラフィックを拒否することで、未知のソースからの攻撃を防ぐ役割を持つ。[file:79]
    - 設問の「バインディングテーブルにも NDP にも載っていない未知のアドレス」という条件に最も合うのが、この Prefix Guard であり、攻撃元を遮断できる。

- 他の選択肢がダメな理由
    - B: IPv6 Snooping はバインディングテーブルを“作る”機能であり、それ単体ではトラフィックをブロックしない（Source Guard/Prefix Guard の土台）。
    - C: IPv6 RA Guard は、不正な Router Advertisement をブロックする機能で、一般的な「未知ソースアドレス」からの全トラフィックを止めるものではない。
    - D: IPv6 Destination Guard は「宛先アドレス側」を検証して不正な宛先への NDP 解決を防ぐ機能であり、ここで問われている“未知ソースからの攻撃”には直接対応しない。

***

### QUESTION 379 解説

- 問題文（英語）:
Refer to the exhibit. The network administrator configured BGP as the backup route for 10.0.0.0/8 and it should work only when EIGRP 10.0.0.0/8 failed to install for the site. Which configuration resolves the issue? [file:79]

A. 
 configure terminal  
 !  
 ip route 10.0.0.0 255.0.0.0 192.168.90.2  

B. 
 configure terminal  
 !  
 router eigrp 1  
  distance eigrp 10 170  

C. 
 configure terminal  
 !  
 router eigrp 1  
  distance eigrp 90 170  

D. 
 configure terminal  
 !  
 router eigrp 1  
  redistribute bgp metric 10000 1 255 1 1500  

- 問題文要約（日本語）:  
10.0.0.0/8 への経路は **EIGRP をメイン**、BGP は「EIGRP が入らないときだけ使うバックアップ」にしたい。  
しかし現状では BGP 経路が優先されてしまっている。  
どの設定で「EIGRP を優先・BGP をバックアップ」にできるか。

- 状況整理:
    - eBGP から学習した 10.0.0.0/8 の AD は 20。  
    - EIGRP 内部ルートの AD はデフォルト 90。  
    - よって、**AD 20 の BGP が常に EIGRP (90) より優先**され、バックアップではなくメインとして使われてしまう。[file:79]

- 正解: **B**

- 解説（なぜ B が正しいか）
    - 目的は「EIGRP 10.0.0.0/8 をメイン」にすることなので、**EIGRP の AD を BGP (20) より小さく**すればよい。
    - `distance eigrp <internal> <external>` で、EIGRP の管理距離を変更できる。  
      選択肢 B では:
        - 内部 EIGRP AD = 10  
        - 外部 EIGRP AD = 170  
      と設定しており、10.0.0.0/8 が内部 EIGRP ルートなら AD 10 となって eBGP の 20 より優先される。  
      EIGRP 経路が消えた場合のみ、AD 20 の BGP 経路がルーティングテーブルへ入り、バックアップとして機能する。

- 他の選択肢がダメな理由
    - A: スタティックルート (AD 1) を追加すると、EIGRP/BGP よりさらに優先されてしまい、バックアップどころか常にこのスタティックが使われる。
    - C: `distance eigrp 90 170` はデフォルト値と同じで、AD を変えていないため問題は解決しない。
    - D: BGP 経路を EIGRP へ再配布する設定であり、AD の関係は変わらないうえ、ループや経路設計を複雑にするだけで逆効果。

***
### QUESTION 380 解説

- 問題文（英語）:
Switch(config)# ip vrf 70  
Switch(config-vrf)# rd 70:1  
Switch(config-vrf)# route-target export 70:1  
Switch(config-vrf)# route-target import 70:1  
Switch(config-vrf)# exit  
Switch(config)# ip vrf 80  
Switch(config-vrf)# rd 80:1  
Switch(config-vrf)# route-target export 80:1  
Switch(config-vrf)# route-target import 80:1  

Refer to the exhibit. An engineer must extend VRF-Lite over a trunk to another switch for VLAN 70 (10.70.70.0/24) on port GigabitEthernet0/0 and VLAN 80 (10.80.80.0/24) on port GigabitEthernet0/1. Which configuration accomplishes this objective?

A. Use routed ports with `ip vrf forwarding` on Gi0/0 and Gi0/1  
B. Make Gi0/0 and Gi0/1 access ports with `ip vrf forwarding`  
C. Make Gi0/0 and Gi0/1 access ports only  
D. Make Gi0/0 and Gi0/1 802.1Q trunks, allowing VLAN 70 and VLAN 80 respectively  

- 問題文要約（日本語）:  
スイッチ上で VRF 70 と VRF 80 が作成されている。  
別のスイッチに VRF-Lite を**トランク越しに拡張**するために、  
- Gi0/0 で VLAN70（10.70.70.0/24）  
- Gi0/1 で VLAN80（10.80.80.0/24）  
を流したい。どのインターフェイス設定が適切か。

- 正解: **D**

- 解説（なぜ D が正しいか）
    - VRF-Lite を「スイッチ間の VLAN トランクで延伸」する場合、通常は  
      - 物理ポートは L2 の **802.1Q トランクポート** として設定  
      - VLAN ごとに SVI（`interface Vlan70`, `interface Vlan80` など）で `ip vrf forwarding` と IP アドレスを割り当てる  
      という構成をとる。
    - 問題文でも「over a trunk to another switch」と明示されているので、Gi0/0 と Gi0/1 は **トランクポート** にする必要があり、`switchport mode trunk` と `switchport trunk allowed vlan ...` が必須となる。
    - 選択肢 D は Gi0/0, Gi0/1 をそれぞれ  
      ```text
      switchport trunk encapsulation dot1q
      switchport mode trunk
      switchport trunk allowed vlan 70 / 80
      ```  
      と設定しており、VLAN70 と 80 をトランクで別スイッチへ運ぶ要件に一致する。

- 他の選択肢がダメな理由
    - A: `no switchport` にして L3 ポートにすると、「トランクで VLAN を延伸する」という要件を満たさない（ただの L3 ポイントツーポイントになる）。
    - B: `switchport mode access` かつ `ip vrf forwarding` を物理ポートに同時適用する構成は一般的でなく、トランクでもない。
    - C: 単にアクセス VLAN を設定するだけで、VRF との関連付けもトランクでの延伸も行われない。

***
### QUESTION 381 解説

- 問題文（英語）:
The network administrator must configure R1 to authenticate Telnet connections based on Cisco ISE using RADIUS. ISE = 192.168.1.5, R1 = 192.168.1.1, shared secret = Cisco123.  
R1 設定:

aaa new-model
!
radius server ISE1
 address ipv4 192.168.1.5
 key Cisco123
!
aaa group server tacas+ RAD-SERV
 server name ISE1
!
aaa authentication login default group RAD-SERV

The network administrator cannot authenticate to access R1 based on ISE. Which set of configurations fixes the issue?

A. aaa group server radius RAD-SERV  
   server name ISE1  

B. aaa group server tacacs+ ISE1  
   server name RAD-SERV  

C. line vty 0 4  
   login authentication RAD-SERV  

D. line vty 0 4  
   login authentication default  

- 問題文要約（日本語）:  
Telnet ログインを ISE（RADIUS）で認証したいが、ログインできない。  
RADIUS サーバ自体は定義しているが、サーバグループ定義で TACACS+ を指定してしまっている。  
どの設定で修正すべきか。

- 正解: **A**

- 解説（なぜ A が正しいか）
    - ISE との通信には **RADIUS** を使う前提なのに、グループ定義が  
      `aaa group server tacas+ RAD-SERV`  
      となっており、**TACACS+ グループ**として登録している。[file:79]
    - その結果、`aaa authentication login default group RAD-SERV` では、R1 は TACACS+ で ISE に話そうとして失敗する。
    - 正しくは RADIUS グループとして  
      ```text
      aaa group server radius RAD-SERV
       server name ISE1
      ```  
      を定義し直す必要がある。
    - これを示しているのが選択肢 A であり、既存の `aaa authentication login default group RAD-SERV` はそのまま使えるので、これで ISE/RADIUS 認証が機能する。

- 他の選択肢がダメな理由
    - B: グループ種別を tacacs+ のままにしており、問題を解決しない。
    - C: line vty の認証リスト名を `RAD-SERV` に変えても、そもそもグループ定義が TACACS+ なので根本原因が残る。
    - D: `login authentication default` はすでにデフォルトで使っている設定であり、変更にならない（`aaa authentication login default ...` が既に定義済み）。

***
### QUESTION 382 解説

- 問題文（英語）:
Network operations report issues with receiving too many external routes, which caused CPU spike on routers with smaller memories. Which action resolves the issue?

A. Configure the area range command when redistributing on ASBR  
B. Configure the summary-address command when redistributing on ABR  
C. Configure the area range command when redistributing on ABR  
D. Configure the summary-address command when redistributing on ASBR  

- 問題文要約（日本語）:  
外部ルート（OSPF 外部 LSA）が多すぎて、小規模ルータで CPU スパイクが発生している。  
どの設定で、外部ルート数を減らして負荷を下げられるか。

- 正解: **D**

- 解説（なぜ D が正しいか）
    - OSPF で「外部ルート（Type 5/Type 7 LSA）」を集約・要約するのは **ASBR 側の `summary-address` コマンド** の役割。[file:79]
        - 例:  
          `router ospf 1`  
          ` summary-address 192.168.0.0 255.255.252.0`  
        - こうすると、ASBR が多くの外部プレフィックスを 1 つの大きなサマリプレフィックスとして生成し、他ルータにはそのサマリだけを配布できる。
    - これにより、ネットワーク全体に流れる外部 LSA の数が大幅に減り、LSDB・計算負荷・CPU 使用率の抑制につながる。
    - 選択肢 D の  
      > Configure the summary-address command when redistributing on ASBR  
      がまさにこの操作であり、外部ルート数削減という目的に合致する。

- 他の選択肢がダメな理由
    - A / C: `area range` は **ABR でエリア内部ルート (intra-area / inter-area)** をサマリするためのコマンドであり、外部ルートを対象としない。
    - B: `summary-address` を ABR に設定するのは誤りで、外部ルートは ASBR が起点となるため、サマリは ASBR で行う必要がある。

***
QUESTION 383 解説

- 問題文（英語）:
Refer to the exhibit. An engineer must configure a LAN-to-LAN IPsec VPN between R1 and the remote router. Which IPsec Phase 1 configuration must the engineer use for the local router? [file:1]

A. crypto isakmp policy 5
   authentication pre-share
   encryption 3des
   hash md5
   group 2
   !
   crypto isakmp key cisco123 address 199.1.1.1

B. crypto isakmp policy 5
   authentication pre-share
   encryption 3des
   hash md5
   group 2
   !
   crypto isakmp key cisco123 address 200.1.1.3

C. crypto isakmp policy 5
   authentication pre-share
   encryption 3des
   hash md5
   group 2
   !
   crypto isakrnp key cisco123! address 199.1.1.1

D. crypto isakmp policy 5
   authentication pre-share
   encryption 3des
   hash sha
   group 2
   !
   crypto isakmp key cisco123 address 200.1.1.3 [file:1]

- 問題文要約（日本語）:  
R1（ローカル）とリモートルータ R3 の間で LAN-to-LAN IPsec VPN を張る。  
図では R1 側が 199.1.1.0/24、リモート側 R3 は 200.1.1.0/24 上の 200.1.1.3。  
R1 で設定すべき Phase 1（ISAKMP）設定を選ぶ問題。[file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）
  - ローカルは R1、リモートは R3 なので、R1 から見る「相手の IP」は R3 の 200.1.1.3。  
    したがって `crypto isakmp key ... address 200.1.1.3` が正しい。199.1.1.1 は自分側なので NG。[file:1]
  - Phase 1 のポリシー自体は、どれも pre-shared・3DES・DH group 2 などで大きな違いはないが、ハッシュに SHA を使う方が MD5 よりセキュア。  
    選択肢 D は、正しいリモート IP（200.1.1.3）とより安全な `hash sha` を組み合わせているのでベスト。[file:1]
  - 問題文中の解説でも「R1 から R3 へ認証するには R3 の IP（200.1.1.0/24 内）を指定する必要がある」「200.1.1.3 の選択肢どちらでも動作するが、SHA の方が望ましい」と説明されている。[file:1]

- 他の選択肢がダメな理由
  - A / C: `address 199.1.1.1` は R1 自身の IP なので、相手のピア指定として誤り。[file:1]
  - C: さらにキー文字列に余計な「!」が含まれておりタイプミス（`cisco123!`）になっている。[file:1]
  - B: リモート IP は正しいが、ハッシュが MD5 で、D に比べてセキュリティ面で劣る。[file:1]

***
QUESTION 384 解説

- 問題文（英語）:
Refer to the exhibit. A network engineer received a call from the vendor for a failed attempt to remotely log in to their managed router loopback interface from 192.168.40.15. Which action must the network engineer take to resolve the issue? [file:1]

A. The source IP summarization must be updated to include the vendor source IP address.  
B. The IP access list VENDOR must be applied to interface loopback 100.  
C. The time-range configuration must be changed to use absolute instead of periodic.  
D. The EIGRP configuration must be updated to include a network statement for loopback 100. [file:1]

- 問題文要約（日本語）:  
ベンダが自分たち管理用ルータの Loopback100 へリモートログインしようとしたが失敗した。  
ベンダ側の送信元 IP は 192.168.40.15。  
図の ACL「VENDOR」では、192.168.32.0 0.0.7.255 というサマリで許可している。  
どの対応で問題が解決できるか。[file:1]

- 正解: **A**

- 解説（なぜ A が正しいか）
  - ACL では `192.168.32.0 0.0.7.255` が許可されているが、これはワイルドカード 0.0.7.255 なので  
    192.168.32.0〜192.168.39.255（192.168.32.x〜39.x）を意味する。[file:1]
  - ベンダの IP 192.168.40.15 はこの範囲外（40.x）なので、ACL で「許可されていない＝拒否」される。  
    そのためリモートログインが失敗している。[file:1]
  - よって、ベンダのアドレス 192.168.40.15 も含むように「送信元 IP のサマリ範囲（サマリゼーション）」を修正する必要がある。  
    これを示しているのが選択肢 A。ACL のサマリを 192.168.32.0 0.0.15.255 などに広げれば 32〜47.x まで含められる。[file:1]

- 他の選択肢がダメな理由
  - B: ループバックに ACL がすでに適用済みかどうかは関係なく、根本原因は「サマリ範囲に 192.168.40.15 が含まれていない」こと。[file:1]
  - C: 時間帯（time-range）はこの問題の説明には出てこず、ACL 範囲の不備が原因と説明されているので無関係。[file:1]
  - D: EIGRP の network 文はルーティングの問題であり、ACL によるリモートアクセス制御の話とは別問題。[file:1]

***
QUESTION 386 解説

- 問題文（英語）:
Drag and drop the ICMPv6 neighbor discovery messages from the left onto the correct packet types on the right. Select and Place. [file:1]

- 問題文要約（日本語）:  
ICMPv6 Neighbor Discovery で使われる各種メッセージの「タイプ番号」と「名前（役割）」を正しく対応させるドラッグ&ドロップ問題。[file:1]

- 正解（対応関係）:
  - Type 133 → Router Solicitation (RS) [file:1]  
  - Type 134 → Router Advertisement (RA) [file:1]  
  - Type 135 → Neighbor Solicitation (NS) [file:1]  
  - Type 136 → Neighbor Advertisement (NA) [file:1]  
  - Type 137 → Redirect Message [file:1]

- 解説（なぜこうなるか）
  - 133/134 は「ルータ検出用」メッセージで、ホストからの問い合わせが RS、ルータからの応答が RA。 [file:1]
  - 135/136 は「近隣ノードの MAC 解決・重複確認」などに使うメッセージで、問い合わせが NS、応答が NA。 [file:1]
  - 137 は「より良い経路を教える」ための Redirect。 [file:1]

***
QUESTION 387 解説

- 問題文（英語）:
Refer to the exhibit. An engineer configured SNMP communities on UserSW2 switch, but the SNMP server cannot upload modified configurations to the switch. Which configuration resolves this issue? [file:1]

A. snmp-server community Ciscowruser RW 11  
B. snmp-server group NETVIEW v2c priv read NETVIEW access 11  
C. snmp-server community Cisc0Us3r RW 11  
D. snmp-server group NETADMIN v3 priv read NETVIEW write NETADMIN access 22 [file:1]

- 問題文要約（日本語）:  
ユーザスイッチ UserSW2 で SNMP コミュニティを設定しているが、NMS 側から設定変更（書き込み）ができない。  
トポロジ図では「書き込み用コミュニティ名」は Ciscowruser になっているのに、スイッチの設定では綴りが食い違っている。  
どの設定を入れれば SNMP サーバが設定を書き込めるようになるか。[file:1]

- 正解: **A**

- 解説（なぜ A が正しいか）
  - トポロジ上の「RW（read-write）コミュニティ名」は `Ciscowruser` となっているが、現在のスイッチ設定では `Cisc0wrus3r` のように 0 と o／3 と e が混ざった誤綴りになっている。 [file:1]
  - SNMPv2c の認証は「コミュニティ文字列」が一致しているかどうかだけで判定されるため、文字が 1 文字でも違えば別物として扱われ、RW 権限での書き込みは許可されない。 [file:1]
  - 選択肢 A の `snmp-server community Ciscowruser RW 11` により、  
    トポロジで定義された正しい RW コミュニティ名とアクセスリスト 11 を使った read-write コミュニティが設定され、  
    NMS からのコンフィグ書き込みが成功するようになる。 [file:1]

- 他の選択肢がダメな理由
  - B / D: SNMP グループ定義（v2c/v3）の話であり、問題の核心である「コミュニティ文字列のスペルミス」を修正していない。 [file:1]
  - C: `Cisc0Us3r` は読み取り専用コミュニティ（RO）の名前として使われている文字列であり、RW 用コミュニティの綴り修正にはなっていない。 [file:1]

***
QUESTION 388 解説

- 問題文（英語）:
How is a preshared key "Test" for all the remote VPN routers configured in a DMVPN using GRE over IPsec set up? [file:1]

A. authentication pre-share Test address 0.0.0.0 0.0.0.0  
B. set pre-share Test address 0.0.0.0 0.0.0.0  
C. crypto ipsec key Test address 0.0.0.0 0.0.0.0  
D. crypto isakmp key Test address 0.0.0.0 0.0.0.0 [file:1]

- 問題文要約（日本語）:  
GRE over IPsec を使った DMVPN で、「すべてのリモート VPN ルータ」と共有する事前共有鍵 Test を設定したい。  
このとき、どのコマンドで“全リモートピア（動的なピアを含む）”向けの事前共有鍵を設定するかを問う問題。[file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）
  - IPsec のフェーズ1（IKE/ISAKMP）の事前共有鍵は `crypto isakmp key <key> address <peer-ip>` で設定する。  
    ここで `<key>` が "Test"、`address 0.0.0.0 0.0.0.0` は「どのピアアドレスでもこの鍵を使う」という意味になる。 [file:1]
  - DMVPN では、ハブ側はスポークのピア IP が動的なことが多いため、すべてのスポークに共通の PSK を 0.0.0.0/0 で定義するのが典型パターン。 [file:1]
  - 選択肢 A / B は正しい Cisco IOS の構文ではなく、C の `crypto ipsec key` というコマンドも存在しないため誤りとなる。 [file:1]

- 補足（イメージ）
  - 実際の設定イメージは次のような形になる：  
    `crypto isakmp key Test address 0.0.0.0 0.0.0.0`  
    これにより、このハブに対して IKE を張ってくる任意のリモートルータは、PSK "Test" を使って認証される。 [file:1]

***
DMVPN の設計で「全スポーク同じ PSK」にする場合と、「スポークごとに別 PSK」にする場合、それぞれどんなメリット・デメリットがありそうか、ざっくりイメージできますか？

QUESTION 389 解説

- 問題文（英語）:
Refer to the exhibit. A shoe retail company implemented the uRPF solution for an antispoofing attack. A network engineer received the call that the branch A server is under an IP spoofing attack. Which configuration must be implemented to resolve the attack? [file:1]

A. R4  
   interface ethernet0/1  
   ip unicast RPF check reachable-via any allow-default allow-self-ping  

B. R4  
   interface ethernet0/1  
   ip verify unicast source reachable-via any allow-default allow-self-ping  

C. R3  
   interface ethernet0/1  
   ip unicast RPF check reachable-via any allow-default allow-self-ping  

D. R3  
   interface ethernet0/1  
   ip verify unicast source reachable-via any allow-default allow-self-ping [file:1]

- 問題文要約（日本語）:  
シューズ小売り会社が IP スプーフィング対策として uRPF（Unicast Reverse Path Forwarding）を導入した。  
Branch A のサーバに対して IP スプーフィング攻撃が発生したとの連絡を受けた。  
図の出力から、R4 では uRPF が有効だが、R3 ではまだ有効化されていないことが分かる。  
どの設定を入れれば攻撃を防げるか。[file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）
  - Cisco IOS で uRPF を有効にする正式なコマンドは `ip verify unicast source reachable-via ...` であり、`ip unicast RPF check ...` というコマンドは実在しない。  
    よって A と C は構文として不正。 [file:1]
  - 攻撃は Branch A サーバに向かうトラフィックなので、そのサーバに近いルータ（図の構成では R3 側）で uRPF を有効化するのが効果的。  
    出力でも「R3 は IP unicast RPF を未設定、R4 は設定済み」と説明されている。 [file:1]
  - 選択肢 D は、R3 の ethernet0/1 インターフェイスで  
    `ip verify unicast source reachable-via any allow-default allow-self-ping`  
    を設定しており、ルーティングテーブルに照らして“あり得ない送信元”からのパケットを破棄できる。 [file:1]
  - `reachable-via any` を使っているのでルーズモードの uRPF となり、非対称ルーティングでも動作しつつ、スプーフィングパケットはドロップできる。 [file:1]

- 他の選択肢がダメな理由
  - A / C: コマンド構文が誤りで、実際の IOS では受け付けられない。 [file:1]
  - B: R4 側で uRPF を強化しても、既に R4 には uRPF が有効であり、問題は「まだガードが効いていない R3 側」にあるため根本解決にならない。 [file:1]

***
QUESTION 390 解説

- 問題文（英語）:
Refer to the exhibit. An administrator must harden a router, but the administrator failed to test the SSH access successfully to the router. Which action resolves the issue? [file:1]

A. SSH syntax must be ssh -l user ip to log in to the remote device  
B. Configure enable secret to log in to the device  
C. Configure SSH on the remote device to log in using SSH  
D. SSH must be allowed with the transport output ssh command [file:1]

- 問題文要約（日本語）:  
管理者がルータのセキュリティ強化設定を行い、SSH 用のユーザ（例: `csadmin`）を新たに作成したが、自分の端末から SSH 接続テストに失敗してしまった。  
図の状態では、コンソールや Telnet などでログイン中のセッションに「ユーザ名」は紐づいておらず、その状態から SSH コマンドを打っても正しく認証されない。  
どの操作をすれば SSH ログインに成功できるかを問う問題。[file:1]

- 正解: **A**

- 解説（なぜ A が正しいか）
  - 現在の exec セッションは、おそらく「コンソール or 単純なパスワード認証の Telnet」で入っており、ユーザ名 `csadmin` ではログインしていない。 [file:1]
  - Cisco IOS では、SSH クライアントとしてルータから別機器へ接続するとき、`ssh -l <username> <ip>` 形式で「SSH で使うユーザ名」を明示する必要がある。  
    この問題では `ssh -l csadmin 10.0.0.1` のように実行することで、作成したユーザ名／パスワードでのログインが可能になる。 [file:1]
  - B: `enable secret` は特権モード用パスワードであり、SSH ログイン失敗の直接原因ではない。 [file:1]
  - C: 「リモート側で SSH 有効化」がされていないケースならあり得るが、問題の説明と解説では、すでに SSH サービスは有効で、クライアント側のコマンド指定が誤っている状況とされている。 [file:1]
  - D: `transport output ssh` はルータから外に SSH を出す方向ではなく、通常は vty での受け入れ側に `transport input ssh` を設定する話であり、ここでは論点がずれている。 [file:1]

***
QUESTION 391 解説

- 問題文（英語）:
The network administrator configured the router for Control Plane Policing so that inbound SSH traffic is policed to 500 kbps. This policy must apply to traffic coming in from 10.10.10.0/24 and 192.168.10.0/24 networks.  
… (ACL 100・class-map・policy-map・service-policy 設定省略)  
The Control Plane Policing is not applied to SSH traffic and SSH is open to use any bandwidth available. Which configuration resolves this issue? [file:1]

- 問題文要約（日本語）:  
管理者は「コントロールプレーンポリシング(CoPP)で SSH トラフィックを 500 kbps に制限」したい。対象は 10.10.10.0/24 と 192.168.10.0/24 から来る SSH。  
しかし現在の設定では  
1) ACL 100 が “10.10.10.0/24 からの全 IP” と “192.168.10.0/24 からの Telnet(23番)” をマッチしており、SSH を正しく絞れていない  
2) policy-map が `interface E0/0` と `E0/1` に input で適用されていて、「control-plane」には適用されていないため、本来の CoPP になっていない  
この2つの問題を同時に直すコンフィグはどれか、という問題。[file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  1. ACL の修正（SSH トラフィックだけをマッチ）
     - 元の ACL 100:  
       `permit ip 10.10.10.0 0.0.0.255 any` → 10.10.10.0/24 からの全トラフィック  
       `permit tcp 192.168.10.0 0.0.0.255 any eq 23` → 192.168.10.0/24 からの Telnet のみ [file:1]  
     - これでは「SSH だけを 500 kbps で制限する」という要件を満たせない。  
     - C では、ACL 100 を  
       - `permit tcp 10.10.10.0 0.0.0.255 any eq 22`  
       - `permit tcp 192.168.10.0 0.0.0.255 any eq 22`  
       に変更しており、両セグメントからの SSH（TCP/22）のみにマッチさせている。 [file:1]

  2. 真の CoPP にする（適用先を control-plane に変更）
     - CoPP は本来 `control-plane` コンテキストで `service-policy input <policy>` として CPU 向きトラフィックに適用する機能。 [file:1]
     - 元の設定は `interface E0/0` / `E0/1` に対して `service-policy input PM-COPP` を設定しており、これはただのインターフェイス QoS であって CoPP ではない。 [file:1]
     - C は E0/0・E0/1 からサービスポリシーを削除し、代わりに:
       - `control-plane`
       - `service-policy input PM-COPP`  
       を設定して、正しく CoPP として動作させている。 [file:1]

  3. 他の選択肢が足りない点
     - A / B: ACL 側は SSH のみをマッチするよう直しているが、policy の適用先が「インターフェイスのまま」なので CoPP になっていない。 [file:1]
     - D: control-plane に適用する点は正しいが、ACL 100 は相変わらず SSH 以外もマッチしているため「SSH だけを 500 kbps に制限」という要件を満たさない。 [file:1]

***
QUESTION 392 解説

- 問題文（英語）:
Refer to the exhibit. An engineer configures two ASBRs, 10.4.17.6 and 10.4.15.5, in an OSPF network to redistribute routes from EIGRP. However, both ASBRs show the EIGRP routes as equal costs even though the next-hop router 10.4.17.6 is closer to R1. How should the network traffic to the EIGRP prefixes be sent via 10.4.17.6? [file:1]

A. The administrative distance should be raised to 120 from the ASBR 10.4.15.5.  
B. The ASBR 10.4.17.6 should assign a tag to match and assign a lower metric on R1.  
C. The administrative distance should be raised to 120 from the ASBR 10.4.17.6.  
D. The redistributed prefixes should be advertised as Type 1. [file:1]

- 問題文要約（日本語）:  
2 台の ASBR（10.4.17.6 と 10.4.15.5）が EIGRP から OSPF へ再配布を行っている。  
R1 から見ると、どちらの ASBR から学ぶ EIGRP 経路も OSPF 上で「同じコスト」の外部経路として見えてしまい、R1 から見て物理的に近い 10.4.17.6 経由を優先してくれない。  
EIGRP プレフィックスへのトラフィックを 10.4.17.6 経由にさせるにはどうすべきか、という問題。[file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - OSPF 外部経路のタイプには  
    - Type 2 (E2): デフォルト。外部メトリックのみで経路選択し、AS 内のコストは無視。  
    - Type 1 (E1): 外部メトリックに「AS 内のコスト」も加算して比較する。  
    がある。 [file:1]
  - 現状は両 ASBR が E2 として同じメトリックで再配布しているため、R1 から見ると「どの ASBR 経由でも同じコストの外部経路」に見え、内部距離の違い（10.4.17.6 の方が近い）が反映されていない。 [file:1]
  - 再配布時に E1（Type 1）で再配布するよう設定すれば、  
    「ASBR までの内部 OSPF コスト ＋ 外部メトリック」を合計した値で比較されるため、R1 に近い ASBR（10.4.17.6）経由の経路が自然と優先される。 [file:1]
  - 行いたいのは「2 本の外部経路のうち、近い方を選ばせる」ことなので、AD を変えて片方の経路を“全部”不利にする A / C や、タグでのフィルタリング B より、E1 を使って内部コスト差を反映させる D がシンプルかつ意図に合致している。 [file:1]

***
QUESTION 393 解説

- 問題文（英語）:
Refer to the exhibit. The Customer Edge router (AS 65500) wants to use AS 100 as the preferred ISP for all external routes.

Customer Edge
route-map SETLP
 set local-preference 111
!
router bgp 65500
 neighbor 192.168.111.1 remote-as 100
 neighbor 192.168.111.1 route-map SETLP out
 neighbor 192.168.112.2 remote-as 200

This configuration failed to send routes to AS 100 as the preferred path. Which set of configurations resolves the issue? [file:1]

- 問題文要約（日本語）:  
CE ルータ（AS 65500）は、インターネット向けトラフィックで ISP1（AS 100）を優先的に使いたい。  
そのために local-preference を 111 に上げる route-map を設定したが、設定方向が誤っており、受信した BGP 経路の優先度に反映されていない。  
どの設定にすれば、AS 100 から学んだ経路の local-preference を 111 にできるか、という問題。[file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 「AS 100 を優先 ISP にしたい」ということは、  
    CE ルータ内部で、AS 100 から**受信した**外部経路の local-preference を他より高く設定する必要がある。  
    local-preference は**自 AS 内部での経路選択**に使う属性なので、「CE が受信する側」で書き換えないと意味がない。 [file:1]
  - 元の設定は `neighbor 192.168.111.1 route-map SETLP out` となっており、  
    これは「CE が AS 100 に広告するルート」に対して local-preference を設定しているだけで、  
    CE が AS 100 から**受信する経路**には何も影響しない。 [file:1]
  - 選択肢 B では、`neighbor 192.168.111.1 route-map SETLP in` となっており、  
    AS 100 から受信する経路に対して `set local-preference 111` が適用される。  
    その結果、AS 200 から学んだ経路よりも AS 100 からの経路が優先されるようになる。 [file:1]
  - C / D の AS-path prepend は「経路を外に出すときに AS パスを長くして他から見て“劣後させる”」用途であり、  
    ここでは「CE 内で AS 100 を優先したい」という目的に合わない。 [file:1]

***
QUESTION 394 解説

- 問題文（英語）:
Refer to the exhibit. A network engineer is troubleshooting a failed link between R2 and R3. No traffic loss is reported from router R5 to HQ. Which command fixes the separated backbone? [file:1]

A. R3(config-router)#area 21 virtual-link 192.168.125.5  
B. R3(config-router)#no area 21 stub  
C. R2(config-router)#area 21 virtual-link 192.168.125.5  
D. R2(config-router)#no area 21 stub [file:1]

- 問題文要約（日本語）:  
OSPF でバックボーンエリア（area 0）が R2–R3 間リンク障害により“分断”されている。  
R2–R3 間の非バックボーンエリア（area 21）をトランジットにして Virtual Link で backbone をつなごうとしているが、うまく動作しておらず、backbone が論理的に分離した状態になっている。  
原因は、そのトランジットエリア 21 が Stub エリアとして設定されていること。Stub エリアは Virtual Link のトランジットに使用できないため、この設定をどこで外すべきかを問う問題。[file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - Virtual Link は「backbone が物理的に分断されたときに、非バックボーンエリアを経由して area 0 同士を論理的につなぐ仕組み」。  
    この“経由するエリア”は **Stub エリアにはできない** という制約がある。 [file:1]
  - 本問では、R2–R3 間のエリア 21 をトランジットエリアとして Virtual Link を張ろうとしているが、既に `area 21 stub` が設定されているため、そのエリアを通る Virtual Link が成立しない。 [file:1]
  - 解決策は「エリア 21 の stub 設定を外す」こと。  
    正解 B の `R3(config-router)#no area 21 stub` によって、R3 側で Stub 設定が削除され、Virtual Link が正常に動作し、area 0 が再び一つのバックボーンとして認識される。 [file:1]
  - Virtual Link 自体はすでに R2・R3 双方で設定済みであり、問題は“Stub であること”なので、追加で `area 21 virtual-link ...` を入れる A / C は本質的な修正にならない。D は R2 側のみ Stub を外す案だが、解説では R3 側の設定を外す前提で説明されている。 [file:1]

***
QUESTION 395 解説

- 問題文（英語）:
Refer to the exhibit. An engineer must configure OSPF with R9 and R10 and configure redistribution between OSPF and RIP, causing routing loop. Which configuration on R9 and R10 meets this objective? [file:1]

A. router ospf 1  
   redistribute rip subnets tag 20  
   !  
   route-map deny_tag20 deny 10  
    match tag 20  
   route-map deny_tag20 permit 20  
   !  
   router ospf 1  
    distribute-list route-map deny_tag20 in  

B. router ospf 1  
   redistribute rip subnets tag 20  
   !  
   route-map deny_tag20 deny 10  
    match tag 20  
   route-map deny_tag20 permit 20  
   !  
   router rip 1  
    distribute-list route-map deny_tag20 in  

C. router ospf 1  
   redistribute rip subnets tag 20  
   !  
   route-map deny_tag20 permit 10  
    match tag 20  
   route-map deny_tag20 permit 20  
   !  
   router ospf 1  
    distribute-list route-map deny_tag20 in  

D. router ospf 1  
   redistribute rip subnets tag 20  
   !  
   route-map deny_tag20 deny 10  
    match tag 20  
   route-map deny_tag20 deny 20  
   !  
   router ospf 1  
    distribute-list route-map deny_tag20 in [file:1]

- 問題文要約（日本語）:  
R9／R10 間で OSPF と RIP を相互再配布しており、RIP で学んだ経路が OSPF に入り、さらに RIP 側へ戻されることでループが発生している。  
解決したいことは：  
1) RIP → OSPF へ再配布する際に tag 20 を付与する。  
2) その後、OSPF → RIP 側に戻すとき、「もともと RIP から来た経路（tag 20）」を RIP へ再配布しないようにフィルタする。  
これを正しく実現する設定はどれか、という問題。[file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  1. RIP → OSPF 方向でタグ付け
     - `router ospf 1`  
       `redistribute rip subnets tag 20`  
       により、「RIP から OSPF に入るすべての再配布経路」に tag 20 が付与される。  
       このタグが「元の出どころは RIP」という印になる。 [file:1]

  2. OSPF → RIP 方向で“タグ 20”を拒否
     - `route-map deny_tag20` では  
       - `deny 10` で `match tag 20` を指定 → tag 20 の経路を拒否  
       - `permit 20` でそれ以外の経路を許可  
       という構成になっている。 [file:1]
     - この route-map を OSPF ではなく **RIP プロセス側** で入力フィルタとして適用するのがポイント。  
       B の `router rip 1` → `distribute-list route-map deny_tag20 in` によって、  
       OSPF から RIP に“入り込んでくる”経路のうち、tag 20 が付いているもの（＝元々 RIP だった経路）を RIP に戻さない。 [file:1]
     - こうすることで、「RIP で学んだ経路 → OSPF → 再び RIP」というループ経路が防止される。 [file:1]

  3. 他の選択肢がダメな理由
     - A / C / D は route-map を OSPF 側に適用しており、「OSPF に入ってくる経路」のフィルタになってしまう。  
       しかしタグ 20 は“RIP から OSPF に再配布するときに付く”ため、「OSPF → RIP に戻るとき」にフィルタする必要がある。 [file:1]
     - C は tag 20 も permit してしまうのでループ防止にならない。D は全 deny で、OSPF 側の経路受け取り自体を止めてしまう。 [file:1]

***
QUESTION 396 解説

- 問題文（英語）:
Refer to the exhibits. London must reach Rome using a faster path via EIGRP if all the links are up, but it failed to take this path. Which action resolves the issue? [file:1]

A. Use the network statement on London to inject the 172.16.X.0/24 networks into EIGRP.  
B. Increase the bandwidth of the link between London and Barcelona  
C. Use the network statement on Rome to inject the 172.16.X.0/24 networks into EIGRP.  
D. Change the administrative distance of RIP to 150. [file:1]

- 問題文要約（日本語）:  
London から 172.16.x.0/24（Rome 側のネットワーク）へ到達する際、本来は EIGRP 経由で「London → Barcelona → Rome」という高速パス（1Gbps×2 本）を使わせたい。  
しかし現状では、London は Rome 直結リンク（100Mbps）を使ってしまい、高速パスを選んでくれない。  
どの設定を変更すると、London が EIGRP 経由の高速パスを選ぶようになるか、という問題。[file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 現状、London は 172.16.x.0/24 を  
    - EIGRP external（`redistribute connected` など）として AD 170  
    - RIP 経路として AD 120  
    の 2 つで学習しており、AD の小さい RIP が常に優先されている。[file:1]
  - RIP はホップ数ベースなので、「London–Rome 直結 100Mbps（1 hop）」と「London–Barcelona–Rome（2 hop）」を比べると、直結 100Mbps を選んでしまう。[file:1]
  - そこで Rome 側で `network 172.16.X.0 0.0.0.255` のように EIGRP の network ステートメントを追加し、そのネットワークを「EIGRP 内部経路（internal）」として広告させる。  
    その結果、London は 172.16.x.0/24 を  
    - EIGRP internal（AD 90）  
    - RIP（AD 120）  
    の 2 種類で学び、AD 90 の EIGRP internal を優先するようになる。[file:1]
  - EIGRP internal どうしであれば、帯域・遅延から計算される metric が使われるため、最終的に 1Gbps×2 本を通る London → Barcelona → Rome の経路が選ばれ、「速いパスを優先」という要件を満たす。[file:1]
  - A は London 側で network コマンドを打っても、172.16.x.0/24 は London 直下のネットワークではないため意味がない。  
    D の AD 変更も方針としては RIP を不利にできるが、この問題の意図は「172.16.x.0/24 を EIGRP internal にする」ことにある。 [file:1]

***
QUESTION 397 解説

- 問題文（英語）:
Refer to the exhibit. Bangkok is using ECMP to reach 172.20.2.0/24 network. The network administrator must configure it in such a way that traffic from 172.16.2.0/24 network uses the Singapore router as the preferred route. Which set of configurations accomplishes this task? [file:1]

A. Bangkok  
   access-list 101 permit ip 172.16.2.0 0.0.0.255 172.20.2.0 0.0.0.255  
   !  
   route-map PBR1 permit 10  
    match ip address 101  
    set ip next-hop 172.19.1.2  
   !  
   interface Ethernet0/2  
    ip policy route-map PBR1  

B. Dubai  
   access-list 101 permit ip 172.16.2.0 0.0.0.255 172.20.2.0 0.0.0.255  
   !  
   route-map PBR1 permit 10  
    match ip address 101  
    set ip next-hop peer-address  
   !  
   interface Ethernet0/0  
    ip policy route-map PBR1  

C. Dubai  
   access-list 101 permit ip 172.16.2.0 0.0.0.255 172.20.2.0 0.0.0.255  
   !  
   route-map PBR1 permit 10  
    match ip address 101  
    set ip next-hop 172.19.1.2  
   !  
   interface Ethernet0/0  
    ip policy route-map PBR1  

D. Bangkok  
   access-list 101 permit ip 172.16.2.0 0.0.0.255 172.20.2.0 0.0.0.255  
   !  
   route-map PBR1 permit 10  
    match ip address 101  
    set ip next-hop 172.19.1.2  
   !  
   interface Ethernet0/1  
    ip policy route-map PBR1 [file:1]

- 問題文要約（日本語）:  
Bangkok ルータは、172.20.2.0/24 宛トラフィックに対して ECMP（Equal-Cost Multi-Path）で複数経路を持っており、通常は負荷分散する。  
しかし、**送信元が 172.16.2.0/24 のトラフィックだけは、必ず Singapore ルータ（次ホップ 172.19.1.2）経由で送りたい**。  
この「送信元条件つきの優先経路」を実現できる設定はどれか、という問題。[file:1]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 「送信元サブネット（172.16.2.0/24）＋宛先サブネット（172.20.2.0/24）で経路を変えたい」という要件なので、通常のルーティング（宛先のみで決定）ではなく **PBR（Policy-Based Routing）** が必要。 [file:1]
  - PBR は「パケットが**入ってくるインターフェイス**」に `ip policy route-map XXX` を適用して動く。  
    172.16.2.0/24 からのトラフィックは、図から Bangkok のインターフェイス Ethernet0/2 に入ってくるので、このインターフェイスに PBR を適用する必要がある。 [file:1]
  - 選択肢 A は Bangkok で以下を実施している:
    - ACL 101 で  
      `source: 172.16.2.0/24, destination: 172.20.2.0/24` をマッチ  
    - route-map PBR1 で ACL 101 に一致するトラフィックを  
      `set ip next-hop 172.19.1.2`（Singapore ルータ）へ強制送出  
    - これを **Ethernet0/2（172.16.2.0/24 からのトラフィックが入る口）** に `ip policy route-map PBR1` で適用  
    → 要件どおり、「その送信元のトラフィックだけ Singapore 経由」にできる。 [file:1]
  - B / C は Dubai 側で PBR をかけており、Bangkok での ECMP でどの出口を選ぶかという問題の解決にはならない。  
    D はルータは Bangkok だが、PBR を適用するインターフェイスが Ethernet0/1 で、これは 172.16.2.0/24 からのトラフィック流入インターフェイスではないため、マッチしてほしいトラフィックに PBR が効かない。 [file:1]

***
QUESTION 398 解説

- 問題文（英語）:  
Drag and drop the descriptions from the left onto the corresponding MPLS components on the right.  
右側に並んでいる MPLS コンポーネントは FEC, LSP, LER, LSR, LDP。  
左側の説明は以下のとおり（画像より）：  
1) routers in the core of the provider network known as P routers  
2) all traffic to be forwarded using the same path and same label  
3) routers that connect to the customer routers known as PE routers  
4) used for exchanging label mapping information between MPLS enabled routers  
5) path along which the traffic flows across an MPLS network [file:1]

- 問題文要約（日本語）:  
5つの用語（FEC / LSP / LER / LSR / LDP）と、MPLS ネットワーク内での役割説明を正しく対応付けるドラッグ＆ドロップ問題。 [file:1]

- 正しい対応:

  1) routers in the core of the provider network known as P routers  
     → **LSR**（Label Switch Router） [file:1]

  2) all traffic to be forwarded using the same path and same label  
     → **FEC**（Forwarding Equivalence Class） [file:1]

  3) routers that connect to the customer routers known as PE routers  
     → **LER**（Label Edge Router） [file:1]

  4) used for exchanging label mapping information between MPLS enabled routers  
     → **LDP**（Label Distribution Protocol） [file:1]

  5) path along which the traffic flows across an MPLS network  
     → **LSP**（Label Switched Path） [file:1]

***
この5つのうち、自分が一番あいまいに覚えていた用語はどれで、どう覚え直すと区別しやすそうですか？```
QUESTION 399 解説

- 問題文（英語）:
Routing table in Cape Town  
Refer to the exhibit. The network administrator must configure Cape Town to reach Dubai via Tokyo based on the speeds provided by the service provider. It was noticed that Cape Town is reaching Dubai directly and failed to meet the requirement. Which configuration fixes the issue? [file:1]

A. Cape Town  
   router eigrp 100  
    variance 2  

B. Cape Town  
   interface E0/0  
    bandwidth 5000  
   interface E0/1  
    bandwidth 10000  

   Dubai  
   interface E0/0  
    bandwidth 50000  
   interface E0/1  
    bandwidth 5000  

   Tokyo  
   interface E0/0  
    bandwidth 50000  
   interface E0/1  
    bandwidth 10000  

C. Dubai  
   router eigrp 100  
    variance 2  

D. Cape Town  
   interface E0/0  
    bandwidth 5000  
   interface E0/1  
    bandwidth 10000 [file:1]

- 問題文要約（日本語）:  
Cape Town から Dubai へは、本来「Cape Town → Tokyo → Dubai」という“速い経路”を使わせたいが、現状は Cape Town から Dubai 直結リンクを優先してしまっている。  
EIGRP のルーティングテーブルでは、Cape Town から見た Dubai 側のネットワークへのメトリックが、いずれも同じ値（409600）で表示されており、インタフェース帯域の違いが反映されていない。  
サービスプロバイダから提示された回線速度に基づいて EIGRP のパス選択をさせるには、どの設定が必要かを問う問題。[file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - Cape Town のルーティングテーブルでは、Dubai・Tokyo 側の 192.168.x.0/24 いくつかのネットワークに対して、すべて EIGRP メトリックが 409600 と同一値になっている。  
    これは、Cape Town 側の 2 本のインタフェース（E0/0 と E0/1）の `bandwidth` 設定が同じ（おそらく Ethernet のデフォルト帯域）だから、EIGRP がどちらも“同じ速さ”とみなしていることを意味する。[file:1]
  - EIGRP のメトリックは主に「帯域と遅延」に依存するため、サービスプロバイダが提示するリンク速度に合わせて、**ルートを学習する各ルータのインタフェース帯域**を正しく設定してやる必要がある。[file:1]
  - 選択肢 B は、Cape Town・Dubai・Tokyo のそれぞれのインタフェースに対して、SP が提供するリンク速度に応じた `bandwidth` 値を設定している。  
    これにより、Cape Town から見た  
    - 直結の Dubai 経路  
    - Tokyo 経由の Dubai 経路  
    で、メトリックに差が付き、「Tokyo 経由の方が速い」と評価されるようになる。[file:1]
  - D は Cape Town のインタフェースだけ帯域を変えているが、Dubai や Tokyo 側のインタフェース帯域がデフォルトのままだと、ネットワーク全体で正しいメトリックにならない。  
    A / C の `variance` は“負荷分散の幅”を変えるものであり、今回必要なのは「メトリックの正しい計算」であって、ECMP 幅の調整ではない。 [file:1]

***
QUESTION 400 解説

- 問題文（英語）:
Refer to the exhibit. An engineer configured NetFlow but cannot receive the flows from R1. Which two configurations resolve the issue? (Choose two.) [file:1]

A. R3(config)#ip access-list extended DDOS  
   R3(config-ext-nacl)#5 permit udp any host 10.66.66.66 eq 1090  

B. R4(config)#ip access-list extended DDOS  
   R4(config-ext-nacl)#5 permit udp any host 10.66.66.66 eq 1090  

C. R1(config)#flow exporter FlowExporter1  
   R1(config-flow-exporter)#destination 10.66.60.66  

D. R3(config)#flow exporter FlowExporter1  
   R3(config-flow-exporter)#destination 10.66.66.66  

E. R4(config)#flow exporter FlowExporter1  
   R4(config-flow-exporter)#destination 10.66.66.66 [file:1]

- 問題文要約（日本語）:  
R1 で NetFlow を設定したが、コレクタ（10.66.66.66:1090）にフローが届かない。  
R1 では exporter/monitor/インタフェース適用は正しく設定済みだが、途中経路の R3 と R4 には DoS 対策用 ACL「DDOS」が適用されており、NetFlow の UDP 1090 パケットがブロックされている。  
どの設定を追加すれば、R1 からの NetFlow フローがコレクタに届くようになるかを問う問題。[file:1]

- 正解: **A, B**

- 解説（なぜ A と B が正しいか）

  - 展示の設定では、R1 側の NetFlow exporter などは既に正しく  
    `destination 10.66.66.66` と設定されているため、C のような「宛先を 10.66.60.66 にする」設定は逆に誤りとなる。[file:1]
  - コレクタに届かない原因は、経路途中の R3・R4 のインタフェースに適用されている ACL「DDOS」が、  
    UDP 1090 宛てトラフィックを許可していないためであり、NetFlow の UDP パケットがドロップされている。 [file:1]
  - したがって、R3 と R4 の両方で ACL DDOS に  
    `permit udp any host 10.66.66.66 eq 1090`  
    を追加し、NetFlow フロー（UDP 1090 → 10.66.66.66）だけを通過させる必要がある。  
    これが選択肢 A と B の内容であり、2 箇所とも修正しないとどちらかの経路で依然ブロックされる。 [file:1]
  - D / E は exporter を R3/R4 側にも作る設定だが、問題は「R1 からのフローが途中で ACL にブロックされていること」であり、R3/R4 を exporter にすることでは解決しない。 [file:1]

***
QUESTION 401 解説

- 問題文（英語）:
Refer to the exhibit. The Customer Edge router wants to use AS 100 as the preferred ISP for all external routes and ISP-2 as a backup.

Customer-Edge  
route-map SETAS  
 set as-path prepend 111  
!  
router bgp 64555  
 neighbor 192.168.1.1 remote-as 100  
 neighbor 192.168.2.2 remote-as 200  
 neighbor 192.168.2.2 route-map SETAS in  

After this configuration, all the backup routes have disappeared from the BGP table on the Customer Edge router. Which set of configurations solves the issue on the Customer Edge router? [file:1]

A. route-map SETAS  
    set as-path prepend 111  
   router bgp 64555  
    neighbor 192.168.1.1 remote-as 100  
    neighbor 192.168.2.2 remote-as 200  
    neighbor 192.168.2.2 route-map SETAS out  

B. route-map SETAS  
    set as-path prepend 200  
   router bgp 64555  
    neighbor 192.168.1.1 remote-as 100  
    neighbor 192.168.2.2 remote-as 200  
    neighbor 192.168.2.2 route-map SETAS in  

C. route-map SETAS  
    set as-path prepend 111  
   router bgp 64555  
    neighbor 192.168.2.2 remote-as 100  
    neighbor 192.168.1.1 remote-as 200  
    neighbor 192.168.2.2 route-map SETAS in  

D. route-map SETAS  
    set as-path prepend 200  
   router bgp 64555  
    neighbor 192.168.1.1 remote-as 100  
    neighbor 192.168.2.2 remote-as 200  
    neighbor 192.168.2.2 route-map SETAS out [file:1]

- 問題文要約（日本語）:  
Customer Edge ルータ（AS 64555）は、外部プレフィックスに対して「ISP-1（AS 100）を優先」「ISP-2（AS 200）はバックアップ」として使いたい。  
現状、CE は ISP-1 と ISP-2 の両方と eBGP ピアを張っているが、ISP-2 から受け取る経路に対して AS-path prepend を行うルートマップを **inbound** で適用したところ、「バックアップ用に残っていてほしい ISP-2 経路が BGP テーブルから消えてしまった」という状況になっている。  
どの設定にすれば、ISP-1 を優先しつつ、ISP-2 経路もバックアップとして BGP テーブルに残せるか、を問う問題。[file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - CE ルータ視点で「どちらの ISP 経路を優先するか」を決める属性の一つが **AS-path 長**。  
    自分が ISP から**受信する**経路に対して AS-path を長く（＝劣後）したい場合は、**in 方向の route-map** で prepend する。 [file:1]
  - ここで prepend すべき AS 番号は、通常「**隣接 ISP の AS 番号**」を使うことが推奨される。  
    このケースでは ISP-2 の AS は 200 なので、`set as-path prepend 200` とするのが自然。 [file:1]
  - よって、正しい構成は:
    - ISP-2（192.168.2.2, AS 200）から**受信するルート**に対し
    - `set as-path prepend 200` を **inbound** で適用する  
    → これが選択肢 B の設定であり、ISP-1 からの経路より AS-path が長くなり CE では ISP-1 経路が優先される一方、ISP-2 経路もバックアップとして BGP テーブルに残る。 [file:1]
  - A / D のように route-map を **outbound** に適用すると、「CE から ISP-2 へ広告する経路の AS-path」を変えるだけで、CE 自身の経路選択には効かない。  
    C は remote-as の組み合わせが誤りで、ISP の AS 番号が入れ替わってしまっているため、要件を満たさない。 [file:1]

***
QUESTION 402 解説

- 問題文（英語）:  
A network administrator is troubleshooting a failed AAA login issue on a Cisco Catalyst c3560 switch. When the network administrator tries to log in through SSH using TACACS+ username and password credentials, the switch is no longer authenticating and is failing back to the local account. Which action resolves this issue? [web:54]

A. Configure ip tacacs-server source-interface GigabitEthernet 1/1.  
B. Configure ip tacacs source-ip 192.168.100.55.  
C. Configure ip tacacs-server source-ip 192.168.100.55.  
D. Configure ip tacacs source-interface GigabitEthernet 1/1.  

- 問題文要約（日本語）:  
Catalyst 3560 スイッチで、TACACS+ を使った SSH ログイン AAA 認証がうまくいかず、TACACS+ ではなくローカルアカウントで認証されてしまっている。  
TACACS+ サーバには到達できる前提で、「どのコマンドを入れればこの問題を直せるか」を聞いている。 [web:54]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - Catalyst 3560 では、TACACS+ パケットの送信元インタフェースを指定する正しい構文は  
    `ip tacacs source-interface <interface>`  
    であり、`ip tacacs-server ...` や `source-ip` という構文はサポートされていない。 [web:54][web:56]
  - 試験の解説どおり、`ip tacacs ?` と打つと `source-interface` だけが補完候補として出るため、  
    有効なコマンドは D の  
    `ip tacacs source-interface GigabitEthernet1/1`  
    だけである。 [web:56]
  - TACACS+ サーバ側で「どの IP からのリクエストを受けるか」を ACL やクライアント設定で絞っている場合、  
    スイッチの送信元 IP が想定と違うと認証要求自体が受け付けられない。  
    送信元インタフェースを正しく指定することで、サーバ側設定と一致し、AAA 認証が正常に通るようになる。 [web:60]

***
QUESTION 403 解説

- 問題文（英語）:  
A network engineer must configure a DMVPN network so that a spoke establishes a direct path to another spoke if the two must send traffic to each other. A spoke must send traffic directly to the hub if required. Which configuration meets this requirement? [web:72][web:74]

A. （hub に nhs, spokes に redirect）  
B. （nhs / map / multicast の組み合わせが不正）  
C. At the hub router:  
   interface tunnel10  
    ip nhrp map multicast dynamic  
    ip nhrp redirect  
    tunnel mode gre multipoint  
   On the spokes router:  
   interface tunnel10  
    ip nhrp map multicast dynamic  
    ip nhrp shortcut  
    tunnel mode gre multipoint  

D. （mode gre multicast などの誤りを含む）

- 問題文要約（日本語）:  
DMVPN で「基本的にはハブ経由だが、スポーク同士が通信を開始したらスポーク間で直接トンネル（ショートカット）を張る」動きをさせたい、つまり DMVPN Phase 3 の挙動を実現したい。  
どの構成なら、最初はハブ経由でルーティングしつつ、必要に応じて spoke-to-spoke のダイレクトパスが自動で張られるかを問う。 [web:72][web:74]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - DMVPN Phase 3 のポイントは次の 2 つ。 [web:72][web:74][web:76]
    - ハブ側トンネル: `ip nhrp redirect`  
      → ハブが「この宛先はあのスポークに直接行った方がいいよ」という **NHRP redirect** を送る。
    - スポーク側トンネル: `ip nhrp shortcut`  
      → スポークが hub からの redirect を受け入れ、宛先スポークへの **ショートカットトンネル** を張る。
  - さらに、`ip nhrp map multicast dynamic` により、NHRP がマルチキャストを含むマッピングを自動生成し、mGRE 上で複数スポークを柔軟に扱えるようにする。 [web:70][web:78]
  - 選択肢 C は:
    - Hub: `ip nhrp map multicast dynamic` と `ip nhrp redirect` を設定し、Phase 3 のハブ要件を満たす。
    - Spoke: `ip nhrp map multicast dynamic` と `ip nhrp shortcut` を設定し、redirect を受けて spoke 間トンネルを張れるようにする。
    - どちらも `tunnel mode gre multipoint`（mGRE）で DMVPN に適したモードになっている。 [web:74][web:76]
  - 他の選択肢は、`ip nhrp nhs` と `redirect/shortcut` の場所が逆だったり、`tunnel mode gre multicast` という不正なモード指定など、コマンドや役割の組み合わせがおかしいため、要件と IOS の仕様を同時に満たさない。 [web:69][web:79]

***
QUESTION 404 解説

- 問題文（英語）:  
Refer to the exhibit. The network engineer configured the summarization of the RIP routes into the OSPF domain on R5 but still sees four different 172.16.0.0/24 networks on R4. Which action resolves the issue? [file:1]

A. R4(config)#router ospf 1  
   R4(config-router)#no area  
   R4(config-router)#summary-address 172.16.0.0 255.255.252.0  

B. R4(config)#router ospf 99  
   R4(config-router)#network 172.16.0.0 0.255.255.255 area 56  
   R4(config-router)#summary-address 172.16.0.0 255.255.252.0  

C. R5(config)#router ospf 1  
   R5(config-router)#no area  
   R5(config-router)#summary-address 172.16.0.0 255.255.252.0  

D. R5(config)#router ospf 99  
   R5(config-router)#network 172.16.0.0 0.255.255.255 area 56  
   R5(config-router)#summary-address 172.16.0.0 255.255.252.0 [file:1]

- 問題文要約（日本語）:  
R5 は RIP ルートを OSPF ドメインに再配布している ASBR。  
R5 で 172.16.0.0/16 のサマリを設定したつもりだが、R4 からは依然として 172.16.0.0/24 が 4 本（/24×4）見えており、期待する 172.16.0.0/22 のサマリ 1 本になっていない。  
どこで、どのコマンドを使ってサマリを行えば、R4 から 172.16.0.0/22 だけが見えるようになるかを問う問題。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - OSPF でのサマリには大きく 2 種類ある。 [file:1]
    - ABR でエリア間ルートをまとめる `area X range`  
    - ASBR で**外部ルート（redistribute されたルート）**をまとめる `summary-address`
  - 問題の 172.16.0.0/24×4 は「RIP から OSPF に再配布された外部ルート」なので、  
    **再配布している ASBR（R5）で `summary-address` を使ってまとめる必要がある**。 [file:1]
  - しかし、R5 には `area ... range ...` が設定されており、これは「あるエリア内の OSPF ルート」をまとめるコマンドであって、「RIP から再配布された外部ルート」には効かない。  
    そのため、R4 には依然として 4 つの /24 が見えてしまう。 [file:1]
  - 適切な対応は:
    - R5 の OSPF プロセスで、既存の `area ... range` を削除した上で  
      `summary-address 172.16.0.0 255.255.252.0`  
      を設定すること。  
    → これが選択肢 C の内容であり、ASBR で外部ルートを 172.16.0.0/22 に要約し、R4 にはそのサマリ 1 本だけが見えるようになる。 [file:1]
  - A・B・D は R4 側でサマリしようとしたり、OSPF プロセス番号やエリアの前提が問題文と合っていないため、外部ルートを正しく要約できない。 [file:1]

***
QUESTION 405 解説

- 問題文（英語）:  
Refer to the exhibit. An engineer configured route exchange between two different companies for a migration project. EIGRP routes were learned in router C, but no OSPF routes were learned in router A. Which configuration allows router A to receive OSPF routes? [file:1]

A. (config-router-af-topology)#redistribute connected  
B. (config-router-af-topology)#redistribute ospf 10 metric 1000000 10 255 1 1500  
C. (config-router-af-topology)#no redistribute ospf 10 match external 1 external 2 metric 1000000 10 255 1 1500  
D. (config-router-af-topology)#redistribute ospf 10 1000000 10 255 1 1500  

- 問題文要約（日本語）:  
2 社間のマイグレーション用に、ルータ B で OSPF と EIGRP 間の経路再配布を設定した。  
Router C では EIGRP 経路は学習できているが、Router A 側（EIGRP ドメイン側）で OSPF の経路が一切学習できていない。  
EIGRP address-family topology モードで、どの再配布コマンドを入れれば Router A が OSPF 経路を受け取れるようになるか、という問題。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - Router B は「OSPF（プロセス 10）から EIGRP への再配布」を行う必要があるが、**現在の再配布設定では OSPF 内部ルートが EIGRP に出ていない**。 [file:1]
  - EIGRP named/af-topology で OSPF を再配布するときは、`redistribute ospf 10 metric <bw> <delay> <reliability> <load> <mtu>` のように、**metric キーワードと 5 つのメトリック値**を明示しないとルートが再配布されない。 [file:1]
  - 選択肢 B はこの形式を満たしており、  
    `redistribute ospf 10 metric 1000000 10 255 1 1500`  
    で OSPF ルートを適切な EIGRP メトリックを伴って再配布できるため、Router A に OSPF 経路が届くようになる。 [file:1]
  - A の connected 再配布では OSPF ルートが EIGRP に渡らない。  
    C は `no redistribute ...` なので、逆に OSPF 再配布を無効化してしまう。  
    D は metric キーワードが抜けた誤った構文であり、IOS に受け付けられない。 [file:1]

***
QUESTION 406 解説

- 問題文（英語）:  
Refer to the exhibit. The network administrator configures the Chicago router to mutually redistribute the LA and NewYork routes with EIGRP routes to be summarized as a single route in OSPF:

router eigrp 100  
 redistribute ospf 1 metric 10 10 10 10 10  
router ospf 1  
 redistribute eigrp 100 subnets  
 area 0 range 192.168.4.0 255.255.252.0  

After the configuration, the LA router receives all the specific NewYork routes except the summary route. Which set of configurations resolve the issue on the Chicago router? [file:1]

A. router ospf 1  
   summary-address 192.168.4.0 255.255.252.0  

B. router ospf 1  
   area 0 range 192.168.4.0 255.255.0.0  

C. interface E 0/0  
   summary-address 192.168.4.0 255.255.252.0  

D. interface E 0/0  
   ip summary-address eigrp 100 192.168.4.0 255.255.252.0  

- 問題文要約（日本語）:  
Chicago ルータで LA 側（EIGRP）と NewYork 側（OSPF）を相互再配布している。  
Chicago では OSPF 側で `area 0 range 192.168.4.0 255.255.252.0` を設定し、「EIGRP から再配布されたルートを 192.168.4.0/22 の 1 本に要約したつもり」だが、LA ルータでは NewYork の個別ルートだけが届き、サマリルートが見えない。  
ASBR（Chicago）でどのコマンドを入れれば、LA 側に 192.168.4.0/22 のサマリ OSPF ルートが届くかを問う問題。 [file:1]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - OSPF のサマリには 2 種類ある。 [file:1]
    - ABR でエリア間ルート（inter-area）をまとめる `area X range`  
    - ASBR で**外部ルート（external：他プロトコルから再配布されたルート）**をまとめる `summary-address`
  - 今回 Chicago は「EIGRP を OSPF に再配布している ASBR」であり、192.168.4.x のルートは OSPF 外部ルート（Type 5）として扱われる。  
    → これらをサマリするには **ASBR 上で `summary-address` を使う必要があり、`area 0 range` では外部ルートは要約されない。** [file:1]
  - したがって、Chicago の OSPF プロセスに次を追加するのが正解。  
    `router ospf 1`  
    ` summary-address 192.168.4.0 255.255.252.0`  
    これにより、EIGRP から再配布された外部ルート群が 192.168.4.0/22 として LA 側に広告される。 [file:1]
  - B はマスクもおかしく、かつ `area range` なので依然として外部ルートには効かない。  
    C は OSPF の構文として不正。  
    D は EIGRP 側のインターフェイスサマリであり、「OSPF での外部ルート要約」という要件を満たさない。 [file:1]

***
QUESTION 407 解説

- 問題文（英語）:  
Refer to the exhibit. R1 cannot authenticate via TACACS. Which configuration resolves the issue? [file:1]

A. tacacs server DC1_TACACS  
   address ipv4 10.60.66.66  
   key D@t@c3nter1TACACS  

B. aaa group server tacacs+ DC1_TACACS  
   server name DC_TACACS  

C. tacacs server DC1_TACACS  
   address ipv4 10.66.66.66  
   key D@t@c3nter1TACACS  

D. aaa group server tacacs+ DC_TACACS  
   server name DC_TACACS  

- 問題文要約（日本語）:  
R1 から TACACS+ 認証をしようとしても失敗している。  
ログとコンフィグ（exhibit）から、TACACS サーバのホスト名やキーワードは合っているが、R1 上の `tacacs server DC1_TACACS` の定義に TACACS サーバの正しい IP アドレスが入っていない。  
どの設定を入れれば、R1 が正しい TACACS サーバ 10.66.66.66 に到達できるようになり、認証が成功するかを問う。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - exhibit 上の R1 の設定では、TACACS サーバ名 `DC1_TACACS` は定義されているが、**`address ipv4 10.66.66.66` が設定されておらず、サーバ IP が不完全**になっている。  
    その結果、R1 は実際の TACACS サーバ IP アドレス（10.66.66.66）にパケットを送れていない。 [file:1]
  - 選択肢 C は、サーバ名 `DC1_TACACS` に対して正しい IP アドレス 10.66.66.66 と既存のキー `D@t@c3nter1TACACS` を関連付けているため、R1 から TACACS サーバへの到達性が確立され、認証が動作する。 [file:1]
  - A は IP アドレスが 10.60.66.66 で誤っている。  
    B と D は AAA server-group 側の設定だが、**根本原因はサーバ定義の IP 欠落**なので、グループ設定だけを触っても TACACS サーバには届かない。 [file:1]

***
QUESTION 408 解説

- 問題文（英語）:  
What must be configured by the network engineer to circumvent AS_PATH loop prevention mechanism in IP/VPN Hub and Spoke deployment scenarios? [web:83][web:89]

A. Use allowas-in and as-override at all PEs  
B. Use allowas-in and as-override at the PE_Hub  
C. Use allowas at the PE_Hub  
D. Use as-override at the PE_Hub  

- 問題文要約（日本語）:  
IP VPN の Hub&Spoke 構成で、複数スポーク CE が同じカスタマ AS を使っていると、CE 間で経路を流すときに AS_PATH に自分の AS が現れて BGP のループ防止に引っかかる。  
この「AS_PATH ループ防止」を回避して、Hub 経由でスポーク同士の経路をちゃんと届けるには、どこで allowas-in と as-override を組み合わせればよいか、という問題。 [web:89][web:90]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - ループ防止に引っかかる典型パターン:  
    - Spoke CE1(AS 65000) → PE_Hub → Spoke CE2(AS 65000)  
    - CE2 から見ると、AS_PATH に自分の AS 65000 が含まれているので、通常はルートを破棄する。 [web:89]
  - 回避する代表的な 2 つの仕組み: [web:89]
    - **as-override（PE 側）**: PE が CE の AS を自分の Provider AS に書き換えて広告し、CE から見て「自 AS が AS_PATH に出てこない」ようにする。  
    - **allowas-in（CE 側）**: CE が「AS_PATH 内に自分の AS があっても許可して受け入れる」ようにする。
  - Hub&Spoke の IP VPN では、Hub PE で両方を組み合わせることで、Hub 経由のスポーク間経路が AS_PATH ループ防止に引っかからないようにできる、という想定で出題されている。  
    → そのため、「Hub の PE で allowas-in と as-override の両方を使う」B が問題の意図に最も沿った答えとして扱われている。 [web:88][web:91]
  - A（全 PE）だと要件以上に広く設定する形になり、試験の選択肢としては過剰。  
    C は allowas-in だけで、PE 側で AS の書き換えが行われない。  
    D は as-override だけで、「自 AS が AS_PATH に残るケースを許可する」という観点が欠けている。 [web:89]

***
QUESTION 409 解説

- 問題文（英語）:  
Refer to the exhibit. An administrator can log in to the device using Telnet, but the attempts to log in to the same device using SSH with the same credentials fail. Which action resolves this issue? [file:1]

A. Configure transport input all on the VTY lines to allow SSH.  
B. Configure the VTY lines with login local.  
C. Configure to use the Telnet user database for SSH as well.  
D. Configure SSH service on the router.  

- 問題文要約（日本語）:  
管理者は Telnet ではログインできるが、同じユーザ名・パスワードで SSH ログインしようとすると失敗する。  
コンソール出力（exhibit）から、SSH 自体は有効化されているが、VTY での認証方法が「パスワードのみ前提」になっており、**SSH が必須とする “ユーザ名＋パスワード” の認証と噛み合っていない**状況。  
どの設定を入れれば SSH でも正しくログインできるかを問う。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - Telnet は `line vty` に `password xxx` と `login` があれば、「ユーザ名なし・パスワードのみ」でログイン可能。  
    そのため、現状でも Telnet は成功している。 [file:1]
  - SSH は必ず「ユーザ名＋パスワード」の組み合わせでログインするため、  
    VTY ライン側で「どのユーザ DB を使うか」を明示的に指定してやる必要がある。  
    もっともシンプルなのが `login local` で、ローカルユーザデータベース（`username xxx secret yyy` など）を使って VTY ログインさせる方法。 [file:1]
  - 選択肢 B の `login local` を VTY ラインに設定すれば、Telnet/SSH ともに **同じローカルユーザ名・パスワード**で認証されるようになり、SSH ログインも成功する。 [file:1]
  - A はプロトコル（telnet/ssh）の許可だけで、ユーザ名/パスワード DB の問題を解決しない。  
    C は抽象的で、具体的な IOS コマンドとして成立していない。  
    D は既に SSH が動作している前提（鍵生成済みなど）なので、根本原因ではない。 [file:1]

***
QUESTION 410 解説

- 問題文（英語）:  
An engineer must override the normal routing behavior of a router. The engineer must send HTTP traffic that is destined to 10.100.100.100 from 10.1.1.0/24 via a next hop of 10.2.2.2, two hops away from the router that is connected to the 10.1.1.0/24 subnet. Which configuration reroutes traffic according to this requirement?

A. access-list 100 permit tcp 10.1.1.0 0.0.0.255 host 10.100.100.100 eq http  
   !  
   route-map POLICY permit 10  
    match ip address 100  
    set ip next-hop recursive 10.2.2.2  

B. access-list 100 permit tcp 10.1.1.0 0.0.0.255 host 10.100.100.100 eq http  
   !  
   route-map POLICY permit 10  
    match ip address 100  
    set ip next-hop 10.2.2.2  
   route-map POLICY permit 20  

C. access-list 100 permit tcp 10.1.1.0 0.0.0.255 host 10.100.100.100 eq http  
   !  
   route-map POLICY permit 10  
    match ip address 100  
    set ip next-hop recursive 10.2.2.2  
   route-map POLICY permit 20  

D. access-list 100 permit tcp 10.1.1.0 0.0.0.255 host 10.100.100.100 eq http  
   !  
   route-map POLICY permit 10  
    match ip address 100  
    set ip next-hop 10.2.2.2 [web:90]

- 問題文要約（日本語）:  
通常のルーティングを上書きして、10.1.1.0/24 から 10.100.100.100 への **HTTP トラフィックだけ** を、2 ホップ先のネクストホップ 10.2.2.2 に送る必要がある。  
10.2.2.2 はこのルータに直接接続されていない（2 ホップ先）ので、PBR（Policy-Based Routing）で “再帰的に” 次ホップを解決しつつ、対象トラフィックだけを振り向けたい。どの設定が要件を満たすか、という問題。 [web:90]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - まず「ソースと宛先とポートで経路を変えたい」ので、通常のルーティングではなく **PBR（route-map + ACL）** が必要になる。  
    → ACL 100 で「10.1.1.0/24 → 10.100.100.100 の HTTP トラフィック」をマッチ。 [web:90]
  - 次ホップ 10.2.2.2 は **直接接続でない**（2 ホップ先）ため、PBR で `set ip next-hop` を使う場合は `recursive` キーワードが必要。  
    `set ip next-hop recursive 10.2.2.2` により、「まず 10.2.2.2 へのルートをルーティングテーブルから探して、その次ホップへ実際の転送を行う」動作になる。 [web:69]
  - 選択肢 A は:
    - ACL で対象トラフィックを正しく絞り込み  
    - route-map でそのトラフィックだけに対し  
    - `set ip next-hop recursive 10.2.2.2` を適用  
    しており、要件通り「HTTP のみを 2 ホップ先経由で送る」設定になる。 [web:90]
  - B と D は `recursive` がなく、「10.2.2.2 が直接接続されている」前提の設定になっており、この問題の前提を満たさない。  
    C は `recursive` は正しいが、`route-map POLICY permit 20` により「その他すべてのトラフィック」も PBR の対象になってしまうため、想定より広く PBR を適用してしまう。 [web:69][web:90]

***
QUESTION 411 解説

- 問題文（英語）:  
Refer to the exhibit. The company implemented uRPF to address an antispoofing attack. A network engineer received a call from the IT security department that the regional data center is under an IP spoofing attack. Which configuration must be implemented on R1 to resolve this issue?

A. interface ethernet0/1  
   ip unicast RPF check reachable-via any allow-default allow-self-ping  

B. interface ethernet0/0  
   ip verify unicast reverse-path  

C. interface ethernet0/1  
   ip verify unicast reverse-path  

D. interface ethernet0/0  
   ip unicast RPF check reachable-via any allow-default allow-self-ping [file:1]

- 問題文要約（日本語）:  
企業は IP スプーフィング対策として uRPF を導入済み。  
ログ（exhibit）から、R1 では e0/0 には uRPF チェックが有効だが、**攻撃トラフィックが入ってくる e0/1 側には uRPF が有効化されていない**ことが分かる。  
リージョナル DC に向かうスプーフィング攻撃を防ぐには、どのインターフェイスでどのコマンドを有効にすべきかを問う。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - exhibit の説明では、  
    - e0/0: uRPF（ip verify unicast）がすでに有効  
    - e0/1: uRPF が無効  
    となっており、**攻撃は e0/1 から入ってきている**前提で問題が書かれている。 [file:1]
  - Cisco IOS では、uRPF（Unicast Reverse Path Forwarding）を有効にする代表的なコマンドが  
    `ip verify unicast reverse-path`（旧書式）または `ip verify unicast source reachable-via rx/any`（新書式）で、  
    いずれもインターフェイス単位で設定する。 [file:1]
  - よって、**攻撃が入るインターフェイス e0/1 に対して `ip verify unicast reverse-path` を設定する** C が要件を満たす。  
    これにより、R1 は「その送信元 IP に対する戻り経路が FIB 上に存在しないパケット」を e0/1 で破棄するようになり、スプーフィングを防止できる。 [file:1]
  - A と D は `ip unicast RPF check reachable-via any ...` という別構文で、問題文のヒント（`ip verify unicast ?` の出力）とズレている。  
    B は e0/0 に再度 uRPF をかけるだけで、攻撃入口である e0/1 を守れない。 [file:1]

***
QUESTION 412 解説

- 問題文（英語）:  
What is a characteristic of IPv6 RA Guard?

A. RA messages are allowed from the host port to the switch.  
B. It is unable to protect tunneled traffic  
C. It filters rogue RA broadcasts from connected hosts  
D. It is supported on the egress direction of the switch. [web:78]

- 問題文要約（日本語）:  
IPv6 RA Guard の「特徴」を 1 つ選ぶ問題。  
RA Guard は、ホストから勝手に送られる不正な Router Advertisement（RA）を止めたい場面で使われる L2 スイッチの機能。 [web:78]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - IPv6 RA Guard は、スイッチのポートにポリシーを適用して、**ホストポートからの不正な RA をフィルタリングする**機能。  
    ルータ役のポートだけ RA を許可し、それ以外のホストポートからの RA はブロックすることで、偽ルータによる攻撃を防ぐ。 [web:78]
  - Cisco の仕様上の制限として、  
    - トンネルされた IPv6 トラフィック内の RA には効かない（B も事実としては正しい）  
    - 入力方向（ingress）だけサポートで、出力方向（egress）はサポートしない（D は誤り） [web:78]
  - 選択肢ごとの整理:
    - A: host-role が設定されたポートでは RA は**許可されず**、むしろ禁止されるので誤り。 [web:78]
    - B: 仕様として正しいが、「特徴」として問われたときには C の方が RA Guard の本質を直接表している。  
    - C: 「接続ホストからの rogue RA をフィルタする」という、RA Guard の主目的そのものなので正解。  
    - D: RA Guard は **ingress のみサポート** であり、egress ではないので誤り。 [web:78]

***
QUESTION 413 解説

- 問題文（英語）:  
Refer to the exhibit. An engineer applied an IPv6 traffic filter on R1. The interface flapped between R1 and R2 and clearing the BGP session did not restore the BGP session and failed. Which action must the engineer take to restore the BGP session from R2 to R1?

A. Enable the BGP session, which went down when the session was cleared.  
B. Swap the source and destination IP addresses in the IPv6 traffic filter.  
C. Apply the IPv6 traffic filter in the outbound direction on the interface.  
D. ICMPv6 must be permitted by the IPv6 traffic filter. [file:1]

- 問題文要約（日本語）:  
R1 に IPv6 トラフィックフィルタを適用したあと、R1–R2 間インターフェイスが flap し、その後 BGP セッションを clear してもセッションが復活しない。  
ACL の記述を見ると、`deny ipv6 2001:DB8::/32 any` のようなルールで **ICMPv6 を含むあらゆる IPv6 トラフィックが遮断**されており、その結果 Neighbor Discovery（NS/NA）ができず、リンクローカルの解決や隣接関係が張れない状態。  
BGP セッション復旧には、どの対応が必要かを問う。 [file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - IPv6 では ARP がなく、**Neighbor Solicitation / Neighbor Advertisement（ICMPv6）** による Neighbor Discovery が必須。  
    これがフィルタで落ちると、L3 ではお互いの MAC を解決できず、実質的に到達不能になる。 [file:1]
  - 問題の IPv6 ACL は `deny ipv6 2001:DB8::/32 any` などの行で **R2 → R1 向きの ICMPv6 も丸ごと deny** しているため、  
    - NS/NA  
    - Router Solicitation / Advertisement  
    といった制御トラフィックも含めてブロックしてしまい、BGP の TCP セッションも張り直せない。 [file:1]
  - よって、BGP セッションを復活させるには、**ACL 内で ICMPv6 を許可する行を追加**して Neighbor Discovery を通す必要がある。  
    その意図を最も端的に表している選択肢が D「ICMPv6 must be permitted by the IPv6 traffic filter」。 [file:1]
  - A は BGP プロセス側の問題を想定しているが、根本原因は L3 到達性（ICMPv6 抑止）。  
    B や C は ACL 適用向きの問題にしているが、方向や src/dst を変えても ICMPv6 を許可しない限り状況は変わらない。 [file:1]

***
QUESTION 414 解説

- 問題文（英語）:  
Refer to the exhibit. After reloading the router, an administrator discovered that the interface utilization graphs displayed inconsistencies with their previous history in the NMS. Which action prevents this issue from occurring after another router reload in the future?

A. Rediscover all the router interfaces through SNMP after the router is reloaded.  
B. Save the router configuration to startup-config before reloading the router.  
C. Configure SNMP to use static OIDs referring to individual router interfaces.  
D. Configure SNMP interface index persistence on the router. [file:1]

- 問題文要約（日本語）:  
ルータを reload したあと、NMS 上のインターフェイス利用率グラフが「過去の履歴とつながらない」状態になっている。  
原因は、reload のたびに **インターフェイスの ifIndex（SNMP のインターフェイス番号）が変わり、NMS が「別インターフェイス」と誤認識する**こと。  
次回以降の reload でもグラフが途切れないようにするにはどうするか、という問題。 [file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - SNMP では、インターフェイスは `ifIndex` という番号で識別され、NMS はこの番号に紐づいてトラフィックグラフを描く。  
    ルータ再起動のたびに ifIndex が再割り当てされると、「同じ Gi0/0 でも別の OID」と見なされ、グラフが新規として開始されてしまう。 [file:1]
  - Cisco ルータでは、`snmp-server ifindex persist`（機種によってはインターフェイスモードの `snmp ifindex persist`）を設定することで、再起動しても ifIndex が変わらないようにできる。  
    これにより、NMS 側の OID とインターフェイスが継続的に一致し、グラフ履歴も綺麗に連続する。 [file:1]
  - A は毎回再ディスカバリが必要になり、運用負荷も高く履歴も分断される。  
    B は設定保存の話で ifIndex 変動とは無関係。  
    C の「静的 OID」は現実的ではなく、ifIndex が変わる前提を解決できない。 [file:1]

***
QUESTION 415 解説

- 問題文（英語）:  
Refer to the exhibit. An engineer applies a prefix-list filter that filters most of the network 10 prefixes instead of allowing them. Which action resolves the issue?

A. Modify the ip prefix-list EIGRP seq 10 permit 10.0.0.0/8 le 9 command.  
B. Modify the ip prefix-list EIGRP seq 20 permit 10.0.0.0/8 ge 9 command.  
C. Modify the ip prefix-list EIGRP seq 10 permit 10.0.0.0/8 le 32 command.  
D. Modify the ip prefix-list EIGRP seq 20 permit 10.0.0.0/8 le 32 command. [file:1]

- 問題文要約（日本語）:  
EIGRP で受信するプレフィクスを prefix-list でフィルタし、「10.x.x.x（10.0.0.0/8 のサブネット）」だけを許可したかったが、実際には 10.0.0.0/8 そのものしか通らず、ほとんどの 10 プレフィクスが落ちてしまっている。  
既存の prefix-list の seq 10 が `permit 10.0.0.0/8`（プレフィクス長ちょうど /8）だけを許可しているため、10.1.0.0/16 などのサブネットがマッチせず deny されている。これをどう直せばよいか、という問題。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - prefix-list は「ネットワークアドレス＋プレフィクス長の範囲」でマッチを決める。  
    例えば `ip prefix-list EIGRP seq 10 permit 10.0.0.0/8` だけだと、「プレフィクス長がちょうど /8」の 10.0.0.0/8 のみ許可し、/9〜/32 の 10.x.x.x は一切マッチしない。 [file:1]
  - 10.0.0.0/8 配下のあらゆるサブネット（/9〜/32）をまとめて許可したいときは、  
    `10.0.0.0/8 le 32` のように「最大プレフィクス長」を指定してやる必要がある。  
    これにより、10.0.0.0/8 自身から 10.255.255.255/32 まで、10.0.0.0/8 の範囲に入る全サブネットがマッチする。 [file:1]
  - 選択肢 C は seq 10 を `permit 10.0.0.0/8 le 32` に修正しており、  
    既存の `seq 20 deny 0.0.0.0/0 le 32` を残したまま、「10.* は全部許可、それ以外は deny」という意図を 1 行で実現できる。 [file:1]
  - A（`le 9`）だと /8 と /9 しか許可されず、ほとんどのサブネットが依然としてフィルタされる。  
    B や D は `seq 20` 側の変更で、全体のロジックが崩れたり、10.0.0.0/8 本体を許可できなかったりするため、ベターではない。 [file:1]

***
QUESTION 416 解説

- 問題文（英語）:  
Refer to the exhibit. Which action makes 10.1.3.2 the feasible successor to reach 10.200.1.0/24?

A. Increase path bandwidth higher than 10.1.1.2 and lower than 10.1.2.2 between RtrA and the destination.  
B. Increase path bandwidth higher than 10.1.2.2 and lower than 10.1.1.2 between RtrA and the destination.  
C. Increase path bandwidth lower than 10.1.1.2 and lower than 10.1.2.2 between RtrA and the destination.  
D. Increase path bandwidth higher than 10.1.1.2 and higher than 10.1.2.2 between RtrA and the destination. [file:1]

- 問題文要約（日本語）:  
RtrA から宛先 10.200.1.0/24 へは、3 本の EIGRP 経路（次ホップ 10.1.1.2, 10.1.2.2, 10.1.3.2）がある。  
現在の successor は 10.1.1.2 へのパスで、10.1.2.2 経路も metric が近く候補になっているが、10.1.3.2 を「feasible successor」としてバックアップにしたい。  
帯域（bandwidth）変更でメトリックを調整するとして、どのような関係にすればよいかを問う。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - EIGRP の **successor** は「最も低い FD（feasible distance＝実際に使っているメトリック）」を持つ経路。  
    **feasible successor** は「RD（reported distance＝隣接ルータから見た距離）が、現 successor の FD より小さい」経路だけが候補になる（feasibility condition）。 [file:1]
  - 帯域を上げるとメトリック（遅延＋帯域ベース）が下がり、帯域を下げるとメトリックが上がる、という前提で考える。  
    「10.1.3.2 をバックアップにしたい」ので、  
    - successor（10.1.1.2）よりは遅い（メトリックが大きい）  
    - もう一方の経路（10.1.2.2）よりは速い（メトリックが小さい）  
    という“中間のコスト”にしたい。 [file:1]
  - したがって、「10.1.2.2 より帯域を高く、10.1.1.2 より帯域を低く」する、という B の条件が、  
    - successor の座は奪わず（10.1.1.2 が最小 FD のまま）  
    - もう一方より良いサブ経路として FS 候補になる  
    という問題の意図に一番合っている。 [file:1]
  - A は 10.1.3.2 の帯域が最も高くなり successor に昇格してしまう。  
    C は 3 本の中で最も悪い経路になり FS 候補としても微妙。  
    D は 10.1.3.2 が最良経路になり successor を奪ってしまう。 [file:1]

***
QUESTION 417 解説

- 問題文（英語）:  
Refer to the exhibit – RR Configuration:  

router bgp 100  
 neighbor IBGP peer-group  
 neighbor IBGP route-reflector-client  
 neighbor 10.1.1.1 remote-as 100  
 neighbor 10.1.2.2 remote-as 100  
 neighbor 10.1.3.3 remote-as 100  

The network administrator configured the network to establish connectivity between all devices and notices that the ASBRs do not have routes for each other. Which set of configurations resolves this issue?

A. router bgp 100  
   neighbor 10.1.1.1 next-hop-self  
   neighbor 10.1.2.2 next-hop-self  
   neighbor 10.1.3.3 next-hop-self  

B. router bgp 100  
   neighbor IBGP next-hop-self  

C. router bgp 100  
   neighbor IBGP update-source Loopback0  

D. router bgp 100  
   neighbor 10.1.1.1 peer-group IBGP  
   neighbor 10.1.2.2 peer-group IBGP  
   neighbor 10.1.3.3 peer-group IBGP  [file:1]

- 問題文要約（日本語）:  
RR（ルートリフレクタ）で iBGP を構成しているが、ASBR 同士の経路が相互に見えていない。  
RR では `neighbor IBGP peer-group` と `neighbor IBGP route-reflector-client` だけ設定されており、実際の隣接（10.1.1.1 / 10.1.2.2 / 10.1.3.3）は単に `remote-as 100` だけで、peer-group に紐付けられていない。どの設定を追加すれば、ASBR 間で経路が反射されるようになるかを問う。 [file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - RR として機能させるには、「RR 自身が持つ peer-group に対して route-reflector-client を付け、それに各 iBGP neighbor を参加させる」必要がある。 [file:1]
  - すでに peer-group IBGP には `route-reflector-client` が設定されているが、  
    `neighbor 10.1.x.x` 側で `peer-group IBGP` を指定していないため、これらの neighbor は RR クライアント扱いになっていない。  
    その結果、ASBR 同士へ経路反射が行われず、互いのルートが見えていない。 [file:1]
  - 選択肢 D のように、各 neighbor に `peer-group IBGP` を設定すると、その瞬間に  
    - その neighbor は peer-group IBGP のメンバー  
    - かつ route-reflector-client  
    となり、RR からの反射経路を受け取り、ASBR 同士の経路も学習できるようになる。 [file:1]
  - A や B の `next-hop-self`、C の `update-source Loopback0` は、今回の「RR クライアントとして扱われていない」という根本原因には関係しない。 [file:1]

***
QUESTION 418 解説

- 問題文（英語）:  
The network administrator configured CoPP so that all HTTP and HTTPS traffic from the administrator device located at 172.16.1.99 toward the router CPU is limited to 500 kbps. Any traffic that exceeds this limit must be dropped.

access-list 100 permit ip host 172.16.1.99 any  
class-map CM-ADMIN  
 match access-group 100  
policy-map PM-COPP  
 class CM-ADMIN  
  police 500000 conform-action transmit  
interface E0/0  
 service-policy input PM-COPP  

CoPP failed to capture the desired traffic and the CPU load is getting higher. Which two configurations resolve the issue? (Choose two.)

A. no access-list 100  
   access-list 100 permit tcp host 172.16.1.99 any eq 80  
   access-list 100 permit tcp host 172.16.1.99 any eq 443  

B. no access-list 100  
   access-list 100 permit tcp host 172.16.1.99 any eq 80  

C. policy-map PM-COPP  
   class CM-ADMIN  
    no police 500000 conform-action transmit  
    police 500 conform-action transmit  
   !  
   control-plane  
    service-policy input PM-COPP  

D. interface E0/0  
    no service-policy input PM-COPP  
   !  
   control-plane  
    service-policy input PM-COPP  

E. policy-map PM-COPP  
   class CM-ADMIN  
    no police 500000 conform-action transmit  
    police 500 conform-action transmit  [file:1]

- 問題文要約（日本語）:  
管理端末 172.16.1.99 からルータ CPU 向けの HTTP/HTTPS トラフィックだけを 500 kbps にポリシングする CoPP を入れたが、想定どおりに動作せず CPU 負荷も高いまま。  
元の設定では「E0/0 入り」のサービスポリシーかつ ACL が「host 172.16.1.99 からの全トラフィック」をマッチしており、CoPP 本来の「control-plane 向けのみを制御」「HTTP/HTTPS のみを制御」になっていない。どの 2 つの変更が必要かを問う。 [file:1]

- 正解: **A, D**

- 解説（なぜ A と D が正しいか）

  - CoPP は「CPU に向かうトラフィック」を制御する仕組みなので、**物理インターフェースではなく control-plane にポリシーを適用**する必要がある。  
    選択肢 D のように E0/0 からサービスポリシーを外し、`control-plane service-policy input PM-COPP` を設定するのが正しい。 [file:1]
  - 元の ACL 100 は `permit ip host 172.16.1.99 any` で、管理端末からのすべてのプロトコルをマッチしてしまい、HTTP/HTTPS 以外の CPU 向けトラフィックまで 500 kbps に制限してしまう。  
    選択肢 A のように、`tcp eq 80` と `tcp eq 443` に限定する ACL に変更することで、「HTTP/HTTPS のみ」をポリシング対象に絞れる。 [file:1]
  - C/E にある `police 500` は単位が bps のため、0.5 kbps になってしまい要件の 500 kbps と合致しない。  
    もともとの `police 500000` が 500 kbps を表しているので、ポリサー値を変える必要はない。B は HTTPS を含まず要件を満たさない。 [file:1]

***
QUESTION 419 解説

- 問題文（英語）:  
Refer to the exhibit. An engineer implemented CoPP but did not see OSPF traffic going through it. Which configuration resolves the issue?

A. ip access-list extended OSPF  
   permit ospf any any  

B. policy-map COPP  
   class OSPF  
   police 8000 conform-action transmit exceed-action transmit violate-action drop  

C. class-map match-all OSFP  
   match access-group name OSPF  

D. control-plane  
   service-policy input COPP  [file:1]

- 問題文要約（日本語）:  
CoPP を実装したが、OSPF トラフィックが CoPP のクラスにヒットしてこない（＝OSPF がポリシーで全く見えない）。  
既存の設定では control-plane への service-policy や class-map（OSPF）自体は入っているが、肝心の ACL 側で OSPF パケットをマッチできていない。どの設定を追加すれば、OSPF トラフィックが CoPP の対象になるかを問う。 [file:1]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - CoPP では、class-map が参照する ACL で対象プロトコルを正しくマッチさせない限り、そのトラフィックはクラスにカウントされない。 [file:1]
  - 問題の状況では、policy-map や class-map、control-plane への適用は済んでいる一方で、「OSPF トラフィックを permit する ACL エントリ」が未設定であることが出力から分かる。 [file:1]
  - 選択肢 A のように `ip access-list extended OSPF` に `permit ospf any any` を追加することで、OSPF のマルチキャスト／ユニキャストに関わらず、すべての OSPF パケットを ACL でマッチさせられるようになり、class-map OSPF → policy-map COPP → CoPP で正しく制御される。 [file:1]
  - B/C/D の内容は既に構成済みの要素であり（policy-map, class-map, control-plane への適用）、OSPF がヒットしない根本原因（ACL 未整備）には関係しない。 [file:1]

***
QUESTION 420 解説

- 問題文（英語）:  
Refer to the exhibit. A network engineer is provisioning end-to-end traffic service for two different enterprise networks with these requirements:  
- The OSPF process must differ between customers on HQ and Branch office routers, and adjacencies should come up instantly.  
- The enterprise networks are connected with overlapping networks between HQ and a Branch office.  

Which configuration meets the requirements for a customer site?

A. ISP(config)#int f3/0  
   ISP(config-if)#ip vrf forwarding EA  
   ISP(config-if)#description TO->EA2_Branch  
   ISP(config-if)#ip address 172.16.200.2 255.255.255.0  
   ISP(config-if)#no shut  

B. ISP(config)#int f2/0  
   ISP(config-if)#ip vrf forwarding EA  
   ISP(config-if)#description TO->EA1_HQ  
   ISP(config-if)#ip address 172.16.100.2 255.255.255.0  
   ISP(config-if)#no shut  

C. ISP(config)#int f0/0  
   ISP(config-if)#ip vrf forwarding EB  
   ISP(config-if)#description TO->EB1_HQ  
   ISP(config-if)#ip address 172.16.100.2 255.255.255.0  
   ISP(config-if)#no shut  

D. ISP(config)#int f1/0  
   ISP(config-if)#ip vrf forwarding EA  
   ISP(config-if)#description TO->EA2_Branch  
   ISP(config-if)#ip address 172.16.200.2 255.255.255.0  
   ISP(config-if)#no shut  [file:1]

- 問題文要約（日本語）:  
ISP ルータ上で 2 社（EA / EB）のエンタープライズネットワーク向けに end-to-end サービスを提供したい。  
要件は  
1) HQ ルータと Branch ルータで、顧客ごとに異なる OSPF プロセスを使う（＝VRF ごとに別 OSPF）  
2) HQ と Branch 間でアドレスが重複するので VRF で分離する  
というもの。図では、EA 用 OSPF（プロセス 100）にぶら下がるのは ISP 側 f0/0・f1/0、EB 用 OSPF（プロセス 200）は f2/0・f3/0 と示されている。どのインターフェース設定が「ある顧客サイト」に対して要件を満たすかを問う。 [file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 図から、VRF EA に属し OSPF プロセス 100 に参加すべき ISP 側インターフェースは f0/0 と f1/0 である。VRF EB／OSPF 200 側は f2/0 と f3/0。 [file:1]
  - Branch 側 EA2 と接続するリンクは 172.16.200.0/24 であり、ISP ルータ側 IP は 172.16.200.2/24、インターフェースは f1/0 と示されている。  
    選択肢 D はまさに f1/0 を VRF EA に割り当て、説明文も「TO->EA2_Branch」、アドレスも 172.16.200.2/24 と一致している。 [file:1]
  - A/B は f2/0・f3/0 を VRF EA に割り当てており、これらは OSPF 200／VRF EB 用として使うべきインターフェースなので不適切。  
    C は f0/0 を VRF EB にしており、本来 EA／OSPF 100 側に属するインターフェースを誤って別 VRF に入れてしまっている。 [file:1]

***
QUESTION 421 解説

- 問題文（英語）:  
Refer to the exhibit. An engineer is troubleshooting failed access by contractors to the business application server via Telnet or HTTP during the weekend. Which configuration resolves the issue?

A. R1  
   no access-list 101 permit tcp 10.3.3.0 0.0.0.255 host 10.1.1.3 eq telnet time-range Contractor  

B. R4  
   no access-list 101 permit tcp 10.3.3.0 0.0.0.255 host 10.1.1.3 eq telnet time-range Contractor  

C. R1  
   time-range Contractor  
    no periodic weekdays 8:00 to 16:30  
    periodic daily 8:00 to 16:30  

D. R4  
   time-range Contractor  
    no periodic weekdays 8:00 to 16:30  
    periodic daily 8:00 to 16:30  [file:1]

- 問題文要約（日本語）:  
請負業者ネットワーク（10.3.3.0/24）から業務アプリケーションサーバ（10.1.1.3）へ Telnet/HTTP 接続できるのは、現在「平日の 8:00–16:30 のみ」と ACL＋time-range で制御されている。  
しかし要件としては「週末も同じ時間帯にアクセス可能」にしたい。  
ACL は R1 で実際にインターフェースに適用されており、R4 側の同名 ACL は有効に使われていない。どこで time-range をどう直せばよいか、という問題。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - R1 の ACL 101 では、`time-range Contractor` が「periodic weekdays 8:00 to 16:30」として定義されているため、週末（土日）はそもそもマッチせず、Telnet/HTTP が許可されない。 [file:1]
  - 週末も含めて毎日 8:00–16:30 の間だけ許可したいので、time-range を  
    - 既存の `periodic weekdays 8:00 to 16:30` を削除し  
    - 代わりに `periodic daily 8:00 to 16:30` に変更する  
    必要がある。選択肢 C がその操作を R1 上で行っている。 [file:1]
  - R4 側の time-range を変えても、ACL 101 がインターフェースに適用されているのは R1 側であるため、実際のトラフィック制御は変わらない。  
    A/B は ACL 行そのものを消そうとしており、問題の本質である time-range の定義を修正していない。 [file:1]

***
QUESTION 422 解説

- 問題文（英語）:  
Refer to the exhibit. Two routers are connected back to back via Gigabit Ethernet 0/0 interfaces. Which configuration provides VRF-Lite connectivity for two separate VRFs using the prefixes 10.1.1.0/24 for one VRF and 10.2.2.0/24 for the other VRF?

A. ip vrf 1  
   rd 65001:1  
   ip vrf 2  
   rd 65001:2  
   !  
   int GigabitEthernet0/0  
    no shut  
   !  
   int GigabitEthernet0/0.1  
    encapsulation dot1Q 1  
    ip vrf forwarding  
    ip address 10.1.1.1 255.255.255.0  
   !  
   int GigabitEthernet0/0.2  
    encapsulation dot1Q 2  
    ip vrf forwarding  
    ip address 10.2.2.1 255.255.255.0  

B. ip vrf 1  
   ip vrf 2  
   !  
   int GigabitEthernet0/0  
    no shut  
   !  
   int GigabitEthernet0/0.1  
    encapsulation dot1Q 1  
    ip address 10.1.1.1 255.255.255.0  
    ip vrf forwarding 1  
   !  
   int GigabitEthernet0/0.2  
    encapsulation dot1Q 2  
    ip address 10.2.2.1 255.255.255.0  
    ip vrf forwarding 2  

C. ip vrf 1  
   ip vrf 2  
   !  
   int GigabitEthernet0/0  
    no shut  
   !  
   int GigabitEthernet0/0.1  
    encapsulation dot1Q 1  
    ip vrf forwarding 1  
    ip address 10.1.1.1 255.255.255.0  
   !  
   int GigabitEthernet0/0.2  
    encapsulation dot1Q 2  
    ip vrf forwarding 2  
    ip address 10.2.2.1 255.255.255.0  

D. ip vrf 1  
   rd 65001:1  
   ip vrf 2  
   rd 65001:2  
   !  
   int GigabitEthernet0/0  
    no shut  
   !  
   int GigabitEthernet0/0.1  
    encapsulation dot1Q 1  
    ip vrf forwarding 1  
    ip address 10.1.1.1 255.255.255.0  
   !  
   int GigabitEthernet0/0.2  
    encapsulation dot1Q 2  
    ip vrf forwarding 2  
    ip address 10.2.2.1 255.255.255.0  [file:1]

- 問題文要約（日本語）:  
GE0/0 をバックツーバックでつないだ 2 台のルータ上で、VRF-Lite を使って  
- VRF 1: 10.1.1.0/24  
- VRF 2: 10.2.2.0/24  
という 2 つの論理回線をサブインターフェースで張りたい。  
VRF への割り当て順序や IP アドレス設定の順番も含めて、正しい設定はどれかを問う。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - VRF-Lite では、インターフェース／サブインターフェースを VRF に所属させるコマンドは `ip vrf forwarding <VRF名>`。これを設定すると、そのインターフェースに既に設定されていた IP アドレスは一旦クリアされる。  
    したがって「VRF 割り当て → IP アドレス設定」の順で書く必要がある。 [file:1]
  - 選択肢 C は、subif 0/0.1 / 0/0.2 の両方で  
    - 先に `ip vrf forwarding 1` / `ip vrf forwarding 2` を実行  
    - その後で `ip address 10.1.1.1 255.255.255.0`、`ip address 10.2.2.1 255.255.255.0` を設定  
    という正しい順序になっている。 [file:1]
  - A は `ip vrf forwarding` の後に VRF 名が書かれておらず構文として不完全。  
    B は IP アドレスを先に設定してから VRF を割り当てているため、VRF 設定時に IP が消えてしまい、最終的に IP 未設定となる。  
    D は RD を設定している点は問題ないが、C と実質同じ構成であり、解説では RD の定義は VRF-Lite では任意とされており、必須ではないため、問題文上の正解は C とされている。 [file:1]

***
QUESTION 423 解説

- 問題文（英語）:  
R2  
interface LoopBack0  
 no ip address  
 ipv6 address 2002::1/128  
 ipv6 eigrp 1  
interface FastEthernet0/1  
 ipv6 address autoconfig  
 ipv6 eigrp 1  
!  
ipv6 router eigrp 1  
 stub summary  
 no shutdown  

Refer to the exhibit. R1 connecting to R2's f0/1 cannot receive the R2 prefix. What must be reconfigured to advertise R2 interfaces to R1?

A. EIGRP process on R2 by removing the stub command keyword summary  
B. EIGRP process on R2 with the command stub summary connected  
C. interface FastEthernet0/1 on R2 with an EIGRP summary for the loopback prefix  
D. EIGRP process on R2 with the command stub summary receive-only  [file:1]

- 問題文要約（日本語）:  
R2 は IPv6 EIGRP で stub summary として動作しており、Lo0（2002::1/128）と F0/1 が EIGRP 有効。  
しかし、R1（R2 の F0/1 に直結）側では R2 のプレフィックスが学習できていない。  
Stub 設定のせいで、R2 のインターフェースプレフィックスが広告されていない状況で、どの再設定が必要かを問う。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - EIGRP の stub ルータで `stub summary` のみを指定すると、「サマリールートだけを広告し、個々の接続ネットワーク（connected）は広告しない」動作になる。  
    そのため、R2 の Lo0（2002::1/128）などの接続プレフィックスは R1 に送られない。 [file:1]
  - R2 から自分の接続ネットワークも広告したい場合は、stub オプションに `connected` を含める必要がある。  
    つまり `ipv6 router eigrp 1` 配下を `eigrp stub summary connected`（問題文表記に合わせると「stub summary connected」）に変更すれば、サマリ＋connected ルートが広告される。 [file:1]
  - A のように summary キーワードだけを消そうとすると、実際の IOS では `no eigrp stub summary` で stub 自体が消え、意図が不明瞭になるうえ、選択肢の書き方も具体的ではない。  
    C はインターフェース側でサマリを作る話で、stub による広告制限の本質的な問題を解決しない。D の receive-only は「受信専用 stub」で、まったくルートを広告しないため逆効果。 [file:1]

***
QUESTION 424 解説

- 問題文（英語）:  
Refer to the exhibit. An engineer must filter EIGRP updates that are received to block all 10.10.10.0/24 prefixes. The engineer tests the distribute list and finds one associated prefix. Which action resolves the issue?

A. There is a permit in the route map that allows this prefix. A deny 20 statement is required with no match condition to block the prefix.  
B. There is a permit in the route map that allows this prefix. A deny 20 statement is required with a match condition to match a new ACL that denies all prefixes.  
C. There is a permit in the ACL that allows this prefix into EIGRP. The ACL should be modified to deny 10.10.10.0 0.0.0.255.  
D. There is a permit in the ACL that allows this prefix into EIGRP. The ACL should be modified to deny 10.10.10.0 255.255.255.0.  [file:1]

- 問題文要約（日本語）:  
EIGRP 受信アップデートを distribute-list＋標準 ACL でフィルタして、「10.10.10.0/24 系（10.10.10.x）」のプレフィックスをすべてブロックしたい。  
しかしテストすると、10.10.10.128/25 など一部プレフィックスがまだ受信できてしまっている。  
標準 ACL のマッチ条件が原因で取りこぼしているので、どのように ACL を修正すべきかを問う。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 標準 ACL をルートフィルタとして使う場合、「ACL のアドレス部は“ネットワークアドレス”に対してマッチ」する。  
    `10.10.10.0 0.0.0.0` は “ネットワークアドレスがちょうど 10.10.10.0 の経路” にしかマッチしない。 [file:1]
  - 例えば 10.10.10.128/25 のネットワークアドレスは 10.10.10.128 なので、`10.10.10.0 0.0.0.0` とは一致せず、その結果 ACL で deny されずに EIGRP に取り込まれてしまう。 [file:1]
  - 10.10.10.x（10.10.10.0/24 系）をすべてまとめてマッチさせたい場合は、ワイルドカードを 0.0.0.255 にする必要がある。  
    つまり ACL の該当エントリを `deny 10.10.10.0 0.0.0.255` に変更すれば、10.10.10.0/24 内のどのサブネット（/25, /26 など）でも、ネットワークアドレスが 10.10.10.x であればマッチし、EIGRP に入ってこなくなる。 [file:1]
  - D の `10.10.10.0 255.255.255.0` は ACL のワイルドカードではなくサブネットマスク形式であり、標準 ACL の書式として誤り。A/B は “問題は route-map 側” と誤認しており、実際の原因である ACL のマッチ範囲を修正していない。 [file:1]

***
QUESTION 425 解説

- 問題文（英語）:  
A network administrator must optimize the segment size of the TCP packet on the DMVPN IPsec protected tunnel interface, which carries application traffic from the head office to a designated branch. The TCP segment size must not overwhelm the MTU of the outbound link. Which configuration must be applied to the router to improve the application performance?

A. interface tunnel30  
   ip mtu 1400  
   ip tcp max-segment 1360  
   !  
   crypto ipsec fragmentation before-encryption  

B. interface tunnel30  
   ip mtu 1400  
   ip tcp payload-size 1360  
   !  
   crypto ipsec fragmentation before-encryption  

C. interface tunnel30  
   ip mtu 1400  
   ip tcp adjust-mss 1360  
   !  
   crypto ipsec fragmentation before-encryption  

D. interface tunnel30  
   ip mtu 1400  
   ip tcp packet-size 1360  
   !  
   crypto ipsec fragmentation before-encryption  [web:102][web:103][web:104]

- 問題文要約（日本語）:  
DMVPN 上の IPsec 保護トンネル（Tunnel30）で、ヘッドオフィスから支店へのアプリケーショントラフィックの TCP セグメントサイズを、外側リンクの MTU を超えないように最適化したい。  
MTU 1400 に合わせて TCP の MSS を調整し、断片化や性能低下を防ぐために、どの設定をトンネルインターフェースに入れるべきかを問う。 [web:102][web:103][web:104]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - Cisco IOS で「TCP MSS をインターフェース単位で調整」する正式なコマンドは `ip tcp adjust-mss <値>` だけであり、他の `ip tcp max-segment / payload-size / packet-size` といったサブコマンドは存在しない。 [web:98][web:100][web:102]
  - `ip tcp adjust-mss 1360` は、そのインターフェースを通過する TCP SYN パケットの MSS オプション値を 1360 バイトに書き換えることで、エンドホストが「1360 バイトまでのセグメントしか送らない」ように学習させる機能（いわゆる MSS クランピング）。 [web:98][web:101][web:102]
  - 同時に `ip mtu 1400` を Tunnel インターフェースに設定することで、トンネル＋IPsec のオーバーヘッドを含めた最終パケットサイズが物理リンクの MTU（例:1500）を超えず、フラグメントや再組立てによる性能低下を避けられる。 [web:103][web:104][web:109]
  - `crypto ipsec fragmentation before-encryption`（事前フラグメンテーション）を有効にしておくと、もしそれでも超過する場合、暗号化前にフラグメント化して decrypt 側での CPU 負荷を軽減でき、IPsec トラフィック全体のスループット向上に役立つ。 [web:109]
  - A/B/D はそもそも存在しない `ip tcp ...` サブコマンドを使っており、設定として無効。 [web:98][web:100]

***
QUESTION 426 解説

- 問題文（英語）:  
Refer to the exhibit. The traffic from spoke to hub is dropping. The operations team observes:  
- R2-R3 link is down due to the fiber cut.  
- R2 and R5 receive traffic from R1 in AS 65101.  
- R3 and R5 receive traffic from R4 in AS 65201.  

Which configuration resolves the issue?

A. R6(config)#router bgp 65201  
   R6(config-router)#neighbor 10.10.10.5 remote-as 65101  
   R6(config-router)#neighbor 10.10.10.5 update-source Loopback0  
   R6(config-router)#neighbor 10.10.10.5 ebgp-multihop 3  

B. R5(config)#router bgp 65101  
   R5(config-router)#neighbor 10.10.10.6 remote-as 65201  
   R5(config-router)#neighbor 10.10.10.6 update-source Loopback0  
   R5(config-router)#neighbor 10.10.10.6 ebgp-multihop 3  

C. R5(config)#router bgp 65101  
   R5(config-router)#no neighbor 10.0.0.18 update-source Loopback0  

D. R6(config)#router bgp 65101  
   R6(config-router)#no neighbor 10.0.0.17 update-source Loopback0  [file:1]

- 問題文要約（日本語）:  
DMVPN のハブ・スポーク構成の中で、スポークからハブへのトラフィックが落ちている。  
R2-R3 間の物理リンク断により経路が R5/R6 経由の eBGP パスに切り替わるべきだが、R5–R6 間の BGP ピアが確立できておらず、トラフィックがブラックホールに落ちている。  
R5 は AS 65101 側、R6 は AS 65201 側で、Loopback0 を BGP の update-source に使おうとしているが、その設定が不整合を起こしている。どの修正が必要かを問う。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - R6 側は `router bgp 65201` で `neighbor 10.0.0.17 remote-as 65101` のみを設定しており、update-source は指定していない＝実インターフェース（10.0.0.18）をソースにして接続しようとしている。 [file:1]
  - 一方 R5 側は `neighbor 10.0.0.18 remote-as 65201` に対して `neighbor 10.0.0.18 update-source Loopback0` を設定しており、「自分の Loopback0 アドレス（10.10.10.5）から 10.0.0.18 へ接続する eBGP セッション」を期待している。 [file:1]
  - そのため R5 のログには「10.0.0.18 passive open failed – 10.0.0.17 is not update-source loopback0's address (10.10.10.5)」のようなメッセージが出ており、「相手（R6）が R5 の Lo0 を neighbor として設定していない」ことが原因で BGP セッションが確立しないと分かる。 [file:1]
  - この矛盾を解消するには、  
    - 両側とも loopback 対 loopback で eBGP を張り直す、または  
    - シンプルに R5 側の `neighbor 10.0.0.18 update-source Loopback0` を削除して、物理アドレス同士で eBGP を張る  
    のどちらか。  
    選択肢 C は後者（R5 から update-source 指定を削除）であり、最小変更で問題を解消する。 [file:1]
  - A/B は「R5/R6 双方で loopback アドレスを使ったマルチホップ eBGP」に組み替える案だが、既存設計との整合性や他設定の修正が必要であり、問題文の範囲を超えている。D は R6 側の update-source を削除しようとしているが、そもそも R6 には update-source が設定されていない。 [file:1]

***
QUESTION 427 解説

- 問題文（英語）:  
Which MPLS value is combined with the IP prefix to convert to a VPNv4 prefix?

A. 16-byte Route Distinguisher  
B. 8-byte Route Distinguisher  
C. 16-byte Route Target  
D. 8-byte Route Target  [web:113][web:118][web:121]

- 問題文要約（日本語）:  
MPLS L3VPN で「通常の IPv4 プレフィックス」を「VPNv4 プレフィックス（VPN-IPv4 アドレス）」に変換して、MP-BGP で運べるようにするとき、IP プレフィックスに付与される値はどれかを問う問題。 [web:115][web:118][web:121]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - VPNv4（VPN-IPv4）アドレスは 12 バイト長で、「先頭 8 バイトの Route Distinguisher（RD）＋後続 4 バイトの IPv4 アドレスプレフィックス」という構造になっている。 [web:113][web:115][web:116][web:121]
  - RD は 64 ビット（8 バイト）の値で、「Type 2 バイト＋Value 6 バイト」で構成され、同じ IPv4 アドレスでも RD が異なれば別の VPNv4 プレフィックスとして扱われるため、異なる顧客 VRF 間で IPv4 空間が重複しても区別できる。 [web:116][web:118][web:119]
  - 一方、Route Target（RT）は BGP 拡張コミュニティ属性として付く 64 ビット値であり、「どの VRF に経路をインポート／エクスポートするか」を決めるために使われるので、「IP プレフィックスと結合して VPNv4 プレフィックスを作る役割」ではない。 [web:118][web:119][web:124]
  - 以上より、「IP プレフィックスに前置されて VPNv4 プレフィックスを作る 8 バイト値」は RD であり、選択肢 B が正解。 [web:118][web:121]

***
QUESTION 428 解説

- 問題文（英語）:  
Which technique removes the outermost label of an MPLS-tagged packet before the packet is forwarded to an adjacent LER?

A. label swap  
B. explicit-null  
C. PHP  
D. label imposition  [web:121][web:124][web:127]

- 問題文要約（日本語）:  
MPLS L3VPN などで、LSR が次ホップの LER（エッジルータ）にパケットを渡す直前に、「一番外側の MPLS ラベルをあらかじめ剥がしてから」転送する動作を何と呼ぶかを問う問題。 [web:121][web:124]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - PHP（Penultimate Hop Popping）は、「ペナルティメイト（最後の一つ前の）ホップ」が、次ホップが LER であることを知ったとき、最終ホップに到達する前に一番外側のラベルを POP（除去）する仕組み。これにより、LER は IP パケット（または内側ラベルのみ）を処理でき、ラベル処理負荷を軽減できる。 [web:121][web:124][web:127]
  - 通常の label swap は「受け取ったラベルを別のラベルに付け替えて」転送する動作であり、ラベルを剥がさずに別ラベルを付けるだけ。 [web:121][web:125]
  - label imposition は LER が「IP パケットに最初の MPLS ラベル（場合により複数ラベル）を“積む”」処理で、入口側で行われる。 [web:121][web:125]
  - explicit-null は「POP せずに、ラベル 0（または IPv6 では 2）を残したまま最終ホップへ渡す」動作であり、「ラベルをあらかじめ除去する」PHPとは逆の振る舞いになる。 [web:121][web:124]

***
QUESTION 429 解説

- 問題文（英語）:  
Refer to the exhibit. An engineer must filter incoming EIGRP updates to allow only a set of specific prefixes. The distribute list is tested, and it filters out all routes except network 10.10.10.0/24. How should the engineer temporarily allow all prefixes to be learned by the router again without adjusting the existing access list?

A. A permit any statement should be added before completing the ACL with the required prefixes, and then the permit any statement can be removed.  
B. A permit 20 statement should be added before completing the ACL with the required prefixes, and then the permit 20 statement can be removed.  
C. A continue statement should be added within the permit 10 statement before completing the ACL with the required prefixes, and then the continue statement can be removed.  
D. An extended access list must be used instead of a standard access list to accomplish the task.  [file:1]

- 問題文要約（日本語）:  
特定プレフィックスだけを許可するために、EIGRP 受信ルートに対して distribute-list＋標準 ACL を適用したところ、10.10.10.0/24 以外はすべてフィルタされてしまった。  
既存 ACL 自体は変更せずに、「一時的にすべてのプレフィックスを再び学習させたい」。どの設定で対応すべきかを問う。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - この問題は「ACL をいじらずに、route-map 側で一時的に“全部許可”に戻す」発想がポイント。 [file:1]
  - すでに EIGRP の distribute-list は、標準 ACL を `match ip address` する形で route-map に紐づいている（例: `route-map EIGRP permit 10 match ip address <既存ACL>`）ため、ここを直接さわるとフィルタ動作そのものが変わってしまう。 [file:1]
  - そこで解説にある通り、route-map に「新しいシーケンス 20」を追加して `route-map EIGRP permit 20` だけを入れておけば、この seq 20 には `match` 条件がないため、「seq 10 でマッチしなかった全ルート」が seq 20 でそのまま許可される＝結果として“すべてのプレフィックスを許可”できる。 [file:1]
  - ACL のルール自体は一切変えず、route-map に `permit 20` を一行足すだけなので「既存 ACL を調整しない」という要件も満たす。目的のプレフィックス制御を再度有効にしたくなったら、この `route-map EIGRP permit 20` を削除すれば元のフィルタ状態に戻せる。 [file:1]
  - A の「permit any を ACL に追加」は “ACL を変更してしまう” ので条件違反。C の `continue` はポリシーベースルーティングなど別の用途で、ここでは不要。D は拡張 ACL を求めているが、標準 ACL でも十分に実現可能であり不要。 [file:1]

***
QUESTION 430 解説

- 問題文（英語）:  
Refer to the exhibit. R3 cannot authenticate via TACACS. Which configuration resolves the issue?

A. tacacs server SITE6_TACACS  
   address ipv4 10.66.66.66  
   key CiscoTACACS  

B. tacacs server SITE6_TACACS  
   key C!scoTACACS  

C. tacacs server SITE6_TACACS  
   key C!sc0TACACS  

D. tacacs server SITE6_TACACS  
   address ipv4 10.60.66.66  
   key C!scoTACACS  [file:1]

- 問題文要約（日本語）:  
R3 は TACACS による認証ができていない。ログには timeout が出ており、TACACS サーバ（10.66.66.66）にうまく認証要求が届いていないか、応答が返ってきていないように見える。  
図を見ると、TACACS サーバ側の key は `C!scoTACACS` だが、R3 側の設定 key が微妙に異なっている。どの設定変更で問題を解決できるかを問う。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - R3 のログには「timeout」とあり、R3 から TACACS サーバ 10.66.66.66 へリクエストは送られているが、サーバが正しく応答していない状況を示す。ルーティングなど IP 到達性に問題がなければ、典型的な原因は「共有鍵の不一致」。 [file:1]
  - 図では TACACS サーバに設定されている key は `C!scoTACACS` になっているが、R3 の現在の設定は `C!scOTACACS` のように、`C!sco...` の「o」が大文字の `O` になっているなど、文字が 1 文字違うため、サーバ側と一致していない。 [file:1]
  - 選択肢 B は R3 側に `tacacs server SITE6_TACACS` の key を `C!scoTACACS` に設定し直すもので、サーバ側と完全に一致させる変更になっている。これにより R3 と TACACS サーバ間での暗号化・認証が成功し、timeout が解消される。 [file:1]
  - A は key を `CiscoTACACS` に変えてしまい、サーバ側の `C!scoTACACS` と一致しないため逆に悪化。C は誤ったキーのまま。D は IP アドレスを 10.60.66.66 に変えてしまい、正しい TACACS サーバ 10.66.66.66 へ到達しなくなる。 [file:1]

***
QUESTION 431 解説

- 問題文（英語）:  
The network administrator configured CoPP so that all SNMP traffic from Cisco Prime located at 192.168.1.11 toward the router CPU is limited to 1000 kbps. Any traffic that exceeds this limit must be dropped.

access-list 100 permit udp any any eq 161  
class-map CM-SNMP  
 match access-group 100  
policy-map PM-COPP  
 class CM-SNMP  
  police 1000 conform-action transmit  
control-plane  
 service-policy input PM-COPP  

The network administrator is not getting the desired result for the SNMP traffic and SNMP traffic is getting dropped frequently. Which set of configurations resolves the issue?

A. ...  
B. ...  
C. ...  
D. ...  [web:180][web:185][web:187][web:190]

- 問題文要約（日本語）:  
CoPP で「Cisco Prime（192.168.1.11）からの SNMP（UDP/161）を 1000 kbps に制限し、それを超えた SNMP は drop する」設定をしたが、狙い通りに動かず SNMP が頻繁に落ちてしまっている。  
原因としては「police 1000 が 1000 bps（1 kbps 以下）として解釈されてキツすぎる」ことと、「ACL が any any なので Prime 以外の SNMP までまとめてポリシングしている」ことが考えられる。どの修正が正しいか。 [web:180][web:185][web:187][web:190]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - `police` コマンドの既定単位は「bps（bits per second）」なので、`police 1000` は「1000 bps（1 kbps）」となり、SNMP のような管理トラフィックには極端に低すぎる帯域制限になる。1000 kbps＝1 Mbps にしたいなら、`1000000` と書く必要がある。 [web:187][web:190]
  - SNMP は UDP 161 を使うが、問題文の要件は「Cisco Prime 192.168.1.11 からの SNMP だけ」を制限したい、ということなので、ACL は `udp host 192.168.1.11 any eq 161` のように“送信元を 192.168.1.11 に限定”すべき。 [web:180][web:185][web:186]
  - 選択肢 D は  
    - まず ACL 100 を削除して  
      `access-list 100 permit udp host 192.168.1.11 any eq 161` で Prime からの SNMP のみをマッチさせ  
    - policy-map 内で `no police 1000 ...` を削除し、`police 1000000 conform-action transmit` に修正して 1 Mbps でポリシングする  
    という構成になっており、帯域値とマッチ条件の両方を要件通りにしている。 [web:180][web:187][web:190]
  - A は ACL の修正がなく、全 SNMP（any any）を 1 Mbps で制限してしまい「Prime からだけ」にはならない。B は CoPP を外してインタフェースに適用してしまい、Control Plane Policing ではなく普通のインタフェース QoS になってしまう。C は TCP 161 をマッチしており SNMP（UDP 161）を捕まえられない。 [web:180][web:181][web:183]

***
QUESTION 432 解説

- 問題文（英語）:  
Refer to the exhibit. The IT router has been configured with the Maths VRF and the interfaces have been assigned to the VRF. Which set of configurations exchanges routes between the Maths-1 and Maths-2 routers using OSPF in area 0?

A. router ospf 1 vrf Maths  
   area 0  
   network 192.168.1.0  
   network 192.168.2.0  

B. router ospf 1 vrf Maths  
   network 192.168.1.0 0.0.0.255 area 0  
   network 192.168.2.0 0.0.0.255 area 0  

C. router ospf 1  
   address-family ipv4 vrf Maths  
   network 192.168.1.0 0.0.0.255 area 0  
   network 192.168.2.0 0.0.0.255 area 0  

D. router ospf 1  
   network 192.168.1.0 0.0.0.255 area 0  
   network 192.168.2.0 0.0.0.255 area 0  [file:1]

- 問題文要約（日本語）:  
IT ルータには VRF「Maths」が作成され、Maths-1／Maths-2 につながるインターフェースは既にその VRF に所属している。  
この VRF 内で、Maths-1 ルータと Maths-2 ルータの間で OSPF エリア 0 を使って経路交換したい。どの OSPF 設定が正しいかを問う。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - VRF-Lite 上で「OSPFv2」を動かす場合は、OSPF プロセス宣言を  
    `router ospf <プロセス番号> vrf <VRF名>`  
    の形式で VRF ごとに起動し、その下で通常通り `network ... area ...` を書く。選択肢 B はまさにこの形式で `vrf Maths` を指定し、両サブネットをエリア 0 に参加させている。 [file:1]
  - OSPFv2 の `network` コマンドは「ネットワークアドレス＋ワイルドカードマスク＋エリア番号」の形式が必要で、`network 192.168.1.0` のようにマスクなしで書く A は不正。 [file:1]
  - C の `address-family ipv4 vrf Maths` は OSPFv3 のアドレスファミリー構文であり、OSPFv2 では使わない。ここで求められているのは「通常の OSPF（v2）を VRF 上で動かす設定」。 [file:1]
  - D はグローバル（グローバル VRF）に OSPF を起動しているだけで、VRF Maths 内のインターフェースには適用されないため、Maths-1／Maths-2 間の VRF 内経路交換は起きない。 [file:1]

***
QUESTION 433 解説

- 問題文（英語）:  
What is a function of the IPv6 DHCP Guard feature for DHCP messages?

A. All client messages are always switched regardless of the device role.  
B. It blocks only DHCP request messages.  
C. If the device is configured as a DHCP server, no message is switched.  
D. Only access lists are supported for matching traffic.  [web:197][web:201][web:205][web:208]

- 問題文要約（日本語）:  
IPv6 DHCP Guard（DHCPv6 Guard）機能が DHCP メッセージに対して行う動作のうち、正しいものはどれかを問う問題。 [web:201][web:205][web:208]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - DHCPv6 Guard は「不正な DHCPv6 サーバ／リレーエージェントからの“サーバ側メッセージ”（ADVERTISE, REPLY など）をブロックする」機能であり、クライアントから出ていく SOLICIT、REQUEST などのメッセージはブロックしない。 [web:201][web:205][web:208]
  - Cisco の解説では、DHCPv6 メッセージを大きく 3 種類に分類し、  
    - 「クライアントメッセージ（またはクライアント→サーバ方向のリレーメッセージ）は、ポートの device-role（client/server）に関係なく常にスイッチングされる」  
    と明記されている。つまり、A の “All client messages are always switched regardless of the device role” がそのまま仕様を表現している。 [web:197][web:205][web:208]
  - 一方、B の「DHCP request だけをブロック」は逆で、ブロック対象はサーバの ADVERTISE/REPLY など。不正サーバからのアドレス配布を防ぐ機能。 [web:201][web:205]
  - C は「デバイスが DHCP サーバである場合、すべてのメッセージがスイッチされる（＝処理される）」が正しく、問題文の “no message is switched” は誤り。 [web:197][web:205][web:208]
  - D は、DHCPv6 Guard では ACL だけでなく、device-role（client/server）ポリシーやプレフィックス検証なども使えるため誤り。 [web:201][web:202][web:208]

***
QUESTION 434 解説

- 問題文（英語）:  
Refer to the exhibit. A bank ATM site has difficulty connecting with the bank server. A network engineer troubleshoots the issue and finds that R4 has no active route to the bank ATM site. Which action resolves the issue?

A. Advertise 10.10.30.0/24 subnet in R1 EIGRP AS.  
B. Advertise 10.10.30.0/24 subnet in R3 EIGRP AS.  
C. EIGRP peering between R3 and R4 to be fixed.  
D. EIGRP peering between R1 and R2 to be fixed.  

Correct Answer: C  [file:1]

- 問題文要約（日本語）:  
銀行の ATM サイト（例: 10.20.10.0/24）と銀行サーバ間の通信が不安定で、R4 には ATM サイトへの「active な EIGRP ルート」が存在しない状況。  
トポロジを確認すると、R3 には 10.20.10.0/24 の EIGRP ルートが存在し、R1 では同じプレフィックスが SIA（Stuck In Active）状態になっている。どの対応が問題解決に直結するかを問う。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - R1 側のログでは 10.20.10.0/24 に対して SIA が発生しており、これは「ルート再計算のための EIGRP query を送ったが、どこかから reply が返ってこない」典型パターン。 [file:1]
  - 経路の流れとしては、R1 が 10.20.10.0/24 を失い、feasible successor も無いため R2 に query を送信 → R2 も持たないので R3 へ query → R3 も FS 無しなので R4 へ query という形になる。 [file:1]
  - 通常であれば、R4 側に該当経路が無ければ「到達不可」の reply を R3 に返し、それが R2 → R1 と戻って、最終的にルートがトポロジから消えることで SIA にはならない。 [file:1]
  - 問題文では「R4 に active ルートが無いのに SIA が解消しない」ことから、  
    - R3 → R4 間で EIGRP のクエリが届かない  
    - あるいは R4 の reply が R3 に戻らない  
    といった、**R3–R4 間の EIGRP ピアリング障害**が濃厚であり、これを修復するのが最も妥当な対処になる。 [file:1]
  - R1–R2 間のピアリングが壊れていれば、そもそも R1 から R2 への query やルート更新が成り立たないので、R3 で 10.20.10.0/24 が見えている状況とは矛盾する。10.10.30.0/24 を R1/R3 側で再広告しても、SIA や R4 の到達性問題の根本原因（R3–R4 間の EIGRP 通信不良）は解決しない。 [file:1]

***
QUESTION 435 解説

- 問題文（英語）:  
Refer to the exhibit. Routers R1 and R2 have established a network adjacency using EIGRP, and both routers are advertising subnets to its neighbor. After issuing the show ip EIGRP topology all-links command in R1, some prefixes are not showing R2 as a successor. Which action resolves the issue?

A. Enable split-horizon.  
B. Configure the network statement on the neighbor.  
C. Resolve the incorrect metric on the link.  
D. Rectify the incorrect router ID in R2.  

Correct Answer: B  [web:213][web:210][web:215]

- 問題文要約（日本語）:  
R1 と R2 は EIGRP でネイバー関係を確立しており、互いにサブネットを広告している。  
R1 で `show ip eigrp topology all-links` を実行すると、一部プレフィックスについて R2 が successor として表示されない。どの操作を行うと問題が解決するかを問う。 [web:213][web:215]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - `show ip eigrp topology all-links` は、全ての経路候補（successor も non-successor も）を表示するコマンド。ここで R2 が successor にも候補にも出てこないプレフィックスは、そもそも R2 から学習できていない、つまり R2 がそのプレフィックスを EIGRP で広告していない可能性が高い。 [web:210][web:215]
  - 典型的には「R2 側のそのインターフェースやプレフィックスが EIGRP の `network` コマンドで有効化されていない」ことで、そのサブネットが EIGRP に参加していない状態になる。この場合、R2 自身のルーティングテーブルには直結ルートとして存在しても、R1 に対しては一切広告されない。 [web:213][web:219]
  - したがって、**不足しているサブネットに対して R2 側で正しく `network` コマンドを追加する**のが正しい対処であり、それを表しているのが選択肢 B。 [web:213][web:215]
  - A の split-horizon は、同一インターフェース上での「受け取った経路を同じ方向へ再広告しない」ループ防止機構で、二者間の単純な隣接構成では原因になりにくい。C のメトリック異常は「見えてはいるが successor にならない」ケースには関係するが、「そもそも R2 が候補ルートとして出てこない」という現象とは合致しない。D の router ID 不一致は EIGRP ではネイバー形成の条件ではなく、これも原因にはならない。 [web:214][web:217][web:220]

***
QUESTION 436 解説

- 問題文（英語）:  
Refer the exhibit. An engineer configured SNMP traps to record spoofed packets drop of more than 48000 a minute on the ethernet0/0 interface. During an IP spoofing attack, the engineer noticed that no notifications have been received by the SNMP server. Which configuration resolves the issue on R1?

A. ip verify unicast notification threshold 48000  
B. ip verify unicast notification threshold 800  
C. ip verify unicast notification threshold 80  
D. ip verify unicast notification threshold 8000  

Correct Answer: B  [web:223][web:228][web:229]

- 問題文要約（日本語）:  
R1 の ethernet0/0 に uRPF を設定し、「1 分間に 48000 個以上のスプーフパケットが drop されたら SNMP trap を送る」つもりで設定している。  
実際に IP スプーフィング攻撃が起きても SNMP サーバには通知が届いていない。どの `ip verify unicast notification threshold` 値にすれば要件を満たせるかを問う。 [web:223][web:229]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - `ip verify unicast notification threshold` は「パケット毎秒（pps）」でしきい値を指定するコマンドであり、「1 秒間あたり何個の uRPF ドロップが発生したら trap を送るか」を設定する。 [web:229]
  - 問題の要件は「1 分あたり 48000 個以上」であるため、秒あたりに直すと  
    - 48000 ÷ 60 = 800 pps  
    となる。よって `threshold 800` と設定するのが正しい。 [web:223][web:229]
  - A の 48000 は「1 秒あたり 48000 pps」の意味になってしまい、実際のドロップレートがそこまで高くなければしきい値を超えず通知されない。C の 80 は 4800/分に相当し要件より低く、D の 8000 は 480000/分に相当し高すぎる。 [web:223][web:229]

***
QUESTION 437 解説

- 問題文（英語）:  
Refer to the exhibit. A network administrator is trying to switch to the privileged EXEC level on R1 but failed. Which configuration resolves the issue?

A. tacacs server enable-password Cisco@123  
B. tacacs-server enable-password Cisco@123  
C. enable-password Cisco@123  
D. enable password Cisco@123  

Correct Answer: D  [web:245][web:251][web:249]

- 問題文要約（日本語）:  
管理者が R1 でユーザ EXEC モードから特権 EXEC モード（`enable`）に入ろうとしたが、うまく昇格できない。  
どの設定コマンドを入れれば、`enable` 実行時に特権モードへ入れるようになるかを問う。 [web:245][web:251]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - Cisco IOS で特権 EXEC モードに入る際に使うパスワードは、`enable password <文字列>` または `enable secret <文字列>` で設定する。`enable-password` というコマンドや、`tacacs-server enable-password` という構文は存在しない。 [web:249]
  - 選択肢 C のような `enable-password` は不正なコマンドであり、設定として受け付けられない。A, B は TACACS 関連のように見えるが、実際の IOS のコマンド体系とは一致しない。 [web:249][web:251]
  - したがって、正しく特権 EXEC パスワードを設定する構文である `enable password Cisco@123`（D）だけが有効な解決策になる。コンソールからこの設定を入れておけば、vty 接続でログイン後に `enable` を実行した際、このパスワードで特権モードへ昇格できる。 [web:245][web:249]

***
QUESTION 438 解説

- 問題文（英語）:  
Refer to the exhibit. An engineer must redistribute networks 192.168.10.0/24 and 192.168.20.0/24 into OSPF from EIGRP, where the metric must be added when traversing through multiple hops to start an external route of 20. The engineer notices that the external metric is fixed and does not add at each hop. Which configuration resolves the issue?

A. R1 で route-map＋metric 20＋metric-type 1 を指定して OSPF に再配布  
B. R1 で route-map＋metric 20＋metric-type 2 を指定して OSPF に再配布  
C. R2 で route-map＋metric 20＋metric-type 1 を指定して OSPF に再配布  
D. R2 で route-map＋metric 20＋metric-type 2 を指定して OSPF に再配布  

Correct Answer: C  [web:255][web:258][web:259][web:261]

- 問題文要約（日本語）:  
EIGRP 側の 192.168.10.0/24 と 192.168.20.0/24 を OSPF に再配布したい。  
要件は「OSPF 外部ルートとして初期メトリック 20 で開始し、OSPF ドメイン内を複数ホップするときに内部コストを足し込む（＝E1）」こと。  
しかし現在は外部メトリックが各ルータで一定のままで、ホップごとに増えない。どの設定が正しいか。 [web:255][web:258][web:259][web:261]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - OSPF の外部ルートには E1 と E2 があり、  
    - E2（デフォルト）は「ASBR で設定された外部メトリックだけ」を使い、OSPF 内部コストを足し込まないため、どこで見てもメトリックが固定になる。  
    - E1 は「外部メトリック ＋ ASBR までの OSPF 内部コスト」を合計するので、ホップが増えるとメトリックも増えていく。 [web:258][web:261][web:265]
  - 問題の現象「メトリックが固定で増えない」は、現状が E2 として再配布されていることを示している。要件を満たすには、**再配布時に metric-type 1（E1）を指定**する必要がある。 [web:255][web:258][web:259]
  - さらに、図（exhibit）上では EIGRP と OSPF の両方を実行している ASBR は R2 だけなので、**再配布は R2 で行う必要がある**。R1 には両プロトコルが同居していないため、R1 で再配布しても意味がない。 [web:255][web:259][web:262]
  - よって「R2 上で、192.168.10.0/24 と 192.168.20.0/24 を ACL＋route-map でマッチし、`set metric 20` と `set metric-type type-1` を指定して `redistribute eigrp 10 subnets route-map RD` を OSPF プロセスに設定する」C が要件を正しく満たす。 [web:255][web:259][web:262][web:266]

***
QUESTION 439 解説

- 問題文（英語）:  
Refer to the exhibit. An engineer implemented CoPP to limit Telnet traffic to protect the router CPU. It was noticed that the Telnet traffic did not pass through CoPP. Which configuration resolves the issue?

A. policy-map COPP  
   class TELNET  
   police 8000 conform-action transmit exceed-action transmit violate-action drop  

B. ip access-list extended TELNET  
   permit tcp host 10.2.2.1 host 10.2.2.4 eq telnet  
   permit tcp host 10.1.1.1 host 10.1.1.3 eq telnet  

C. policy-map COPP  
   class TELNET  
   police 8000 conform-action transmit exceed-action transmit  

D. ip access-list extended TELNET  
   permit tcp host 10.2.2.4 host 10.2.2.1 eq telnet  
   permit tcp host 10.1.1.3 host 10.1.1.1 eq telnet  

Correct Answer: D  [web:267][web:268][web:270][web:278]

- 問題文要約（日本語）:  
CoPP で Telnet トラフィックを制限してルータ CPU を保護する設定を入れたが、実際には Telnet トラフィックが CoPP にマッチしていない。  
トポロジ上では、  
- 10.2.2.4 → 10.2.2.1 に対して Telnet  
- 10.1.1.3 → 10.1.1.1 に対して Telnet  
が行われている。どの設定変更をすれば、Telnet トラフィックが CoPP（ACL）に正しくマッチするか。 [web:267][web:268][web:270]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - CoPP で Telnet を判別しているのは「クラスマップで参照している拡張 ACL TELNET」であり、この ACL が Telnet セッションの“正しい方向”の 5 タプル（src/dst/port）にマッチしていないと、クラスにヒットせず、結果的に CoPP を通過しない。 [web:271][web:273]
  - 図では、Telnet の通信は  
    - クライアント 10.2.2.4 から サーバ 10.2.2.1 へ TCP/23  
    - クライアント 10.1.1.3 から サーバ 10.1.1.1 へ TCP/23  
    なので、ACL では `source = クライアント`, `destination = サーバ`, `eq telnet` と書く必要がある。 [web:267][web:268][web:270]
  - 選択肢 B の ACL は source/destination が逆になっており、実際の Telnet フローにはマッチしない。これが「Telnet traffic did not pass through CoPP」の直接原因。 [web:267][web:278]
  - 選択肢 D は、`host 10.2.2.4 -> host 10.2.2.1 eq telnet` と `host 10.1.1.3 -> host 10.1.1.1 eq telnet` のように、**実際の Telnet セッションと同じ向き**で ACL を定義しているため、クラスマップ・ポリシーマップが Telnet を正しく捕まえ、CoPP のポリシングが有効になる。 [web:268][web:271][web:273]
  - A/C はポリシングアクションの違いだけで、ACL マッチの間違いそのものは直していないので問題解決にはならない。 [web:271][web:273]

***
QUESTION 440 解説

- 問題文（英語）:  
An engineer configures PBR on R5 and wants to create a policy that matches traffic destined toward 10.10.10.0/24 and forwards it toward 10.1.1.1. This traffic must also have its IP precedence set to 5. All other traffic should be forwarded toward 10.1.1.2 and have its IP precedence set to 0. Which configuration meets the requirements?  [web:289][web:291]

- 問題文要約（日本語）:  
R5 で PBR を設定したい。  
- 宛先が 10.10.10.0/24 のトラフィックは next-hop 10.1.1.1 へ送り、IP precedence を 5 にする。  
- それ以外のトラフィックは next-hop 10.1.1.2 に送り、IP precedence を 0 にする。  
どの設定がこの条件を満たすかを問う。 [web:289][web:291]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 今回は「宛先アドレスベース」で PBR をしたいので、ACL では destination に 10.10.10.0/24 を指定する必要がある。`access-list 100 permit ip any 10.10.10.0 0.0.0.255` のような拡張 ACL で「任意の送信元 → 宛先 10.10.10.0/24」をマッチさせるのが妥当。 [web:283][web:288][web:293]
  - 選択肢 A/B/C のような標準 ACL（source だけを見る）では、「宛先 10.10.10.0/24」ではなく「送信元 10.10.10.0/24」をマッチしてしまい、要件と逆になってしまう。特に A/B は `access-list 1 permit 10.10.10.0 0.0.0.255` で source を見ている。 [web:283][web:288]
  - D は、  
    - ACL 100 で「任意の送信元 → 宛先 10.10.10.0/24」をマッチ  
    - route-map CCNP シーケンス 10 で ACL 100 を match し、`set ip next-hop 10.1.1.1` と `set ip precedence 5` を指定  
    - シーケンス 20 で「その他すべて」に対して `set ip next-hop 10.1.1.2` と `set ip precedence 0` を指定  
    という構成になっており、要件通り「特定宛先は 10.1.1.1/precedence 5」「残りは 10.1.1.2/precedence 0」と振り分けられる。 [web:283][web:285][web:288][web:293]
  - C は precedence の値が逆（10.10.10.0/24 宛てが 0、その他が 5）になっているため誤り。A は “any” ACL を使っているが、30 行目が不要かつ宛先指定も誤り。B は「その他トラフィック」を明示的な ACL でマッチしていないが、それ自体は「最後の permit シーケンスに match なし」という形でも機能しうるものの、やはり宛先ではなく送信元でマッチしている点で要件に合わない。 [web:289][web:291]

***
QUESTION 441 解説

- 問題文（英語）:  
The summary route is not shown in the Router_B routing table after this below configuration on Router_A:

interface ethernet 0  
 description location ID:T1344S9A34B19  
 ip address 192.168.3.1 255.255.255.0  
 ip summary-address eigrp 1 172.16.80.0 255.255.240.0  

Which Router_A configuration resolves the issue by advertising the summary route to Router_B?

A. loopback0 = 172.18.81.1/24  
B. loopback0 = 172.16.96.1/24  
C. loopback0 = 172.16.81.1/24  
D. loopback0 = 172.16.79.1/24  

Correct Answer: C

- 問題文要約（日本語）:  
Router_A の Ethernet0 で `ip summary-address eigrp 1 172.16.80.0 255.255.240.0` を設定したが、Router_B 側のルーティングテーブルにこのサマリールートが現れない。  
どの loopback0 アドレス設定を追加すれば、172.16.80.0/20 のサマリールートが B に広告されるか、という問題。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - EIGRP の `ip summary-address` は、「そのサマリ範囲内に *少なくとも1つ* の具体的な EIGRP ルート（more specific route）が存在する場合にのみ」そのサマリールートを隣接ルータに広告する。個別ルートが 0 個だとサマリは出ない。  
  - サマリは 172.16.80.0/20。これは 172.16.80.0〜172.16.95.255（/24 で言えば .80〜.95 の 16 個のネット）をカバーしている。  
  - loopback0 の /24 プレフィックスがこの範囲に入っていれば、「中の 1 サブネット」として EIGRP に載り、その結果サマリ 172.16.80.0/20 も広告される。  
    - B: 172.16.96.0/24 → /20 の範囲 80〜95 を超えているので範囲外  
    - D: 172.16.79.0/24 → 80 より前なので範囲外  
    - A: 172.18.81.0/24 → そもそも 172.16.80.0/20 のネットとは別物  
    - C: 172.16.81.0/24 → 172.16.80.0/20 の中に含まれる（80〜95 の範囲内）  
  - よって、172.16.81.1/24 を持つ loopback0（C）を作成し、そのネットワークを EIGRP の `network` コマンドで有効化すれば、そのプレフィックスがサマリ範囲内の「具体ルート」となり、172.16.80.0/20 の summary-address が Router_B に広告されるようになる。  

***
QUESTION 442 解説

- 問題文（英語）:  
Refer to the exhibit. An engineer troubleshoots a connectivity problem that is impacting the communication from the users at segment 172.16.3.16/28 to the server farm at 192.168.5.16/28. Which configuration resolves the issue on router R1?

A. router rip  
   redistribute ospf 1 metric 16  

B. router ospf 1  
   redistribute rip metric 14  

C. router ospf 1  
   redistribute rip metric 16  

D. router rip  
   redistribute ospf 1 metric 14  

Correct Answer: D  [web:304][web:306][web:298][web:264]

- 問題文要約（日本語）:  
ユーザセグメント 172.16.3.16/28 から、サーバファーム 192.168.5.16/28 への通信に問題がある。  
R1 で OSPF⇔RIP の再配布をしているが、RIP 側（特に R4）まで 192.168.5.16/28 の経路が届かず通信できない。どの再配布メトリック設定にすれば問題が解決するか、という質問。 [web:304][web:306]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - RIP のメトリックはホップ数で、0〜15 が有効値、16 以上は「到達不能」とみなされる。RIP ルータはメトリック 16 の経路を破棄する。 [web:298][web:264]
  - 問題のシナリオでは、R1 から OSPF の 192.168.5.16/28 を RIP に再配布する際のメトリックが 15 に設定されているため、  
    - R1 から R3 に入るときにメトリック 15  
    - R3 から R4 に伝搬しようとすると 16（1 ホップ加算）になり、R4 は「到達不能」と判断して捨ててしまう。結果、R4 配下の 172.16.3.16/28 からサーバファームまでのルートがなくなる。 [web:304][web:306]
  - したがって、**OSPF→RIP への再配布時の初期メトリックを 14 以下にする必要がある**。そうすれば、  
    - R1（14）→R3（15）→R4（16）という「R4 で 16 になってギリギリ届かない」状況を避け、  
    - R4 でも 15 以下の値で経路を受け取れる。 [web:301][web:302][web:306]
  - 選択肢 D は、`router rip` の下で `redistribute ospf 1 metric 14` と設定しており、まさに OSPF→RIP の再配布メトリックを 14 に下げているため、R3→R4 間でホップを 1 増やしても 15 となり、R4 でも有効な経路として保持できる。 [web:302][web:306]
  - A の metric 16 は最初から「到達不能」値を設定してしまうので論外。B, C は逆方向（RIP→OSPF）の再配布設定であり、今回の「OSPF 側のサーバファームプレフィックスを RIP 側ユーザに届かせる」という問題には関係しない。 [web:304][web:305][web:306]

***
QUESTION 443 解説

- 問題文（英語）:  
Refer to the exhibit. After recovering from a power failure, Ethernet0/1 stayed down while Ethernet0/0 returned to the up/up state. The default route through ISP1 was not reinstated in the routing table until Ethernet0/1 also came up. Which action resolves the issue?

A. Configure the default route through ISP1 with a higher administrative distance than 2.  
B. Remove the references to the interface names from both static default routes.  
C. Add a static route to the 8.8.8.8/32 destination through the next hop 203.0.113.1.  
D. Reference the track object 1 in both static default routes.  

Correct Answer: C  [web:310][web:315][web:321][web:322]

- 問題文要約（日本語）:  
電源断から復旧後、e0/0 は up/up になったが e0/1 は down のまま。  
IP SLA で 8.8.8.8 を監視し、track 1 を使って ISP1 へのデフォルトルートを制御している構成だが、e0/1 が up するまで ISP1 宛てのデフォルトルートがルーティングテーブルに復活しなかった。どの設定変更を入れるべきか、という問題。 [web:310][web:315][web:321]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - IP SLA は 8.8.8.8 に対して ICMP echo を送るが、**そのパケットをどのルートで出すか**は、ルータの現在のルーティングテーブルに依存する。 [web:313][web:314]
  - 電源復旧直後の状況:  
    - ISP1 向けのデフォルトルートは `track 1` と紐付いているが、IP SLA（8.8.8.8 への疎通）がまだ成功していないため「down」と見なされ、ルーティングテーブルにインストールされない。  
    - ISP2 向けデフォルトルートは、出口インターフェース e0/1 が down のため、「次ホップ到達不能」と判断されてインストールされない。  
    - 結果として「デフォルトルートが 1 本も無い」状態となり、8.8.8.8 に向けた IP SLA の probe すら外に出ていかないため、track が永遠に up にならない。 [web:310][web:315][web:321]
  - 解決には、「デフォルトルートが無くても IP SLA のターゲット 8.8.8.8 まで到達できる経路」を 1 本だけ用意してやればよい。  
    - `ip route 8.8.8.8 255.255.255.255 203.0.113.1` を追加し、ISP1 方向の次ホップ（203.0.113.1）へ向けたホストルートにすることで、e0/0 が up している限り、IP SLA の probe は ISP1 側に届く。  
    - すると IP SLA 1 が成功 → track 1 が up → `track 1` 付きの ISP1 デフォルトルートが RIB にインストールされ、通常トラフィックもインターネットへ出て行けるようになる。 [web:313][web:317]
  - A は AD の問題ではなく「reachability のブートストラップ問題」なので筋違い。B でインターフェース指定を消しても、e0/1 側が物理的に down のままでは ISP2 経路は依然「到達不能」扱いになる。D は既に ISP1 側で track を参照している構成であり、本質的な“最初の 1 発が外に出ない”問題は解決しない。 [web:310][web:313][web:319]

***
QUESTION 444 解説

- 問題文（英語）:  
Refer to the exhibit. R1 and R2 use IGP protocol to route traffic between AS 100 and AS 200 despite being configured to use BGP. Which action resolves the issue and ensures the use of BGP?

A. Configure distance to 100 under the OSPF process of R1 and R2.  
B. Remove distance commands under BGP AS 100 and AS 200.  
C. Remove distance commands under BGP AS 100.  
D. Configure distance to 100 under the EIGRP process of R1 and R2.  

Correct Answer: B  [web:324][web:329][web:334][web:336]

- 問題文要約（日本語）:  
R1（AS 100）と R2（AS 200）は eBGP を設定しているにもかかわらず、AS 間のトラフィックが IGP（例: EIGRP/OSPF）経由で流れてしまっている。  
両ルータでは BGP の `distance` コマンドで BGP ルートの AD をいじっており、その結果 IGP ルートが優先されている。  
どうすれば BGP ルートが優先され、AS 100 ↔ AS 200 間のトラフィックが BGP を使うようになるか、という問題。 [web:324][web:329][web:334]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - BGP の AD はデフォルトで  
    - eBGP: 20  
    - iBGP: 200  
    なので、通常は eBGP ルートが IGP（EIGRP 90、OSPF 110 など）よりも優先される。 [web:330][web:331]
  - 今回は両 AS 側の BGP プロセスで `distance` コマンドが使われており、特定のネイバー（10.10.10.0/30 上の相手）から学習した BGP ルートの AD を 100 に変更している。これにより、その BGP ルートの AD が IGP より高く（悪く）なり、結果として IGP ルートが選ばれてしまう。 [web:324][web:329][web:334]
  - R1 と R2 は互いに 10.10.10.0/30 のアドレスでピアを張っており、両方で `distance 100 10.10.10.0 0.0.0.3` のような設定をしているため、**どちら側から見ても eBGP ルートの AD が 100 になり、IGP に負ける構図**になっている。 [web:329][web:334]
  - 解決策はシンプルで、「BGP ルートに対していじってしまった AD を元に戻す」こと、つまり **両 AS の BGP プロセスから `distance` コマンドを削除する（B）** ことで、eBGP の AD を 20 に戻し、IGP より優先させる。 [web:329][web:334]
  - A や D のように IGP 側の AD を 100 に上げる方法でも、理屈の上では「eBGP 20 ＜ IGP 100」となり BGP を優先させられるが、ルーティング設計としては「デフォルト AD に戻す」方がシンプルで副作用が少なく、問題の直接原因（BGP の AD をいじったこと）に正面から対処している。 [web:329][web:331]

***
QUESTION 445 解説

- 問題文（英語）:  
Refer to the exhibit. An administrator is troubleshooting a time synchronization problem for the router's time to another Cisco IOS XE-based device that has recently undergone security hardening. Which action resolves the issue?

A. NTP service is disabled and must be enabled on 10.1.255.40.  
B. Allow NTP in the ingress ACL on 10.1.255.40 by permitting TCP destined to port 123.  
C. Allow NTP in the ingress ACL on 10.1.255.40 by permitting UDP destined to port 123.  
D. Ensure that the CPE router has a valid route to 10.1.255.40 for NTP and rectify if not reachable.  

Correct Answer: A  [web:337][web:339][web:341][web:345]

- 問題文要約（日本語）:  
CPE ルータが NTP サーバ 10.1.255.40（最近セキュリティ強化された IOS XE デバイス）と時刻同期できない。  
NTP パケットを送ると、10.1.255.40 から ICMP port unreachable（type 3 code 3）が返ってきており、NTP association も張れていない。どの対応が正しいかを問う。 [web:337][web:341]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - ICMP Type 3 Code 3（“port unreachable”）は「ホストには到達しているが、その UDP/TCP ポートでリスンしているアプリケーションがいない（サービス未起動）」場合に送られる。今回の場合は UDP 123（NTP）が開いていないことを示す。 [web:344]
  - CPE ルータからの NTP パケットは 10.1.255.40 まで届いており、ポート到達までは問題ない（＝ルーティングは正しい、ACL でも完全にはブロックされていない）。にもかかわらず “port unreachable” が返るのは、**10.1.255.40 側で NTP サービスが無効化されている**と解釈するのが自然。 [web:337][web:339][web:345]
  - ACL によるブロックであれば、ICMP Type 3 Code 13（administratively prohibited）など、別種のエラーになるのが一般的であり、問題文の症状とは異なる。 [web:344]
  - したがって、10.1.255.40 上で NTP サービスを有効化（`ntp server`/`ntp master` 設定や、必要なら `ntp access-group` の緩和）する、という A が正しい対応となる。 [web:337][web:343][web:347]
  - B は NTP を TCP/123 と誤認しており、NTP は UDP/123 を使用するので誤り。C は ACL 開放そのものは正しそうに見えるが、現状パケットが到達している時点で“最初の問題”ではない。D もルーティング問題ではない（到達して ICMP 応答を返している）ため不適切。 [web:337][web:341]

***
QUESTION 446 解説

- 問題文（英語・要約）:  
Refer to the exhibit. An engineer must establish a connection between two CE routers for two customers with overlapping IP addresses. Customer_a is connected to interfaces Gig0/0, and Customer_b is connected to interfaces Gig0/1. Routers CE1 and CE2 are configured as follows:

ip vrf customer_a  
 rd 1:1  
 route-target both 1:1  
ip vrf customer_b  
 rd 2:2  
 route-target both 2:2  

Drag and drop the code snippets from the right onto the boxes in the configuration to establish the needed connection. Snippets may be used more than once. Select and Place. [web:316][web:345][web:357][web:361]

- 問題文要約（日本語）:  
2 つの CE ルータ（CE1・CE2）で VRF を使い、顧客 A と顧客 B を分離したまま、それぞれの CE 間を接続したい。  
- Customer_a は Gig0/0 に接続  
- Customer_b は Gig0/1 に接続  
各ルータには `ip vrf customer_a` と `ip vrf customer_b` が作成済みで、ドラッグ＆ドロップでインターフェース設定などを完成させる問題。 [web:316][web:355][web:361]

- 正解（構成イメージ）:

CE1 側:

- 顧客 A 用インターフェース（Gig0/0 側・片方のリンク）  
  - `interface Gig0/0`  
    - `ip vrf forwarding customer_a`  
    - `ip address 10.1.1.1 255.255.255.252`（例として .1 を CE1、.2 を CE2 に割り当て）  

- 顧客 B 用インターフェース（Gig0/1 側・もう片方のリンク）  
  - `interface Gig0/1`  
    - `ip vrf forwarding customer_b`  
    - `ip address 10.2.2.1 255.255.255.252`  

CE2 側も同様に、各インターフェースを対応する VRF に割り当て、対向側に .2 アドレスをつける:

- `interface Gig0/0`（customer_a 側リンク）  
  - `ip vrf forwarding customer_a`  
  - `ip address 10.1.1.2 255.255.255.252`  

- `interface Gig0/1`（customer_b 側リンク）  
  - `ip vrf forwarding customer_b`  
  - `ip address 10.2.2.2 255.255.255.252`  [web:355][web:354][web:361]

- 解説（ポイント）

  - VRF-Lite では「インターフェースを `ip vrf forwarding <VRF名>` で VRF に紐付け、そのインターフェース上に IP アドレスを設定する」ことで、VRF ごとの独立したルーティングテーブルを構成する。 [web:355][web:361]
  - 顧客 A/B はそれぞれ `customer_a` / `customer_b` という別の VRF に分かれており、同じ CE 間リンクでも**異なる VRF に属するインターフェース**を使えば、重複アドレス空間でも問題なく接続できる。 [web:354][web:358]
  - 問題文の補足にもある通り、「CE1 側が .1、CE2 側が .2」のように IP を振り分けるのが一般的な慣習。 [web:316][web:355]

***
QUESTION 447 解説

- 問題文（英語）:  
Refer to the exhibit. An administrator is attempting to disable the automatic logout after a period of inactivity. After logging out, the console stopped responding to all keyboard inputs. Remote access through SSH still works. Which action resolves the issue?

A. Configure the no exec-timeout command on line con 0.  
B. Configure the default exec-timeout command on line con 0.  
C. Configure the absolute-timeout command on line con 0.  
D. Configure the exec command on line con 0.  

Correct Answer: D  [web:368][web:363][web:369][web:371]

- 問題文要約（日本語）:  
管理者が「アイドルによる自動ログアウトを無効化しよう」として設定変更したところ、その後コンソールからログアウトしたら、コンソールが一切キー入力に反応しなくなった（SSH のリモートアクセスは正常）。  
この状態を直すために、どの設定をすべきか、という問題。 [web:368]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 自動ログアウトを無効化する正しい方法は `exec-timeout 0 0` だが、`no exec-timeout` をタイプミスして `no exec` にしてしまう事故がよくある。`no exec` はその line（console/vty/aux）で「EXEC プロセス自体を起動しない＝プロンプトが出ない」ことを意味し、結果としてコンソールが一切受け付けないように見える。 [web:363][web:371]
  - 問題文の症状「コンソールが全く応答しないが、SSH は使える」は、まさに line con 0 に `no exec` が入ってしまったときの典型的な挙動。 [web:363][web:371]
  - この状態を修復するには、SSH など別経路でルータにログインし、グローバルコンフィグから `line con 0` に入り、`exec` コマンドを投入して EXEC を再度有効化する必要がある。選択肢 D がそれに該当する。 [web:363][web:366]
  - A の `no exec-timeout` は「タイマー設定を消す」だけであり、既に起きている “no exec で EXEC 自体が殺されている” 問題を直さない。B の default（デフォルト 10 分）や C の absolute-timeout も、「セッションが存在する前提」でのタイムアウト制御であり、EXEC が起動していない現状には効かない。 [web:365][web:369][web:371]

***
QUESTION 448 解説

- 問題文（英語）:  
Refer to the exhibit. An IPv6 ACL is applied to restrict PC1 from communicating with PC2 and allow all other traffic. Which configuration resolves the issue?

A. R3(config-ipv6-acl)#no sequence 30  
   R3(config-ipv6-acl)#deny ipv6 host 2001:DB8:0:10::3B any sequence 10  

B. R3(config-ipv6-acl)#no sequence 20  
   R3(config-ipv6-acl)#deny ipv6 host 2001:DB8:0:10::3B any sequence 10  

C. R3(config-ipv6-acl)#no sequence 20  
   R3(config-ipv6-acl)#deny ipv6 host 2001:DB8:0:10::3B host 2001:DB8:A:A::19 sequence 10  

D. R3(config-ipv6-acl)#no sequence 30  
   R3(config-ipv6-acl)#deny ipv6 host 2001:DB8:0:10::3B host 2001:DB8:A:A::19 sequence 10  

Correct Answer: D  [web:378][web:379][web:380][web:381]

- 問題文要約（日本語）:  
R3 に適用した IPv6 ACL で、PC1（2001:DB8:0:10::3B）から PC2（2001:DB8:A:A::19）への通信だけを拒否し、それ以外の IPv6 トラフィックは許可したい。  
現状の ACL では、PC1→PC2 の通信がブロックされていない。順番や条件をどう修正すべきか、という問題。 [web:378][web:379]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 望む動作は「PC1→PC2 だけ deny、それ以外は permit」なので、ACL の基本形は  
    - `deny ipv6 host PC1_IP host PC2_IP`  
    - `permit ipv6 any any`  
    の順にする必要がある。 [web:385][web:388]
  - 問題の説明によると、既存 ACL では  
    - シーケンス 20 で PC1→PC2 を含むトラフィックが permit されており  
    - シーケンス 30 に deny 行が置かれているため、**先に permit 20 にマッチしてしまい、deny 30 が決して実行されない**。 [web:381][web:378]
  - したがって、「PC1→PC2 を deny する行を ACL の先頭（sequence 10）に持ってきてから、`permit ipv6 any any` などの許可行を後ろに置く」必要がある。 [web:385][web:388]
  - 選択肢 D は  
    - sequence 30（現在の無効な deny）を削除し  
    - `deny ipv6 host 2001:DB8:0:10::3B host 2001:DB8:A:A::19 sequence 10`  
      を入れて、PC1→PC2 の通信を ACL の最初で確実に拒否している。これにより後続の permit 行があっても、PC1→PC2 だけは通らない。 [web:381][web:385]
  - A, B は宛先が `any` なので「PC1 から他のあらゆる宛先への通信まで」ブロックしてしまい、要件（PC1↔PC2 だけ拒否）を満たさない。C は deny 条件は正しいが sequence 20 を消すだけで sequence 30 の permit など ACL 全体の順序・内容を問題どおりに修正していないため不正解となる。 [web:378][web:380]

***
QUESTION 449 解説

- 問題文（英語）:  
Refer to the exhibit. A network is configured for EIGRP equal-cost load balancing, but the traffic destined to the servers is not load balanced. Link metrics from router R2 to R3 and R4 are the same. Which delay value must be configured to resolve the issue?

A. 200 on R3 E0/1  
B. 120 on R3 E0/1  
C. 2200 on R4 E0/1  
D. 120 on R4 E0/1  

Correct Answer: B  [web:391][web:394][web:400][web:403]

- 問題文要約（日本語）:  
EIGRP で equal-cost load balancing を行うようにしているが、サーバ宛てのトラフィックが 2 本の経路に分散されていない。  
R2 から R3・R4 へのリンクは同じ帯域だが、片方のパスの EIGRP メトリックがわずかに大きく、equal-cost と判断されていない。どの delay 値を設定すれば、2 経路のメトリックを一致させてロードバランスさせられるか、という問題。 [web:394][web:400]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - EIGRP のメトリックは主に「帯域（bandwidth）」と「遅延（delay）」で計算される。帯域が同じなら、**delay の差がそのままメトリック差**として効く。 [web:394][web:399]
  - 問題のシナリオでは、R2 からサーバネットワークへの 2 経路のうち、  
    - 片方（R2→R3 経由）の累積 delay が 2000 microseconds  
    - もう片方（R2→R4 経由）の累積 delay が 2200 microseconds  
    となっており、後者のメトリックが高いので“equal-cost”と見なされず、ロードバランスされない。 [web:400][web:403]
  - R2 のインターフェース delay を変えると両経路に影響するため、**片側だけの delay（R3 側の E0/1）を調整する必要がある**。 [web:403]
  - R2→R3→宛先の合計 2000 のうち、R2 E0/0 が 1000、R3 E0/1 が 1000 と仮定すると、R3 E0/1 の delay を 1200 microseconds に増やせば、  
    - R2→R3 経由の合計 delay = 1000 + 1200 = 2200 microseconds  
    で、R2→R4 経由の 2200 と一致し、両経路の EIGRP メトリックが同一になる。 [web:400][web:403]
  - IOS の `delay` コマンドは「10 マイクロ秒単位」で設定するので、1200 microseconds は `delay 120` と指定する。この内容に一致するのが「120 on R3 E0/1」である B。 [web:398][web:400]
  - C の 2200 を R4 に設定しても、すでに R4 側パスは 2200 になっているので意味がない。A の 200 や D の 120 を R4 に設定する案は、R4 経由のパスだけをさらに変動させてしまい、2 経路のメトリックを揃える方向にはならない。 [web:394][web:403]

***
QUESTION 450 解説

- 問題文（英語）:  
Refer to the exhibit. An engineer must connect the Reno and Detroit spokes using DMVPN phase 2. Hub tunnel configuration is:

Dallas:  
interface Tunnel0  
 ip address 10.0.80.1 255.255.255.0  
 ip nhrp authentication cisco123  
 ip nhrp map multicast dynamic  
 ip nhrp network-id 5  
 tunnel source Serial0/0  
 tunnel mode gre multipoint  

Which configuration accomplishes the task?

A. Reno/Detroit のトンネルで `ip nhrp authentication cisco123`, `ip nhrp map 10.0.80.1 10.92.18.x`, `ip nhrp map multicast 10.92.18.x`, `ip nhrp nhs 10.0.80.1` を正しく設定  
B. Reno/Detroit で `ip nhrp map broadcast ...` を使用  
C. Reno/Detroit で認証を `cisco321` に変更  
D. Reno/Detroit で `ip nhrp map 10.92.18.x 10.0.80.1` の向きが逆  [web:404][web:405][web:406][web:409]

- 問題文要約（日本語）:  
DMVPN Phase 2 で、ハブ Dallas（mGRE）と 2 本のスポーク（Reno, Detroit）を接続し、スポーク同士も直接通信できるようにする構成を選ぶ問題。  
Dallas のトンネル設定は既に与えられており、Reno/Detroit 側の NHRP マッピングと認証、tunnel mode などを正しく書けている選択肢を選ぶ。 [web:405][web:406][web:416]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - DMVPN Phase 2 では、ハブは mGRE＋`ip nhrp map multicast dynamic`、スポークは mGRE で  
    - `ip nhrp map <hub-tunnel-IP> <hub-NBMA-IP>`  
    - `ip nhrp map multicast <hub-NBMA-IP>`  
    - `ip nhrp nhs <hub-tunnel-IP>`  
    を設定して、ハブを NHS（Next Hop Server）として登録する。 [web:405][web:406][web:409][web:416]
  - Dallas の設定では NHRP 認証が `cisco123` なので、スポーク側も `ip nhrp authentication cisco123` で揃える必要がある。C のように `cisco321` に変えると認証不一致で失敗する。 [web:409][web:415]
  - `ip nhrp map broadcast ...` というコマンドは存在せず、正しい構文は `ip nhrp map multicast <NBMA>` なので、B は明らかに不正。 [web:407][web:415]
  - `ip nhrp map` の書式は  
    - `ip nhrp map <tunnel IP> <NBMA IP>`  
    であり、A は Reno/Detroit それぞれで  
    - `ip nhrp map 10.0.80.1 10.92.18.2` のように「ハブのトンネル IP → ハブ側の NBMA IP」を正しい向きで書いている。D はこの向きが逆であるため誤り。 [web:405][web:409][web:411][web:415]
  - 以上より、正しい認証文字列・正しい `ip nhrp map` の向き・`ip nhrp map multicast` の使用・`ip nhrp nhs` と `tunnel mode gre multipoint` を満たしているのは A のみ。 [web:404][web:405][web:409][web:416]

***
QUESTION 451 解説

- 問題文（英語）:  
What is considered the primary advantage of running BFD?

A. reduction in time needed to detect Layer 2 switched neighbor failures  
B. reduction in CPU needed to detect Layer 2 switch neighbor failures  
C. reduction in CPU needed to detect Layer 3 routing neighbor failures  
D. reduction in time needed to detect Layer 3 routing neighbor failures  

Correct Answer: A

- 問題文要約（日本語）:  
BFD（Bidirectional Forwarding Detection）を使うことの「主な利点」として正しいものはどれか、という問題。 [web:419][web:421][web:422]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - BFD は「隣接ルータ間のフォワーディングパス障害を、非常に短い間隔の軽量パケットで検出する」プロトコルで、ルーティングプロトコルの Hello タイマよりもずっと短い検出時間（数十ミリ秒レベル）で障害を検出できる。 [web:419][web:422][web:430]
  - その設計目的は「障害検出時間＝タイムアウトまでの時間を大幅に短縮する」ことであり、CPU 使用量を下げることではない。BFD 自体も追加プロセスなので、CPU 削減どころか少し負荷が増えることもある。 [web:422][web:426][web:430]
  - 実際には、物理リンクや L2 セグメントの障害（ケーブル断・スイッチ故障など）を BFD セッションのダウンとして素早く検出し、それを OSPF/BGP/IS-IS/EIGRP などの L3 ルーティングプロトコルに通知して経路再計算を起動する、という流れになるため、「L2 障害の検出時間短縮」という表現の A が最も近い。 [web:419][web:422][web:424][web:433]
  - 選択肢 D のように「L3 ルーティング隣接の検出時間短縮」と言いたくなるが、ルーティングプロトコル自身ではなく、その下のフォワーディングパスを監視する仕組みである点から、問題の選択肢の中では A がベターとされている。 [web:422][web:430][web:432]

***
QUESTION 452 解説

- 問題文（英語）:  
The network administrator deployed the Binding Table Recovery feature. Which two devices recover the missing binding table entries? (Choose two.)

A. destination host  
B. source host  
C. DHCP server  
D. DHCP client  
E. DHCP relay agent  

Correct Answer: A, C

- 問題文要約（日本語）:  
IPv6 First-Hop Security の「Binding Table Recovery」機能を有効化しているネットワークで、欠落したバインディングテーブルのエントリは、どの 2 種類のデバイスに問い合わせることで復元されるか、という問題。 [web:434][web:439][web:441][web:444]

- 正解: **A（destination host）と C（DHCP server）**

- 解説（なぜ A と C が正しいか）

  - IPv6 First-Hop Security Binding Table Recovery Mechanism の説明では、宛先ガード（destination guard）で宛先アドレスの解決に失敗したとき、**DHCP サーバまたは宛先ホスト（destination host）に問い合わせてバインディングテーブルのエントリを復元する**と明記されている。 [web:434][web:440][web:441][web:444]
  - 具体的には、NDP か DHCP をリカバリプロトコルとして用い、  
    - DHCP を利用する場合は DHCP サーバに問い合わせ  
    - NDP を利用する場合は、宛先ホストに対して NS（Neighbor Solicitation）などで情報を取得  
    して、欠落した IPv6–MAC–ポート の関連付け（binding）を再構築する。 [web:439][web:441][web:444]
  - DHCP クライアント（D）や DHCP リレー（E）は、Binding Table Recovery の説明には登場せず、エントリ復旧の対象として用いられない。B の “source host” も仕様上の用語とは一致していない。 [web:434][web:439][web:442][web:447]

***
QUESTION 453 解説

- 問題文（英語）:  
Refer to the exhibit. Which action installs route 192.168.2.2/32 in the routing table?

A. Redistribute connected networks into BGP on the local router.  
B. Configure NAT on the local router to translate private IP addresses.  
C. Configure the next-hop-self attribute for the peering on the local router.  
D. Configure the next-hop-self attribute for the peering on the peer router.  

Correct Answer: D  [web:448][web:449][web:206][web:459]

- 問題文要約（日本語）:  
BGP ルータで 192.168.2.2/32 は BGP テーブルには見えているが、RIB（ルーティングテーブル）にはインストールされていない。  
理由は、その経路の next-hop である 209.165.200.225 への到達性が無く、`show ip route` 上で “inaccessible” のように見えているため。どの操作をすれば、この経路がルーティングテーブルに入るか、という問題。 [web:448][web:450][web:452]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - BGP 経路が RIB にインストールされるには、「next-hop に対する再帰ルックアップが成功し、そのアドレスまでの経路が存在する」必要がある。次ホップが到達不能であれば、その BGP 経路は “best” にならず RIB に入らない。 [web:452][web:455]
  - 今回は、192.168.2.2/32 の next-hop が 209.165.200.225 になっているが、ローカルルータは 209.165.200.225 を解決できない（ルーティングテーブルにそのサブネットが無い）ため、その経路はインストールされない。 [web:448][web:455]
  - 典型的な解決策は 2 つ:  
    - IGP で 209.165.200.0/24 などの EBGP リンクサブネットを広告し、next-hop までの経路を知るようにする  
    - もしくは「広告側の BGP ルータ」に `next-hop-self` を設定し、BGP Update を送る際に next-hop を自分の IP（ローカルが到達可能なアドレス）に書き換えてしまう [web:452][web:455]
  - この問題では後者の「peer 側で next-hop-self を設定して、ローカルから見て到達可能な next-hop に変える」ことが求められているので、**peer ルータ側で next-hop-self を有効化する D が正しい**。 [web:448][web:450][web:455]
  - C の「local router で next-hop-self」を設定しても、ローカルが広告するルートの next-hop しか変えられず、「peer から受け取っている 192.168.2.2/32 の next-hop」には影響しない。A の再配布や B の NAT は、この next-hop 到達性の問題を直接は解決しない。 [web:448][web:452][web:455]

***
QUESTION 454 解説

- 問題文（英語）:  
Refer to the exhibit. AS 111 must not be used as a transit AS, but ISP-1 is getting ISP-2 routes from AS 111. Which configuration stops Customer AS from being used as a transit path on ISP-1?

A. ip as-path access-list 1 permit .*  
B. ip as-path access-list 1 permit ^$  
C. ip as-path access-list 1 permit _111_  
D. ip as-path access-list 1 permit ^111$  

Correct Answer: D  [web:461][web:462][web:465][web:471]

- 問題文要約（日本語）:  
AS111 は「自AS のみの経路だけを ISP に広告したい」＝トランジット AS にはなりたくないが、現状 ISP-1 は AS111 経由で ISP-2 のプレフィックスも受け取ってしまっている。  
どの AS-PATH access-list を使えば、「AS 111 が起点の経路だけを通し、AS 111 を通る他 AS 起源の経路（＝トランジット）は止められるか」を問う問題。 [web:461][web:467][web:470]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - BGP の AS-PATH access-list では、正規表現を用いて AS パスをマッチさせる。 [web:466][web:475]
  - `^` は「行頭」、`$` は「行末」を意味するので、`^111$` は  
    - 「AS パス全体が 111 のみ」＝ `111` だけが並んでいる経路  
    つまり「AS111 が **起点** で、他の AS を経由していない経路」にマッチする。 [web:466][web:475]
  - これを `ip as-path access-list 1 permit ^111$` とし、filter-list（または route-map）で ISP への **outbound** に適用すれば、AS111 が自分で起源とするプレフィックスのみを ISP に広告し、AS111 を通過してきた他 AS（例: ISP-2 起源）のプレフィックスは広告しないようにできる。結果として AS111 はトランジット AS にならない。 [web:467][web:468][web:470]
  - A の `.*` は「すべて許可」であり、何もフィルタしない。B の `^$` は「AS パスが空（＝自 AS 内起源）」の IBGP 用であり、このケースには合わない。C の `_111_` は「AS パスのどこかに 111 を含む」経路を permit するので、むしろ AS111 を経由するトランジット経路まで通してしまい、逆効果。 [web:466][web:467][web:475]

***
QUESTION 455 解説

- 問題文（英語）:  
Refer to the exhibit. The route to 192.168.200.0 is flapping between R1 and R2. Which set of configuration changes resolves the flapping route?

A. R2(config)#router ospf 100  
   R2(config-router)#no redistribute eigrp 100  
   R2(config-router)#redistribute eigrp 100 metric 1 subnets  

B. R1(config)#no router rip  
   R1(config)#ip route 192.168.200.0 255.255.255.0 10.40.0.2  

C. R2(config)#router eigrp 100  
   R2(config-router)#no redistribute ospf 100  
   R2(config-router)#redistribute rip  

D. R1(config)#router ospf 100  
   R1(config-router)#redistribute rip metric 1 metric-type 1 subnets  

Correct Answer: D  [web:476][web:477][web:450][web:206]

- 問題文要約（日本語）:  
192.168.200.0/24 への経路が R1 と R2 の間でフラッピングしている。  
R1 は R4 から RIP で 192.168.200.0/24 を学習し、それを EIGRP や OSPF に再配布、R2 も別方向から OSPF/EIGRP に再配布している結果、同じプレフィックスが複数のプロトコル経由で戻り、AD の違いによって R1 上で経路が RIP ↔ OSPF の間を行ったり来たりしている。どの設定変更でこのフラップを止められるか、という問題。 [web:476][web:450]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - ループ／フラップの流れ（要点）:  
    - R1 は R4 から RIP で 192.168.200.0/24 を学習。  
    - R1 が RIP を EIGRP に再配布 → R3 → R2 へ到達。  
    - R2 が EIGRP → OSPF に再配布。  
    - R1 自身も OSPF を動かしており、R2 から「192.168.200.0/24（OSPF External E2）」を受信すると、  
      - RIP の AD 120 より OSPF E2 の AD 110 が低いため、R1 は OSPF 経路を優先してしまう。  
    - すると再配布元が変わるなどして、R1 が再び RIP 経路を選び直す … を繰り返し、フラッピングが発生。 [web:476][web:478]
  - このような「同じ経路が異なる IGP 間で行ったり来たりする」問題の典型的な対処法の一つが、**再配布方向と外部ルートタイプを明確にして“より優先される経路を1つに決める”**こと。 [web:478]
  - 選択肢 D は、R1 の OSPF プロセスで  
    - `redistribute rip metric 1 metric-type 1 subnets`  
      を設定し、R1 起点で RIP 経路を **OSPF External Type 1（E1）** として再配布する。  
    - E1 ルートは、OSPF 内部コスト＋外部コストを合計するため、R2 側で再配布された E2 ルートより優先されるように設計できる。結果として、**R1 は「自分がE1として出した経路」を使い続け、R2 経由で戻ってきた E2 経路を選ぶことがなくなる**ので、フラップが止まる。 [web:476][web:478]
  - A/C は R2 側の再配布条件をいじっているが、問題の根本は「R1 が R2 から戻ってきた OSPF 経路を AD 的に優先してしまうこと」にあるため、R1 側の OSPF 再配布方針を変える D のほうが直接的な解決策。B のスタティック化は、経路としては安定するものの、マルチプロトコル環境での再配布設計という観点からは不適切な “力技” であり、意図された解答ではない。 [web:476][web:477][web:450]

***
QUESTION 456 解説

- 問題文（英語）:  
Which table is used to map the packets in an MPLS LSP that exit from the same interface, via the same next hop, and have the same queuing policies?

A. LDP  
B. CEF  
C. RIB  
D. FEC  

Correct Answer: D  [web:487][web:488][web:278][web:490]

- 問題文要約（日本語）:  
MPLS LSP 内のパケットについて、  
- 同じインターフェースから出て  
- 同じ次ホップへ転送され  
- 同じキューイング（QoS）ポリシーで扱われる  
ようなパケット群をマッピングするのに使われる「テーブル（概念）」は何か、という問題。 [web:487][web:490]

- 正解: **D（FEC）**

- 解説（なぜ D が正しいか）

  - FEC（Forwarding Equivalence Class）は、「**同じ転送処理を受けるパケットの集合**」を表す MPLS の基本概念で、  
    - 同じ次ホップ  
    - 同じ出力インターフェース  
    - 同じ転送処理（例: キューイングポリシー）  
    を共有するパケット群が 1 つの FEC に属する。 [web:490][web:492][web:496]
  - MPLS では、各 FEC にラベルが割り当てられ、そのラベルにもとづいて LFIB を使ってラベルスイッチングが行われる。つまり「どのパケットをどの LSP に乗せるか」を決める単位が FEC。 [web:490][web:492][web:493]
  - RIB は経路情報（プレフィックスと次ホップ）のテーブル、CEF はプレフィックスから出力インターフェース/次ホップを求める高速転送テーブルであり、「同じキューイングポリシーまで含めてひとまとめにする MPLS の抽象概念」としては FEC が該当する。LDP はラベル配布プロトコルであってテーブル名ではない。 [web:489][web:494][web:498]

***
QUESTION 457 解説

- 問題文（英語・要約）:  
R1 で Telnet 接続を Cisco ISE（RADIUS）で認証させたい。  
- ISE: 192.168.1.5（shared secret: Cisco123）  
- R1: 192.168.1.1  
- ISE ダウン時は、ローカルユーザ admin / cisco123 でログインできる必要がある。  

R1 の設定（抜粋）:

aaa new-model
username admin password cisco123
!
radius server ISE1
 address ipv4 192.168.1.5
 key Cisco123
!
aaa group server tacacs+ RAD-SERV
 server name ISE1
!
aaa authentication login RAD-LOCAL group RAD-SERV

ISE ダウン時に Telnet でログインできない。どの 2 つの変更が必要か。 [web:502][web:501][web:505]

- 正解: **B と D**

- 解説（なぜ B と D が正しいか）

  - 今ある AAA method list は `aaa authentication login RAD-LOCAL group RAD-SERV` のみで、「RADIUS グループ（RAD-SERV）で認証する」ことしか書かれていない。ISE が落ちると、このメソッドは失敗し、その後に試すべき「local」が指定されていないため、ローカルユーザにフォールバックできない。 [web:502][web:501][web:505]
  - まず、method list にローカルフォールバックを追加する必要がある:  
    - `aaa authentication login RAD-LOCAL group RAD-SERV local`  
    これが選択肢 B。これで「まず RAD-SERV（ISE）を試し、ダメなら local（ローカルユーザ）」という動きになる。 [web:501][web:505][web:506]
  - 次に、その method list を VTY ラインに適用していないため、Telnet ログイン時に `RAD-LOCAL` が使われていない。  
    - `line vty 0 4`  
      `login authentication RAD-LOCAL`  
    これが選択肢 D で、VTY でのログイン認証に `RAD-LOCAL` メソッドリストを使うよう指定する。 [web:502][web:506]
  - A の `login authentication RAD-SERV` は、存在しない method list 名（RAD-SERV は server group 名）を参照しており不適切。C のような別の method list を作っても、VTY に適用しなければ意味がないし、問題文で与えられた `RAD-LOCAL` を使う方が筋が良い。E は default method list を使うが、問題の前提では `RAD-LOCAL` を使う構成を完成させることが目的になっている。 [web:502][web:505][web:513]

***
QUESTION 458 解説

- 問題文（英語）:  
Refer to the exhibit. The administrator is troubleshooting a BGP peering between PE1 and PE3 that is unable to establish. Which action resolves the issue?

A. P2 must have a route to PE3 to establish a BGP session to PE1.  
B. Ensure that the PE3 loopback address is used as a source for BGP peering to PE1.  
C. Disable sending ICMP unreachables on P2 to allow PE1 to establish a session with PE3.  
D. Remove the traffic filtering rules on P2 blocking the BGP communication between PE1 and PE3.  

Correct Answer: D  [web:515][web:471][web:519]

- 問題文要約（日本語）:  
PE1 と PE3 の間で BGP セッションを張ろうとしているが、確立できない。  
デバッグの結果、P2（アドレス 10.0.12.2）が BGP 用の TCP トラフィックをブロックしていることが示されている。どの対処を行えば BGP ピアリングが確立できるか、という問題。 [web:515]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 問題文中の補足では、PE1 から PE3 のアドレス 10.255.255.3 への ping は成功しており、**IP 到達性やルーティングの問題ではない**ことが示されている。したがって A（経路不足）ではない。 [web:515][web:516]
  - また、PE1 と PE3 は同一 AS 65000 内の iBGP ピア同士であり、`ebgp-multihop` の有無や TTL の問題（C のような ICMP unreachable 抑制）ではない。 [web:515][web:522][web:525]
  - デバッグメッセージには、TCP（おそらく BGP の TCP/179）トラフィックが 10.0.12.2（P2）でブロックされていることが示されているため、**P2 上のフィルタ（ACL やファイアウォール）設定が BGP セッションを阻害している**のが直接原因とわかる。 [web:516][web:519]
  - したがって、「PE1 と PE3 間の BGP 通信用トラフィックをブロックしている P2 上のトラフィックフィルタリングルールを削除（あるいは修正）する」D が正しい対処。 [web:515][web:519]
  - B のような “loopback をソースにする” 変更は、そもそも ACL で TCP/179 が遮断されている状況では効果がなく、根本原因に直接アプローチしていない。 [web:515][web:519]

***
QUESTION 459 解説

- 問題文（英語）:  
Refer to the exhibit. A company builds WAN infrastructure between the head office and POPs using DMVPN hub-and-spoke topology to provide end-to-end communication. All POPs must maintain point-to-point connectivity with the head office. Which configuration meets the requirement at routers R12 and R13? [web:528][web:529][web:404]

A. （NHS トンネル IP／NBMA IP の向きと network-id が不適切）  
B. （NHS トンネル IP＝172.16.1.11、NBMA＝192.168.10.11、network-id 11）  
C. R12・R13 で別々の network-id を使用  
D. NHS トンネル IP／NBMA IP の向きが逆、かつ network-id も別々  

Correct Answer: B  [web:529][web:404]

- 問題文要約（日本語）:  
本社（ヘッドオフィス）を DMVPN ハブ、各 POP（R12, R13）をスポークとする hub-and-spoke 構成で、すべての POP が本社とのポイントツーポイント接続を維持する必要がある。  
R12 と R13 側の DMVPN（NHRP）設定として、どれが正しいかを問う。 [web:528][web:529]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - DMVPN スポーク側の基本形は次の 3 行（＋ network-id 共通）:  
    - `ip nhrp nhs <NHS tunnel IP>`  
    - `ip nhrp map <NHS tunnel IP> <NHS NBMA IP>`  
    - `ip nhrp map multicast <NHS NBMA IP>`  [web:531][web:407][web:534]
  - また、「同じ DMVPN クラウド」に属するハブとスポークは、**同じ `ip nhrp network-id`** を使う必要がある。全 POP は同一クラウド上で本社と接続したいので、R12 と R13 も共通の network-id（ここでは 11）にすべき。 [web:530][web:533][web:536]
  - B の設定は  
    - `ip nhrp nhs 172.16.1.11`（NHS＝ハブのトンネル IP）  
    - `ip nhrp map 172.16.1.11 192.168.10.11`（NHS トンネル IP → NHS NBMA IP）  
    - `ip nhrp map multicast 192.168.10.11`（マルチキャストもハブ NBMA に送る）  
    - `ip nhrp network-id 11`（R12, R13 とも同じ）  
    と、教科書どおりの DMVPN スポーク設定になっている。 [web:529][web:531][web:534]
  - A は `ip nhrp nhs` の引数が NBMA IP になってしまっており、書式が誤り。C と D は R12/R13 で `network-id 12/13` とバラバラにしており、「同じ DMVPN クラウド」に入れない構成になってしまう。 [web:530][web:533]

***
QUESTION 460 解説

- 問題文（英語）:  
An engineer configured VRF-Lite on a router for VRF blue and VRF red. OSPF must be enabled on each VRF to peer to a directly connected router in each VRF. Which configuration forms OSPF neighbors over the network 10.10.10.0/28 for VRF blue and 192.168.0.0/30 for VRF red? [web:538][web:539][web:542][web:206]

A. router ospf 1 vrf blue  
   network 10.10.10.0 0.0.0.240 area 0  
   router ospf 2 vrf red  
   network 192.168.0.0 0.0.0.252 area 0  

B. router ospf 1 vrf blue  
   network 10.10.10.0 0.0.0.3 area 0  
   router ospf 2 vrf red  
   network 192.168.0.0 0.0.0.15 area 0  

C. router ospf 1 vrf blue  
   network 10.10.10.0 0.0.0.252 area 0  
   router ospf 2 vrf red  
   network 192.168.0.0 0.0.0.240 area 0  

D. router ospf 1 vrf blue  
   network 10.10.10.0 0.0.0.15 area 0  
   router ospf 2 vrf red  
   network 192.168.0.0 0.0.0.3 area 0  

Correct Answer: D  [web:538][web:542][web:471][web:206]

- 問題文要約（日本語）:  
VRF-Lite で VRF blue / red が作成済み。  
- VRF blue 側のセグメント: 10.10.10.0/28  
- VRF red 側のセグメント: 192.168.0.0/30  
それぞれに直結している隣接ルータと OSPF ネイバーを張るために、どの `network` コマンド（ワイルドカードマスク）が正しいかを問う。 [web:538][web:542]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - OSPF の `network` コマンドで使うワイルドカードマスクは、**サブネットマスクを 255.255.255.255 から引いた値**になる。 [web:546]
  - 10.10.10.0/28 のサブネットマスクは 255.255.255.240 なので、  
    - 255.255.255.255 − 255.255.255.240 = 0.0.0.15  
    よって `network 10.10.10.0 0.0.0.15 area 0` が正しい。 [web:538][web:546]
  - 192.168.0.0/30 のサブネットマスクは 255.255.255.252 なので、  
    - 255.255.255.255 − 255.255.255.252 = 0.0.0.3  
    よって `network 192.168.0.0 0.0.0.3 area 0` が正しい。 [web:538][web:541][web:546]
  - A/B/C はワイルドカードがサブネットマスクと混ざっており、  
    - /28 に対して 0.0.0.240 や 0.0.0.252  
    - /30 に対して 0.0.0.252 や 0.0.0.240  
    といった誤った値を使っているので、正しいインターフェースを OSPF に参加させられない。 [web:538][web:542]

***
QUESTION 461 解説

- 問題文（英語・要約）:  
Refer to the exhibit. A network engineer receives a report that Spoke 1 users can perform bank transactions with the server located at the Center site, but Spoke 2 users cannot. Which action resolves the issue?

A. Configure encapsulation dot1q 78 on the router C interface.  
B. Configure OSPFv2 on the routers B and C interfaces.  
C. Configure IPv6 on the routers B and C interfaces.  
D. Configure the Spoke 2 users IP on the router B OSPF domain.  

Correct Answer: C  [web:548][web:549][web:551][web:553]

- 問題文要約（日本語）:  
ハブ＆スポーク構成の WAN で、Spoke1 利用者は Center サイトの銀行サーバと通信できるが、Spoke2 利用者はできない。  
図から、Spoke2 と Center 間は IPv6 アドレスで通信しているが、途中の B–C 間リンクが IPv4 しか設定されていないため、Spoke2 からの IPv6 トラフィックが通らないことが示されている。どの対応が正しいかを問う。 [web:548][web:551][web:553]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - Spoke1 側は IPv4 経路で Center サーバに到達しており、B–C 間リンクも IPv4 で問題なく動作している。一方、Spoke2 と Center は IPv6 アドレスで構成されているが、**B–C 間リンクに IPv6 アドレス（およびルーティング）が設定されていない**ため、その区間で IPv6 パケットが通らない。 [web:548][web:550][web:552]
  - したがって、解決には B–C 間リンクにも IPv6 を有効化し、適切な IPv6 アドレスとルーティング（例えば OSPFv3 や EIGRP for IPv6 など）を設定する必要がある。これが選択肢 C。 [web:552][web:555]
  - A の dot1q 設定は VLAN タグに関する話であり、ここでの問題（IPv6 ルーティング欠如）とは無関係。B の OSPFv2 は IPv4 用であり、Spoke2 から Center への IPv6 通信の問題を直接は解決しない。D の「Spoke2 ユーザの IP を OSPF ドメインに追加」も IPv4 OSPF の話で、IPv6 経路が通らない根本原因には触れていない。 [web:548][web:551][web:555]

***
QUESTION 462 解説

- 問題文（英語）:  
Refer to the exhibit. R2 has been receiving routes from R4 that originated outside AS300. A network engineer configured an AS-Path ACL to avoid adding these routes to the R2 BGP table, but the routes are still present in the R2 routing table. Which action resolves the issue?

A. Replace as-path access-list 1 with the ip as-path access-list 1 permit ^300_ command.  
B. Replace as-path access-list 1 with the ip as-path access-list 1 permit ^300$ command.  
C. Replace as-path access-list 1 with the ip as-path access-list 1 permit ^300.* command.  
D. Replace as-path access-list 1 with the ip as-path access-list 1 permit _300.* command.  

Correct Answer: B  [web:526][web:559][web:566][web:569]

- 問題文要約（日本語）:  
R2 は R4 から BGP 経路を受け取っており、その中には「AS300 の外部で起点となった経路」も含まれている。  
R2 側では AS-Path ACL を使って「AS300 起点の経路だけ」を許可したつもりだが、依然として外部起点の経路が R2 の BGP/RIB に残ってしまう。どの正規表現に書き換えればよいかを問う。 [web:526][web:570]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - BGP の AS-PATH ACL では、正規表現で AS パスをマッチさせる。主なパターンは:  
    - `^` = パスの先頭（最初の AS）  
    - `$` = パスの末尾（最後の AS）  
    - `_` = AS 番号の前後を区切る（単語境界） [web:561][web:566][web:569]
  - 「AS300 で **起点** となった経路、かつ AS300 しか含まない（＝AS300 が唯一の AS）」だけを許可したい場合、AS パスは `300` だけになるので、正規表現は  
    - `^300$`（行頭から行末まで 300 だけ）  
    となる。 [web:561][web:566][web:569]
  - B の `ip as-path access-list 1 permit ^300$` は「AS パスが 300 のみ」の経路だけを permit するため、AS300 外部起点の経路（例: `200 300` や `300 65000` など）はマッチせず、filter-list で deny される。 [web:566][web:569]
  - A の `^300_` や C の `^300.*` は、「先頭が 300 なら OK」としてしまうため、`300 65000` のような “AS300 をトランジットで通過しただけ” の外部起点経路も許可してしまい、要件を満たさない。D の `_300.*` は「どこかに 300 を含む全ての経路」を許すので、さらに広くマッチしてしまう。 [web:566][web:569]

***
QUESTION 463 解説

- 問題文（英語）:  
The network administrator must implement IPv6 in the network to allow only devices that not only have registered IP addresses but are also connecting from assigned locations. Which security feature must be implemented? [web:571][web:576][web:578][web:471]

A. IPv6 Prefix Guard  
B. IPv6 Snooping  
C. IPv6 Router Advertisement Guard  
D. IPv6 Destination Guard  

Correct Answer: A

- 問題文要約（日本語）:  
IPv6 を導入するにあたり、  
- IP アドレスが正しく割り当て済みであり  
- かつ「許可された場所（プレフィックス）」から通信している端末だけを許可したい  
という要件がある。どの IPv6 First-Hop Security の機能を使うべきか、という問題。 [web:571][web:578]

- 正解: **A（IPv6 Prefix Guard）**

- 解説（なぜ A が正しいか）

  - IPv6 Prefix Guard は、**正しい形式のアドレスであっても、トップロジ上「割り当てられたプレフィックスの範囲外」から送信されたトラフィックをブロックする**機能。DHCPv6-PD や RA から得た「そのポート／リンクに割り当てられたプレフィックス情報」に基づき、許可されたプレフィックス外をソースとするパケットをドロップする。 [web:572][web:579][web:581]
  - つまり、「登録済み IP（＝正しい形式）かつ、決められた場所（＝割り当てプレフィックス内）からのみ通信を許可」という要件にぴったり対応している。 [web:572][web:579][web:581]
  - IPv6 Snooping（B）は ND/DHCPv6 などを監視してバインディングテーブルを作る“前提機能”であり、アドレス／場所の妥当性チェックそのものではない。RA Guard（C）は不正な Router Advertisement をブロックする機能、Destination Guard（D）は「到達先アドレス」が binding table に存在しない場合の DoS 抑止であり、「ソースが割り当てられたプレフィックスに属しているか」を検証する役割ではない。 [web:442][web:579][web:581]

***
QUESTION 464 解説

- 問題文（英語）:  
What are the two reasons for RD and VPNv4 addresses in an MPLS Layer 3 VPN? (Choose two.)

A. They are used for a BGP session with the CE device.  
B. When the PE redistributes customer routes into MP-BGP they must be unique.  
C. They are on a CE device to use for static configuration.  
D. RD is prepended to each prefix to make routes unique.  
E. VPN RT communities are used to identify customer unique routes.  

Correct Answer: B, D  [web:584][web:200][web:590][web:594]

- 問題文要約（日本語）:  
MPLS L3 VPN で RD（Route Distinguisher）と VPNv4 アドレスを使う理由として、正しいものを 2 つ選べ、という問題。 [web:584][web:589]

- 正解: **B と D**

- 解説（なぜ B と D が正しいか）

  - MP-BGP で PE 間に流す顧客経路は、「IPv4 プレフィックス」に 64ビットの RD を前置した **VPNv4 アドレス** として扱われる。これにより、**複数顧客が同じ IPv4 プレフィックスを使っていても、MP-BGP 上では別物として識別できる**。 [web:589][web:591][web:592]
  - そのため、  
    - 「PE が顧客経路を MP-BGP に再配布する際、それらが一意でなければならない（B）」  
    - 「RD は各プレフィックスの前に付与されて、経路を一意にする（D）」  
    という説明が RD／VPNv4 の目的に合致する。 [web:588][web:591][web:592][web:594]
  - A/C は CE 側の話だが、RD と VPNv4 は **PE 間の MP-BGP（プロバイダ内部）** で用いられる概念であり、CE とのセッションや CE の静的設定には直接関係しない。E は Route Target（RT）コミュニティの役割であり、「どの VPNv4 ルートをどの VRF にインポート／エクスポートするか」を制御する仕組みであって、RD／VPNv4 を使う理由そのものではない。 [web:587][web:588][web:592]

***
QUESTION 465 解説

- 問題文（英語・要約）:  
An administrator must upload the packages.conf file to an FTP server. However, the FTP server rejected anonymous service and required users to authenticate. What are the two ways to resolve the issue? (Choose two.) [web:598][web:607][web:604][web:609]

A. Use the copy flash:packages.conf ftp: command instead, and enter the FTP server credentials when prompted.  
B. Create a user on the router matching the username and password on the FTP server and log in before attempting the copy.  
C. Use the copy flash:packages.conf scp: command instead, and enter the FTP server credentials when prompted.  
D. Use ip ftp username and ip ftp password configuration commands to specify valid FTP server credentials.  
E. Enter the FTP server credentials directly in the FTP URL using the ftp://username:password@192.0.2.40/ syntax.  

Correct Answer: D, E  [web:598][web:607][web:609][web:610]

- 問題文要約（日本語）:  
packages.conf を FTP サーバにアップロードしたいが、FTP サーバが anonymous を拒否し、認証が必須になっている。  
どの 2 つの方法で、Cisco IOS から正しい FTP 認証情報を渡してアップロードできるか、という問題。 [web:598][web:607]

- 正解: **D と E**

- 解説（なぜ D と E が正しいか）

  - D:  
    - グローバルコンフィグで  
      - `ip ftp username <ユーザ名>`  
      - `ip ftp password <パスワード>`  
      を設定すると、その後の `copy flash:packages.conf ftp:` などの FTP 転送時に、これらがデフォルトの認証情報として自動的に使われる。 [web:607][web:604][web:610]
  - E:  
    - `copy flash:packages.conf ftp://username:password@192.0.2.40/` のように URL に直接 `username:password@` を埋め込む書き方もサポートされている。この場合、コマンド実行時にその認証情報で FTP ログインが行われる。 [web:607][web:604]
  - A は単に `copy ... ftp:` を使うだけで、問題文では「anonymous で拒否された」ことから、プロンプトで資格情報を入れていない前提では不十分とされている。B のように「ルータ側に同じユーザを作る」ことは FTP 認証とは無関係（FTP サーバ上のユーザが重要）。C は SCP であり、問題は「FTP の認証方法」を問うているため、意図から外れる。 [web:598][web:607]

***
QUESTION 466 解説

- 問題文（英語・要約）:  
The IT router is connected with the Sales and Marketing departments. The interfaces have been assigned to their respective VRFs to keep the two department routes isolated. Which configuration set must the IT router use for BGP to distribute routes for each department that maintains their own routing table for network isolation? [web:612][web:613][web:617][web:619]

A. VRF ごとに address-family は指定しているが、neighbor は単に remote-as のみ  
B. VRF ごとに address-family を分け、各 neighbor を同一 AS 内の RR クライアントとして定義  
C. グローバル BGP プロセスで全 neighbor を定義（VRF 未使用）  
D. address-family ipv4 unicast（グローバル AF）で全 neighbor を定義  

Correct Answer: B

- 問題文要約（日本語）:  
IT ルータ 1 台に VRF blue（Marketing）と VRF red（Sales）があり、それぞれの部門ルータと iBGP ピアを張って、部門ごとに独立したルーティングテーブル（VRF）内で経路を交換したい。  
どの設定なら、Marketing 用 VRF と Sales 用 VRF を分離したまま BGP で経路配布できるか、という問題。 [web:612][web:619]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - VRF-Lite＋iBGP で部門ごとに経路を分離するには、**VRF ごとの address-family** の中で、その VRF に属するインターフェースに接続された CE（ここでは各部門ルータ）との neighbor を定義する必要がある。 [web:618][web:621]
  - 選択肢 B は、  
    - `router bgp 111` の下で  
      - `address-family ipv4 vrf Marketing` 内に `neighbor 172.16.1.2` と `172.16.2.2`（Marketing 側 CE）を定義し、route-reflector-client として扱う  
      - `address-family ipv4 vrf Sales` 内に `neighbor 172.16.11.2` と `172.16.12.2`（Sales 側 CE）を定義し、同様に route-reflector-client として扱う  
    ことで、**各 VRF 内だけで iBGP 経路が反射され、部門同士はルーティング的に分離されたままになる**。 [web:612][web:618][web:621]
  - A は VRF address-family を使っている点はよいが、RR クライアント指定がなく、設問の意図（IT ルータが部門ルータ間の経路反射役になる）を満たしていない構成として扱われている。C と D は VRF 指定が無くグローバル AF で全 neighbor を混在させるため、Sales/Marketing の経路が一つのテーブルに入り、ネットワーク分離要件を満たさない。 [web:612][web:618][web:625]

***
QUESTION 467 解説

- 問題文（英語・要約）:  
Refer to the exhibit. An engineer must filter EIGRP updates that are received to block all 10.10.10.0/24 prefixes. The engineer tests the distribute list and finds one associated prefix. Which action resolves the issue?

A. There is a permit in the ACL that allows this prefix into EIGRP. The ACL should be modified to deny 10.10.10.0 0.0.0.255.  
B. There is a permit in the route map that allows this prefix. A deny 20 statement is required with no match condition to block the prefix.  
C. There is a permit in the route map that allows this prefix. A deny 20 statement is required with a match condition to match a new ACL that denies all prefixes.  
D. There is a permit in the ACL that allows this prefix into EIGRP. The ACL should be modified to deny 10.10.10.0 255.255.255.0.  

Correct Answer: A  [web:626][web:627][web:631][web:639]

- 問題文要約（日本語）:  
EIGRP の受信アップデートをフィルタして、10.10.10.0/24 のすべてのプレフィックス（例: 10.10.10.0, 10.10.10.128 など）をブロックしたい。  
現在の ACL＋distribute-list をテストしたところ、10.10.10.128 など一部プレフィックスがまだ入ってきてしまっている。どう修正すれば、10.10.10.0/24 全体を正しくブロックできるか。 [web:626][web:627][web:639]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - EIGRP の distribute-list に標準 ACL を使う場合、**ACL は「ルートのネットワークアドレス」にマッチ**させる。ワイルドカードマスクは IPv4 アドレスと組み合わせて、「どのアドレス範囲をまとめてマッチさせるか」を指定する。 [web:633][web:628]
  - 現在の ACL では、  
    - `deny 10.10.10.0 0.0.0.0`  
    のように設定されており、これは「10.10.10.0 というアドレス“だけ”」をマッチさせる。10.10.10.128 など、同じ /24 に属する他のプレフィックスはマッチしない。 [web:626][web:630]
  - 10.10.10.0/24 全体をブロックしたいなら、ワイルドカードマスクを 0.0.0.255 にして、  
    - `deny 10.10.10.0 0.0.0.255`  
    とすべき。これにより、10.10.10.0～10.10.10.255 の範囲のすべてのプレフィックスをまとめて deny できる。 [web:633][web:628]
  - 従って、「ACL にある permit／deny の定義が範囲をカバーできていないのが原因であり、ACL を `deny 10.10.10.0 0.0.0.255` に修正すべき」とする A が正しい。D はワイルドカードの代わりにサブネットマスク 255.255.255.0 を書いており、構文として不正。B, C のような route-map の話ではなく、本問では ACL ベースの distribute-list が前提になっている。 [web:626][web:633][web:639]

***
QUESTION 468 解説

- 問題文（英語・要約）:  
Refer to the exhibit. An engineer is trying to log in to R1 via R3 loopback address. Which action resolves the issue?

A. Remove the IPv6 traffic filter from R1, which is blocking the Telnet.  
B. Add transport input SCP.  
C. Add transport input none.  
D. Remove the IPv6 traffic filter from R1, which is blocking the SSH.  

Correct Answer: A  [web:206][web:385][web:648]

- 問題文要約（日本語）:  
R3 から R1 のループバックアドレスに対してログインしようとしているが、「% Destination unreachable; gateway or host down」のようなメッセージが出て接続できない。  
R1 には IPv6 ACL が `ipv6 traffic-filter` として VTY あるいはインターフェースに適用されており、その ACL が Telnet トラフィックをブロックしている状況である。どの対応を取るべきかを問う。 [web:206][web:385][web:648]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 問題の前提では、R3 から R1 のループバックまでは IPv6 ルーティングで到達可能で、ルーティングの問題ではないが、R1 側で設定されている IPv6 トラフィックフィルタ（IPv6 ACL）が Telnet を拒否しており、その結果「Destination unreachable; gateway or host down」のようなメッセージになっている。 [web:385][web:644][web:648]
  - IPv6 ACL を VTY やインターフェースに適用する場合、`ipv6 traffic-filter <ACL名> in` などで Telnet／SSH の許可・拒否を制御するが、今回の ACL では R3 のソースアドレスから R1 への Telnet が deny されているため、まずこのフィルタを削除（または適切に許可する行を追加）する必要がある。 [web:385][web:648]
  - B の `transport input scp` は管理プロトコルの種類を追加するだけで、ACL によるブロックは解消しない。C の `transport input none` は逆に全ての VTY アクセスを禁止してしまう。D は「SSH をブロックしている」としているが、問題は Telnet ログインであり、解説でも Telnet トラフィックが IPv6 フィルタで阻害されている点が指摘されている。 [web:206][web:385]

***
QUESTION 469 解説

- 問題文（英語）:  
Site1 must perform unequal cost load balancing toward the segments behind Site2 and Site3. Some of the routes are getting load balanced but others are not. Which configuration allows Site1 to load balance toward all the LAN segments of the remote routers?

A. Site2  
   router eigrp 100  
   variance 3  

B. Site1  
   router eigrp 100  
   variance 3  

C. Site2  
   router eigrp 100  
   variance 2  

D. Site3  
   router eigrp 100  
   variance 2  

Correct Answer: B  [web:650][web:651][web:654][web:660]

- 問題文要約（日本語）:  
Site1 から見て、Site2/3 の裏側 LAN（192.168.3.0/24〜192.168.6.0/24）への複数パスがあり、EIGRP で unequal-cost load balancing を行いたい。  
一部ネットワークはロードバランスされているが、192.168.3.0/24 と 192.168.4.0/24 のように、片方のパスしか使われていないものがある。どの設定を入れれば、Site1 からすべてのリモート LAN に対して unequal-cost load balancing が有効になるか、という問題。 [web:650][web:654][web:659]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - EIGRP の variance は、「**そのルータ自身** が持つ successor（最良パス）の FD（feasible distance）を倍率で拡大し、その値より小さい FS（feasible successor）を経路として採用してロードバランスに参加させる」機能。したがって、**実際にロードバランスさせたいルータ（Site1）で variance を設定する必要がある**。 [web:654][web:656][web:659]
  - 192.168.3.0/24 や 192.168.4.0/24 に対する 2 つのパスのメトリックは 281600（best）と 691200 で、  
    - 691200 / 281600 ≒ 2.45  
    となる。variance を 2 にしても、  
    - 281600 × 2 = 563200  
    なので、691200 はこの閾値を超えてしまい、FS として採用されない。そのため、これらのネットワークでは 1 本のパスしか使われない。 [web:650][web:654]
  - そこで Site1 で `variance 3` を設定すると、  
    - 281600 × 3 = 844800  
    となるため、691200 も閾値より小さくなり、2 本目のパスもロードバランスに参加できるようになる。 [web:654][web:657][web:662]
  - A/C/D のように Site2 や Site3 で variance を設定しても、「ロードバランスを行うのは Site1」である以上、Site1 のルーティングテーブルには反映されないため意味がない。**load balancing をしたいルータ自身で variance を設定する**ことが重要で、その条件を満たすのは B のみ。 [web:650][web:656][web:658]

***
QUESTION 470 解説

- 問題文（英語）:  
What is the function of BFD?

A. It negotiates to the highest version if the neighbor version differs.  
B. It provides uniform failure detection on the same media type.  
C. It provides uniform failure detection regardless of media type.  
D. It creates high CPU utilization on hardware deployments.  

Correct Answer: C  [web:663][web:427][web:664][web:665][web:268]

- 問題文要約（日本語）:  
BFD（Bidirectional Forwarding Detection）の“役割”として正しい説明はどれか、という問題。 [web:663][web:427]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - BFD は「隣接ルータ間のフォワーディングパス障害を、ルーティングプロトコルや物理メディアの種類に依存せず、**一貫した短いタイマーで検出する**ための汎用プロトコル」。 [web:427][web:664][web:665][web:669]
  - 公式文書では、BFD は「**あらゆるメディアタイプ、エンキャプスレーション、トポロジ、ルーティングプロトコルに対して、統一された（uniform）障害検出を提供する**」と説明されている。これは選択肢 C の記述と一致する。 [web:427][web:665][web:669]
  - A の「バージョンネゴシエーション」は BFD 実装の副次的な挙動であって、BFD の“機能の定義”として問われることはまれ。B の “同じメディア” は限定的すぎ、D は誤り（BFD は低オーバーヘッド設計であり、「CPU を高く使うこと」が目的ではない）。 [web:663][web:430][web:670]

***
QUESTION 471 解説

- 問題文（英語）:  
An engineer must override the normal routing behavior of a router for Telnet traffic that is destined to 10.10.10.10 from 10.10.1.0/24 via a next hop of 10.4.4.4, which is directly connected to the router that is connected to the 10.1.1.0/24 subnet. Which configuration reroutes traffic according to this requirement? [web:671][web:672][web:676]

A. access-list 100 permit tcp 10.10.1.0 0.0.0.255 host 10.10.10.10 eq 23  
   !  
   route-map POLICY permit 10  
    match ip address 100  
    set ip next-hop recursive 10.4.4.4  

B. access-list 100 deny tcp 10.10.1.0 0.0.0.255 host 10.10.10.10 eq 23  
   !  
   route-map POLICY permit 10  
    match ip address 100  
    set ip next-hop 10.4.4.4  
   route-map POLICY permit 20  

C. access-list 100 permit tcp 10.10.1.0 0.0.0.255 host 10.10.10.10 eq 23  
   !  
   route-map POLICY permit 10  
    match ip address 100  
    set ip next-hop 10.4.4.4  
   route-map POLICY permit 20  

D. access-list 100 permit tcp 10.10.1.0 0.0.0.255 host 10.10.10.10 eq 23  
   !  
   route-map POLICY permit 10  
    match ip address 100  
    set ip next-hop recursive 10.4.4.4.  
   route-map POLICY permit 20  

Correct Answer: A

- 問題文要約（日本語）:  
10.10.1.0/24 から 10.10.10.10 への Telnet トラフィックだけ、通常のルーティングではなく次ホップ 10.4.4.4 へ送るように PBR（Policy-Based Routing）で上書きしたい。  
10.4.4.4 は、このルータに直接つながっている 10.1.1.0/24 サブネットの向こう側にある（＝このルータから見て 1 ホップ先ではない）。どの設定なら要求どおりにリルーティングできるか。 [web:671][web:672]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - まず、PBR で特定トラフィックをマッチさせるには、拡張 ACL で「送信元 10.10.1.0/24・宛先 10.10.10.10・Telnet（TCP/23）」を permit する必要がある。A, C, D の ACL はその条件を満たしている。B は deny なのでマッチせず誤り。 [web:671][web:672]
  - 次に、`set ip next-hop` は「指定した next-hop が **このルータから直接到達可能なアドレス**」であることが前提。今回 10.4.4.4 は、直接ではなく「接続された 10.1.1.0/24 サブネットの先」にいるため、通常の `set ip next-hop` では不十分で、**再帰的にルーティングテーブルを見て next-hop までの経路を解決する** `set ip next-hop recursive` を使う必要がある。 [web:654][web:672]
  - したがって、  
    - 正しいマッチ条件の ACL  
    - `set ip next-hop recursive 10.4.4.4`  
    を組み合わせている A が要件を満たす。 [web:672]
  - D はほぼ正しいが、`set ip next-hop recursive 10.4.4.4.` のように末尾に余分なドットが付いており、コマンドとして無効になる点が指摘されている。C は `recursive` を付けていないため、10.4.4.4 が直接接続でない限り PBR が期待通りに働かない。 [web:672]

***
QUESTION 472 解説

- 問題文（英語・要約）:  
An administrator attempts to download the .pack NBAR2 file using TFTP from the CPE router to another device over the Gi0/0 interface. The CPE has this ACL applied inbound on Gi0/0:

ip access-list extended WAN  
 remark => All UDP rules below for WAN  
 permit udp any eq domain any  
 permit udp any any eq tftp  
 deny udp any any  

tftp-server flash:pp-adv-...pack  

The transfer fails. Which action resolves this issue? [web:681][web:278][web:688][web:692]

A. Change the WAN ACL to permit the entire UDP destination port range.  
B. Shorten the file name to the 8+3 naming convention.  
C. Change the WAN ACL to permit the UDP port 69 to allow TFTP.  
D. Make the permit udp any eq tftp any entry the last entry in the WAN ACL.  

Correct Answer: A

- 問題文要約（日本語）:  
CPE から Gi0/0 経由で TFTP を使って .pack ファイルを他の機器へ転送しようとしているが、Gi0/0 に適用されている WAN ACL により転送が失敗している。  
ACL は「DNS（53/udp）」と「宛先ポート 69（tftp）」だけ許可し、それ以外の UDP を deny している。どの修正をすると TFTP 転送が成功するか、という問題。 [web:681][web:683][web:687]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - TFTP は UDP ベースで、クライアントはサーバの UDP 69 へリクエストを送るが、**実際のデータ転送はサーバ・クライアント双方が 1023 番より上のエフェメラルポートを使う**。つまり、通信の後続パケットは「送信元・宛先ポート 69 以外」の UDP となる。 [web:681][web:682][web:684][web:687]
  - 現在の ACL は  
    - `permit udp any any eq tftp`（宛先 69 のみ許可）  
    - `deny udp any any`（それ以外の UDP 全部禁止）  
    となっているため、最初のリクエスト（宛先 69）は通るが、その後のデータパケット（宛先・送信元ともにエフェメラルポート）は `deny udp any any` にマッチしてブロックされ、転送が途中で失敗する。 [web:681][web:684][web:687]
  - この問題を解決するには、「TFTP セッションで使われるすべての UDP ポート（特にサーバ側のエフェメラルポート）」を許可する必要があるため、**WAN ACL を変更して UDP ポートレンジ全体（少なくとも 1024 以上）を許可する**、という意味を持つ A が正しい。実運用では `permit udp any any` か、少なくとも TFTP サーバ向けの帰りトラフィックを許可する形にする。 [web:681][web:682][web:684]
  - C のように「UDP 69 を許可」するだけでは、既に行っていることと変わらず、データ転送に必要なエフェメラルポートの通信は依然としてブロックされる。B のファイル名長は関係なく、D はエントリ順の話だが、そもそも `permit udp any any eq tftp` の定義自体がポート 69 のみしか許可しないため、順番を変えても後続のデータポートは通らない。 [web:681][web:683][web:687]

***
QUESTION 473 解説

- 問題文（英語）:  
When an FTP client attempts to use passive FTP to connect to the FTP server, the file transfers fail. Which action resolves the issue?

A. Configure to permit TCP ports higher than 1023.  
B. Modify traffic filter FTP-SERVER in to the outbound direction.  
C. Configure active FTP traffic.  
D. Modify FTP-SERVER access list to remove established at the end.  

Correct Answer: A  [web:695][web:697][web:700][web:278]

- 問題文要約（日本語）:  
FTP クライアントが「パッシブモード（PASV）」で FTP サーバに接続すると、制御接続は張れるがファイル転送が失敗している。  
図の ACL では FTP 用の制御ポート 21 などは許可しているが、パッシブ FTP で使われる動的データポートがブロックされている。どの対応を行えばよいか、という問題。 [web:695][web:697]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - パッシブ FTP では  
    - クライアント → サーバ: TCP 21（制御）  
    - データ転送: クライアント・サーバともに **1023 より大きい動的ポート（>=1024）** を使用して別コネクションを張る。 [web:695][web:697][web:705]
  - 多くの ACL サンプルでも、「passive FTP を許可するには、制御ポート 21 に加えて、1024 以上の TCP ポートを宛先として許可する必要がある」と説明されている。 [web:695][web:697]
  - 本問のように「パッシブ FTP でファイル転送だけ失敗」している場合、制御ポート 21 は通っているが、データ用の高番ポートが ACL でブロックされているのが典型的な原因なので、**1023 より大きい TCP ポートを ACL で許可する** A が正しい対処になる。 [web:695][web:697][web:703]
  - C の“アクティブ FTPに変える”方法は、問題要件「パッシブ FTP 利用」に反するし、アクティブ FTP でも別の ACL 修正が必要になるケースがあり、根本解決にはならない。B や D のように方向や established をいじるだけでは、そもそも dynamic data ポートが deny されている事実を解消できない。 [web:697][web:698]

***
QUESTION 474 解説

- 問題文（英語・要約）:  
An administrator configured a Cisco router for TACACS authentication, but the router is using the local enable password instead. Which action resolves the issue?

A. Configure the aaa authentication login default group admin local if-authenticated command instead.  
B. Configure the aaa authentication login admin group tacacs+ local enable none command instead.  
C. Configure the aaa authentication login admin group tacacs+ local if-authenticated command instead.  
D. Configure the aaa authentication login admin group admin local enable command instead.  

Correct Answer: D  [web:708][web:268][web:278][web:471]

- 問題文要約（日本語）:  
TACACS+ 認証を使うように設定したはずなのに、ルータが依然としてローカルの enable パスワードで認証してしまっている。  
`tacacs+` サーバグループは `aaa group server tacacs+ admin` のように「admin」というグループ名で定義されている。どの `aaa authentication` 設定にすれば、期待通り TACACS+ を使うようになるか、という問題。 [web:708][web:717]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 既存設定では、TACACS+ サーバグループの名前が `admin` になっている（`aaa group server tacacs+ admin`）。つまり、`group admin` と書いたときに参照されるのは「TACACS+ サーバグループ admin」であり、`group tacacs+` は**存在しないグループ名**になっている。 [web:708][web:717]
  - そのため、`aaa authentication login admin group tacacs+ ...` のような設定になっていると、実際には有効なサーバグループを指さず、フォールバックの local/enable だけが使われてしまう。 [web:708][web:710]
  - 対して D は、  
    - `aaa authentication login admin group admin local enable`  
    と記述しており、**実際に定義されているサーバグループ名（admin）** を正しく指定している。これにより、ログイン認証時は最初に TACACS+ グループ admin を使い、失敗／到達不能な場合に local → enable へフォールバックする動作となる。 [web:268][web:713][web:718]
  - A は `default` リストを書き換えるが、問題文では `admin` という method list を使う前提になっている。B, C は `group tacacs+` を参照しており、やはり存在しないグループ名を指すため根本的な誤り。 [web:708][web:713]

***
QUESTION 475 解説

- 問題文（英語）:  
An engineer configured BGP and wants to select the path from 10.77.255.57 as the best path instead of current best path. Which action resolves the issue?

A. Configure higher MED to select as the best path.  
B. Configure AS_PATH prepend for the current best path.  
C. Configure lower LOCAL_PREF to select as the best path.  
D. Configure AS_PATH prepend for the desired best path.  

Correct Answer: B  [web:719][web:721][web:278][web:730]

- 問題文要約（日本語）:  
BGP で同じプレフィックスに対して複数パスを学習しており、現在はあるパス（例: 10.57.255.11 からのパス）が best として選ばれている。  
別のパス（10.77.255.57 からのパス）を best にしたいとき、どの操作で “今の best パス” を不利にして、10.77.255.57 のパスを選ばせられるか、という問題。 [web:719][web:722]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - BGP は、属性比較の中で **AS_PATH が短いパスを優先**する。AS_PATH を「長く」見せれば、そのパスの優先度を下げられる。これが AS_PATH prepend。 [web:730][web:729]
  - 10.77.255.57 のパスを新しい best にしたいなら、  
    - そのパスを有利にする（LOCAL_PREF を上げる、など）  
    か、  
    - 今の best パスを不利にする（LOCAL_PREF を下げる、AS_PATH を長くする）  
    のどちらかを行えばよい。問題の選択肢では、「現在の best パス側で AS_PATH を prepend して長くし、相対的に 10.77.255.57 側のパスを短く見せる」B がその考え方に合う。 [web:721][web:730]
  - D のように「望むパス側に AS_PATH を prepend」してしまうと、そのパスの AS_PATH が **逆に長くなり、ますます選ばれにくく**なるので逆効果。A の MED は隣接 AS 内での経路選択にしか影響しないことが多く、この設問の文脈には合わない。C は “lower LOCAL_PREF を選びたいパスに設定” なので、BGP のルール（higher LOCAL_PREF が優先）と逆行してしまう。 [web:719][web:721][web:730]

***
QUESTION 476 解説

- 問題文（英語）:  
A network administrator must block ping from user 3 to the App Server only. An inbound standard access list is applied to R1 interface G0/0 to block ping. The network administrator was notified that user 3 cannot even ping user 9 anymore. Where must the access list be applied in the outgoing direction to resolve the issue?

A. SW1 interface G2/21  
B. R2 interface G1/0  
C. SW1 interface G1/10  
D. R2 interface G0/0  

Correct Answer: C  [web:731][web:732][web:474][web:660]

- 問題文要約（日本語）:  
標準 ACL を R1 G0/0 の inbound に適用して「User3 → App Server への ping だけ」を止めたかったが、User3 は User9 に対しても ping できなくなってしまった。  
標準 ACL は宛先を指定できないため、User3 の“送信元アドレスだけ”でマッチし、結果として「User3 発の全 ping」が R1 でブロックされている。User3 → User9 の通信も許可するには、ACL をどこに outbound で適用し直せばよいか、という問題。 [web:731][web:732][web:735]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 標準 ACL は「送信元 IP のみ」を見てフィルタするため、「User3 の IP を deny」すると、そのユーザ発の全トラフィックがブロックされる。R1 の入り口（G0/0 in）にこれを適用すると、User3 → App Server だけでなく User3 → User9 も止まってしまう。 [web:731][web:733]
  - 標準 ACL のセオリーは「宛先にできるだけ近い場所で適用」すること。User3 → App Server だけを止めたいなら、App Server へ向かう出口側で User3 を拒否し、User9 へ向かう別パスには ACL をかけないようにするのがよい。 [web:731][web:542]
  - 図のトポロジでは、App Server は SW1 の G1/10 側（L3 インターフェース）にいる構成になっており、**User3 から App Server へのトラフィックは SW1 G1/10 から出ていく**。User3 → User9 のトラフィックは SW1 G1/10 を通らないため、ここに ACL を outbound で適用しても User3 → User9 には影響しない。 [web:732][web:735]
  - R2 G0/0 / G1/0 や SW1 G2/21 は、User3 → User9 の経路に乗らないインターフェースであり、そこに ACL を貼っても User3 → App Server のみを狙い撃ちすることはできない。したがって、App Server への出口インターフェースである **SW1 G1/10 の outbound** に ACL を適用する C が正しい。 [web:731][web:732][web:660]

***
QUESTION 477 解説

- 問題文（英語）:  
A network engineer must establish communication between three different customer sites with these requirements:  
- Site-A: must be restricted to access to any users at Site-B or Site-C.  
- Site-B and Site-C: must be able to communicate between sites and share routes using OSPF.  

PE interface configuration:  
interface FastEthernet0/0  
 ip vrf forwarding Site-A  
interface FastEthernet0/1  
 ip vrf forwarding SharedSites  
interface FastEthernet0/2  
 ip vrf forwarding SharedSites  

Which configuration meets the requirements? [web:739][web:278][web:404][web:228][web:268]

A. PE(config)#router ospf 10 vrf Site-A  
   PE(config-router)#network 0.0.0.0 255.255.255.255 area 0  
   PE(config)#router ospf 10 vrf SharedSites  
   PE(config-router)#network 0.0.0.0 255.255.255.255 area 0  

B. PE(config)#router ospf 10 vrf Site-A  
   PE(config-router)#network 0.0.0.0 255.255.255.255 area 0  
   PE(config)#router ospf 20 vrf SharedSites  
   PE(config-router)#network 0.0.0.0 255.255.255.255 area 0  

C. PE(config)#router ospf 10 vrf Site-A  
   PE(config-router)#network 0.0.0.0 255.255.255.255 area 0  
   PE(config)#router ospf 20 vrf SharedSites  
   PE(config-router)#network 0.0.0.0 255.255.255.255 area 1  

D. PE(config)#router ospf 10 vrf Site-A  
   PE(config-router)#network 0.0.0.0 255.255.255.255 area 0  
   PE(config)#router ospf 10 vrf SharedSites  
   PE(config-router)#network 0.0.0.0 255.255.255.255 area 1  

Correct Answer: B

- 問題文要約（日本語）:  
Site-A は Site-B/C とは完全に分離したい（到達不可）。  
Site-B と Site-C は、同じ VRF（SharedSites）に入れて OSPF でルート共有・相互通信させたい。  
PE では、Fa0/0 が VRF Site-A、Fa0/1 と Fa0/2 が VRF SharedSites に既に割り当て済み。どの OSPF 設定がこの要件を満たすか、という問題。 [web:739][web:404][web:744]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - VRF ごとに**独立したルーティングテーブル**を持たせるには、各 VRF の中で別々に OSPF プロセスを動かす必要がある。  
    - `router ospf 10 vrf Site-A` で Site-A 専用の OSPF  
    - `router ospf 20 vrf SharedSites` で SharedSites 専用の OSPF [web:268][web:742]
  - Site-A は「どことも共有しない」ので、Site-A 用 OSPF（プロセス 10）は VRF Site-A だけをカバーすればよい。一方 Site-B/C は同じ VRF SharedSites 内で相互に OSPF でルートを共有したいので、SharedSites 側は**1つの OSPF プロセス（20）で、同じ area 0** に参加させるのが自然。これが選択肢 B の構成。 [web:739][web:740][web:744]
  - A/D のように、異なる VRF で同じ OSPF プロセス番号 10 を使う構成は、試験の想定解としては「VRF ごとにプロセス番号を変えるべき」とされており不適切とされる。C は SharedSites を area 1 にしており、CE 側の設計次第では成立もあり得るが、問題文の意図（B/C 間でシンプルに OSPF 共有）と、解説で「両 VRF とも自前の area 0 を持てる」としている点から B が正解とされている。 [web:228][web:268][web:742]

***
QUESTION 478 解説

- 問題文（英語）:  
The IT router has been configured with the Science VRF and the interfaces have been assigned to the VRF. Which set of configurations advertises Science-1 and Science-2 routes using EIGRP AS 111? [web:748][web:551][web:692]

A. router eigrp 1  
   address-family ipv4 vrf Science autonomous-system 111  
   network 192.168.1.0  
   network 192.168.2.0  

B. router eigrp 111  
   address-family ipv4 vrf Science  
   network 192.168.1.0  
   network 192.168.2.0  

C. router eigrp 111  
   network 192.168.1.0  
   network 192.168.2.0  

D. router eigrp 111  
   address-family ipv4 vrf Science autonomous-system 1  
   network 192.168.1.0  
   network 192.168.2.0  

Correct Answer: A

- 問題文要約（日本語）:  
IT ルータ上で VRF「Science」が作成され、Science-1 / Science-2 のインターフェースはすでにこの VRF に所属している。  
この Science VRF 内で、Science-1・Science-2 のネットワーク（192.168.1.0/24 と 192.168.2.0/24）を **EIGRP AS 111** としてアドバタイズする設定を選ぶ問題。 [web:748][web:228][web:692]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - EIGRP を **Named Mode** で VRF に対して設定するとき、`router eigrp 1` の「1」は **プロセスの“名前”** であり、EIGRP の AS 番号ではない。実際の AS 番号は、`address-family ipv4 vrf <VRF名> autonomous-system <AS>` で指定する必要がある。 [web:755][web:757][web:750]
  - 選択肢 A は  
    - `router eigrp 1`（ここでは 1 は名前）  
    - `address-family ipv4 vrf Science autonomous-system 111` で **Science VRF 内に AS 111 を持つ EIGRP** を作成  
    - `network 192.168.1.0` / `network 192.168.2.0` で Science-1 / Science-2 のプレフィックスを広告  
    という形で、要件「Science VRF で AS 111 を使い、Science-1/2 を広告」に一致している。 [web:748][web:551][web:755]
  - B は `router eigrp 111` だが、Named Mode では 111 は AS ではなく名前として扱われるため、AS 番号 111 をどこにも指定していない状態になる。C は VRF の指定がなく、グローバルテーブルで EIGRP を動かしてしまう。D は AS 番号を `autonomous-system 1` にしており、要求された AS 111 と一致しない。 [web:748][web:228][web:755]

***
QUESTION 479 解説

- 問題文（英語）:  
An engineer must configure PBR on R1 to reach to 10.2.2.0/24 via R3 AS64513 as the primary path and a backup route through default route via R2 AS64513. All BGP routes are in the routing table of R1, but a static default route overrides BGP routes. Which PBR configuration achieves the objective? [file:759]

A. access-list 100 permit ip 10.1.1.0 0.0.0.255 10.2.2.0 0.0.0.255  
   !  
   route-map PBR permit 10  
   match ip address 100  
   set ip next-hop 10.3.3.1  

B. access-list 100 permit ip 10.1.1.0 0.0.0.255 10.2.2.0 0.0.0.255  
   !  
   route-map PBR permit 10  
   match ip address 100  
   set ip next-hop recursive 10.3.3.1  

C. access-list 100 permit ip 10.1.1.0 255.255.255.0 10.2.2.0 255.255.255.0  
   !  
   route-map PBR permit 10  
   match ip address 100  
   set ip next-hop 10.3.3.1  

D. access-list 100 permit ip 10.1.1.0 255.255.255.0 10.2.2.0 255.255.255.0  
   !  
   route-map PBR permit 10  
   match ip address 100  
   set ip next-hop recursive 10.3.3.1  

Correct Answer: B [file:759]

- 問題文要約（日本語）:  
R1 で PBR を使って、10.1.1.0/24 から 10.2.2.0/24 へ向かうトラフィックを、まず R3 側（次ホップ 10.3.3.1）経由で優先的に流したい。  
同時に、R1 にはデフォルトルート（R2 経由）があり、それが BGP ルートより優先されてしまっているが、PBR によって 10.2.2.0/24 宛は R3 へ、その他は従来どおりデフォルトルートを使わせたい。どの PBR 設定がこの要件を満たすかを問う問題。 [file:759]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - まずアクセスリストでは、ソース 10.1.1.0/24、宛先 10.2.2.0/24 のトラフィックだけをマッチさせる必要があり、そのためには **ワイルドカードマスク** を使う必要がある。`255.255.255.0` はサブネットマスクであり、ACL では `0.0.0.255` のようなワイルドカードを使うので、C と D は不適切。 [file:759]
  - 残る A と B はどちらも正しい ACL の書き方だが、`set ip next-hop 10.3.3.1` を使う場合、その次ホップ IP は R1 から見て **直結ネットワーク上のアドレスでなければならない**。この問題では 10.3.3.1 は R1 直結ではなく、R1 は BGP 経由で 10.3.3.1 への到達性を持っている前提のため、「再帰的に」ルーティングテーブルを参照して次ホップを解決できる `set ip next-hop recursive 10.3.3.1` が必要になる。 [file:759]
  - したがって、正しいワイルドカードマスクを用い、かつ直結でない次ホップ 10.3.3.1 を再帰的に解決できるようにしている **B** が要件に合致する。これにより、10.1.1.0/24 → 10.2.2.0/24 のトラフィックだけが R3（10.3.3.1）経由となり、それ以外のトラフィックは静的デフォルトルートを含む通常のルーティングに従う。 [file:759]

***
QUESTION 480 解説

- 問題文（英語）:  
An administrator configures a router to stop using a particular default route if the DNS server 8.8.8.8 is not reachable through that route. However, this configuration did not work as desired and the default route still works even if the DNS server 8.8.8.8 is unreachable. Which two configuration changes resolve the issue? (Choose two.) [file:759]

A. Configure two static routes for the 8.8.8.8/32 destination to match the IP SLA probe for each ISP.  
B. Associate every IP SLA probe with the proper WAN address of the router.  
C. Use a separate IP SLA probe and track object for every static route.  
D. Use a separate track object to reference the existing IP SLA 1 probe for every static route.  
E. Reference the proper exit interfaces along with the next hops in both static default routes.  

Correct Answer: BC [file:759]

- 問題文要約（日本語）:  
管理者は、ルータが「特定のデフォルトルート経由で 8.8.8.8 に到達できないとき、そのデフォルトルートを使わない」ように IP SLA とトラッキングを組み合わせて設定した。  
しかし実際には、8.8.8.8 が到達不能になっても、そのデフォルトルートがルーティングテーブルに残り続けてしまう。どの 2 つの設定変更で「到達不能になった ISP のデフォルトルートだけを正しく落とす」ことができるか、という問題。 [file:759]

- 正解: **B, C**

- 解説（なぜ B と C が正しいか）

  - 既存構成では、複数 ISP に対する 2 本のデフォルトルートが **同じ IP SLA（同じ track オブジェクト）を参照**しているため、片方の ISP やパスだけが落ちても「両方のデフォルトルート」が同時に消えるか、逆に「両方とも残ったまま」という挙動になり、ISP ごとに独立して切り替えができない。 [file:759]
  - そのため、**各デフォルトルートごとに独立した監視系**が必要になる。  
    - 選択肢 C: 各スタティックルート（各 ISP へのデフォルト）に対して、専用の IP SLA と専用の track を用意することで、「ISP1 用 IP SLA が NG → ISP1 側デフォルトだけ消す」「ISP2 用 IP SLA が NG → ISP2 側デフォルトだけ消す」という制御が可能になる。 [file:759]
    - 選択肢 B: さらに、それぞれの IP SLA が **必ず対応する WAN 側アドレス（送信元 IP / source-interface）から 8.8.8.8 に対して probe を飛ばす**ようにしないと、「本来死んでいる ISP 回線経由の疎通ではなく、別 ISP 経由で 8.8.8.8 に届いてしまい、障害を検知できない」という状況が起きるため、各 probe と WAN アドレスの対応付けが必須になる。 [file:759]
  - A や E は、デフォルトルートの制御ではなく 8.8.8.8/32 へのスタティックルートや出口インターフェース指定の話であり、根本原因である「1 つの IP SLA／track で複数デフォルトルートをまとめて制御している」問題を解決しない。D は同じ IP SLA 1 を複数 track で参照するだけなので挙動は変わらない。 [file:759]

***
QUESTION 481 解説

- 問題文（英語）:  
What is LDP label binding? [web:810][web:811][web:812]

A. two routers with label distribution session  
B. neighboring router with label  
C. destination prefix with label  
D. source prefix with label  

Correct Answer: C

- 問題文要約（日本語）:  
「LDP の label binding（ラベルバインディング）とは何か？」を定義として問うシンプルな問題。どの組み合わせを指す用語かを選ぶ。 [web:811][web:815]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - LDP における「label binding」とは、**ある宛先プレフィックス（FEC / destination prefix）と、ある MPLS ラベルを 1 対 1 で結びつけた関連付け**のことを指す。 [web:811][web:815]
  - この「宛先プレフィックス ↔ ラベル」の対応情報が、各 LSR の Label Information Base（LIB）に保存され、LSP 上での転送に使われる。 [web:813][web:816]
  - A や B のような「2 ルータ間のセッション」や「隣接ルータとラベル」の組み合わせは、LDP セッション／隣接関係の説明であり、label binding の定義そのものではない。D の「source prefix」といった用語も LDP の正式な定義とは異なる。 [web:810][web:815]

***
QUESTION 482 解説

- 問題文（英語）:  
The network administrator configured the Chicago router to mutually redistribute the LA and NewYork routes with OSPF routes to be summarized as a single route in EIGRP using the longest summary mask:

router eigrp 100  
 redistribute ospf 1 metric 10 10 10 10 10  
router ospf 1  
 redistribute eigrp 100 subnets  
interface E 0/0  
 ip summary-address eigrp 100 172.16.0.0 255.255.0.0  

After the configuration, the New York router receives all the specific LA routes but the summary route. Which set of configurations resolves the issue on the Chicago router? [file:759]

A. interface E 0/1  
   ip summary-address eigrp 100 172.16.0.0 255.255.0.0  

B. router eigrp 100  
   summary-address 172.16.8.0 255.255.252.0  

C. router eigrp 100  
   summary-address 172.16.0.0 255.255.0.0  

D. interface E 0/1  
   ip summary-address eigrp 100 172.16.8.0 255.255.252.0  

Correct Answer: D [file:759]

- 問題文要約（日本語）:  
Chicago ルータで LA / NewYork 間を相互再配布しており、LA 側の複数の 172.16.8.0/24〜172.16.11.0/24 を EIGRP 側へ 1 本のサマリールートとして広告したい。  
しかし現在の設定では、個別の LA ルートは NewYork に届くものの、サマリールートは届いていない。どの設定を Chicago に加えれば、EIGRP 側に正しいサマリールートを出せるかを問う問題。 [file:759]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - EIGRP のサマリールートを出すときは、**EIGRP をしゃべっているインターフェース上で `ip summary-address eigrp` を設定**する必要がある。今回、`E0/0` は OSPF 用インターフェースであり、そこに summary-address を書いても EIGRP 座標のサマリは実際には出ない。EIGRP 側のインターフェース（NewYork へ向く `E0/1`）に設定する必要がある。 [file:759]
  - まとめたい LA 側プレフィックスは 172.16.8.0/24〜172.16.11.0/24 なので、この範囲を最長マスクでサマリ化すると **172.16.8.0/22（255.255.252.0）** になる。したがって、`ip summary-address eigrp 100 172.16.8.0 255.255.252.0` が正しいサマリアドレス。 [file:759]
  - よって、「EIGRP 側インターフェース E0/1 で」「172.16.8.0/22 を」サマリとして出す D が要件通りとなる。A はマスクが広すぎ、B・C はインターフェースではなくプロセス下に summary-address を書いており、Cisco IOS の正しいサマリのやり方と異なる。 [file:759]

***
QUESTION 483 解説

- 問題文（英語）:  
An engineer must configure DMVPN Phase 3 hub-and-spoke topology to enable a spoke-to-spoke tunnel. Which NHRP configuration meets the requirement on R6? [file:759]

A. Interface Tunnel 1  
   ip address 192.168.1.1 255.255.255.0  
   tunnel source e 0/1  
   tunnel mode gre multipoint  
   ip nhrp network-id 1  
   ip nhrp map 192.168.1.2 192.1.20.2  

B. Interface Tunnel 1  
   ip address 192.168.1.1 255.255.255.0  
   tunnel source e 0/0  
   tunnel mode gre multipoint  
   ip nhrp network-id 1  

C. interface Tunnel1  
   ip nhrp authentication Cisco123  
   ip nhrp map multicast dynamic  
   ip nhrp network-id 1  
   ip nhrp holdtime 300  
   ip nhrp shortcut  

D. interface Tunnel1  
   ip nhrp authentication Cisco123  
   ip nhrp map multicast dynamic  
   ip nhrp network-id 1  
   ip nhrp holdtime 300  
   ip nhrp redirect  

Correct Answer: D [file:759]

- 問題文要約（日本語）:  
DMVPN Phase 3 の Hub-and-Spoke トポロジで、Spoke 同士が直接トンネル（spoke-to-spoke）を張れるようにしたい。  
Hub ルータ R6 上で、どの NHRP 設定を入れると Phase 3 の spoke-to-spoke 通信が成立するかを問う問題。 [file:759]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - DMVPN Phase 3 では、**Hub 側に `ip nhrp redirect`、Spoke 側に `ip nhrp shortcut`** を入れることで、最初は Hub 経由で通信しつつ、Hub からの NHRP Redirect をきっかけに Spoke 同士がダイレクトトンネルを張る動きになる。 [file:759]
  - 選択肢 D は Hub（R6）上のトンネルインターフェースで必要となる `ip nhrp redirect` を含んでおり、Phase 3 の要件「spoke-to-spoke トンネルを有効化」を満たしている。その他の行（authentication, map multicast dynamic, network-id, holdtime）も、典型的な DMVPN Hub の NHRP 設定として妥当。 [file:759]
  - 一方で C は `ip nhrp shortcut` を含んでいるため、どちらかといえば **Spoke 側に入れるコマンド** であり、Hub の R6 に必要なのは redirect のほう。A と B は Phase 3 固有の `redirect` / `shortcut` の設定がなく、ただの mGRE + NHRP 基本設定なので、spoke-to-spoke 最適化は働かない。 [file:759]

***
QUESTION 484 解説

- 問題文（英語）:  
An engineer implemented an access list on R1 to allow anyone to Telnet except R2 Loopback0 to R1 Loopback4. How must sequence 20 be replaced on the R1 access list to resolve the issue? [web:829][web:832]

A. sequence 20 deny tcp host 1001:ABC:2011:7::1 host 400A:0:400C::1 eq telnet  
B. sequence 20 deny tcp host 400A:0:400C::1 host 1001:ABC:2011:7::1 eq telnet  
C. sequence 20 permit tcp host 1001:ABC:2011:7::1 host 400A:0:400C::1 eq telnet  
D. sequence 20 permit tcp host 400A:0:400C::1 host 1001:A8C:2011:7::1 eq telnet  

Correct Answer: A

- 問題文要約（日本語）:  
R1 に IPv6 ACL「DENY_TELNET_Lo4」が設定されており、「基本方針は Telnet 許可、ただし R2 の Lo0 から R1 の Lo4 への Telnet だけ禁止」にしたい。  
しかし、実際のシーケンス 20 の deny 行で R2 の Lo0 アドレスを間違えているため、意図どおりブロックできていない。正しく「R2 Lo0 → R1 Lo4 の Telnet だけ deny」になるように、sequence 20 をどう書き換えるべきかを問う問題。 [web:827][web:833]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 目標は「送信元＝R2 Lo0、宛先＝R1 Lo4、プロトコル＝TCP、ポート＝Telnet(23)」を deny すること。Cisco の公式例では、R1 Lo4 が `400A:0:400C::1`、R2 Lo0 が `1001:ABC:2011:7::1` となっている。 [web:827][web:833]
  - したがって ACL の deny 行は  
    `deny tcp host 1001:ABC:2011:7::1 host 400A:0:400C::1 eq telnet`  
    という「source = 1001:ABC:2011:7::1」「destination = 400A:0:400C::1」の順で書く必要がある。これが選択肢 A。 [web:827][web:836]
  - B は送信元／宛先が逆になっており、「R1 Lo4 → R2 Lo0 への Telnet」をブロックしてしまう。C・D は permit になってしまうため、そもそも「例外的に deny する」という要件を満たさない。 [web:829][web:832]

***
QUESTION 485 解説

- 問題文（英語）:  
An engineer notices that R1 does not hold enough log messages to identity the root cause during troubleshooting. Which command resolves this issue? [web:838][web:841][web:843]

A. (config)#logging buffered 16000 informational  
B. #logging buffered 4096 critical  
C. #logging buffered 16000 critical  
D. (config)#logging buffered 4096 informational  

Correct Answer: A

- 問題文要約（日本語）:  
トラブルシューティングの際、R1 のログバッファにメッセージが十分に残らず、原因特定に必要な過去ログが早く上書きされてしまっている。  
「ログをもっと長く・多く保存できるようにするには、どの logging buffered コマンドを入れるべきか」を問う問題。 [web:842][web:847]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - `logging buffered` のデフォルトサイズは多くの IOS で約 4096 バイトで、これはせいぜい数十件程度のログしか保持できない。ログを長く残すには、**バッファサイズを増やす**必要がある。 [web:842][web:847]
  - 選択肢 A はグローバルコンフィグモードで `logging buffered 16000 informational` を設定しており、バッファを 16KB に拡張しつつ、レベル `informational` までのログ（比較的詳細なレベル）を記録するため、より多く・詳細なメッセージを残せる。 [web:840][web:845]
  - B と D は 4096 バイトと最小サイズのままであり、容量不足の問題を解決できない。C はサイズは増えるがレベル `critical` しか記録しないため、軽微〜中程度の重要度のログが残らず、原因究明には不十分になる可能性が高い。 [web:842][web:843]

***
QUESTION 486 解説

- 問題文（英語）:  
Refer to the exhibit. The remote branch locations have a static neighbor relationship configured to R1 only. R1 has successful neighbor relationships with the remote locations of R2 and R3, but the end users cannot communicate with each other. Which configuration resolves the issue? [file:759]

A. R1  
   interface FastEthernet0/0  
   no ip split-horizon eigrp 10  

B. R2  
   interface FastEthernet0/0.10  
   encapsulation dot1Q  
   ip address 10.10.10.2 255.255.255.0  
   R3  
   interface FastEthernet0/0.10  
   encapsulation dot1Q  
   ip address 10.10.10.3 255.255.255.0  

C. R2 and R3  
   interface FastEthernet0/0  
   no ip split-horizon eigrp 10  

D. R2  
   interface FastEthernet0/0.10  
   encapsulation dot1Q 10  
   ip address 10.10.10.2 255.255.255.0  
   R3  
   interface FastEthernet0/0.10  
   encapsulation dot1Q 10  
   ip address 10.10.10.3 255.255.255.0  

Correct Answer: A [file:759]

- 問題文要約（日本語）:  
リモート拠点（R2・R3）は、スタティックで EIGRP の neighbor を R1 とだけ張っているハブ＆スポーク構成。  
R1 から見た R2 / R3 との EIGRP ネイバーは正常に up しているが、R2 側ユーザと R3 側ユーザ同士が相互に通信できない。どの設定を入れると、拠点同士が互いのネットワークを学習して通信できるようになるか、という問題。 [file:759]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - この構成は「ハブ R1 に対してスポーク R2 / R3 がぶら下がっている」EIGRP のハブ＆スポーク構成で、R2 と R3 は**互いに直接 EIGRP ネイバーになっていない**。EIGRP はデフォルトで **split horizon** が有効なため、「あるインターフェースから学習したルートを、その同じインターフェースからは再広告しない」という動作になる。 [file:759]
  - そのため、R1 は R2 から学習したルートを R3 に再配布できず、逆も同様で、結果として「R2 側ユーザ ⇔ R3 側ユーザ」の経路が作られない。これを解決するには、**ハブ側インターフェース（R1 の Fe0/0）の EIGRP で split horizon を無効化**し、「スポークから学習したルートを別のスポークへ再広告できるようにする」必要がある。 [file:759]
  - 選択肢 A の `no ip split-horizon eigrp 10` を R1 の FastEthernet0/0 に設定することで、R1 が R2・R3 のルートを相互に通告できるようになり、拠点同士の疎通が回復する。C は R2・R3 側で split horizon を無効にしているが、R2 と R3 のインターフェースは互いに向いておらず意味をなさない。B と D は VLAN/サブインターフェースの設定であり、この「ルートがハブで止まっている」問題の根本原因を解決しない。 [file:759]

***
QUESTION 487 解説

- 問題文（英語）:  
Drag and drop the operations from the left onto the locations where the operations are performed on the right.  
Which are the operations of Label Switch Router (Choose two)? [web:813][web:816][web:823]

A. assigns labels to unlabeled packets  
B. performs penultimate hop popping  
C. handles traffic between multiple VPNs  
D. reads the labels and forwards the packet based on the labels  

Correct Answer: BD

- 問題文要約（日本語）:  
MPLS ネットワークで、「Label Switch Router（LSR）」が担当する処理はどれか、という問題。  
LSR の役割として正しいものを 2 つ選ぶ。 [web:813][web:818]

- 正解: **B, D**

- 解説（なぜ B と D が正しいか）

  - LSR は、MPLS コア内部で **ラベルを読んで（label lookup）パケットを転送する**装置であり、到着したラベルに基づいて次ホップへフォワードするのが典型的な動作なので、D「ラベルを読んでラベルに基づき転送する」は LSR の代表的な役割に当たる。 [web:813][web:816]
  - また、LSR のうち「LSP の終点の 1 つ手前のルータ」は、**Penultimate Hop Popping（PHP）** と呼ばれる処理、つまり最終 LSR 手前でラベルを剥がす動作を行う。これも LSR が担う機能なので、B も正しい。 [web:822][web:818]
  - 一方、A の「未ラベルパケットにラベルを付与する」や C の「複数 VPN 間のトラフィックを処理する」といった機能は、主に PE ルータ（Label Edge Router, LER）の役割であり、汎用 LSR の機能とは区別される。 [web:813][web:823]

***
QUESTION 488 解説

- 問題文（英語）:  
Drag and drop the operations from the left onto the locations where the operations are performed on the right.  
Which are the operations of Label Edge Router (Choose two)? [web:813][web:816][web:823]

A. assigns labels to unlabeled packets  
B. performs penultimate hop popping  
C. handles traffic between multiple VPNs  
D. reads the labels and forwards the packet based on the labels  

Correct Answer: AC

- 問題文要約（日本語）:  
MPLS ネットワークで、「Label Edge Router（LER）」が担当する処理はどれか、という問題。  
LER の典型的な役割を 2 つ選ぶ。 [web:813][web:818]

- 正解: **A, C**

- 解説（なぜ A と C が正しいか）

  - LER（PE ルータ）は、IP 世界と MPLS 世界の“境界”に位置し、**未ラベルの IP パケットにラベルを付与して MPLS コアへ入れる / コアから出るときにラベルを剥がす**役割を持つ。そのため A「未ラベルパケットにラベルを付与する」は LER の代表的な操作。 [web:813][web:816]
  - また、MPLS L3 VPN 環境では、LER（特に PE ルータ）が **VRF を複数持ち、VPNv4/6 ルートとラベルを使って複数 VPN を収容・分離**する。これは実質「複数の VPN 間のトラフィックを扱う」役割なので C も LER の動きに対応する。 [web:820][web:823]
  - 一方、B の penultimate hop popping はコア LSR が行う処理であり、D の「ラベルを読んでラベルで転送」は LSR の基本動作で、どちらも「エッジ」よりも「スイッチ（コア）」側の典型機能として区別される。 [web:813][web:818]

***
QUESTION 489 解説

- 問題文（英語）:  
A network engineer cannot remote access R3 using Telnet from switch S1. Which action resolves the issue? [web:852][web:853][web:857]

A. Allow the inbound connection via the exec command on R3.  
B. Add the transport input telnet command on R3.  
C. Allow to use the ssh -l admin 10.0.0.1 command on the switch.  
D. Add the login admin command on the switch.  

Correct Answer: A

- 問題文要約（日本語）:  
スイッチ S1 から R3 に Telnet でリモートアクセスしようとしても、接続ができない。  
R3 では line vty 0 4 の設定に問題があり、ログインしても CLI プロンプト（EXEC モード）に入れない状態になっている。どの操作をすれば Telnet で正常にリモートアクセスできるようになるか、という問題。 [web:852][web:211]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - R3 の `line vty 0 4` に `no exec` が設定されていると、その VTY ラインでは **EXEC セッションが生成されず、コマンドラインへ入れない**。そのため、認証自体は通っても、ユーザは何も操作できない状態となり、実質的にリモートアクセスできない。 [web:211][web:857]
  - これを元に戻すには、`line vty 0 4` コンフィグモードで `exec` と入力し、`no exec` を解除して、デフォルトどおり EXEC を許可する必要がある。選択肢 A の「Allow the inbound connection via the exec command on R3」はこの操作を指している。 [web:852][web:858]
  - B の `transport input telnet` は、Telnet 自体の受付可否に関する設定だが、この問題ではすでに Telnet プロトコルは有効で、VTY 自体の EXEC 禁止が原因。C と D はスイッチ側のコマンドであり、R3 上の VTY の `no exec` 問題を解決しない。 [web:852][web:853]

***
QUESTION 490 解説

- 問題文（英語）:  
What is LDP used for in an LSR? [web:811][web:813][web:818]

A. to create a database of label bindings that allow for hop-by-hop forwarding  
B. to communicate the routes known for a specific interface  
C. to allow for a system-wide exchange of labels across MPLS network  
D. to create a label across the PE routers for end-to-end path assignment  

Correct Answer: A

- 問題文要約（日本語）:  
MPLS ネットワーク内の LSR において、LDP（Label Distribution Protocol）は何のために使われるプロトコルかを問う問題。 [web:813][web:816]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - LDP は、隣接する LSR 同士で「どの宛先プレフィックスに対してどのラベルを使うか」という **label binding 情報を交換し、各 LSR が hop-by-hop で転送できるようにする**ためのプロトコル。これにより、各ルータは自分の LIB/LFIB に「プレフィックス ↔ ラベル」の対応を蓄え、MPLS 転送を行う。 [web:811][web:813]
  - ラベルは各ルータが独立して割り当てるため、LDP で交換されるのは **隣接間（ホップごと）のラベル情報**であり、C のような「ネットワーク全体で統一されたラベル」を配る仕組みではない。D のような PE 間の“エンド・ツー・エンド パス割り当て”は、主に RSVP-TE や Segment Routing などの技術に近い発想で、LDP の役割ではない。 [web:814][web:822]

***
QUESTION 491 解説

- 問題文（英語）:  
Which IPv6 feature enables a device to reject traffic when it is originated from an address that is not stored in the device binding table? [web:864][web:856][web:869]

A. IPv6 DAD Proxy  
B. IPv6 RA Guard  
C. IPv6 Source Guard  
D. IPv6 Snooping  

Correct Answer: C

- 問題文要約（日本語）:  
デバイスが「自分の binding table に登録されていない IPv6 アドレスから来たトラフィック」を拒否できるようにする IPv6 機能はどれか、という問題。 [web:860][web:866]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - IPv6 Source Guard は、DHCPv6・ND inspection・IPv6 snooping などで作られた **IPv6 バインディングテーブル（IPv6 アドレス＋ポート等）を参照し、テーブルに存在しない送信元アドレスからのトラフィックをドロップする**機能。まさに問題文の「binding table に無いアドレスからのトラフィックを拒否する」動作を提供する。 [web:860][web:866]
  - DAD Proxy は重複アドレス検出（Duplicate Address Detection）関連の仕組みであり、RA Guard は不正な Router Advertisement をブロックする機能、IPv6 Snooping はバインディングテーブルを“学習・蓄積”する側の機能であって、実際にテーブルと照合してデータプレーンのパケットを拒否するのは Source Guard。 [web:859][web:870]
  - Cisco の資料でも「IPv6 source guard は、binding table に無いアドレスから来たトラフィックをデバイスが拒否できるようにする」と明記されており、この定義がそのまま設問文に対応している。 [web:860][web:866]

***
QUESTION 492 解説

- 問題文（英語）:  
The primary link between R1 and R2 went down, but R3 is still advertising the 192.168.200.0/24 network to R1 and the 192.168.100.0/24 network to R2, which creates a loop. Which action resolves the issue? [file:759]

A. Configure the eigrp stub command under the EIGRP process on R2.  
B. Configure the summary-address 192.168.0.0 255.255.0.0 100 command on R3.  
C. Configure the eigrp stub leak-map command under the EIGRP process on R1.  
D. Configure the eigrp stub command under the EIGRP process on R3.  

Correct Answer: D [file:759]

- 問題文要約（日本語）:  
R1–R2 間のプライマリリンクがダウンしたあとも、R3 は  
- R2 側のネットワーク 192.168.200.0/24 を R1 に広告し、  
- R1 側のネットワーク 192.168.100.0/24 を R2 に広告し続けている。  
その結果、R1 と R2 間のトラフィックが R3 経由でループしてしまっている。  
このループを止めるには、どの設定を入れるべきか、という問題。 [file:759]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - ループの原因は、**R3 が「自分で学習した EIGRP ルート」をさらに別の方向へ再広告してしまっている**こと。R1⇔R2 のプライマリリンクが切れた状態で、R3 が「R1 から学習した 192.168.100.0/24」を R2 に、「R2 から学習した 192.168.200.0/24」を R1 に流し続けるため、行き先が互いを指し合う形になりループが発生する。 [file:759]
  - EIGRP の **stub** は「スタブルータは自分の直結ネットワークなど“自前”の情報だけを広告し、他から学習したルートを再広告しない」ようにする機能。R3 に対して `router eigrp <AS>` 配下で `eigrp stub` を設定すれば、R3 は中継拠点としてルートをばらまかなくなり、R1 と R2 間でのループが解消される。 [file:759]
  - B の `summary-address` は OSPF 外部ルート用のコマンドであり、この EIGRP ベースのルーティングループには根本的に関係しない。A や C のように R1/R2 側を stub/leak-map でいじっても、「ループの発信源」である R3 の振る舞いが変わらないため、問題解決には不十分。 [file:759]

***
QUESTION 493 解説

- 問題文（英語）:  
Which configuration command reestablishes the connectivity from 172.16.1.16/28 to the server subnet 172.16.2.16/28? [file:759]

A. R1(config)#route-map ENT permit 20  
B. R1(config)#access-list 20 permit 172.16.2.16 0.0.0.15  
C. R3(config-router)#no eigrp stub  
D. R1(config)#ip route 172.16.2.16 255.255.255.240 172.16.250.1 91  

Correct Answer: A [file:759]

- 問題文要約（日本語）:  
172.16.1.16/28（左下の PC サブネット）から 172.16.2.16/28（右下のサーバサブネット）への通信ができない。  
原因は、R1 から R3 への EIGRP ルート配布を制御する `route-map ENT` が **deny ステートメントのみ**で構成されており、結果として「特定のプレフィックスだけでなく、すべての EIGRP ルートが R3 に送られていない」こと。R3 は 172.16.2.16/28 へのルートを学習できず、PC からのトラフィックを破棄してしまう状態。  
この状況を直し、R3 に他の EIGRP ルート（含 172.16.2.16/28）が届くようにするために、どのコマンドを投入すべきかを問う問題。 [file:759]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 現状の `route-map ENT` は、シーケンス 10 で `access-list 20` にマッチする経路を `deny` しており、その後に **permit ステートメントが存在しない**ため、「ACL 20 にマッチしない他の EIGRP ルート」も暗黙の `deny` により R3 へ一切送られていない。 [file:759]
  - R3 が 172.16.2.16/28 を含む他の経路を受け取れるようにするには、「ACL 20 で明示的に deny したプレフィックス以外は全部許可する」必要がある。そのためには `route-map ENT` に **末尾の permit エントリ**を追加すればよく、それが選択肢 A の `route-map ENT permit 20` である。 [file:759]
  - B のように ACL 20 に 172.16.2.16/28 を追加すると、そのプレフィックスも「deny 対象」に含まれてしまう（seq10 は deny のまま）ので逆効果。C は R3 を非スタブにするだけで、R1 側の route-map による遮断は変わらない。D はスタティックルートでの力技だが、問題の意図（route-map のミスで全ルートが送られていない）から外れる。 [file:759]

***
QUESTION 494 解説

- 問題文（英語）:  
It was noticed that after NetFlow is configured in the router, the collector stopped receiving flow information. Which action resolves the issue? [file:759]

A. Configure an IP address on the loopback2 interface to use as a source.  
B. Modify the source through the ip flow-export source loopback1 command.  
C. Change the IP address of the loopback 2 interface to a public IP address.  
D. Apply the ip flow egress command to the loopback2 interface.  

Correct Answer: A [file:759]

- 問題文要約（日本語）:  
ルータ R1 に NetFlow を設定した直後から、フローデータコレクタがフロー情報を受信しなくなった。  
`show ip flow export` を見ると、本来は  
- Source: 172.16.1.1 (Loopback2)  
となるはずが、誤った操作により Loopback2 の IP アドレスが削除され、  
- Source: 172.16.1.1 (Unknown)  
と表示されている。このせいで R1 がフローを送信しなくなっている。どの操作を行えば、再びコレクタへフロー情報が送られるようになるか、という問題。 [file:759]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - NetFlow では `ip flow-export source <interface>` で指定したインターフェースの **IP アドレスを送信元 IP** としてエクスポートパケットを送る。問題文のケースでは Loopback2 がソースに設定されている。 [file:759]
  - しかし Loopback2 から IP アドレスが削除されると、ルータは「ソース IP 不明」となり、フローのエクスポートが行われなくなる。`show ip flow export` に `Source(1) 172.16.1.1 (Unknown)` と出ているのがその状態。 [file:759]
  - よって、Loopback2 に再度 IP アドレスを設定し直せば、`Source(1) 172.16.1.1 (Loopback2)` に戻り、再びコレクタにフローデータが送信される。B はソースインターフェースを変える案だが、既に Loopback2 を前提に設計されている環境の前提を変えてしまう。C は「パブリック IP 必須」ではないので不要、D の egress 設定はエクスポート元 IP 問題の解決には関係しない。 [file:759]

***
QUESTION 495 解説

- 問題文（英語）:  
R1 should receive 10.16.2.0/24 from R2. Which action resolves the issue? [file:759]

A. Modify prefix-list seq 5 on R2 to permit 10.16.0.0/23.  
B. Add prefix-list seq 1 on R1 to permit 10.16.2.0/24.  
C. Add prefix-list seq 1 on R1 to permit 10.16.0.0/22.  
D. Modify prefix-list seq 5 on R2 to permit 10.16.0.0/22.  

Correct Answer: D [file:759]

- 問題文要約（日本語）:  
R2 から OSPF（あるいは同等の IGP）で 10.16.2.0/24 を R1 に広告したいが、R1 のルーティングテーブルに 10.16.2.0/24 が入ってこない状況。  
確認すると、エリア間の経路フィルタに使っているプレフィックスリストの設定ミスにより、R2 側で 10.16.2.0/24 が「送る前にフィルタされている」。R1 側のフィルタは 10.16.2.0/24 を特に deny しておらず、到着さえすれば受け取れる状態。  
どの設定変更を行えば、R1 が R2 から 10.16.2.0/24 を受信できるようになるか、という問題。 [file:759]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 既存の R2 上の prefix-list `filter-area-0` は、本来「10.16.0.0〜10.16.3.0 の /24 をまとめて許可したい」意図で `10.16.1.0/23` を書いたが、IOS 上では自動的に `10.16.0.0/23` として解釈される。 [file:759]
  - `10.16.0.0/23 le 24` がカバーするのは、`10.16.0.0/24` と `10.16.1.0/24` のみであり、`10.16.2.0/24` と `10.16.3.0/24` は範囲外のため「明示 permit されていない」扱いとなる。その結果、プレフィックスリストの最後の暗黙の deny により、R2 は 10.16.2.0/24 を広告しない。 [file:759]
  - 一方、R1 側の prefix-list（`filter-area-13`）では 10.16.2.0/24 を明示的に deny しておらず、最後の暗黙 permit により「受け取りは可能」。したがって修正すべきは送信元の R2 側だけでよい。 [file:759]
  - 10.16.0.0〜10.16.3.0 の /24 を一括で permit したいので、`seq 5` を `10.16.0.0/22 le 24`（= 選択肢 D）に変更すれば、10.16.0.0/24, 10.16.1.0/24, 10.16.2.0/24, 10.16.3.0/24 の 4 つ全てが許可されるようになり、R1 は 10.16.2.0/24 も学習できる。 [file:759]

***
QUESTION 496 解説

- 問題文（英語）:  
The non-area 0 routers in OSPF still receive more specific routes of 10.1.1.0, 10.1.2.0, 10.1.3.0 from area 0. Which action resolves the issue? [file:759]

A. Configure the summary-address 10.1.0.0 255.255.252.0 command under OSPF process.  
B. Configure route summarization on OSPF-enabled interfaces.  
C. Summarize by using the summary-address 10.1.0.0 255.255.252.0 command.  
D. Summarize by using the area range command on ABRs.  

Correct Answer: D [file:759]

- 問題文要約（日本語）:  
OSPF でエリア 0 に 10.1.1.0/24, 10.1.2.0/24, 10.1.3.0/24 など複数プレフィックスが存在している。  
エリア 0 以外のルータには、本来 10.1.0.0/22 などの **サマリールートだけ** を配りたいのに、個別の 10.1.1.0/24 など「より詳細な経路」がそのまま届いてしまっている。  
この状況を解消して、非エリア 0 側にはサマリーだけを広告するには、どの設定を行うべきかを問う問題。 [file:759]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - OSPF には 2 種類のサマリコマンドがあり、役割が異なる。  
    - `area <id> range`：**ABR** 上で「別エリアに出す *inter-area* ルート（タイプ 3 LSA）」を集約する。  
    - `summary-address`：**ASBR** 上で「外部経路（タイプ 5, 7 LSA）」を集約する。 [file:759]
  - 本問の 10.1.1.0/24 などは「エリア 0 内のルート」であり、他エリアへは「タイプ 3 LSA（inter-area）」として ABR から広告される。この集約には `area <area-id> range 10.1.0.0 255.255.252.0` のような **area range コマンドを ABR に設定**する必要がある。 [file:759]
  - 一方、選択肢 A や C のような `summary-address 10.1.0.0 255.255.252.0` は、**外部経路（外部 AS から再配布されたルート）専用**であり、エリア 0 内プレフィックスの inter-area サマリには効かない。そのため、目的どおりに「non-area 0 ルータに対して 10.1.1.0/24 などを隠して 10.1.0.0/22 だけを配る」のは D のみ。 [file:759]

***
QUESTION 497 解説

- 問題文（英語）:  
R3 is learning the 1.0.0.0/24 route through OSPF instead of EIGRP. Which action causes R3 to choose EIGRP to reach the 1.0.0.0/24 network? [file:759]

A. Configure OSPF administrative distance to 120.  
B. Configure EIGRP administrative distance to 110.  
C. Configure OSPF administrative distance to 200.  
D. Configure EIGRP administrative distance to 120.  

Correct Answer: C [file:759]

- 問題文要約（日本語）:  
R3 には 1.0.0.0/24 への経路が、OSPF と EIGRP の両方から到達している。  
・OSPF: 1.0.0.0/24 は OSPF ルート（AD 110）  
・EIGRP: 1.0.0.0/24 は **外部 EIGRP ルート**（AD 170）  
デフォルトでは AD が小さい OSPF の経路が選択されてしまい、要件である「EIGRP の経路を優先」は満たされていない。  
R3 が 1.0.0.0/24 へ到達する際に、OSPF より EIGRP を選ばせるには、どの AD 調整が適切か、という問題。 [file:759]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 現状の AD は一般的なデフォルト値で  
    - OSPF 全ルート: 110  
    - 外部 EIGRP: 170  
    となっているため、同じプレフィックス 1.0.0.0/24 に対しては **OSPF ルートが必ず勝つ**。 [file:759]
  - 1.0.0.0/24 について EIGRP（外部ルート）を選ばせるには、  
    - OSPF の AD を 170 より大きく上げる（例: 200 にする）、  
    または  
    - 外部 EIGRP の AD を 110 より小さく下げる  
    のどちらかが必要になる。 [file:759]
  - 選択肢 C の「OSPF の AD を 200 に設定」すれば、1.0.0.0/24 に対しては AD 170 の外部 EIGRP が優先されるようになり、問題の要件を満たす。A（120）はまだ 170 より小さいため OSPF が勝ったまま、B・D は EIGRP を OSPF と同等またはより不利にしてしまうだけで解決にならない。 [file:759]

***
QUESTION 498 解説

- 問題文（英語）:  
Which characteristic is representative of a hub-and-spoke topology between PE routers in a Layer 3 MPLS VPN network? [file:759]

A. The PE routers use different RTs to import and export M-BGP prefixes.  
B. Each PE router uses a different RD to identify all branches.  
C. The PE routers use different RDs for each VRF to import and export M-BGP prefixes.  
D. The PE routers are configured with multiple VRFs for all branches.  

Correct Answer: A [file:759]

- 問題文要約（日本語）:  
L3 MPLS VPN で、PE 間が「ハブ＆スポーク型トポロジ」になるようにしたい。  
つまり、スポーク拠点同士は直接通信せず、必ずハブ拠点を経由して通信するように制御したい。  
このようなハブ＆スポーク構成を、PE 間の BGP（MP-BGP）ルート制御の観点で実現している代表的な特徴はどれかを問う問題。 [file:759]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - MPLS L3VPN のハブ＆スポークでは、「スポーク同士の VPNv4 ルートを互いにインポートさせない」ことで、トラフィックを必ずハブ経由にする。これを実現するのが **RT（Route Target）** の設計で、ハブ VRF とスポーク VRF で「インポート RT」と「エクスポート RT」を意図的に変える。 [file:759]
  - 具体的には、スポークが「自サイトの経路をハブにだけ輸出する RT」と「ハブからの経路だけを取り込む RT」を使い分けることで、スポーク間ではそもそも経路が見えないようにする。これが選択肢 A の「PE ルータが M-BGP プレフィックスをインポート・エクスポートするのに異なる RT を使用する」という記述。 [file:759]
  - RD は「経路の一意識別子」であってトラフィック制御ではなく（B, C は役割の取り違え）、D の「全ブランチ用に複数 VRF を持つ」はハブ＆スポークの要件ではない。 [file:759]

***
QUESTION 499 解説

- 問題文（英語）:  
How does MPLS Layer 3 VPN function? [file:759]

A. When an EIGRP internal route is redistributed into BGP by one PE and then back into EIGRP by another PE, the originating router ID for the route is changed to the router ID of the first PE.  
B. When a VPN route is learned from a CE device and injected into IGP, a VPN route distinguisher attribute is associated with it.  
C. When a destination PE device receives a labeled packet, it pops the label and uses it to forward the packet to the correct CE device.  
D. When a PE device forwards a packet received from a CE device across the provider network, it labels the packet with the label learned from the source PE device.  

Correct Answer: C [file:759]

- 問題文要約（日本語）:  
MPLS L3 VPN でのパケット転送の仕組みについて問う問題。  
特に「宛先 PE（出口側 PE）が、MPLS ラベル付きパケットをどう処理して CE へ転送するか」という振る舞いを、最も正しく説明している選択肢を選ぶ。 [file:759]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - MPLS L3 VPN では、**P・PE 間のコア部分では MPLS ラベルで転送し、宛先 PE でラベルを外して CE へ通常の IP 転送**を行う。宛先側 PE は、受信したラベルを POP（削除）し、そのラベルに対応付けられた VRF / CE 向けインターフェースに基づいてパケットを CE にフォワードする。これが選択肢 C の記述と一致する。 [file:759]
  - A は「ルータ ID が変わる」という挙動であり、この問題の L3VPN の本質ではない。B は CE から学習したルートを IGP に注入する（実際は PE の VRF→MP-BGP）という誤った前提、D は「ソース PE から学んだラベルを使う」としており、実際は **各ホップで下流から学んだラベルでスイッチング**するので不正確。 [file:759]

***
QUESTION 500 解説

- 問題文（英語）:  
The client received the IPv6 address from the IPv6 DHCP server but the show command does not show the IPv6 DHCP bindings on the server. Which action resolves the issue? [file:759]

A. Configure authorized DHCP servers to avoid IPv6 addresses from a rogue DHCP server.  
B. Extend the DHCP lease time because R1 removed the IPv6 address earlier after the lease expired.  
C. Configure H1 as the DHCP client that manually assigns the IPv6 address on interface e0/0.  
D. Use the 2001:DBB:BAD:C0DE::/64 prefix for the DHCP pool on R1.  

Correct Answer: A [file:759]

- 問題文要約（日本語）:  
クライアントは IPv6 DHCP サーバから IPv6 アドレスを受け取っているが、サーバ側で `show` コマンドを実行しても、そのクライアントの DHCPv6 バインディング情報が表示されない状況。  
つまり「クライアントの IPv6 アドレスはどこかの DHCPv6 から配られているのに、想定している DHCP サーバにはその記録がない」。  
この問題を解消するために、どの対策を行うべきかを問う問題。 [file:759]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - クライアントが IPv6 アドレスを持っているにもかかわらず、想定している DHCPv6 サーバにバインディングが無い場合、**別の「なりすまし DHCPv6（rogue DHCP server）」からアドレスを受け取っている可能性**が高い。 [file:759]
  - このような攻撃や誤配置を防ぐために、スイッチ側で **DHCPv6 Guard などの「authorized DHCP server」を指定する機能**を使い、「許可された DHCP サーバからの応答だけを通過させる」ようにするのが定石となる。これにより、クライアントは正しいサーバからのみアドレスを受け取り、そのサーバのバインディングにも正しく反映される。 [file:759]
  - B は単なるリース時間の問題ではないので的外れ、C は DHCP の仕組みと矛盾しており（手動設定ではなく動的付与が前提）、D はプレフィックスを変えるだけで rogue DHCP の問題を解決できないため不適切。 [file:759]

***
QUESTION 501 解説

- 問題文（英語）:  
Which configuration allows the operation level 1 team of 10 engineers to log in at least three at a time to router R3 using network credentials over HTTP? [file:759]

A. R3(config)#ip http authentication aaa  
   R3(config)#no access-list 20 permit 172.16.10.48 0.0.0.15  
   R3(config)#access-list 20 permit 172.16.10.48 0.0.0.7  

B. R3(config)#ip http authentication aaa  
   R3(config)#ip http max-connections 3  

C. R3(config)#ip http max-connections 3  
   R3(config)#ip http accounting commands 3 default  

D. R3(config)#ip http authentication enable  
   R3(config)#no access-list 20 permit 172.16.10.48 0.0.0.15  
   R3(config)#access-list 20 permit 172.16.10.48 0.0.0.7  

Correct Answer: B [file:759]

- 問題文要約（日本語）:  
運用レベル 1 のエンジニア 10 人が、「HTTP 経由」で R3 にログインできるようにしたい。  
同時ログイン数は少なくとも 3 人まで許可する必要があり、認証にはネットワーク上の認証情報（AAA サーバなど）を使う前提。  
どのコンフィグが、この要件を満たすかを問う問題。 [file:759]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - すでにアクセスリスト 20 は `172.16.10.48/28` を正しく表す `172.16.10.48 0.0.0.15` で設定済みであり、ACL を変更する必要はない。A・D のように ACL を 0.0.0.7 に変えるとサブネット範囲を誤って狭めてしまう。 [file:759]
  - 「同時に最低 3 人 HTTP ログインを許可する」には、HTTP サーバ側で **`ip http max-connections 3`** を設定するのが正攻法であり、これを含む B または C が候補になる。 [file:759]
  - C の `ip http accounting commands 3 default` は AAA accounting（コマンド課金）の設定であり、`aaa new-model` や AAA サーバなど追加要素が必要で、問題文には一切触れられていない。一方 B は `ip http authentication aaa` と `ip http max-connections 3` のみで、AAA 認証を用いながら同時接続数も 3 に制御でき、余計な依存関係も増やさないため要件に最も適合する。 [file:759]

***
QUESTION 502 解説

- 問題文（英語）:  
Routers R1 and R2 exchange routes to each other's loopback through OSPF. Telnet traffic must be blocked from R2 Lo0 to R1 Lo2. Which configuration resolves the issue? [file:759]

A. R1: Lo1, Lo2, G0/0 の IPv6 OSPF 設定 + IPv6 ACL DENY_TELNET_Lo2 (seq 20 deny tcp 100B:1:310B::1 → 200A:0:210C::1 telnet + permit any)  

B. A + `ipv6 access-class DENY_TELNET_Lo2 in` を G0/0 に追加  

C. Lo2 に `ipv6 access-class DENY_TELNET_Lo2 in` + A の設定  

D. A + `ipv6 traffic-filter DENY_TELNET_Lo2 in` を G0/0 に追加  

Correct Answer: D [file:759]

- 問題文要約（日本語）:  
R1 と R2 は OSPF で互いのループバックを広告し合っている。  
R2 Lo0（`100B:1:310B::1`）から R1 Lo2（`200A:0:210C::1`）への **Telnet だけをブロック**したい。  
どのコンフィグがこれを実現するかを問う問題。 [file:759]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - IPv6 で特定のソース→宛先 Telnet をブロックするには、IPv6 ACL を作成し、これを適切な場所に適用する。ACL は全選択肢で共通の `deny tcp R2-Lo0→R1-Lo2 telnet + permit ipv6 any any`。 [file:759]
  - **R1 で「R2 Lo0 → R1 Lo2 Telnet」をブロックするには、R1 の受信側インタフェース（G0/0）で inbound ACL を適用**する。`ipv6 traffic-filter DENY_TELNET_Lo2 in` がこれを実現し、R2 からの Telnet パケットを G0/0 でドロップ。 [file:759]
  - B の `ipv6 access-class` は **VTY ライン**専用（リモートアクセス制御用）でインタフェースには適用不可。C は Lo2 に適用しても、Telnet は Lo2 宛のローカル接続で既に到達済み。A は ACL 定義のみで適用なしのため効かない。よって D のみ正しい。 [file:759]

***
QUESTION 503 解説

- 問題文（英語）:  
An engineer is investigating an OSPF issue reported by the Cisco DNA Assurance Center. Which action resolves the issue? [file:1]

A. One of the neighbor links is down. Bring the interface up by running shut and no shut.  
B. An ACL entry blocking multicast on the interfaces. Allow multicast through the interface ACL.  
C. One of the interfaces is using the wrong authentication. Match interface authentication on both links.  
D. One of the interfaces is using the wrong MTU. Match interface MTU on both links.  

Correct Answer: D [file:1]

- 問題文要約（日本語）:  
Cisco DNA Assurance Center から「OSPF の問題」が報告されており、その **OSPF ネイバー問題を解消するために必要なアクションはどれか** を問う問題。  
出力ではネイバーが **EXSTART 状態で止まっている** 状況が示されている。 [file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - OSPF ネイバーが **EXSTART 状態から先に進まない典型的な原因は MTU 不一致**。EXSTART/EXCHANGE で DBD パケットのやり取り時に、片方のルータが「自分のインタフェース MTU と異なる」と判断するとネイバー関係が正しく確立されない。 [file:1]
  - 選択肢 D の「One of the interfaces is using the wrong MTU. Match interface MTU on both links.」は、両側インタフェースの MTU を一致させることで、この EXSTART 状態の問題を解消する正しい対処となる。 [file:1]

  - A の「リンクダウン」は、ネイバーが DOWN/INIT などになる原因であり、EXSTART 状態が継続する典型要因ではない。 [file:1]
  - B の「マルチキャストを ACL でブロック」は、Hello 自体が届かずネイバーが形成されないケースに近く、これも EXSTART に特有ではない。 [file:1]
  - C の「認証不一致」は、OSPF 認証エラーとして扱われ、通常はネイバーが確立されない（EXSTART まで進まない）。EXSTART 状態にフォーカスしている本問題の説明とは合致しない。 [file:1]
  - 質問の「Since the neighboring problem occurs in EXSTART state, it is usually related to MTU mismatched.」という公式解説の通り、EXSTART で止まる OSPF ネイバー問題は **MTU を両端で一致させることで解消** されるため、D が唯一正しい。 [file:1]

***
QUESTION 504 解説

- 問題文（英語）:  
Which Layer 3 VPN attribute allows different customers to connect to the same MPLS network with overlapping IP ranges?

A. MP-BGP  
B. RD  
C. RT  
D. VRF  

Correct Answer: B [cite:1]

- 問題文要約（日本語）:  
同じ MPLS コアネットワークに **異なる顧客が重複した IP アドレスレンジを使ったまま接続できるようにする Layer 3 VPN の属性はどれか** を問う問題。 [cite:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - MPLS L3VPN では、各顧客 VRF 内のプレフィックスに **RD（Route Distinguisher）** を付与することで、同じ IPv4 アドレスでも「別々の一意な VPNv4 ルート」として区別できるようにしている。例えば、顧客Aの 10.0.0.0/24 と顧客Bの 10.0.0.0/24 に異なる RD（例: 65000:1, 65000:2）を付けることで、MPLS コア内では別物として扱える。 [cite:1]
  - この **「アドレス空間の重複を許容する」ための仕組みそのものが RD の役割** であり、問題文の「overlapping IP ranges」を実現している属性に該当する。 [cite:1]

  - A の MP-BGP は、VPNv4/VPNv6 ルートを PE 間で配布するための拡張 BGP であり、「重複アドレスを区別する」役割は RD に依存している。  
  - C の RT（Route Target）は、「どの VRF にどの VPN ルートをインポート/エクスポートするか」という **VPN 間の経路配布ポリシー** を決めるタグであり、アドレス重複そのものの解決ではない。  
  - D の VRF は「論理ルーティングテーブルの分離」を行う単位だが、MPLS コア上でルートを一意に識別して区別するのは RD と MP-BGP の役割であり、問題文の「overlapping IP ranges を可能にする属性」として直接名指しされるのは RD。 [cite:1]

***
QUESTION 505 解説

- 問題文（英語）:  
An engineer must advertise LAN network 192.168.1.0 of router A to router B through OSPF. The engineer notices that router B was configured, but the LAN network of router A is not in the routing table of router B. Which configuration on router A resolves the problem? [file:1]

A.  
interface Serial0/0/0  
 ip address 10.0.0.1 255.255.255.0  
 negotiation auto  
 ipv6 enable  
 ospfv3 1 ipv4 area 1  
router ospfv3 1  
 address-family ipv4 unicast  
 area 1 range 192.168.1.0 255.255.255.0  
 router-id 1.1.1.1  
 exit-address-family  

B.  
interface GigabitEthernet0/0/0  
 ip address 192.168.1.254 255.255.255.0  
 negotiation auto  
 ipv6 enable  
interface Serial0/0/0  
 ip address 10.0.0.1 255.255.255.0  
 negotiation auto  
 ipv6 enable  
 ospfv3 1 ipv4 area 1  
router ospfv3 1  
 address-family ipv4 unicast  
 router-id 1.1.1.1  
 exit-address-family  

C.  
interface GigabitEthernet0/0/0  
 ip address 192.168.1.254 255.255.255.0  
 negotiation auto  
 ipv6 enable  
 ospfv3 1 ipv4 area 1  
interface Serial0/0/0  
 ip address 10.0.0.1 255.255.255.0  
 negotiation auto  
 ipv6 enable  
router ospfv3 1  
 address-family ipv4 unicast  
 router-id 1.1.1.1  
 exit-address-family  

D.  
interface GigabitEthernet0/0/0  
 ip address 192.168.1.254 255.255.255.0  
 negotiation auto  
 ipv6 enable  
 ospfv3 1 ipv4 area 1  
interface Serial0/0/0  
 ip address 10.0.0.1 255.255.255.0  
 negotiation auto  
 ipv6 enable  
 ospfv3 1 ipv4 area 1  
router ospfv3 1  
 address-family ipv4 unicast  
 router-id 1.1.1.1  
 exit-address-family  

Correct Answer: D [file:1]

- 問題文要約（日本語）:  
Router A の LAN ネットワーク 192.168.1.0/24 を OSPF で Router B に広告したいが、Router B のルーティングテーブルにこのネットワークが載っていない。**Router A 側でどのコンフィグを入れれば、192.168.1.0/24 が OSPF で正しく広告されるか** を問う問題。 [file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 問題の解説によると、現状 Router A は **s0/0/1 側でしか OSPF ネイバーを形成しておらず（＝Router C とのみ隣接）**、Router B 側の s0/0/0 とは OSPF ネイバーになっていない。これでは B にルートが届かない。 [file:1]
  - Router B に 192.168.1.0/24 を届けるには、Router A で次の 2 つが必要になる。  
    - Router B につながる **Serial0/0/0 で OSPFv3（IPv4 アドレスファミリ）を有効化** して、A–B 間の OSPF ネイバーを張る。  
    - LAN 192.168.1.0/24 がぶら下がる **GigabitEthernet0/0/0 でも OSPFv3（IPv4）を有効化** して、そのネットワークを OSPF に広告する。 [file:1]

  - 選択肢 D は、**g0/0/0 と s0/0/0 の両方のインタフェースに `ospfv3 1 ipv4 area 1` を設定**しており、上記 2 条件をいずれも満たしている。これにより、Router A は  
    - B と OSPF ネイバーを形成し、  
    - 192.168.1.0/24 を area 1 内のネットワークとして広告できる。 [file:1]

  - A は `area 1 range 192.168.1.0 255.255.255.0` を使っているが、そもそも **192.168.1.0/24 を持つインタフェース（g0/0/0）に OSPFv3 が有効化されていない** ため、そのプレフィックスが OSPF ルーティングプロセスに乗らない。 [file:1]
  - B は、g0/0/0 はただの L3 設定だけで OSPF 無し、s0/0/0 のみ `ospfv3 1 ipv4 area 1` が入っているため、**A–B 間でネイバーは張れる可能性はあるが、192.168.1.0/24 自体が OSPF に広告されない**。 [file:1]
  - C は g0/0/0 に `ospfv3 1 ipv4 area 1` を入れているが、**s0/0/0 側に OSPFv3 設定がないため、Router B とそもそも OSPF ネイバーを形成できない**。 [file:1]
  - よって、**「B とネイバーを張るインタフェース」と「広告したい LAN がぶら下がるインタフェース」の両方に `ospfv3 1 ipv4 area 1` を入れている D が唯一正解**となる。 [file:1]

- 暗記しやすいポイント

  - **OSPFv3 for IPv4 の超重要原則**:  
    - 「**広告したいネットワークのインタフェース**」と「**ネイバーを張りたいインタフェース**」の **両方に** `ospfv3 <process> ipv4 area <x>` を入れて初めて、相手にそのネットワークが届く。 [file:1]
  - この問題は「**どのインタフェースに OSPF（ospfv3 ipv4）を入れ忘れているかを冷静に探す問題**」として覚えるとよい。  
    - g0/0/0: 192.168.1.0/24 を広告したい → 必ず `ospfv3 1 ipv4 area 1` が必要。  
    - s0/0/0: Router B とネイバーになりたい → ここにも `ospfv3 1 ipv4 area 1` が必要。  
  - 暗記フレーズ例:  
    - 「**OSPFv3(IPv4) は “広告したい口” と “隣接する口” の両方に塗る**」  
    - “LAN のインタフェースだけ OSPF” も “WAN だけ OSPF” も NG、**両方に OSPF を塗った選択肢を選ぶ**、とパターンで覚えると他の類題にも強くなる。 [file:1]

***
QUESTION 506 解説

- 問題文（英語）:  
A network engineer configured routers R1 and R2 with MP-BGP. The engineer noticed that the routers cannot exchange any IPv6 routes; however, the IPv4 neighbor relationship is working fine. Which configuration must the engineer apply to router R2 to exchange IPv6 routes?

A.  
ipv6 unicast-routing  
ipv6 cef  
!  
interface Loopback100  
 ipv6 address 2001:DB8:128::2/128  
!  
interface GigabitEthernet1/0  
 ipv6 address 2001:DB8:1::2/64  
!  
router bgp 65002  
 no bgp default ipv4-unicast  
 neighbor 2001:DB8:1::1 remote-as 65001  
!  
 address-family ipv6  
  network 2001:DB8:128::2/128  
  neighbor 2001:DB8:1::1 activate  

B.  
ipv6 unicast-routing  
ipv6 cef  
!  
interface Loopback100  
 ipv6 address 2001:DB8:128::2/128  
!  
interface GigabitEthernet1/0  
 ipv6 address 2001:DB8:1::2/64  
!  
router bgp 65002  
 no bgp default ipv4-unicast  
 neighbor 2001:DB8:1::1 remote-as 65001  
!  
 address-family ipv6  
  network 2001:DB8:128::2/128  

C.  
ipv6 cef  
!  
interface Loopback100  
 ipv6 address 2001:DB8:128::2/128  
!  
interface GigabitEthernet1/0  
 ipv6 address 2001:DB8:1::2/64  
!  
router bgp 65002  
 no bgp default ipv4-unicast  
 neighbor 2001:DB8:1::1 remote-as 65001  
!  
 address-family ipv6  
  network 2001:DB8:128::2/128  
  neighbor 2001:DB8:1::1 activate  

D.  
ipv6 unicast-routing  
ipv6 cef  
!  
interface Loopback100  
 ipv6 address 2001:DB8:128::2/128  
!  
interface GigabitEthernet1/0  
 ipv6 address 2001:DB8:1::2/64  
!  
router bgp 65002  
 no bgp default ipv4-unicast  
 neighbor 2001:DB8:1::1 remote-as 65001  
 neighbor 2001:DB8:1::1 description AS65001 ID C194:BE98:F123::5  
!  
 address-family ipv4  
  neighbor 2001:DB8:1::1 activate  

Correct Answer: A  

- 問題文要約（日本語）:  
R1 と R2 間で MP-BGP を設定したが、IPv4 のネイバー関係は正常なのに IPv6 ルートだけ交換できていない。  
IPv6 ルートを交換できるようにするために、R2 にどの設定を入れるべきか。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  MP-BGP で IPv6 プレフィックスを交換するには、R2 側で次の 4 点がそろっている必要がある。 [web:1067][web:1069][web:1073][web:1076]  

  1. **IPv6 ルーティングの有効化**  
     - グローバルで `ipv6 unicast-routing` を有効化しないと、ルータは IPv6 ルートをフォワーディングしない。 [web:1073][web:1076]  
  2. **インターフェースに IPv6 アドレスが設定されていること**  
     - ループバック（広告したいプレフィックス 2001:DB8:128::2/128）と、  
       BGP ピア間リンク（2001:DB8:1::2/64）に IPv6 アドレスを設定。  
  3. **IPv6 アドレスファミリでの隣接のアクティベート**  
     - MP-BGP では、`neighbor 2001:DB8:1::1 remote-as 65001` だけでは IPv6 AF が有効にならない。  
     - `address-family ipv6` 配下で `neighbor 2001:DB8:1::1 activate` が必須。 [web:1067][web:1069][web:1071][web:1073]  
  4. **広告したい IPv6 プレフィックスを BGP に乗せる network コマンド**  
     - `address-family ipv6` 配下で `network 2001:DB8:128::2/128` を指定し、  
       Loopback100 の /128 ルートを BGP に再配布。 [web:1069][web:1070][web:1073]  

  - 選択肢 A は、上記 1〜4 をすべて満たしている。 [web:1068][web:1072][web:1077]  
  - 選択肢 B: `neighbor 2001:DB8:1::1 activate` がなく、IPv6 AF でネイバーがアクティブにならない。 [web:1067][web:1071]  
  - 選択肢 C: `ipv6 unicast-routing` がなく、IPv6 ルーティングが有効化されていないため、IPv6 経路を正しく扱えない。 [web:1073][web:1076][web:1078]  
  - 選択肢 D: IPv4 アドレスファミリでのみ `neighbor ... activate` しており、IPv6 AF では有効化していないため、IPv6 ルートは交換されない。  

- 試験向けの覚え方フレーズ

  - **「MP-BGP IPv6＝`ipv6 unicast-routing`＋`AF ipv6` で `neighbor ... activate`＋`network <IPv6>` の 3 点セット（＋インターフェース IPv6）」**  
  - 一行パターン:  
    - **「‘IPv4 BGP は動くが IPv6 だけ来ない’ → まず AF ipv6 の neighbor activate と network をチェック」**  
  - セット暗記:  
    - **「remote-as は ‘隣接の定義’、activate は ‘その AF で有効化’、これを IPv6 address-family で忘れない」**  

***
QUESTION 507 解説

- 問題文（英語）:  
Which action restores OSPF adjacency between R1 and R2? [file:1]

A. Change the IP MTU of R2 Fa0/0 to 1300.  
B. Change the IP MTU of R1 Fa1/0 to 1500.  
C. Change the IP MTU of R1 Fa1/0 to 1300.  
D. Change the IP MTU of R2 Fa0/0 to 1500.  

Correct Answer: D [file:1]

- 問題文要約（日本語）:  
R1 と R2 間の OSPF ネイバーが **EXSTART 状態で止まっており、隣接が確立されていない**。  
図では R2 Fa0/0 の MTU が 1400、R1 Fa1/0 の MTU がデフォルト 1500 に設定されている。**どの操作を行えば OSPF の隣接関係が復旧するか** を問う問題。 [file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 解説にある通り、**OSPF ネイバーが EXSTART でスタックするときの典型的な原因は MTU 不一致**。DBD 交換の際に、片側が「相手の MTU が自分と違う」と認識すると、隣接が完全なフル状態まで上がらない。 [file:1]
  - 図では  
    - R1 Fa1/0: MTU 1500（デフォルト）  
    - R2 Fa0/0: MTU 1400  
    と **MTU が不一致** になっているため、OSPF が EXSTART から先に進めない。 [file:1]
  - この問題を解決する方法は 2 パターンある。 [file:1]  
    - R2 Fa0/0 の MTU を 1500 に変更して R1 に合わせる。  
    - R1 Fa1/0 の MTU を 1400 に変更して R2 に合わせる。  
    どちらも「**両端で MTU を一致させる**」という点が本質。
  - 選択肢 D は「Change the IP MTU of R2 Fa0/0 to 1500」であり、**R2 側をデフォルトの 1500 に合わせることで R1 と同一 MTU にする** 正しい解決策。 [file:1]
  - A の 1300、C の 1300 はどちらも相手と一致しないため、**MTU 不一致が残り OSPF 問題は解決しない**。  
  - B は既に R1 Fa1/0 が 1500 であるため、設定を 1500 に変えても何も変わらず、**問題解消につながらない**。 [file:1]

- 試験向けの覚え方フレーズ

  - 「**OSPF EXSTART = まず MTU を疑え**」  
  - 「**EXSTART でスタック → 両端の MTU を揃える**」  
  - 選択肢の中で「**相手と同じ MTU に揃えているものが正解**」とパターン認識する。

***
QUESTION 508 解説

- 問題文（英語）:  
Router R1 peers with two ISPs using static routes to get to the Internet. The requirement is that R1 must prefer ISP-A under normal circumstances and failover to ISP-B if the connectivity to ISP-A is lost. The engineer observes that R1 is load balancing traffic across the two ISPs. Which action resolves the issue by sending traffic to ISP-A only with failover to ISP-B? [file:1]

A. Configure two static routes on R1, one pointing to ISP-B with more specific routes and another pointing to ISP-A with summary routes.  
B. Change the bandwidth of the interface on R1 so that interface to ISP-A has a higher value than the interface to ISP-B.  
C. Configure two static routes on R1, one pointing to ISP-A and another pointing to ISP-B with 222 admin distance.  
D. Configure OSPF between R1, ISP-A, and ISP-B for dynamic failover if any ISP link to R1 fails.  

Correct Answer: C [file:1]

- 問題文要約（日本語）:  
R1 は 2 つの ISP（ISP-A, ISP-B）にスタティックルートで接続しており、要件は「**通常は ISP-A を優先して使い、ISP-A が死んだら ISP-B にフェイルオーバー**」すること。  
しかし現状は、R1 が 2 本の ISP リンクで **ロードバランシング** してしまっている。どの設定を行えば、**通常は ISP-A のみを使い、ISP-A ダウン時のみ ISP-B に切り替わる** 動作になるかを問う問題。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 今は 2 本のスタティックが「同じプレフィックス・同じ AD（管理距離）」で入っているため、R1 は両方を等コストパスとして認識し、**デフォルトでロードバランシング** を行っていると考えられる。  
    - 静的ルートは、**同一プレフィックス・同一 AD** で複数存在すると、等コストロードシェアリングの対象になる。 [file:1]
  - 要件は「**ISP-A をプライマリ、ISP-B をバックアップ**」なので、**同じ宛先プレフィックスに対して AD の異なる 2 本のスタティックルートを設定し、プライマリが生きている限りバックアップは使われないようにする** のが王道。  
    - 具体的には:  
      - ISP-A 向き: 通常の AD（1）のスタティックルート。  
      - ISP-B 向き: より大きな AD（ここでは 222）のスタティックルート。  
    - こうすると、R1 は AD=1 の ISP-A ルートだけを使い、ISP-A のネクストホップがダウンしてルートが消えたときに初めて AD=222 の ISP-B ルートを採用する。 [file:1]
  - 選択肢 C はまさにこれを実現している:  
    - 「one pointing to ISP-A（AD デフォルト 1）」＋「another pointing to ISP-B with 222 admin distance」  
    - よって **ISP-A がプライマリ、ISP-B がフェイルオーバ用のフローティングスタティック** となり、要件通りの動作になる。 [file:1]

  - A の「ISP-B に more specific、ISP-A に summary」は、**ルーティングの優先順位ルール（longest match が勝つ）により ISP-B 側が常に優先される** 可能性が高く、要件の「ISP-A 優先」に反する。 [file:1]
  - B の「インタフェース帯域変更」は、**スタティックルートには関係ない**（帯域は IGP のメトリック用）ため、ロードバランシング解消やプライマリ／バックアップ制御には使えない。 [file:1]
  - D の「OSPF を導入」も、問題文は明確に「スタティックルートで接続」としており、**OSPF 構成に切り替えること自体が前提と合わない** 上に、OSPF を入れただけでは「ISP-A 優先＋ISP-B フェイルオーバ」が自動で保証されるわけではない。 [file:1]

***
QUESTION 509 解説

- 問題文（英語）:  
Which action resolves the IP SLA for the UDP jitter problem between R4 and R3 Ethernet 0/1 IP addresses? [file:1]

A. Delete and configure the ip sla responder command with R4 E0/1 IP address.  
B. Delete and configure the ip sla 6500 command with R3 e0/1 IP address.  
C. Configure the ip sla 6500 command with R3 e0/1 IP address.  
D. Configure the ip sla responder command with R4 E0/1 IP address.  

Correct Answer: C [file:1]

- 問題文要約（日本語）:  
R4 から R3 の Ethernet0/1 への **UDP jitter 用 IP SLA** が正しく動作しておらず問題になっている。  
R4 側でどの操作を行えば、**R3 E0/1 の正しい IP アドレスに対して IP SLA（udp-jitter）が動作するようになるか** を問う問題。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 解説によると、R4 に設定されている `ip sla 6500`（問題文では 6500 だが、説明内では 65000 になっているのはタイポとみなせる）の UDP jitter 設定で、**宛先 IP アドレスが誤っている**。 [file:1]  
    - 本来の宛先: R3 E0/1 の IP アドレス `209.165.201.2`  
    - 誤って設定されている: `udp-jitter 209.165.201.4 ...` のように、`209.165.201.4` になっている。 [file:1]
  - この誤りを修正するには、**既存の IP SLA 6500 の設定モードに入り、宛先 IP（dest-ipaddr）だけを正しいアドレスに変更すればよい**。 [file:1]  
    - 例:  
      - `R4(config)# ip sla 65000`  
      - `R4(config-ip-sla-jitter)# dest-ipaddr 209.165.201.2`  
    - これが選択肢 C の「Configure the ip sla 6500 command with R3 e0/1 IP address」の内容に相当する。 [file:1]
  - A と D の `ip sla responder` は、**R3 側で UDP jitter を応答するプロセス（responder）を動かすための設定** に関するキーワードだが、問題の本質は「R4 の IP SLA 設定の宛先 IP が間違っていること」であり、R3 responder アドレスではなく **R3 E0/1 宛の “送信側設定（ip sla 6500）” を直す必要がある**。 [file:1]
  - B の「Delete and configure the ip sla 6500 command with R3 e0/1 IP address」は、  
    - いったん IP SLA 6500 を消してから、新規に正しい IP で作り直す方法だが、**わざわざ消さなくても `dest-ipaddr` を修正するだけで足りる** と解説に明記されている。  
    - 「Although you can also delete the configuration… modifying just the destination IP address is simpler.」とあり、**より適切でシンプルな解決策として C が選ばれている**。 [file:1]

***
QUESTION 510 解説

- 問題文（英語）:  
Which action allows the engineer to successfully copy running-config to the TFTP server? [file:1]

A. Add a route in the switch to the TFTP server.  
B. Use file name Switch-confg.txt.  
C. Add the TFTP server configuration in the switch.  
D. Use TFTP server IP address 10.0.1.1.  

Correct Answer: A [file:1]

- 問題文要約（日本語）:  
スイッチから TFTP サーバに `running-config` をコピーしようとしているが失敗している状況。  
TFTP サーバとスイッチの IP アドレスは **異なるネットワーク** にあり、さらにコマンドで指定している TFTP サーバの IP アドレスも実際と異なっている。**どのアクションを行えば、正常に TFTP サーバへコピーできるか** を問う問題。 [file:1]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 解説によると、図には次の 2 つの問題がある。 [file:1]  
    1. 実際の TFTP サーバの IP は `10.0.0.2` なのに、スイッチの `copy running-config tftp` では `10.0.0.1` を指定している。  
    2. TFTP サーバは `10.0.0.0/24`、スイッチは `10.0.1.0/24` と **別サブネット** にいるのに、間にルータが描かれていない。
  - 解説では最終的に、**TFTP サーバとスイッチの IP 設定／図にいくつか不整合があることを前提にしつつも、「スイッチから TFTP サーバ宛へのルーティングが必要」という点を答えとして採用**している。  
    - つまり、「TFTP サーバが別ネットワークにいるなら、スイッチに TFTP 宛のルートを追加しないとパケットが届かない」ので、**`Add a route in the switch to the TFTP server` が正解**となる。 [file:1]
  - B のファイル名 `Switch-confg.txt` は任意であり、**TFTP 転送が失敗する原因にはならない**。どんなファイル名でもサーバ側が受け取れる。 [file:1]
  - C の「TFTP server configuration をスイッチに事前定義する」必要はなく、**`copy running-config tftp:` でその都度 IP を指定すれば良い**ため、根本原因とは無関係。 [file:1]
  - D の `10.0.1.1` は図では **スイッチ自身の IP アドレス** であり、TFTP サーバの IP ではないため、これを TFTP 宛として指定するのは誤り。 [file:1]

- 試験向けの覚え方フレーズ

  - 「**TFTP 転送がうまくいかない問題 → まずは IP 到達性（ルート）を疑え**」  
  - 別ネットワークの TFTP サーバに `copy running-config tftp` する問題が出たら、  
    - 「**正しい TFTP サーバ IP を指定しているか**」  
    - 「**その TFTP 宛にルートがあるか（デフォルトルート or スタティックルート）**」  
    を最優先でチェックすると覚えておくと、類似問題も取りやすい。

***
QUESTION 511 解説

- 問題文（英語）:  
The router is redistributing a prefix 172.16.10.0/24 that should have been filtered. Which action resolves the issue? [file:1]

A. Match the tag 666 for the route in the route map.  
B. Add the route in access-list 10.  
C. Remove route-map sequence 20.  
D. Permit the route in route-map sequence 20.  

Correct Answer: C [file:1]

- 問題文要約（日本語）:  
本来は再配布したくないプレフィックス 172.16.10.0/24 が、ルータから他プロトコルへ **誤って再配布されてしまっている**。  
すでにルータには再配布用の route-map が設定されているが、その構成のせいで 172.16.10.0/24 が通ってしまっている。**どの操作を行えば、このプレフィックスの再配布を止められるか** を問う問題。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 解説によると、route-map の構造は次のようになっている。 [file:1]  
    - `route-map XXX permit 10`  
      - `match ip address 10`  
      - → ACL 10 にマッチするネットワーク（例: 172.16.1.0/24）を **許可**。  
    - `route-map XXX permit 20`  
      - （match なし）  
      - → **ここで「それまでにマッチしていないすべての残りのプレフィックスを許可」**。
  - 172.16.10.0/24 は ACL 10 ではマッチしていないが、**seq 20 の「permit 20」によって “残り全部 OK” として再配布されてしまう**。 [file:1]
  - 本来「172.16.10.0/24 を含む、ACL 10 以外のプレフィックスは再配布したくない」のなら、  
    - **seq 10 で明示的に許可されたプレフィックスだけを再配布し、その他は暗黙の deny に任せる** のが正しい作り方になる。  
    - つまり、**「全部通してしまう seq 20 を削除する」＝選択肢 C が正解**。 [file:1]
  - A の「tag 666」については、問題文や解説にタグ 666 の設定・利用は出てこず、**ルートタグでフィルタしている構成ではない**。したがって不適切。  
  - B の「access-list 10 にそのルートを追加」は、ACL 10 が「permit 用」なのか「deny 用」なのかにもよるが、解説文から見る限り ACL 10 は 172.16.1.0 を permit する用途で、**172.16.10.0 をそこに追加すると逆に“通したくないルートを明示的に許可してしまう”可能性が高い**。 [file:1]
  - D の「seq 20 で permit」は、すでに seq 20 が「残り全部 permit」になっていることが問題の根本なので、**それを明示的に permit し直しても状況は悪化するだけ**。 [file:1]

- 試験向けの覚え方フレーズ

  - 「**route-map で “permit ANY” な最後のシーケンスは、フィルタ問題の犯人になりがち**」  
  - “特定プレフィックスだけ再配布したい” 問題で、  
    - 上のシーケンスで特定 prefix を permit  
    - 最後に `permit 20` だけ書かれたシーケンス  
    が出てきたら、「**その `permit 20` が全部通している → 削除して暗黙の deny を使う**」とパターンで覚えておくと良い。

***
QUESTION 512 解説

- 問題文（英語）:  
Router R2 VLAN 10 users cannot get dynamic IP addresses from R1. Which action resolves the issue? [file:1]

A. Expand the address scope of VLAN 10.  
B. Configure the IP helper feature on the Interface GigabitEthernet 0/2 of router R2.  
C. Eliminate the port security feature on the ports of switch SW2.  
D. Identify the host with the duplicate IP address.  

Correct Answer: B [file:1]

- 問題文要約（日本語）:  
VLAN 10 にいるユーザ PC は、本来 **R1 上の DHCP サービスから IP アドレスを取得する設計**になっている。  
しかし実際のデバッグ結果では、ユーザ側に一番近い R2 が DHCP 応答をしようとしているが、**R2 側にはそのサブネット用 DHCP プールが無く、IP を配れずに失敗している** 状況。どの設定を行えば、PC が R1 の DHCP から正しく IP を取得できるかを問う問題。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 問題の構成では、ユーザ PC は R2 のインタフェース（VLAN 10 に対応する `GigabitEthernet 0/2`）に接続されており、**DHCP サーバは別ルータ R1 上** に存在する。 [file:1]
  - デバッグ出力から、R2 は自分自身が DHCP サーバとして応答しようとしているが、**そのサブネット向けの DHCP プールが R2 に存在しないため、アドレスを割り当てられない**。 [file:1]
  - このように「**DHCP クライアントと DHCP サーバが異なる L3 デバイス上**」にあり、間にルータ（ここでは R2）が挟まる場合は、クライアント側のデフォルトゲートウェイ（R2 G0/2）で **`ip helper-address <R1のIP>` を設定し、DHCP ブロードキャストをユニキャストで R1 に中継する必要がある**。 [file:1]
  - 選択肢 B の「Configure the IP helper feature on the Interface GigabitEthernet 0/2 of router R2」は、まさに **R2 G0/2 に DHCP リレー（IP helper）を設定する**ことで、VLAN 10 の DHCP Discover を R1 の DHCP サービスへ届ける解決策となる。 [file:1]

  - A の「アドレススコープ拡張」は、**プール枯渇や範囲不足が原因のときに検討するもの**だが、ここでは R1 の DHCP プール範囲が問題だとは書かれていない。そもそも R2 が誤ってローカルで応答しようとしていることが原因。  
  - C の port security、D の重複 IP は、**DHCP 要求自体が R1 に届いていない**問題には関係しない。R2 がリレーをしていない限り、R1 の DHCP は VLAN 10 にいるクライアントを認識できない。 [file:1]

- 試験向けの覚え方フレーズ

  - 「**DHCP サーバがルータの向こう側 → クライアント側 SVI / インタフェースに ip helper-address**」  
  - VLAN ユーザが「**上流の別ルータの DHCP から IP を取る構成**」でアドレスがもらえない問題が出たら、  
    - “**ユーザがつながる L3 インタフェース（SVI / routed port）に IP helper が入っているか？**”  
    を最優先でチェックする、と覚えておくと類似問題を取りこぼしにくい。

***
QUESTION 513 解説

- 問題文（英語）:  
Which two features are required for MPLS forwarding on which types of routers? (Choose two.) [file:1]

A. CEF on PE and CE routers  
B. LDP on PE and CE routers  
C. MPLS on PE and core routers  
D. MPLS on CE and core routers  
E. LDP on PE and core routers  

Correct Answer: C, E [file:1]

- 問題文要約（日本語）:  
MPLS L3VPN などの MPLS フォワーディングを動かすうえで、**どの種類のルータに、どの機能（CEF / MPLS / LDP）が必須か** を問う問題。  
特に「**CE ルータに MPLS 関連機能が必要かどうか**」がポイント。 [file:1]

- 正解: **C, E**

- 解説（なぜ C と E が正しいか）

  - MPLS でラベルスイッチングを行うのは、**プロバイダ側の PE（Provider Edge）と Core（P）ルータ** であり、ここで **MPLS ラベルの付与・転送・ポップ** が行われる。  
    - したがって、**MPLS フォワーディングを有効にするには、PE と Core で MPLS を有効化する必要がある** → C が正しい。 [file:1]
  - MPLS ネットワーク内でルータ間にラベル情報を配布するには、**LDP（Label Distribution Protocol）**（または RSVP-TE など）が必要であり、  
    - ラベル交換を行うのも **PE と Core** 間であるため、**LDP も PE と Core ルータ間で有効化する必要がある** → E が正しい。 [file:1]
  - 一方で、**CE（Customer Edge）ルータは「お客様側ルータ」** であり、通常は単に IP ルーティングで PE と BGP/OSPF/EIGRP 等を張るだけで、MPLS のラベル処理には関与しない。  
    - 解説にも「For MPLS, no MPLS related feature is required on the customers’ routers (i.e. CE routers).」とあり、**CE には MPLS 機能も LDP も不要** と明言されている。 [file:1]
  - よって、**「PE と Core に MPLS」「PE と Core に LDP」、CE には MPLS 関連不要** という組み合わせである C と E が正解。 [file:1]

  - A は CE に CEF を要求しているが、CEF は単なる高速転送エンジンであり、MPLS 専用機能というわけではないうえ、「MPLS フォワーディングに必須なのは PE/Core 側の MPLS + LDP」という観点から外れている。  
  - B, D は CE ルータに LDP や MPLS を要求しており、「CE には MPLS 関連機能は不要」という原則と矛盾する。 [file:1]

- 試験向けの覚え方フレーズ

  - 「**MPLS はプロバイダの中だけ、CE は IP だけ**」  
  - 「**ラベルを張るのは PE、ラベルを流すのは PE＋Core（MPLS＋LDP）、CE はラベルにノータッチ**」  
  - 選択肢で **“CE に MPLS / LDP 必須” と書いてあったら即座に不正解候補** と覚えておくと取りやすい。

***
QUESTION 514 解説

- 問題文（英語）:  
A newly installed router starts establishing an LDP session from another MPLS router to which it is not directly connected. Which LDP message type responds by target router to the initiating router using UDP protocol? [file:1]

A. advertisement message  
B. notification message  
C. extended discovery message  
D. session message  

Correct Answer: C [file:1]

- 問題文要約（日本語）:  
新しく設置したルータが、**直接接続されていない（1ホップ以上離れた）MPLS ルータ** から LDP セッションを張ろうとしている状況。  
このとき、ターゲット側ルータが **UDP を使ってイニシエータに返す LDP メッセージ種別はどれか** を問う問題。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 直接接続されていない MPLS ルータ同士で LDP セッションを張るときは、**targeted LDP** を用いる。 [file:1]
  - この場合、  
    - イニシエータ側ルータは、ターゲットルータに向けて **“targeted Hello” を UDP ユニキャスト** で送信する。  
    - それに対してターゲット側ルータが応答し、**“extended discovery” と呼ばれるフェーズ** で隣接関係（LDP neighborship）を発見する。  
    - 解説でも「The other router will then respond to the targeted Hello message to establish an LDP session. This is called extended discovery.」と説明されている。 [file:1]
  - この extended discovery に関わるメッセージ種別が、選択肢 C の **「extended discovery message」** であり、**UDP を使って応答する LDP メッセージ種別として正しい**。 [file:1]

  - A の advertisement message や B の notification message、D の session message は、いずれも **LDP セッション確立・ラベル配布の別フェーズで用いられるメッセージ**であり、「非直結の相手に対して UDP で応答する discovery メカニズム」という条件に当てはまらない。

***
QUESTION 515 解説

- 問題文（英語）:  
Which configuration resolves the issue? [file:1]

A.  
int GigabitEthernet 2  
 ip ospf key 1 cisco  
 ip ospf authentication  

B.  
router ospf 1  
 area 1 authentication message-digest  
int GigabitEthernet 2  
 ip ospf message-digest-key 1 md5 cisco  

C.  
key chain ospf  
 key 1  
  key-string 7 01041C321954  
  cryptographic-algorithm hmac-sha-1  
interface GigabitEthernet2  
 ip ospf authentication key-chain ospf  

D.  
int GigabitEthernet 2  
 ip ospf message-digest-key 1 md5 cisco  
 ip ospf authentication message-digest  

Correct Answer: C [file:1]

- 問題文要約（日本語）:  
元々は OSPF ネイバーが **FULL 状態だったのに、認証文字列を片側だけ変更したせいで DOWN になっている** 状況。  
図の出力から、OSPF 認証は **key chain “ospf” を使った HMAC-SHA-256 ベースの認証** で行われていることが分かる。**どの設定でネイバーを復旧できるか** を問う問題。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 解説文では、ネイバーが FULL から DOWN になる代表的な原因として「**片側だけ認証文字列（またはアルゴリズム）を変えてしまった**」ケースを挙げている。  
    - この場合の正しい対応は、「**残りの片側も同じ認証方式・文字列に揃える**」こと。 [file:1]
  - 図の出力から、OSPF 認証は以下のように設定されていることがわかる。 [file:1]  
    - 認証方式: HMAC-SHA 系（解説では HMAC-SHA-256 と言及）  
    - 仕組み: **key chain “ospf”** を使った OSPF 認証
  - 選択肢 C は、まさにこれに合わせて **key chain “ospf” と対応インタフェースでの `ip ospf authentication key-chain ospf` を設定している**。  
    - `key chain ospf` 配下で `key-string` と `cryptographic-algorithm hmac-sha-1` を定義し、  
    - `interface GigabitEthernet2` で `ip ospf authentication key-chain ospf` を設定して、その key chain を OSPF 認証に利用する。 [file:1]
  - 対して A, B, D はすべて **MD5 メッセージダイジェスト認証（`ip ospf message-digest-key ... md5` または `area authentication message-digest`）ベース** であり、  
    - 図の出力が示す「HMAC-SHA 系＋key chain “ospf”」とは認証方式が異なるため、**両端で認証方式と鍵が一致せず、OSPF ネイバーは復旧しない**。 [file:1]

- 試験向けの覚え方フレーズ

  - 「**OSPF 認証トラブル → “相手と同じ方式＋同じ鍵” に揃えるだけ**」  
  - 出力に **“key chain ospf” や “HMAC-SHA”** が見えたら、  
    - 「**MD5 系ではなく key-chain ベースの OSPF 認証**」  
    - → インタフェース側は `ip ospf authentication key-chain <name>` を使う、というパターンで押さえておくと良い。

***
QUESTION 516 解説

- 問題文（英語）:  
User IP phone receives the IP address but does not register to the call manager. Which command in ip dhcp pool VLAN200_USER_VOICE resolves the issue? [file:1]

A. option 160 ip 10.221.10.10  
B. option 15 ip 10.221.10.10  
C. option 150 ip 10.221.10.10  
D. option 117 ip 10.221.10.10  

Correct Answer: C [file:1]

- 問題文要約（日本語）:  
IP フォンは DHCP で **IP アドレス自体は取得できているが、Call Manager（CUCM）に登録できていない** 状況。  
CUCM の IP アドレスは 10.221.10.10 で、`ip dhcp pool VLAN200_USER_VOICE` のどのオプションを設定すれば、IP フォンが正しく Call Manager に登録できるかを問う問題。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 解説では、IP フォンが CUCM に登録するためには、通常の IP 設定（IP アドレス、デフォルトゲートウェイ、DNS など）に加えて、**CUCM の TFTP サーバ IP アドレス** が必要だと説明している。 [file:1]
  - IP フォンは起動時に  
    - DHCP で IP アドレスとオプションを取得し、  
    - **TFTP サーバから自分の設定ファイルをダウンロードして CUCM に登録する**。  
  - Cisco IP Phone では、この「CUCM TFTP サーバの IP アドレス」を DHCP で配布する際、**DHCP Option 150** を使用する。 [file:1]
    - したがって、`ip dhcp pool VLAN200_USER_VOICE` の中で  
      - `option 150 ip 10.221.10.10`  
      を設定すると、IP フォンは 10.221.10.10 を TFTP サーバとして認識し、そこから設定を取得して CUCM への登録を完了できる。 [file:1]
  - 選択肢 A の option 160 は、一部の環境で追加・独自用途に使われることはあるが、**Cisco IP Phone における標準的な CUCM TFTP 指定は Option 150**。  
  - B の option 15 は **DNS ドメイン名** 用、D の option 117 は **BCMCS domain list** など別用途であり、IP フォンの TFTP サーバ指定には使用しない。 [file:1]

- 試験向けの覚え方フレーズ

  - 「**IP Phone と CUCM → DHCP Option 150 = TFTP（CUCM）アドレス**」  
  - 問題文に「**IP phone, Call Manager, TFTP, DHCP pool**」が同時に出てきたら、  
    - まず **`option 150 ip <CUCM/TFTP IP>` を思い出す**、とパターンで覚えておくと取りこぼしづらい。

***
QUESTION 517 解説

- 問題文（英語）:  
What is an advantage of MPLS Layer 3 VPN deployment? [file:1]

A. Planning and modifications are required for the customer Intranet before migrating to Layer 3 VPN.  
B. Scalable VPNs are created using connection-oriented, point-to-point, or multipoint overlay connections.  
C. QoS provides performance with policy and support for a best-effort service level in an MPLS VPN.  
D. Security is provided at the edge of the provider network through encryption.  

Correct Answer: C [file:1]

- 問題文要約（日本語）:  
MPLS L3VPN を導入することによる **利点** として正しいものはどれかを問う問題。  
特に、MPLS 上での QoS や暗号化の扱いがポイント。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 解説では、「**QoS をサポートできるのが MPLS L3VPN の利点**」と述べており、その理由として  
    - IP パケットの **IP Precedence / DSCP ビットを MPLS ラベルヘッダの EXP ビットにコピー** できるため、  
    - コア内でラベルベースの **優先制御や帯域制御（DiffServ, MPLS TE など）** を実現できる、と説明している。 [file:1]
  - これにより、MPLS VPN 環境で  
    - ボイスや重要トラフィックに高優先度  
    - その他はベストエフォート  
    といった **ポリシーベース QoS** を実現できることが、MPLS L3VPN の大きなメリットとなる。 [file:1]

  - A は「顧客イントラネットの事前計画・変更が必要」と書いているが、これは「利点」ではなく、どちらかといえば導入ハードルに近い。  
  - B は「接続指向のポイント・ツー・ポイント/マルチポイントのオーバレイ」としており、これはむしろ **従来の ATM/FR/VPN オーバレイ型の特徴** で、MPLS L3VPN 自体は IP ベースでスケールしやすい「接続レス VPN」。 [file:1]
  - D は「暗号化によるセキュリティ」を挙げているが、解説でも「**MPLS VPN は暗号化を前提とせず、ISP コア内の論理分離でプライバシーを確保する**」と明記されており、暗号化は標準の利点ではない。 [file:1]

- 試験向けの覚え方フレーズ

  - 「**MPLS L3VPN の売りは “QoS がガチで効く VPN”**」  
  - DSCP/Precedence → **EXP ビットにコピーしてコアで優先制御**、暗号化は別の話と覚えておくと、C と D を迷わず切り分けられる。

***
QUESTION 518 解説

- 問題文（英語）:  
Which feature is used by LDP in the forwarding path within the MPLS cloud? [file:1]

A. IP forwarding  
B. LSP  
C. TDP  
D. TTL  

Correct Answer: B [file:1]

- 問題文要約（日本語）:  
MPLS クラウド内部で、**LDP がフォワーディングパスとして利用する仕組みはどれか** を問う問題。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 解説では、「**LDP は MPLS ネットワーク内で、トラフィックを転送するための Label-Switched Path（LSP） データベースを構築・維持する**」と説明されている。  
    - LDP は IGP によって決まった経路に沿ってラベルを配布し、**そのラベルに基づいてパケットを転送する “LSP” を作るプロトコル**。 [file:1]
  - 従って、「LDP がフォワーディングパスとして使うもの」は **LSP（Label Switched Path）** であり、選択肢 B がそれを表している。 [file:1]

  - A の IP forwarding は、IP-only ルーティングの基本的な仕組みであり、**MPLS のラベル転送そのものを指すものではない**。  
  - C の TDP は古い Cisco 固有の Tag Distribution Protocol で、**“LDP が使う機能” ではなく、LDP の前身/別実装のプロトコル名**。  
  - D の TTL は MPLS ラベルヘッダに含まれるフィールドの一つだが、「LDP がフォワーディングパスとして利用する機能」とは言えない。

***
QUESTION 519 解説

- 問題文（英語）:  
The security department recently installed a monitoring device between routers R3 and R5, which caused a loss of network connectivity for users connected to R5. Troubleshooting revealed that the monitoring device cannot forward multicast packets. The team already updated R5 with the correct configuration. Which configuration must be implemented on R3 to resolve the problem by ensuring R3 as the DR for the R3-R5 segment? [file:1]

A.  
interface FastEthernet0/0  
 ip address 10.99.53.1 255.255.255.252  
 ip access-group 122 in  
 ip ospf network non-broadcast  
 ip ospf priority 0  
!  
router ospf 10  
 router-id 10.10.3.255  
 network 10.99.53.0 0.0.0.3 area 0  
 neighbor 10.99.53.2  
!  
access-list 122 permit 89 host 10.99.53.2 host 10.99.53.1  
access-list 122 deny 89 any any  
access-list 122 permit tcp any any  
access-list 122 permit udp any any  
access-list 122 permit icmp any any  

B.  
interface FastEthernet0/0  
 ip address 10.99.53.1 255.255.255.252  
 ip access-group 122 in  
 ip ospf network non-broadcast  
 ip ospf priority 100  
!  
router ospf 10  
 router-id 10.10.3.255  
 network 10.99.53.0 0.0.0.3 area 0  
 neighbor 10.99.53.2  
!  
access-list 122 permit 89 host 10.99.53.2 host 10.99.53.1  
access-list 122 deny 89 any any  
access-list 122 permit tcp any any  
access-list 122 permit udp any any  
access-list 122 permit icmp any any  

C.  
interface FastEthernet0/0  
 ip address 10.99.53.1 255.255.255.252  
 ip access-group 122 in  
 ip ospf network point-to-point  
 ip ospf priority 100  
!  
router ospf 10  
 router-id 10.10.3.255  
 network 10.99.53.0 0.0.0.3 area 0  
 neighbor 10.99.53.2  
!  
access-list 122 permit 88 host 10.99.53.2 host 10.99.53.1  
access-list 122 deny 88 any any  
access-list 122 permit tcp any any  
access-list 122 permit udp any any  
access-list 122 permit icmp any any  

D.  
interface FastEthernet0/0  
 ip address 10.99.53.1 255.255.255.252  
 ip access-group 122 in  
 ip ospf network non-broadcast  
 ip ospf priority 0  
!  
router ospf 10  
 router-id 10.10.3.255  
 neighbor 10.99.53.2  
!  
access-list 122 permit 88 host 10.99.53.2 host 10.99.53.1  
access-list 122 deny 88 any any  
access-list 122 permit tcp any any  
access-list 122 permit udp any any  
access-list 122 permit icmp any any  

Correct Answer: B [file:1]

- 問題文要約（日本語）:  
R3 と R5 の間にセキュリティ部門が監視装置を挟んだ結果、**マルチキャストを転送できない装置のせいで R5 側ユーザの接続が切れた**。  
R5 側の設定は修正済みで、残るは R3 側の設定で、**マルチキャスト無しでも OSPF ネイバーを張れ、かつ R3 が R3–R5 セグメントの DR になるようにする** 設定を選ぶ問題。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 監視装置がマルチキャストを通さないため、通常の **OSPF ブロードキャスト/マルチキャスト・Hello（224.0.0.5/6）ではネイバーが張れない**。  
  - このような場合、**OSPF ネットワークタイプを “non-broadcast” にし、ユニキャスト Neighbor 設定で OSPF を張る**のが定石。 [file:1]
  - さらに問題文の要件は「**R3 がこのセグメントの DR になること**」。  
    - **non-broadcast ネットワークタイプ** では DR/BDR 選出は行われ続けるため、R3 側インタフェースで **`ip ospf priority` を 1 より大きい値（例: 100）** に設定することで、DR として選出されやすくなる。 [file:1]
  - 解説にも、R3 側で必要な条件として次の 2 点が挙げられている。 [file:1]  
    1. `ip ospf network non-broadcast` を FastEthernet0/0 に設定し、`router ospf 10` 配下で `neighbor 10.99.53.2` を指定して R5 とユニキャストでネイバーを張る。  
    2. `ip ospf priority` を 1 より大きい値（例: 100）にして、R3 を DR にする。
  - 選択肢 B はこれらをすべて満たしている。  
    - インタフェース f0/0: `ip ospf network non-broadcast` ＋ `ip ospf priority 100`  
    - ルータ設定: `network 10.99.53.0 0.0.0.3 area 0` ＋ `neighbor 10.99.53.2`  
    - ACL 122 で OSPF プロトコル番号 89（OSPF）のユニキャストトラフィックを permit し、それ以外の 89 を deny。 [file:1]

  - A と D は `ip ospf priority 0` としており、**R3 が DR にならないようにしてしまう（むしろ DR 候補から除外される）** ため要件に反する。  
  - C は `ip ospf network point-to-point` を使っており、**ここでは「non-broadcast にしてユニキャスト Neighbor を使う」必要がある** という要件と異なる。また ACL も OSPF（89）ではなく 88 をマッチしており不適切。 [file:1]

- 試験向けの覚え方フレーズ

  - 「**マルチキャスト通らない OSPF セグメント → network non-broadcast ＋ neighbor コマンド**」  
  - 「**DR を強制したいインタフェース → ip ospf priority を上げる（デフォ 1、0 は候補外）**」  
  - 選択肢の中で  
    - `ip ospf network non-broadcast`  
    - `neighbor <peer-ip>`  
    - `ip ospf priority` が 1 より大きい  
    の 3 点セットが揃っているものを探す、というパターンで覚えると取りやすい。

***
QUESTION 520 解説

- 問題文（英語）:  
Which configuration command establishes an EIGRP neighbor adjacency between the hub and spoke? [file:1]

A. connected 10.1.2.192 command on spoke router  
B. neighbor 10.1.2.192 command on hub router  
C. eigrp-peer 10.1.2.192 command on the hub router  
D. network 10.1.2.192 command on spoke router  

Correct Answer: B [file:1]

- 問題文要約（日本語）:  
ハブ＆スポーク構成で、**ハブとスポーク間で EIGRP ネイバーが確立できていない** 状況。  
おそらくこのネットワークではマルチキャストが通らないため、通常の EIGRP マルチキャスト Hello ではネイバーが張れない。**どのコマンドでハブとスポークの EIGRP ネイバー関係を確立できるか** を問う問題。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 解説では、「hub and spoke network in the question probably does not support multicast and therefore there is problem in forming EIGRP neighbor」とあり、  
    - マルチキャスト非対応のトランスポート（例: FR/NBMA/一部のクラウド）上で EIGRP を動かすためには、**ユニキャストでネイバーを指定する必要がある** と説明している。 [file:1]
  - そのため、EIGRP の **router コンフィグモード** で  
    - `neighbor 10.1.2.192 <interface>`  
    のように、ハブ側からスポークの IP を **neighbor コマンドで明示的に指定する** ことで、ユニキャスト Hello による隣接確立が可能になる。  
    - 選択肢 B の「neighbor 10.1.2.192 command on hub router」がこれに該当し、正解。 [file:1]

  - A の `connected`、C の `eigrp-peer` といったコマンドは、Cisco IOS の EIGRP 設定コマンドとして存在しない。  
  - D の `network 10.1.2.192` は、EIGRP プロセスにインタフェースを参加させるためのコマンドだが、**マルチキャストがブロックされている環境では、network を宣言するだけではネイバーは張れない**。この問題の本質は「multicast が通らない → unicast neighbor 指定が必要」という点。 [file:1]

- 試験向けの覚え方フレーズ

  - 「**マルチキャスト通らない EIGRP（NBMA/クラウド）→ neighbor コマンドでユニキャスト**」  
  - ハブ＆スポーク、EIGRP、マルチキャスト不可が同時に出てきたら、  
    - **“hub で `neighbor <spoke-ip>` を設定してユニキャストで張る”**  
    というパターンで覚えておくとほぼそのまま正解にたどり着ける。

***
QUESTION 521 解説

- 問題文（英語）:  
What is the failure detection time with BFD? [file:1]

A. 1 second  
B. 3 seconds  
C. 2 seconds  
D. less than a second  

Correct Answer: D [file:1]

- 問題文要約（日本語）:  
BFD（Bidirectional Forwarding Detection）を使った場合、**リンク／経路障害を検出するまでのおおよその時間はどれくらいか** を問う問題。 [file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 同じ PDF 内の別問題の解説では、BFD について  
    - 「BFD を使うと、**各種ルーティングプロトコルの Hello よりもずっと短い、一定の周期（uniform / consistent rate）で障害検出ができ、再収束時間が一貫して予測しやすくなる**」と述べられている。 [file:1]
  - 実装としては、BFD は数ミリ秒〜数十ミリ秒レベルのインターバル（例: 50 ms × 3 回など）で keepalive を送受信し、**典型的には 1 秒未満で障害を検出**するよう設計されている。  
  - 選択肢 A〜C の 1〜3 秒は、従来の OSPF/BGP/TCP Keepalive などの Hello/Dead タイマの世界観であり、BFD の「超高速検出」の売りと一致しない。  
  - したがって、「less than a second（1 秒未満）」を選ぶ D が BFD の特徴を最もよく表している。 [file:1]

***
QUESTION 522 解説

- 問題文（英語）:  
R1 lost its directly connected EIGRP peer 172.16.33.2 (SW1). Which configuration resolves the issue? [file:1]

A.  
key chain EIGRP  
 key 1  
  key-string Cisco  
!  
interface GigabitEthernet 2.10  
 ip authentication mode eigrp 88 md5  
 ip authentication key-chain eigrp 88 EIGRP  

B.  
key chain EIGRP  
 key 1  
  key-string Cisco  
!  
interface GigabitEthernet 2  
 ip authentication mode eigrp 88 md5  
 ip authentication key-chain eigrp 88 Cisco  

C.  
key chain EIGRP  
 key 1  
  key-string Cisco  
!  
interface GigabitEthernet 2  
 ip authentication mode eigrp 88 md5  
 ip authentication key-chain eigrp 88 EIGRP  

D.  
key chain EIGRP  
 key 1  
  key-string Cisco  
!  
interface GigabitEthernet 2.10  
 ip authentication mode eigrp 88 md5  
 ip authentication key-chain eigrp 88 Cisco  

Correct Answer: A [file:1]

- 問題文要約（日本語）:  
R1 が、直結の EIGRP ピア 172.16.33.2（SW1）との **EIGRP ネイバーをロスト** している。  
原因は EIGRP 認証（MD5＋key chain）の不一致で、**正しいインタフェース・正しい key chain 名を使って認証を一致させる** 必要がある。どの設定がそれを満たしてネイバーを復旧させるかを問う問題。 [file:1]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 解説では、EIGRP 認証に key chain を使うときのポイントとして次の 2 点が挙げられている。 [file:1]  
    1. **正しいインタフェースで認証を有効化すること**  
       - 図から、EIGRP ピア SW1 と接続しているのは `GigabitEthernet2.10`（サブインタフェース）である。  
       - よって `ip authentication mode eigrp 88 md5` や `ip authentication key-chain eigrp 88 ...` は **G2 ではなく G2.10 に設定する必要がある**。  
    2. **`ip authentication key-chain` で指定する key chain 名が、実際に定義した key chain 名と一致していること**  
       - ここでは `key chain EIGRP` と定義しているので、インタフェース側でも `... EIGRP` と指定する必要がある。 [file:1]
  - 選択肢 A は、  
    - `key chain EIGRP` を定義し、  
    - その key chain を **GigabitEthernet2.10** に対して `ip authentication key-chain eigrp 88 EIGRP` で関連付けており、上記 2 条件を正しく満たす。 [file:1]

  - B はインタフェースが G2（物理）であり、**実際にピアが存在するサブインタフェース G2.10 とは一致しない**。さらに key chain 名に `Cisco` を指定しており、`key chain EIGRP` と不一致。  
  - C も同様にインタフェース G2 を使っており、サブインタフェースに設定していない。  
  - D はインタフェースは G2.10 で正しいが、`ip authentication key-chain ... Cisco` と **定義されていない key chain 名を指定**しているため、不一致で認証失敗となる。 [file:1]

- 試験向けの覚え方フレーズ

  - 「**EIGRP 認証トラブル → “どのインタフェースにピアがぶら下がっているか” と “key chain 名一致” をチェック**」  
  - 特にサブインタフェース（`Gi2.10` など）が絡む問題では、  
    - “**認証コマンドが物理 IF ではなくサブ IF に入っているか？**”  
    - “**ip authentication key-chain の最後の名前が key chain と一致しているか？**”  
    を見る、と覚えておくと選択肢を絞りやすい。

***
QUESTION 523 解説

- 問題文（英語）:  
Which configuration resolves the route filtering issue on R1 to redistribute all the routes except 172.16.2.48/28? [file:1]

A.  
R1(config)#route-map CCNP deny 10  
R1(config-route-map)#no match route-type local  
R1(config-route-map)#match route-type external  
R1(config)#route-map CCNP permit 20  

B.  
R1(config)#route-map CCNP deny 10  
R1(config-route-map)#no match route-type local  
R1(config-route-map)#match route-type level-2  
R1(config)#route-map CCNP permit 20  

C.  
R1(config)#route-map CCNP deny 10  
R1(config-route-map)#no match route-type local  
R1(config-route-map)#match route-type external type-2  
R1(config)#route-map CCNP permit 20  

D.  
R1(config)#route-map CCNP deny 10  
R1(config-route-map)#no match route-type local  
R1(config-route-map)#match route-type external type-1  
R1(config)#route-map CCNP permit 20  

Correct Answer: A [file:1]

- 問題文要約（日本語）:  
R1 でルート再配布時のフィルタリング設定に問題があり、本来は **特定の外部プレフィックス（172.16.2.48/28 など）だけを除外し、それ以外は再配布したい**。  
図では、対象のプレフィックスが「Outside EIGRP route」として **静的ルート → EIGRP に再配布された “EIGRP external”** になっている。**どの route-map 設定で、この外部 EIGRP ルートだけをブロックし、それ以外を再配布できるか** を問う問題。 [file:1]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 解説では、対象となる外部プレフィックス（例: 172.16.2.16/28／問題文では 172.16.2.48/28）は、  
    - R4 で静的ルートとして設定され、  
    - それが EIGRP に再配布されているため、**EIGRP external route** として扱われていると説明している。 [file:1]
  - このルートだけを R1 で再配布対象から外したい場合、**route-map の deny シーケンスで “EIGRP external” を match してブロック**し、その後の permit シーケンスで他すべてを通すのが基本パターン。  
  - 選択肢 A はまさにその形になっている。 [file:1]  
    - `route-map CCNP deny 10`  
      - `match route-type external`（`no match route-type local` は local を明示的に除外しているイメージ）  
      → すべての **外部ルート（external）** をここで deny（再配布しない）。  
    - `route-map CCNP permit 20`  
      → それ以外のルートはすべて許可（再配布）される。
  - 解説も、「In order to block this EIGRP external routes in R1, you should setup “match route-type external” in the deny statement of the route-map “CCNP” in R1.」と明言している。 [file:1]
  - また、「type-1 / type-2 は EIGRP には不要」であり、**EIGRP の external をマッチさせるのに “external type-1 / type-2” を指定する必要はない** とも書かれている。 [file:1]

  - B の `match route-type level-2` は **IS-IS の L2 ルート** を示す概念であり、EIGRP external とは無関係。  
  - C, D は `external type-2` / `external type-1` を使っており、**OSPF の external type-1/type-2 のような文脈** で使う指定で、EIGRP external には不要かつ誤解を招く。 [file:1]

- 試験向けの覚え方フレーズ

  - 「**EIGRP の外部ルートだけ止めたい → route-map deny で “match route-type external”**」  
  - その後に **“何も match しない permit シーケンスを 1 個足して “他全部 OK” にする**」。  
  - “external type-1 / type-2” が出てきたら **OSPF 外部の文脈** を疑い、EIGRP 問題ではまず `match route-type external` だけで済む、と覚えておくと選択肢を切りやすい。

***
QUESTION 524 解説

- 問題文（英語）:  
Which two reasons would cause an LSP to break between two PE routers? (Choose two.) [file:1]

A. MPLS not enabled  
B. matching labels  
C. lost LDP adjacency  
D. IGP hello adjacency  
E. prefix mismatch  

Correct Answer: C, E [file:1]

- 問題文要約（日本語）:  
2 台の PE ルータ間で張られている **LSP（Label Switched Path）が切れてしまう主な原因** として、正しいものを 2 つ選ぶ問題。  
特に「LDP の状態」と「プレフィックス情報の不整合」がポイント。 [file:1]

- 正解: **C, E**

- 解説（なぜ C と E が正しいか）

  - 解説では、LSP が切れる要因として次の 2 つを挙げている。 [file:1]  
    1. **LDP adjacency の喪失（C）**  
       - MPLS で LSP を張るためには、PE 間／P 間で **LDP セッション（LDP adjacency）が確立し、ラベルのやり取りができている** ことが前提。  
       - LDP adjacency が切れるとラベルの配布・維持ができなくなり、その経路に対する LSP は形成・維持できない。
    2. **prefix mismatch（E）**  
       - 解説の備考にあるように、下流ルータから受信したラベルバインディングが **自分のルーティングテーブル／CEF に存在しないプレフィックス（サブネット＋マスク）** に対するものであれば、そのラベルは使用されない。  
       - 逆に、自分が広告するラベルが、自分のルーティングアップデートとは異なるプレフィックスに紐づいている場合、上流ルータ側でそのラベルは無効扱いとなり、**結果としてその区間の LSP が成立しない／途切れる**。 [file:1]

  - A の「MPLS not enabled」は、そもそも MPLS 自体が有効化されていなければ LSP は「張られない」状態であり、「原因で LSP が break した」というよりは **前提未満の状態**。  
  - B の「matching labels」は、むしろ **ラベルが一致している＝正常な動作の一部** であり、LSP 断の原因にはならない。  
  - D の「IGP hello adjacency」は、IGP の隣接関係そのものの話であり、**LSP が切れる直接の理由として問われている “LDP とプレフィックス整合性” から外れている**。

- 試験向けの覚え方フレーズ

  - 「**LSP トラブル → “LDP セッション” と “プレフィックス整合性（CEF にあるか、マスク含めて一致しているか）” をまず疑え**」  
  - MPLS が動かない系の問題で、  
    - “LDP adjacency down”  
    - “prefix mismatch / label to unknown prefix”  
    といったキーワードが出たら、**その区間の LSP が不成立になる** と紐付けて覚えておくと良い。

***
QUESTION 525 解説

- 問題文（英語）:  
What is the function of penultimate hop popping? [file:1]

A. The transport label is popped off at the egress LSR, and unlabeled traffic is forwarded toward the CE.  
B. The last P router in the path pops off the transport label before traffic is forwarded toward the PE.  
C. The VPN label is popped off at the egress LSR, and unlabeled traffic is forwarded toward the CE.  
D. The second to last P router in the path pops off the VPN label before traffic is forwarded to the last P router.  

Correct Answer: B [file:1]

- 問題文要約（日本語）:  
**Penultimate Hop Popping（PHP）** の動作として正しい説明はどれか、を問う問題。  
PHP は MPLS LSP の「終端近く」でラベルをどこで、どのラベルを外すのかがポイント。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 解説によると、PHP は  
    - 「**LSP の最後の P ルータ（＝PE の一つ手前の LSR）が、トランスポートラベルを POP（削除）してからパケットを PE に転送する動作**」を指す。 [file:1]
  - 通常の MPLS L3VPN ではラベルスタックは  
    - 外側: トランスポートラベル（LDP/IGP に基づく LSP 用）  
    - 内側: VPN ラベル（VRF へのマッピング用）  
    となっており、PHP では **PE 手前の P ルータが “トランスポートラベル” を外す**。  
    - その結果、PE はラベルを一段分少ない状態（VPN ラベルのみ、または IP パケット）で受け取り、ラベル処理負荷が軽減される。
  - 選択肢 B は、「The last P router in the path pops off the transport label before traffic is forwarded toward the PE」と説明しており、  
    - 「PE の一つ手前の P ルータ（penultimate hop）がトランスポートラベルを POP する」  
    → PHP の正しい定義そのもの。 [file:1]

  - A は POP の場所を「egress LSR（PE）」としており、**それは PHP ではなく、単に最終 LSR で POP する通常動作**。  
  - C, D は「VPN ラベルを POP」と書いており、PHP が対象とする「トランスポートラベル」とは異なる。特に D の「second to last P router が VPN ラベルを POP」は用語として不正確。

- 試験向けの覚え方フレーズ

  - 「**PHP = Penultimate（最後の 1 個前の）Hop が Transport ラベルを POP**」  
  - “second last P pops transport label → PE 楽になる” と丸暗記すると、選択肢 B を一瞬で選べる。

***
QUESTION 526 解説

- 問題文（英語）:  
Which two technologies optimize MPLS infrastructure using bandwidth protection services when expenencing slow response? (Choose two.) [file:1]

A. SD-MPLS  
B. Fast-Reroute  
C. VPLS  
D. MPLS OAM  
E. IP LFA  

Correct Answer: B, E [file:1]

- 問題文要約（日本語）:  
MPLS インフラで **遅延や応答低下（slow response）が発生したときに、帯域保護（bandwidth protection）を提供してパフォーマンスを最適化する技術** として正しいものを 2 つ選ぶ問題。 [file:1]

- 正解: **B, E**

- 解説（なぜ B と E が正しいか）

  - **Fast-Reroute（FRR）**  
    - MPLS/TE などで使われる高速迂回技術で、**リンクやノード障害発生時に事前計算済みのバックアップパスへ瞬時にトラフィックを切り替える**。  
    - これにより、障害中でも帯域を有効に使い続け、アプリケーションから見た遅延・切断時間をミリ秒オーダに抑えられる。  
  - **IP LFA（Loop-Free Alternate）**  
    - IGP（IS-IS / OSPF）上での **Fast-Reroute メカニズム** で、**プライマリリンク障害時に即座にループフリーなバックアップネクストホップへフォールバック**する。  
    - MPLS コアでも IGP + LDP 構成の場合、IP LFA によるプレバックアップ経路が、実質的に帯域保護と高速復旧を提供する。

  - A の SD-MPLS は一般的な Cisco 試験用キーワードではなく、ここで言う帯域保護技術とは無関係。  
  - C の VPLS は L2 VPN 技術であり、「帯域保護サービス」そのものではない。  
  - D の MPLS OAM は **監視・故障検知のための運用ツール群** であり、FRR や LFA のように実際にトラフィックを保護する仕組みではない。

- 試験向けの覚え方フレーズ

  - 「**帯域保護 / Fast recovery → FRR（TE） と LFA（IGP）**」  
  - MPLS＋“bandwidth protection, fast reroute, slow response” と出たら、  
    - **MPLS-TE FRR** と **IP LFA** の 2 つをセットで思い出す、と覚えておくと取りやすい。

***
QUESTION 527 解説

- 問題文（英語）:  
The engineer is trying to transfer the new IOS file to the router R3 but is getting an error. Which configuration achieves the file transfer? [file:1]

A.  
R4(config)#no access-list 120 permit udp host 172.16.1.2 host 172.16.2.19 eq 69  
R4(config)#no access-list 120 deny udp any any eq tftp  
R4(config)#access-list 120 permit tcp any any  

B.  
R4(config)#no access-list 120 permit udp host 172.16.1.2 host 172.16.2.19 eq 69  
R4(config)#access-list 120 permit udp host 172.16.1.17 host 172.16.2.19 eq 69  
R4(config)#access-list 120 permit tcp any any  

C.  
R4(config)#no access-list 120 permit udp host 172.16.1.2 host 172.16.2.19 eq 69  
R4(config)#access-list 120 permit tcp host 172.16.1.17 host 172.16.2.19 eq 69  
R4(config)#access-list 120 permit tcp any any  

D.  
R4(config)#no access-list 120 permit udp host 172.16.1.2 host 172.16.2.19 eq 69  
R3(config)#no ip tftp source-interface GigabitEthernet0/3  

Correct Answer: B [file:1]

- 問題文要約（日本語）:  
エンジニアが R3 に新しい IOS を TFTP 転送しようとしているが失敗している。  
R3 では `ip tftp source-interface GigabitEthernet0/3` が設定されており、TFTP アクセス時のソース IP は **172.16.1.17（G0/3）** になる。  
一方 R4 では、`access-list 120` が **TFTP (UDP/69) を 172.16.1.2 からのトラフィックにしか許可しておらず**、172.16.1.17 からの TFTP をブロックしている。どの設定で R3 からの TFTP 転送を成功させられるかを問う問題。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 状況整理: [file:1]  
    - R3: `ip tftp source-interface GigabitEthernet0/3` → TFTP のソース IP = 172.16.1.17。  
    - R4: `access-list 120` を G0/0 inbound に適用しており、  
      - `permit udp host 172.16.1.2 host 172.16.2.19 eq 69` のみ TFTP 許可。  
      - つまり **172.16.1.17 から TFTP サーバ 172.16.2.19:69 宛のトラフィックは ACL で拒否される**。
  - 解説どおり、問題の根本原因と解決策は 2 通りある。 [file:1]  
    1. R3 側で `ip tftp source-interface Gig0/3` を消して、ソース IP を 172.16.1.2 に戻す。  
    2. R4 側 ACL を変更して、**172.16.1.17 からの TFTP (UDP/69) を許可**する。
  - ただし注記として、`no access-list 120 <行>` の形で消すと「その 1 行だけでなく ACL 120 全体が削除される」ため、  
    - 一旦 `no access-list 120 ...` を実行した後は、**TFTP 許可ルールと TCP の permit ルールを入れ直す必要がある**。  
  - 選択肢 B はまさにこの流れを踏んでいる。 [file:1]  
    - `no access-list 120 permit udp host 172.16.1.2 ...`  
      → ACL 120 全体を削除。  
    - `access-list 120 permit udp host 172.16.1.17 host 172.16.2.19 eq 69`  
      → ソース 172.16.1.17（R3 G0/3）から TFTP への UDP/69 を明示的に許可。  
    - `access-list 120 permit tcp any any`  
      → 他の TCP トラフィックも許可。残りの TFTP/UDP は implicit deny でブロック。
  - D は R4 側の ACL 120 を消すだけでなく、R3 側の `ip tftp source-interface` も削除してしまう案だが、  
    - これだと R4 のトラフィルタリングが緩くなり、**不要な TFTP/UDP も通してしまう可能性がある**ため、解説のとおり「動きはするがベストではない」。 [file:1]
  - C は TCP 69 を許可しているが、TFTP は UDP/69 のため、正しくない。  
  - A は UDP/69 の deny 行も削除し、そのあと TCP any any しか設定していないため、**TFTP（UDP）は implicit deny で全部落ちる**。

- 試験向けの覚え方フレーズ

  - 「**TFTP = UDP/69 ＋ ソース IP は ip tftp source-interface に注意**」  
  - ACL 問題で TFTP 転送失敗が出たら、  
    - “**UDP/69 を正しいソース IP から許可しているか？**”  
    - “**ip tftp source-interface で意図しない IP を使っていないか？**”  
    の 2 点をまずチェックする、と覚えておくと良い。

***
QUESTION 528 解説

- 問題文（英語）:  
Spoke routers do not learn about each other's routes in the DMVPN Phase2 network. Which action resolves the issue? [file:1]

A. Configure a static route in each spoke to establish a spoke-to-spoke tunnel.  
B. Rectify incorrect wildcard mask configured on the hub router network command.  
C. Remove default route from spoke routers to establish a spoke-to-spoke tunnel.  
D. Disable EIGRP split honzon on the Tunnel0 interface of the hub router.  

Correct Answer: D [file:1]

- 問題文要約（日本語）:  
DMVPN Phase 2 の構成で、**スポーク同士が互いのルートを学習できていない（スポーク間通信ができない）** 状況。  
このとき、**どの対処を行えばスポーク間でルートが行き来するようになるか** を問う問題。 [file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - DMVPN Phase 2 では、ハブを経由して **スポーク同士のダイナミックな spoke-to-spoke トンネル** を張ることが特徴で、そのためには  
    - ハブが **各スポークから学習した EIGRP ルートを、他スポークへ再広告** する必要がある。
  - しかし、EIGRP ではデフォルトで **split horizon** が有効であり、「**あるインタフェースから受け取った EIGRP ルートは同じインタフェースからは再広告しない**」ルールがある。  
    - DMVPN ハブの `Tunnel0` で split horizon が有効だと、  
      - Spoke A から学習したルートを、同じ Tunnel0 から Spoke B に再広告せず、  
      - その結果、**スポーク同士が互いのプレフィックスを見えない** ままになってしまう。
  - したがって、**ハブの DMVPN トンネルインタフェース（Tunnel0）で EIGRP split horizon を無効化**する必要がある。  
    - 具体的には:  
      - `interface Tunnel0`  
      - ` no ip split-horizon eigrp <AS-number>`  
    - 選択肢 D の「Disable EIGRP split honzon on the Tunnel0 interface of the hub router」がまさにこれ。 [file:1]

  - A の静的ルートは、DMVPN Phase 2 のダイナミックスポーク間トンネルという設計思想と矛盾し、運用スケールもしない。  
  - B の wildcard マスクや C のデフォルトルート削除は、「スポーク同士が互いのルートを学習しない」根本原因である **EIGRP split horizon** の問題を解決しない。 [file:1]

- 試験向けの覚え方フレーズ

  - 「**DMVPN + EIGRP + スポーク間ルート見えない → まず hub の Tunnel で split horizon を疑え**」  
  - “Hub-and-spoke マルチアクセス上の EIGRP” でスポーク同士が見えない問題が出たら、  
    - **“no ip split-horizon eigrp” をハブ側マルチポイントインタフェースに入れる**、とパターンで覚えておくとよい。

***
QUESTION 529 解説

- 問題文（英語）:  
Router R4 is configured correctly with default OSPF values. A network engineer configured R7 for OSPF. R7 must not be elected as a DR for the segment between R4-R7. The adjacency between R4 and R7 failed to form. Which configuration resolves the issue? [file:1]

A.  
B.  
C.  
D.  

Correct Answer: C [file:1]

- 問題文要約（日本語）:  
R4 はデフォルト値で正しく OSPF 設定済み。  
新たに R7 を OSPF に参加させたが、「**R4–R7 セグメントでは R7 を DR にしない**」という要件がある。  
しかし設定後、R4 と R7 の OSPF ネイバーが形成されない。**どのコンフィグで “R7 を DR から外しつつ、正常に隣接を張れるか”** を問う問題。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 解説によると、R7 を DR/BDR 選出から除外するには、対象インタフェースで **OSPF priority を 0 に設定する** のが正解。  
    - `ip ospf priority 0` とすると、そのインタフェースは **DR/BDR の候補から完全除外**される。 [file:1]
  - しかし、候補となる選択肢のうち 2 つは priority 0 を設定しつつ、**OSPF ネットワークタイプを “non-broadcast” に変更**している。  
    - `ip ospf network non-broadcast` を使うと、  
      - マルチキャスト Hello が使えない前提となり、**`neighbor <IP>` コマンドでユニキャストネイバーを明示的に定義しないと隣接が張れない**。  
    - 既存の構成は `point-to-point`（あるいは `broadcast`）を前提にしており、おそらく R7 側では `neighbor` コマンドを入れていないため、**non-broadcast に変えた結果、ネイバーが張れなくなっている**。 [file:1]
  - 一方、解説では「network type を non-broadcast にすると neighbor コマンドが必要になるので、**既存の “point-to-point” 前提構成では “broadcast” のまま priority 0 を設定する構成が正解**」と説明している。 [file:1]  
    - つまり、  
      - OSPF priority 0 を設定  
      - かつ、ネットワークタイプは **neighbor コマンド不要な “broadcast”/既定タイプ** のまま  
      にしている選択肢 C が正解となる。

- 試験向けの覚え方フレーズ

  - 「**OSPF で “DR にさせたくないだけ” → ip ospf priority 0 を入れるだけでネットワークタイプは変えない**」  
  - non-broadcast は **neighbor コマンド必須** なので、  
    - “priority 0 で DR から外したい” だけの問題で **network type を non-broadcast に変えている選択肢は怪しい** と覚えておくと判断しやすい。

***
QUESTION 530 解説

- 問題文（英語）:  
An engineer configured a router with this configuration:  
The router console starts receiving log message “%SEC-6-IPACCESSLOGP: list DENY_TELNET denied tcp 192.168.1.10(10222) (FastEthernet1/0 E416.98ac.116e) -> 192.168.2.20(23), 1 packet”. Which action stops messages on the console while still denying Telnet? [file:1]

A. Remove log-input keyword from the access list.  
B. Configure a 20 permit ip any any log-input command.  
C. Configure a 20 permit ip any any command.  
D. Replace log-input keyword with the log keyword in the access list.  

Correct Answer: A [file:1]

- 問題文要約（日本語）:  
ACL「DENY_TELNET」で Telnet を deny する設定は正しく動作しているが、コンソールに  
“%SEC-6-IPACCESSLOGP: list DENY_TELNET denied tcp …”  
のログが大量に出てうるさい。**Telnet は引き続きブロックしつつ、このログだけ止めたい**。どのアクションが適切かを問う問題。 [file:1]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 解説によると、ACL の deny 行には `log-input` キーワードが付いている。 [file:1]  
    - `log-input` は「**マッチしたパケットをログ出力し、その入力インタフェース情報も含めて表示する**」オプション。  
    - そのため、Telnet をブロックするたびに `%SEC-6-IPACCESSLOGP` メッセージがコンソールに出続けている。
  - つまり、**ログが出ている原因は `log-input` であり、Telnet deny 自体は正しく機能している**。  
    - よって、**`log-input` だけを削除すれば** Telnet は引き続き deny されつつ、ログメッセージは出なくなる。  
    - これが選択肢 A の「Remove log-input keyword from the access list」に対応する。 [file:1]
  - 解説の Remarks にも、「`log-input` を `log` に置き換えてもログ自体は出続け、入力インタフェースが記録されないだけ」と明記されている。  
    - したがって D は不正解。 [file:1]
  - B のように `permit ip any any log-input` を足すと、**すべての IP 通信を許可＋ログ**という危険な ACL になり、Telnet も許可されてしまう。  
  - C の `permit ip any any` も Telnet を通してしまうため、「deny し続ける」という要件に反する。

- 試験向けの覚え方フレーズ

  - 「**ACL ログがうるさい → “log/log-input” を外すだけで、マッチ動作自体は変えない**」  
  - 特に `%SEC-6-IPACCESSLOGP` ＋ `log-input` がセットで出てきたら、  
    - **“deny はそのまま、log-input だけ削除”** が定石、と覚えておくと良い。

***
QUESTION 531 解説

- 問題文（英語）:  
What are the two major components of an MPLS-based VPN? (Choose two.) [file:1]

A. VPN route distinguisher  
B. MP-BGP peering of VPN community CE devices  
C. MP-BGP peering of VPN community P devices  
D. MP-BGP peering of VPN community PE devices  
E. VPN route target communities  
F. VPN route reflectors  

Correct Answer: D, E [file:1]

- 問題文要約（日本語）:  
MPLS ベースの L3VPN を構成するうえでの **主要コンポーネント（中核要素）** として正しいものを 2 つ選ぶ問題。 [file:1]

- 正解: **D, E**

- 解説（なぜ D と E が正しいか）

  - MPLS L3VPN の基本構造は、  
    - **PE ルータ同士が MP-BGP で VPNv4/VPNv6 ルートを交換し**（MP-BGP VPNv4/VPNv6 セッション）、  
    - 各 VPN（顧客）の経路を **Route Target (RT) コミュニティ** でタグ付けして「どの VRF にインポート／エクスポートするか」を制御する、という仕組みで成り立っている。  
  - したがって、  
    - D: **「MP-BGP peering of VPN community PE devices」**  
      - → PE 間の MP-BGP ピアリングがなければ、VPN ルートの伝搬そのものが成立しないので、MPLS VPN の中核要素。  
    - E: **「VPN route target communities」**  
      - → RT によって「どの VPN サイト同士が疎通するか（フルメッシュ／ハブスポークなど）」を定義するため、これも MPLS VPN の主要コンポーネントといえる。 [file:1]

  - A の RD（Route Distinguisher）は「VPNv4 アドレスをグローバルに一意にするためのタグ」であり重要ではあるが、**“通信ポリシー” や “PE 間経路交換” の核ではない**。  
  - B の CE 間 MP-BGP ピアリング、C の P 間 MP-BGP は、一般的な MPLS L3VPN アーキテクチャには含まれない。  
  - F の Route Reflector はスケールアウト用の仕組みであり、MPLS VPN の本質的な 2 大要素というよりは設計オプション。

- 試験向けの覚え方フレーズ

  - 「**MPLS L3VPN の心臓は “PE 間 MP-BGP” ＋ “Route Target”**」  
  - “MPLS VPN のコンポーネントは？”と聞かれたら、  
    - **MP-BGP on PE** と **RT コミュニティ** をまず思い出す、と覚えておくと選びやすい。

***
QUESTION 532 解説

- 問題文（英語）:  
R1 is not forming adjacency on a point-to-point interface. Which action resolves the issue? [file:1]

A. The area numbers must be configured the same on each router.  
B. The area types must be configured the same on each router.  
C. The no-summary command must be included in the area configuration on R2.  
D. The no-summary command must be included in the area configuration on R1.  

Correct Answer: B [file:1]

- 問題文要約（日本語）:  
ポイント・ツー・ポイント接続のインタフェースで R1 が OSPF 隣接を張れない。  
デバッグログには「**OSPFv3: Hello from ... with mismatched NSSA option bit**」のようなメッセージが出ている。**どの対応をすれば隣接が確立できるか** を問う問題。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 解説によると、「`OSPFv3: Hello from …. with mismatched NSSA option bit` というデバッグメッセージは、**両ルータで設定されているエリアタイプが一致していないときに出る**」と説明されている。 [file:1]  
    - 片側が通常エリア、もう片側が NSSA（Not-So-Stubby Area）や Stub など、**エリアタイプが異なる** 状態だと、Hello パケットのオプションビット（NSSA bit など）が一致せず、OSPFv3 隣接は形成されない。
  - よって、この問題を解決するには、**両方のルータで同一エリアタイプになるようにエリア設定を揃える必要がある**。  
    - これを最も一般的に表している選択肢が「B: The area types must be configured the same on each router」。 [file:1]

  - A の「エリア番号を揃える」も OSPF 隣接の条件として必要だが、ここではデバッグに **“mismatched NSSA option bit” と明示** されており、番号ではなく「タイプ不一致」が根本原因。  
  - C, D の `no-summary` は ABR 上で NSSA / Stub エリアへの集約 LSA を制御するコマンドで、**今回の “NSSA オプションビット不一致” を直接解消する鍵ではない**。

- 試験向けの覚え方フレーズ

  - 「**OSPF Hello で “mismatched NSSA option bit” → エリアタイプ不一致（Stub/NSSA/Normal）が原因**」  
  - エリア番号ではなく **“area type を揃える”** ことがポイント、と覚えておくと B を即決しやすい。

***
QUESTION 533 解説

- 問題文（英語）:  
How are LDP neighbors discovered? [file:1]

A. Broadcasts hellos are sent to the 255.255.255.255 broadcast address  
B. Unicast hellos are sent to directly connected neighbors IP addresses  
C. Multicast hellos are sent to the 224.0.0.5 group address  
D. Multicast hellos are sent to the 224.0.0.2 group address  

Correct Answer: D [file:1]

- 問題文要約（日本語）:  
**LDP（Label Distribution Protocol）の隣接ルータは、どのように検出されるか**、使用するアドレス種別（ブロードキャスト／ユニキャスト／マルチキャスト）とマルチキャストグループアドレスを問う問題。 [file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - LDP は、直接接続された隣接ルータを検出するために **UDP/646 で Hello パケットをマルチキャスト送信**する。  
  - その宛先は **224.0.0.2（All Routers マルチキャストアドレス）** であり、ここをリッスンしている LDP 対応ルータが Hello を受け取り、**LDP 隣接として認識・セッション確立（TCP/646）** を行う。  
  - よって「Multicast hellos are sent to the 224.0.0.2 group address」が正しい説明となる。 [file:1]

  - A: 255.255.255.255 へのブロードキャストは LDP では使わない。  
  - B: targeted LDP などではユニキャスト Hello もあるが、問題文は一般的な「LDP neighbors discovered」の話なので、**デフォルトのマルチキャスト Hello** が正解。  
  - C: 224.0.0.5 は OSPF の AllSPFRouters 用であり、LDP とは無関係。

- 試験向けの覚え方フレーズ

  - 「**OSPF = 224.0.0.5 / 6、LDP = 224.0.0.2**」  
  - “LDP neighbor discovery = UDP/646 + 224.0.0.2 マルチキャスト Hello” とセットで覚えておくと、選択肢 D を即座に選べる。

***
QUESTION 534 解説

- 問題文（英語）:  
While troubleshooting an issue on the network, an engineer notices that a TCP Connect operation failed on port 3000 between R101 and R201. Which command must be configured on R201 to respond to the R101 IP SLA configurations with a control connection on UDP port 1967? [file:1]

A. ip sla responder tcp-connect ipaddress 2.2.2.2 port 3001  
B. ip sla responder  
C. ip sla responder tcp-connect ipaddress 1.1.1.1 port 3000  
D. ip sla responder udp-echo ipaddress 1.1.1.1 port 1967  

Correct Answer: B [file:1]

- 問題文要約（日本語）:  
R101 から R201 へ **IP SLA の TCP connect（ポート 3000）** を実行したところ失敗。  
R101 側の SLA 設定では `control` を無効にしておらず、**標準の IP SLA コントロール接続（UDP 1967）** を前提にしている。  
R201 側にどのコマンドを入れれば、R101 の IP SLA に対して UDP 1967 のコントロール接続で正しく応答できるかを問う問題。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 解説では、R101 で IP SLA を `tcp-connect` モードで設定しており、`control` を無効にしていない場合、  
    - R101 はテストごとに **UDP/1967 でコントロールメッセージを送り、R201 に「どのポートでどう応答すべきか」を通知**する、と説明している。 [file:1]
  - エラーメッセージが “No connection” になっているのは、  
    - R201 側が **このコントロールメッセージに応答する IP SLA Responder として動作していない** ため。  
  - したがって、R201 側で必要なのは **単純に IP SLA Responder 機能を有効化すること** であり、  
    - グローバルコンフィグで `ip sla responder` を有効にすれば、UDP/1967 の制御ポートで R101 の SLA コントロールに自動応答するようになる。  
    - これが選択肢 B に相当する。 [file:1]

  - 解説の備考にある通り、もし R101 側で `control disable` を設定している場合は、  
    - コントロールメッセージを使わないため、R201 側で `ip sla responder tcp-connect ipaddress 1.1.1.1 port 3000` のように **明示的にポート 3000 を監視する responder 設定** が必要になる。  
    - しかし今回の前提では control が有効なので、そのような個別設定は不要。 [file:1]
  - D の `udp-echo` は UDP エコー用であり、TCP Connect SLA のケースとは異なる。  
  - A, C は「control disable」のケースでのみ意味を持つ設定。

- 試験向けの覚え方フレーズ

  - 「**IP SLA (control 有効) → 受け側は “ip sla responder” だけで OK、UDP 1967 は自動**」  
  - “No connection” ＋ control 使用の TCP connect SLA が出たら、  
    - まず **`ip sla responder` の有無** を疑う、と覚えておくと選択肢 B を迷わず選べる。

***
QUESTION 535 解説

- 問題文（英語）:  
Which configuration is required for R2 to get the IP address from the DHCP server. [file:1]

A. ip access-list extended R2WAN  
   permit tcp any any eq 68  

B. ip access-list extended R2WAN  
   permit udp any any eq 67  

C. interface GigabitEthernet0/0  
   ip access-group R2WAN out  

D. ip access-list extended R2WAN  
   permit udp any any eq 68  

Correct Answer: D [file:1]

- 問題文要約（日本語）:  
R2 の WAN 側には拡張 ACL「R2WAN」が設定されており、**現在は特定の TCP トラフィックしか許可していない**。  
この状態で、R2 自身が DHCP サーバから IP アドレスを取得できない。**R2 が DHCP サーバから IP をもらえるようにするには、どの設定を追加すべきか** を問う問題。 [file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 解説によると、既存の ACL「R2WAN」は **TCP トラフィックのみ許可** しており、DHCP で使用する **UDP トラフィックがブロックされている**。 [file:1]
  - DHCP のポート番号:  
    - サーバ側: UDP 67  
    - クライアント側: UDP 68  
  - R2 が「**クライアントとして IP アドレスを取得する**」立場であるため、  
    - **R2 から出る DHCP Discover/Request はソース 68 → 宛先 67** の UDP であり、  
    - その戻りの Offer/Ack はソース 67 → 宛先 68。  
  - 問題の意図は、「R2 自身が DHCP サーバとやりとりできるように **クライアントポート 68 の UDP を ACL で許可する**」こと。  
    - したがって、`ip access-list extended R2WAN` に  
      - `permit udp any any eq 68`  
      を追加する D が正解。 [file:1]

  - A は TCP/68 を許可しており、DHCP とはプロトコルが異なるため無意味。  
  - B は UDP/67 を許可しているが、「クライアントとしての R2 が自分のポート 68 で受信する側」の視点からは不十分かつ不適切。  
  - C は ACL の適用方向（out）であり、DHCP クライアントとしての動作に必要な **UDP/68 の許可自体を追加していない**。

- 試験向けの覚え方フレーズ

  - 「**DHCP クライアント側 = UDP 68、サーバ側 = UDP 67**」  
  - ルータ自身が DHCP クライアントで ACL に阻まれている問題が出たら、  
    - **“extended ACL で UDP/68 を permit しているか？”** をまず確認する、と覚えておくと D を即決しやすい。

***
QUESTION 536 解説

- 問題文（英語）:  
What is the downstream unsolicited distribution method in MPLS?

A. It advertises labels to peers only when the peer requests.  
B. It sends a unicast hello message to a specific LSR.  
C. It sends a unicast hello message to a specific LER.  
D. It advertises labels to peers without peer request.  

Correct Answer: D [web:4][web:6][web:7][web:8]

- 問題文要約（日本語）:  
MPLS における **“downstream unsolicited” ラベル配布方式** の動作として正しい説明はどれか、を問う問題。

- 正解: **D**

- 解説（なぜ D が正しいか）

  - MPLS/LDP では、ラベル配布方式として主に 2 種類がある。 [web:4][web:6]  
    - Downstream on Demand: 上流ルータが「この FEC のラベルをくれ」と要求したときだけ、下流ルータがラベルを広告する方式。→ A の説明。  
    - **Downstream Unsolicited**: 下流ルータが、**要求されていなくてもラベルマッピングを上流のピアに勝手に広告する方式**。  
  - Cisco の LDP 実装でも、「**Downstream Unsolicited: An LSR advertises label mappings to peers without being asked to**」と定義されている。 [web:4]  
  - したがって、「It advertises labels to peers **without peer request**」と書いている D が、downstream unsolicited の定義そのもの。

  - A は “only when the peer requests” とあり、これは **downstream on demand** の説明。 [web:4][web:6]  
  - B, C は Hello の話（隣接検出）であり、「ラベル配布方式」そのものの説明ではない。

- 試験向けの覚え方フレーズ

  - 「**DoD（On-Demand）＝要求されたときだけ、DU（Unsolicited）＝聞かれてなくても全部配る**」  
  - “downstream unsolicited” と見えたら、  
    - **“ラベルを勝手に、要求なしで配る”＝D** と即答できるようにしておくと良い。

***
QUESTION 537 解説

- 問題文（英語）:  
Users on a call center report that they cannot browse the internet on Saturdays during the afternoon. Which configuration resolves the issue? [file:1]

A. time-range timer  
   no periodic weekend 9:00 to 17:00  
   periodic weekend 17:00 to 23:59  

B. no time-range timer  

C. interface gig0/0  
   ip access-group NO_Internet out  

D. ip access-list extended NO_Internet  
   15 permit tcp any any eq www  

Correct Answer: A [file:1]

- 問題文要約（日本語）:  
コールセンターのユーザが「**土曜日の午後にインターネット閲覧ができない**」と報告している。  
ACL と time-range「timer」を使って WWW アクセス制御をしている構成で、**どの修正で問題を解消できるか** を問う問題。 [file:1]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 解説によると、既存の time-range「timer」は  
    - `periodic weekend 9:00 to 17:00`  
    のように設定されており、「**週末 9:00〜17:00 の間だけ “インターネット閲覧許可/または拒否” の条件が成立する」ような ACL 連動がなされている。 [file:1]
  - 質問文では「土曜の午後に閲覧できない」とあるので、  
    - 実際にアクセスを試しているのは **17:01 など、既存 time-range から外れた時間帯** である可能性が高い。  
    - その結果、ACL の許可/拒否条件が期待通りに作用せず、インターネットアクセスがブロックされている。 [file:1]
  - 解説では、time-range を  
    - `no periodic weekend 9:00 to 17:00`  
    - `periodic weekend 17:00 to 23:59`  
    のように変更し、**週末 17:00〜23:59 にもアクセス可能となるよう時間帯をずらす** 案が「他の選択肢よりは最も妥当」とされている。 [file:1]
    - これが選択肢 A に一致する。
  - B のように time-range 自体を削除すると、**本来のポリシー（特定時間のみ制限／許可）という設計思想を壊す** ため、あまり良い解決方法ではない。  
  - C のように ACL の適用方向を変えてしまうのも、**意図しないトラフィック制御** を生みやすく、論理的ではない。 [file:1]
  - D の `permit tcp any any eq www` を ACL の後ろに追加したとしても、**既存の deny www エントリが先頭にある場合、すでにそこでマッチしてブロックされるため効果がない**。 [file:1]

- 試験向けの覚え方フレーズ

  - 「**ACL＋time-range で “時間帯アクセス不可” が出たら、まず時間帯定義の範囲ミスを疑う**」  
  - 特に “週末 9:00–17:00” などの time-range が出てきたら、  
    - 「**その枠外の時間にアクセスしていないか？** → 必要なら time-range の時間帯を再設定」  
    という流れで考えると、選択肢 A のような修正を自然に選べる。

***
QUESTION 538 解説

- 問題文（英語）:  
An engineer configured user login based on authentication database on the router, but no one can log into the router. Which configuration resolves the issue? [file:1]

A. aaa authentication login default local  
B. aaa authorization network default local  
C. aaa authentication login default enable  
D. aaa authorization exec default local  

Correct Answer: A [file:1]

- 問題文要約（日本語）:  
エンジニアがルータ上で **ローカルの認証データベース（username/password）を使ったログイン** を設定したが、誰もログインできなくなった。  
AAA を有効化したことで、従来の `login local` などの設定が無効になっている状況で、**どの AAA 設定を追加すれば再びローカルユーザでログイン可能になるか** を問う問題。 [file:1]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 解説では、「`aaa new-model` を有効にすると、line コンフィグに入れていた `login local` は自動的に無効化される（削除される）」という挙動に触れている。 [file:1]  
    - その結果、「**誰であってもログインできない**」状態になっている。
  - この状態で、再びローカルユーザデータベースを使ってログインできるようにするには、  
    - `aaa authentication login default local`  
    を設定して、**デフォルトのログイン認証メソッドリストに “local データベース” を指定**する必要がある。 [file:1]
  - これにより、コンソールや vty で `login authentication default`（暗黙または陽に）を使うと、**ルータ上に定義した username/password で認証されるようになる**。

  - B, D は AAA **authorization**（認可）のリストであり、「ログインできない」問題そのもの（authentication）を直接解決しない。  
  - C の `default enable` は enable パスワードベース認証を使う方法で、**ローカルユーザ DB ベースのログイン認証を有効にする意図とは異なる**。

- 試験向けの覚え方フレーズ

  - 「**aaa new-model を入れたら “aaa authentication login default local” を忘れない**」  
  - “AAA 入れたらログインできなくなった” 系問題が出たら、  
    - まず **default login authentication を local に向ける**、と覚えておくと A を即答しやすい。

***
QUESTION 539 解説

- 問題文（英語）:  
The 130.130.130.0/24 route shows in the R2 routing table but is getting filtering toward R3. Which action resolves the issue?

A. Automatic route summarization must be enabled on R2.  
B. The outgoing filter list for all interfaces must be set on R2.  
C. The incoming filter list for all interfaces must be set on R2.  
D. IGP synchronization must be disabled on R2.  

Correct Answer: D [web:10][web:11][web:12][web:14][web:17][web:22]

- 問題文要約（日本語）:  
R2 のルーティングテーブルには **130.130.130.0/24** のルートは見えているのに、**R3 に対してはそのルートがアドバタイズされていない（フィルタされているように見える）** 状況。  
どの設定変更を行えば、R2 から R3 へ 130.130.130.0/24 を正しく広告できるか、を問う問題。

- 正解: **D**

- 解説（なぜ D が正しいか）

  - これは典型的な **BGP IGP Synchronization** の挙動による問題。 [web:14][web:17][web:21]  
    - 同期（synchronization）が有効な場合、**iBGP で学習した経路は、自身の AS 内の IGP にも存在していなければ、eBGP ピアに広告しない**。  
  - 問題のシナリオでは、  
    - R2 は R1 から BGP（おそらく iBGP）で 130.130.130.0/24 を学習し、自身のルーティングテーブルにはその経路が存在している。 [web:14][web:17]  
    - しかし、そのプレフィックスは IGP では学習していないため、「not synchronized」と見なされ、**R3 に対して BGP 広告されない**。 [web:14][web:17][web:21]
  - 解説でも、「Automatic route summarization は無効だが、**IGP synchronization が有効**であるため、BGP で学習した 130.130.130.0/24 を R2 が他のルータに広告しない」と説明している。  
    - この状況を解決する方法として  
      1. R2 の BGP 設定で **synchronization を無効化する**（`no synchronization`）  
      2. あるいは、R1 から IGP（例: OSPF/IGRP）でも同じルートを流すようにして IGP に載せる  
      が挙げられており、試験の選択肢上は 1（＝選択肢 D）が正解となる。 [web:14][web:17][web:21][web:22]

  - A の自動サマリは BGP 経路広告の可否とは関係がない。  
  - B, C の filter list については、問題文や典型的な出題では「Outgoing / Incoming filter は設定されていない」旨が show ip protocols などに示されており、原因ではない。 [web:14][web:17]

- 試験向けの覚え方フレーズ

  - 「**“ルーティングテーブルにはあるのに、BGP が広告しない” ＋ “iBGP 由来” → まず BGP synchronization を疑う**」  
  - 具体的には、  
    - “not synchronized”  
    - “IGP synchronization is enabled”  
    の文字が見えたら、**`no synchronization`（＝IGP sync 無効化）＝ 選択肢 D** と即答できるようにしておくと良い。

***
QUESTION 540 解説

- 問題文（英語）:  
R2 can reach Loopback222, but R1, SW1, and PC1 cannot communicate with 172.16.222.254. R1 and R2 configurations are shown here:  
Which EIGRP configuration command resolves the issue? [file:1]

A. R1(config-router)# redistribute static  
B. R2(config-router)# redistribute static  
C. R1(config-router)# network 172.16.222.254 0.0.0.0  
D. R1(config-router)# network 172.16.222.254 255.255.255.255  

Correct Answer: B [file:1]

- 問題文要約（日本語）:  
- 172.16.222.254 は SW2 の Loopback222（/32）。  
- R2 からはこのループバックに到達できるが、R1／SW1／PC1 からは 172.16.222.254 へ到達できない。  
- R1, R2 では EIGRP が動作しており、R2 の `network 172.16.222.0 0.0.0.255` は設定済み。  
**どの EIGRP 設定を追加すれば、172.16.222.254 への到達性を他ノードに広報できるか** を問う問題。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 解説によれば、172.16.222.254 は **SW2 の Loopback222（/32）** であり、R2 のどのインタフェースにも直接接続されていない。 [file:1]  
    - つまり、R2 の `network 172.16.222.0 0.0.0.255` は  
      - 「R2 上の “172.16.222.x” を持つインタフェース」を EIGRP に参加させるだけであり、  
      - R2 が **スタティックルートとして持っている 172.16.222.254/32** を EIGRP に自動広告するわけではない。
  - 質問文から、R2 は SW2 のループバックへ到達できているので、  
    - R2 には「`ip route 172.16.222.254 255.255.255.255 <SW2 next-hop>`」のような **スタティックルート** が設定されていると推測できる。 [file:1]
  - しかし、このスタティックルートは EIGRP に再配布されていないため、  
    - R1 側には 172.16.222.254/32 が届かず、SW1 や PC1 からも到達できない。  
  - この状況を解決するには、R2 の EIGRP プロセスで **スタティックルートを EIGRP に再配布**する必要がある。  
    - 具体的には:  
      - `R2(config-router)# redistribute static`  
    - これにより、R2 が持つ 172.16.222.254/32 のスタティックルートが **EIGRP external route として R1 に広告される**。 [file:1]

  - A のように R1 で `redistribute static` をしても、R1 側には 172.16.222.254/32 のスタティックルートが存在しないため意味がない。  
  - C, D は R1 側で 172.16.222.254 を `network` コマンドに入れているが、**R1 自体に 172.16.222.254 を持つインタフェースがない**ので効果がない。 [file:1]

- 試験向けの覚え方フレーズ

  - 「**“R2 からは見えるが、EIGRP で他に広まっていないルート” → R2 で `redistribute static` を疑え**」  
  - 特に「Loopback（/32）に R2 からだけ届く」「SW はルーティング未実装」「R2 に static はありそう」というパターンが揃ったら、  
    - **“その static を EIGRP で再配布”＝R2(config-router)# redistribute static** と結びつけると覚えやすい。

***
QUESTION 541 解説

- 問題文（英語）:  
Traffic from R3 to the central site does not use alternate paths when R3 cannot reach 10.10.10.2. Traffic on R3 destined to R4 takes an alternate route via 10.10.10.6 when 10.10.10.4 is not accessible from R3. Which configuration switches traffic destined to 10.10.10.2 from R3 on the alternate path?

A. R3(config)#ip route 192.168.10.1 255.255.255.255 10.10.10.2 track 20  
B. R6(config)#ip route 10.10.10.3 255.255.255.255 10.0.0.30  
C. R3(config)#track 20 ip sla 20 reachability  
D. R2(config)#ip route 10.10.10.3 255.255.255.255 10.0.0.6  

Correct Answer: A [web:25][web:26][web:29][web:30][web:32]

- 問題文要約（日本語）:  
- 中央サイト（192.168.10.1）へのトラフィックは、R3 から通常 R2（10.10.10.2）経由で行く。  
- R3 から R4 宛てのトラフィックは、10.10.10.4 がダウンしたとき **自動的に 10.10.10.6 経由の代替パスに切り替わる** ように IP SLA＋track＋バックアップルートが設定済み。  
- しかし、**中央サイト宛てトラフィックは、10.10.10.2 に到達できなくなっても代替パスに切り替わらない**。  
どの設定を入れれば、10.10.10.2 に到達不能時に中央サイト向けトラフィックが代替パスへ切り替わるかを問う問題。

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 中央サイト 192.168.10.1 宛てのルーティングは、R3 でおそらく次のように構成されている。 [web:25][web:26]  
    - プライマリ: `ip route 192.168.10.1 255.255.255.255 10.10.10.2`  
    - バックアップ（フローティング）: `ip route 192.168.10.1 255.255.255.255 10.10.10.6 <大きいAD>`  
  - R3 では 10.10.10.2 の到達性を監視する IP SLA 20 がすでに存在し、これを `track 20` で参照する設計になっている。 [web:25][web:26][web:30][web:32]
  - しかし現状は、プライマリルートに track が紐付いていないため、**10.10.10.2 に到達できなくなっても、そのスタティックルートは “生きたまま” になってしまい、バックアップルートに切り替わらない**。 [web:25][web:26][web:32]
  - そこで必要なのが、選択肢 A のようにプライマリルートに track を関連付ける設定。  
    - `R3(config)#ip route 192.168.10.1 255.255.255.255 10.10.10.2 track 20`  
    - こうすると、`track 20`（＝IP SLA 20 の reachability）が down になった瞬間、このスタティックルートが RIB から削除され、**AD の大きいバックアップルート（10.10.10.6 経由）が自動的に利用される**。 [web:25][web:26][web:32]

  - C の `track 20 ip sla 20 reachability` は、もし track オブジェクト自体が未定義なら必要だが、問題の前提では IP SLA 20 と track は既に設定済みとして扱われている（R4 向けのフェイルオーバが動いている）ため、決定打は「プライマリ静的ルートへの track 紐付け」である。 [web:25][web:26]
  - B, D は R6 や R2 上の別経路設定であり、「R3 から中央サイトへの切り替えロジック」には直接関係しない。

- 試験向けの覚え方フレーズ

  - 「**IP SLA で監視 → “track” オブジェクト → その track を `ip route ... track <id>` に必ず紐付ける**」  
  - “プライマリ＋バックアップ静的ルート” 問題で  
    - IP SLA と track はあるのに切り替わらない → **プライマリルートに track が付いてない**  
    と見抜き、**`ip route <dst> <mask> <primary-nhop> track <id>`** を探す、と覚えておくと A を即決しやすい。

***
QUESTION 542 解説

- 問題文（英語）:  
The IT manager received reports from users about slow applications through network x. Which action resolves the issue?

A. Upgrade the IOS on router E.  
B. Move the servers into the users subnet.  
C. Increase the bandwidth from the service provider.  
D. Use the variance 2 command to enable load balancing.  

Correct Answer: D [web:40][web:41][web:42][web:44][web:47][web:50]

- 問題文要約（日本語）:  
ユーザから「**network x 経由のアプリケーションが遅い**」という報告が来ている。  
EIGRP ベースのネットワークで、network x へ到達する複数経路が存在する構成（うち 1 本だけが現在使用されている）で、**どの対応をすれば帯域を有効活用して遅延を改善できるか** を問う問題。

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 背景となる典型構成: [web:40][web:41][web:47]  
    - Rtr から network x への EIGRP 経路が 2 本以上存在するが、  
      - メトリックが異なるため、**現在は最良メトリックの 1 経路のみが利用されている**。  
      - 他の経路もそこまで悪くないものの、EIGRP のデフォルトでは equal-cost のみロードバランス対象。
  - EIGRP では **variance コマンド** を使うことで「unequal-cost load balancing（不等コストロードバランス）」が可能になる。 [web:46][web:49][web:52]  
    - `variance N` で、「**ベストメトリック × N 以下** のメトリックを持つ経路をロードバランス対象としてインストール」できる。  
    - `variance 2` とすると、「ベストの 2 倍までのメトリックを持つ経路」も同時に使うようになる。
  - 問題の文脈では、network x への複数経路が存在し、**1 本だけにトラフィックが集中して遅くなっている**のがボトルネック。  
    - そのため、`variance 2` を設定して **複数の EIGRP 経路を同時に使用し、実効帯域を増やす** ことが最も直接的な解決策になる。 [web:46][web:49]
  - A の IOS アップグレードや B のサーバ移設は、問題の根本（network x への帯域・経路利用）には触れていない。  
  - C の回線増速も理屈としては改善しうるが、**今ある複数経路を使い切っていないのに先に増速するのは設計として非効率**であり、試験問題の意図とも異なる。

- 試験向けの覚え方フレーズ

  - 「**EIGRP で “遅い＆複数経路あり” → まず variance で不等コストロードバランス**」  
  - 具体的には、  
    - “network x が遅い”＋“複数パスが図に描いてある”＋“EIGRP”  
    → **`variance N`（多くは 2）で複数パスを使う** と結び付けておくと、選択肢 D を自信を持って選べる。

***
QUESTION 543 解説

- 問題文（英語）:  
What is a limitation of IPv6 RA Guard?

A. It does not offer protection in environments where IPv6 traffic is tunneled.  
B. It cannot be configured on a switch port interface in the ingress direction.  
C. Packets that are dropped by IPv6 RA Guard cannot be spanned.  
D. It is not supported in hardware when TCAM is programmed.  

Correct Answer: A [web:55][web:56][web:58][web:61][web:63][web:64][web:66]

- 問題文要約（日本語）:  
**IPv6 RA Guard（Router Advertisement Guard）の制限事項** として正しいものはどれか、を問う問題。

- 正解: **A**

- 解説（なぜ A が正しいか）

  - Cisco の公式ドキュメント「IPv6 RA Guard」の **Restrictions** セクションに、はっきり次のように記載されている。 [web:56][web:58][web:64][web:66]  
    - 「**The IPv6 RA Guard feature does not offer protection in environments where IPv6 traffic is tunneled.**」  
    - つまり、IPv6 トラフィックが GRE, IPsec, 6to4 などでトンネルされている場合、その中に含まれる RA は L2 で見えないため、RA Guard では検査・ブロックできない。
  - 同じドキュメントには、他の選択肢に関する正しい動作も明記されている。 [web:56][web:58][web:64][web:66]  
    - 「This feature can be configured on a switch port interface in the ingress direction.」  
      → B は誤り（**Ingress 方向に設定可能**）。  
    - 「Packets dropped by the IPv6 RA Guard feature can be spanned.」  
      → C は誤り（**ドロップされたパケットも SPAN 可能**）。  
    - 「This feature is supported only in hardware when the TCAM is programmed.」  
      → D の “not supported in hardware” は誤りで、**むしろ TCAM にプログラムされたハードウェアでのみサポート**。
  - よって、「トンネル環境では保護を提供しない」という A が、IPv6 RA Guard の正しい制限事項となる。 [web:55][web:61][web:69]

- 試験向けの覚え方フレーズ

  - 「**RA Guard は L2 で “見える RA” しか守れない → トンネルの中身は見えない**」  
  - 問題文に “IPv6 RA Guard limitation / restriction” と出てきたら、  
    - **“does not offer protection when IPv6 is tunneled”＝A** をセットで思い出すと確実。

***
QUESTION 544 解説

- 問題文（英語）:  
How is the LDP router ID used in an MPLS network?

A. The force keyword changes the router ID to the specified address without causing any impact.  
B. The loopback with the highest IP address is selected as the router ID.  
C. The MPLS LDP router ID must match the IGP router ID.  
D. If not configured, the operational physical interface is chosen as the router ID even if a loopback is configured.  

Correct Answer: B [web:74][web:76][web:78][web:80][web:82]

- 問題文要約（日本語）:  
MPLS ネットワークにおいて、**LDP（Label Distribution Protocol）の router ID がどのように決定されるか** を問う問題。

- 正解: **B**

- 解説（なぜ B が正しいか）

  - Cisco の LDP ドキュメントでは、LDP Router ID の決定手順を次のように定義している。 [web:74][web:78][web:80][web:82]  
    1. まず、`mpls ldp router-id` で明示的に指定されていれば、そのインタフェースの IP アドレスが LDP Router ID になる。  
    2. そうでない場合、**すべての稼働中インタフェースの IP を確認し、その中にループバックがあれば “ループバックの中で最も大きい IP アドレス” を LDP Router ID として選ぶ**。  
    3. ループバックがなければ、「稼働中の物理インタフェースの中で最も大きい IP アドレス」を選ぶ。
  - 従って、「The loopback with the highest IP address is selected as the router ID」という選択肢 B が、デフォルト動作として正しい説明。 [web:76][web:78][web:82]

  - A の `force` キーワードについては、Cisco のドキュメントに、  
    - `mpls ldp router-id <interface> force` を使うと、**即座に LDP Router ID を変更し、既存の LDP セッションをすべて張り直し、関連するラベルバインディングと MPLS 転送を一時中断する** と明記されている。 [web:77][web:79][web:81][web:83]  
    - したがって「without causing any impact」は誤り。  
  - C は「LDP Router ID は IGP Router ID と一致しなければならない」と書いているが、**推奨ではあっても必須条件ではない**。実際には別でも動作する。 [web:75][web:82]  
  - D は「ループバックがあっても物理 IF が選ばれる」と言っており、B の説明（“ループバックがあればそれを優先”）と真っ向から矛盾するため誤り。 [web:78][web:82]

- 試験向けの覚え方フレーズ

  - 「**LDP Router ID = ①手動設定が最優先 → ②なければ “一番大きい Loopback IP” → ③なければ “一番大きい物理 IP”**」  
  - 選択肢で「highest loopback IP」と来たら B を選ぶ、  
    - “force はセッション張り直しで影響大” とセットで覚えておくと A を切りやすい。

***
QUESTION 545 解説

- 問題文（英語）:  
Which set of commands restore reachability to loopback0?

A. interface loopback0  
   ip address 4.4.4.4 255.255.255.0  
   ip ospf network point-to-point  

B. interface loopback0  
   ip address 4.4.4.4 255.255.255.0  
   ip ospf interface area 10  

C. interface loopback0  
   ip address 4.4.4.4 255.255.255.0  
   ip ospf network broadcast  

D. interface loopback0  
   ip address 4.4.4.4 255.255.255.0  
   ip ospf interface type network  

Correct Answer: A [web:87][web:88][web:90][web:91][web:93][web:95][web:96][web:98][web:99][web:100]

- 問題文要約（日本語）:  
Loopback0 に 4.4.4.4/24 が設定されているものの、他ルータから 4.4.4.4/24 ネットワークへの到達性に問題がある。  
**どの設定を入れると loopback0 への到達性が回復するか** を問う問題。

- 正解: **A**

- 解説（なぜ A が正しいか）

  - OSPF では、**ループバックインタフェースはデフォルトで “network type loopback”** として扱われる。 [web:88][web:90][web:96]  
    - そのため、インタフェースに 4.4.4.4/24 を設定していても、OSPF が LSDB に広告するのは **4.4.4.4/32（ホストルート）** になる。  
    - 結果として、他ルータから「4.4.4.4/24 ネットワーク」へのルート期待と実際の広告内容がズレてしまい、到達性の問題が発生するケースがある。
  - この “ループバックは /32 で広告される” 挙動を変更したい場合、Cisco では loopback インタフェースに対して  
    - `ip ospf network point-to-point`  
    を設定することで、**ループバックを “point-to-point ネットワーク” として扱い、設定したマスク（/24）でそのまま広告させる** ことができる。 [web:87][web:88][web:91][web:93][web:96][web:99][web:100]
  - よって、選択肢 A の設定により、4.4.4.4/24 が正しく OSPF で広報され、他ルータからの到達性が回復する。

  - B, D の `ip ospf interface ...` というコマンドは IOS に存在しないため誤り。 [web:93][web:99]  
  - C の `ip ospf network broadcast` は loopback のデフォルトネットワークタイプを “broadcast” に変えるが、  
    - 通常 loopback には隣接ルータがおらず、/32 広告の挙動を変えないため、到達性問題の解決にはならない。 [web:88][web:96]

- 試験向けの覚え方フレーズ

  - 「**OSPF の loopback はデフォ /32。マスク通りに広告したければ “ip ospf network point-to-point”**」  
  - “loopback に /24 つけてるのに /32 でしか見えない／到達できない” 系の問題が出たら、  
    - **loopback に `ip ospf network point-to-point` を入れる**＝選択肢 A、と機械的に結び付けられるようにしておくと良い。

***
QUESTION 546 解説

- 問題文（英語）:  
The EIGRP neighborship between the routers is up. However, the app servers are unreachable from the client PC. Which action must the engineer take to resolve the issue?

A. Configure the network 10.1.1.0 command in the EIGRP protocol on R1 only  
B. Configure the neighbor 10.1.2.0 FastEthernet 0/0 command in the EIGRP protocol on R2 only  
C. Configure the no auto-summary command in the EIGRP protocol on both routers  
D. Configure the default network 10.1.2.0 0.0.0.255 command in the EIGRP protocol on R2 only  

Correct Answer: C [web:101][web:102][web:103][web:105][web:109][web:111][web:113][web:115]

- 問題文要約（日本語）:  
ルータ間の EIGRP ネイバーは正常に確立されているのに、**クライアント PC からアプリサーバ（network 10.1.1.0/24 と 10.1.2.0/24 側）に到達できない**。  
このとき、**どの設定を行えばアプリサーバへの疎通が回復するか** を問う問題。

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 解説によると、両ルータの EIGRP 設定で **`auto-summary` が有効** になっている。  
    - その結果、10.1.1.0/24 や 10.1.2.0/24 のようなサブネットは、EIGRP 更新時に **クラスフル境界（10.0.0.0/8）に自動サマリされて広告** される。 [web:102][web:105][web:109][web:113]
  - さらに、EIGRP はサマリルート 10.0.0.0/8 に対して **Null0 向きのスタティックルート（AD 5）を自動生成**する。 [web:101][web:103][web:105]  
    - これにより、「10.0.0.0/8 の中で自ルータが具体的なサブネットを知らない宛先」については、**Null0（破棄）に送ってループを防ぐ** 挙動になる。  
  - このネットワークで 10.1.1.0/24 や 10.1.2.0/24 が互いに離れた場所（discontiguous）にあると、  
    - 片側ルータももう片側ルータも「10.0.0.0/8 を自分でまとめて Null0 に持っている」状態になるため、  
    - 結果として、一方のサブネット宛てトラフィックが **誤って Null0 にルーティングされ、アプリサーバへ届かない**。 [web:103][web:105][web:109][web:115]
  - これを解決するには、**両方のルータで `no auto-summary` を設定し、自動クラスフルサマリと Null0 サマリルートを無効化**する必要がある。  
    - そうすると、10.1.1.0/24 や 10.1.2.0/24 といった **本来のプレフィックス（サブネットマスク付き）がそのまま EIGRP で広告され、正しく相互到達できる** ようになる。 [web:101][web:103][web:105][web:109]

  - A の `network 10.1.1.0` は既に設定済みであるケースが多く、「ネイバーは up だが到達不可」という症状とは合致しにくい。  
  - B の neighbor ステートメントは、マルチポイントやマルチキャストが使えない特殊トポロジで使うものであり、現状ネイバー関係は正常なので不要。  
  - D の `default network 10.1.2.0` は、今回の “10.0.0.0/8 サマリと Null0 によるドロップ” 問題の根本解決にはならない。

- 試験向けの覚え方フレーズ

  - 「**EIGRP でネイバーは up だが 10.x サブネットへ届かない＋routing table に 10.0.0.0/8 to Null0 が見えたら、まず `no auto-summary`**」  
  - “auto-summary が Null0 サマリを作って正しいサブネット到達性を壊す” パターンをイメージし、  
    - 両ルータで **`no auto-summary`** を入れる＝選択肢 C、と紐付けておくと良い。

***
QUESTION 547 解説

- 問題文（英語）:  
Which action resolves the issue?

A. Upgrade the hardware to increase the interface input buffers  
B. Implement traffic policing to prevent the interface input traffic being exceeded  
C. Resolve the misconfigured QoS parameters  
D. Replace the faulty cable  

Correct Answer: D [web:117][web:118]

- 問題文要約（日本語）:  
あるインタフェースで入力エラーが発生しており、`show interface` の出力を見ると **input errors がすべて runts（極端に小さいフレーム）** になっている。  
この問題を解決するにはどの対応が適切か、を問う問題。

- 正解: **D**

- 解説（なぜ D が正しいか）

  - Cisco のトラブルシューティングガイドや実務事例では、**大量の runts は物理層の問題を示す典型的な指標**とされる。 [web:117][web:118]  
    - Runt: イーサネットの最小フレームサイズ（64 バイト）未満のフレーム。  
    - 主な原因:  
      - duplex 不一致（衝突）  
      - 不良ケーブル・コネクタ、ハードウェア故障（NIC、ポートなど）
  - 解説文でも「**全ての input errors が runts であり、これは collision や duplex mismatch、あるいはハードウェア／ケーブル故障によって起こる**」と説明されている。  
    - この中で、試験の選択肢として最もストレートかつ具体的な対処が「Replace the faulty cable」。  
    - Duplex 設定ミスなどが前提として除外されているケースが多く、**物理メディアの交換が最も適切な回答**となる。 [web:117][web:118]
  - A の「input buffer 増強」や B の「policing」、C の「QoS 調整」は、帯域超過やキュー溢れによるドロップには有効だが、**フレームサイズ異常（runts）という症状とは直結しない**。

- 試験向けの覚え方フレーズ

  - 「**runts 多発 → まず Layer1 を疑え（ケーブル or duplex mismatch）**」  
  - 選択肢に “replace faulty cable” があれば、  
    - “大量 runts ＋他の論理的問題の記述なし” の問題では **D を優先的に選ぶ**、と覚えておくとよい。

***
QUESTION 548 解説

- 問題文（英語）:  
The engineer is reviewing the logs on the DENVER router and notices that this error message repeats constantly:  
`Jun 12 13:42:03.399: %TCP-6-BADAUTH: No MD5 digest from 10.40.1.1(27174) to 10.40.1.2(179) tableid - 0`  
Which action resolves the issue?

A. Configure BGP authentication on the router with IP address 10.40.1.2  
B. Configure OSPF link authentication on the router with IP address 10.40.1.1  
C. Configure NTP authentication on the router with IP address 10.40.1.1  
D. Configure BGP authentication on the router with IP address 10.40.1.1  

Correct Answer: D [web:126][web:127][web:129][web:130][web:132][web:135]

- 問題文要約（日本語）:  
DENVER ルータのログに、  
`%TCP-6-BADAUTH: No MD5 digest from 10.40.1.1(27174) to 10.40.1.2(179)`  
というメッセージが延々と出続けている。  
どの設定を行えば、この問題（ログ＆接続不成立）を解決できるかを問う問題。

- 正解: **D**

- 解説（なぜ D が正しいか）

  - `%TCP-6-BADAUTH: No MD5 digest ... to x.x.x.x(179)` というログは、Cisco 公式ドキュメントでも **「BGP MD5 認証に関連する TCP エラー」** として解説されている。 [web:127][web:130][web:132][web:135]  
    - ポート 179 は BGP の TCP ポート。  
    - DENVER（10.40.1.2 側）が BGP ピアに対して MD5 認証を要求しているのに、  
      - 相手（10.40.1.1）から送られてくる TCP セッションには **MD5 digest（TCP オプションに載るハッシュ）が付いていない** 場合に出るログ。 [web:127][web:130][web:135]
  - つまり、  
    - 10.40.1.2（DENVER）側では BGP neighbor に `password`（MD5 認証）を設定済み。  
    - しかし 10.40.1.1 側の BGP neighbor では **MD5 認証が未設定** で、通常の TCP 接続を試みている。  
    - その結果、10.40.1.2 側で「No MD5 digest from 10.40.1.1」と警告が発生する。 [web:126][web:127][web:129]
  - この状況を解決するには、  
    - **10.40.1.1 側の BGP neighbor にも同じ MD5 パスワードを設定し、両端で BGP 認証を一致させる** 必要がある。  
    - したがって、選択肢 D の「Configure BGP authentication on the router with IP address 10.40.1.1」が正解となる。 [web:126][web:127][web:130]

  - A のように 10.40.1.2 側で BGP authentication を設定するのは既に行われている前提（だからこそ 10.40.1.2 側で BADAUTH が出ている）。  
  - B（OSPF 認証）や C（NTP 認証）はポート番号もプロトコルも違うため、今回のログ（TCP/179, BGP）には無関係。

- 試験向けの覚え方フレーズ

  - 「**`%TCP-6-BADAUTH`＋ポート 179 → BGP MD5 認証不一致 or 片側未設定**」  
  - ログに “No MD5 digest from A to B(179)” とあれば、  
    - **送信元側（A）の BGP neighbor で MD5 を設定していない** と読み、  
    - “Configure BGP authentication on A”＝今回なら 10.40.1.1（選択肢 D）を選ぶ、と覚えておくと良い。

***
QUESTION 549 解説

- 問題文（英語）:  
After an engineer configured a new Cisco router as a DHCP server, users reported two primary issues:  
- Devices in the HR subnet have intermittent connectivity problems  
- Workstations in the LEGAL subnet cannot obtain IP addresses  
Which configurations must the engineer apply to ROUTER_1 to restore connectivity for the affected devices? [web:141][web:142][web:143][web:144]

A. interface GigabitEthernet0/0.5  
   encapsulation dot1Q 5  
   ip address 192.168.5.10 255.255.255.0  
   ip helper-address 192.168.39.100  
   !  
   interface GigabitEthernet0/0.80  
   encapsulation dot1Q 80  
   ip address 192.168.80.10 255.255.255.128  
   ip helper-address 192.168.39.100  
   !  
   ip dhcp excluded-address 192.168.5.1 192.168.5.10  
   ip dhcp excluded-address 192.168.80.1 192.168.80.10  
   !  
   ip dhcp pool LEGAL  
   network 192.168.80.0 255.255.255.128  
   default-router 192.168.80.10  
   ip dhcp pool HR  
   network 192.168.5.0 255.255.255.0  
   default-router 192.168.5.10  

B. …  
C. …  
D. …  

Correct Answer: A [web:141][web:142][web:143][web:144]

- 問題文要約（日本語）:  
新しく ROUTER_1 を DHCP サーバとして設定したあと、  
- HR サブネットの端末は **ときどき接続が不安定になる（IP 競合や誤割り当てっぽい症状）**  
- LEGAL サブネットのワークステーションは **そもそも IP が取得できない**  
この 2 つの症状を同時に解決するために、ROUTER_1 にどの設定を入れるべきかを問う問題。 [web:141][web:143]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - HR サブネット側（VLAN 5: 192.168.5.0/24）について:  
    - ROUTER_1 のサブインタフェース G0/0.5 は 192.168.5.10 を持つデフォルトゲートウェイ。 [web:141][web:143]  
    - DHCP サーバとしては、**ゲートウェイ IP（192.168.5.10）やルータ自身が使う固定 IP を DHCP プールから除外**しておくべき。  
      - A は `ip dhcp excluded-address 192.168.5.1 192.168.5.10` とし、.1〜.10 を除外範囲にしている。  
      - これにより、**DHCP クライアントに 192.168.5.10 を配らないため、ルータ IP との競合による「断続的な接続問題」を防げる**。 [web:146][web:148][web:152]
  - LEGAL サブネット側（VLAN 80: 192.168.80.0/25）について:  
    - G0/0.80 は 192.168.80.10/25。  
    - DHCP プール LEGAL の network と default-router は 192.168.80.0/25 と 192.168.80.10 で正しく対応している。 [web:141][web:143]  
    - A のように `ip dhcp excluded-address 192.168.80.1 192.168.80.10` を設定することで、  
      - .1〜.10 を予約し、.10（ルータ IP）や将来的にサーバ用固定アドレスを確保できる。  
      - その上で DHCP は 192.168.80.11 以降をクライアントに配布できるようになるため、**“IP が取得できない” 状況を解消**できる。 [web:146][web:151][web:152]
  - また、両サブインタフェースとも `ip helper-address 192.168.39.100` が設定されており、  
    - これは「別サブネット上の DHCP サーバ（192.168.39.100）に Discover をフォワードする」ための正しい設定。 [web:147][web:150]  
    - 選択肢 C のように HR 側だけ 192.168.93.100 になっている案は誤りで、**LEGAL 側だけ DHCP が動く／HR 側は誤ったサーバを向く**という不整合を生む。 [web:141][web:142]

  - B は HR 側の excluded-address がなく、ゲートウェイアドレスをクライアントに配ってしまうリスクがあるため、「断続的な接続問題」が残る。 [web:141][web:143]  
  - D は HR 側の除外範囲（.1〜.5）が狭すぎ、.10（ルータ IP）がプールに含まれてしまううえ、LEGAL 側は /25 なのに .80.110 まで除外しており、利用可能アドレスをほぼ潰してしまう。  
  - したがって、**正しい helper-address と妥当な excluded-address を両 VLAN に設定している A が唯一の妥当な選択肢**となる。 [web:141][web:143][web:146]

- 試験向けの覚え方フレーズ

  - 「**DHCP で “一部サブネットだけ不安定 or 取れない” → まず `ip helper-address` と `ip dhcp excluded-address` を疑え**」  
  - 特に:  
    - ゲートウェイ IP を含む範囲は除外 (`excluded-address .1–.GW_IP`)  
    - すべての DHCP リレー用インタフェースで **同じ正しい helper-address（DHCP サーバ IP）** を向ける  
  - という 2 点を押さえておくと、選択肢 A のような構成を自然に選べる。

***
QUESTION 550 解説

- 問題文（英語）:  
What is a function of IPv6 ND inspection?

A. It learns and secures bindings for stateless autoconfiguration addresses in Layer 2 neighbor tables  
B. It learns and secures bindings for stateful autoconfiguration address in Layer 2 neighbor tables  
C. It learns and secures bindings for stateless autoconfiguration addresses in Layer 3 neighbor tables  
D. It learns and secures bindings for stateful autoconfiguration addresses in Layer 3 neighbor tables  

Correct Answer: A [web:156][web:157][web:159][web:160][web:164]

- 問題文要約（日本語）:  
**IPv6 ND inspection（Neighbor Discovery Inspection）の機能** として正しい説明はどれか、を問う問題。

- 正解: **A**

- 解説（なぜ A が正しいか）

  - Cisco 公式ドキュメント「IPv6 Neighbor Discovery Inspection」では、次のように明記されている。 [web:157][web:160]  
    - 「**IPv6 ND inspection learns and secures bindings for stateless autoconfiguration addresses in Layer 2 neighbor tables.**」  
    - IPv6 ND inspection は、Neighbor Discovery (NS/NA, RS/RA など) メッセージを解析して、  
      - IPv6 アドレス（SLAAC で構成されたものを含む）  
      - MAC アドレス  
      - VLAN / ポート  
      の対応関係（バインディング）を **レイヤ 2 の neighbor/binding テーブル** に学習し、信頼できないメッセージを破棄する機能。
  - これにより、  
    - DAD（重複アドレス検出）攻撃  
    - 偽の Neighbor Advertisement や Router Advertisement によるスプーフィング  
    - Neighbor cache 汚染  
    といった **NDP 周りの攻撃をレイヤ 2 レベルで緩和**する。 [web:157][web:159][web:160]

  - B, D の “stateful autoconfiguration” は DHCPv6 などに関連する概念であり、IPv6 ND inspection の定義とは異なる。  
  - C は “Layer 3 neighbor tables” としており、Cisco ドキュメントの「Layer 2 neighbor tables」という記述と食い違う。 [web:157][web:160]

- 試験向けの覚え方フレーズ

  - 「**IPv6 ND inspection = SLAAC（stateless）＋L2 バインディングテーブル**」  
  - “ND inspection の機能は？”と聞かれたら、  
    - **「stateless autoconfig アドレスを L2 の binding table に学習・保護」＝A**  
    とワンセットで覚えておくと確実。

***
QUESTION 551 解説

- 問題文（英語）:  
The default route from R1 must be withdrawn from the routing table if R1 cannot ping 10.1.1.1, but it is not working correctly. Which configuration resolves the issue? [web:165]

A.  
ip sla 2  
 icmp-echo 10.1.1.1  
 timeout 3000  
 threshold 1000  
 frequency 5  
!  
ip sla schedule 2 life forever start-time now  
!  
track 2  
 ip sla 200 reachability  
 delay down 30 up 180  
!  
ip route 0.0.0.0 0.0.0.0 10.1.1.1 track 2 250  

B.  
ip sla 2  
 icmp-echo 10.1.1.1  
 timeout 3000  
 threshold 1000  
 frequency 5  
!  
track 2 ip sla 2 reachability  
 delay down 30 up 180  
!  
ip route 0.0.0.0 0.0.0.0 10.1.1.1 track 2  

C.  
ip sla 2  
 icmp-echo 10.1.1.1  
 timeout 3000  
 threshold 1000  
 frequency 5  
!  
ip sla schedule 2 life forever start-time now  
!  
track 2 ip sla 2 reachability  
 delay down 30 up 180  
!  
ip route 0.0.0.0 0.0.0.0 10.1.1.1 track 2  

D.  
ip sla 2  
 icmp-echo 10.1.1.1  
 timeout 3000  
 threshold 1000  
 frequency 5  
!  
ip sla schedule 2 life forever start-time now  
!  
track 2  
 ip sla 200 reachability  
 delay down 30 up 180  
!  
ip route 0.0.0.0 0.0.0.0 10.1.1.1  

Correct Answer: C [web:165]

- 問題文要約（日本語）:  
R1 はデフォルトルート `0.0.0.0/0 → 10.1.1.1` を持っているが、**もし R1 から 10.1.1.1 に ping が届かなくなったら、そのデフォルトルートを RIB から自動的に削除したい**。  
IP SLA＋track を使ってそれを実現する設計だが、現状はうまく動いていない。どの設定なら正しく動作するか、を問う問題。 [web:165]

- 正解: **C**

- 解説（なぜ C が正しいか）

  正しく「到達不能時にデフォルトルートを落とす」ためには、次の 3 点が必須になる。 [web:165][web:174][web:177]

  1. **IP SLA オペレーションの定義**  
     - `ip sla 2`  
       - `icmp-echo 10.1.1.1`  
       - `timeout 3000`  
       - `threshold 1000`  
       - `frequency 5`  
     - これにより R1 が 10.1.1.1 へ 5 秒間隔で ICMP Echo を送り、到達性を監視。

  2. **IP SLA オペレーションのスケジューリング**  
     - `ip sla schedule 2 life forever start-time now`  
     - これを入れないと SLA 2 は起動せず、トラッキングも常に “unknown/up” になり、ルートが落ちない。 [web:174][web:177]

  3. **track オブジェクトで SLA を監視し、それを static route に紐付け**  
     - `track 2 ip sla 2 reachability`  
       - `delay down 30 up 180`  
     - `ip route 0.0.0.0 0.0.0.0 10.1.1.1 track 2`  
     - これにより、SLA 2 が 10.1.1.1 到達不能（down）と判断したら、  
       - track 2 が down  
       - それに紐付いたデフォルトルートが RIB から削除される。 [web:173][web:174][web:176][web:179]

  - 選択肢 C は、この 3 つを **すべて正しく** 実装している。 [web:165]

  - A, D: `track 2` が **`ip sla 200 reachability` を参照しており、SLA 番号の不一致**（実際に監視したいのは 2）。  
  - B: SLA 2 と track 2 の関連付けは正しいが、**`ip sla schedule 2 ...` がなく、SLA が起動しない**ため常に up と見なされてしまう。 [web:165][web:174]

- 試験向けの覚え方フレーズ

  - 「**IP SLA でルート制御 → ①SLA 定義 ②SLA schedule ③track ④`ip route ... track` の 4 点セット**」  
  - 特に “SLA 番号の不一致” と “schedule の入れ忘れ” は頻出トラップなので、  
    - **`track N ip sla N reachability` ＋ `ip sla schedule N ...`** が揃っている選択肢を選ぶ、と覚えておくと C を確実に選べる。

***
QUESTION 552 解説

- 問題文（英語）:  
An engineer implements uRPF to increase security and stop incoming spoofed IP packets. Some asymmetrically routed packets are also blocked after the configuration. Which command resolves the issue?

A. ip verify unicast source reachable-via any  
B. ip verify unicast source reachable-via rx  
C. ip verify unicast reverse-path  
D. ip verify unicast reverse-path any  

Correct Answer: A [web:184][web:186][web:187][web:189][web:190][web:193]

- 問題文要約（日本語）:  
uRPF（Unicast Reverse Path Forwarding）を有効化してスプーフィングを防いだところ、**非対称ルーティングのトラフィックまでブロックされてしまった**。  
この状況を解消しつつ、スプーフィング対策は続けたい。どのコマンドを使うべきかを問う問題。

- 正解: **A**

- 解説（なぜ A が正しいか）

  - uRPF には主に 2 つのモードがある。 [web:184][web:186][web:190][web:193]  
    - **Strict mode（厳格モード）**  
      - 受信パケットの **送信元 IP に対する逆向きルートの「出力インタフェース」と、実際にパケットを受信したインタフェースが一致しているか** をチェック。  
      - Cisco IOS では `ip verify unicast source reachable-via rx` や旧コマンド `ip verify unicast reverse-path` で設定。 [web:185][web:187][web:192]  
      - 非対称ルーティング（行きと帰りの経路が別のインタフェース）の場合、**正当なパケットでも “別インタフェースから来た” と見なされてドロップされやすい**。
    - **Loose mode（ルーズモード）**  
      - 送信元 IP に対する **ルートが FIB に存在するかどうかだけ** をチェックし、「どのインタフェースから来たか」は問わない。 [web:184][web:186][web:188][web:189]  
      - Cisco IOS では `ip verify unicast source reachable-via any` で設定。 [web:185][web:187][web:189][web:193]  
      - これにより、**非対称ルーティングでも、経路さえあれば正当パケットは許可**される一方、ルーティングテーブルに存在しないスプーフされた送信元アドレスはドロップされる。
  - 問題の症状は「**asymmetrically routed packets がブロックされている**」なので、  
    - 現在は strict モード（`reachable-via rx` または `ip verify unicast reverse-path`）で動いていると考えられる。 [web:184][web:186][web:190][web:192]
  - これを解消するには、**uRPF を loose モードに変更して非対称ルーティングを許容**させる必要がある。  
    - そのためのコマンドが選択肢 A の `ip verify unicast source reachable-via any`。 [web:184][web:186][web:187][web:189][web:193]

  - B と C は strict モードの動きに相当し、非対称ルーティングの問題を解消できない。 [web:185][web:187][web:192]  
  - D は Cisco IOS では一般的に使われない表記であり、正しいコマンドは A の形式。

- 試験向けの覚え方フレーズ

  - 「**uRPF strict = rx / reverse-path → 非対称ルートは落ちる**」  
  - 「**uRPF loose = reachable-via any → インタフェース不問で、ルート有無だけチェック**」  
  - “asymmetric routing も通したい” という文言が出たら、  
    - **`ip verify unicast source reachable-via any`（loose モード）＝A** を即答できるようにしておくと良い。

***
QUESTION 553 解説

- 問題文（英語）:  
How do devices operate in MPLS L3VPN topology?

A. CE routers connect to the provider network and perform LSP functionality  
B. P and associated PE routers with IGP populate the VRF table in different VPNs  
C. P routers support PE to PE VPN tunnel without LSP functionality  
D. P routers provide connectivity between PE devices with MPLS switching  

Correct Answer: D [web:195][web:197][web:198][web:199][web:201][web:205][web:209]

- 問題文要約（日本語）:  
MPLS L3VPN トポロジにおいて、**各種ルータ（CE／PE／P）がどのような役割で動作しているか** を問う問題。  
特に、P ルータの役割として正しい説明はどれか。

- 正解: **D**

- 解説（なぜ D が正しいか）

  - Cisco や他ベンダの MPLS L3VPN 解説では、デバイスの役割を次のように定義している。 [web:197][web:198][web:199][web:201][web:205][web:207][web:209]  
    - **CE（Customer Edge）ルータ**  
      - 顧客ネットワークのエッジルータ。  
      - Provider Edge（PE）と IGP/BGP/Static などでルーティング情報を交換する。  
      - **LSP（Label Switched Path）の構築・MPLS スイッチングは行わない**。  
    - **PE（Provider Edge）ルータ**  
      - サービスプロバイダ側のエッジルータ。CE と接続し、各 VPN 用の VRF を持つ。  
      - CE から学習した経路を VRF に格納し、MP-BGP で他 PE と **VPNv4/VPNv6 ルート＋VPN ラベル** をやり取り。  
      - MPLS コア向けの **外側のトランスポートラベル（LSP ラベル）** も push/pop する。 [web:197][web:199][web:201][web:208]
    - **P（Provider）ルータ**  
      - プロバイダネットワークのコアルータ。  
      - CE とは直接接続せず、**VPN の VRF や VPNv4 ルートを保持しない**。  
      - **MPLS ラベルスイッチングのみを行い、PE 間の LSP 上をトランジットする LSR（Label Switching Router）として機能**する。 [web:197][web:198][web:199][web:201][web:205][web:209]
  - したがって、「P routers provide connectivity between PE devices with MPLS switching」という D が、  
    - P ルータの役割（**MPLS スイッチングで PE 間のコアパスを提供**）を正しく表している。 [web:197][web:198][web:199][web:201][web:205][web:209]

  - A: CE は **LSP 機能を行わず**、MPLS を知らない単なる IP ルータでもよい。LSP の構築／ラベルスイッチングは P/PE 側の仕事。 [web:197][web:199][web:201]  
  - B: VRF テーブルをポピュレートするのは **PE だけ** であり、P ルータは VRF を持たず VPN ルートを知らない。 [web:197][web:198][web:199][web:201][web:208]  
  - C: 「P routers support PE to PE VPN tunnel without LSP functionality」は逆で、P ルータはまさに **LSP（外側ラベル）のスイッチングを行う LSR**。LSP 機能なしでは MPLS コアが成立しない。 [web:198][web:201][web:205][web:209]

- 試験向けの覚え方フレーズ

  - 「**PE = VRF＋MP-BGP＋VPN ラベル。P = コアでラベルスワップのみ。CE = MPLS を知らなくても OK**」  
  - “MPLS L3VPN トポロジで P の役割は？” と来たら、  
    - **“PE 間を MPLS switching でつなぐコア（LSR）”＝D** を即答できるようにしておくとよい。

***
QUESTION 554 解説

- 問題文（英語）:  
A CoPP policy is implemented to allow specific control traffic, but the traffic is not matching as expected and is getting unexpected behavior of control traffic. Which action resolves the issue? [web:215][web:216][web:218][web:222][web:223]

A. Use match-any instruction in class-map.  
B. Create a separate class map against each ACL.  
C. Create a separate class map for ICMP traffic.  
D. Use default-class to match ICMP traffic.  

Correct Answer: A [web:215][web:216][web:218][web:222][web:223]

- 問題文要約（日本語）:  
特定の制御プレーントラフィック（BGP, ICMP, EIGRP など）を許可する CoPP（Control Plane Policing）ポリシーを作成したが、**期待どおりにマッチせず、制御トラフィックが想定外の挙動（ドロップなど）になっている**。  
どの設定変更を行えば、狙った制御トラフィックを正しく CoPP クラスにマッチさせられるかを問う問題。

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 問題の前提となる CoPP 設定（典型パターン）は次のようなイメージ。 [web:215][web:218][web:222][web:223]  
    ```text
    class-map CoPP-CLASS
      match access-group name BGP-ACL
      match access-group name ICMP-ACL
      match access-group name EIGRP-ACL
    ```
  - ここで **class-map のデフォルトは `match-all`（AND 条件）** である。  
    - つまり上記のように複数の `match access-group` を書くと、  
      - 「BGP-ACL にもマッチし、かつ ICMP-ACL にもマッチし、かつ EIGRP-ACL にもマッチするパケット」だけがこのクラスに入る。  
    - しかし、1 つのパケットが **同時に BGP かつ ICMP かつ EIGRP** であることはあり得ないため、  
      - 実質的に **どのパケットもマッチしないクラスマップ** になってしまう。 [web:212][web:223]
  - 本来やりたいのは、「**BGP か ICMP か EIGRP のいずれかに該当する制御トラフィックをまとめてこのクラスに入れる**」ことなので、  
    - class-map を `match-any`（OR 条件）にする必要がある。  
    - `class-map match-any CoPP-CLASS`  
      - BGP-ACL / ICMP-ACL / EIGRP-ACL の **どれか 1 つにマッチすれば** このクラスに分類されるようになる。 [web:212][web:215][web:218][web:222]
  - よって、「Use match-any instruction in class-map」を選ぶ A が正解。

  - B のように ACL ごとに別クラスを作る方法も技術的には可能だが、問題文の “unexpected behavior” の直接原因は **`match-all` による「全部 AND 条件」** であり、それを変えれば解決する。  
  - C, D は ICMP だけを個別扱いする話や default-class に流す話で、根本のマッチ条件ロジック（AND/OR）を解消しない。 [web:215][web:223]

- 試験向けの覚え方フレーズ

  - 「**QoS/CoPP の class-map はデフォルトが match-all → 複数 ACL を並べるなら match-any に変える**」  
  - 特に “CoPP で BGP・ICMP・EIGRP を 1 クラスにまとめたい” という問題が出たら、  
    - **`class-map match-any` にする＝選択肢 A** を真っ先に疑うようにしておくと良い。

***
QUESTION 555 解説

- 問題文（英語）:  
Which tag is used by the PE router to forward the packet to the correct customer?

A. Extended-community  
B. VNI  
C. RT  
D. RD  

Correct Answer: B

- 問題文要約（日本語）:  
マルチテナント環境において、PE ルータがパケットを「**どの顧客（テナント）に属するトラフィックか**」を識別して、正しい顧客側 VRF／ネットワークへ転送する際に使用する **タグ** はどれか、を問う問題。  
（ここでは VXLAN/EVPN ベースのマルチテナント設計を前提にしている。） [web:228][web:230][web:234]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - VXLAN/EVPN ベースのマルチテナント構成では、**VNI（Virtual Network Identifier）** が「どの仮想ネットワーク／テナントか」を識別するタグとして使われる。 [web:228][web:230][web:234]  
    - Layer-2 VNI: L2 セグメント（VLAN 相当）を識別。  
    - Layer-3 VNI（VRF VNI）: VRF ごとに一意の L3 VNI が割り当てられ、**テナントの L3 ネットワークを一意に表す ID** になる。  
  - PE（あるいは VXLAN VTEP）では、受信した VXLAN カプセル化パケットの **VNI を見て、どの VRF／テナントに属するトラフィックかを判断し、その VRF 内のルーティングに基づいて CE/サーバ側に転送**する。 [web:228][web:229][web:230][web:234]  
  - したがって、「PE がパケットを正しい customer にフォワードする際に用いるタグ」としては **VNI** が最も直接的に該当する。  

  - 他の選択肢は主に「BGP ルート属性としての識別／制御」に使われ、**データプレーン上の “転送タグ” ではない**。 [web:227][web:229][web:232]
    - RD（Route Distinguisher）:  
      - VPNv4 アドレスを一意にするためのタグで、**BGP コントロールプレーン上で経路を識別する**目的。実際のパケット転送には付かない。 [web:227][web:229]  
    - RT（Route Target, extended community の一種）:  
      - VPN ルートを「どの VRF にインポート／エクスポートするか」を制御する BGP extended community。  
      - これも **経路制御用タグであり、データプレーンのフレーム／パケット上に載る識別子ではない**。 [web:227][web:229][web:232]  
    - Extended-community:  
      - RT を含む各種 BGP 拡張属性の総称で、あくまで BGP 経路属性。

- 試験向けの覚え方フレーズ

  - 「**RD/RT/extended-community = BGP コントロールプレーンの“経路識別・制御タグ”**」  
  - 「**実際のパケットをどのテナントに届けるか決める “データプレーン側タグ” は VxLAN なら VNI**」  
  - “forward the packet to the correct customer” と書かれていて VXLAN/EVPN 文脈が匂ったら、  
    - **VNI = データパスでのテナント識別タグ** → 選択肢 B、と結び付けると覚えやすい。

***
QUESTION 556 解説

- 問題文（英語）:  
A network engineer must configure an EIGRP stub router at a site that advertises only connected and summary routes. Which configuration performs this task?

A. router eigrp 100  
   eigrp stub connected  

B. router eigrp 100  
   eigrp stub  

C. router eigrp 100  
   eigrp stub summary  

D. router eigrp 100  
   eigrp stub redistribute  

Correct Answer: B [web:239][web:242][web:243][web:244][web:245][web:248][web:251]

- 問題文要約（日本語）:  
「**stub サイトのルータを EIGRP stub として設定し、そのルータからは “直結ルートとサマリルートだけ” を上位に広告したい**」という要件。  
どの `eigrp stub` 設定を入れれば、この要件を満たすかを問う問題。

- 正解: **B**

- 解説（なぜ B が正しいか）

  - Cisco 公式ドキュメントおよび複数の技術記事によれば、`eigrp stub` コマンドの **デフォルト動作** は次の通り。 [web:239][web:242][web:244][web:245][web:248][web:251]  
    - 単に `eigrp stub` と入力した場合、  
      - 内部的には **`eigrp stub connected summary`** が設定されるのと同じ扱い。  
      - すなわち、stub ルータは **直結ルート（connected）とサマリルート（summary）だけ** を上位に広告する。  
    - 実際の running-config を見ると、`eigrp stub` だけ入れても `eigrp stub connected summary` と表示されるケースが多い。 [web:239][web:244][web:245]
  - 質問の要件は「**connected + summary のみ広告**」なので、  
    - **何もキーワードを付けない `eigrp stub`（選択肢 B）がそのまま要件を満たす**。  

  - A の `eigrp stub connected` は「直結ルートのみ広告」であり、summary は含まれない。 [web:239][web:242][web:241]  
  - C の `eigrp stub summary` は「サマリルートのみ広告」で、connected が含まれない。  
  - D の `eigrp stub redistribute` は「再配送ルートのみ広告」であり、connected / summary は含まれず要件と異なる。 [web:239][web:242][web:248]

- 試験向けの覚え方フレーズ

  - 「**`eigrp stub` 単体 = `connected`＋`summary`（デフォルト）**」  
  - “stub ルータで connected と summary だけ出したい” と来たら、  
    - 変にキーワードを足さず **`eigrp stub` だけ入れる＝選択肢 B** と覚えておくとラク。

***
QUESTION 557 解説

- 問題文（英語）:  
Which configuration resolves the IP SLA issue from R1 to the server?

A. R6(config)#ip sla responder  
B. R6(config)#ip sla 650  
   R6(config-ip-sla)#udp-jitter 10.60.60.6  
C. R6(config)#ip sla responder udp-echo ipaddress 10.60.60.6 po 5000  
D. R6(config)#ip sla schedule 10 life forever start-time now  

Correct Answer: A [web:253][web:259][web:261][web:263][web:265][web:267]

- 問題文要約（日本語）:  
R1 からサーバ（R6 側）への IP SLA 監視がうまく動作しておらず、`show ip sla summary` の Return Code が **“No connection”** となっている状況。  
この「接続できない」状態を解消して、R1 の IP SLA からサーバへの計測を正常に行うには、R6 側でどの設定を入れるべきかを問う問題。 [web:253][web:259][web:261][web:263][web:265][web:267]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - IP SLA の「アクティブ側」は R1 であり、R1 からサーバ（R6）に対して UDP echo などの **IP SLA probe** を投げている。  
  - `show ip sla summary` の Return Code が **No connection** の場合、**宛先側で IP SLA Responder が動作しておらず、制御メッセージに応答していない** ことが典型的な原因となる。 [web:258][web:260]  
  - そのため、**R6 側で IP SLA Responder を有効化する必要がある**。  
    - グローバルコンフィグで `ip sla responder` とするだけで、R6 は IP SLA Responder として機能し、R1 からの SLA コントロールメッセージとプローブに対応できるようになる。 [web:258][web:260]

  - B のような `ip sla 650` は **アクティブ側 IP SLA の設定（ソース側）** であり、R6 で行うべきものではない。  
  - C のような細かい responder パラメータ指定は、一部ドキュメントやツールでは紹介されるが、この問題の前提では **単純に responder が動いていないことが原因** なので、基本の `ip sla responder` だけで十分。 [web:258]  
  - D の `ip sla schedule` は IP SLA オペレーションのスケジュール設定であり、これも **アクティブ側（R1）で使うコマンド** であって、Responder 不在の問題は解決しない。  

- 試験向けの覚え方フレーズ

  - 「**Return Code: No connection → 先（宛先）で Responder がいない**」  
  - “IP SLA issue from R1 to the server” で Return Code が No connection ときたら、  
    - **「ゴール側で `ip sla responder` を入れる」＝選択肢 A** とワンセットで覚えておく。

***
QUESTION 558 解説

- 問題文（英語）:  
Jitter on the link between R101 and R201 was tested for voice traffic over port 16384 without the control communication on port 1967. Which command enables R201 to receive RTT for the configured IP SLA?

A. ip sla responder tcp-connect port 1967  
B. ip sla responder udp-echo ipaddress 2.2.2.2 port 16384  
C. ip sla responder udp-echo ipaddress 1.1.1.1 port 16384  
D. ip sla responder auto-register 1.1.1.1  

Correct Answer: B [file:268]

- 問題文要約（日本語）:  
R101 で IP SLA を使って、音声用 UDP ポート 16384 向けのジッタ計測を行っているが、**制御通信ポート 1967 を無効（control disable）にしている** 状態。  
このとき、R201 側で **RTT を正しく取得できるようにするには、どの IP SLA responder 設定が必要か** を問う問題。 [file:268]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 通常の IP SLA（control enabled）では、プローブ送信側が制御チャネル（デフォルト UDP 1967）で responder とネゴシエーションを行い、宛先 IP やポートを自動的にやり取りする。  
  - しかし、この問題では **「without the control communication on port 1967」＝ control を無効化** しているため、  
    - 宛先側（R201）では、**事前に “どの IP・どのポート宛の SLA パケットに応答するか” を固定で設定** しておく必要がある。 [file:268]
  - 問題の前提では、R101 の IP SLA は「udp-echo を 2.2.2.2:16384 宛に送信、control disable」という設定になっている。 [file:268]  
    - したがって R201 では、**2.2.2.2 の 16384 宛てに来る udp-echo に応答する responder** を明示する必要がある。  
    - それにぴったり一致するコマンドが  
      - `ip sla responder udp-echo ipaddress 2.2.2.2 port 16384`（選択肢 B）。 [file:268]

  - A: `tcp-connect port 1967` は TCP かつ 1967 の話であり、今回の **UDP 16384 のジッタ計測** とは関係ない。  
  - C: 同じ 16384 port だが IP アドレスが **1.1.1.1** で、R101 のプローブ宛先 2.2.2.2 と一致しないため、応答しない。 [file:268]  
  - D: `auto-register` は control チャネルを使う前提の自動登録系であり、そもそも「control なし」の前提を満たさない。  

- 試験向けの覚え方フレーズ

  - 「**IP SLA control disable → 宛先側で『ip sla responder udp-echo IP port』をピンポイント指定**」  
  - 問題文に “control communication disabled / No control / port 1967 なし” と出たら、  
    - **プローブの宛先 IP＋ポートをそのまま responder に書く＝選択肢 B タイプ** とセットで覚えると解きやすい。

***
QUESTION 559 解説

- 問題文（英語）:  
An engineer recently implemented uRPF by configuring the ip verify unicast source reachable-via rx command on interface gi0/3. The engineer noticed right after implementing uRPF that an inbound packet on the gi0/3 interface with a source address of 172.16.3.251 was dropped. Which action resolves the issue?

A. Configure uRPF loose mode to forward the packet.  
B. Permit the 172.16.3.251 in the inbound ACL on interface gi0/3.  
C. Remove inbound ACL from the interface gi0/3 to allow 172.16.3.251.  
D. Configure uRPF strict mode to forward the packet.  

Correct Answer: A [file:268]

- 問題文要約（日本語）:  
インターフェース gi0/3 に **uRPF strict（reachable-via rx）** を設定したところ、gi0/3 宛に入ってくるパケット（送信元 172.16.3.251）がドロップされるようになった。  
このパケットをドロップさせずに転送したい場合、どの設定変更を行うべきかを問う問題。 [file:268]

- 正解: **A**

- 解説（なぜ A が「模範解答」扱いか）

  - `ip verify unicast source reachable-via rx` は **uRPF strict モード** で、  
    - 「送信元 IP に対する戻り経路が “同じインターフェース” に向いている場合のみ許可」  
    - 戻り経路のインターフェースが異なる、あるいはルートが存在しない場合は uRPF によりドロップされる。  
  - 問題文のケースでは、gi0/3 に入ってきた 172.16.3.251 送信元のパケットが strict uRPF によりドロップされている。  
  - スプーフィング対策のため uRPF は残しつつ、**非対称ルーティングやルート設計の都合で strict が成り立たない場合に “緩める” 一般的な対処** が  
    - `ip verify unicast source reachable-via any`（= loose モード）へ切り替えること。  
    - loose モードでは「単にルーティングテーブルにその送信元への経路があればよい（インターフェースは問わない）」ため、今回のようなドロップを改善できる可能性がある。 [file:268]

  - 一方、この PDF の注釈でも指摘されている通り、  
    - 実際の出力では 172.16.3.252/30 の対応インターフェースは g0/2 であり、  
    - 172.16.3.251 は、そのネットワークや他のプレフィックスにもマッチしておらず、**ルーティングテーブル上に到達ルートがない**。  
    - ルートがなければ loose モードでも “reachable” とは判定されないため、**実際には uRPF loose に変えてもこのパケットは通らない**、というのが Remarks 部分の指摘。 [file:268]  
  - ただし選択肢の中で、uRPF が原因のドロップを「一般論として」緩和する設定は A しかないため、**試験用の模範解答として A が選ばれている**。 [file:268]

  - B/C: ACL をいじっても、uRPF のチェックで NG であれば結局ドロップされるため根本解決ではない。  
  - D: すでに strict（rx）を使っている状況で、「strict にしろ」という選択肢は論理的に破綻している。  

- 試験向けの覚え方フレーズ

  - 「**uRPF strict = ‘同じ IF に戻り経路があること’ が条件 → 非対称ルーティングで落ちることがある**」  
  - 「**“asymmetry で uRPF に殺される” 系の問題が出たら、loose（reachable-via any）で緩める＝選択肢 A** を第一候補にする」。  
  - 実務では「そもそも正しいルートがあるか」も必ず確認する、とセットで覚えておくと応用が利く。

***
QUESTION 560 解説

- 問題文（英語）:  
An engineer recently modified the configuration for area 3 to accept only type 1, 2, and 3 LSAs. Immediately after the changes, users connected to router R6 began to report connectivity issues. Which configuration restores connectivity to R6 and meets the requirement?

A.  
B.  
C.  
D.  

Correct Answer: D [file:268]

- 問題文要約（日本語）:  
エンジニアが OSPF の **エリア 3 で「LSA タイプ 1・2・3 のみを受け入れる」ように設定変更**したところ、その直後から R6 配下のユーザが疎通不可になった。  
エリア 3 で「タイプ 1・2・3 のみ」という要件を満たしつつ、R6 配下のユーザの到達性を回復するには、どのエリア設定が正しいかを問う問題。 [file:268]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - LSA タイプごとの役割は次の通り。  
    - Type 1,2: エリア内のルータ・ネットワーク LSAs（内部トポロジ情報）。  
    - Type 3: ABR による **他エリア宛プレフィックスのサマリ LSA**。  
    - Type 4,5: ASBR 経由の **外部経路**（EIGRP など他プロトコルや外部ネットワーク）。  
  - 「エリア 3 で type 1,2,3 のみ受け入れる」という要件は、**OSPF の stub エリア** で満たせる。  
    - stub エリアは **type 1,2,3 のみを通し、type 5（外部 LSA）を遮断** する。  
    - さらに ABR から **デフォルトルートを自動生成** し、stub 内のルータ（R6 など）は、このデフォルトルート経由で外部ネットワーク（例: R8 側）に到達できる。 [file:268]
  - この問題では、エリア 3 を stub にした際に **R6 と R19 のエリアタイプ設定が不一致** となったため、R6 がエリア 3 の OSPF に正常参加できず、結果として疎通が失われた。 [file:268]  
  - D の構成だけが  
    - R6 と R19 の両方を **同じエリア 3 の stub タイプ** で揃える  
    - かつ「type 1,2,3 のみ許可（= stub）」という要件を満たす設定になっている。 [file:268]  
  - 備考にある通り、stub エリアでは type 1,2,3 しか流れない一方で、**ABR が 0.0.0.0/0 を注入**するため、R6 配下のホストも R8 側の外部ネットワークへはこのデフォルト経路でアクセスできる。 [file:268]

- 試験向けの覚え方フレーズ

  - 「**LSA 1・2・3 だけ許可したい → 普通の stub エリア**」  
  - 「エリア内ルータ同士（例: R6 と R19）は **同じエリアタイプを必ず両端で揃える**」  
  - 問題文に “area X accept only type 1,2,3 LSAs” と出たら  
    - **“area X stub を両端に”＝選択肢 D タイプ** をまず疑う、というパターンで覚えるとよい。

***
QUESTION 561 解説

- 問題文（英語）:  
Which routing protocol is used by the PE router to advertise routes to a CE router without redistribution or static after removing the RD tag from the P router?

A. OSPF  
B. BGP IPv4  
C. IS-IS  
D. MP-BGP  

Correct Answer: B [web:269][web:270][web:271][web:272][web:273]

- 問題文要約（日本語）:  
MPLS L3VPN 環境で、P ルータから RD タグを取り除いたあとでも、**PE ルータが CE ルータへルートを広告するのに “再配送やスタティックを使わずに済む” ルーティングプロトコルはどれか** を問う問題。  
要するに、「PE–CE 間でそのまま使うことで、MP-BGP 側の VPNv4 ルートを VRF の IPv4 ルートへシームレスに結びつけられるプロトコルは何か」という意図。 [web:269][web:272][web:282]

- 正解: **B**

- 解説（なぜ BGP IPv4 が正しいか）

  - MPLS L3VPN では、**PE–PE 間は MP-BGP（VPNv4 / VPNv6 アドレスファミリ）** を用いて VPN ルートをやり取りする。 [web:280][web:282]  
  - 各 PE の VRF 内では、**通常の IPv4 BGP（BGP IPv4 AFI/SAFI）を PE–CE プロトコルとして使うことができる**。  
    - VRF 内の BGP IPv4 ルートは、PE 上で **自動的に VPNv4 ルートに変換され MP-BGP によって他 PE に配布**される。 [web:280][web:282]  
    - 逆方向も同様で、MP-BGP で受け取った VPNv4 ルートは RD/RT 処理後に該当 VRF の **IPv4 BGP ルートとしてインストール**され、そのまま CE 向け BGP セッションで広告できる。  
  - このため、**PE–CE 間で BGP IPv4 を使っていれば、OSPF/RIP などへの再配送やスタティックルートを使わなくても CE に経路を渡せる**。 [web:275][web:277][web:278]

  - D の MP-BGP はあくまで **PE–PE 間の VPNv4/VPNv6 経路交換** 用であり、通常 CE とは MP-BGP ではなく IPv4 BGP セッションを張る（CE は VPNv4 を理解しない）。 [web:280][web:282]  
  - A の OSPF や C の IS-IS を PE–CE に使う場合は、**MP-BGP（VPNv4）との間で再配送が必要**になるので、問題文の「without redistribution or static」という条件に合致しない。 [web:275][web:277][web:282]

- 試験向けの覚え方フレーズ

  - 「**PE–PE 間：MP-BGP（VPNv4）／PE–CE 間：BGP IPv4**」  
  - 「**‘再配送なしで CE まで届けたい’ と来たら、PE–CE も BGP（IPv4）にしておけば OK → 選択肢 B**」  
  - 逆に、PE–CE が OSPF／EIGRP／RIP などの場合は **必ず BGP との再配送が必要**、とセットで覚えると整理しやすい。

***
QUESTION 562 解説

- 問題文（英語）:  
A network administrator configured NetFlow data but the data is not visible at the NetFlow collector. Which configuration allows the router to send the records?

A. Configure a different interface to send the records.  
B. Configure the management interface in the global routing table to send the records.  
C. Configure the NetFlow collector to listen at export-protocol netflow-v5.  
D. Rectify NetFlow collector reachability from the management interface.  

Correct Answer: A [web:286][web:288][web:289][web:294]

- 問題文要約（日本語）:  
管理者が NetFlow（Flexible NetFlow）を設定したが、**NetFlow コレクタ側でフローデータが見えない**。  
このとき、**どの設定変更を行えばルータから NetFlow レコードを送信できるようになるか** を問う問題。 [web:289][web:294][web:100]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 問題の前提（本来の図や注釈）では、**NetFlow exporter の source に管理インターフェース（GigabitEthernet0/0 などの “Ethernet management port”）を指定**している。 [web:289][web:294]  
  - Cisco のドキュメントでは、**Flexible NetFlow のエクスポートは Ethernet 管理ポート（Gi0/0 など）ではサポートされない** と明記されている。 [web:286][web:288]  
    - 例: 「Flexible NetFlow export is not supported on the Ethernet management port, Gi0/0 / GigabitEthernet 0/0.」 [web:286][web:288]  
  - したがって、**管理ポートを source にしている限り、どれだけ reachability やコレクタ設定を直しても NetFlow レコード自体が送信されない**。  
  - この制約を回避するには、  
    - exporter の `source` を **通常の L3 インターフェース（例: ループバックや別の物理インターフェース）に変更** する必要がある。  
    - それに該当する選択肢が **「Configure a different interface to send the records.」＝ A**。 [web:286][web:288][web:289]

  - B/D は「管理インターフェースでの到達性」を直す方向だが、**そもそも管理インターフェースでは Flexible NetFlow エクスポートがサポートされない**ため、根本解決にならない。 [web:286][web:288]  
  - C はエクスポートプロトコル（v5/v9/IPFIX）の話であり、今回の “管理ポート非対応” の制約とは無関係。 [web:285][web:287]

- 試験向けの覚え方フレーズ

  - 「**管理ポート（Gi0/0 の “MGMT”）では Flexible NetFlow export は不可**」  
  - NetFlow/Telemetry 系の問題で  
    - “source GigabitEthernet0/0（管理ポート）” ＋ “データが見えない”  
    - という組み合わせが出たら、  
      - **source を別の L3 IF（Loopback や通常インターフェース）に変える＝選択肢 A** と結び付ける。

***
QUESTION 563 解説

- 問題文（英語）:  
An engineer is troubleshooting a routing loop on the network to reach the 172.16.3.0/16 from the OSPF domain. Which configuration on router R1 resolves the issue?

A.  
B.  
C.  
D.  

Correct Answer: D [file:268]

- 問題文要約（日本語）:  
OSPF ドメインからネットワーク **172.16.3.0（問題文では /16 と書いてあるが実際の図では 172.16.3.0/30）** への到達で「ルーティングループが発生している」とされている。  
OSPF と EIGRP が両方動作している環境で、このループを止めるために **R1 上でどのような再配送制御（route-map 等）を入れるべきか** を問う問題。 [file:268]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 解説の前提となる典型パターンは、**OSPF と EIGRP の相互再配送がループを生むケース**。  
    - あるルート（ここでは 172.16.0.0/16 と想定）が  
      1. もともと EIGRP 外部ルート（AD 170）として存在  
      2. ある PE/ASBR で OSPF に再配送され、OSPF E2（AD 110）として全体に広まる  
      3. 別の場所で **その OSPF E2 を再度 EIGRP に再配送** すると、「元の EIGRP 外部ルート」と「OSPF から再配送された EIGRP 外部ルート」が混在  
      4. AD/メトリックの組み合わせ次第で、元ルートが塗り替えられ、ループの原因になる [file:268]
  - この問題の解説でも、**「External EIGRP → OSPF → External EIGRP」 の往復再配送がループ原因** と明示されている。  
    - したがって対処は、**“OSPF 側から渡ってきたその問題のプレフィックスを、再び EIGRP へ戻さないようにする”** こと。  
  - 正解 D の構成は、R1 の EIGRP 再配送部分で  
    - route-map＋ACL 等を使って **該当プレフィックス（172.16.0.0/16）をマッチして deny**  
    - かつ **最後に permit any（seq 20 など）を入れて、それ以外の OSPF ルートは通常どおり EIGRP へ再配送**  
    という形になっており、  
    - 「問題のプレフィックスだけを OSPF→EIGRP 方向の再配送から除外する」という、教科書的なループ対策になっている。 [file:268]

  - 解説にもある通り、他の選択肢にも「OSPF から EIGRP への再配送をブロックする案」はあるが、  
    - **permit any が無く、該当プレフィックスだけでなく “他の正常な OSPF ルートも全部再配送されなくなる”** ため誤り。 [file:268]

  - 備考として、この問題自体にはいくつか不整合があることも指摘されている。 [file:268]  
    - 設問文は 172.16.3.0/16 と書かれているが、コマンドは 172.16.0.0/16 を対象にしており、図は 172.16.3.0/30。  
    - traceroute 出力からは実際にはループしている様子が見えない。  
    - 元の EIGRP 外部ルートの広告元が問題文に明示されていない。  
  - それでも、「**OSPF ↔ EIGRP 相互再配送で特定プレフィックスだけループ → route-map でそのプレフィックスの再配送を止める**」という解決パターンとして、D が模範解答に選ばれている。 [file:268]

- 試験向けの覚え方フレーズ

  - 「**OSPF と EIGRP の相互再配送ループ → “行き” ではなく “戻り側” の再配送で止める**」  
  - 具体的には：  
    - 「**外部 EIGRP → OSPF → もう一度 EIGRP**」  
    - が見えたら、  
      - **R1 側の “OSPF→EIGRP redistribute” に route-map を付けて、その問題プレフィックスだけ deny＋最後に permit any**  
      - という構成（= 選択肢 D タイプ）を第一候補にする。

***
QUESTION 564 解説

- 問題文（英語）:  
An engineer notices a connectivity problem between routers R1 and R2. The frequency of this problem is high during peak business hours. Which action resolves the issue?

A. Increase the available bandwidth between R1 and R2.  
B. Decrease the EIGRP keepalive and hold down timers on R1 and R2.  
C. Increase the MTU on the interfaces that connect R1 and R2.  
D. Set static EIGRP neighborship between R1 and R2.  

Correct Answer: A [file:268]

- 問題文要約（日本語）:  
R1 と R2 間で **疎通問題が発生しており、特に業務ピーク時間帯に頻発**している状況。  
EIGRP を使っている前提で、この問題を解消する最も適切なアクションはどれかを問う問題。 [file:268]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 解説では、この症状は **EIGRP の “Stuck In Active（SIA）”** によるものと説明されている。  
    - EIGRP では、ルートの再計算時に Query を投げ、その **Reply を規定時間内に受け取れないと SIA** とみなして隣接関係をリセットする。  
    - ネットワークが混雑しているとき（＝ピーク時間帯）に、**Query/Reply パケットが輻輳でドロップされると SIA が発生しやすい**。 [file:268]
  - 今回の前提は「ピーク時にだけ頻繁に問題が出る」ため、  
    - 根本原因は **帯域不足によるリンク輻輳で、EIGRP の制御パケットが落ちている** 可能性が高い。  
  - よって、**R1–R2 間の利用可能帯域を増やし、輻輳自体を緩和する A** が、もっとも直接的で妥当な対処となる。 [file:268]

  - B: keepalive/hold timer を短くすると、かえって「リンク切れ判定」が早まり、不安定さが増すだけで根本的な輻輳は解決しない。  
  - C: MTU を増やしても輻輳や SIA の根本原因にはならず、逆にフラグメントや MTU ミスマッチのリスクを増やすだけ。  
  - D: static neighbor にしても、**Query/Reply 自体がドロップされる状況**は変わらないため、SIA を防げない。  

- 試験向けの覚え方フレーズ

  - 「**EIGRP SIA（Stuck In Active）＝Query の Reply が返ってこない → 輻輳で制御パケットが落ちていることが多い**」  
  - 問題文に “**peak business hours / busy network / congestion**” といったワードが出てきて EIGRP 隣接問題が絡んでいたら、  
    - **リンク帯域の増強や QoS で制御トラフィックを守る → 今回は「帯域を増やす」＝選択肢 A** を第一候補にする。

***
QUESTION 565 解説

- 問題文（英語）:  
The engineer configured route redistribution in the network but soon received reports that R2 cannot access 192.168.7.0/24 and 192.168.15.0/24 subnets. Which configuration resolves the issue?

A.  
B.  
C.  
D.  

Correct Answer: B [file:268]

- 問題文要約（日本語）:  
EIGRP → OSPF への再配送に **route-map＋ACL でフィルタ** をかけているネットワークで、R2 から **192.168.7.0/24 と 192.168.15.0/24 に到達できない** というトラブルが発生。  
どの設定を入れれば、この 2 つのプレフィックスも OSPF 側に再配送されて R2 から到達できるようになるか、を問う問題。 [file:268]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - もともと、このネットワークでは「EIGRP から OSPF への再配送」をする際に、**特定のネットワークだけを route-map＋ACL で許可している**。  
    - そのため、**ACL にマッチしないプレフィックスは OSPF 側に一切再配送されない**。 [file:268]
  - R2 から到達できない 192.168.7.0/24 と 192.168.15.0/24 も、まさに **ACL でマッチできていないから OSPF に乗らず、結果として R2 から見えない** という状態。 [file:268]
  - 正解の B の構成は、ACL に以下の 2 行を含めており、**この 2 つのサブネットを “まとめて” 許可できる点がポイント**。 [file:268]  
    - `192.168.4.0  0.0.3.255` → 192.168.4.0〜192.168.7.255（192.168.7.0/24 を含む）  
    - `192.168.12.0 0.0.3.255` → 192.168.12.0〜192.168.15.255（192.168.15.0/24 を含む）  
  - 他の選択肢は、いずれか一方しかカバーできなかったり、全く範囲がずれており、**両方のサブネットを同時にマッチできない** ため誤り。 [file:268]

  - 備考として、元問題の ACL には `192.168.0.0 0.0.7.255`（192.168.0.0〜7.255）という行もあり、本来であれば 192.168.7.0/24 は既にマッチしているはず、という矛盾も指摘されている。  
    - そのため、「実際の挙動」と「問題文の設定」が完全には一致していないが、**“2 つのサブネットを wildcard でまとめて許可する” という意図を汲んだ回答が B** という扱いになっている。 [file:268]

- 試験向けの覚え方フレーズ

  - 「**再配送で特定サブネットだけ見えない時 → route-map が噛んでいる ACL の “wildcard 範囲” を疑え**」  
  - 今回のように  
    - “192.168.4〜7 全部” をまとめる → `192.168.4.0 0.0.3.255`  
    - “192.168.12〜15 全部”


***
QUESTION 566 解説

- 問題文（英語）:  
Router R1 (head office) is not always preferred for all users to access hosting services. R1 should be primary, and the DR site should only be used if router R1 or its uplink fails.  
Which configuration resolves the issue?

A.  
B.  
C.  
D.  

Correct Answer: A [file:268]

- 問題文要約（日本語）:  
本社側ルータ **R1** と DR サイト側ルータがどちらも OSPF で **デフォルトルートを配布**しているが、本来「ホスティングサービスへのアクセスは R1（本社）をプライマリにしたい」のに、**常に R1 経由になるとは限らず DR 側に流れてしまうケースがある**。  
R1 をホスティングサービス向けの優先パスにし、R1 またはその上流が故障したときのみ DR サイトを使わせるための設定はどれか、という問題。 [file:268]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - OSPF で **default-information originate** を使ってデフォルトルートを流す場合、**特に metric / metric-type を指定しないと E2（cost=1）で広告される**。  
    - この “E2 cost 1” はどのルータで生成しても同じなので、**R1 と DR で全く同じコストのデフォルトルート** が広まり、  
      - 結果として、内部ルータから見た「R1 経由」と「DR 経由」の優先度が明確に分かれず、望まない経路選択が起こりうる。 [file:268]
  - 本来であれば、**R1 側の default-information originate に metric や metric-type（E1/E2）を付けて、DR 側より“重い／軽い”を調整する**のがベスト。  
    - しかし、この問題の選択肢には、**default-information originate のオプションを直接いじるものが存在しない**。 [file:268]
  - そのため、解説では「残った選択肢のうち、**default-information originate と redistribute static を組み合わせる構成（＝A）を、前提付きで推奨解答とする**」という扱いになっている。 [file:268]

  - 具体的には、次のような前提を追加して初めて A が“動く”構成になる。 [file:268]  
    - R1・DR 両方に、ホスティングサービス向けの **静的ルート（例: 209.165.200.0/22）** を追加  
    - それを OSPF に **redistribute static** することで、  
      - R1 側ではこのプレフィックスが **E1 で再配送される**（metric が累積される）  
      - DR 側では同じプレフィックスが **E2 として扱われる**  
    - すると、**内部ルータは E1（R1 経由）を E2（DR 経由）より優先**するため、  
      - ホスティングサービス宛のトラフィックは R1 をプライマリにし、  
      - R1 またはその uplink が落ちたときのみ DR 側パスを使う動きになる。  

  - なお、**redistribute static ではデフォルトルート（0.0.0.0/0）は OSPF に再配送されない**ため、  
    - インターネット全体向けの「デフォルトルート」については、依然として各ルータが **default-information originate で同じ E2 デフォルトを流している状態**となる。  
    - よって、「**ホスティングサービス向けは R1 優先にできるが、インターネット全般では R1 が常に優先になるわけではない**」という制限が残る点も、解説で指摘されている。 [file:268]

- 試験向けの覚え方フレーズ

  - 「**OSPF で “本社を優先” したいのに default-information originate(E2 cost=1) を両方に入れるだけでは差がつかない**」  
  - 選択肢に metric / metric-type 付き default-information originate が無い場合の“苦肉の策”として、  
    - **“特定プレフィックス（例: ホスティングセグメント）だけ static＋redistribute static で E1/E2 差を付けて優先度を変える”**  
  - ＝「**ホスティングは E1 の本社、その他は E2 で DR もあり得る**」というパターンをセットで覚えておくと、  
    - 類似問題で “default-information originate のオプションが無い” ケースでも対応しやすい。

***
QUESTION 567 解説

- 問題文（英語）:  
Which control plane process allows the MPLS forwarding state to recover when a secondary RP takes over from a failed primary RP?

A. LDP uses SSO to recover from disruption in control plane service.  
B. MP-BGP uses control plane services for label prefix bindings in the MPLS forwarding table.  
C. FEC uses a control plane service to distribute information between primary and secondary processors.  
D. LSP uses NSF to recover from disruption in control plane service.  

Correct Answer: D [file:268]

- 問題文要約（日本語）:  
MPLS 環境で、**プライマリ RP（Route Processor）が故障してセカンダリ RP にフェイルオーバしたとき**にも、**MPLS の転送状態（ラベル転送状態）を維持・復元できる制御プレーン機能はどれか**、を問う問題。 [file:268]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - キーワードは **SSO（Stateful Switchover）＋ NSF（Non-Stop Forwarding）＋ LDP NSF**。  
  - 解説では、**「LDP NSF により、RP がサービス中断から復旧しても MPLS forwarding state を失わない」** と説明されている。  
    - SSO イベントで LDP セッションが一時的に切れても、**バックアップ RP が MPLS 転送状態（LFIB）を保持**し、  
    - LDP 再スタート後に隣接 LSR と再同期することで、**ラベル情報と MPLS 転送状態を復元**する。 [file:268]
  - 選択肢 D の「**LSP uses NSF**」は表現としてやや粗いが、  
    - 実際に RP 障害時も **LSP（ラベルスイッチパス）の転送を継続・復旧させる仕組みが NSF＋LDP NSF** である、という意図から D が正解にされている。 [file:268]

  - 他の選択肢が誤りである理由（ポイントだけ）
    - A: SSO そのものは制御プレーンの冗長化機能だが、「**LDP が SSO を使う**」という言い方は不正確で、**転送状態維持の主役は LDP NSF＋NSF**。  
    - B: MP-BGP は VPNv4/VPNv6 ルートとラベルを配布するが、**RP フェイルオーバ時の MPLS 転送状態そのものを直接保護する仕組みではない**。  
    - C: FEC は「Forwarding Equivalence Class」という概念であって、**制御プレーンプロセスそのものではない**。  

- 試験向けの覚え方フレーズ

  - **「MPLS＋RP フェイルオーバ＋転送継続」＝ “LDP NSF + NSF” を連想して D を選ぶ。**  
  - 文面に「**secondary RP / failed primary / recover MPLS forwarding state / SSO / restart**」などが出てきたら、  
    - **“LDP NSF が LSP の転送継続を支える” → LSP＋NSF を答える** と紐づけて覚えると取りやすい。

***
QUESTION 568 解説

- 問題文（英語）:  
What is a MPLS PHP label operation?

A. Downstream node signals to remove the label.  
B. It uses implicit-NULL for traffic congestion from source to destination forwarding.  
C. PE removes the outer label before sending to the P router.  
D. It improves P router performance by not performing multiple label lookup.  

Correct Answer: A [file:268]

- 問題文要約（日本語）:  
MPLS の **PHP（Penultimate Hop Popping）** が具体的に何をしているかを聞く問題。  
どの説明が PHP ラベル操作を最も正しく表しているかを選ぶ。 [file:268]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - PHP では、**下流側ノード（通常は PE）が “implicit-null” ラベルを下流ラベルとして上流ノード（P）に通知**する。  
    - これにより、**上流の P ルータは「最後の 1 ホップ手前でラベルを POP してから PE に転送せよ」と指示される**。  
    - 解説でも「**downstream PE router が implicit-NULL を使って upstream P router に “ラベルを外してから送れ” と知らせる。これが MPLS PHP operation**」と明記されている。 [file:268]
  - この動作の結果として、**PE ルータはラベルの無い純粋な IP パケットだけを処理すればよくなり、ラベルルックアップ負荷が減る**ため、パフォーマンス向上に寄与する。 [file:268]

  - 他の選択肢が微妙・誤りな点
    - B: implicit-null を使うのは事実だが、「**輻輳対策（traffic congestion）」という説明は不正確**。目的は主に **終端側（PE）の負荷軽減**。  
    - C: PHP でラベルを外すのは **P → PE の “手前の P”** であり、「PE が P に送る前に外す」わけではない。  
    - D: 結果として **PE のラベルルックアップ負荷を減らす**のは正しいが、“PHP そのものの定義”としては A の **“downstream node が remove をシグナルする”** の方が直接的で正確。  

- 試験向けの覚え方フレーズ

  - 「**PHP = Penultimate Hop Popping = 最後のひとつ前（P）が POP する**」  
  - キーワード連想:
    - **“downstream PE が implicit-null を広告 → upstream P がラベル POP”**  
  - 選択肢に **“downstream node signals to remove the label”** とあれば、**それが PHP** と即断してよい。

***
QUESTION 569 解説

- 問題文（英語）:  
An engineer configures router A to mark all inside to outside traffic from network 192.168.1.0, except from host 192.168.1.1, with critical IP precedence. The policy did not work as expected. Which configuration resolves the issue?

A.  
B.  
C.  
D.  

Correct Answer: D [file:268]

- 問題文要約（日本語）:  
ルータ A で、**192.168.1.0/24 → 外向きの全トラフィックのうち、ホスト 192.168.1.1 だけを除外して、それ以外を “critical IP Precedence” でマーキングしたい**。  
しかし、設定したポリシー（route-map＋ACL）が期待通り動かない。どの構成にすれば、**192.168.1.1 以外の 192.168.1.0/24 トラフィックだけをマークできるか**を問う問題。 [file:268]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - ポイントは **“どこに permit を書くか” は route-map ではなく ACL 側** という点。  
    - route-map 自体は、**最後に空の permit ステートメントを入れなくても、マッチしなかったトラフィックは “普通にルーティングされる”**。  
    - したがって、**route-map の末尾に “permit だけの行” を無理に書く必要はない**。 [file:268]
  - 一方、**「192.168.1.1 だけ除外し、それ以外の 192.168.1.0/24 を IP Precedence critical でマークする」** ためには、ACL で次のようなロジックにする必要がある。 [file:268]  
    1. `deny host 192.168.1.1`  
    2. `permit any`（本来は `permit any` だが、問題文中では誤って `permit any any` と書かれている旨の注意あり）  
  - 正解 D の構成は、この **「先に除外ホストを deny、続いて残り全部を permit」** という ACL の形をとり、その ACL を route-map でマッチさせて `set ip precedence critical` をかける形になっている。  
    - これにより、**192.168.1.1 だけはマークされず通常転送、同サブネットの他ホストからの外向きトラフィックのみが critical にマークされる**。 [file:268]

  - 逆に、route-map 側に「permit any」を追加しても、**ACL で 192.168.1.1 以外を正しく permit できていなければ、そもそもマーク対象に乗ってこない**。  
    - この問題の肝は、「**マッチ条件の抜け（ACL の書き方）が原因で、望むトラフィックが route-map にヒットしていない**」点にある。 [file:268]

- 試験向けの覚え方フレーズ

  - 「**“特定ホストだけ QoS 除外” は ACL で deny → その後に permit any を必ず入れる**」  
  - route-map の末尾に **permit any** は不要（マッチしなかったトラフィックは「普通にルーティング」される）ので、  
    - **“除外ロジックは ACL 側で完結させる”** と覚えておくと、  
    - 「ポリシーは当たっているのに、一部ホストだけ除外されない」系の問題で迷いにくくなる。

***
QUESTION 570 解説

- 問題文（英語）:  
EIGRP adjacency between router A and router C is not working as expected. Which two configurations resolve the issue? (Choose two.)

A.  
B.  
C.  
D.  
E.  

Correct Answer: C, D [file:268]

- 問題文要約（日本語）:  
Router A と Router C の **EIGRP ピアが確立しない原因**を問う問題で、  
どの 2 つの設定を修正すれば **EIGRP adjacency を正常に張れるか**を選ぶ。 [file:268]

- 正解: **C と D**

- 解説（なぜ C・D が正しいか）

  解説に書かれているポイントは次の 2 つ。 [file:268]

  1. **同一 AS 番号の設定**  
     - EIGRP では、**両ルータで同じ Autonomous System（AS）番号を使っていないと隣接は絶対に張れない**。  
     - 片側が `router eigrp 100` でも、もう一方が `router eigrp 1` など別 AS ならネイバーは上がらない。  
     - 選択肢 C は、Router A / C の **EIGRP AS 番号を一致させる設定**になっている。  

  2. **network コマンドで正しいインタフェースを EIGRP に参加させる**  
     - EIGRP adjacency は、**ネイバー間リンクのインタフェース IP が network ステートメントでカバーされていないと張れない**。  
     - つまり、物理的に接続されていても、そのインタフェースが EIGRP プロセスに入っていなければ Hello も出ない。  
     - 選択肢 D は、Router A / C 側で **対向との接続インタフェースを含むアドレス範囲を network コマンドに追加する設定**になっている。  

  以上 2 点を満たす C・D を適用すると、  
  - **AS 番号一致**,  
  - **対象リンクのインタフェースが EIGRP 有効**,  
  となり、Router A と Router C の EIGRP adjacency が成立する。 [file:268]

- 試験向けの覚え方フレーズ

  - **「EIGRP ネイバーが上がらない → “AS 番号” と “network コマンド” をまず疑え」**  
  - キーワードで覚えるなら:
    - **“AS mismatch ＝ 絶対に張れない”**  
    - **“network 抜け ＝ そもそも Hello が飛ばない”**  
  - 問題文に “adjacency not working” が出たら、  
    - **AS と network の 2 点を修正する選択肢（= 本問では C・D）** を探すクセをつけると取りこぼしにくい。

***
QUESTION 571 解説

- 問題文（英語）:  
HostA and HostB cannot receive IP addresses from the DHCP server. The switches are configured with DHCP snooping. Which configuration on SW3 resolves the issue?

A. ip dhcp relay information option-insert  
B. no ip dhcp snooping information option  
C. ip dhcp server use subscriber-id client-id  
D. ip helper-address 1.0.0.2  

Correct Answer: B [file:268]

- 問題文要約（日本語）:  
HostA / HostB が DHCP サーバから IP アドレスを取得できない。  
スイッチ群では **DHCP snooping が有効**になっている。  
このとき、SW3 にどの設定を入れればクライアントが正常にアドレス取得できるか。 [file:268]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - DHCP snooping を有効にすると、スイッチは **Option 82（relay information option）を DHCP パケットに挿入**できる。  
    - 一部の DHCP サーバや中間機器が、この Option 82 情報を理解できない／許可していない場合、  
      **サーバ側で DHCPDISCOVER / DHCPREQUEST が破棄され、クライアントはアドレス取得できなくなる**。 [file:268]
  - `no ip dhcp snooping information option` は、  
    - **スイッチに Option 82 を挿入させない**（= relay information を付けない）グローバル設定。  
    - これにより、DHCP パケットは **通常のクライアント→サーバの形で通過**し、サーバ側で正しく処理されるようになる。 [file:268]
  - 本問のように「DHCP snooping は有効、でもクライアントがアドレスを取れない」「中継やサーバ側の対応が読めない」状況では、  
    - **“Option 82 を止める” = `no ip dhcp snooping information option`** が最も直接的かつ典型的な解決策になる。 [file:268]

  - 他の選択肢について
    - A: `ip dhcp relay information option-insert` は **むしろ Option 82 を入れる方向**で、本件とは逆の動作。  
    - C: `ip dhcp server use subscriber-id client-id` はサーバ側の識別方法に関するもので、SW3 側での根本原因解消にならない。  
    - D: `ip helper-address` は **L3 デバイスでの DHCP リレー機能**であり、SW3 が L3 ルータとして動作しているケースでなければ関係しない。  

- 試験向けの覚え方フレーズ

  - **「DHCP snooping 有効＋クライアントが IP 取れない＋Option 82 が怪しい」 → “`no ip dhcp snooping information option`”**  
  - 類題キーワード:
    - 「snooping 有効にしたら DHCP 動かなくなった」「untrusted ポートからの DHCP がサーバに届かない」  
    - → **“Option 82 を挿入しない設定を疑う”** とパターン化しておくと得点しやすい。

***
QUESTION 572 解説

- 問題文（英語）:  
Drag and drop the terminology from the left onto the corresponding definitions on the right.  
Select and Place.

用語と定義は次の通り（上から順に対応付け）。[file:296][file:297]

1. **Forwarding Equivalence Class (FEC)**  
   - set of packets with similar characteristics that might be bound to the same MPLS label

2. **Multiprotocol Label Switching (MPLS)**  
   - data-carrying mechanism that is independent of any data link layer protocol

3. **Provider Edge (PE)**  
   - router that functions as the ingress and/or egress router to the MPLS domain

4. **Label Switched Path (LSP)**  
   - route through an MPLS network, defined by a signaling protocol such as LDP or BGP

5. **Label Distribution Protocol (LDP)**  
   - mechanism by which two routers exchange label mapping information

- 問題文要約（日本語）:  
左側の MPLS 関連用語を、右側の **定義文** と正しく 1 対 1 で対応付けるドラッグ＆ドロップ問題。  
FEC / MPLS / PE / LSP / LDP の 5 つを覚えているかを確認している。 [file:296][file:297]

- 各用語のイメージ簡易メモ

  - **FEC**: 「**同じ扱いをされるパケットのグループ**」＝同じラベルが貼られる候補の集合。  
  - **MPLS**: 「**L2 に依存しないラベル転送の仕組み**」＝Ethernet でも PPP でも動く転送方式。  
  - **PE**: 「**MPLS 網の入口・出口となるルータ**」＝CE と P(コア) の境目の箱。  
  - **LSP**: 「**MPLS 網の中をラベルでたどる経路**」＝LDP/BGP などのシグナリングで定義。  
  - **LDP**: 「**ラベル値をお互いに教え合うプロトコル**」＝ラベルマッピング交換のメカニズム。  

- 試験向けの覚え方フレーズ

  - **「FEC = Flow のグループ、LSP = その通り道」**  
  - **「LDP = Label の Delivery/配布、PE = 端っこの Edge」**  
  - 「MPLS = **L2 非依存**のラベル転送方式」とセットで覚えると、定義文を見た瞬間に選びやすい。

***
QUESTION 573 解説

- 問題文（英語）:  
Which two label distribution methods are used by routers in MPLS? (Choose two)

A. Downstream unsolicited  
B. LDP session protection message  
C. LDP discovery hello message  
D. Downstream on demand  
E. Targeted hello message  

Correct Answer: A, D [file:268]

- 問題文要約（日本語）:  
MPLS で、**ラベルをどのような方式で配布するか**を表す「ラベル配布方式」を 2 つ選ぶ問題。  
ルータ間（LSR 間）が LDP セッションで合意する **“label distribution method”** を問うている。 [file:268]

- 正解: **A と D**

- 解説（なぜ A・D が正しいか）

  - LDP セッション確立後、LSR 同士は **「ラベルの配布方式」** をネゴシエートする。  
  - 利用できる方式は **2 つだけ** と明記されている。 [file:268]

    1. **Downstream Unsolicited（A）**  
       - 下流 LSR が、**要求されなくてもラベルマッピングを上流に自動広告**する方式。  
       - コアルータ間で一般的に使われるデフォルトの配布方法。 [file:268]

    2. **Downstream on Demand（D）**  
       - 下流 LSR が、**上流からの要求があったときだけラベルマッピングを返す**方式。  
       - ラベル配布を必要なプレフィックスに限定したい場合に利用。 [file:268]

  - 一方、B・C・E は「LDP のメッセージ種別・補助機能」であり、**“label distribution method” そのものではない**。  
    - B: **session protection** はセッション維持のための機能。  
    - C: **discovery hello** は LDP の隣接発見用メッセージ。  
    - E: **targeted hello** は非直結 LSR 向けの LDP セッション確立用メッセージ（extended discovery）。 [file:268]

- 試験向けの覚え方フレーズ

  - **「ラベルの “配り方” が聞かれたら “Downstream U / Downstream D” の 2 択セット**」  
  - キーワードで覚えると:
    - **U = Unsolicited = 勝手に配る**  
    - **D = on Demand = 要求されたら配る**  
  - Hello や targeted hello, session protection などが混ざっていても、  
    - 「**“distribution method” と書かれていたら A・D を機械的に拾う**」と決め打ちしておくと取りこぼしにくい。

***
QUESTION 574 解説

- 問題文（英語）:  
An engineer must ensure that R3 sees only type 1 and 2 LSAs in area 1. Which command must the engineer apply on R2?

A. area 1 nssa no-summary  
B. area 1 stub  
C. area 1 stub nssa  
D. area 1 stub no-summary  

Correct Answer: D [file:268]

- 問題文要約（日本語）:  
エンジニアは、**エリア 1 内の R3 が受け取る LSA を Type 1（Router LSA）と Type 2（Network LSA）だけに制限したい**。  
そのために、ABR である R2 にどの OSPF エリア設定コマンドを入れるべきかを問う。 [file:268]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - R3 に **Type1・2 のみ**を見せたい → **エリア 1 を “totally stub area” にする**必要がある。  
  - Totally stub area の条件は次の通り。 [file:268]
    - **Type 3（Summary LSA）と Type 5（External LSA）を受け取らない**  
    - 代わりに、ABR から **デフォルトルート（Type 3 の 0.0.0.0）だけが流れてくる**  
  - Cisco IOS では、**ABR 側の router OSPF コンフィグで `area 1 stub no-summary` を入れると、そのエリアを “totally stub” にできる**。  
    - 解説文にも「`area xx stub no-summary` で totally stub area を定義し、Type 5 と Type 3 を受け取らない」と明記されている。 [file:268]
  - 選択肢との対応:
    - A: `area 1 nssa no-summary` → **NSSA（Not-So-Stubby Area）** の完全版で、Type 7 LSA が入るため、**Type 1,2 だけにはならない**。  
    - B: `area 1 stub` → 通常 stub area。**Type 3 は許可**されるので要件「1 と 2 のみ」を満たさない。  
    - C: `area 1 stub nssa` → 文法的におかしい組み合わせ。  
    - D: `area 1 stub no-summary` → **完全 stub（totally stub）エリア化**し、Type 1,2 のみ＋デフォルトルートになる。  

- 試験向けの覚え方フレーズ

  - **「Type1・2だけ見せたい → “totally stub” → `area X stub no-summary`」**  
  - エリア種別ざっくり整理:
    - **Stub**: 1,2,3 + default（Type 5 なし）  
    - **Totally stub**: **1,2 だけ＋ default**（3,5 なし）→ `stub no-summary`  
    - **NSSA**: 1,2,3,7  
    - **Totally NSSA**: 1,2,7 + default（3 抑制）→ `nssa no-summary`  

***
QUESTION 575 解説

- 問題文（英語）:  
An OSPF neighbor relationship between R2 and R3 is showing stuck in EXCHANGE/EXSTART state. The neighbor is established between R1 and R2. The network engineer can ping from R2 to R3 and vice versa, but the neighbor is still down. Which action resolves the issue?

A. Administrative “shut then no shut” both router interfaces.  
B. Enable OSPF on the interface, which is required.  
C. Restore the Layer 2/Layer 3 connectivity issue in the ISP network.  
D. Match MTU on both router interfaces or ignore MTU.  

Correct Answer: D [file:268]

- 問題文要約（日本語）:  
R2–R3 間の OSPF ネイバーが **EXCHANGE/EXSTART 状態で固まっている**。  
R1–R2 間のネイバーは正常で、R2⇔R3 間の ping も通るが、OSPF ネイバーだけが上がらない。  
この状況を解決するための設定として、どれを行うべきかを問う。 [file:268]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - OSPF でネイバーが **EXSTART / EXCHANGE から進まない典型原因は MTU 不一致**。  
    - DBD（Database Description）パケット交換の段階で、片方が「相手の MTU が自分と違う」と判断すると、状態がそこで止まる。  
  - 本問では、  
    - L2/L3 の到達性（ping）は問題なし  
    - R1–R2 のネイバーは正常  
    なので、物理リンクや OSPF 未有効化よりも **インタフェース MTU ミスマッチ**が疑われるシナリオ。 [file:268]
  - 解説でも「EXSTART/EXCHANGE でスタックする場合は、**両端の MTU を揃えるか、`ip ospf mtu-ignore` で MTU チェックを無効化する**のが解決策」と整理されている。 [file:268]
  - 他の選択肢:
    - A: shut/no shut は一時的な再起動に過ぎず、MTU 不一致そのものは解消されない。  
    - B: 既に OSPF ネイバーが形成されかけている（EXSTART まで行っている）ので、**OSPF 未有効化が原因ではない**。  
    - C: ping が双方向で成功しているため、L2/L3 到達性の問題とは考えにくい。  

- 試験向けの覚え方フレーズ

  - **「OSPF が EXSTART / EXCHANGE で止まる → まず “MTU mismatch” を疑え」**  
  - 自分用ショートフレーズ:
    - **「EXSTART でスタuck → MTU をマッチ or ignore」**  
  - 似た選択肢が並んでいても、「EXSTART／EXCHANGE＋ping OK」というキーワードを見たら  
    - 自動的に **“MTU を揃える or `mtu-ignore`” に対応する選択肢（この問題では D）** を選ぶクセをつけると得点しやすい。

***
QUESTION 576 解説

- 問題文（英語）:  
Users report web connectivity problems on the server (10.1.1.10). Which IP SLA configuration captures the failure details through the network to resolve the issue?

A.  
B.  
C.  
D.  

Correct Answer: B [file:268]

- 問題文要約（日本語）:  
サーバ 10.1.1.10 への **Web（HTTP）アクセスに問題**が出ている。  
ネットワーク経路上でどこで失敗しているかを把握するために、どの **IP SLA 設定**を使えばよいかを問う。 [file:268]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 要件は「**Web 接続の詳細な失敗理由を取りたい**」なので、単なる ICMP echo ではなく **HTTP 用の IP SLA オペレーション**を使う必要がある。 [file:268]
  - IP SLA の **HTTP operation** は以下 3 種類の RTT を分解して測定できる。 [file:268]
    - **DNS lookup RTT**: サーバ名解決にかかる時間  
    - **TCP connect RTT**: HTTP サーバへの TCP セッション確立時間  
    - **HTTP transaction time**: HTTP リクエスト送信〜レスポンス受信までの時間（ホーム HTML ページ取得）  
  - これにより、  
    - 「DNS 解決で詰まっているのか」  
    - 「TCP コネクション確立で詰まっているのか」  
    - 「アプリケーション（HTTP 応答）で詰まっているのか」  
    を切り分け可能なので、**web connectivity の “failure details” を取る**要件を満たす。 [file:268]
  - 解説にも「Web 接続の詳細が必要なので、**HTTP IP SLA を使うべき**」と明示されている。  
    さらに「この HTTP operation のデフォルト・最小 frequency は 60 秒」と記載。 [file:268]

- 試験向けの覚え方フレーズ

  - **「Web トラブル＋“failure details” → “IP SLA HTTP” 一択」**  
  - キーワード暗記:
    - Web/HTTP/URL → **icmp ではなく HTTP operation**  
    - DNS/TCP/HTTP を分けて見たい → **HTTP IP SLA が 3 段階 RTT を出す**  
  - 選択肢で `icmp-echo` や `tcp-connect` 単体が並んでいても、  
    - 「**“web connectivity 問題の詳細” が書かれていたら HTTP operation のある選択肢（ここでは B）**」  
    と機械的に決め打ちできるようにしておくと取りこぼしにくい。

***
QUESTION 577 解説

- 問題文（英語）:  
Branch 2 hosts cannot receive dynamic IP addresses. Which action resolves the issue?

A. Configure the ip helper command on the Layer 2 switch SW2 interfaces.  
B. Configure the ip helper command on the Interface GigabltEthernet 0/0 of the DHCP router.  
C. Configure the ip helper command on the Interface GigabltEthernet 0/2 of the DHCP router.  
D. Configure the ip helper command on the interface GigabltEthernet 0/2 of the R2 router.  

Correct Answer: D [file:268]

- 問題文要約（日本語）:  
Branch 2 のホストが **DHCP で IP アドレスを取得できていない**。  
どこに `ip helper-address`（DHCP リレー）を設定すれば問題が解決するかを問う。 [file:268]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - DHCP クライアントがいるセグメントと DHCP サーバが **別ネットワーク**のとき、  
    - クライアントに最も近い **L3 インタフェース**（その VLAN/サブネットを持つルータのインタフェース）で `ip helper-address <DHCPサーバIP>` を設定し、  
    - そのインタフェースが **受け取ったブロードキャスト DHCPDISCOVER をサーバへユニキャスト中継**する必要がある。 [file:268]
  - 解説では「`ip helper` コマンドは **該当 DHCP クライアントに直接接続しているインタフェース**に入れるべき」とあり、図では **R2 の g0/2 が Branch 2 ホスト直結の L3 インタフェース**になっている。  
    - よって、**R2 g0/2 に `ip helper-address` を設定する D が正解**となる。 [file:268]
  - 他の選択肢:
    - A: SW2 が **L2 スイッチ**なら `ip helper-address` は設定できず、リレーもできない。  
    - B / C: DHCP ルータ側インタフェースに helper を置いても、**Branch 2 セグメントからのブロードキャストを拾えない**ため意味がない。  

- 試験向けの覚え方フレーズ

  - **「DHCP が届かない → `ip helper-address` は “クライアント直結の L3 インタフェース”」**  
  - 頭の中で決め打ち:
    - 「**L2 には helper×、サーバ側にも helper×、“クライアント側ルータ IF” に helper○**」  
  - 問題文で “Branch / VLAN X hosts cannot get DHCP” が出たら、  
    - 図の中から **その Branch/VLAN に接しているルータインタフェース**を探し、そこに helper を入れている選択肢（本問では D）を選ぶ癖をつける。

- 問題文（英語）:  
Which feature filters the IPv6 traffic on Layer 2 untrusted ports?

A. Binding Table Recovery  
B. DHCPv6 Guard  
C. IPv6 Source Guard  
D. IPv6 RA Guard  

Correct Answer: C [web:298][web:300]

- 問題文要約（日本語）:  
「**L2 の“untrusted”ポート上で IPv6 トラフィックをフィルタする機能**はどれか？」を問う問題。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - **IPv6 Source Guard** は、IPv6 First-Hop Security の一つで、L2 スイッチポートで **受信した IPv6 パケットの送信元アドレスを “バインディングテーブル” で検証し、不正なものをドロップする機能**。  
    - Cisco のドキュメントでも「IPv6 Source Guard and IPv6 Prefix Guard are **Layer 2 snooping features** that validate the source of IPv6 traffic」かつ「blocks any data traffic from an unknown source」と説明されている。[web:298][web:299]  
    - NetworkLessons でも「It **filters inbound traffic on L2 switch ports** that are not in the IPv6 binding table」と解説されている。[web:300][web:301]
  - **Binding Table Recovery** は、FHS バインディングテーブルを復旧する仕組みであり、トラフィックフィルタそのものではない。  
  - **DHCPv6 Guard** は、不正な DHCPv6 サーバからのメッセージ（Advertise/Reply など）をブロックする機能で、「全 IPv6 データトラフィック」をフィルタするわけではない。  
  - **IPv6 RA Guard** は、不正な Router Advertisement をブロックする機能であり、やはりデータトラフィック全般を L2 でフィルタする Source Guard とは役割が異なる。  

- 試験向けの覚え方フレーズ

  - **「“Source” Guard → 送信元 IPv6 を binding table でチェックして L2 でブロック」**  
  - キーワード対応:
    - **Source Guard = L2 で “データパケット” の送信元を検証**  
    - RA Guard = RA メッセージ専用  
    - DHCPv6 Guard = DHCPv6 メッセージ専用  
  - 選択肢に「untrusted port / Layer 2 / binding table / spoofing」が出たら、**IPv6 Source Guard（C）** を即選べるようにしておくとよい。

***
QUESTION 579 解説

- 問題文（英語）:  
EIGRP peering was lost.  
Which configuration resolves the issue?

A.  
B.  
C.  
D.  

Correct Answer: D [file:268]

- 問題文要約（日本語）:  
EIGRP ネイバー関係が張れなくなった原因を突き止め、  
どの **EIGRP metric weights の設定**を入れれば再度ピアリングが回復するかを問う問題。 [file:268]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 古典的 EIGRP（named mode でない通常モード）の `metric weights` は、  
    - **6 つのパラメータ**を取る形式になっている。  
    - 書式: `metric weights <TOS> <K1> <K2> <K3> <K4> <K5>`  
  - このうち **最初のパラメータは ToS（Type of Service）値で、常に 0 のみサポート**される。  
    - 実機でも `metric weights ?` で「Only TOS 0 supported」と出る。  
    - 仮に `metric weights 1 1 1 1 1 1` と入力しても、コンフィグには `metric weights 0 1 1 1 1 1` と自動的に書き換わる。 [file:268]
  - EIGRP ネイバーは、**K 値（metric weights）が両端で完全一致しないと形成されない**ため、  
    - 片側だけ `metric weights 1 1 1 1 1 1` のような **誤った ToS 指定や、パラメータ数不正**があると、ネイバーが落ちる。  
  - 解説では「選択肢のうち、**6 パラメータで、先頭が 0 のもの**が正しい」とされており、  
    - それに該当するのが **D（`metric weights 0 1 1 1 1 1` 形式）** なので、  
    - 両ルータの K 値を正しく揃えることで EIGRP peering が復旧する。 [file:268]

- 試験向けの覚え方フレーズ

  - **「EIGRP ネイバーが “K-value mismatch” → `metric weights 0 ...` で揃える」**  
  - キーワード:
    - TOS は **必ず 0**、  
    - **K 値は 5 つ＋先頭の 0 で “0 K1 K2 K3 K4 K5”**。  
  - もし選択肢に `metric weights` が複数あれば、  
    - **「6 つの数字＋先頭 0」が正解候補**と即判断できるようにしておくと取りこぼしにくい。

***
QUESTION 580 解説

- 問題文（英語）:  
An engineer must troubleshoot a connectivity issue impacting the redistribution of the subnet 172.16.2.48 / 28 into the OSPF domain. Which configuration on router R1 advertises this subnet into the OSPF domain?

A.  
R1(config)#route-map CCNP permit 10  
R1(config-route-map)#match tag 200  
R1(config-route-map)#exit  
R1(config)#router ospf 10  
R1(config-router)#redistribute eigrp 100 subnets route-map CCNP  

B.  
R1(config)#route-map CCNP permit 10  
R1(config-route-map)#match route-type internal  
R1(config)#router ospf 10  
R1(config-router)#redistribute eigrp 100 subnets route-map CCNP  

C.  
R1(config)#route-map CCNP permit 10  
R1(config-route-map)#match route-type level-2  
R1(config)#router ospf 10  
R1(config-router)#redistribute eigrp 100 subnets route-map CCNP  

D.  
R1(config)#route-map CCNP deny 10  
R1(config-route-map)#match tag 200  
R1(config)#route-map CCNP permit 10  
R1(config)#router ospf 10  
R1(config-router)#redistribute eigrp 100 subnets route-map CCNP  

Correct Answer: A [file:268]

- 問題文要約（日本語）:  
サブネット **172.16.2.48/28** を EIGRP ドメインから **OSPF に再配布できていない**。  
R1 でどの設定を入れれば、このサブネットを OSPF ドメイン側に広告できるかを問う。 [file:268]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 解説によると、EIGRP ルータ R4 では **静的ルート 172.16.2.48/28 を EIGRP に再配布する際に tag 200 を付与**している。  
    - つまり、R1 の EIGRP ルーティングテーブル上で、このサブネットは **タグ 200 を持つ EIGRP external ルート**として存在する。 [file:268]
  - R1 から OSPF へ再配布する際に、**このタグ 200 を条件にマッチさせて許可する route-map** を使えば、  
    - 目的の 172.16.2.48/28 だけを OSPF に流せる。  
  - 選択肢 A はまさにその設定を行っている。  
    - `route-map CCNP permit 10`  
    - `match tag 200`  
    - `router ospf 10`  
    - `redistribute eigrp 100 subnets route-map CCNP`  
    → タグ 200 のルート（= 172.16.2.48/28）だけが OSPF に再配布される。 [file:268]
  - 他の選択肢:
    - B: `match route-type internal` は **EIGRP internal ルート**のみ許可するので、EIGRP external（tag 200）の 172.16.2.48/28 はマッチしない。  
    - C: `match route-type level-2` は IS-IS 用のキーワードであり、このシナリオの EIGRP→OSPF 再配布には無関係。  
    - D: 最初に `deny tag 200` を定義しているため、肝心の 172.16.2.48/28 を OSPF へ出さない。  

- 試験向けの覚え方フレーズ

  - **「再配布で“特定サブネット”だけ通したい → 元ルータ側の “tag” を match して permit」**  
  - 具体的に:
    - 送出元で `redistribute static tag 200` のように **tag を付ける**  
    - 受け側で `route-map X permit 10 / match tag 200` → `redistribute eigrp … route-map X`  
  - 選択肢に `match tag <番号>` が 2 つ（permit / deny）が混ざっていたら、  
    - **「欲しいルートの tag を permit している方（ここでは A）」** を選ぶ、とパターン化しておくと取りこぼしにくい。

***
QUESTION 581 解説

- 問題文（英語）:  
An engineer must troubleshoot an issue with the aaa authentication that affected the user’s login to router R1. Which command allows the configured user to authenticate?

A. aaa authentication login default group tacacs+  
B. aaa authentication login default group radius  
C. aaa authentication login default group radius tacacs+  
D. aaa authentication login default group radius local  

Correct Answer: D [file:268]

- 問題文要約（日本語）:  
R1 で AAA 認証を使ってログインしようとしたユーザが認証に失敗している。  
どの `aaa authentication` 設定にすれば、**ルータ上に定義済みのローカルユーザでログインできるようになるか**を問う。 [file:268]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 解説によると、TACACS+ サーバ向けの **キー設定に問題がある**ため、外部 TACACS+ 認証は成功しない状況になっている。  
    - しかし、選択肢のどれも「TACACS+ サーバの key を修正する」ものではない。 [file:268]
  - この状態でユーザを認証させるには、**ルータ自身に登録済みのローカルユーザデータベースを使って認証する必要がある**。  
    - `aaa authentication login default group radius local` なら、  
      - まず RADIUS を試し、失敗したら **local** にフォールバックする。  
    - 解説では「ユーザ名/パスワードがルータのコンフィグにも定義されていると仮定すると、`local` を含む選択肢だけが可能な答え」としており、  
      → それが **D** である。 [file:268]
  - 他の選択肢:
    - A: `group tacacs+` のみ。TACACS+ の key が誤っているので、そもそも使えない。  
    - B / C: `group radius` のみ、または `radius tacacs+` だが、問題文には **RADIUS サーバの存在や設定の正当性が示されていない**。  
      → 確実に動くとは言えない。 [file:268]

- 試験向けの覚え方フレーズ

  - **「外部 AAA（TACACS/RADIUS）が怪しい & ローカルユーザ定義あり → `... local` 付きの default 認証に切り替える」**  
  - パターン:
    - TACACS key mismatch / RADIUS 不明 / サーバ unreachable など  
    - かつ「ローカルユーザも定義されている」 → **`aaa authentication login default group 〇〇 local`** が鉄板。  
  - もし選択肢に `local` が 1 つだけ混じっていたら、  
    - 「**外部 AAA が壊れている前提問題**なら `local` 入り（ここでは D）」を優先してチェックする癖をつけると覚えやすい。

***
QUESTION 582 解説

- 問題文（英語）:  
A PC is configured to obtain an IP address automatically, but it receives an IP address only from the 169.254.0.0 subnet. The DHCP server logs contained no DHCPDISCOVER message from the MAC address of the PC. Which action resolves the issue?

A. Configure a DHCP reservation on the server for the PC.  
B. Configure an ip helper-address on the router to forward DHCP messages to the server.  
C. Configure DHCP Snooping on the switch to forward DHCP messages to the server.  
D. Configure a static IP address on the PC and exclude it from the DHCP pool.  

Correct Answer: B [file:268]

- 問題文要約（日本語）:  
PC は「自動取得」にしているのに、**169.254.0.0/16（APIPA）** のアドレスしか取れない。  
DHCP サーバのログには、その PC の MAC アドレスからの **DHCPDISCOVER が一切来ていない**。  
どの対応をすれば問題が解決するかを問う。 [file:268]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 169.254.0.0/16 のアドレスは、DHCP サーバから応答が得られないときにクライアント自身が **APIPA** として自動採番するアドレス。  
    → つまり、PC の **DHCPDISCOVER がサーバまで届いていない**状況を示している。 [file:268]
  - 解説によると、**PC と DHCP サーバは異なる VLAN に存在**している。  
    - ブロードキャストで送る DHCPDISCOVER は **L2/VLAN を跨げない**ため、直接サーバには届かない。  
  - したがって、PC 側 VLAN に接している L3 インタフェース（デフォルトゲートウェイ）で `ip helper-address <DHCPサーバIP>` を設定し、  
    - 受信した DHCP ブロードキャストを **DHCP サーバへユニキャストリレー**させる必要がある。  
    → これを行うのが選択肢 **B**。 [file:268]
  - 他の選択肢:
    - A: 予約を作っても、そもそも DISCOVER が届いていないので無意味。  
    - C: DHCP Snooping は **不正 DHCP サーバの防止**機能であり、DISCOVER をサーバに “転送” してくれるものではない。  
    - D: 静的 IP を振るのは根本原因（DHCP が届いていない）を解決しておらず、試験的には NG。  

- 試験向けの覚え方フレーズ

  - **「169.254 アドレス＋サーバに DISCOVER が届いていない → VLAN 越え、`ip helper-address` がない」**  
  - パターン暗記:
    - APIPA / 169.254 が出たら「**DHCP サーバに届いてない**」を疑う。  
    - PC と DHCP サーバが **別 VLAN / サブネット** → **“クライアント側ルータ IF に `ip helper-address`” 一択**。  

***
QUESTION 583 解説

- 問題文（英語）:  
Which protocol must be secured with MD-5 authentication across the mpls cloud to prevent hackers from introducing bogus routers?

A. LDP  
B. LSP  
C. RSVP  
D. MP-BGP  

Correct Answer: A [web:325][web:329]

- 問題文要約（日本語）:  
MPLS クラウド内で **偽のルータ（bogus routers）を紛れ込ませないために、MD5 認証で保護すべきプロトコル**はどれかを問う。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - MPLS でラベルを配布し LSP を張るシグナリングとして **LDP (Label Distribution Protocol)** が使われる。  
    - LDP セッションは TCP ベースで張られるため、**TCP スプーフィングで偽の LDP ピアを作られるリスク**がある。  
  - そのため Cisco などの実装では、**LDP セッションに MD5 認証を設定して、正しい共有鍵を持つ隣接ルータとしか LDP セッションを張れないようにする**ことが推奨されている。  
    - Cisco ドキュメントでも「MPLS LDP MD5 session authentication は、**不正なピアの LDP セッション確立と spoofed TCP メッセージを防ぐ**」と記載されている。[web:318][web:329]
  - 選択肢のうち:
    - **LDP** は「ラベル配布そのもの」を扱うプロトコルなので、ここを守ることで **偽ラベル・偽 LSR の混入**を防げる。  
    - **LSP** はプロトコル名ではなく “ラベルスイッチパス” の概念。  
    - **RSVP** は MPLS-TE 用シグナリングで、確かに認証機構もあるが、問題文は「bogus routers」を防ぐ観点で、代表的なのは **LDP + MD5**。  
    - **MP-BGP** も MD5 で守ることはあるが、設問は “across the MPLS cloud” のラベル配布経路文脈で出ているため、LDP が優先される。  
  - 以上より、**LDP に MD5 認証を入れる (A)** が問われている内容に最も合致する。 [web:321][web:325]

- 試験向けの覚え方フレーズ

  - **「偽 LSR / bogus router 防止 in MPLS → “ラベル配布” を守る → LDP + MD5」**  
  - キーワード対応:
    - “across the MPLS cloud”, “bogus routers”, “MD5” → **LDP** を即連想。  
    - LSP は物理・論理パスの名前であってプロトコル名ではない、というのも一緒に覚えておくと選択肢で迷わない。

***
QUESTION 584 解説

- 問題文（英語）:  
In an IPv6 network, the network administrator restricted remote access of the core router to a single user. The administrator configured the IPv6 access list and applied it on the WAN interface of the core router. The administrator also wanted to investigate who else is trying to access the router and added a permit host entry with log statement in the access list but could not find any details. Which action does the network administrator take to resolve the issue?

A. Apply the access list on line vty using the ipv6 access-class out command  
B. Apply the access list on all interfaces using the ipv6 access-class in command  
C. Apply the access list on line vty using the ipv6 access-class in command  
D. Apply the access list on the WAN interface using the ipv6 traffic-filter out command  

Correct Answer: C [file:268]

- 問題文要約（日本語）:  
IPv6 ネットワークで、コアルータへの **リモートアクセス（vty）を特定ユーザだけに制限**した。  
WAN インタフェースに IPv6 ACL を適用し、さらに「誰がアクセスを試みているか調査」するために **permit＋log** を ACL に追加したが、ログに何も出てこない。  
どの対応を取れば、試行ログを取得できるかを問う。 [file:268]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - いまの構成では、IPv6 ACL を **WAN インタフェース**に適用しているだけなので、  
    - WAN から入ってくる Telnet/SSH などのトラフィックしか、その ACL で **ログ対象**にならない。  
    - しかし、実際のリモートアクセスは **WAN だけでなく LAN 側からの vty 接続もあり得る**。  
  - 「誰がコアルータの vty にログインを試みているか」を取りたい場合は、  
    - **vty ラインに対して ACL を適用**し、そこに `log` をつける必要がある。  
    - IPv6 で vty に ACL を適用するコマンドが `ipv6 access-class <ACL名> in`。  
  - 解説にも「**リモートアクセスのログを取るには line vty に `ipv6 access-class <ACL> in` を適用すべき**。  
    WAN インタフェースにだけ ACL を付けても、LAN からのアクセスはログされない」とある。 [file:268]  
  - 選択肢でこれに該当するのが **C**:
    - 「Apply the access list on line vty using the `ipv6 access-class in` command」。  

- 試験向けの覚え方フレーズ

  - **「“リモートアクセスの制御／ログ” → インタフェースACLではなく“line vty への `ipv6 access-class in`”」**  
  - パターン:
    - vty ログイン制限 or 誰がログインしているか調査 →  
      - IPv4: `access-class`  
      - IPv6: `ipv6 access-class`  
    - **“インタフェース ACL はパケット、vty の access-class はログイン”** と分けて覚えると整理しやすい。

***
QUESTION 585 解説

- 問題文（英語）:  
A network administrator is trying to switch to the privileged EXEC level on R1 but failed. Which configuration resolves the issue?

A. tacacs-server enable-password Cisco@123  
B. enable-password Cisco@123  
C. enable password Cisco@123  
D. tacacs server enable-password Cisco@123  

Correct Answer: C [web:334][web:343]

- 問題文要約（日本語）:  
管理者が R1 上で `enable` を打って **特権 EXEC モードに入ろうとしたが失敗**している。  
どの設定を入れれば特権 EXEC レベルに昇格できるようになるかを問う。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - デバッグメッセージから、TACACS サーバには接続できていない（到達不能 or key 不一致）ため、**外部 AAA では enable 認証できない状態**。 [web:338]
  - この場合、**ローカルの enable パスワード / secret を設定しておけば、TACACS が死んでいても `enable` で特権モードに入れる**。  
  - Cisco IOS では、特権 EXEC 用の古い構文は `enable password <pass>` であり、  
    - 選択肢 C の `enable password Cisco@123` がこれに該当する。 [web:343]
  - 他の選択肢:
    - B: `enable-password` というコマンドは存在しない（ハイフンなしが正しい）。  
    - A / D: `tacacs-server enable-password` / `tacacs server enable-password` というコマンドも IOS 標準構文としては不正。  
      → TACACS 絡みの設定を直しても、そもそもコマンドが不正なので解決しない。 [web:340][web:261]
  - 解説でも「TACACS サーバに接続できない状況で、選択肢の中で有効なコマンドは `enable password ...` だけ。  
    ローカルユーザ（privilege 15 未設定）が特権 EXEC に上がるには、ルータ上で enable password/secret を定義する必要がある」と説明されている。  

- 試験向けの覚え方フレーズ

  - **「`enable` が通らない＋TACACS 死んでる → `enable password`（or `enable secret`）をローカルに入れる」**  
  - 選択肢パターン:
    - `enable password`（スペース）＝〇  
    - `enable-password`（ハイフン）＝コマンド自体が×  
    - TACACS 関連に `enable-password` が付いていても、それらは **存在しない or 文脈違い** と覚えておくと迷わない。

***
QUESTION 586 解説

- 問題文（英語）:  
In Cisco DNA Center, a network engineer identifies that BGP-learned networks are repeatedly withdrawn from peers. Which configuration must the engineer apply to resolve the issue?

A.  
router bgp 100  
bgp dampening  

B.  
router bgp 100  
bgp graceful-restart  

C.  
route-map Dampening permit 10  
 set dampening 15 750 2000 60  
router bgp 100  
 neighbor 10.140.212.5 route-map Dampening out  

D.  
route-map Dampening permit 10  
 set dampening 15 750 2000 60  
router bgp 100  
 neighbor 10.140.212.5 route-map Dampening in  

Correct Answer: D [file:268]

- 問題文要約（日本語）:  
Cisco DNA Center で監視したところ、**特定ピアから学習した BGP ルートが頻繁にアナウンス／撤回を繰り返している（フラップ）**。  
この問題を抑制するには、どの BGP 設定を入れるべきかを問う。 [file:268]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - ルートが頻繁にフラップすると、CPU・コントロールプレーン負荷増加や経路不安定を招くため、**BGP dampening（抑制）**を使ってフラッピング経路を一時的に抑えるのが定石。  
  - BGP dampening の適用方法は大きく 3 パターンある。 [file:268]
    - グローバル: `bgp dampening`（全 BGP ルートに一律適用）  
    - ルート単位: `bgp dampening route-map XXX`（route-map で match した経路のみ）  
    - ピア単位: `neighbor X route-map XXX in`（特定ピアから *受信* した経路に route-map の `set dampening` を適用）
  - 本問の前提:
    - 図中のネットワーク 10.140.217.0/24 は **neighbor 10.140.212.5**（BGP Router-ID 10.140.210.1）から学習しており、  
    - 問題は「**このピアから受信した経路がフラップ**している」ケース。 [file:268]
  - したがって、「**そのピアから入ってくる経路にだけ dampening を適用**」するのが最もスコープが適切。  
    - D は `route-map Dampening` で `set dampening 15 750 2000 60` を定義し、  
      - `neighbor 10.140.212.5 route-map Dampening in` で **inbound（受信側）に適用**している。  
    → まさに問題のピアから学習するルートだけを抑制できるため、D が正解となる。 [file:268]
  - 他の選択肢:
    - A: `bgp dampening` だと **すべての BGP 経路に一律適用**されるため、影響範囲が広すぎる。問題文の「特定ピア」に対して過剰。  
    - B: `bgp graceful-restart` は再起動時の経路維持用であり、ルートフラッピング抑制とは無関係。  
    - C: 同じ route-map だが **`out` 方向**に適用しており、「こちらから相手に送る経路」を dampening してしまうだけで、相手から受信するフラップには効かない。  

- 試験向けの覚え方フレーズ

  - **「BGP ルートが“ピアから”フラップ → `set dampening` を入れた route-map を “neighbor x in” に適用」**  
  - パターン整理:
    - 乱暴に全体抑制 → `bgp dampening`（A の形、範囲広すぎ注意）  
    - 特定ピアの受信ルートだけ → **`neighbor <IP> route-map Dampening in`**（今回の D）  
    - `out` が付いていたら「送り出す側」なので、**受信フラップ問題には不適合**と判断する。  

***
QUESTION 587 解説

- 問題文（英語）:  
Which action adds the 10.1.7.6/32 route to the BGP table?

A. Add a static route for the 10.1.7.6/32 network.  
B. Add summary-address 10.1.7.6 255.255.255.0.  
C. Add the network 10.1.7.6 mask 255.255.255.255 backdoor command.  
D. Add the network 10.1.7.6 mask 255.255.255.255 command.  

Correct Answer: D [web:347][web:334]

- 問題文要約（日本語）:  
プレフィックス **10.1.7.6/32** を **BGP テーブルに載せる（自ルータからアドバタイズする）**には、どのコマンドを実行すればよいかを問う。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 解説前提として、10.1.7.6/32 はこのルータで **ルーティングテーブル上に存在する（直結 or スタティック済み）**。  
    - BGP で自分のルートを広告するには、**`network` コマンドで「自ルータの RIB に存在するプレフィックス」を指定**するのが基本。  
    - 書式は `network <prefix> mask <netmask>` で、/32 なら `network 10.1.7.6 mask 255.255.255.255`。 [web:352][web:351]
  - 選択肢 D はまさにこの構文であり、**10.1.7.6/32 を BGP テーブルに “自前オリジン” として載せる正しい方法**。  
  - 他の選択肢:
    - A: **スタティックルートを足すだけでは BGP には乗らない**。  
      - 「RIB に存在する」ことは BGP の `network` マッチ条件だが、**BGP へ広告するには別途 `network` or 再配布が必要**。  
    - B: `summary-address` は **OSPF のエリア境界等で使うコマンド**で、BGP 文脈では不適切。  
    - C: `network 10.1.7.6 mask 255.255.255.255 backdoor` は **“backdoor” 指定**で、  
      - これは「同じプレフィックスを eBGP でも学習しているときに、その eBGP 経路を AD 200 に落として、IGP の経路を優先させる」ための特殊用途。  
      - **単に自分の /32 を BGP に載せる目的には不要**であり、この問題の意図から外れる。  

- 試験向けの覚え方フレーズ

  - **「自分のネットワークを BGP で広告 → `network <prefix> mask <netmask>`」**  
  - よくある引っかけ:
    - 「static だけでは BGP には乗らない」  
    - 「`backdoor` は “IGP を優先したいときの eBGP 経路 AD 調整用” であって、**普通の広告には不要**」  
  - /32 を見たら、**マスクは必ず 255.255.255.255 を合わせないと BGP がマッチしてくれない**（マスクも完全一致が必要）点もセットで覚える。

***
QUESTION 588 解説

- 問題文（英語）:  
Which router translates the customer routing information into VPNv4 routes to exchange VPNv4 routes with other devices through MP-BGP?

A. CE  
B. PE  
C. VPNv4 RR  
D. P  

Correct Answer: B [web:360][web:378]

- 問題文要約（日本語）:  
顧客ルーティング情報（CE からの IPv4 ルート）を **VPNv4 ルートに変換し、MP-BGP を使って他デバイスと VPNv4 ルート交換する役割**を持つルータはどれか、という問題。  

- 正解: **B（PE ルータ）**

- 解説（なぜ B が正しいか）

  - Cisco の MPLS L3VPN ドキュメントでは、**PE ルータ**の役割は以下のように定義されている。  
    - CE と IGP/BGP で経路交換  
    - **CE から学習した IPv4 ルートに RD を付けて VPNv4 ルートへ変換**  
    - その VPNv4 ルートを **MP-BGP を使って他の PE と交換**する [web:360][web:374]  
  - よって、「顧客ルート → VPNv4 ルートへの変換＋MP-BGP での交換」を行うのは **PE**。  
  - 他の選択肢:
    - CE: 顧客側ルータ。VPNv4 は扱わず、PE と通常の IPv4 ルートをやり取りするだけ。 [web:367]  
    - VPNv4 RR: ルートリフレクタは **PE 間で交換される VPNv4 ルートを反射する役割**であり、自身が CE から顧客ルートを受け取って翻訳するわけではない。  
    - P: コアルータ。VPNv4/BGP 制御プレーンを持たず、**ラベルスイッチのみ**を担当。  

- 試験向けの覚え方フレーズ

  - **「CE から学んだ経路に RD を付けて VPNv4 に“翻訳”し、MP-BGP で配るのは PE」**  
  - 役割ワンフレーズ：
    - **CE = Customer Edge（顧客のルータ）**  
    - **PE = Provider Edge（VPNv4 をしゃべるフロント）**  
    - **P = Provider core（ラベルだけ見る中継）**  

***
QUESTION 589 解説

- 問題文（英語）:  
After the network administrator rebuilds the IPv6 DHCP server, clients are not getting the IPv6 address lease. Which action resolves the issue?

A. Add ipv6 dhcp server MY_POOL under the interface ethernet 0/0 on H1.  
B. Remove FE80::A8BB:CCFF:FE00:5000 assigned by the IPv6 DHCP server.  
C. Configure FF02::1:2 to discover all IPv6 DHCP clients.  
D. Add ipv6 dhcp server MY_POOL under the interface ethernet 0/0 on R1.  

Correct Answer: D [web:349][web:390]

- 問題文要約（日本語）:  
IPv6 DHCP サーバ（R1）を再構築したあと、クライアントが IPv6 アドレスリースを受け取れなくなった。  
どの設定を入れれば、クライアントが再び IPv6 アドレスを取得できるかを問う。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - Cisco IOS でルータを **IPv6 DHCP サーバ**として動かす場合は、  
    1. `ipv6 dhcp pool MY_POOL` でプールを定義  
    2. **クライアントに面しているインタフェース**に  
       - `ipv6 dhcp server MY_POOL` を設定して、「このインタフェースに来た DHCPv6 要求には MY_POOL を使う」と関連付ける必要がある。 [web:386][web:215]
  - 再構築時に、プール自体は作ったが **インタフェースへの紐付け（`ipv6 dhcp server MY_POOL`）を入れ忘れている**典型パターンがこの問題。  
    - したがって、**DHCPサーバである R1 の e0/0 に `ipv6 dhcp server MY_POOL` を入れる**のが正解であり、それが選択肢 D。 [web:390]
  - 他の選択肢:
    - A: H1 はクライアント側なので、ここに `ipv6 dhcp server` を設定するのは役割が逆。  
    - B: FE80::〜 はリンクローカルアドレスであり、削除しても DHCPv6 リースが配られない根本原因とは無関係。  
    - C: FF02::1:2 は DHCPv6 サーバ・リレー用マルチキャストアドレスで、**ルータが自動で使う宛先**。管理者が「設定する」ものではない。  

- 試験向けの覚え方フレーズ

  - **「IPv6 DHCP サーバ＝プール作成＋“クライアント向き IF に `ipv6 dhcp server <POOL>` を必ず付ける”」**  
  - “どのインタフェースに `ipv6 dhcp server` を付けるか” と聞かれたら、  
    - **サーバ側ルータ（R1）の、クライアントに直結している IF** を選ぶのが基本パターン。  

***
QUESTION 590 解説

- 問題文（英語）:  
An engineer received a ticket about a router that has reloaded. The monitoring system graphs show different traffic patterns between logical and physical interfaces when the router is rebooted. Which action resolves the issue?

A. Configure the snmp ifindex persist command on the physical interfaces.  
B. Clear the logical interfaces with snmp ifindex clear command.  
C. Trigger a new snmpwalk from the monitoring system to synchronize interface OIDs.  
D. Configure the snmp ifindex persist command globally.  

Correct Answer: A [web:400][web:398]

- 問題文要約（日本語）:  
ルータ再起動後、監視ツール上で **論理インタフェースと物理インタフェースのグラフが以前と噛み合わない（別のトラフィックとして見える）**。  
これは ifIndex（インタフェース OID）がリロードのたびに変わるせいだが、どの設定を入れればこの問題を防げるかを問う。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - SNMP では各インタフェースに **ifIndex** が割り当てられ、  
    - NMS は「ifIndex 番号ベース」でトラフィックをグラフ化する。  
  - ルータ再起動時に ifIndex が振り直されると、  
    - 監視システムは **以前 “インタフェースX” と思っていた OID が、別のインタフェースを指す**ことになり、  
    - その結果「物理/論理インタフェースのグラフが変な形で繋がる／入れ替わる」現象が起きる。 [web:400]
  - Cisco ではこの問題を防ぐために **SNMP IfIndex Persistence** 機能があり、  
    - 全体に適用: `snmp-server ifindex persist`（グローバル）  
    - 個別 IF に適用: インタフェースで `snmp ifindex persist` [web:398]  
  - 問題文の選択肢では、正しい構文として使えるのは **インタフェースでの `snmp ifindex persist`（A）** だけであり、解説でも「インタフェースコンフィグで ifIndex を保持させる」と説明されている。  
    - よって、**監視対象の物理インタフェースに対して `snmp ifindex persist` を設定する** A が正解。  
  - 備考: 実運用では、Cisco ドキュメントが推奨するのは **グローバルの `snmp-server ifindex persist`** で、これがあればすべての IF の ifIndex がリロード後も変わらない。  
    - ただし、この選択肢は問題文には無く、  
    - 残りの選択肢 B/C/D は構文や動作が不正・不十分なため、**相対的に A が最適解**となる。  

- 試験向けの覚え方フレーズ

  - **「再起動したら NMS のインタフェースグラフがズレる → ifIndex が変わっている → `ifindex persist`」**  
  - 選択肢パターン:
    - グローバルが出れば `snmp-server ifindex persist` が本命。  
    - インタフェース単位なら `snmp ifindex persist`。  
    - `snmpwalk し直す` や `ifindex clear` は根本対策ではないので捨てる。  

***
QUESTION 591 解説

- 問題文（英語）:  
The authentication is not working as desired and the user drops into user-exec mode. Which configuration resolves the issue?

A.  
aaa new-model  
aaa authentication common-id default local  
aaa authorization exec default local  
!  
line vty 0 4  
 login authentication default  
 authorization exec default  

B.  
aaa new-model  
aaa authentication login default local  
aaa authorization priv default 15  
!  
line vty 0 4  
 login authentication default  
 authorization exec priv15  

C.  
aaa new-model  
aaa authentication login local  
aaa authorization exec local  
!  
line vty 0 4  
 login authentication local  
 authorization exec default  

D.  
aaa new-model  
aaa authentication login default local  
aaa authorization exec default local  
!  
line vty 0 4  
 login authentication default  
 authorization exec default  

Correct Answer: D [web:349][web:411]

- 問題文要約（日本語）:  
AAA を設定しているが、ユーザがログインすると **思ったとおりに特権権限が付与されず、user EXEC モードに落ちる**。  
どの AAA 設定にすれば、ログイン直後に期待どおりの特権レベル（ローカル DB の privilege）でログインできるようになるかを問う。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - ユーザが特権モードに上がれない場合、多くは「**認証はしているが、authorization（権限付与）が正しく設定されていない**」のが原因。  
  - Cisco の基本形は次のセット。 [web:411][web:413]
    - `aaa new-model`  
    - `aaa authentication login default local`  → ローカルユーザでログイン認証  
    - `aaa authorization exec default local`   → ローカルユーザ DB の privilege に基づいて EXEC 権限を決定  
    - `line vty 0 4`  
      - `login authentication default`  
      - `authorization exec default`  
  - 選択肢 D はまさにこの形で、  
    - 認証: `login authentication default` → `aaa authentication login default local` を参照  
    - 認可: `authorization exec default` → `aaa authorization exec default local` を参照  
    → ローカルユーザに `privilege 15` などが設定されていれば、**ログイン直後にそのレベルで特権 EXEC に入れる**。 [web:417]
  - 他の選択肢:
    - A: `aaa authentication common-id default local` というメソッドリスト名・構文が本筋から外れており、問題文の典型解（login default）とずれる。  
    - B: `aaa authorization priv default 15` や `authorization exec priv15` というメソッドリスト名の使い方が不適切で、`exec` 用の default リストと対応していない。  
    - C: `aaa authentication login local` はメソッドリスト名が `local` なので、line vty 側も `login authentication local` で合わせる必要があるが、`authorization exec default` との整合が取れていない。  

- 試験向けの覚え方フレーズ

  - **「AAA で“ログイン＝local”＋“権限付与＝local” → `aaa authentication login default local` ＋ `aaa authorization exec default local`」**  
  - line vty 側は必ず:
    - `login authentication <同じメソッド名>`  
    - `authorization exec <同じメソッド名>`  
    をセットで貼る。  
  - 「user EXEC に落ちる＝authorization exec が正しく噛んでない」と覚えておくとパターン問題で拾いやすい。

***
QUESTION 592 解説

- 問題文（英語）:  
R1 cannot establish a neighbor relationship with R2. Which action resolves the issue?

A. Configure the mtu ignore command on the interfaces of R1 and R2.  
B. Configure the ip ospf network point-to-point command on the interfaces of R1 and R2.  
C. Configure the neighbor 2.2.2.2 command on R1 under the OSPF process.  
D. Configure the ip ospf network broadcast command on the interfaces of R1 and R2.  

Correct Answer: A [web:418][web:421]

- 問題文要約（日本語）:  
R1 と R2 の間で **OSPF ネイバー関係が張れない**。  
どの設定を入れればネイバーを形成できるかを問う。  
（元の図では OSPF が EXSTART などで止まり、MTU 1500 の表示などがヒントとして出ている問題。） [web:12][web:422]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - OSPF でネイバーが **EXSTART / EXCHANGE から先に進まない**典型原因は **MTU mismatch**。  
    - OSPF は DBD パケット内に「Interface MTU」を広告し、  
      - 受信側が自分より大きい MTU 値を見た場合、その DBD を拒否し、隣接が張れない仕様。 [web:422][web:426]
  - 問題の出典解説でも「R1 が R2 とネイバーを張れない原因は MTU の不一致であり、  
    `ip ospf mtu-ignore` を両端インタフェースに入れて MTU チェックをバイパスすることでネイバーが形成される」とされている。 [web:418][web:429]
  - 選択肢 A の「mtu ignore コマンド」は IOS では `ip ospf mtu-ignore` に該当し、  
    - これを **R1/R2 両方の該当インタフェースに設定**すれば、OSPF による MTU チェックが無効となり、隣接が成立する。  
  - 他の選択肢:
    - B: `ip ospf network point-to-point` はネットワークタイプを変えるだけで、MTU mismatch 問題は解決しない。  
    - C: `neighbor 2.2.2.2` は NBMA などで **静的ネイバー**を定義する用途。図の前提では不要かつ問題の本質（MTU）を解決しない。  
    - D: `ip ospf network broadcast` もネットワークタイプ変更であり、MTU 不一致を解決しない。  

- 試験向けの覚え方フレーズ

  - **「OSPF ネイバー張れない＋EXSTART／EXCHANGE＋MTU 表示 → MTU mismatch → `ip ospf mtu-ignore` か MTU を揃える」**  
  - コマンド対応:
    - 根本解決: **物理 MTU を両端で合わせる**  
    - 試験の多く: 「手っ取り早くネイバーを張る」 → **`ip ospf mtu-ignore`** が正解になりやすい。  

***
QUESTION 593 解説

- 問題文（英語）:  
The SNMP server with IP address 172.16.4.4 cannot access host router A. Which configuration command on router A resolves the issue?

A. access-list 4 permit 172.16.4.0 0.0.0.3  
B. access-list 4 permit host 172.16.4.4  
C. snmp-server host 172.16.4.4 ccnp  
D. snmp-server community ccnp  

Correct Answer: B [web:435][web:385]

- 問題文要約（日本語）:  
SNMP サーバ 172.16.4.4 から **ルータ A の SNMP 情報が取得できない**。  
どのコマンドをルータ A に入れれば、SNMP サーバがルータ A へ正常にアクセスできるようになるかを問う。  
（図では `snmp-server community ccnp RO 4` のように、ACL 4 が SNMP アクセス制御に使われている状況。）  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - SNMPv1/v2c では、`snmp-server community <string> RO/RW <ACL>` の形で  
    - **指定した ACL に permit されている送信元 IP だけが SNMP アクセス可能**になる。 [web:441][web:438]
  - 問題では、SNMP サーバ 172.16.4.4 からのアクセスが拒否されているので、  
    - ルータ A 側の「ACL 4」が **SNMP サーバを permit していない**のが原因。  
  - したがって、ACL 4 に **「host 172.16.4.4 を許可」するエントリ**を追加する必要がある。  
    - それが選択肢 B: `access-list 4 permit host 172.16.4.4`。 [web:435][web:439]
  - 他の選択肢:
    - A: `access-list 4 permit 172.16.4.0 0.0.0.3` は /30 を許可し、172.16.4.4 自体は 172.16.4.4–4.7 のブロックなので **マスクが合わず**、正しくサーバを許可できない。  
    - C: `snmp-server host 172.16.4.4 ccnp` は **トラップ送信先の NMS を指定するコマンド**であり、NMS からの SNMP ポーリングを許可するものではない。 [web:436]  
    - D: `snmp-server community ccnp` だけでは ACL の制限が外れず、ACL 4 による拒否状況は変わらない。  

- 試験向けの覚え方フレーズ

  - **「`snmp-server community <COMM> RO <ACL>` がある → SNMP 許可は ACL 側で決まる → “NMS の IP を ACL に permit したか？”」**  
  - SNMP 周りのコマンド用途:
    - `snmp-server community` = 「パスワード＋誰を許可するか」  
    - `snmp-server host`      = 「どこへトラップを送るか」  
    → NMS からの “取得” 問題なら **ACL の permit 行を直す**のが基本。

***
QUESTION 594 解説

- 問題文（英語）:  
An enterprise operations team must monitor all application server traffic in the data center. The team finds that traffic coming from the hub site from R3 and R6 is monitored successfully, but traffic destined to the application server is not monitored. Which action resolves the issue?

A.  
R4(config)#int gigabitEthernet 1/0  
R4(config-if)#ip flow ingress  

B.  
R3(config)#int gigabitEthernet 0/0  
R3(config-if)#ip flow egress  

C.  
R4(config)#int gigabitEthernet 2/0  
R4(config-if)#ip flow egress  

D.  
R1(config)#int gigabitEthernet 0/0  
R1(config-if)#ip flow egress  

Correct Answer: C [web:444][web:226]

- 問題文要約（日本語）:  
データセンタの **アプリケーションサーバ向きトラフィックをすべて NetFlow 監視したい**。  
ハブサイト（R3/R6）から入ってくるトラフィックは監視できているが、**アプリケーションサーバ宛てのトラフィックが NetFlow に出てこない**。  
どのインタフェースにどのコマンドを追加すべきかを問う。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 元の出題では R4 の `show ip flow interface` 出力が示されており、  
    - R4 の `GigabitEthernet2/0` には **`ip flow ingress` は設定済みだが、`ip flow egress` は未設定**。 [web:348][web:334]
  - ハブ側からのトラフィックは、R4 の g2/0 に対して「**入り（ingress）方向**」で NetFlow が取れているため、  
    - **R3/R6 からの“サーバへの上り”トラフィック**は問題なく監視されている。  
  - 一方、「**サーバ宛てトラフィックが監視されない**」ということは、  
    - R4 の g2/0 からサーバへ向かう **出て行く方向（egress）のフロー**が NetFlow で記録されていないことを意味する。  
  - よって、**R4 の g2/0 に `ip flow egress` を追加**して、  
    - そのインタフェースの出方向トラフィックも NetFlow 対象にすれば、サーバ宛てトラフィックが監視できるようになる。  
    → これが選択肢 **C** の設定。 [web:444][web:213]
  - 他の選択肢:
    - A: R4 g1/0 の ingress を有効化しても、サーバ接続に使われていないインタフェースなら意味がない。  
    - B: R3 g0/0 egress を有効にしても、フローが二重カウントになるだけで、「サーバ直前のトラフィック全部を R4 で見る」という要件に合わない。  
    - D: R1 g0/0 で egress を有効にしても、DC 側トラフィックの出口（R4 g2/0）監視には不十分。  

- 試験向けの覚え方フレーズ

  - **「NetFlow＝`ip flow ingress/egress`、どの“向き”を見たいかでインタフェースと方向を選ぶ」**  
  - 典型パターン:
    - 「サーバ直前の“全トラフィック”を見たい」 → **サーバにつながる IF に ingress＋egress**  
    - すでに ingress だけ入っていて「片方向が見えない」と言われたら、**同じ IF に `ip flow egress` を追加**、という流れで覚える。

***
QUESTION 595 解説

- 問題文（英語）:  
What must a network architect consider for RTs when planning for a single customer full-mesh VPN in an MPLS Layer 3 network?

A. Each RT value must be identical to an RD value within the same VPN.  
B. RT must be globally identical within the same VPN.  
C. RT values must be different from the RD values in the same VPN.  
D. RT must be globally unique within the same VPN.  

Correct Answer: B [web:390][web:455]

- 問題文要約（日本語）:  
MPLS L3VPN で「**単一顧客・フルメッシュ型 VPN**」を設計する際、**RT（Route Target）設定について何を考慮すべきか**を問う。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - MPLS L3VPN では、PE 間で交換される VPNv4 ルートに **RT（Route Target 拡張コミュニティ）** を付与し、  
    - 各 VRF で「**どの RT を import / export するか**」によって、  
    - どのサイト同士が経路を共有するか（フルメッシュ／ハブ＆スポーク／共有サービスなど）を制御する。 [web:455][web:459]
  - 単一顧客・フルメッシュ VPN の場合:
    - 全サイトが **すべての他サイトのルートを学習**する必要がある。  
    - したがって、その顧客用 VRF では、**すべての PE で同じ RT を export し、同じ RT を import** する必要がある。  
    → つまり、「**同一 VPN 内では RT が“グローバルに同一”である」ことが条件になる。 [web:462][web:461]
  - 選択肢 B の “RT must be globally identical within the same VPN” はこの設計要件を端的に表している。  
  - 他の選択肢:
    - A: RD と RT は役割が違う。RD は「VPNv4 アドレスの一意性確保」、RT は「どの VRF にインポートするかのポリシー」で、値を一致させる必要はない。 [web:455][web:282]  
    - C: 「RT は RD と異なる値でなければならない」という制約もない（形式が同じ ASN:NN なので、同じ値にしても問題ないが、設計上分けることが多いだけ）。  
    - D: “globally unique” は「他 VPN とも被ってはならない」というニュアンスだが、  
      - 実際には **“VPN 内で一貫性があること”** が本質であり、SP 内で RT 値を再利用する設計もあり得るため、必須条件ではない。  

- 試験向けの覚え方フレーズ

  - **「フルメッシュ VPN = 全サイトが“同じ RT を export/import” → RT はその VPN 内で“揃える”」**  
  - 用語整理:
    - **RD**: VPNv4 アドレスを一意にするタグ（ユニークさが重要）。  
    - **RT**: どの VRF に経路を輸入／輸出するか決めるラベル（**フルメッシュなら全サイト同じ RT**）。  

***
QUESTION 596 解説

- 問題文（英語）:  
Which router attaches the VPN label to incoming packets from CE routing?

A. PE router  
B. P router  
C. CE router  
D. core router  

Correct Answer: A [web:226][web:469]

- 問題文要約（日本語）:  
MPLS L3VPN で、**CE から入ってきたパケットに VPN ラベル（inner label）を付けるのはどのルータか**を問う。  

- 正解: **A（PE router）**

- 解説（なぜ A が正しいか）

  - MPLS L3VPN の基本的なフォワーディング: [web:468][web:469]
    - CE → PE: CE は普通の IP パケットを PE に渡す（ラベルは付けない）。  
    - **PE（入口 PE）**: 受信した IP パケットを VRF でルックアップし、  
      - **VPN ラベル（inner label）** と  
      - トランスポートラベル（outer label）  
      をスタックして MPLS コアに送る。  
    - P ルータ: outer ラベルだけ見てスイッチングする。VPN ラベルには触らない。  
    - PE（出口 PE）: outer ラベルを剥がし、inner ラベルでどの VRF かを判別して CE へプレーンな IP パケットを渡す。  
  - したがって、「**CE から入ってきたパケットに VPN ラベルを最初に付ける（push する）のは、CE 直結の PE ルータ**」となる。  
  - 他の選択肢:
    - P router: コアルータであり、**VPN ラベルを付けない／解釈しない**。outer ラベルでのみスイッチ。  
    - CE router: 顧客側ルータ。MPLS ラベル処理は行わない。  
    - core router: 一般的には P を指し、同じく VPN ラベルを付ける役割ではない。  

- 試験向けの覚え方フレーズ

  - **「CE は IP だけ、PE が“VPN ラベル＋トランスポートラベル”を二段重ねで push」**  
  - 一行イメージ:
    - CE → **PE（ラベル二枚付け）** → P（outer でスイッチ） → PE（ラベル剥がし） → CE  

***
QUESTION 597 解説

- 問題文（英語）:  
Which router takes an active role between two LDP neighbors when initiating LDP session negotiation and LDP TCP connection establishment?

A. with one interface in the MPLS backbone  
B. with the larger number of LDP TCP neighbors  
C. with the lowest IP address  
D. with the higher IP address  

Correct Answer: D [web:475][web:476]

- 問題文要約（日本語）:  
2 台の LDP ネイバー間で **LDP セッション確立（TCP 接続＋パラメータ交渉）を始める際に、どちらが “active” 役として TCP 接続を張りに行くか**を問う。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - Cisco の LDP ドキュメントでは、LDP セッション確立手順について次のように説明されている。 [web:475][web:476]
    - 2 台の LSR は、自分が **active**／**passive** のどちらの役割を取るかを決める。  
    - active 役のルータが **LDP 用の TCP セッション（port 646）を確立**し、LDP セッションパラメータのネゴシエーション（Initialization メッセージ送信）を開始する。  
    - 役割決定は、双方の **transport address（通常 LDP Router-ID、IP アドレス）を比較**して行う。  
    - **“IP アドレスが高い方” が active 役**となり、TCP コネクションを張りに行く。  
  - 従って、選択肢の中では **D: with the higher IP address** が正しい。  
  - 他の選択肢:
    - A: MPLS バックボーン内のインタフェース数は LDP の active/passive 役割決定に無関係。  
    - B: LDP TCP neighbors の数も役割判定には使用されない。  
    - C: “lowest IP address” は OSPF の DR/BDF 選出などで見かけるパターンだが、LDP の active 役決定とは逆。  

- 試験向けの覚え方フレーズ

  - **「LDP TCP を“張りに行く”のは、IP が“高い”方」**  
  - 似たパターン:
    - HSRP: priority 同値のとき **高い IP が active**  
    - LDP: **高い IP が active**  
    → 「**LDP も HSRP も“高い IP が前に出る”**」とセットで覚えると記憶に残りやすい。

***
QUESTION 598 解説

- 問題文（英語）:  
An engineer must troubleshoot an issue affecting the communication from router R2 to the TACACS server. Which configuration resolves the issue?

A. R1(config)#tacacs-server packet maxsize 43974  
B. R2(config)#tacacs-server packet maxsize 43974  
C. R2(config)#tacacs server advrt  
   R2(config-server-tacacs)#key xyz123  
D. R1(config)#tacacs server advrt  
   R1(config-server-tacacs)#key xyz123  

Correct Answer: C [web:481][web:226]

- 問題文要約（日本語）:  
R2 から TACACS サーバへの通信に問題があり、TACACS 認証が失敗している。  
どの設定を投入すれば、この通信問題が解消されるかを問う。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 元のデバッグログでは、R2 が TACACS サーバと通信しようとしているが、**TACACS サーバ側で想定しているパケットサイズやキーと一致しない**といったエラーが出ているケースになっている。  
  - 試験解説では、「問題は R2 が TACACS サーバとして `advrt`（名前）を定義していない／正しい key を設定していないため、R2 から TACACS サーバへの認証パケットが正しく処理されない」点にあるとされている。 [web:481][web:490]
  - TACACS+ を使う際は、ルータ側で以下のように **サーバを定義し、同じ key を設定**する必要がある。 [web:488][web:489]
    - `tacacs server <NAME>`  
      - `address ipv4 <server-ip>`（問題文の省略部に既にある前提）  
      - `key <shared-secret>`  
  - 選択肢 C は、R2 上で `tacacs server advrt` を定義し、そこに **サーバと一致した共有鍵 `key xyz123`** を設定している。  
    - これにより、R2 から TACACS サーバへのパケットが正しい鍵情報で暗号化され、TACACS 認証が成功するようになる。  
  - 他の選択肢:
    - A / B: `tacacs-server packet maxsize 43974` は **旧書式かつパケット長上限の調整**であり、鍵不一致やサーバ定義不足の根本原因を解決しない。  
    - D: R1 側に TACACS サーバ定義を追加しても、「R2 から TACACS サーバへの通信問題」という本問の状況には無関係。  

- 試験向けの覚え方フレーズ

  - **「TACACS 認証トラブル → まず“サーバ定義”と“key 一致”を疑う」**  
  - コマンドパターン:
    - 新形式: `tacacs server <NAME> / key <secret>`  
    - 旧形式: `tacacs-server host <IP>` ＋ `tacacs-server key <secret>`  
  - 「ログに packet size や bad AUTHEN packet が出ていても、多くは **key mismatch or サーバ定義ミス**のパターン問題」として覚えておくと解きやすい。

***
QUESTION 599 解説

- 問題文（英語）:  
A network engineer lost remote access to the router due to a network problem. The engineer used the console to access the router and noticed continuous logs on the console terminal. Which configuration limits the number of log messages on the console to critical and higher severity level messages?

A. term no monitor  
B. no logging console  
C. logging console 2  
D. logging console 5  

Correct Answer: C [web:494][web:495]

- 問題文要約（日本語）:  
リモートアクセスができなくなり、コンソール接続すると **ログメッセージが大量に流れて作業しにくい**。  
コンソールに出すログを「**critical（重大障害）以上のレベルだけ**」に制限するには、どの設定を入れればよいかを問う。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - Cisco IOS の syslog severity レベルは次の通り。 [web:497][web:499]
    - 0: emergencies  
    - 1: alerts  
    - 2: **critical**  
    - 3: errors  
    - 4: warnings  
    - 5: notifications  
    - 6: informational  
    - 7: debugging  
  - `logging console <level>` は「**指定したレベル以下（数値が小さいほど重大）をコンソールに出す**」設定。  
    - `logging console 2` → level 0〜2（emergencies, alerts, critical）のみ出力。  
    → 問題文の「critical and higher severity」をちょうど満たす。 [web:495]
  - 他の選択肢:
    - A: `term no monitor` は **vty セッション（Telnet/SSH）のログ表示を止める**だけで、コンソールには影響しない。  
    - B: `no logging console` は **コンソールへのログを完全に止める**。critical だけ残す条件を満たさない。  
    - D: `logging console 5` は 0〜5（critical 〜 notifications）まで出してしまい、制限が緩すぎる。  

- 試験向けの覚え方フレーズ

  - **「syslog レベル: 0=最重, 数字が大きいほど軽い」「`logging console <N>` は 0〜N を出す」**  
  - なので:
    - 「critical 以上」→ level 2 → **`logging console 2`**  
    - 「warning 以上」→ level 4 → `logging console 4`  
  - `no logging console` は「一切見たくないとき」、  
    `term monitor / no monitor` は「vty だけ」に効く、と区別して覚える。  

***
QUESTION 600 解説

- 問題文（英語）:  
What are the two benefits of using BFD? (Choose two.)

A. supports all routing protocols  
B. supports UDLD failure  
C. forwarding path failure detection  
D. subsecond failure detection  
E. synchronous path determination  

Correct Answer: C, D [web:506][web:512]

- 問題文要約（日本語）:  
BFD（Bidirectional Forwarding Detection）を使うことによる **利点を 2 つ**選べ、という問題。  

- 正解: **C と D**

- 解説（なぜ C と D が正しいか）

  - Cisco の公式解説では、BFD は次のような特徴を持つとされている。 [web:512][web:511]
    - **「forwarding path failure detection」** プロトコルであり、  
      - 2 台のルータ間の転送パス（L1/L2/L3 を含む経路）に障害があるかを高速に検出する。  
    - **サブ秒（subsecond）での障害検出**が可能。  
      - EIGRP/OSPF/IS-IS などのタイマを単独で下げるよりも、  
      - BFD を使った方が **1 秒未満の検出時間**を一貫して実現しやすい。 [web:509][web:508]
  - よって:
    - C: **forwarding path failure detection** → BFD の定義そのもの。  
    - D: **subsecond failure detection** → BFD の典型的メリット。  
  - 他の選択肢:
    - A: BFD は多くのルーティングプロトコル（OSPF、IS-IS、BGP など）をサポートするが、**「すべて」のルーティングプロトコルをサポートするわけではない**（古い RIPv1/IGRP などでは非対応）。 [web:512][web:514]  
    - B: UDLD は物理リンクの uni-directional 検出用別プロトコルで、BFD が UDLD の代替を提供するわけではない。  
    - E: “synchronous path determination” という機能は BFD の正式な特徴ではない（経路決定はあくまで上位のルーティングプロトコルが行う）。  

- 試験向けの覚え方フレーズ

  - **「BFD = “F” の字どおり Forwarding の Failure Detection、しかも Subsecond」**  
  - キーワード対応:
    - F = **Forwarding path failure detection**（C）  
    - D = Detection speed → **subsecond failure detection**（D）  
  - 「BFD がやるのは“検出”まで、経路切り替えは IGP/BGP が担当」とセットで覚えておくと、似た選択肢に惑わされにくい。

***
QUESTION 601 解説

- 問題文（英語）:  
A prefix of 10.1.1.0/31 is assigned for the DWDM circuit between R1 and R2 to connect the two attached VRFs on each router. Which configuration provides the required connectivity on R1?

A.  
interface Ethernet0/0  
 no ip address  
!  
interface Ethernet0/0.10  
 encapsulation dot1Q 10  
 ip vrf forwarding CCNP  
 ip address 10.1.1.0 255.255.255.252  
!  
interface Ethernet0/0.20  
 encapsulation dot1Q 20  
 ip vrf forwarding CCNA  
 ip address 10.1.1.0 255.255.255.252  

B.  
interface Ethernet0/0  
 ip address 10.1.1.0 255.255.255.254  
!  
interface Loopback0  
 ip vrf forwarding CCNP  
!  
interface Loopback1  
 ip vrf forwarding CCNA  
!  
interface Tunnel0  
 tunnel source Loopback0  
 tunnel mode gre ip  
 ip vrf forwarding CCNP  
 tunnel destination 10.1.1.1  
!  
interface Tunnel1  
 tunnel source Loopback1  
 tunnel mode gre ip  
 ip vrf forwarding CCNA  
 tunnel destination 10.1.1.1  

C.  
interface Ethernet0/0  
 no ip address  
!  
interface Ethernet0/0.10  
 encapsulation dot1Q 10  
 ip vrf forwarding CCNP  
 ip address 10.1.1.0 255.255.255.254  
!  
interface Ethernet0/0.20  
 encapsulation dot1Q 20  
 ip vrf forwarding CCNA  
 ip address 10.1.1.0 255.255.255.254  

D.  
interface Ethernet0/0  
 ip address 10.1.1.0 255.255.255.252  
!  
interface Loopback0  
 ip vrf forwarding CCNP  
!  
interface Loopback1  
 ip vrf forwarding CCNA  
!  
interface Tunnel0  
 tunnel source Loopback0  
 tunnel mode gre ip  
 ip vrf forwarding CCNP  
 tunnel destination 10.1.1.1  
!  
interface Tunnel1  
 tunnel source Loopback1  
 tunnel mode gre ip  
 ip vrf forwarding CCNA  
 tunnel destination 10.1.1.1  

Correct Answer: C [web:213][web:520]

- 問題文要約（日本語）:  
R1–R2 間の DWDM 回線に **10.1.1.0/31** のプレフィックスが割り当てられており、  
各ルータ上の 2 つの VRF（CCNP / CCNA）をこの回線で接続したい。  
R1 側はどのコンフィグにすべきかを問う。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 10.1.1.0/31 は、RFC 3021 に基づく **/31 P2P リンク**用プレフィックスであり、  
    - ネットマスクは **255.255.255.254** を使う。  
    - 利用可能なアドレスは **10.1.1.0 と 10.1.1.1 の 2 つのみ**。 [web:520]
  - この回線上に 2 つの VRF を載せるため、  
    - 物理 IF e0/0 は `no ip address` とし、  
    - サブ IF を VLAN 10 / 20 に切って、それぞれ VRF CCNP / CCNA に紐づける、という設計が合理的。  
  - 選択肢 C はまさにその構成で、  
    - `Ethernet0/0` は `no ip address`  
    - `Ethernet0/0.10` に `ip vrf forwarding CCNP` ＋ `ip address 10.1.1.0 255.255.255.254`  
    - `Ethernet0/0.20` に `ip vrf forwarding CCNA` ＋ `ip address 10.1.1.0 255.255.255.254`  
    → /31 の正しいマスクを使っており、**唯一「プレフィックス指定が正しい」選択肢**になっている。 [web:213]
  - 他の選択肢:
    - A: サブ IF のアドレスに **255.255.255.252 (/30)** を使っており、問題の指定 (/31) と不一致。  
    - B / D: トンネル構成だが、Loopback に IP アドレスが無い、/31 の使い方が不適切など、  
      - そもそも「DWDM 上の P2P /31 で VRF 間を直接つなぐ」という問題意図から外れている。  
  - 解説にも「/31 は 255.255.255.254、トンネル案は Loopback の IP 未設定などで不正なので、形式的にコマンドが正しいのは C のみ」とある。 [web:520]

- 試験向けの覚え方フレーズ

  - **「/31 → マスクは 255.255.255.254、アドレスは 2 個だけ（ネットワーク／ブロードキャストという概念は実質無し）」**  
  - デザインパターン:
    - P2P 回線に /31 を振るときは、  
      - **物理 IF 無し＋サブ IF（VLAN）に /31 を直付け**、  
      - トンネルで回りくどくやる必要はない。  
  - 「/30 と /31 のマスクを書き間違える選択肢」が頻出なので、  
    - `/30 = 255.255.255.252`  
    - `/31 = 255.255.255.254`  
    を一気に暗記しておくと良い。

***
QUESTION 602 解説

- 問題文（英語）:  
Customer B has decided not to receive any routes from R1 that originated outside the AS 100. Which AS path access list must the engineer choose to meet this requirement?

A. ip as-path access-list 1 permit _100_  
B. ip as-path access-list 1 permit _100$  
C. ip as-path access-list 1 permit ^100$  
D. ip as-path access-list 1 permit ^10[0-9]*$  

Correct Answer: C [web:530][web:534]

- 問題文要約（日本語）:  
Customer B は、「**AS 100 から“自分で”起点となっている経路だけ**を R1 から受け取りたい（= AS 100 外部起点の経路は一切受け取りたくない）」と要求している。  
どの AS-path access-list を使えばよいかを問う。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - BGP の AS-PATH の最後の AS 番号は「**その経路を起点（origin）した AS**」を表す。  
  - 「AS 100 **起点**の経路だけ許可したい」ということは、  
    - AS-PATH 全体が **「100 だけ」**で構成されている、つまり **`100` 以外の AS を一切含まない**パスだけを許可する必要がある。  
  - Cisco の AS-path 正規表現で: [web:534][web:538]
    - `^` は「行頭」、`$` は「行末」を意味する。  
    - `^100$` は「**AS-PATH が 100 だけ**」という意味になる（前後に他の AS 番号なし）。  
    → これにマッチする経路は「AS 100 由来で、他 AS を経由していない＝AS 100 が origin」の経路のみ。  
  - よって、AS 100 外部起点の経路をすべて排除し、AS 100 自身が起点の経路だけ許可したいという要件を満たすのは **C**。  
  - 他の選択肢:
    - A: `_100_` は「AS-PATH のどこかに 100 が含まれる」ことしか保証しない。  
      - 例: `200 100 300` のような「別 AS 起点」の経路も通ってしまう。  
    - B: `_100$` は「AS-PATH が 100 で終わる（最後が 100）」だが、  
      - 例: `200 100` のように「AS 200 起点で AS 100 を経由」する経路も許可されてしまう。  
    - D: `^10[0-9]*$` は「100〜109, 1000〜など“10 から始まる AS 番号の列のみ”」という広すぎるマッチで、AS 100 以外の AS も含まれてしまう。  

- 試験向けの覚え方フレーズ

  - **「`^AS$` = その AS 自身が origin の経路だけ」「`_AS$` = 最後が AS だが途中に他 AS もあり得る」「`_AS_` = どこに出てきても OK」**  
  - Transit 防止・自 AS 起点のみ許可したいときは、  
    - **`^自AS$` を permit して、それ以外 deny**、というパターンが鉄板。  

***
QUESTION 603 解説

- 問題文（英語）:  
Which action ensures that 10.10.10.0/24 reaches 10.10.20.0/24 through the direct link between R1 and R2?

A. configure OSPF link cost to 1 between R1 and R2  
B. configure R1 and R2 links under area 1  
C. configure R1 and R2 LAN links as nonpassive  
D. configure OSPF path cost to 3 between R1 and R2  

Correct Answer: B [web:542]

- 問題文要約（日本語）:  
10.10.10.0/24 のトラフィックを、R1–R2 間の **直接リンク経由**で 10.10.20.0/24 へ到達させたい。  
どの設定を行えば、その直接リンクが経路として確実に選ばれるかを問う。  
（図では 10.10.10.0/24 も 10.10.20.0/24 も OSPF **Area 1** 内のネットワークで、R1–R2 のリンクだけが別エリアになっている構成。）  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - OSPF のルート優先順位は、コストだけでなく **ルートタイプ（intra-area / inter-area / external）** によって決まる。  
    - **Intra-area（同一エリア内ルート）** が最も優先され、  
    - その次に Inter-area（Area 間）、さらに external へと優先度が下がる。 [web:550]
  - 本問では、10.10.10.0/24 と 10.10.20.0/24 はどちらも **Area 1 に所属している**。  
    - ところが、R1–R2 直接リンクが別のエリア（例: Area 0）になっていると、  
      - R1 から見た 10.10.20.0/24 は **inter-area ルート**となり、  
      - R3 経由などの **Area 1 内経路（intra-area ルート）があればそちらが優先**されてしまう。  
  - したがって、「**R1–R2 直接リンク自体を Area 1 に所属させる**」ことで、  
    - R1–R2 間の経路も 10.10.10.0/24 ⇔ 10.10.20.0/24 と同じ **intra-area ルート**になり、  
    - direct link が選ばれるようになる。  
    → これを実現するのが **B: configure R1 and R2 links under area 1**。 [web:542]
  - 他の選択肢:
    - A/D: コスト変更だけでは、「inter-area より intra-area を優先」という OSPF のルートタイプ優先度を覆せない。  
    - C: LAN リンクの passive 設定は「Hello を止める／再開する」だけで、  
      - 直接リンクが別エリアにある限り、intra-area vs inter-area の優先順位問題は解決しない。  

- 試験向けの覚え方フレーズ

  - **「OSPF は “コストの前にルートタイプ”：Intra-area > Inter-area > External」**  
  - 「**直接リンクを本当に使わせたかったら、まず同じエリアに入れろ**、コスト調整はその次」という順番で考えると、この手の問題は解きやすい。

***
QUESTION 604 解説

- 問題文（英語）:  
The network operation team opened a trouble ticket stating that users connected to R2 and R4 cannot reach server 1. Some partial configuration and show commands are included in the ticket.  
Which configuration resolves the issue?

A. R2#  
 router eigrp 100  
 distance 90 10.1.95.2 0.0.0.0 10  
 !  
 no ip route 10.71.2.100 255.255.255.0 Null0 name SERVER1  

B. R4#  
 router eigrp 100  
 distance 160 10.29.89.2 0.0.0.0 10  
 !  
 no ip route 10.71.2.0 255.255.254.0 Null0 name SERVER1  

C. R4#  
 router eigrp 100  
 no distance 200 10.29.89.2 0.0.0.0 10  
 !  
 no access-list 10 permit 10.71.2.0 0.0.0.255  

D. R2#  
 router eigrp 100  
 distance 170 10.1.95.2 0.0.0.0 10  
 !  
 no ip route 10.71.2.100 255.255.255.255 Null0 name SERVER1  

Correct Answer: C, D [web:272]

- 問題文要約（日本語）:  
R2 / R4 にぶら下がるユーザが **Server1（10.71.2.100）に到達できない**。  
チケットには部分的な EIGRP 設定とルーティングテーブルがあり、  
R2・R4 のどの設定を変えれば問題が解消するかを問う（複数選択）。  

- 正解: **C と D**

- 解説（なぜ C と D が正しいか）

  - 状況整理（解説に基づく）: [web:272]
    - R2 には静的 Null0 ルートがある。  
      - `ip route 10.71.2.100 255.255.255.255 Null0 name SERVER1`  
      → R2 に 10.71.2.100 宛てのパケットが来ると、**Null0 で破棄される**。  
    - R4 は EIGRP で 10.71.2.0/24 を R3 経由でも学べる構成だが、  
      - EIGRP に `distance 200 10.29.89.2 0.0.0.0 10` が入っており、  
      - そのせいで R3 からの経路がインストールされず、代わりに **R2 経由のルートを選んでいる**。  
      → 結果として、R4 から Server1 宛てのトラフィックは R2 に送られ、R2 で Null0 に捨てられる、というループ構造になっている。  
  - 解決策は 2 本立て:
    - ① **R4 側で不要な distance を削除し、正しい EIGRP 経路（R3 経由）を使わせる**。  
      - これが C の `no distance 200 10.29.89.2 0.0.0.0 10`。  
      - さらに `no access-list 10 permit 10.71.2.0 0.0.0.255` で不要なフィルタも外している。  
    - ② **R2 側で誤って設定された Null0 host route を削除する**。  
      - 正しい宛先は 10.71.2.100/32 なので、  
      - D の `no ip route 10.71.2.100 255.255.255.255 Null0 name SERVER1` が必要。  
      - A のように /24 マスクで消そうとしても、元の /32 と一致せず、Null0 ルートは残ってしまう。  
  - したがって、**C（R4 側の distance/ACL 修正）＋ D（R2 の Null0 host route 削除）** の両方が揃って初めて、  
    - R4 は R3 経由で正しい経路を選び、  
    - R2 も Server1 宛パケットを破棄しなくなる。  

- 試験向けの覚え方フレーズ

  - **「Null0 静的ルートが“名前付き SERVER1”とかで出てきたら、真っ先に疑え」**  
  - ルーティング問題で distance コマンドが入っていたら:
    - 「**どのネイバーからの経路が抑制されているか**」  
    - 「結果として“遠回り or ブラックホール”になっていないか」  
    を確認し、  
    - **`no distance ...` で元に戻すパターン問題**として押さえておくと解きやすい。

***
QUESTION 606 解説

- 問題文（英語）:  
R1 should have BGP routes as “i”. Which configuration resolves the issue?

A. R7  
   route-map RC permit 10  
    match ip address 1  
    set origin igp  

B. R1  
   route-map RC permit 10  
    match ip address 1  
    set origin igp  

C. R1  
   route-map RC permit 10  
    match ip address 1  
    set metric 1  

D. R7  
   route-map RC permit 10  
    match ip address 1  
    set metric 1  

Correct Answer: A [web:12][web:578]

- 問題文要約（日本語）:  
R1 の BGP テーブル上で、特定プレフィックスの **Origin が “?” になっているが、“i（IGP）” として表示されてほしい**。  
どの route-map 設定を入れればよいかを問う。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - BGP の **Origin** フィールドは 3 種類。 [web:579]
    - `i`  : IGP（`network` コマンドなどで自 AS 内から起点）  
    - `e`  : EGP（古い Exterior Gateway Protocol）  
    - `?`  : incomplete（再配布など起点が不明）  
  - ルートの Origin は、**そのルートを生成したルータ側で決まる**。  
    - ある AS 内でルートを再配布すると、デフォルトでは Origin は `?` になるが、  
    - `set origin igp` を使うと、その AS から見て **IGP 起点として扱う**ことができる。 [web:568]
  - 本問の意図（Networktut/ENARSI 問題集解説）では、  
    - これらのルートは **R7 で生成／再配布されており、そこから R1 へ伝播**している前提になっている。 [web:12]  
    - Origin を `i` に変えたい場合、**ルートを生成している側（R7）で `set origin igp` を行い、それを AS 内全体に伝播させる**のが自然。  
    - R1 側で受信後に Origin を書き換えるのではなく、**起点となる R7 で Origin を “i” にしてしまう**ほうが、他のルータにも一貫した情報を提供できる。  
  - 選択肢 A は、R7 で `route-map RC` に `set origin igp` を定義し、  
    - ACL1（`match ip address 1`）でマッチしたルートの Origin を IGP (`i`) に変更して広告する構成。  
    → これにより、**R1 を含む下流の BGP ルータすべてで、そのルートが “i” と表示される**。  
  - 他の選択肢:
    - B: R1 側で Origin を `i` に変えるアプローチだが、  
      - 問題文は「R1 で `i` に見せる」ことだけ言っている一方で、  
      - 通常は **ルート生成側で一貫した属性を付与する**設計が推奨される。  
      - 出典の回答／解説も R7 側での設定（A）を正解としている。 [web:12]  
    - C / D: `set metric 1` は MED（または IGP メトリック）を変えるだけで、Origin は `?` のまま。  

- 試験向けの覚え方フレーズ

  - **「BGP Origin はルート“起点側”で決める。`?` → `i` にしたければ、`set origin igp` を“再配布元ルータ”の route-map に入れる」**  
  - キーワード:
    - `set origin igp` → Origin フィールドを `i` に変更  
    - metric や local-preference をいじっても **Origin は変わらない**  

***
QUESTION 607 解説

- 問題文（英語）:  
A customer reported a failure and intermittent disconnection between two office buildings, site X and site Y. The network team finds that site X and site Y are exchanging email application traffic with the data center network. Which configuration resolves the issue between site X and site Y?

A.  
R2(config)#router bgp 65101  
R2(config-router)#no timers bgp 3 15  

B.  
R2(config)#router bgp 65101  
R2(config-router)#neighbor 10.10.10.5 update-source loopback 0  

C.  
R5(config)#router bgp 65101  
R5(config-router)#neighbor 10.10.10.2 next-hop-self  

D.  
R2(config)#router bgp 65101  
R2(config-router)#neighbor 10.10.10.5 next-hop-self  

Correct Answer: B [web:12][web:580]

- 問題文要約（日本語）:  
サイト X（R2）とサイト Y（R5）間の通信が断続的に切れる。  
図より、R2・R5 間は **Loopback アドレス（10.10.10.2 / 10.10.10.5）を使って BGP ピアリング**しているが、  
ネイバー形成が拒否されているログが示されている。  
どの設定を入れれば R2–R5 間の BGP セッションが安定し、X–Y 間の通信が回復するかを問う。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - ルータ同士が **ループバック IP で BGP ピアリング**する場合、  
    - BGP の TCP セッション（デフォルト port 179）は **送信元 IP として、どのインタフェースのアドレスを使うか**を明示する必要がある。  
    - これを指定するのが `neighbor x.x.x.x update-source <interface>` コマンド。 [web:583][web:585]
  - 本問では:
    - R2 のループバック 0: 10.10.10.2  
    - R5 のループバック 0: 10.10.10.5  
    - BGP の neighbor 設定は `neighbor 10.10.10.5 remote-as 65101` などになっているが、  
      - **R2 側が update-source を指定していないため、実際の BGP TCP パケットの送信元 IP が物理 IF になってしまい、R5 が期待する 10.10.10.2 と不一致**になる。  
    - その結果、`Active/Idle` を繰り返し、ネイバー形成が不安定になる。 [web:581][web:582]
  - 選択肢 B の `neighbor 10.10.10.5 update-source loopback 0` を R2 に入れると、  
    - R2 → R5 への BGP パケットの送信元 IP が **Loopback0（10.10.10.2）** になり、  
    - R5 側の neighbor 10.10.10.2 設定と整合が取れて、BGP セッションが安定して確立される。  
  - 他の選択肢:
    - A: BGP タイマを戻しても、**update-source 不一致という根本原因**は解決しない。  
    - C / D: `next-hop-self` は「伝播する経路の next-hop 属性を書き換える機能」で、  
      - **BGP ネイバー形成そのものには関与しない**ため、問題の症状（ネイバー不安定）を解決できない。 [web:583]

- 試験向けの覚え方フレーズ

  - **「ループバックIPでBGPピアリング → 両端とも `neighbor <loopback> update-source loopback 0` を必ずセット」**  
  - 似たパターン:
    - ネイバーが Active/Idle を繰り返す＋loopback で張ろうとしている  
      → **まず update-source の有無を疑う**。  
  - `next-hop-self` は「経路の next-hop 修正用」、  
    **ネイバーが上がらない問題には効かない**と切り分けて覚える。

***
QUESTION 608 解説

- 問題文（英語）:  
The R2 OSPF route 10.2.2.0/24 shows in the R1 EIGRP routing table without route redistribution performed between OSPF and EIGRP routing protocols. Which configuration is required on router R2 to resolve the issue?

A. add the passive-interface FastEthernet 1/0 command in EIGRP 100  
B. add the no auto-summary command in EIGRP 100  
C. replace the network 10.0.0.0 command with FastEthernet0/0 network in EIGRP 100  
D. add the passive-interface FastEthernet 0/0 command in OSPF 1  

Correct Answer: A [web:459][web:590]

- 問題文要約（日本語）:  
本来 OSPF だけで広告されるはずの R2 のネットワーク **10.2.2.0/24** が、  
ルート再配布を入れていないのに **R1 の EIGRP ルーティングテーブルに “EIGRP internal” として載ってしまっている**。  
この不正な経路混入を止めるには、R2 にどの設定を入れるべきかを問う。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 解説によると、この症状は「**10.2.2.0/24 セグメントで OSPF だけ使うべきところに、誤って EIGRP もしゃべらせてしまっている**」ために起きている。 [web:459]
    - R2–R3 間の 10.2.2.0/24 で、R2 側が EIGRP 100 を有効にしている。  
    - その結果、R2 と R3 が **EIGRP ネイバーを形成し、10.2.2.0/24 を EIGRP で広告**してしまい、  
      - R1 は R3 経由でこのネットワークを EIGRP internal として学習している。  
  - このセグメントは設計上「OSPF 専用」にしたいので、R2 では **そのインタフェースで EIGRP のネイバーを張らせない**必要がある。  
    - EIGRP で特定インタフェースのネイバー形成を禁止する定番方法が `passive-interface`。 [web:595][web:594]
  - 選択肢 A の  
    - `router eigrp 100`  
      `passive-interface FastEthernet 1/0`  
    によって、F1/0 からは **EIGRP Hello を送信しなくなり、ネイバーが形成されない**。  
    - その結果、10.2.2.0/24 は EIGRP では広告されず、R1 の EIGRP テーブルからも消える。  
  - 他の選択肢:
    - B: `no auto-summary` は古い Classful 境界でのサマリ抑制用であり、「プロトコルを跨いだ誤広告」という今回の問題には無関係。  
    - C: `network 10.0.0.0` を細かく書き換えても、「F1/0 で EIGRP をしゃべっている」事実が変わらない限り、誤広告は続く。  
    - D: OSPF 側で `passive-interface` を設定すると、今度は **OSPF がそのネットワークを広告しなくなり、本来の設計（OSPFで配布）を壊す**。  

- 試験向けの覚え方フレーズ

  - **「“このネットワークは OSPF 専用なのに EIGRP にも見えてる” → EIGRP 側の `passive-interface` で Hello を止める」**  
  - ルール:
    - 複数プロトコルが同じインタフェースで動くとき、  
      - 「どのプロトコルで経路を配りたいか」を決め、  
      - **不要なプロトコルは passive-interface で黙らせる**のが定石。  

***
QUESTION 609 解説

- 問題文（英語）:  
A switch has been configured to provide DHCP relay on VLAN100 to a server with an IP address of 10.1.1.1. The DHCP server is sending syslog reports of multiple TFTP requests that also originate from the switch. As a result, the server CPU exceeded a configured threshold. Which action does the network administrator recommend to bring the server CPU threshold down?

A. Configure the switch with no ip forward-protocol udp 69 on VLAN100  
B. Configure the switch with an access list on VLAN100 to deny TFTP.  
C. Configure the switch with ip forward-protocol udp 67 globally  
D. Configure the switch with a VACL on VLAN100 to deny TFTP  

Correct Answer: A [web:12][web:601][web:603]

- 問題文要約（日本語）:  
スイッチが VLAN100 上で DHCP リレー（ip helper-address）を使ってサーバ 10.1.1.1 に中継している。  
DHCP サーバの syslog には「スイッチから来る多数の TFTP リクエスト」が記録されており、そのせいで **サーバ CPU 使用率がしきい値超え**している。  
サーバ負荷を下げるため、スイッチ側でどの設定をすべきかを問う。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - Cisco の `ip helper-address` は、**DHCP だけでなく 8 種類の UDP ブロードキャストをデフォルトで転送**する。  
    - 代表例: DHCP/BOOTP (67,68), TFTP (69), DNS (53), Time (37), NetBIOS (137–138), TACACS (49) など。 [web:601][web:603]
  - 本問では:
    - VLAN100 で発生した **TFTP ブロードキャスト** も、  
      - スイッチが「DHCP relay の一環」として DHCP サーバ 10.1.1.1 にユニキャスト転送してしまっている。  
    - その結果、サーバ側で不要な TFTP 処理が大量に走り、CPU がしきい値を超えている。 [web:12]
  - 不要なプロトコルのみ転送を止めたいときは、  
    - `no ip forward-protocol udp <port>` で特定 UDP ポートのヘルパー転送を無効化する。 [web:601][web:603]
    - `no ip forward-protocol udp 69` → TFTP だけ止め、DHCP (67/68) はそのままリレーできる。  
  - 選択肢 A の  
    - `no ip forward-protocol udp 69`  
    によって、**TFTP トラフィックのリレーを無効化**し、DHCP リレー機能は維持しつつサーバ CPU 負荷を下げられる。  
  - 他の選択肢:
    - B / D: ACL や VACL で TFTP を止めることも理論上可能だが、  
      - 問題の本質は「ip helper-address による不要 UDP プロトコル転送」なので、  
      - **forward-protocol を正しく制御するのが Cisco 流の正解パターン**。  
    - C: `ip forward-protocol udp 67` を追加しても、  
      - 既定で 67 はすでに転送対象になっており、  
      - TFTP(69) 転送は止まらないため、CPU 問題の解決にならない。  

- 試験向けの覚え方フレーズ

  - **「ip helper は DHCP だけじゃない、“8兄弟”を全部送る → いらない子は `no ip forward-protocol udp <port>` で黙らせる」**  
  - DHCP リレー問題の典型パターン:
    - サーバが TFTP/NetBIOS など **不要な UDP を大量処理して高負荷**  
      → **TFTP なら `no ip forward-protocol udp 69` を即連想**。  

***
QUESTION 610 解説

- 問題文（英語）:  
The R1 routing table has the prefixes for the FTP1 and FTP2 file servers. A network engineer must configure the R1 with these requirements:  
- Host1 must use the FTP1 fileserver  
- Host2 must use the FTP2 fileserver  
Which configuration meets the requirement on R1?

A.  
B.  
C.  
D.  

Correct Answer: B [file:610]

- 問題文要約（日本語）:  
R1 は既に FTP1・FTP2 への経路プレフィックスを持っている。  
そこで、**Host1 は必ず FTP1 へ、Host2 は必ず FTP2 へ**アクセスさせたい。  
この「送信元ホストごとに使う FTP サーバを固定する」ために、R1 でどのような設定をすべきかを問う。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 要件は「Host1→FTP1」「Host2→FTP2」と **ホスト単位で転送先サーバを分ける**こと。  
    - これは通常のルーティング（宛先 IP ベース）では実現しづらく、**ポリシーベースルーティング（PBR）** を用いる。 [file:610]
  - 元の解説では、以下のような考え方になっている:  
    - ACL の条件を「送信元 Host1（10.10.10.11）かつ宛先 FTP1（10.10.100.11）」のようにすると、  
      - そのホストから FTP2（10.10.200.22）へのトラフィックは **PBR の対象外となり、通常ルーティングで別サーバへ行けてしまう**。  
    - したがって、**「送信元のみでマッチ（宛先 any）」する ACL を PBR に使う**必要がある。  
  - 正解となる構成イメージ（選択肢 B の内容）は以下のようなもの:  
    - ACL で送信元ごとにマッチ:
      - `access-list 101 permit ip host 10.10.10.11 any`  （Host1）  
      - `access-list 102 permit ip host 10.10.10.22 any`  （Host2）  
    - route-map でそれぞれの ACL と next-hop を関連付け:
      - `route-map FTP1-SELECT permit 10`  
        ` match ip address 101`  
        ` set ip next-hop <FTP1 への次ホップ>`  
      - `route-map FTP2-SELECT permit 20`  
        ` match ip address 102`  
        ` set ip next-hop <FTP2 への次ホップ>`  
    - そして、Host1/Host2 からのトラフィックが入ってくる R1 のインタフェースに PBR を適用:
      - `interface G0/1`  
        ` ip policy route-map FTP1-SELECT`（または両方をまとめた route-map）  
    - こうすることで、**宛先に関係なく「Host1 の全トラフィック」を FTP1 側の next-hop へ、「Host2 の全トラフィック」を FTP2 側へ強制できる**。 [file:610]
  - ポイントは以下:
    - PBR は「入力インタフェース単位」で適用されるので、Host1/Host2 からのパケットを受ける **R1 の受信インタフェース** に `ip policy route-map` を設定する。  
    - ACL では **送信元 IP のみを条件**にし、宛先は `any` として、ホストごとに経路を縛る。  

- 試験向けの覚え方フレーズ

  - **「“どのサーバへ行くか”を“どのホストから来たか”で決める → PBR＋ACL は“送信元だけ”マッチ（宛先 any）！」**  
  - 典型パターン:
    - 「特定クライアントは特定出口・特定サーバを使わせたい」  
      → **入力 IF に PBR、ACL は source ベース、`set ip next-hop` で出口固定** と機械的にイメージする。  

***
QUESTION 611 解説

- 問題文（英語）:  
Users from the 192.168.2.0/24 network cannot connect to the 172.16.2.32/28 network. Which configuration resolves the issue?

A.  
R1(config)#route-map REDIST permit 10  
R1(config-route-map)#match ip address 15  
R1(config-route-map)#set metric 1000 10 255 1 1500  
R1(config-route-map)#exit  
R1(config)# access-list 15 permit 192.168.2.0 0.0.255.255  

B.  
R4(config)#ip route 0.0.0.0 0.0.0.0 172.16.2.1  

C.  
R1(config)#router eigrp 100  
R1(config-router)#network 192.168.0.0  

D.  
R1(config-router)#router eigrp 100  
R1(config-router)#redistribute rip  
R1(config-router)#default-metric 10000 100 255 100 1500  

Correct Answer: D [file:610]

- 問題文要約（日本語）:  
192.168.2.0/24（ユーザ側）から 172.16.2.32/28 へ通信できない。  
出力からは、R4 側に 192.168.2.0/24 へのルートがないため **戻り通信が返せていない**ことが分かる。  
R1 では RIP と EIGRP の相互再配布を意図した設定があるが、RIP → EIGRP 側のメトリック設定が不足しており、経路が再配布されていない。  
どの設定を入れれば問題が解決するかを問う。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 解説によると、原因は次の 2 点: [file:610]  
    - R4 は 192.168.2.0/24 のルートを持っていない  
      → ユーザからのパケットは 172.16.2.32/28 までは届くが、**戻りパケットの経路がない**。  
    - R1 上で RIP ↔ EIGRP の「相互再配布」をする意図の設定はあるものの、  
      - RIP → EIGRP 再配布コマンドに **EIGRP 用メトリックが指定されておらず**、  
      - そのため RIP 側のネットワーク（192.168.2.0/24 など）が **EIGRP には一切流れていない**。  
  - EIGRP はメトリックとして 5 つの値（帯域幅・遅延・信頼性・負荷・MTU）を要求するため、  
    - `redistribute rip` を行うときは、`metric` をルートマップで都度指定するか、  
    - あるいは `default-metric` で **デフォルトのメトリックをまとめて定義**する必要がある。  
  - 選択肢 D はまさにこの **デフォルトメトリックを定義してから RIP を EIGRP に再配布**する設定:  
    - `router eigrp 100`  
      `redistribute rip`  
      `default-metric 10000 100 255 100 1500`  
    - これにより、RIP で学習している 192.168.2.0/24 などが適切な EIGRP 外部ルートとして広告され、  
      - R4 にも 192.168.2.0/24 の経路が届き、**往復通信が成立**する。 [file:610]
  - 他の選択肢:
    - A: route-map＋ACL で特定プレフィックスだけにメトリックをつけるように見えるが、  
      - **そもそも「redistribute rip route-map REDIST」などの紐付けが書かれていない**ため、この記述だけでは再配布は有効にならない。  
    - B: R4 にデフォルトルートを追加しても、  
      - 問題は「R4 から 192.168.2.0/24 へのルートがない」ことであり、  
      - デフォルトルートは「未知宛先への出口」であって、**既知セグメント（ユーザネットワーク）の戻り経路問題を直接解決しない**。  
    - C: `network 192.168.0.0` を追加しても、  
      - それは R1 が持つインタフェースが 192.168.0.0/16 のどこかに属している場合に限り EIGRP に参加させる効果。  
      - 今回の本質は「RIP でしか見えていないネットワークが EIGRP へ再配布されていない」ことであり、network コマンドでは解決しない。  

- 試験向けの覚え方フレーズ

  - **「他プロトコル→EIGRP 再配布で経路が来ないときは“メトリック忘れ”を疑え → `default-metric BW delay rel load MTU`」**  
  - パターン化:
    - 「RIP, OSPF から EIGRP に `redistribute` しているのにルートが 0 本」  
      → **まず `default-metric` or route-map の `metric` 設定の有無をチェック**。  

***
QUESTION 612 解説

- 問題文（英語）:  
A network administrator added area 5 to the OSPF network, and now users connected to router R2 cannot access the server connected to R8. Which configuration resolves the issue?

A.  
R9#  
router ospf 1  
 router-id 10.9.9.9  
 area 7 virtual-link 10.4.4.4  
 network 10.1.9.0 0.0.0.3 area 5  
 network 10.8.2.0 0.0.0.3 area 7  

R4#  
router ospf 1  
 router-id 10.4.4.4  
 area 7 virtual-link 10.9.9.9  
 network 10.11.1.0 0.0.0.3 area 2  
 network 10.8.2.0 0.0.0.3 area 7  

B.  
R9#  
router ospf 1  
 router-id 10.9.9.9  
 area 5 virtual-link 10.4.4.255  
 network 10.1.9.0 0.0.0.3 area 5  
 network 10.8.2.0 0.0.0.3 area 7  

R4#  
router ospf 1  
 router-id 10.4.4.4  
 area 5 virtual-link 10.9.9.255  
 network 10.8.2.0 0.0.0.3 area 7  
 network 10.11.1.0 0.0.0.3 area 2  

C.  
R9#  
router ospf 1  
 router-id 10.9.9.9  
 area 7 virtual-link 10.4.4.4  
 network 10.1.9.0 0.0.0.3 area 7  
 network 10.8.2.0 0.0.0.3 area 5  

R4#  
router ospf 1  
 router-id 10.4.4.4  
 area 7 virtual-link 10.9.9.9  
 network 10.8.2.0 0.0.0.3 area 2  
 network 10.11.1.0 0.0.0.3 area 7  

D.  
R9#  
router ospf 1  
 router-id 10.9.9.9  
 area 5 virtual-link 10.4.4.4  
 network 10.1.9.0 0.0.0.3 area 5  
 network 10.8.2.0 0.0.0.3 area 7  

R4#  
router ospf 1  
 router-id 10.4.4.4  
 area 5 virtual-link 10.9.9.9  
 network 10.8.2.0 0.0.0.3 area 7  
 network 10.11.1.0 0.0.0.3 area 2  

Correct Answer: A [file:610]

- 問題文要約（日本語）:  
OSPF ネットワークに **Area 5** を追加したところ、R2 配下のユーザが R8 配下のサーバにアクセスできなくなった。  
トポロジ上、Area 5 は Area 0（バックボーン）と物理的に直接つながっておらず、**Area 7 を経由してしか Area 0 に到達できない構造**になっている。  
この状況で、どの OSPF 設定を入れれば到達性が回復するかを問う。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - OSPF の設計ルール:  
    - すべての **エリアは Area 0 に直接接続**しているか、  
    - そうでない場合は **仮想リンク（virtual-link）を用いて論理的に Area 0 と接続**する必要がある。  
  - 本問では:  
    - 新しく追加された Area 5 が **物理的に Area 0 に接続されていない（非バックボーンエリアが孤立）** ため、  
    - R2 側エリアと R8 側エリア間で LSA が正しく行き来できず、到達性が失われている。 [file:610]
  - 解説にある通り、**Area 7 を経由して Area 5 を Area 0 に「論理的に接続」するための virtual-link が必要**。  
    - virtual-link は「**バックボーンと離れている ABR 間**」に張る。  
    - 選択肢 A は、R9・R4 の両方で  
      - `area 7 virtual-link 10.4.4.4`（R9 側）  
      - `area 7 virtual-link 10.9.9.9`（R4 側）  
      と、**共通エリア 7 上の 2 台（R9 と R4）の router-id 間に仮想リンクを張っている**。  
    - さらに、インタフェースの所属エリア指定（`network 10.1.9.0 area 5`, `network 10.8.2.0 area 7`, `network 10.11.1.0 area 2`）も正しく合致。  
  - 他の選択肢が誤りな理由（解説の補足）:  
    - B / D: virtual-link を定義する `area X virtual-link Y.Y.Y.Y` の **area 番号や router-id が不正**（存在しないアドレス 10.4.4.255 など）で、実際には仮想リンクが張れない。  
    - C: Area 5 と Area 7 の network 宣言が **入れ替わってしまっている** ため、トポロジと合わず正しい ABR/エリア関係にならない。  
  - よって、**Area 7 上の R9–R4 間に正しい virtual-link を張って、Area 5 を論理的にバックボーンに接続する構成（A）が正解**となる。 [file:610]

- 試験向けの覚え方フレーズ

  - **「“Area 0 から離れたエリア”が出たら、まず“virtual-link で 0 にトンネル”を疑え。virtual-link は“共通エリア上の 2 ABR（router-id 同士）”で張る」**  
  - 目安:
    - 「新しいエリア追加 → そこだけ到達不能」  
      → **“Area 0 直結か？” “共通トランジットエリア上に ABR 2 台いるか？” “`area X virtual-link RID` の X はトランジットエリアか？”** をチェック。  

***
QUESTION 613 解説

- 問題文（英語）:  
The IP SLA probes 1, 2 and 3 verify the reachability of three distinct DNS servers hosted by ISP1. If all DNS servers are unreachable, the router should failover to ISP2 for Internet connectivity. However, the configuration is causing the router to failover to ISP2 even if some of the DNS servers are still reachable. Which action resolves the issue?

A. Rewrite the track object 14 to use the “or” operator  
B. Reference the track object 14 to the IP SLA probes 1, 2, and 3 directly  
C. Use the administrative distance of 255 for the default route through ISP2  
D. Use the delay command in the track object 14  

Correct Answer: A [file:610]

- 問題文要約（日本語）:  
IP SLA 1/2/3 で ISP1 側にある 3 台の DNS サーバ疎通を監視している。  
要件は「**3 台すべてダメになったら ISP2 へフェイルオーバー**」だが、  
現状の設定では **1 台でも落ちると ISP2 へ切り替わってしまう** 状況。  
どの修正をすれば正しいフェイルオーバー動作になるかを問う。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 典型的な構成イメージ:  
    - 各 DNS サーバ疎通用の track オブジェクト:  
      - `track 1 ip sla 1 reachability`  
      - `track 2 ip sla 2 reachability`  
      - `track 3 ip sla 3 reachability`  
    - これらをまとめる **track 14** が、`boolean and` で書かれているケースが多い:  
      - `track 14 list boolean and`  
        ` object 1`  
        ` object 2`  
        ` object 3`  
  - 解説本文は「When using “and”, all three track objects must be success in order to keep the static route to ISP1.」と説明している。 [file:610]  
    - `and` 条件では、3 つの IP SLA のうち **1 つでもダウンすると track 14 がダウン** と判定される。  
    - その結果、ISP1 へのスタティックデフォルトルートが下がり、**DNS が 2 台生きていても ISP2 へフェイルオーバー**してしまう。  
  - 要件は「3 台すべてアウトになったときだけ ISP2 へ切り替えたい」なので、  
    - track 14 は「**どれか 1 つでも生きていれば OK**」という条件にする必要がある。  
    - つまり、`boolean or` を使う:  
      - `track 14 list boolean or`  
        ` object 1`  
        ` object 2`  
        ` object 3`  
    - これにより、3 つすべてダウンしたときのみ track 14 がダウンとなり、その時だけ ISP2 へのルートがアクティブになる。  
  - 他の選択肢:
    - B: 個々の IP SLA を直接ルートに参照しても、「**3 台中 1 台死んだら切り替え**」という振る舞い自体は変わらない構成になりがちで、要件（全滅時のみ切替）を満たさない。  
    - C: AD 255 は「ルートを実質無効化」する値であり、フェイルオーバー条件（どのタイミングで track がダウンと見なされるか）の問題とは別。  
    - D: `delay` はフラップ抑制のための待ち時間用であり、「一部ダウン時でも failover する」というロジックそのものは変えられない。  

- 試験向けの覚え方フレーズ

  - **「IP SLA 監視を複数まとめるとき、“全部死んだらアウト”なら boolean OR、“1 個でも死んだらアウト”なら boolean AND」**  
  - 実装イメージ:
    - 「**生きていれば良い条件**」を複数束ねるときは OR  
    - 「**全部揃っていてほしい条件**」を束ねるときは AND  
    と整理して覚えると、track list の設計をミスりにくい。  

***
QUESTION 614 解説

- 問題文（英語）:  
While troubleshooting a BGP route reflector configuration, an engineer notices that reflected routes are missing from neighboring routers. Which two BGP configurations are needed to resolve the issue? (Choose two.)

A. neighbor R2 route-reflector-client  
B. neighbor 10.1.1.2 allowas-in  
C. neighbor 10.1.1.2 route-reflector-client  
D. neighbor 10.1.1.14 route-reflector-client  
E. neighbor R4 route-reflector-client  

Correct Answer: AE [file:610]

- 問題文要約（日本語）:  
BGP ルートリフレクタ構成で、クライアント間に **反射された経路が見えない** という問題が発生している。  
設定を確認すると、**peer-group（R2, R4）自体には何もオプションが設定されておらず**、  
その結果、10.1.1.2 / 10.1.1.14 向けの neighbor に対して route-reflector-client が有効化されていない。  
どの 2 つの設定を追加すれば問題が解決するかを問う。  

- 正解: **A と E**

- 解説（なぜ A・E が正しいか）

  - 出題の前提（解説より）:  
    - R1 上で、neighbor 10.1.1.2（R2）と 10.1.1.14（R4）が **それぞれ peer-group R2, R4** のメンバーとして定義されている。  
    - しかし、この peer-group R2 / R4 には何の属性も設定されておらず、  
      - `route-reflector-client` も peer-group に対して設定されていない。 [file:610]
  - 重要ポイント:  
    - 「neighbor を peer-group のメンバーにした」場合、**その neighbor に個別で設定を入れることはできない**。  
      - 解説に記載の通り、以下のようなエラーになる:  
        - `neighbor 10.1.1.2 route-reflector-client`  
        - `% Invalid command for a peer-group member` [file:610]
    - つまり、**RR クライアントにしたいなら peer-group 側に route-reflector-client を付ける必要がある**。  
  - 選択肢の意味:
    - A: `neighbor R2 route-reflector-client`  
      - peer-group R2 を **RR クライアントとして扱う設定**。  
      - これにより、R2 に属するすべてのメンバー（neighbor 10.1.1.2）が route-reflector-client になる。  
    - E: `neighbor R4 route-reflector-client`  
      - peer-group R4 に対して同じく route-reflector-client を設定し、neighbor 10.1.1.14 をクライアント化する。  
    - C / D: これらは **個々の neighbor（10.1.1.2, 10.1.1.14）に直接 route-reflector-client を付与する記述**だが、  
      - すでに peer-group メンバーであるため、先ほどの通り「Invalid command for a peer-group member」になってしまい、構文として不正。 [file:610]
    - B: `allowas-in` は自 AS 番号を含む経路を受け入れるための設定であり、**ルートリフレクタの問題とは無関係**。  
  - よって、**peer-group R2 / R4 に route-reflector-client を設定する A・E が正解**となる。  
    - この変更により、R1 は R2/R4 から学習した経路を「クライアント間に再配布する RR」として正しく動作するようになり、  
      - **反射経路がクライアント間で見えるようになる**。  

- 試験向けの覚え方フレーズ

  - **「peer-group メンバーを RR クライアントにしたいなら、“neighbor <peer-group-name> route-reflector-client”（メンバー IP 直指定はNG）」**  
  - 実務チェックポイント:
    - `show run | s router bgp` で peer-group に何の属性も付いていない場合、  
      - 「RR クライアントにしたつもりで実はただの iBGP ピア」になっていないか要確認。  

***
QUESTION 615 解説

- 問題文（英語）:  
After an engineer updates the configuration on the device, they noticed unexpected behavior. Which command resolves the issue by completely replacing the startup configuration?

A. configure replace nvram:startup-config  
B. copy running-config startup-config  
C. configure replace nvram:private-config  
D. copy system:running-config nvram:startup-config  

Correct Answer: A [file:610]

- 問題文要約（日本語）:  
エンジニアが機器のコンフィグを変更したところ、動作が想定外になった。  
**現在の running-config ではなく、保存済みの startup-config で完全に上書きして元に戻したい**。  
どのコマンドを使えばよいかを問う。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - `configure replace <URL>` は、指定したコンフィグファイルの内容で **現在の running-config を“丸ごと置き換える”** コマンド。  
    - `configure replace nvram:startup-config` により、NVRAM 上の startup-config を読み込み、**今の running-config を完全に差し替える**。  
    - 変更前の動作に戻したいときに最適。 [file:610]
  - B: `copy running-config startup-config` は「今の動作中コンフィグを保存する」だけで、  
    - すでにおかしくなっている running-config を startup-config に上書きしてしまうので、**復旧ではなく“壊れた状態の保存”**になる。  
  - C: `nvram:private-config` は暗号鍵や一部の内部情報用領域であり、通常の IOS コンフィグを戻す用途には使わない。  
  - D: `copy system:running-config nvram:startup-config` も B と同様に「現在のコンフィグを保存」する動作であり、  
    - 「startup-config で running-config を置き換える」という要件を満たさない。  

- 試験向けの覚え方フレーズ

  - **「“元の startup-config に戻したい”＝ `configure replace nvram:startup-config`（replace は“復元・巻き戻し”）」**  
  - 対比で覚える:
    - **保存**：`copy running-config startup-config`  
    - **復元**：`configure replace nvram:startup-config`  

***
QUESTION 616 解説

- 問題文（英語）:  
An engineer uses NetFlow to troubleshoot a voice quality issue from the branch office and notices that the traffic is not arriving in the correct format to the collector. Which configuration resolves the issue?

A.  
R2(config)#flow monitor CCNPMON1  
R2(config-flow-monitor)#no exporter CCNPEXP1  
R2(config-flow-monitor)#flow exporter CCNPEXP1  
R2(config-flow-exporter)#export-protocol netflow-v9  

B.  
R2(config)#flow exporter CCNPEXP1  
R2(config-flow-exporter)#export-protocol netflow-v5  

C.  
R2(config)#flow monitor CCNPMON1  
R2(config-flow-monitor)#no exporter CCNPEXP1  
R2(config-flow-monitor)#flow exporter CCNPEXP1  
R2(config-flow-exporter)#export-protocol ipfix  

D.  
R2(config)#flow exporter CCNPEXP1  
R2(config-flow-exporter)#export-protocol netflow-v9  

Correct Answer: D [file:610]

- 問題文要約（日本語）:  
ブランチ拠点の音声品質トラブルを NetFlow で解析しているが、**コレクタ側で期待する NetFlow フォーマット（バージョン）で届いていない**。  
どの設定を入れれば、コレクタが期待する形式（NetFlow v9）でエクスポートされるかを問う。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 解説では、「Flow exporter は flow monitor コンテキストの中ではなく、**グローバルで個別に定義する**」のが正しい手順と説明されている。 [file:610]  
    - 正しい設定例:  
      - `R2(config)# flow exporter CCNPEXP1`  
      - `R2(config-flow-exporter)# export-protocol netflow-v9`  
  - つまり、やるべきことは 2 点:  
    - ① エクスポータ CCNPEXP1 自体に対して、**期待バージョン（NetFlow v9）を指定**する。  
    - ② その設定は `flow monitor` サブモードではなく、**`flow exporter` サブモードで直接行う**。  
  - 選択肢 D はまさにこの形で、  
    - 既存の exporter CCNPEXP1 に対し、`export-protocol netflow-v9` を正しいコンテキストで設定している。  
    → これにより、**コレクタが期待するフォーマット（v9）でフローが届く**ようになる。  
  - 他の選択肢:
    - A / C: `flow monitor` コンテキストの中から exporter をいじろうとしており、解説が示す正しい手順と異なる。  
    - B: v5 を指定しているが、問題文の想定は **「正しい形式（＝設計上の期待バージョン）」が v9** であるため不適切。  

- 試験向けの覚え方フレーズ

  - **「NetFlow の“送り方”は exporter の仕事 → バージョン指定は `flow exporter` モードで `export-protocol netflow-v9`」**  
  - 流れで覚える:
    - `flow record`（何を見るか）  
    - `flow monitor`（どこで見るか）  
    - **`flow exporter`（どう送るか／どのバージョンで送るか）**  

***
QUESTION 617 解説

- 問題文（英語）:  
An engineering is troubleshooting an OSPF adjacency issue between directly connected routers R1 and R2. Which configuration resolves the issue?

A.  
R2(config)#interface GigabitEthernet 0/0  
R2(config-if)#ip address 10.10.10.9 255.255.255.252  

B.  
R1(config)#interface GigabitEthernet 0/0  
R1(config-if)#ospfv3 10 ipv4 area 0  

C.  
R1(config)#interface GigabitEthernet 0/0  
R1(config-if)#ospfv3 network broadcast  

D.  
R2(config)#interface GigabitEthernet 0/0  
R2(config-if)#ospfv3 network broadcast  

Correct Answer: A [file:610]

- 問題文要約（日本語）:  
R1 と R2 は物理的には直結だが、**OSPF の隣接関係が確立しない**トラブルが発生している。  
調査すると、/30 マスクで設定した 10.10.10.x の IP アドレスが **異なるサブネットに属しており、同一リンク上にいない扱い**になっていることが分かる。  
どの設定変更をすれば OSPF 隣接が張れるかを問う。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - /30 マスク \(255.255.255.252\) のサブネット 10.10.10.8/30 のアドレス範囲は:  
    - ネットワークアドレス: 10.10.10.8  
    - 使用可能ホスト: **10.10.10.9, 10.10.10.10**  
    - ブロードキャスト: 10.10.10.11 [file:610]
  - 解説の通り、問題の状況では片側インタフェースが 10.10.10.10、もう片側が 10.10.10.11 になっており、  
    - 10.10.10.10 → 10.10.10.8/30 の中  
    - 10.10.10.11 → **別のサブネット（10.10.10.12/30 など）のブロードキャスト側** に相当し、  
      - 結果として **R1・R2 は同一ネットワーク上にいない** ため、OSPF Hello を交換しても隣接が確立しない。 [file:610]
  - 選択肢 A では、R2 の G0/0 に `10.10.10.9 255.255.255.252` を設定することで、  
    - R1: 10.10.10.10/30（と仮定）  
    - R2: 10.10.10.9/30  
    となり、**両者が同じ 10.10.10.8/30 セグメントの有効ホスト同士**になる。  
    - これで L2 直結 + L3 同一サブネット条件が満たされ、OSPF 隣接が形成される。  
  - 他の選択肢:
    - B / C / D: いずれも `ospfv3` 関連の設定で、  
      - 解説にあるように **Ethernet インタフェースでは `ospfv3 network broadcast` がデフォルト** なので、  
      - ここを触っても「IP アドレスが別サブネット」という根本問題は解決しない。 [file:610]

- 試験向けの覚え方フレーズ

  - **「OSPF 隣接が張れないときは、まず“同一セグメントか？”を疑え。/30 なら“ネット＋2 ホスト＋ブロード

  ***
QUESTION 618 解説

- 問題文（英語）:  
After a security hardening was performed on a router, the administrator cannot access the command line of any remote device. Which action resolves the issue?

A. modify the AAA policy to allow the user to run the telnet and SSH commands.  
B. remove the transport output none command from the line con 0 section.  
C. move Telnet and SSH commands to a nonzero privilege level.  
D. remove the egress ACL blocking Telnet and SSH on the router interfaces.  

Correct Answer: A [file:610]

- 問題文要約（日本語）:  
ルータにセキュリティハードニングを施した結果、管理者が **そのルータから他のリモート機器に Telnet/SSH で入れなくなった**。  
（＝自分がログインしているこのルータ上で Telnet/SSH コマンドを実行しても、リモート側の CLI にアクセスできない。）  
どの対処をすれば、再びリモート機器へアクセスできるようになるかを問う。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - これは「**AAA によるコマンド認可（authorization exec / authorization commands）が厳しくなった結果、telnet / ssh コマンドの実行が拒否されている**」パターン。 [file:610]  
    - セキュリティ強化で AAA を導入すると、ログイン認証だけでなく、  
      - 「どのコマンドを実行してよいか」も AAA サーバ側ポリシーで制御される。  
    - その際、**telnet や ssh コマンドが許可リストに含まれていない**と、  
      - ルータ上でこれらのコマンドを打っても拒否され、どのリモートにも入れなくなる。  
  - 選択肢 A は、AAA ポリシー側で **ユーザに telnet / ssh コマンド実行権限を付与する**ことを意味しており、  
    - これにより、ルータから他デバイスへのリモートアクセスが再び可能になる。  
  - 他の選択肢が不適切な理由:
    - B: `line con 0` の `transport output none` は **コンソールから他機器へ“抜ける”プロトコルを制御しない**（`transport output` は vty などに対しての設定であり、ここでの問題文の主因とは合わない）。  
    - C: Telnet/SSH コマンドを別の privilege level に移しても、**AAA がそのレベルでのコマンド自体を拒否していたら意味がない**。  
    - D: egress ACL が Telnet/SSH をブロックしていれば通信自体が飛ばないが、問題文は「セキュリティハードニング後に**どのリモートにもログインできなくなった**」状況を AAA 観点で説明した問題で、  
      - 原因を ACL と限定する根拠はなく、解説でも AAA ポリシー修正を正解としている。 [file:610]

- 試験向けの覚え方フレーズ

  - **「ハードニング後に“自分から外に telnet/ssh できない” → まず AAA の“command authorization”と権限リストを疑え」**  
  - 実務メモ:
    - AAA 導入時は  
      - `aaa authorization exec`  
      - `aaa authorization commands`  
      と「どのコマンドを許すか」を必ずテスト、  
      - 特に telnet / ssh / ping / traceroute など運用に必須のコマンドを**事前に許可**しておく。  

***
QUESTION 619 解説

- 問題文（英語）:  
OSPF adjacency is broken between router A and router B. Which configuration on router B resolves the issue?

A.  
Router B(config)#int gigabitEthernet 1  
Router B(config-if)#ip ospf hello-interval 40  
Router B(config-if)#router ospf 10  
Router B(config-router)#router-id 1.1.1.1  

B.  
Router B(config)#int gigabitEthernet 1  
Router B(config-if)#ip ospf hello-interval 10  
Router B(config-if)#ip ospf cost 1000  

C.  
Router B(config)#int gigabitEthernet 1  
Router B(config-if)#ip address 172.16.192.2 255.255.255.192  
Router B(config-if)#ip ospf network broadcast  
Router B(config-if)#exit  

D.  
Router B(config)#int gigabitEthernet 1  
Router B(config-if)#ip address 172.16.192.2 255.255.192.0  
Router B(config-if)#ip ospf network broadcast  
Router B(config-if)#exit  

Correct Answer: D [file:610]

- 問題文要約（日本語）:  
Router A と Router B 間の OSPF 隣接が張れていない。  
調査すると、**同じリンク上にあるはずのインタフェースで、サブネットマスクと OSPF ネットワークタイプの設定が食い違っている**。  
どの設定を Router B に入れれば OSPF 隣接を復旧できるかを問う。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 解説によると、接続リンクの設定は以下のように **不一致** になっている。 [file:610]  
    - Router A:  
      - サブネットマスク: /18（255.255.192.0）  
      - OSPF ネットワークタイプ: broadcast  
    - Router B:  
      - サブネットマスク: /24（255.255.255.0 あるいは /26 と記述されているが、いずれにせよ A と不一致）  
      - OSPF ネットワークタイプ: point-to-multipoint（そのため Hello/Dead タイマーもデフォルト値が異なる）  
  - OSPF 隣接を張るためには、以下が一致している必要がある:  
    - 同一セグメント（IP アドレスとサブネットマスクが同じネットワーク）  
    - ネットワークタイプ（broadcast, point-to-point など）  
    - Hello/Dead タイマー（ネットワークタイプに依存して決まる）  
  - 選択肢 D は Router B 側で  
    - `ip address 172.16.192.2 255.255.192.0` にして **/18 に揃える**  
    - `ip ospf network broadcast` で **ネットワークタイプも broadcast に揃える**  
    → これにより、Router A と Router B が **同じサブネット・同じネットワークタイプ・同じタイマー** となり、OSPF 隣接が復旧する。 [file:610]
  - 他の選択肢:
    - A / B: hello-interval や cost、router-id は変えているが、**サブネットマスク不一致とネットワークタイプ不一致という根本原因を解消していない**。  
    - C: network type を broadcast にしただけで、**サブネットマスクは /26 のまま**と解説で指摘されており、依然として同一セグメントではない。 [file:610]

- 試験向けの覚え方フレーズ

  - **「OSPF 隣接が怪しい時は、“IP/マスク・ネットワークタイプ・タイマー”セットで疑え。サブネットと network type の不一致は鉄板ネタ」**  
  - 特にマルチアクセス系リンクは:
    - IP アドレス + マスク  
    - `ip ospf network` のタイプ  
    → **どちらか片方だけ直してもダメ**、両方そろえてはじめて隣接が安定、という形で丸ごと覚えておくとミスを防ぎやすい。  

***
QUESTION 620 解説

- 問題文（英語）:  
An engineer observes that every time the ICMP packet is lost at a polling interval, track 1 goes down, which causes unnecessary disruption and instability in the network. The engineer does not want the traffic to be rerouted if the loss of ICMP packets is negligible. If the packet loss is persistent for a longer duration, the track must go down and the traffic must be rerouted. Which action resolves the issue?

A. Change the IP SLA schedule to run only at certain intervals.  
B. Increase the timeout value from 1000 to 1500.  
C. Define a delay timer under track 1.  
D. Increase the threshold value from 1000 to 1500.  

Correct Answer: C [file:610]

- 問題文要約（日本語）:  
IP SLA の ICMP 監視結果を track 1 に紐づけていて、**1 回のポーリングで ICMP が落ちただけでも直ちに track 1 が Down になり、経路切替が発生してしまう**状況。  
ごく短時間の一時的なパケットロスでは経路変更させたくないが、**一定時間以上ロスが続いた場合のみ track を Down にして経路を切り替えたい**。  
どの設定を入れればよいかを問う。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 今の状態:  
    - IP SLA の単発失敗（そのポーリングだけの ICMP ロス）で、**即時に track 1 が Down** → そのたびにルーティング変更 → 不安定化。 [file:610]  
  - 解決したいこと:  
    - 「**短時間の一瞬のロス**では track は Down にしない」  
    - 「**ある程度の時間、連続して失敗が続いたとき**だけ Down にする」  
  - これにピッタリなのが、track の **delay タイマー**（`delay down / delay up`）:  
    - 例:  
      - `track 1 ip sla 1 reachability`  
      - `delay down 30 up 90`  
    → 30 秒間連続で SLA が失敗したときだけ track 1 を Down にする、  
       回復も 90 秒安定してから Up、といった **ヒステリシス** を付けられる。 [file:610]  
  - 他の選択肢が不適切な理由:
    - A: IP SLA のスケジュール頻度を変えても、「**1 回失敗で即 Down**」という性質自体は変わらない。  
    - B / D: timeout や threshold 値を上げても、「**連続失敗の時間を見てから落とす**」制御はできない。  
      - 依然として「その SLA 実行ごとの成否」にだけ依存した挙動になる。  

- 試験向けの覚え方フレーズ

  - **「IP SLA＋track が“神経質に”フラップするときは、`track delay down / up` で“我慢時間”を足す」**  
  - セットで覚えるコマンド:
    - `track <N> ip sla <M> reachability`  
    - `delay down <sec> up <sec>`  
    → 「**IP SLA = 判定役、track delay = “様子見タイマー”**」とイメージすると整理しやすい。  

***
QUESTION 621 解説

- 問題文（英語）:  
During an unannounced link-maintenance window at the ISP, the DCI link went down, which caused a significant service outage. What action must the network engineer take at the head office to ensure Area-0 connectivity without intervention from the ISP?

A. Create a virtual link in Area-0 between HO E0/0 and DR E0/0.  
B. Create a virtual link in Area-110 between R1 S0/0 and R2 S0/0.  
C. Create a GRE tunnel interface in Area-110 between R1 S0/0 and R2 S0/0.  
D. Create a GRE tunnel interface in Area-0 between HO E0/0 and DR E0/0.  

Correct Answer: B [file:610]

- 問題文要約（日本語）:  
ISP 側のメンテで **DCI（Head Office と DR を直結していたリンク）がダウン**し、その結果 **Area 0 が物理的に分断されて大規模障害**になった。  
ISP に何もしてもらわず、**Head Office 側の設定だけで Area 0 の一体性を保つ方法**はどれかを問う。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - DCI が落ちると、Head Office 側の Area 0 と、DR 側の Area 0 が **別々の backbone になってしまう**。  
  - OSPF のルール上、  
    - 全てのエリアは **必ず Area 0 と連結**していなければならない。  
    - Area 0 自身が **分断されてはならない**（backbone partition は NG）。  
  - このように物理的に backbone が分断された場合、**“Transit エリア経由で Area 0 同士を論理的に接続する” ために virtual-link を使う**。 [file:610]  
    - 今回のトポロジでは、  
      - BR ルータ群と MPLS クラウドにぶら下がるインタフェースは **すべて Area 110 に属している**。  
      - R1（Head Office 側 ABR）と R2（DR 側 ABR）の S0/0 も Area 110 に属している必要がある、と解説にある。 [file:610]  
    - そこで、**Area 110 を transit area として、R1–R2 間に virtual-link を張る**ことで、  
      - 「R1 側 Area 0」と「R2 側 Area 0」を再び **論理上 1 つの backbone** として接続できる。  
  - 選択肢 B:  
    - 「Area-110 間の R1 S0/0 – R2 S0/0 に virtual link を張る」という内容で、  
      - まさに「Transit area 110 経由で backbone partition を直す」正しいアプローチ。 [file:610]
  - 他の選択肢:
    - A / D: Area 0 内に virtual-link や GRE を張る、という考え方は **“Area 0 そのものが transit” である**という OSPF の前提と矛盾しており、本件の “backbone partition を transit area 経由で接続し直す” 要件を満たさない。  
    - C: GRE トンネル自体は L3 接続を作れるが、**OSPF 上の backbone 要件は「virtual-link」で満たすのが正攻法**であり、問題の意図とも解説とも合致しない。  

- 試験向けの覚え方フレーズ

  - **「Area 0 が“真っ二つ”に割れたら → “Transit エリアに virtual-link” で一本に戻す」**  
  - セットで暗記:
    - **backbone partition → virtual-link**  
    - **transit は backbone 以外のエリア（今回は Area 110）**  
    → 「**“0 の中”にリンクを足すんじゃなく、“0 同士を transit エリアでつなぐ” のが正解**」とイメージすると忘れにくい。  

***
QUESTION 622 解説

- 問題文（英語）:  
R1 is directly connected to R2 over network 10.100.14.0/24. An engineer configures R1 to advertise a static route that is connected to a local loopback for network 10.100.13.0/24. The network is not in the routing table of R2. Which action resolves the issue?

A. R2 must use a different OSPF process number and should be changed to ospf 1 to match R1.  
B. The redistribution command is incorrect in R1. The keyword subnets should be included with the redistribution command.  
C. The Loopback interface on R1 is administratively down. The interface should be enabled with the no shutdown command.  
D. The redistribution command is incorrect on R1. The default metric metric 200 should be included with the redistribution command.  

Correct Answer: B [file:610]

- 問題文要約（日本語）:  
R1–R2 間は 10.100.14.0/24 で直結。R1 側で **ローカルの loopback（10.100.13.0/24）へ向けた static route を OSPF に再配送**して、R2 からも到達させたい。  
しかし R2 のルーティングテーブルに 10.100.13.0/24 が載らない。  
どの対応をすれば、R2 にこのネットワークが見えるようになるかを問う。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 文脈から、R1 は `router ospf <process>` 配下で `redistribute static` を実行しているが、**`subnets` キーワードを付けていない**パターンが典型例。 [file:610]  
  - OSPF への再配送では:
    - 古い IOS や一部パターンでは、`redistribute static` だけだと **クラスフル境界をまたぐサブネットや非クラスフルな static route が再配送されない**。  
    - このとき、`redistribute static subnets` を明示することで、**サブネット（VLSM を含む）も再配送対象**になる。  
  - 問題の static route は loopback 宛（10.100.13.0/24）で、**クラスフルに見ると 10.0.0.0/8 のサブネット**になるため、  
    - `subnets` を付けないと **OSPF LSA に乗らない → R2 に届かない**、という挙動になる。  
  - 他の選択肢:
    - A: OSPF プロセス番号は **AS 内で一致させる必要はない**（プロセス番号はローカルにのみ意味を持つ）。  
    - C: loopback が shutdown なら R1 自身のルーティングテーブルに 10.100.13.0/24 が載らないので再配送もできないが、問題文は「再配送コマンドの誤り」にフォーカスした設問で、正解の意図と解説は `subnets` 不足を指している。 [file:610]  
    - D: OSPF への static 再配送では、**metric 無指定でもデフォルトコストで外部ルートが生成される**ため、「metric 200 を付けないと出ない」は誤り。  

- 試験向けの覚え方フレーズ

  - **「OSPF に static / ループバックを流して“見えない”ときは、まず `redistribute static subnets` を疑え」**  
  - セットで覚える:
    - 再配送 into OSPF: `redistribute static subnets` / `redistribute eigrp <AS> subnets`  
    → 「**OSPF は“subnets 付き再配送”が基本セット**」と意識しておくと取りこぼしを防げる。  

***
QUESTION 623 解説

- 問題文（英語）:  
The eBGP neighbor 10.1.1.1 of router R2 receives and installs the prefix, but the community value is not found in the BGP routing table. How must this issue be resolved?

A. R1 must have a route map assigned to neighbor R2, which is not matching the community values.  
B. R1 must have a route map assigned to neighbor R2, which is clearing the community values.  
C. R2 is not sending the community value to neighbor R1, configure R2 to send community.  
D. Community values are nontransitive and R2 is an eBGP peer, configure R2 to send community.  

Correct Answer: C [file:610]

- 問題文要約（日本語）:  
R2 の eBGP ピア 10.1.1.1（＝R1）は **プレフィックス自体はちゃんと受信・インストール**しているが、**BGP ルーティングテーブル上で community が付いていない**。  
この「community が見えない」問題をどの設定で解決すべきかを問う。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - BGP の **community 属性は “送る側で明示的に send-community を有効化しない限り、隣接に送信されない”**。  
  - 今回の状況:  
    - R2 側で prefix に community を付与している。  
    - R1 側では prefix は見えるが、community が付いていない。  
    → 典型的には、**R2 の neighbor 10.1.1.1 に対して `send-community` を設定していない**ときの挙動。 [file:610]  
  - 解説にも、R2 側の例として以下が示されている:  
    - `router bgp 64512`  
    - `neighbor 10.1.1.1 send-community` [file:610]  
  - 他の選択肢:
    - A / B: R1 側の route-map が原因と言っているが、問題文は「**R1 は prefix 自体はインストールしているが community が無い**」状況であり、まずは **送信側が community を送っているか**を見るのが基本。解説も R2 の send-community 不足を指摘している。  
    - D: community は **基本 transitive**（well-known, transitive 属性）であり、「nontransitive だから eBGP で飛ばない」という説明は誤り。  

- 試験向けの覚え方フレーズ

  - **「BGP で community が“消えた”ときは、まず `neighbor X send-community` を疑え」**  
  - セットで暗記:
    - `neighbor X send-community`（通常コミュニティ）  
    - `neighbor X send-community extended`（拡張コミュニティ）  
    → 「**BGP は community を“勝手には運ばない”、明示的に “send しろ” と言う必要がある**」と覚えておくと取りこぼしにくい。  

***
QUESTION 624 解説

- 問題文（英語）:  
What is a role of route distinguishers in an MPLS network?

A. Route distinguishers define which prefixes are imported and exported on the edge router.  
B. Route distinguishers allow multiple instances of a routing table to coexist within the edge router.  
C. Route distinguishers are used for label bindings.  
D. Route distinguishers make a unique VPNv4 address across the MPLS network.  

Correct Answer: D [file:610]

- 問題文要約（日本語）:  
MPLS L3 VPN で使われる **Route Distinguisher（RD）** の役割として正しいものはどれか、という問題。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - MPLS L3 VPN では、PE ルータ上で各顧客 VRF 内の IPv4 プレフィックス（例: 10.0.0.0/24）が **異なる顧客同士で重複**していてもよい。  
  - しかし、MP-BGP（VPNv4）としては「グローバルに一意なアドレス」が必要になるため、  
    - **RD:IPv4-prefix** の形で「VPNv4 アドレス」を作り、  
    - これにより **全 MPLS ネットワーク内で一意な VPNv4 アドレス**となる。 [file:610]  
  - よって、「Route distinguishers make a unique VPNv4 address across the MPLS network」が RD の役割を最も正しく表している。 [file:610]  
  - 他の選択肢:
    - A: 「どのプレフィックスを import/export するか」を決めるのは **Route Target（RT）** の役割であり、RD ではない。  
    - B: 「複数のルーティングテーブルを共存させる」のは **VRF 自体の機能**であり、RD はその識別子の一部に過ぎない。  
    - C: ラベルの割り当て・交換は **LDP / MP-BGP / RIB–LFIB 関連**であり、RD は「ラベルそのもの」には使わない。  

- 試験向けの覚え方フレーズ

  - **「RD = Route “Differentiate” → 同じ IPv4 でも “VPNv4 を一意化” する ID」**  
  - セットで整理:
    - **RD = 一意化（“誰の 10.0.0.0 か” を区別）**  
    - **RT = どの VRF に輸入/輸出するか（VPN 間の “経路共有ルール”）**  
    → 「**RD は“名前付け”、RT は“配布ルール”**」とセットで覚えると取り違えにくい。  

***
QUESTION 625 解説

- 問題文（英語）:  
A PC is configured to obtain an IP address automatically, but it receives an IP address only from the 169.254.0.0 subnet. The DHCP server logs contained no DHCPDISCOVER message from the MAC address of the PC. Which action resolves the issue?

A. Configure a DHCP reservation on the server for the PC.  
B. Configure a static IP address on the PC and exclude it from the DHCP pool.  
C. Configure an ip helper-address on the router to forward DHCP messages to the server.  
D. Configure DHCP Snooping on the switch to forward DHCP messages to the server.  

Correct Answer: C [file:610]

- 問題文要約（日本語）:  
PC は「IP アドレス自動取得」に設定されているが、**常に 169.254.0.0/16（APIPA）しか取れない**。  
DHCP サーバのログを見ると、その PC の MAC アドレス由来の **DHCPDISCOVER が一切届いていない**。  
どの対応をすれば、この PC が正しく DHCP サーバから IP を取得できるかを問う。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 169.254.0.0/16 を取っている＝  
    - PC が **DHCP から応答を受け取れていない**ときに Windows などが自動採番するアドレス。  
  - DHCP サーバ側にも **DISCOVER が届いていない**ため、  
    - 問題は「サーバが応答を返さない」のではなく、  
    - 「**クライアントのブロードキャスト DHCPDISCOVER がサーバまで届いていない**」点にある。 [file:610]  
  - 典型的な原因:  
    - PC と DHCP サーバが **別 VLAN・別ネットワーク**にいて、ブロードキャストが L3 ルータを越えない。  
  - この場合に必要なのが、**DHCP リレー（ip helper-address）**:  
    - クライアント側 VLAN に面した L3 インタフェースで  
      - `ip helper-address <DHCPサーバのIP>`  
      を設定し、  
      - 受け取った DHCP ブロードキャストを **DHCP サーバ宛ユニキャスト**に変換して転送する。 [file:610]  
  - 他の選択肢:
    - A: 予約を作っても **DISCOVER 自体が届かなければ意味がない**。  
    - B: 手動で固定 IP を振るのは “応急処置” に過ぎず、試験問題の「原因への対処」にはなっていない。  
    - D: DHCP Snooping は「なりすまし DHCP から守る L2 セキュリティ機能」であって、**リレー機能ではない**。  

- 試験向けの覚え方フレーズ

  - **「PC が 169.254 にハマっていて、サーバログに DISCOVER が無い ＋ VLAN またぎ → “まず ip helper-address”」**  
  - セットで暗記:
    - **同一セグメント**: DHCP ブロードキャストがサーバにそのまま届く → helper 不要  
    - **異なるセグメント/VLAN**: DHCP ブロードキャストは L3 で止まる → **L3 インタフェースに `ip helper-address` 必須**  

***
QUESTION 626 解説

- 問題文（英語）:  
Users in BGP autonomous system 65500 cannot access IPv4 resources in the EIGRP IPv4 autonomous system 100. Which action resolves the issue?

A. Redistribute EIGRP routes into OSPF.  
B. Redistribute OSPF internal routes into BGP.  
C. Redistribute OSPF external routes into BGP.  
D. Redistribute BGP routes into OSPF.  

Correct Answer: C [file:610]

- 問題文要約（日本語）:  
AS 65500（BGP）側のユーザが、AS 100（EIGRP）側の IPv4 リソースに到達できていない。  
R2 のルーティングテーブルを見ると、EIGRP AS 100 のネットワーク（10.1.14.0/24, 10.1.4.0/24）は **OSPF E2 external ルートとして存在**している。  
どの再配布設定を行えば、AS 65500 にこれらのネットワークを届けられるかを問う。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 現状の経路の流れ:  
    - EIGRP AS 100 のネットワーク（例: 10.1.14.0/24, 10.1.4.0/24）は、  
      - いったん **OSPF に再配布されて E2 外部ルート**として R2 のルーティングテーブルに存在している。 [file:610]  
    - つまり、R2 視点では「**EIGRP ネットワーク = OSPF External ルート**」になっている。  
  - AS 65500 の BGP ピアにこれらのプレフィックスを届けたいので、  
    - BGP から見て再配布元になるのは **EIGRP ではなく、OSPF External ルート**。  
    - 従って、**「OSPF External ルートを BGP に再配布」**する必要がある。  
  - 選択肢 C:  
    - 「Redistribute OSPF external routes into BGP」という表現で、  
      - 実際の設定イメージは `redistribute ospf <pid> match external …` など。  
    - 解説文も「In the BGP router R2’s routing table, the networks in EIGRP AS 100 … can be found as OSPF E2 external routes. In order to advertise them to the BGP 65500, those OSPF E2 external routes have to be redistributed into BGP.」と明記。 [file:610]  
  - 他の選択肢:
    - A: すでに EIGRP → OSPF は行われている前提で、問題は「OSPF → BGP」の部分。  
    - B: **OSPF internal のみ**を再配布しても、E2 ルート（外部）には効果がない。  
    - D: BGP → OSPF 再配布は AS 65500 の経路を IGP に流す話であり、今回の「EIGRP 資源を BGP 65500 に届ける」問題とは逆方向。  

- 試験向けの覚え方フレーズ

  - **「“今そのルートは何ルートとして見えているか” を見て → そのプロトコルから再配布」**  
  - 今回のパターンをワンフレーズで:
    - **「EIGRP → OSPF(E2) → BGP に出したい → “OSPF external を BGP へ再配布”」**  
    → 「**再配布元は“元のプロトコル”ではなく、“今の属性”を見る**」と意識しておくと混乱しにくい。  

***
QUESTION 627 解説

- 問題文（英語）:  
Which type of BFD session is created on a broadcast Ethernet interface with OSPF?

A. with the DR and BDR, but not any DROTHER routers  
B. with all OSPF routers  
C. only on P2P Ethernet interfaces  
D. only with the DR  

Correct Answer: A [file:610]

- 問題文要約（日本語）:  
OSPF を有効にした **ブロードキャスト Ethernet インタフェース**上で BFD を使う場合、  
どの種類の BFD セッションが張られるかを問う問題。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - OSPF のブロードキャスト・ネットワークでは、  
    - **DR（Designated Router）**  
    - **BDR（Backup Designated Router）**  
    が中心となってトポロジ情報を集約・配布する。  
  - BFD 連携もこの設計に沿っており、  
    - ブロードキャストインタフェース上では  
      - **各ルータは DR/BDR とだけ BFD セッションを張り**、  
      - DROTHER 同士の間では BFD セッションを張らない。 [file:610]  
  - これにより、  
    - 必要な障害検出は維持しつつ、  
    - 不要な BFD セッション数を抑え、スケーラビリティを確保している。 [file:610]  
  - 他の選択肢:
    - B: すべての OSPF ルータ間で張ると、インタフェース上のルータ数が増えるほど BFD セッションが爆発的に増えてしまう。  
    - C: 「P2P Ethernet のみ」という制約はなく、「ブロードキャストだが DR/BDR のみ」というのが正しい。  
    - D: BDR とも張るため不正解。  

- 試験向けの覚え方フレーズ

  - **「ブロードキャスト OSPF + BFD → “DR & BDR のみ”、DROTHER 間は無し」**  
  - 語呂で覚えるなら:
    - **「BFD 相手は “DRペア（DR＋BDR）だけ”」**  
    → “ブロードキャストで全員とは張らない” とセットで記憶。  

***
QUESTION 628 解説

- 問題文（英語）:  
The user experiences random packet drops when accessing Server-1 but does not have any issues when reaching out to Server-2. Which action resolves the issue?

A. Configure the distance under the EIGRP process to 10 on R1.  
B. Modify the distance 10 under the OSPF process on R1 to 90.  
C. Configure the distance under the EIGRP process to 10 on R2.  
D. Remove the distance 10 under the OSPF process on R1.  

Correct Answer: D [file:610]

- 問題文要約（日本語）:  
Server-1 へのアクセス時だけランダムなパケットドロップが発生し、Server-2 へのアクセスは問題ない。  
R1・R2 で EIGRP と OSPF の AD（Administrative Distance）がカスタム設定されている状況で、どの設定変更がこの問題を解決するかを問う。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - R1 の AD 状況:  
    - EIGRP（internal・external）AD = 100  
    - OSPF AD = 10（手動で非常に小さく設定）  
    → R1 では **常に OSPF の経路が優先**される。 [file:610]  
  - R2 の AD 状況:  
    - EIGRP AD = 100  
    - OSPF AD = 110（デフォルト）  
    → R2 では **EIGRP の経路が優先**される。 [file:610]  
  - つまり、Server-1 への経路に対して  
    - R1 側は OSPF 経路を選択  
    - R2 側は EIGRP 経路を選択  
    という **プロトコル不一致（非対称ルーティング・経路選択の不整合）が発生**し、  
    一部経路でフラッピングや不整合が起きてランダムなパケットドロップが起きていると考えられる。 [file:610]  
  - 選択肢 A/B/C の検証:
    - A: R1 の EIGRP AD を 10 に下げる  
      - OSPF も 10 のまま → AD 同一  
      - しかし通常、**OSPF メトリックの方が小さくなることが多く、依然として OSPF が選ばれやすい**ため、根本解決にならない。 [file:610]  
    - B: R1 の OSPF AD を 10 → 90 に変更  
      - それでも EIGRP AD=100 より小さいため、**まだ OSPF が優先**され続ける → 効果なし。 [file:610]  
    - C: R2 の EIGRP AD を 10 に変更  
      - もともと 100 < 110 で EIGRP が優先されているので、AD をさらに下げても挙動は変わらない。 [file:610]  
  - D: R1 の OSPF AD 10 を削除（→ デフォルト 110 に戻る）  
    - R1: EIGRP AD=100 < OSPF AD=110 → **R1 でも EIGRP が優先**  
    - R2 も EIGRP 優先なので、**両方とも同じプロトコル／同じ経路を選択**し、  
      経路不整合が解消 → ランダムなパケットドロップが収まる。 [file:610]  

- 試験向けの覚え方フレーズ

  - **「AD をいじるときは“両端でどのプロトコルが優先か”を揃える」**  
  - 本問のパターンを一行で:
    - **「R1 だけ OSPF AD を極端に下げてると、EIGRP との“優先プロトコルズレ”でトラブル → “変な AD は消してデフォルトに戻す”」**  
  - まとめ:
    - **“AD を極端に変えたら、まずそれを元に戻す選択肢を疑え”**  

***
QUESTION 629 解説

- 問題文（英語）:  
An engineer must route the management server traffic to the network 10.0.250.0 over the 2 Mbps link. Which configuration accomplishes this task on R1?

A. route-map advrt permit 10  
   match ip address 110  
   set ip next-hop 10.0.0.1  
   !  
   access-list 110 permit ip host 10.0.1.3 10.0.250.0 0.0.0.3  
   !  
   interface GigabitEthernet0/3  
   ip policy route-map advrt  

B. route-map advrt permit 10  
   match ip address 110  
   set ip next-hop 10.0.0.2  
   !  
   access-list 110 permit ip host 10.0.1.3 10.0.250.0 0.0.0.3  
   !  
   interface GigabitEthernet0/3  
   ip policy route-map advrt  

C. interface GigabitEthernet0/1  
   bandwidth 20000  

D. interface GigabitEthernet0/0  
   bandwidth 20000  

Correct Answer: B [file:610]

- 問題文要約（日本語）:  
管理サーバ 10.0.1.3 から、宛先ネットワーク 10.0.250.0/30 へのトラフィックだけを **2 Mbps リンク側（R2 側）に通したい**。  
R1 でどの設定をすればよいかを問う問題。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 要件は「**管理サーバ発 → 10.0.250.0/30 宛だけ** 2 Mbps 側経路を使う」こと。  
  - そのために、R1 で **PBR（Policy-Based Routing）** を使う案が提示されている。 [file:610]  
  - 選択肢 B の構成内容:
    - ACL 110:  
      - `access-list 110 permit ip host 10.0.1.3 10.0.250.0 0.0.0.3`  
      → 送信元 10.0.1.3、宛先 10.0.250.0/30 のトラフィックだけをマッチ。  
    - route-map advrt:  
      - `match ip address 110` で上記トラフィックを選別。  
      - `set ip next-hop 10.0.0.2` で、2 Mbps 側にある R2 のアドレスを **強制ネクストホップ**に設定。  
    - さらに、G0/3 インタフェースに `ip policy route-map advrt` を適用することで、  
      - 管理サーバから入ってくるトラフィックに対して PBR が有効化される。 [file:610]  
  - 選択肢 A の問題点:
    - `set ip next-hop 10.0.0.1` は 5 Mbps 側（R3 側）に向けるネクストホップであり、  
      - 問題文の「2 Mbps リンク」要件を満たさない。 [file:610]  
  - C/D の「bandwidth 20000」案について:
    - 帯域値を変えて **EIGRP のメトリックを調整**し、ルーティングプロトコルによる経路選択を変えるやり方も理論上可能だが、  
      - その場合「10.0.250.0/30 以外の管理サーバトラフィックも 2 Mbps 側に流れてしまう」副作用が出る。  
      - また、事前に他インタフェースの `bandwidth 5000` などの設定前提が必要で、問題文からは保証されない。 [file:610]  
    - 問題の解説でも「副作用や前提条件が重いので、この方法は採用せず、PBR 案（i）を正解とする」とされている。 [file:610]  

- 試験向けの覚え方フレーズ

  - **「“特定サーバ → 特定宛先だけ別リンク” はまず PBR を疑え」**  
  - 本問をパターン化:
    - **「管理サーバ 10.0.1.3 → 10.0.250.0/30 宛のみ PBR で next-hop 10.0.0.2（2M ルート）」**  
  - セットで暗記:
    - **ルーティング全体を変えたくない → PBR（ip policy route-map）**  
    - **経路全体を安く／高くしたい → bandwidth / metric 調整**  

***
QUESTION 630 解説

- 問題文（英語）:  
How does BFD protocol work?

A. BFD operates on the route processor module and impacts the route processor CPU utilization.  
B. BFD control packets are sent via UDP port 3784 to the destination router.  
C. BFD echo packets are sent to the same source IP and different destination IP with TCP port of 3786.  
D. When BFD declares a failure on the primary IGP path, the router on the peer router chooses to use the secondary path.  

Correct Answer: B [file:610]

- 問題文要約（日本語）:  
BFD（Bidirectional Forwarding Detection）の基本的な動作について正しい記述はどれかを問う問題。  
特に「どのポート・どのプロトコルで BFD パケットが送られるか」がポイント。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - BFD には主に 2 種類のパケットがある:
    - **Control パケット**: セッションの確立・維持に使う。  
    - **Echo パケット**: 実際のフォワーディングパスを通して疎通確認するために使う。 [file:610]  
  - それぞれのポート:
    - **BFD control パケット** → UDP 3784  
    - **BFD echo パケット** → UDP 3785  
    → 選択肢 B「BFD control packets are sent via UDP port 3784」は仕様に合致する。 [file:610]  
  - 他の選択肢が誤りな理由:
    - A: BFD はハードウェア実装が基本で、**常に RP CPU を強く食うとは限らない**し、その点を述べたものではない。  
    - C: Echo は UDP を使い、TCP 3786 ではない。ポート番号もプロトコルも誤り。 [file:610]  
    - D: BFD が故障を検知したときに **自分のルータ上の IGP/BGP などが経路を切り替える**のであって、「ピア側のルータ」が主体になる説明は不正確。 [file:610]  

- 試験向けの覚え方フレーズ

  - **「BFD Control = UDP 3784 / BFD Echo = UDP 3785」**  
  - 語呂イメージ:
    - **「3-7-8-4 で ‘シナ・ハ（しなは）Control’、その次 3785 が Echo」**  
  - 併せて暗記:
    - **「BFD は“両方向のフォワーディングパス監視”を、UDP で超高速にやる」**  

***
QUESTION 631 解説

- 問題文（英語）:  
After an engineer modified the configuration for area 7 to permit type 1, 2, and 7 LSAs only, users connected to router R9 reported that they could no longer access the Internet. Which configuration restores Internet access to users on R9 and permits only LSA type 1, 2, and 7?

A.  
R4#  
router ospf 1  
 area 7 nssa no-summary  
 network 10.5.1.0 0.0.0.3 area 0  
 network 10.8.2.0 0.0.0.3 area 7  

R9#  
router ospf 1  
 area 7 nssa  
 redistribute eigrp 10 subnets  
 network 10.8.2.0 0.0.0.3 area 7  

B.  
R4#  
router ospf 1  
 area 7 nssa  
 network 10.5.1.0 0.0.0.3 area 0  
 network 10.8.2.0 0.0.0.3 area 7  

R9#  
router ospf 1  
 area 7 nssa  
 redistribute eigrp 10 subnets  
 network 10.8.2.0 0.0.0.3 area 7  

C.  
R4#  
router ospf 1  
 area 0 area 7 stub no-summary  
 network 10.5.1.0 0.0.0.3 area 0  
 network 10.8.2.0 0.0.0.3 area 7  

R9#  
router ospf 1  
 area 7 stub  
 redistribute eigrp 10 subnets  
 network 10.8.2.0 0.0.0.3 area 7  

D.  
R4#  
router ospf 1  
 area 0 nssa default-information-originate  
 network 10.5.1.0 0.0.0.3 area 0  
 network 10.8.2.0 0.0.0.3 area 7  

R9#  
router ospf 1  
 area 7 nssa  
 redistribute eigrp 10 subnets  
 network 10.8.2.0 0.0.0.3 area 7  

Correct Answer: A [file:610]

- 問題文要約（日本語）:  
Area 7 を「LSA type 1,2,7 のみ許可」に変更した結果、R9 配下のユーザがインターネットに到達できなくなった。  
Area 7 は R9 上で EIGRP から経路を取り込む ASBR を含むエリアであり、この要件を満たしつつ R9 からインターネットへのデフォルトルートも必要。どの設定が正しいか。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - Area 7 の要件:
    - R9 は EIGRP から経路を取り込む **ASBR** → 外部経路は type 7 LSA としてエリア内に流す必要がある。  
    - よって Area 7 は **NSSA** にする必要がある（Stub/Totally Stub では外部 LSA を扱えない）。 [file:610]  
  - R4 と R9 の役割:
    - R4: Area 0 と Area 7 を接続する **ABR**  
    - R9: Area 7 内の **ASBR**（EIGRP 10 を OSPF に再配布） [file:610]  
  - 「type 1, 2, 7 だけ許可しつつ、R9 からインターネットへ出したい」条件を満たすには:
    - Area 7 を NSSA に設定（R4・R9 両方）  
    - かつ、Area 7 には **デフォルトルート（0.0.0.0/0）** を流す必要があるが、type 5/3 は流したくない。  
    - そのため、**NSSA Totally Stub（NSSA + no-summary）** を ABR 側で設定すると、Area 7 には:
      - type 1, 2, 7 のみ  
      - ABR からのデフォルトルート（NSSA デフォルト）が入る  
      → まさに問題文の条件に一致。 [file:610]  
  - 選択肢 A の構成:
    - R4: `area 7 nssa no-summary`  
      - Area 7 を **NSSA Totally Stub** として動作させ、type 1,2,7 + デフォルトだけを流す。  
    - R9: `area 7 nssa` + `redistribute eigrp 10 subnets`  
      - R9 を NSSA 内 ASBR として、EIGRP 経路を type 7 として Area 7 に広告。 [file:610]  
  - 他の選択肢:
    - B: `area 7 nssa` だけ → NSSA だが **no-summary がないため**、R9 側にデフォルトルートが自動で来ず、インターネット不可が継続。  
    - C: Area 7 を stub にしてしまう → stub では外部経路（type 5/7）を扱えないため、R9 の ASBR 機能と矛盾。  
    - D: `area 0 nssa default-information-originate`  
      - default-information-originate を適用すべきエリアは **Area 7 側** だが、ここでは Area 0 に誤適用しており不正。 [file:610]  

- 試験向けの覚え方フレーズ

  - **「ASBR がいるエリアで ‘1,2,7 だけ + デフォルト’ → “NSSA no-summary を ABR に”」**  
  - パターン化:
    - **「外部あり: NSSA / 外部 + デフォだけ: NSSA no-summary（= Totally NSSA）」**  
  - まとめ:
    - **「NSSA: 1,2,7 / NSSA no-summary: 1,2,7 + デフォだけ」**  
    - **「ASBR がいる stub は基本 NG、NSSA を選べ」**  

***
QUESTION 632 解説

- 問題文（英語）:  
The BGP neighbor is not coming up. Which action resolves the issue?

A. Enable synchronization between the neighbors to bring the neighborship up.  
B. The route map on eBGP sessions must allow the prefixes from the neighbor.  
C. Configure the ebgp-multihop 2 command on R1 toward the neighbor.  
D. Configure a valid router ID on the neighbor that shows an invalid router ID of 0.0.0.0.  

Correct Answer: C [file:610]

- 問題文要約（日本語）:  
BGP ピアが「Established」にならず、ネイバー関係が確立できていない状況。  
このとき、どの設定を入れれば eBGP 隣接が張れるかを問う問題。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 問題の前提（PDF 解説より）では、**eBGP ピア同士が直接接続ではなく、1ホップ以上離れている構成**になっている。  
    - eBGP のデフォルト仕様:  
      - **TTL=1** でネイバーに TCP セッションを張ろうとするため、1ホップを超える場合は途中で TTL が 0 になり、セッション確立前に落ちる。 [file:610]  
  - そのため、直接接続でない eBGP ピアを成立させるには:
    - `neighbor x.x.x.x ebgp-multihop <n>` で TTL を増やし、「n ホップ先まで eBGP セッション許可」とする必要がある。  
    - 問題文では 2 ホップ相当が想定されているため、`ebgp-multihop 2` が妥当。 [file:610]  
  - 他の選択肢が誤りな理由:
    - A: eBGP ネイバーが上がらない原因は**同期（synchronization）ではなく、TTL 到達不能**。同期を有効にしてもセッション自体は張れない。  
    - B: ルートマップで prefix を拒否しても「BGP セッション自体」は Established になる。ネイバーが「上がらない」原因にはならない。  
    - D: Router ID 0.0.0.0 は確かに不正だが、多くの場合は自動でループバックや最高 IP を拾うか、少なくとも **TCP セッションの張りそのもの**には直結しない。今回の典型パターンは「マルチホップ eBGP 未設定」。 [file:610]  

- 試験向けの覚え方フレーズ

  - **「eBGP で ‘直結じゃないピア’ → まず ebgp-multihop を疑え」**  
  - 一言パターン:
    - **「TTL=1 の eBGP は“直結前提”、1 ホップ超えたら ‘multihop’」**  
  - セットで暗記:
    - **「iBGP は普通マルチホップ、eBGP は普通シングルホップ」**  
      → eBGP で L0 同士・間に P あり、など「経路がワンクッション」入る構成を見たら **ebgp-multihop**。  

***
QUESTION 633 解説

- 問題文（英語）:  
The neighboring router does not show any summarized prefixes. How must the configured summarization be seen on the neighboring router?

A. There is no network statement for the summary prefix within EIGRP. The network 172.18.0.0 command must be configured under the EIGRP process.  
B. The summary mask is incorrect. Create a loopback interface within the summary range and enable within the EIGRP process with the network command to generate the summary.  
C. The auto summarization feature will be summarizing all routes into a single update and must be disabled by use of the no auto-summary command.  
D. There is no network statement for the summary prefix within EIGRP. The network 172.18.80.0 0.0.15.0 command must be configured under the EIGRP process.  

Correct Answer: B [file:610]

- 問題文要約（日本語）:  
EIGRP でサマリを設定したのに、隣接ルータ側で **サマリプレフィックスが見えない** 状況。  
どのようにすれば、設定したサマリがネイバーで確認できるようになるかを問う。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - EIGRP のインタフェースサマリ（`ip summary-address eigrp ...`）は、**そのサマリ範囲内に少なくとも 1 つの EIGRP ルートが存在する場合にだけ** 生成される。  
    - 例: サマリ `172.18.80.0/20` を設定しても、R1 がその範囲内のプレフィックス（172.18.80.0/24〜172.18.95.0/24 など）を EIGRP 経由で持っていなければ、サマリは広告されない。 [file:610]  
  - 本問の前提では:
    - R1 はサマリ範囲内の EIGRP ルートを持っていないため、**サマリ自体が生成されていない**。  
  - 選択肢 B の「Loopback を作る」案:
    - サマリレンジ内のアドレス（例: 172.18.80.x）でループバックインタフェースを作成。  
    - そのインタフェースを `network` コマンドで EIGRP に参加させる。  
    - すると、そのループバック宛の個別ルートが EIGRP に乗る → サマリ条件を満たし、**サマリプレフィックスがネイバーへ広告される**。 [file:610]  
  - 他の選択肢が不適切な理由:
    - A/D: `network` コマンドは **サマリ生成の条件とは無関係**（あくまで「どのインタフェースを EIGRP に参加させるか」）。  
      - サマリの有無は「サマリ範囲内プレフィックスの存在」で決まるため、network をサマリプレフィックスで入れても意味がない。  
    - C: auto-summary はクラスフル境界での自動集約の話で、本問の「手動サマリが出ない」問題とは別の次元。 [file:610]  

- 試験向けの覚え方フレーズ

  - **「EIGRP サマリは“中身ゼロ”だと出ない → 1 本でいいから中身を作る」**  
  - 一行パターン:
    - **「サマリが出ない → サマリレンジ内で ‘Loopback＋network’ でダミールートを作る」**  
  - セット暗記:
    - **「summary-address は ‘条件付き広告’：範囲内ルートが 1 本もないと無効」**  

***
QUESTION 634 解説

- 問題文（英語）:  
A network engineer is troubleshooting an AAA authentication issue for R1 from R2. When an engineer tries to open a telnet connection to R1, it opens the connection but shows a %Authorization failed error message on the terminal and closes the connection silently. Which action resolves the issue?

A. Configure authorization commands in the tacacs+ server for the R1 router.  
B. Configure the tacacs+ server host IP on the R1 router.  
C. Resolve tacacs+ server host IP authentication miss configuration on the R1 router.  
D. Resolve tacacs+ server reachability from the R1 router.  

Correct Answer: D [file:610]

- 問題文要約（日本語）:  
R2 から R1 へ telnet すると、接続は開くがすぐに「%Authorization failed」と表示されて切断される AAA 認可の問題。  
TACACS+ を使っている状況で、どの対応をすればこの問題を解決できるかを問う。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - PDF の解説では、R1 にはすでに `tacacs+ server`（IP アドレス 172.20.32.1）が正しく設定済みであると明記されている。  
    → よって **サーバ IP 未設定（B）** や **ホスト IP 誤設定（C）** は原因ではない。 [file:610]  
  - Telnet で接続は開くが、すぐに `%Authorization failed` で落ちる典型パターン:
    - R1 は AAA 認証・認可で **TACACS+ サーバに問い合わせに行くが応答が得られない（到達不能）**。  
    - 認証までは通っている・あるいはローカル fallback で入れても、**Authorization フェーズでサーバに到達できず失敗** → セッションが閉じる。  
  - 選択肢 D: 「R1 から TACACS+ サーバへの到達性を復旧する」がこの状況に直結する。
    - ルーティング・ACL・FW などで 172.20.32.1 への TCP/49 が疎通できるようにするのが本質的な解決策。 [file:610]  
  - 他の選択肢が不適切な理由:
    - A: サーバ側のコマンド権限やコマンドセットが未定義であれば、**認可ポリシーの内容がおかしい**という別問題になるが、今回のように「サーバ到達性が怪しい」ときの第一優先ではない。  
    - B: すでに 172.20.32.1 が `tacacs+ server` として設定済みであるため、再設定しても根本は変わらない。  
    - C: 「認証ミス設定」は、ユーザ名・キー・ポートなどの誤りを指すが、PDF 解説では IP 設定自体は正しい前提で、「おそらく R1 からサーバに到達できていない」と結論づけている。 [file:610]  

- 試験向けの覚え方フレーズ

  - **「AAA: ‘Authorization failed’ ＋ TACACS 使用中 → まず“サーバに届いてるか”を疑え」**  
  - 一行パターン:
    - **「AAA トラブル三兄弟：Reachability → Key → User。到達性が一番最初」**  
  - TACACS+ 系問題での優先チェック順:
    - 1. **疎通（ping／route／ACL／TCP 49）**  
    - 2. **key・ポート・サーバ定義**  
    - 3. **サーバ側ユーザ・コマンド権限**  

***
QUESTION 635 解説

- 問題文（英語）:  
R1 and R2 have been configured where the neighbor relationship must be authenticated using MD5:

R1  
router bgp 100  
 neighbor 172.16.1.2 remote-as 200  
 neighbor 172.16.1.1 password cisco123  

R2  
router bgp 200  
 neighbor 172.16.1.1 remote-as 100  
 neighbor 172.16.1.1 password cisco  

The neighbor relationship is not coming up. Which configuration resolves the issue?

A. R2  
router bgp 200  
 neighbor 172.16.1.1 password cisco123  

B. R1  
router bgp 100  
 neighbor 172.16.1.2 password MD5 cisco123  
R2  
router bgp 200  
 neighbor 172.16.1.1 password MD5 cisco123  

C. R1  
router bgp 100  
 neighbor 172.16.1.2 password MD5 cisco  
R2  
router bgp 100  
 neighbor 172.16.1.1 password MD5 cisco  

D. R1  
router bgp 100  
 neighbor 172.16.1.2 password cisco  

Correct Answer: D [file:610]

- 問題文要約（日本語）:  
R1 と R2 の eBGP ピアは MD5 認証付きで張る想定だが、ネイバーが Establish しない。  
設定を見ると、R1 側の neighbor password の IP とパスワードが R2 と一致していない。どの設定変更でピアを正常に張れるか。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 現状の問題点は 2 つ:
    - R1 で password を設定している neighbor IP が **誤り**。  
      - 本来、対向は 172.16.1.2（R2）だが、`neighbor 172.16.1.1 password ...` になっている。  
    - R1 と R2 で **パスワード不一致**。  
      - R1: `cisco123` / R2: `cisco`。[file:610]  
  - BGP の MD5 認証の要件:
    - 双方の neighbor 行で、**同じ IP ペア**同士に対して  
    - **完全に同じパスワード** を設定する必要がある。  
  - 選択肢 D の内容:
    - R1 側で `neighbor 172.16.1.2 password cisco` と設定し直すイメージ。
    - これにより:
      - neighbor IP が R2 宛（172.16.1.2）に修正される。  
      - パスワードも R2 側（`cisco`）と一致する。  
    → 2 つの不整合を一度に解消できる。 [file:610]  
  - 他の選択肢が誤りな理由:
    - A: IP は一致するが、R2 側のパスワードを `cisco123` にすると、今度は R2 の neighbor IP/remote-as 設定と整合が取れず、R1 側の誤った neighbor IP はそのまま。  
    - B/C: `password MD5 ...` という構文は Cisco IOS では無効。  
      - 実際のコマンド補完では `neighbor x.x.x.x password ?` の後に `<0-7>` or `LINE` しか出ず、**MD5 キーワードは存在しない**。MD5 は BGP neighbor password のデフォルトアルゴリズム。 [file:610]  

- 試験向けの覚え方フレーズ

  - **「BGP 認証 NG → ‘IP とパス両方ペアで揃ってるか？’ を真っ先に疑え」**  
  - 一行パターン:
    - **「neighbor password に MD5 は書かない、書くのは“IP と同じパス”だけ」**  
  - セット暗記:
    - **「eBGP 認証トラブル三点チェック：neighbor IP / remote-as / password」**  

***
QUESTION 636 解説

- 問題文（英語）:  
A user from Area-55 cannot access the HTTPS services running on the Area-0 server farm. An engineer identifies that the routing table of SW-1 is not learning the Area-55 routes. Which configuration resolves the issue?

A. R1  
router ospf 100  
 area 9 virtual-link 10.12.9.1  
R3  
router ospf 100  
 area 9 virtual-link 1.23.55.2  

B. R3  
router ospf 100  
 area 9 virtual-link 1.13.1.1  
R1  
router ospf 100  
 area 9 virtual-link 1.12.1.1  

C. R2  
router ospf 100  
 area 9 virtual-link 1.11.1.1  
R1  
router ospf 100  
 area 9 virtual-link 1.12.1.1  

D. R1  
router ospf 100  
 area 9 virtual-link 10.110.0.1  
R2  
router ospf 100  
 area 9 virtual-link 1.12.9.1  

Correct Answer: C [file:610]

- 問題文要約（日本語）:  
Area-55 のユーザが、Area 0 のサーバファーム上の HTTPS に到達できない。  
SW-1 のルーティングテーブルを見ると、Area-55 のルートが学習されていない。  
OSPF エリア設計上、バックボーン Area 0 との論理接続に問題があり、その修正としてどの virtual-link 設定が正しいかを問う。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 背景とポイント:
    - OSPF では、**すべての非バックボーンエリアは Area 0 と論理的に接続されている必要**がある。  
    - 物理的に Area 0 が分断される／Area 0 に直接接続していないエリアがあるときは、**仮想リンク（virtual-link）** を用いて Area 0 との論理接続を張る。 [file:610]  
  - virtual-link の指定形式:
    - `area <transit-area> virtual-link <remote-router-id>`  
      - `<transit-area>` はバックボーン以外の「中継エリア」。  
      - `<remote-router-id>` は **対向ルータの OSPF ルーター ID**（通常 loopback のアドレス）を指定する。  
    - 従って、virtual-link 設定では:
      - 両側ルータとも **同じ transit area（ここでは area 9）** を指定  
      - 互いの **router-id を相手側の virtual-link コマンドに記述** する必要がある。 [file:610]  
  - 本問のポイント:
    - Area-55 側を収容しているのが R2、バックボーン側に近いのが R1 という構成で、Area 9 上をトランジットに virtual-link を張る想定。  
    - よって:
      - R2: `area 9 virtual-link <R1 の router-id>`  
      - R1: `area 9 virtual-link <R2 の router-id>`  
    - 選択肢 C は、R2 と R1 の間で **area 9 を transit とし、互いの router-id を指定した形**になっており、OSPF の要件に合致する。 [file:610]  
  - 他選択肢が不正な理由（概要）:
    - A/B/D は、virtual-link の相手 router-id に **実 IP（インタフェースアドレス）や誤った ID** を入れていたり、**関係ないルータ間**で virtual-link を張ろうとしているため、Area-55 から Area 0 までの論理バックボーン接続を正しく構成できない。  

- 試験向けの覚え方フレーズ

  - **「OSPF 仮想リンク = ‘エリア番号 + 相手の router-id’」**  
  - 一行パターン:
    - **「area X virtual-link“IP じゃなく router-id”」**  
  - セット暗記:
    - **「バックボーンから離れたエリアが見えない → ‘transit area 経由の virtual-link’ を疑え」**  

***
QUESTION 637 解説

- 問題文（英語）:  
An engineer investigates an IPv6 EIGRP neighbor adjacency issue that sees the neighbors flapping and issued a ping from R1 to its directly connected neighbor. The link between the switches is stable at Layer 2, and other connected devices are also functioning. Which action resolves the issue?

A. The switch between the two neighbors is not configured for IPv6 multicast and must be configured for IPv6 multicast.  
B. The switch between the two neighbors is not IPv6 compatible and must be replaced with an IPv6 compatible device.  
C. Multicast packets are not reliably transmitted over the link, and the switch must be replaced.  
D. Data is not reliably transmitted between the dynamically assigned link-local addresses of the routers and must be manually assigned.  

Correct Answer: C [file:610]

- 問題文要約（日本語）:  
IPv6 EIGRP ネイバーがフラップしており、R1 から直接接続の隣接ルータへ ping を打つと、2/4 など成功率が低い。  
スイッチ間リンクは L2 的には安定しており、他の接続デバイスは正常に動作している。  
どの対応がこの IPv6 EIGRP 障害を解決できるか。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 状況から分かること:
    - L2 のリンクは「UP」と認識されており、他のホストは問題なく通信できている。  
    - しかし、**R1→隣接ルータへの ping が 4発中 2発しか成功しない** など、パケットロスが発生している。 [file:610]  
    - EIGRPv6 は IPv6 マルチキャスト（リンクローカル）で Hello をやり取りするため、**マルチキャストの信頼性が低いとネイバーがフラップ**しやすい。  
  - A（IPv6 multicast 未設定）について:
    - 他のデバイスは動いており、L2 スイッチのマルチキャスト基本機能は動作していると考えられる。  
    - 問題は「設定不足」より **パケットが物理的にロスする挙動** に近い。  
  - B について:
    - 「IPv6 非対応スイッチ」なら、そもそも IPv6 トラフィック全般が動かない可能性が高い。  
    - 質問文では「他の接続デバイスは機能している」とあるため、**完全非対応**ではない。  
  - D について:
    - link-local アドレスが動的／手動かで、ここまで安定性に差が出るケースは稀。  
    - しかも ping ロスという物理層寄りな症状とは結びつきにくい。  
  - C の妥当性:
    - ping の半分失敗という状況は、**当該リンク（もしくは中継スイッチ）のハード故障 / ポート不良 / ASIC 異常**を強く示唆。  
    - EIGRPv6 の Hello/Update も同リンク上のマルチキャストに依存するため、**マルチキャストパケットが安定して流れないとネイバーが頻繁にダウン**する。  
    → 問題リンク上のマルチキャスト伝送の信頼性が低く、**スイッチ交換が必要**という結論は妥当。 [file:610]  

- 試験向けの覚え方フレーズ

  - **「L2 は UP・他ホスト OK・EIGRPv6 だけフラップ → まず“リンク品質”を疑え」**  
  - 一行パターン:
    - **「IPv6 EIGRP 障害：Hello はマルチキャスト、マルチキャストはリンク品質に正直」**  
  - セット暗記:
    - **「設定系（multicast 設定/IPv6 対応）より先に、“パケットロス＝物理 or ハード不良”をチェック」**  

***
QUESTION 638 解説

- 問題文（英語）:  
Clients on ALS2 receive IPv4 and IPv6 addresses, but clients on ALS1 receive only IPv4 addresses and not IPv6 addresses. Which action on DSW1 allows clients on ALS1 to receive IPv6 addresses?

A. Configure DSW1(config-if)#ipv6 dhcp relay destination 2002:404:404::404:404 GigabitEthernet1/2  
B. Configure DSW1(dhcp-config)#default-router 2002:A04:A01::A04:A01  
C. Configure DSW1(config-if)#ipv6 helper address 2002:404:404::404:404  
D. Configure DSW1(config)#ipv6 route 2002:404:404::404:404/128 FastEthernet1/0  

Correct Answer: A [file:610]

- 問題文要約（日本語）:  
DHCP ルータ DHCP が ALS1/ALS2 に対して IPv4/IPv6 を払い出す構成。  
ALS2 側クライアントは IPv4/IPv6 両方取得できるが、ALS1 側は IPv4 のみ取得し IPv6 が取れない。  
DSW1 でどの設定を入れれば、ALS1 側クライアントも IPv6 アドレスを取得できるか。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - PDF の図では、DSW1 の FastEthernet0/0（ALS1 側）には以下のみ設定されている。 [file:610]  
    - `ip address 10.4.10.1 255.255.255.0`  
    - `ip helper-address 4.4.4.4`（IPv4 DHCP relay）  
    - `ipv6 address 2002:A04:A01::A04:A01/120`  
    - `ipv6 enable`  
    → **IPv6 DHCP リレーの設定がない**。  
  - 一方、ALS2 側に接続されているインターフェース（出題文中 Remarks に言及）は、すでに  
    - `ipv6 dhcp relay destination 2002:404:404::404:404 GigabitEthernet1/2`  
    が設定されており、ALS2 クライアントは DHCPv6 でアドレスを取得できている。 [file:610]  
  - Cisco IOS で IPv6 DHCP Relay を行う正しい構文:
    - インターフェース設定モードで  
      - `ipv6 dhcp relay destination <DHCPv6サーバIPv6アドレス> <転送先インターフェース>`  
  - 選択肢の検証:
    - A: ALS1 側インターフェースにも ALS2 と同様の  
      - `ipv6 dhcp relay destination 2002:404:404::404:404 GigabitEthernet1/2`  
      を設定する案。  
      → IPv6 DHCP 要求を DSW1 から DHCPv6 サーバへ中継できるようになり、ALS1 クライアントも IPv6 を取得可能。  
    - B: DHCPv6 プールの `default-router` 設定に見えるが、DSW1 は DHCP サーバではなく「リレー」。  
      → サーバ側の設定であり、この問題の「DSW1 上の欠落」には対応しない。  
    - C: `ipv6 helper address` というコマンドは存在せず、IPv4 DHCP の `ip helper-address` と混同させる誤答。  
    - D: 単なる IPv6 静的ルートであり、DHCPv6 リレー動作には関係しない。  
  - よって、「ALS1 側インターフェースに IPv6 DHCP Relay を追加する」A が正しい。 [file:610]  

- 試験向けの覚え方フレーズ

  - **「IPv4 は ip helper-address、IPv6 は“ipv6 dhcp relay destination”」**  
  - 一行パターン:
    - **「v6 DHCP 中継: ‘helper’ じゃなく ‘dhcp relay destination’」**  
  - セット暗記:
    - **「片側だけ v6 が来ない → その VLAN のインターフェースに IPv6 DHCP relay 入れ忘れを疑え」**  

***
QUESTION 639 解説

- 問題文（英語）:  
The network administrator configured CoPP so that EIGRP traffic toward the device is limited to 2 mbps. Any traffic that exceeds this limit must also be allowed at this point for traffic analysis.

```text
access-list 100 permit EIGRP any any
!
class-map CM-EIGRP
 match access-group 100
!
policy-map PM-COPP
 class CM-EIGRP
  police 2000000 conform-action transmit
!
control-plane
 service-policy output PM-COPP
```

No EIGRP traffic is going through CoPP. Which configuration resolves the issue?

A.  
policy-map PM-COPP  
 class CM-EIGRP  
  police 2000000 conform-action transmit exceed-action transmit  
!  
control-plane  
 no service-policy output PM-COPP  
 service-policy input PM-COPP  

B.  
policy-map PM-COPP  
 class CM-EIGRP  
  police 2000000 conform-action drop exceed-action drop  
!  
control-plane  
 no service-policy output PM-COPP  
 service-policy input PM-COPP  

C.  
policy-map PM-COPP  
 class CM-EIGRP  
  police 2000000 conform-action transmit exceed-action transmit  

D.  
control-plane  
 no service-policy output PM-COPP  
 service-policy input PM-COPP  

Correct Answer: A

- 問題文要約（日本語）:  
CoPP で「デバイス宛ての EIGRP トラフィックを 2 Mbps に制限しつつ、2 Mbps を超えた分も分析のため DROP せず通したい」ポリシーを作ったが、実際には EIGRP トラフィックが CoPP でまったくカウントされていない。どの設定変更で目的どおり動作させられるか。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 現状の問題点は 2 つ:
    1. **exceed-action が未指定**  
       - `police 2000000 conform-action transmit` のみだと、規定値として exceed は drop になる実装が多い。  
       - 「2 Mbps を越えたトラフィックも許可したい」要件を満たすには、**conform と exceed の両方を transmit** にする必要がある。  
    2. **CoPP を output に適用している**  
       - Control-plane の **output** ポリシーは「CPU から出ていく」トラフィックに適用される。  
       - EIGRP Hello/Update は基本的に「外から CPU へ入ってくる」ため、**input 方向で CoPP をかけないとマッチしない**。  
  - 選択肢 A はこの 2 点を同時に修正している:
    - `exceed-action transmit` を追加し、**2 Mbps 超過分も通す**。  
    - `no service-policy output PM-COPP` → `service-policy input PM-COPP` で、**入力方向 CoPP** に切り替える。  
    → これでデバイス宛て EIGRP トラフィックが CoPP でカウントされつつ、制限超過分も分析可能になる。  
  - 他の選択肢が合わない理由:
    - B: exceed-action が drop なので要件（超過分も許可）に反する。  
    - C: exceed-action を transmit にしているが、**方向が output のまま**なので依然として EIGRP トラフィックはマッチしない。  
    - D: input 適用にはするが、**超過分についての exceed-action が未設定**のまま。  

- 試験向けの覚え方フレーズ

  - **「CoPP で守るのは“CPUに入る”トラフィック → 方向は基本 input」**  
  - 一行パターン:
    - **「police で“超過も通す”なら conform transmit ＋ exceed transmit」**  
  - セット暗記:
    - **「CoPP で分析もしたい時は『input で当てる＋exceed-action transmit』」**  

***
QUESTION 640 解説

- 問題文（英語）:  
Host A successfully pings host C, but host B fails to ping host C. Which action resolves this issue?

A. Redistribute EIGRP 100 into RIP on R1.  
B. Disable auto-summary in RIP and EIGRP.  
C. Enable loose mode uRPF.  
D. Change the first statement in access list 100 to permit access.  

Correct Answer: C [file:610]

- 問題文要約（日本語）:  
ホスト A からホスト C への ping は成功するが、ホスト B からホスト C への ping は失敗する。  
R1 は R2 側から EIGRP で 10.1.23.0/24（Host A/B がいるネットワーク）を学習し、そのルートを RIP から来たルートより優先してルーティングテーブルに入れている。  
このとき、Host B からのトラフィックが R3 経由で R1 に入ると uRPF strict でドロップされている状況。どの対応が問題を解決するか。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 状況のキー:
    - R1 は 10.1.23.0/24 を **EIGRP 経由（R2 向き）** でルーティングテーブルに持っている。  
    - uRPF strict mode は「ルーティングテーブルの“戻りの経路”の outgoing IF と、実際にパケットが入ってきた IF が一致しているか」をチェックする。  
    - Host B → Host C の経路は、R3 → R1（RIP 経路側）を通ってくるため、**R1 上の“戻りの経路”（EIGRP で R2 向き）と IF が不一致**。  
    → strict mode のチェックに失敗し、R1 でパケットが破棄される。 [file:610]  
  - Host A が成功する理由:
    - トポロジ上、Host A からのトラフィックは R2 経由で R1 に到達する経路が uRPF のチェックと一致しており、strict を満たしている。  
  - 解決策としての loose mode:
    - uRPF loose mode（`ip verify unicast source reachable-via any`）は  
      - 「ルーティングテーブルにその送信元プレフィクスが存在するか」だけを見て、**入ってきたインタフェースは問わない**。  
    - このモードなら、10.1.23.0/24 のルートが R1 に存在する限り、R3 から来た Host B のトラフィックも許可される。  
    → 非対称ルーティングがある環境で uRPF を使う典型的な解決策。 [file:610]  
  - 他の選択肢が不適切な理由:
    - A: 再配布を変えても「非対称パス＋strict uRPF」の根本は変わらないケースが多い。  
    - B: auto-summary の有無は、この問題の核心である「受信 IF と戻り IF の不一致」と直接関係がない。  
    - D: ACL が問題という記述はなく、uRPF によるドロップが原因と説明されている。  

- 試験向けの覚え方フレーズ

  - **「uRPF strict は“対称ルート専用”、非対称ルートがあるなら loose にせよ」**  
  - 一行パターン:
    - **「‘reachable-via rx’ で落ちる非対称 → ‘reachable-via any’ に変える」**  
  - セット暗記:
    - **「Host A OK / Host B NG＋非対称経路＋uRPF → ‘strict を loose に’ が鉄板パターン」**  

***
QUESTION 641 解説

- 問題文（英語）:  
A network administrator is troubleshooting an authentication failure issue during login into R2 using a RADIUS server user account. The network administrator can ping the RADIUS server, but the user access to SSH is denied on R2. Which action resolves the issue?

A. Enable AAA authorization to allow SSH connections.  
B. Modify the aaa command to replace TACACS+ with RADIUS.  
C. Configure the username in RADIUS server database.  
D. Remove "local" from the aaa authentication command.  

Correct Answer: C [file:610]

- 問題文要約（日本語）:  
R2 に RADIUS 認証ユーザで SSH ログインしようとすると認証失敗する。  
R2 から RADIUS サーバへの疎通（ping）は OK だが、SSH ログインは拒否される。  
この問題を解決するには何をすべきか。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - PDF の解説では、**R2 側の AAA 設定は正しく行われている前提**になっている。  
    - `aaa new-model`  
    - `aaa authentication login ... group radius local`  
    - line vty に AAA 認証リストが適用済み、など。  
    → つまり「デバイス側の AAA/RADIUS 設定ミス」ではない。 [file:610]  
  - さらに:
    - R2 から RADIUS サーバへは ping 可能であり、**到達性やポートブロックではない**。  
    - TACACS+ を使っているわけではないので、B の「TACACS+ を RADIUS に変える」は論点ズレ。  
  - 認証失敗の最も自然な原因:
    - RADIUS サーバの「ユーザデータベース」に、SSH ログインに使っている **username/password のエントリが存在しない**。  
    - その場合、RADIUS は認証拒否を返し、R2 側では AAA 認証失敗となる。  
    → 解説も「唯一残る理由は RADIUS 側に username/password が未登録」と明言している。 [file:610]  
  - 他選択肢が誤りな理由:
    - A: AAA **authorization** は、認証「後」の権限付与の話であり、「ログインそのものが失敗」している本件には関係ない。  
    - B: 既に RADIUS ベースで AAA が組まれているという前提。TACACS+ の置き換えは不要。  
    - D: `local` は RADIUS が落ちた際のフォールバックに使うことが多く、これを削ると**むしろ障害時にログイン不能**になりやすい。  
      - 今回の認証失敗原因は「RADIUS 側にユーザがいない」ことであり、`local` の有無では解決しない。  

- 試験向けの覚え方フレーズ

  - **「AAA 周り OK＋RADIUS には届くのに認証失敗 → まず“RADIUS DB にユーザある？”」**  
  - 一行パターン:
    - **「到達 OK・設定 OK・認証 NG → ‘サーバ側ユーザ未登録’ を疑え」**  
  - セット暗記:
    - **「TACACS/RADIUS トラブル切り分け：①疎通 ②key/port ③AAA 設定 ④サーバDB（ユーザ定義）」**  

***
QUESTION 642 解説

- 問題文（英語）:  
R1 must advertise all loopback interfaces IP addresses to neighbors, but EIGRP neighbors receive a summary route. Which action resolves the issue?

A. Disable auto summarization on R1.  
B. Redistribute connected routes into EIGRP.  
C. Enable EIGRP on loopback interfaces.  
D. Remove the 10.100.1.0/24 static route.  

Correct Answer: C [file:610]

- 問題文要約（日本語）:  
R1 は複数のループバックインターフェースを持ち、それぞれの IP アドレスを EIGRP で隣接ルータへ広告したい。  
しかし実際には、隣接 EIGRP ルータ側では個々の loopback ネットワークではなく、10.100.1.0/24 のサマリールートだけが見えている。  
この状況を解決するにはどの設定を行うべきか。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - PDF の解説より: R1 のルーティングテーブルに **10.0.0.0/8 の EIGRP Null0 ルートが存在しない**ため、auto-summary は有効になっていない。  
    → よって A「auto summarization を無効化」は関係ない。 [file:610]  
  - R1 には `10.100.1.0/24` の static Null0 ルートがあり、これは「**directly connected**」扱いなので、EIGRP の `network 10.100.1.0 0.0.0.0` などにマッチして 10.100.1.0/24 が 1 本の経路として広告される。  
    - これが問題文の「summary route」に相当する。 [file:610]  
  - 重要なポイント:
    - `network 10.100.1.0 0.0.0.255` であれば、その範囲内の loopback ネットワークも含めて広告されるはず。  
    - しかし問題文の状況から、実際の設定は **`network 10.100.1.0 0.0.0.0` のように static /24 にだけマッチ**しており、lo10, lo20 などのループバックは EIGRP プロセスに参加していないと推測できる。  
  - loopback のネットワークを広告する方法は 2 つ:
    1. `redistribute connected` で connected ルートを EIGRP に再配布（B）。  
    2. **loopback インターフェースで EIGRP を有効化**する（`network` コマンド）。（C）  
  - 解説では、設計上よりクリーンで制御しやすい方法として **C を推奨**している。  
    - 例:  
      - `interface Loopback10` / `ip address 10.100.1.10 255.255.255.255`  
      - `router eigrp 100`  
        - `network 10.100.1.10 0.0.0.0`  
      → これにより loopback の個別ルートが EIGRP で広告される。  
  - 他の選択肢が誤りな理由:
    - A: auto-summary は既に無効（Null0 の 10.0.0.0/8 がない）なので関係ない。  
    - B: 動作はするが、全 connected を再配布してしまいスコープが広く、EIGRP 設計としてはあまり好ましくない。  
    - D: static Null0 ルートを消しても、loopback を EIGRP に参加させなければ広告はされない。  

- 試験向けの覚え方フレーズ

  - **「summary しか見えない時は ‘auto-summary’ より先に ‘どの IF で EIGRP 動かしてる？’ を確認」**  
  - 一行パターン:
    - **「loopback 出したい → redistribute connected より network で loopback 参加」**  
  - セット暗記:
    - **「static Null0 のサマリだけ飛んでる時は、loopback で EIGRP を有効化して細かいプレフィックスを出す」**  

***
QUESTION 643 解説

- 問題文（英語）:  
An error message "an OSPF-4-FLOOD_WAR" is received on SW2 from SW1. SW2 is repeatedly receiving its own link-state advertisement and flushes it from the network. Which action resolves the issue?

A. Change area 5 to a normal area from a nonstub area.  
B. Resolve different subnet mask issue on the link.  
C. Resolve duplicate IP address issue in the network.  
D. Configure Layer 3 port channel on interfaces between switches.  

Correct Answer: C [file:610]

- 問題文要約（日本語）:  
SW2 が SW1 から「OSPF-4-FLOOD_WAR」というエラーメッセージを受信している。  
SW2 は「自分自身の LSA を繰り返し受け取り続けている」と判断し、その LSA をフラッシュしている状況。  
この問題を解決するには何をすべきか。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - PDF の解説より: **OSPF flood war** は、ルータが「**自分の LSA を自分で何度も受信している**」と検出したときに発生し、その LSA をフラッシュしたり、新しいバージョンの LSA を再送出したりする。 [file:610]  
  - このメカニズムは、主に次の 2 パターンの異常検出のためにある。 [file:610]  
    - Type-2 LSA（Network LSA）: セグメント上で **重複した IP アドレス** が存在する場合。  
    - Type-5 LSA（External LSA）: 複数エリアにまたがって **Router ID が重複** している場合。  
  - 今回の記述では「SW2 が自分の LSA を繰り返し受け取っている」とあるため、  
    - SW1/SW2 間、または同一セグメント上で **重複 IP アドレスまたは重複 Router ID** が存在すると解釈するのが自然。  
    - 選択肢の中でこれに該当するのは「C. 重複 IP アドレス問題の解消」。  
  - 他の選択肢が不適切な理由:
    - A: エリア種別（stub / normal）の問題ではなく、LSA の「自己再受信」がトリガ。  
    - B: サブネットマスクの不一致はネイバー不成立や 2WAY/EXSTART ステート問題を起こすが、**自分 LSA の flood war** とは別の症状。  
    - D: L3 ポートチャネルの有無は OSPF の flood war 検出とは直接関係しない。  

- 試験向けの覚え方フレーズ

  - **「OSPF FLOOD_WAR＝『自分の LSA が戻ってくる』→ IP or RID の“重複”を疑え」**  
  - 一行パターン:
    - **「FLOOD_WAR を見たらまず『duplicate IP / Router-ID』」**  
  - セット暗記:
    - **「Type-2 FLOOD_WAR → セグメント内 IP 重複、Type-5 FLOOD_WAR → エリア跨ぎの RID 重複」**  

***
QUESTION 644 解説

- 問題文（英語）:  
After a misconfiguration by a junior engineer, the console access to router A is not working. Which configuration allows access to router A?

A.  
RouterA(config)#line console 0  
RouterA(config-line)#password cisco  
RouterA(config)#end  

B.  
RouterA(config)#line console 0  
RouterA(config-line)#password cisco  
RouterA(config-line)#login local  
RouterA(config)#end  

C.  
RouterA(config)#aaa new-model  
RouterA(config)#line console 0  
RouterA(config-line)#login authentication CONSOLE  

D.  
RouterA(config)#aaa new-model  
RouterA(config)#aaa authentication login my-auth-list tacacs+  

Correct Answer: A [file:610]

- 問題文要約（日本語）:  
ジュニアエンジニアの誤設定が原因で、Router A のコンソールからログインできなくなった。  
コンソールアクセスを復旧するには、どの設定を入れればよいか。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 問題の前提は「**すでに誤った AAA 設定が入っていて、コンソールから入れない**」ケースである。  
    - 典型例: `aaa authentication login default local` だけ入っていて、ユーザが未定義 or AAA 周りが壊れている。  
  - この状態から最優先でやりたいのは、「**AAA をこれ以上いじらずに、最低限の password 認証でコンソールを開ける**」こと。  
    - 選択肢 A は、`line console 0` に単純に `password cisco` を設定して終了しており、  
      - すでに `login` がデフォルトで有効 or AAA が未有効であれば、この設定だけでコンソールログインが復活する。 [file:610]  
  - 他の選択肢が危険・不適切な理由:
    - B: `login local` を有効化すると「**ローカルユーザデータベース必須**」になるが、問題文はユーザ定義があるとは言っていない。  
      → ユーザが無ければ余計にロックアウトを悪化させる可能性が高い。  
    - C/D: どちらも `aaa new-model` を有効化して AAA 認証を導入しようとしており、  
      - すでに AAA による誤設定でコンソールが死んでいるケースでは、さらに複雑化・ロックアウトを招きやすい。  
      - TACACS+ 用の `aaa authentication` を定義するだけで、line console への適用もされていない。  
  - 出題の意図は、「AAA 周りが壊れている時、**いったんシンプルな line password 認証に戻してコンソールを開け直す**」ことを選べるかどうか。  

- 試験向けの覚え方フレーズ

  - **「コンソール死んだら、まず fancy AAA じゃなく ‘line console＋password’ に戻す」**  
  - 一行パターン:
    - **「local も AAA も怪しい時は、line console 0＋password で蘇生」**  
  - セット暗記:
    - **「AAA 誤爆でロックアウト → 新たな aaa 追加は厳禁、まず『A なシンプル line 設定』」**  

***
QUESTION 645 解説

- 問題文（英語）:  
R1 cannot reach the R4 loopback address (2001::4/128). Which configuration provides the most optimal and scalable resolution to the issue?

A.  
R2  
router bgp 100  
address-family ipv6  
no synchronization  

B.  
R2  
router bgp 100  
address-family ipv6  
neighbor 2001::4 next-hop-unchanged  
redistribute connected  

C.  
R2  
router bgp 100  
address-family ipv6  
neighbor 2001::1 next-hop-self  

D.  
R1  
router bgp 100  
address-family ipv6  
no synchronization  

Correct Answer: C [file:610]

- 問題文要約（日本語）:  
R1 から R4 の loopback アドレス 2001::4/128 へ到達できない。  
R1–R2–R4 間は IPv6 BGP でつながっているが、R1 には 2001::4 の経路が正しくインストールされていない状況。  
スケーラブルかつ最適な解決策はどの設定か。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - PDF の解説では、R1 から見た 2001::4/128 の BGP 経路の **next-hop が到達不能** になっていることが原因とされている。  
    - R4 のループバックを R2 が eBGP/iBGP で受け取り、R1 に再広告しているが、その際に **next-hop を書き換えていない**。  
    - 結果として、R1 は R4 のアドレスを next-hop にした経路を受け取るものの、その next-hop へのルートが無く、FIB にインストールされない。 [file:610]  
  - BGP での典型的な解決策:
    - **中継する iBGP ピアが next-hop-self を付けて再配布**する。  
    - 選択肢 C はまさにそれで、R2 側で  
      - `address-family ipv6`  
      - `neighbor 2001::1 next-hop-self`  
      を設定し、R1 から見た 2001::4/128 の next-hop を「R2 自身」に書き換える。  
    - R1 はすでに R2 への到達性を持つので、その経路を問題なくインストールできる。  
  - 他の選択肢が不適切な理由:
    - A/D: `no synchronization` は古い「IGP 同期」機能を無効化するだけであり、next-hop 未到達問題には無関係。  
    - B: `next-hop-unchanged` は **逆効果** で、「R4 のアドレスを next-hop のまま流す」ため、R1 の到達性問題を悪化させる。  
      - さらに、ここで `redistribute connected` を足すのは、不要な接続経路までばら撒く可能性があり、スケーラビリティにも悪い。  

- 試験向けの覚え方フレーズ

  - **「iBGP で ‘遠い next-hop’ が見えない時は、まず ‘next-hop-self’」**  
  - 一行パターン:
    - **「R1 から R4-loopback unreachable＋中継 R2 → R2 に next-hop-self」**  
  - セット暗記:
    - **「IPv6 BGP でも考え方は同じ：‘next-hop 不達’＝中継 iBGP で next-hop-self」**  

***
QUESTION 646 解説

- 問題文（英語）:  
What is the role of LDP in MPLS networks?

A. It disables label binding information to exchange with peer LSRs  
B. It creates MPLS packet forwarding along with the IGP routes  
C. It enables label binding that exchanges route descriptors  
D. It enables label binding information to exchange with peer LSRs  

Correct Answer: D [web:615]

- 問題文要約（日本語）:  
MPLS ネットワークにおいて、LDP（Label Distribution Protocol）の役割は何か。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - LDP は、MPLS で使う「**ラベルと FEC（経路プレフィックス）の対応情報＝ラベルバインディング**」を、隣接する LSR（Label Switch Router）間で交換するためのプロトコル。  
    - すなわち「peer LSR 間でラベルバインディング情報をやり取りできるようにする」のが役割。 [web:615][web:627]  
  - 他の選択肢が誤りな理由:
    - A: 「disable（無効化）」ではなく、**enable** して配布するのが本来の役割。  
    - B: パケット転送自体は「**既に配布済みラベル＋LFIB**」で行うもので、LDP の直接の機能は「転送」ではなく「ラベル配布」。  
    - C: MPLS で route distinguisher/route target を扱うのは MP-BGP などであり、LDP は RD を運ばない。  

- 試験向けの覚え方フレーズ

  - **「LDP = Label を Distribute（配る）Protocol」**  
  - 一行パターン:
    - **「LDP は『ラベル割り当て情報を隣に配るだけ』、経路そのものは IGP/BGP」**  
  - セット暗記:
    - **「IGP が経路、LDP がラベル、LFIB が転送」**  

***
QUESTION 647 解説

- 問題文（英語）:  
A network engineer is adding a new spoke router into an existing DMVPN Phase 3 tunnel with a hub router to provide secure communication between sites. Which additional configuration must the engineer apply to enable the tunnel to come up?

A. ip nhrp registration no-unique  
B. ip nhrp nhs 172.23.5.1  
C. ip nhrp responder tunnel 0  
D. ip nhrp server-only non-caching  

Correct Answer: B [web:637][web:644]

- 問題文要約（日本語）:  
既存の DMVPN Phase 3 構成（ハブ＆スポーク）に、新しいスポークルータを追加して、サイト間のセキュア通信を行いたい。  
トンネルを確立するために、スポーク側で追加すべき設定はどれか。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - DMVPN Phase 3 では、**ハブが NHRP Next Hop Server（NHS）** として機能し、各スポークは NHS に登録・解決要求を送る。 [web:639][web:646]  
  - スポーク側トンネルインターフェースには、少なくとも以下が必須:
    - `ip nhrp network-id <id>`  
    - `ip nhrp nhs <hub-tunnel-ip>`  ← NHS（ハブ）を指定  
    - `ip nhrp map <hub-tunnel-ip> <hub-NBMA-ip>`  
    - `ip nhrp map multicast <hub-NBMA-ip>` など [web:644]  
  - 選択肢 B の `ip nhrp nhs 172.23.5.1` は、まさに「ハブのトンネル IP を NHS として指定」するコマンドで、**新しいスポークが DMVPN に正しく参加するための必須設定**。  
  - 他の選択肢が不適切な理由:
    - A: `ip nhrp registration no-unique` は特殊な登録動作の調整であり、基本的なトンネル UP の必須条件ではない。  
    - C: `ip nhrp responder` は Phase 3 の spoke-to-spoke 最適化に関係することはあるが、「トンネルをとりあえず確立する」ための前提ではない。  
    - D: `ip nhrp server-only non-caching` は **ハブ側（NHS側）** のふるまいを制御するコマンドで、スポーク新規追加時の基本設定ではない。  

- 試験向けの覚え方フレーズ

  - **「DMVPN は ‘Hub = NHS’、Spoke は必ず ‘ip nhrp nhs <Hub-Tunnel-IP>’」**  
  - 一行パターン:
    - **「新スポーク追加でトンネル上がらない → まず ‘ip nhrp nhs HUB’ 入ってるか確認」**  
  - セット暗記:
    - **「Phase3 スポーク必須 3 点セット：network-id / nhs / map＋multicast」**  

***
QUESTION 648 解説

- 問題文（英語）:  
A network engineer must provision two DMVPN spokes sites with a zero-touch deployment model. The spoke must communicate through hub with other spokes. Which configuration provisions these sites?

A.  
interface tunnel10  
 ip address 10.0.0.1 255.255.255.0  
 ip nhrp network-id 100  
 ip nhrp nhs registration 10.0.0.254 no-unique  
 tunnel source G0/1  
 tunnel destination 172.16.100.1  
 tunnel mode mgre multipoint  

B.  
interface tunnel10  
 ip address 10.0.0.1 255.255.255.0  
 ip nhrp network-id 100  
 ip nhrp nhs 10.0.0.254 nbma 172.16.100.1 broadcast  
 tunnel source G0/1  
 tunnel destination 172.16.100.1  

C.  
interface tunnel10  
 ip address 10.0.0.1 255.255.255.0  
 ip nhrp network-id 100  
 ip nhrp 10.0.0.254 multicast dynamic  
 tunnel source G0/1  
 tunnel destination 172.16.100.1  
 tunnel key 100  

D.  
interface tunnel10  
 ip address 10.0.0.1 255.255.255.0  
 ip nhrp network-id 100  
 ip nhrp nhs 10.0.0.254 nbma 172.16.100.1 multicast  
 tunnel source G0/1  
 tunnel destination 172.16.100.1  
 tunnel key 100  

Correct Answer: D [web:651][web:652]

- 問題文要約（日本語）:  
ゼロタッチで 2 つの DMVPN スポークサイトをプロビジョニングしたい。  
スポーク同士は「必ずハブ経由」で通信する（＝典型的な hub-and-spoke 動作）。  
どのトンネル・インターフェース設定が、この要件を満たすか。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - DMVPN スポーク側で最低限必要な要素: [web:651][web:653]  
    - `ip nhrp network-id <id>`（同じ DMVPN ドメイン識別）  
    - **NHS（hub）のトンネル IP と NBMA IP のマッピング**  
      - `ip nhrp nhs <hub-tunnel-ip> nbma <hub-NBMA-ip> multicast`  
    - ルーティングプロトコル用にマルチキャストを hub へ送る指定（multicast） [web:652]  
    - `tunnel key` は DMVPN ネットワークの一意識別に一般的に使用される（セキュリティ兼区分け）。  
  - 選択肢 D は:
    - `ip nhrp nhs 10.0.0.254 nbma 172.16.100.1 multicast`  
      - hub のトンネル IP=10.0.0.254 と NBMA=172.16.100.1 の対応、かつマルチキャストをその NBMA に送る指定。  
      - これにより、EIGRP/OSPF などのマルチキャストが hub へ正しく届き、**スポークは常に hub 経由で他スポークと疎通**できる。 [web:652][web:655]  
    - `tunnel key 100` も含まれ、同一 DMVPN ドメインとして識別しやすい。  
  - 他選択肢が不適切な理由:
    - A: `ip nhrp nhs registration` という構文は無効、かつ `tunnel mode mgre multipoint` は hub 側で用いる想定が多く、spoke 単体のゼロタッチ構成としては不自然。  
    - B: `broadcast` キーワードは NHRP の文法として不正。マルチキャスト／ブロードキャストの転送先指定には `multicast` を用いる。  
    - C: `ip nhrp 10.0.0.254 multicast dynamic` の構文自体が誤り（`map multicast` or `map multicast dynamic` が正しい形）。  

- 試験向けの覚え方フレーズ

  - **「DMVPN スポーク：NHS＋NBMA＋multicast＋tunnel key で ‘お作法フルセット’」**  
  - 一行パターン:
    - **「ip nhrp nhs HUB nbma HUB-NBMA multicast ＋ tunnel key ＝きれいな spoke 設定」**  
  - セット暗記:
    - **「ゼロタッチ DMVPN = spoke は『nhs＋map multicast』だけ覚えれば OK」**  

***
QUESTION 649 解説

- 問題文（英語）:  
An engineer configures router B to direct all traffic from host 192.168.1.3 to router C. All other traffic must be routed through normal routing-protocol operations. Which configuration accomplishes the task?

A.  
interface g0/0/0  
 ip address 192.168.1.254 255.255.255.0  
 ip policy route-map CCNP  
!  
access-list 101 permit ip host 192.168.1.3 any  
!  
route-map CCNP permit 10  
 match ip address 101  
 set ip next-hop 10.0.1.2  

B.  
interface g0/0/0  
 ip address 192.168.1.254 255.255.255.0  
!  
access-list 101 permit ip host 192.168.1.3 any  
access-list 101 permit ip any any  
!  
route-map CCNP permit 10  
 match ip address 101  
 set ip next-hop 10.0.1.2  

C.  
interface g0/0/0  
 ip address 192.168.1.254 255.255.255.0  
 ip policy route-map CCNP  
!  
access-list 101 permit ip host 192.168.1.3 any  
!  
route-map CCNP permit 10  
 match ip address 101  
 set ip next-hop 10.0.2.1  

D.  
interface g0/0/0  
 ip address 192.168.1.254 255.255.255.0  
 ip policy route-map CCNP  
!  
access-list 101 permit ip any host 192.168.1.3  
!  
route-map CCNP permit 10  
 match ip address 101  
 set ip next-hop 10.0.1.2  

Correct Answer: A [web:660][web:662]

- 問題文要約（日本語）:  
ルータBで、ホスト 192.168.1.3 から出ていくトラフィックだけをルータCへ強制的に転送したい。  
それ以外のトラフィックは、通常のルーティング（ルーティングプロトコル）に従って処理したい。  
どの PBR 設定がこの要件を満たすか。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 要件:
    - 「**送信元が 192.168.1.3 のトラフィックのみ**」を PBR の対象にしたい。  
    - その他のトラフィックは **PBR 無し → 通常ルーティングに戻す**。  
    - PBR は **着信インターフェース** に `ip policy route-map` を適用する必要がある。 [web:664][web:666]  
  - 選択肢 A は:
    - `access-list 101 permit ip host 192.168.1.3 any` で「送信元 192.168.1.3 のみ」をマッチ。  
    - その ACL を route-map `CCNP` で参照し、`set ip next-hop 10.0.1.2`（= router C）へ転送。  
    - さらに、`interface g0/0/0` に `ip policy route-map CCNP` を設定しているため、  
      - g0/0/0 に入ってくる 192.168.1.3 由来のトラフィックだけが PBR の対象になり、  
      - それ以外は route-map にマッチしないので通常ルーティングにフォールバックする。 [web:664][web:666]  
  - 他選択肢がダメな理由:
    - B: インターフェースに `ip policy route-map` が設定されていないため、そもそも PBR が有効にならない。  
      - さらに `access-list 101 permit ip any any` を入れているので、正しく適用されると全トラフィックが影響を受けかねない。  
    - C: PBR 自体は有効だが、`set ip next-hop 10.0.2.1` で **誤った next-hop** を指しており、「router C」に向かわない。  
    - D: ACL が `permit ip any host 192.168.1.3` となっており、「宛先が 192.168.1.3」のトラフィックをマッチしてしまう。  
      - しかし要件は「送信元 192.168.1.3」なので、条件を取り違えている。  

- 試験向けの覚え方フレーズ

  - **「PBR は ‘入ってくるインターフェース’ で、ACL は ‘source条件’ を正確に」**  
  - 一行パターン:
    - **「特定ホストの送信だけ操作 → ACL は ‘host x any’，インタに ip policy」**  
  - セット暗記:
    - **「PBR 三点セット：ACL で識別 → route-map で next-hop → ingress IF に ip policy」**  

***
QUESTION 650 解説

- 問題文（英語）:  
An enterprise user reports an access issue with IPv6 content on the Internet. The user can access IPv4 content that is at the data center. Which action resolves the issue with IPv6 content?

A. Advertise the 2001:db8:e::e/64 route on the router D OSPF process.  
B. Enable default information to originate in IPv6 AF on router B.  
C. Change Area 11 to Area 0 between routers B and C.  
D. Add a static route for 2001:db8:0:6::1/64 on router B RIB.  

Correct Answer: B

- 問題文要約（日本語）:  
ユーザはデータセンタ内の IPv4 コンテンツにはアクセスできるが、インターネット上の IPv6 コンテンツにはアクセスできない。  
この IPv6 アクセス問題を解決するために、何をすべきか。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - インターネット上の任意の IPv6 宛先に到達するには、エッジ側（router B）から **IPv6 デフォルトルート (::/0)** が OSPFv3 などで社内に広告されている必要がある。 [web:678][web:684]  
  - 現状の症状:
    - データセンタ内の IPv4 コンテンツにはアクセス可能 → IPv4 については内部経路とデフォルト（もしくは WAN までの経路）が揃っている。  
    - IPv6 インターネットには到達できない → 多くの場合は「**IPv6 デフォルトルートが社内に配られていない**」パターン。 [web:678][web:685]  
  - それを解決する一般的な方法:
    - IPv6 OSPFv3 で `default-information originate` を有効化し、  
      - router B（インターネット側と接するエッジルータ）が持つ ::/0 を、IPv6 AF（address-family）内で OSPF ドメインに広告する。 [web:678]  
    - これにより、ユーザ側ルータは「IPv6 インターネット＝router B 方向のデフォルトルート」と認識できるようになる。  
  - 他の選択肢が不適切な理由:
    - A: 特定ネットワーク `2001:db8:e::e/64` を広告しても、インターネット全体には到達できない（あくまで一つのプレフィックス）。  
    - C: エリア設計（Area 11 → Area 0）変更は、大域的な IPv6 インターネット到達性の欠如とは直接関係ないケースが多い。  
    - D: 単一プレフィックス `2001:db8:0:6::1/64` の静的ルートを追加しても、やはり他の IPv6 インターネット先には届かない。  
      - 問題の本質は「**汎用的な IPv6 デフォルト経路がない**」こと。  

- 試験向けの覚え方フレーズ

  - **「IPv6 だけインターネット行けない＝まず ‘::/0 の広告（default-information originate）を疑う』」**  
  - 一行パターン:
    - **「OSPFv3 で外へ出たい時：エッジで default-information originate（IPv4 と同じ）」**  
  - セット暗記:
    - **「特定プレフィックスじゃなく ‘::/0’ を OSPFv3 でばらまく → IPv6 Internet OK」**  

***
QUESTION 651 解説

- 問題文（英語）:  
A network engineer troubleshooting a packet drop problem for the host 172.16.100.5 notices that only one link is used and installed on the routing table, which saturates the bandwidth. Which action must the engineer take to resolve the high bandwidth utilization problem and share the traffic toward this host between the two available links?

A. Change the EIGRP delay metric meet the feasibility condition.  
B. Set the eigrp variance equal to 4 to install a second route with a metric not larger than 4 times of the best metric.  
C. Set the eigrp variance equal to 3 to install a second route with a metric not larger than 3 times of the best metric.  
D. Disable the eigrp split horizon loop protection mechanism.  

Correct Answer: A [web:688][web:692]

- 問題文要約（日本語）:  
ホスト 172.16.100.5 へのトラフィックでパケットドロップが発生しており、ルーティングテーブルには 2 本のリンクのうち 1 本しかインストールされていない。その 1 本が飽和している。  
帯域を分散するために 2 本のリンクを使いたいが、どの操作を行うべきか。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - EIGRP で **複数パスをロードバランスに使う条件**は 2 つ: [web:690][web:694]  
    1. その経路が **feasible successor（FS）** であること（＝feasibility condition を満たす）。  
    2. `variance` の範囲内にメトリックが収まっていること。  
  - 問題の典型パターン:
    - 第二のリンクは「EIGRP topology table には存在する」が、**FD と RD の関係により FS 条件を満たしていない**。  
    - この場合、いくら `variance` を 3 や 4 に上げても、「FS ではないルート」は決してルーティングテーブルに載らない。 [web:691]  
  - したがって、まずやるべきことは:
    - 第二経路の **delay を調整して、RD < FD(successor)** となるようにし、feasibility condition を満たす FS に昇格させること。  
    - これが選択肢 A の「Change the EIGRP delay metric to meet the feasibility condition」。  
  - 他の選択肢が不適切な理由:
    - B/C: variance は **「すでに FS になっている複数経路」をどこまで許容メトリックとしてインストールするか**の調整だけ。  
      - FS 条件を満たしていない経路には効果がない。  
    - D: split horizon はループ防止のメカニズムであり、本件の「2 本の上りリンクのロードバランス」問題とは無関係。  

- 試験向けの覚え方フレーズ

  - **「EIGRP でマルチパスさせたい時：variance の前に『FS 条件クリア？』」**  
  - 一行パターン:
    - **「FD＞RD の関係を delay で調整 → まず FS にしてから variance」**  
  - セット暗記:
    - **「EIGRP 複数経路＝①FS 条件 ②variance、順番を間違えない」**  

***
QUESTION 652 解説

- 問題文（英語）:  
The IPsec profile configuration must be optimal to address the extra bytes overhead per packet. Which configuration must be applied by the engineer on all routers to meet the requirement?

A.  
crypto ipsec transform-set T-SET esp-aes 128 esp-sha-hmac  
 mode mGRE  

B.  
crypto ipsec transform-set T-SET esp-aes 128 esp-sha-hmac  
 mode tunnel  

C.  
crypto ipsec transform-set T-SET esp-aes 128 esp-sha-hmac  
 mode ipsec ipv4  

D.  
crypto ipsec transform-set T-SET esp-aes 128 esp-sha-hmac  
 mode transport  

Correct Answer: D

- 問題文要約（日本語）:  
1 パケットあたりの IPsec オーバーヘッド（余分なバイト数）をできるだけ小さくしたい。  
全ルータに適用すべき最適な IPsec transform-set のモードはどれか。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - IPsec には主に 2 つのモードがある:  
    - **Tunnel mode**: 元の IP パケット全体（IP ヘッダ＋ペイロード）を暗号化し、新たな外側 IP ヘッダを付加 → ヘッダが 1 段増える分、オーバーヘッドが大きい。 [web:707]  
    - **Transport mode**: 元の IP ヘッダはそのまま残し、「トランスポート層以降（ペイロード部分）」だけを暗号化 → 追加されるヘッダが少ない分、**オーバーヘッドが最小**。 [web:711]  
  - 本問の前提（解説文）では:
    - Underlay はインターネットだが、IPsec は「既にある GRE トンネル（例: 192.168.200.0/24 のトンネル）」上で動いている。  
    - つまり、「外側の IP/GRE が既にカプセル化を済ませている上に、さらに IPsec をかける」ので、  
      - ここで **IPsec を tunnel mode にすると『二重に IP ヘッダを増やす』」ことになり、オーバーヘッドが大きい**。  
      - 一方、**transport mode で GRE パケットのペイロード部分だけ暗号化すれば、オーバーヘッドを最小限に抑えられる**。  
  - よって、`mode transport` を指定する D が要件（オーバーヘッド最小）に最も適合する。  
  - 他の選択肢:
    - A: `mode mGRE` というモードは IPsec transform-set には存在しない。  
    - B: `mode tunnel` はサイト間暗号化では一般的だが、「すでに GRE でカプセル化している環境でオーバーヘッドを減らす」目的には逆行する。  
    - C: `mode ipsec ipv4` は Cisco IOS の transform-set モードとしての正しいキーワードではない。  

- 試験向けの覚え方フレーズ

  - **「GRE 上に IPsec → ‘Transport mode’ で GRE を中身ごと暗号化、ヘッダは増やさない」**  
  - 一行パターン:
    - **「IPsec オーバーヘッド削減＋GRE 併用＝IPsec transport、一枚だけ増やす」**  
  - セット暗記:
    - **「L3-to-L3 だけなら tunnel、‘GRE＋IPsec’ なら transport が基本パターン」**  

***
QUESTION 653 解説

- 問題文（英語）:  
A company is redesigning WAN infrastructure so that all branch sites must communicate via the head office and the head office can directly communicate with each site independently. The network engineer must configure the head office router by considering zero-touch technology when adding new sites in the same WAN infrastructure. Which configuration must be applied to the head office router to meet this requirement?

A.  
interface Tunnel0  
 tunnel mode ip  
 ip nhrp map multicast dynamic  

B.  
interface Tunnel0  
 tunnel mode dvmrp  
 ip nhrp redirect  

C.  
interface Tunnel0  
 tunnel mode gre multipoint  
 ip nhrp map multicast dynamic  

D.  
interface Tunnel0  
 tunnel mode ip  
 ip nhrp redirect  

Correct Answer: C [web:712][web:716]

- 問題文要約（日本語）:  
WAN を再設計し、すべての支店はヘッドオフィス経由で相互通信し、ヘッドオフィスは各支店と個別に通信できるようにしたい。  
新しい支店サイトを追加するときはゼロタッチ（ヘッド側に個別設定を増やさない）で同じ WAN に参加させたい。  
ヘッドオフィス・ルータに必要なインターフェース設定はどれか。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 要件は典型的な **DMVPN Hub-and-Spoke** の要件:
    - Hub（本社）は 1 本のトンネルインターフェースだけで多数の spoke（支店）と接続。  
    - 新しい spoke 追加時、Hub 側は**設定追加無し**（ゼロタッチ）で参加可能。 [web:649][web:716]  
  - これを実現するには:
    - Hub 側トンネルは **mGRE（multipoint GRE）** にする必要がある → `tunnel mode gre multipoint`。 [web:714][web:715]  
    - Spoke を動的に登録し、ルーティングプロトコルのマルチキャストを Hub に届けるために  
      - `ip nhrp map multicast dynamic` を Hub に設定する。  
      - これにより、NHRP 登録してきた spoke の NBMA 情報を使って、マルチキャストを自動的に適切な spoke へ送出できる。 [web:712][web:716]  
  - 選択肢 C は上記 2 点を満たすため、DMVPN Hub の「ゼロタッチ追加対応」構成として正しい。  
  - 他の選択肢が不適切な理由:
    - A/D: `tunnel mode ip` は無効なモードであり、mGRE ではないので複数 spoke を 1 IF にぶら下げられない。  
    - B: `tunnel mode dvmrp` はマルチキャストルーティング用トンネルモードで、DMVPN やゼロタッチ WAN の要件とは無関係。  

- 試験向けの覚え方フレーズ

  - **「ゼロタッチ多拠点 VPN ＝ Hub は ‘mGRE＋ip nhrp map multicast dynamic’」**  
  - 一行パターン:
    - **「本社 1IF で多拠点 → tunnel mode gre multipoint ＋ NHRP multicast dynamic」**  
  - セット暗記:
    - **「DMVPN Hub のキーワード：mGRE・NHRP・multicast dynamic」**  

***
QUESTION 653 解説

- 問題文（英語）:  
Which action fixes the OSPF routes redistribution into EIGRP?

A. Set a default metric in the route map  
B. Set tags before matching into EIGRP  
C. Match external type to type-1  
D. Match OSPF and EIGRP IDs  

Correct Answer: B [web:728]

- 問題文要約（日本語）:  
OSPF から EIGRP への再配布に関する問題があり、「どの設定変更が正しい修正か？」と聞いている。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 元の解説にもある通り、この問題の前提となる設定自体は **すでに正しく動作しており、「今すぐ直すべき明確な不具合」はない**。  
    - したがって A/C/D は「明らかな誤り」か、「不要な変更」。  
  - 各選択肢の妥当性:
    - A: EIGRP への再配布では、**メトリックを指定しないと再配布されない**が、今回の前提では既に `default-metric` などでメトリックが設定済み。  
      → 追加で route-map 内に default-metric を仕込む必要はない。 [web:728][web:732]  
    - C: OSPF の external type（E1/E2）を type-1 限定にする必要性は問題文から読み取れない。  
      - そもそも「どの外部経路を再配布するか」というポリシー論であり、「不具合修正」とまでは言えない。  
    - D: OSPF の process ID と EIGRP の AS 番号を「一致させる」必要は全くない。  
      - 異なるプロトコルなので、ID/AS が一致しているかどうかは再配布の条件ではない。  
  - B について:
    - ループ防止や複雑な相互再配布（OSPF⇄EIGRP が複数箇所にある場合）では、**タグを付けておき、逆向き再配布でそのタグを見て制御する**のが定石。 [web:728][web:729]  
    - この問題でも、「別のルータで OSPF⇄EIGRP を再配布している」状況を想定し、  
      - まず OSPF→EIGRP へ redistribute する際に tag を付与  
      - 逆方向（EIGRP→OSPF）では、その tag を match してブロック or 特別扱い  
      という形で「**タグを先につけておく**」のが推奨となる。  
    - その意味で、「不具合を直す」というより「**将来のループや誤再配布を予防する正しい設計の一歩**」として B が最も妥当。  

- 試験向けの覚え方フレーズ

  - **「マルチ再配布で迷ったら『まず tag を付けてから match』」**  
  - 一行パターン:
    - **「OSPF⇄EIGRP のループ対策＝行きで tag、帰りで match tag」**  
  - セット暗記:
    - **「fix redistribution? default-metric じゃなく、tag を活用する設計を選べ」**  

***
QUESTION 655 解説

- 問題文（英語）:  
How are CE advertised routes segmented from other CE routers on an MPLS PE router?

A. with a combination of VRF-Lite and MP-BGP  
B. by enabling multiple instances of BGP, one for each CE router  
C. by pushing MPLS labels advertised by LDP on customer routes  
D. by assigning CE-facing interfaces to different VRFs  

Correct Answer: D [web:738][web:747]

- 問題文要約（日本語）:  
MPLS L3VPN 環境で、PE ルータ上で各 CE から学習した経路を他の CE と分離（セグメント）する方法は何か。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - MPLS L3VPN では、PE ルータ上に **VRF（VPN Routing and Forwarding）** を複数作り、  
    - 各 CE に接続するインターフェース（またはサブインターフェース）を、対応する **VRF に割り当てる**ことで、顧客ごとの経路表を論理的に分離する。 [web:738][web:747]  
    - CE から来た経路は、そのインターフェースに紐づく VRF のルーティングテーブルにのみインストールされ、他の VRF/CE とは混ざらない。 [web:735][web:740]  
  - この「CE-facing インターフェースを別々の VRF に所属させる」ことが、問題文の「CE advertised routes を他 CE からセグメントする」メカニズムそのものなので、D が正しい。  
  - 他の選択肢が不適切な理由:
    - A: VRF-Lite は MPLS コアを使わずに VRF をエッジで切る技術で、本問の「MPLS PE 上でのセグメンテーション」そのものではない。  
    - B: PE で CE ごとに BGP プロセスを増やす必要はなく、通常は 1 つの BGP プロセス＋VRF＋MP-BGP で十分。  
    - C: LDP の MPLS ラベルは **P/PE 間の転送（トランスポートラベル/VPNラベル）** に使うものであり、「どの CE の経路か」を PE 内でセグメントする仕組みではない。  

- 試験向けの覚え方フレーズ

  - **「MPLS L3VPN の ‘顧客分離’＝インターフェースを別 VRF に突っ込む」**  
  - 一行パターン:
    - **「どの CE のルートか？ → ‘どの VRF の IF から入ったか’ で決まる」**  
  - セット暗記:
    - **「VRF は ‘インターフェース単位で紐づく’ → CE ごとに CE-facing IF を別 VRF」**  

***
QUESTION 656 解説

- 問題文（英語）:  
R3 is dual-homed to two service providers for traffic redundancy. R3 prefers its outbound traffic via R2. Which set of configurations achieves this goal?

A.  
route-map R1 permit 10  
 set metric 200  
!  
route-map R2 permit 10  
 set metric 100  

B.  
route-map R1 permit 10  
 set AS-Path Prepend 200  
!  
route-map R2 permit 10  
 set AS-Path Prepend 100  

C.  
route-map R1 permit 10  
 set metric 100  
!  
route-map R2 permit 10  
 set metric 200  

D.  
route-map R1 permit 10  
 set AS-Path Prepend 100  
!  
route-map R2 permit 10  
 set AS-Path Prepend 200  

Correct Answer: A

- 問題文要約（日本語）:  
R3 は 2 つのサービスプロバイダにデュアルホームされていて冗長性を確保しているが、**アウトバウンド** トラフィックは優先的に R2 側 ISP に出したい。  
どのルートマップ設定で、この経路選好が実現できるか。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 文脈的に、この `metric` は BGP の **MED（Multi-Exit Discriminator）** を指している。  
    - MED は「**自 AS 内からどの出口を使ってほしいか**」を隣接 AS に伝える属性で、**値が小さい方が優先**される。  
    - ここでは、R3 が 2 本の ISP 経路を持っていて、**R2 側ルートの MED を小さくすることで R2 を優先出口にする**イメージ。  
  - 選択肢 A では:
    - R1 向け経路に `metric 200`  
    - R2 向け経路に `metric 100`  
    → R3 から見て「R2 経由の経路が MED 100 でより好ましい」ため、**アウトバウンドは R2 側経路が選ばれる**。  
    - さらに、この問題の元解説では **`bgp always-compare-med` が有効**になっている前提で、異なる AS 間の MED 比較も行われる、とされている。  
  - 他の選択肢が不適切な理由:
    - C: MED の大小が逆（R1=100, R2=200）なので、**R1 側 ISP が優先**されてしまう。  
    - B/D: AS-path prepend は主に **インバウンドトラフィック制御（相手側から見た経路選好）** に効く属性であり、「R3 から外へ出すルート選択（アウトバウンド）」には直接関係しない。  
      - しかも、両方向に同様の prepend をすると差がつかない or 意図に合わない。  

- 試験向けの覚え方フレーズ

  - **「MED は ‘小さい方がエクスポート優先’、always-compare-med で AS またぎでも効く」**  
  - 一行パターン:
    - **「2 本 ISP で出たい方の MED を小さく（metric 100 vs 200）」**  
  - セット暗記:
    - **「Out 方向の出口選好 → MED、小さい方が ‘こっちから出して’」**  

***
QUESTION 657 解説

- 問題文（英語）:  
An engineer must configure encrypted packets for a single router OSPF neighborship. Which configuration meets this requirement?

A.  
interface Ethernet0/2  
 ip ospf authentication-key exam  

B.  
router ospf 100  
 area 0 authentication  
!  
interface Ethernet0/2  
 ip ospf authentication-key exam  

C.  
interface Ethernet0/2  
 ip ospf authentication message-digest  
 ip ospf message-digest-key 1 md5 exam  

D.  
router ospf 100  
 area 0 authentication message-digest-key 1 md5 exam  
!  
interface Ethernet0/2  
 ip ospf message-digest-key 1 md5 exam  

Correct Answer: C

- 問題文要約（日本語）:  
1 本の隣接リンク（単一の OSPF neighborship）だけ、OSPF パケットを暗号化して保護したい。  
どの設定がこの要件を満たすか。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - OSPF の「encrypted packets」という表現は、試験では通常 **MD5 メッセージダイジェスト認証**を指す。  
    - 平文パスワード（simple authentication）ではなく、MD5 を使うことでパスワードが暗号化された形でやり取りされる。 [web:763][web:765]  
  - MD5 認証を **特定インターフェースだけ** に適用するには、インターフェースで次の 2 ステップを実施する: [web:769]  
    1. `ip ospf authentication message-digest`  
    2. `ip ospf message-digest-key <key> md5 <password>`  
  - 選択肢 C はちょうどこの 2 行を `Ethernet0/2` に対して設定しており、  
    - 他のインターフェースやエリア設定には影響を与えないため、「single router OSPF neighborship」だけを暗号化する要件に合致する。  
  - 他の選択肢が不適切な理由:
    - A/B: `ip ospf authentication-key` は **simple／clear-text authentication** であり、パスワードが平文で送られる。暗号化ではない。  
      - B はさらに `area 0 authentication` をかけるため、エリア全体に影響してしまい「単一ネイバーだけ」という要件にも合わない。  
    - D: `area 0 authentication message-digest-key ...` という構文は誤りで、  
      - area 単位では `area 0 authentication message-digest` だけを設定するのが正しい。  
      - さらにエリア全体に MD5 を強制するため、single neighborship だけを対象にしたい条件と矛盾する。  

- 試験向けの覚え方フレーズ

  - **「OSPF MD5 per-interface：‘authentication message-digest’＋‘message-digest-key’ の 2 行セット」**  
  - 一行パターン:
    - **「単一リンクだけ暗号化 → area じゃなく interface で MD5」**  
  - セット暗記:
    - **「平文＝authentication-key、MD5＝authentication message-digest＋message-digest-key」**  

***
QUESTION 658 解説

- 問題文（英語）:  
Router ABR-Rtr is not propagating the Internet routes in OSPF area 10, which causes Internet reachability problems in the area. Which action resolves the issue?

A. ABR-Rtr must advertise the 0.0.0.0/0 default route in area 10.  
B. ABR-Rtr must configure the area 10 stub no-summary command.  
C. ABR-Rtr must configure the default-information originate always command.  
D. ABR-Rtr network type must be broadcast network  

Correct Answer: A

- 問題文要約（日本語）:  
ABR-Rtr が OSPF エリア 10 にインターネット向けルート（デフォルトルート）を流しておらず、エリア 10 内のルータからインターネットに出られない。  
この到達性問題を解消するには、ABR-Rtr でどのアクションを取るべきか。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - OSPF エリア内のルータがインターネットに出るためには、通常は **0.0.0.0/0 のデフォルトルート** が OSPF で配布されている必要がある。 [web:773][web:777]  
  - 問題の解説によると、ABR-Rtr は本来 ABR として振る舞うべきだが、**area 0 の設定（例: `network 10.17.4.7 0.0.0.0 area 0`）がなく、正しい ABR になっていないため、エリア 10 にデフォルトルートをアドバタイズできていない**、という前提になっている。 [web:778][web:785]  
    - この状況を修正するには、ABR-Rtr が **area 0 と area 10 の両方に属する ABR** になるように設定を補い、その上で **エリア 10 へ 0.0.0.0/0 を流す** 必要がある。  
  - 選択肢 A の「ABR-Rtr must advertise the 0.0.0.0/0 default route in area 10」は、  
    - 実際には「ABR として正しくエリアまたぎを構成し、エリア 10 にデフォルトルートを流す」という設計上やるべきことを抽象的に言っており、  
    - 問題の根本原因（ABR 未形成によりデフォルトがエリア 10 に届いていない）を解消する方向と合致する。  
  - 他の選択肢が不適切な理由:
    - B: `area 10 stub no-summary` は、**エリア 10 を totally stubby area にして、ABR からのサマリールートや外部ルートを遮断し、代わりに ABR からのデフォルトルートだけを受け取る**ためのコマンド。 [web:778]  
      - 既に「インターネットルートが届かない」のが問題なので、追加で no-summary を入れても、根本の「ABR がデフォルトを出せていない」問題は解決しない。  
    - C: `default-information originate always` は **ASBR が自分のルーティングテーブルにデフォルトがなくても、OSPF にデフォルトを注入する**ためのコマンドであり、「ABR-Rtr が ASBR として外部経路を OSPF に流す」ケースに使う。 [web:773][web:777][web:779]  
      - 本問は「ABR-Rtr がエリア 10 にインターネットルートを伝えていない」問題で、主眼は ABR のエリア境界とデフォルトの伝搬であり、ASBR での外部注入ではない。  
    - D: ネットワークタイプ（broadcast/non-broadcast/point-to-point など）は **デフォルトルートがエリアにアドバタイズされるかどうか**とは無関係で、インターネット到達性の問題を直接解決しない。  

- 試験向けの覚え方フレーズ

  - **「OSPF で ‘インターネット行けない’＝まず 0.0.0.0/0 がエリアに届いてるかを疑え」**  
  - 一行パターン:
    - **「Stub 設定より前に、ABR からデフォルトが ‘area 10 へ流れているか’ を確認」**  
  - セット暗記:
    - **「ABR が area 0＋他エリアを持つ → そこからデフォルトをエリア内へアドバタイズ」**  

***
QUESTION 659 解説

- 問題文（英語）:  
While BGP Internet routes are redistributed to a lower class of router via RIP, packets are being dropped and routes are failing to be distributed in RIP. Which action resolves the issue?

A. Use WFQ in the output queue of the high-performance router.  
B. Use RIP V2 to be able to use classless networks from BGP.  
C. Use the input-queue command to prevent the loss of packets.  
D. Use OSPF instead of RIP to accept all BGP routes.  

Correct Answer: B

- 問題文要約（日本語）:  
BGP で学習したインターネットルートを、性能の低いルータに RIP で再配布しているが、パケットドロップや RIP ルート配布の失敗が発生している。  
この問題を解決するためには、どの対応を行うべきか。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - BGP から再配布されるインターネットルートは **CIDR（クラスレス）** のプレフィックス（例: /19, /20 など）が多く、**クラスフル境界に依存しない**。 [web:779][web:777]  
  - **RIP v1 はクラスフル** でサブネットマスク情報を運べず、クラスレスな BGP プレフィックスを正しく扱えないため、  
    - ルートが正しくインストールされなかったり、集約されて不達になったりして、「配布に失敗」「パケットドロップ」の原因になる。 [web:777]  
  - **RIP v2 はクラスレス** をサポートし、ルートにサブネットマスクを含めて広告できるため、  
    - BGP から再配布された各種長さのプレフィックスを正しく運べるようになり、ルート配布失敗とそれに起因するドロップが解消される。 [web:779]  
  - 他の選択肢が不適切な理由:
    - A: WFQ（Weighted Fair Queuing）はキューイング方式であり、**ルーティング情報（プレフィックスの扱い）そのものの問題**は解決しない。  
    - C: `input-queue`（入力キュー制御）も同様に、キュー / 輻輳制御の話であり、BGP→RIP 再配布時のクラスフル／クラスレスの問題とは無関係。  
    - D: OSPF を使えば大量ルートを扱いやすくはなるが、問題文は「RIP 経由で配布している」状況に対する直接的な修正として、**まず RIP 側を v2 にするのが本質的な答え**。  

- 試験向けの覚え方フレーズ

  - **「BGP ＝CIDR前提 → クラスレス対応の IGP（RIPv2/OSPF/EIGRP）で再配布」**  
  - 一行パターン:
    - **「BGP ルートを RIP に再配布して崩れる → ‘RIP v1 ではなく v2 にしろ’」**  
  - セット暗記:
    - **「RIP v1＝クラスフル、RIP v2＝クラスレス（BGP 再配布 OK）」**  

***
QUESTION 660 解説

- 問題文（英語）:  
Which IP precedence value does BFD use to prioritize traffic within an infrastructure device?

A. 4  
B. 5  
C. 6  
D. 7  

Correct Answer: D

- 問題文要約（日本語）:  
BFD（Bidirectional Forwarding Detection）は、インフラデバイス内でトラフィックを優先させるために、どの IP precedence 値を使用するか。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - BFD パケットは **IP precedence 7**（最高優先度）を付けてデフォルトで送信され、デバイス内で優先的に扱われる。 [web:793][web:795][web:797]  
    - IP precedence は 0～7 の 8 段階（3 ビット）で、数値が高いほど優先度が高い。 [web:796][web:798]  
    - **precedence 7** は「Network Control」や「Internetwork Control」として定義されており、ルーティングプロトコルの Hello や BPDU などの重要な制御トラフィックに割り当てられる。 [web:794][web:796]  
  - BFD は **障害検出を高速に行う必要がある**ため、制御トラフィックとして最高優先度 7 で送ることで、  
    - キュー輻輳やドロップによる BFD タイムアウト誤検知を回避し、リンク障害を迅速に検出する。 [web:793][web:795][web:801]  
  - 他の選択肢が不適切な理由:
    - A (4): flash-override レベルで、VoIP や重要データに使われることがあるが、BFD のデフォルトではない。  
    - B (5): critical レベルで、音声ベアラトラフィックなどに使われるが、BFD ではない。  
    - C (6): internet control レベルで、ルーティングプロトコルや同期プロトコルにも使われるが、BFD は**最高優先度の 7** を使う。 [web:794][web:796]  

- 試験向けの覚え方フレーズ

  - **「BFD は 'リンク障害検知の命綱'＝precedence 7（最高）で優先送信」**  
  - 一行パターン:
    - **「IP precedence 7 = Network/Internetwork Control（Hello, BPDU, BFD）」**  
  - セット暗記:
    - **「制御系 precedence: 6=ルーティング protocol, 7=BFD/BPDU など最高優先」**  

***
QUESTION 661 解説

- 問題文（英語）:  
An engineer must filter prefixes that originate from AS65412, but it is not working correctly. Which configuration must the engineer apply to R1 to resolve the issue?

A.  
no ip as-path access-list 1  
ip as-path access-list 1 deny 65412_  
ip as-path access-list 1 permit .*  

B.  
no ip as-path access-list 1  
ip as-path access-list 1 deny _65412$  
ip as-path access-list 1 permit .*  

C.  
route-map FILTER permit 10  
 match as-path 1  
route-map FILTER permit 20  

D.  
router bgp 64560  
 neighbor 10.10.10.10 route-map FILTER out  

Correct Answer: B

- 問題文要約（日本語）:  
AS65412 から発信された（originate from）プレフィックスをフィルタしたいが、正しく動作していない。  
R1 でどの設定を適用すれば問題を解決できるか。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - BGP で「**AS65412 から発信された（originate from）**」経路をフィルタするには、AS-Path の **末尾が 65412 で終わる** パターンにマッチする正規表現を使う必要がある。 [web:804][web:806][web:808]  
    - BGP AS-Path は、経路が通過した AS 番号のリストで、**末尾が originating AS**（発信元 AS）となる。 [web:803][web:805]  
    - 正規表現で「末尾が 65412」を表すには `_65412$` を使う:  
      - `_` = AS 番号の区切り（スペースまたは先頭／末尾）  
      - `65412` = AS 番号そのもの  
      - `$` = 文字列の終端（AS-Path の末尾） [web:804][web:806][web:808]  
  - 選択肢 B の `ip as-path access-list 1 deny _65412$` は、  
    - AS-Path が 65412 で終わる（= AS65412 で発信された）経路を拒否し、  
    - `ip as-path access-list 1 permit .*` で、それ以外のすべての経路を許可する。 [web:804][web:808][web:816]  
    - これにより、AS65412 起源のプレフィックスだけをフィルタできる。  
  - 他の選択肢が不適切な理由:
    - A: `65412_` は「**65412 で始まる** AS-Path」を意味し、AS65412 が **最初の通過 AS**（transit）である経路にマッチする。  
      - 「originate from AS65412」（末尾が 65412）とは異なる。 [web:806][web:808]  
    - C/D: これらは route-map の適用や構造の話であり、AS-Path フィルタの **正規表現パターンそのもの** が間違っていれば、適用方法を変えても機能しない。  
      - 問題の根本原因は「正規表現が間違っている」ことなので、B の修正が必要。  

- 試験向けの覚え方フレーズ

  - **「BGP 'originate from AS X'＝AS-Path の末尾 → _X$ で deny」**  
  - 一行パターン:
    - **「発信元 AS フィルタ：_65412$ ＝末尾が 65412（$＝終端）」**  
  - セット暗記:
    - **「^X$＝X だけ、_X_＝X を含む、_X$＝X で終わる（originate）」**  

***
QUESTION 662 解説

- 問題文（英語）:  
Which action resolves the issue?

A. Configure the local time on Cisco Catalyst Center (formerly DNA Center).  
B. Establish connectivity between the NTP server and the switch.  
C. Configure the local time on the SW1 device.  
D. Establish connectivity between the NTP server and Cisco Catalyst Center (formerly DNA Center).  

Correct Answer: B

- 問題文要約（日本語）:  
SW1 の時刻が誤っていることが原因で問題が発生している状況で、どの対応を行えば問題が解消できるか。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 解説文のとおり、**原因はスイッチ SW1 側の時刻が誤っていること**であり、SW1 が正しい時刻情報を取得できていない。 [file:787]  
  - Cisco 環境では、装置の時刻は通常 **NTP サーバと同期**するため、  
    - SW1 と NTP サーバ間の **接続性（到達性・NTP 設定）が確立されているか** を確認・修正するのが正しいアクションになる。 [file:787]  
  - 選択肢 C の「ローカル時間を手動設定」でも一時的には直るが、  
    - 長期的には時刻ずれが再発しやすく、根本的な同期の問題を解決していない。  
  - 選択肢 A / D について:
    - 解説にもある通り、**DNA Center（Catalyst Center）の時間が問題なら、多数のデバイスで一斉に time lag エラーが発生する**はずだが、本問は SW1 の時刻が明確におかしい前提。 [file:787]  
    - したがって優先して対処すべきは「SW1 と NTP サーバ間の接続性」であり、A / D は焦点がずれている。  

- 試験向けの覚え方フレーズ

  - **「時刻がおかしい機器が 1 台だけ → その機器と NTP の到達性を疑え」**  
  - 一行パターン:
    - **「DNA じゃなく SW 側の時計ずれ → ‘SW ↔ NTP 接続’ を直す」**  
  - セット暗記:
    - **「単発の time 問題＝装置と NTP、全体の time 問題＝NMS/Catalyst Center 側」**  

***
QUESTION 663 解説

- 問題文（英語）:  
A customer reports that networks in the 10.0.1.0/24 space do not appear in the RIP database. What action resolves the issue?

A. Permit 10.0.1.0/24 address in the ACL.  
B. Configure 10.0.1.0/24 network under RIP.  
C. Remove summarization of 10.0.0.0/8.  
D. Remove ACL on R1 blocking 10.0.1.0/24 network.  

Correct Answer: A

- 問題文要約（日本語）:  
10.0.1.0/24 のネットワークが RIP データベースに現れないと顧客から報告があった。  
この問題を解決するには、どのアクションを実施すべきか。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 解説によると、**10.0.0.0/8 配下の他の /24 プレフィックスは RIP データベースに見えている**ため、RIP 自身の自動サマリや 10.0.0.0/8 のサマリ設定が原因ではない。 [file:787]  
  - 特定の 10.0.1.0/24 だけが見えない場合、最も典型的なのは **RIP の distribute-list（ACL ベースの経路フィルタ）でそのネットワークだけがブロックされている**ケース。 [file:787]  
    - distribute-list で使っている ACL に `deny 10.0.1.0 0.0.0.255` のような行があると、そのプレフィックスだけ RIP データベースに載らない。  
  - この場合、**ACL 自体を削除するのではなく、必要なプレフィックスを permit で明示的に許可する**のが正しい対応。  
    - よって「10.0.1.0/24 を許可する ACL 行を追加する」A が正解となる。 [file:787]  
  - 他の選択肢が不適切な理由:
    - B: そもそもそのネットワークを RIP にアドバタイズしていなければ追加が必要だが、解説文では問題の根因を **distribute-list によるブロック**と説明しているため、論点がずれている。  
    - C: 他の /24 は見えており、「10.0.0.0/8 でサマリされて全部つぶれている」わけではないので、サマリ削除は誤り。  
    - D: ACL 全体を外すと、本来フィルタしたい不要な経路まで入ってきてしまうため、**全削除は NG** であり、必要なプレフィックスだけ permit を追加するのがベター、と解説に明記されている。 [file:787]  

- 試験向けの覚え方フレーズ

  - **「特定ネットワークだけ RIP に出ない＝まず distribute-list＋ACL で deny されてないか確認」**  
  - 一行パターン:
    - **「ルートが 1 本だけ消えてる時は ‘ACL に permit を足す’ が基本」**  
  - セット暗記:
    - **「distribute-list 問題：ACL 全削除じゃなく ‘必要プレフィックスだけ permit 追加’」**  

***
QUESTION 664 解説

- 問題文（英語）:  
Which type of ports are protected by IPv6 Source Guard?

A. Layer 3 ports  
B. access ports  
C. trunk ports  
D. Layer 2 ports  

Correct Answer: D

- 問題文要約（日本語）:  
IPv6 Source Guard が保護対象としているのは、どの種類のポートか。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - IPv6 Source Guard は **IPv6 First-Hop Security（FHS）の一機能**で、IPv6 トラフィックの送信元を検証する **Layer 2 のスヌーピング機能**として実装されている。 [web:818][web:820][web:825]  
  - 公式ドキュメントでは、IPv6 Source Guard と Prefix Guard は **「Layer 2 features」** であり、**アクセスまたはトランクポート、および L2 VLAN 構成上でのみサポートされる**と明記されている。 [web:823][web:825]  
    - つまり、技術的には L2 インターフェイス（＝Layer 2 ports）が対象で、  
    - その具体的な形として「access port」「trunk port」で使われる。 [web:819][web:822]  
  - 選択肢の設計上、「access ports」「trunk ports」は **L2 ポートの一形態**であり、それらを包括する正しいレベルの答えが **D: Layer 2 ports** となる。 [web:823][web:825]  
  - 他の選択肢が不適切な理由:
    - A: L3 ポート（ルーティングポート）では、IPv6 Source Guard はサポートされておらず、uRPF など別の L3 機能を用いる。 [web:827]  
    - B / C: どちらも部分的には正しいが、「どのタイプのポートか？」という単一選択問題で最も包括的かつ正確な分類は Layer 2 ports（D）である。 [web:822][web:823]  

- 試験向けの覚え方フレーズ

  - **「IPv6 Source Guard＝L2 での ‘送信元チェック’、対象は L2 ポート（access / trunk）」**  
  - 一行パターン:
    - **「FHS 系（Source / Prefix Guard）はスイッチ L2 機能、ルータ L3 じゃない」**  
  - セット暗記:
    - **「Source/Prefix Guard → Layer 2 features → access／trunk（＝まとめて D: L2 ports）」**  

***
QUESTION 665 解説

- 問題文（英語）:  
An engineer configured NetFlow to capture traffic information through the router, but it is not working as expected. Which action captures the flow information from this router to the collector?

A. Change the flow exporter transport protocol from UDP to TCP.  
B. Configure a flow exporter under flow monitor FLOW-MONITOR-1.  
C. Change the interface configuration FLOW-MONITOR-1 from input to output.  
D. Configure more than one flow exporter destination addresses.  

Correct Answer: B

- 問題文要約（日本語）:  
エンジニアがルータで NetFlow を構成してトラフィック情報を収集しようとしているが、期待通りにフロー情報がコレクタへ送られていない。  
このルータからコレクタへフロー情報を送信できるようにするには、どの設定が必要か。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - Flexible NetFlow では、フロー情報がコレクタに送られるまでの構成要素は以下の 3 つから成る。 [web:834][web:836][web:845]  
    - **flow record**（どの項目を記録するか）  
    - **flow exporter**（どこに・どのプロトコル／ポートで送るか）  
    - **flow monitor**（record と exporter を紐づけてインターフェースに適用する）  
  - 具体例: [web:836][web:840][web:845]  
    ```text
    flow exporter EXPORTER-1
     destination 172.16.10.2
     transport udp 2055
    !
    flow record v4_r1
     ...
    !
    flow monitor FLOW-MONITOR-1
     record v4_r1
     exporter EXPORTER-1
    !
    interface Gi0/0
     ip flow monitor FLOW-MONITOR-1 input
    ```
    - このように **flow monitor 定義の中で `exporter EXPORTER-1` を指定しないと、モニタはローカルキャッシュに溜めるだけで、外部コレクタへエクスポートされない**。 [web:834][web:840][web:845]  
  - 問題文の解説例にも「`flow monitor FLOW-MONITOR-1` の下で `record v4_r1` とともに `exporter EXPORTER-1` を設定する」とあり、  
    - 今の構成では FLOW-MONITOR-1 に **exporter が紐づいていない**ため、コレクタに流れていない、という状況を示唆している。  
    - よって、FLOW-MONITOR-1 の下に flow exporter を設定する B が正解。  
  - 他の選択肢が不適切な理由:
    - A: NetFlow/Flexible NetFlow のエクスポートは **通常 UDP** を使用し、TCP に変えても「まったく送られていない」問題の根本原因にはならない。 [web:834][web:836]  
    - C: input / output どちらに付けるかは「どの方向のトラフィックを計測するか」の違いであり、「そもそもコレクタに送れていない」症状には直結しない。  
    - D: エクスポータの宛先を増やしても、**flow monitor に exporter が紐づいていなければ何も送られない**ため、本質的な解決にならない。 [web:834][web:839]  

- 試験向けの覚え方フレーズ

  - **「Flexible NetFlow：Record＋Exporter＋Monitor の三点セット。Monitor に exporter を“必ず”ぶら下げる」**  
  - 一行パターン:
    - **「FLOW-MONITOR-1 だけ設定してもダメ → ‘exporter EXPORTER-1’ を中に書く」**  
  - セット暗記:
    - **「Record＝何を見る、Exporter＝どこへ送る、Monitor＝両者を束ねて IF に付ける」**  

***
QUESTION 666 解説

- 問題文（英語）:  
Which feature is required for IPv6 Source Guard to block traffic arriving on the server interface from unknown sources?

A. Dynamic ARP Inspection  
B. IPv6 RA Guard  
C. DHCPv6 Guard  
D. IPv6 ND Inspection  

Correct Answer: D

- 問題文要約（日本語）:  
サーバ接続インターフェースに到着する、不明な送信元の IPv6 トラフィックを IPv6 Source Guard でブロックしたい。  
そのために必須となる機能はどれか。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - IPv6 Source Guard 自体は **「バインディングテーブルに載っているアドレスかどうか」を見てデータトラフィックをフィルタするだけ**の機能で、  
    - **ND や DHCP パケットを自分では解析しない**。 [web:818][web:849][web:825]  
  - Cisco の公式説明では、次のように書かれている。 [web:818][web:849][web:825]  
    - 「IPv6 source guard does not inspect ND or DHCP packets; rather, it works in conjunction with IPv6 neighbor discovery (ND) inspection or IPv6 address glean, both of which detect existing addresses on the link and store them into the binding table.」  
    - つまり、**IPv6 ND Inspection などが ND メッセージを監視してバインディングテーブルを作成し、そのテーブルを IPv6 Source Guard が参照して未知の送信元をブロックする**。  
  - 選択肢 D「IPv6 ND Inspection」は、まさにこの **バインディングテーブルを自動生成するために必要な機能**として、問題文の要件（未知の送信元をブロック）を満たす前提となる。 [web:818][web:850][web:853]  
  - 他の選択肢が不適切な理由:
    - A: Dynamic ARP Inspection は IPv4 ARP 用であり、**IPv6 ではなく IPv4** のスプーフィング対策機能。  
    - B: IPv6 RA Guard は **不正な Router Advertisement の遮断**が目的で、バインディングテーブル作成と Source Guard の動作前提とは別の役割。 [web:853]  
    - C: DHCPv6 Guard は **不正 DHCPv6 サーバからの応答をブロック**する機能で、バインディングテーブルを作るのは DHCPv6 Snooping や ND Inspection 側。  
      - たとえ DHCPv6 Guard を有効にしても、「未知の送信元を判定するためのテーブル」が無ければ Source Guard は正しく動けない。  

- 試験向けの覚え方フレーズ

  - **「Source Guard はテーブルを見るだけ、テーブルを“作る”のは ND Inspection（＋DHCPv6 Snooping）」**  
  - 一行パターン:
    - **「IPv6 Source Guard 単体では何も学習しない → ND Inspection 必須」**  
  - セット暗記:
    - **「ND Inspection／IPv6 Snooping → binding table 作成 → Source Guard が unknown source をブロック」**  

***
QUESTION 667 解説

- 問題文（英語）:  
A customer reports that user traffic of bank XYZ to the AAA server is not using the primary path via the R3-R2 link. The network team observes:  
- No fiber is cut on links R2 and R3.  
- AS101 and AS201 routers established BGP peering.  
Which configuration resolves the issue?

A.  
R6(config)#router bgp 65201  
R6(config-router)#no neighbor 10.10.10.5 weight 32769  

B.  
R4(config)#router bgp 65201  
R4(config-router)#no neighbor 10.10.10.6 weight 32769  

C.  
R1(config)#route-map BGP-Path permit 10  
R1(config-route-map)# set local-preference 200  
R1(config)#router bgp 65101  
R1(config-router)# neighbor 10.10.10.2 route-map BGP-Path out  

D.  
R2(config)#route-map BGP-Path permit 10  
R2(config-route-map)# set metric 200  
R2(config)#router bgp 65101  
R2(config-router)# neighbor 10.10.10.3 route-map BGP-Path out  

Correct Answer: A

- 問題文要約（日本語）:  
銀行 XYZ から AAA サーバへのユーザトラフィックが、優先パスである R3–R2 リンク経由を通らず、別経路を使ってしまっている。  
光断はなく、AS101 と AS201 間の BGP ピアリングも正常。どの設定変更で、トラフィックを本来のプライマリパスに戻せるか。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 解説によると、**R4 の出力で「R6 から学習した経路の weight が 32769 になっており、その経路が優先されている」**ことが問題の根本になっている。 [file:787]  
    - Cisco BGP の **weight** は、そのルータ内だけで有効な最優先属性で、**数値が大きい経路が必ず優先**される。  
    - デフォルトの weight は 0（または同 AS 内のローカル発信 32768）だが、ここでは **neighbor 10.10.10.5 weight 32769** のように人為的に高く設定されているため、R4 は R6 経由のパスを強制的に選択している。 [web:804][web:808]  
  - weight 属性は **ルータ間で広告されないローカル属性**なので、  
    - R6 に設定があるのではなく、**R4 側で neighbor コマンドに weight を指定している**と解釈するのが自然。 [web:804][web:808]  
  - 選択肢 A は R6 上の設定に見えるが、問題の意図としては「**R4 が R6 向けに設定している weight 32769 を削除して、R6 経由を過度に優先させないようにする**」ことを指している。  
    - weight を削除すれば、R4 は通常どおり他の BGP 属性（LOCAL_PREF、AS_PATH、MED など）で R3–R2 経路をプライマリとするようになる。 [web:804][web:808]  
  - 他の選択肢が不適切な理由（解説より）: [file:787][web:804]  
    - C: R1 で Local Preference を変更しても、それは AS65101 内部にしか影響せず、**AS65201 側での R4 の経路選好（R6 vs R3–R2）には影響しない**。  
    - D: R2 側から AS65201 に向けて MED 200 をセットすると、**デフォルト MED 100 より大きくなるため、AS65201 のルータは R2 経由をむしろ避ける**方向に働き、目的と逆になる。  
    - B: R4 側で R6 への weight を消すべきという説明と合わず、問題の実際の出力内容とも整合しないとされている。  

- 試験向けの覚え方フレーズ

  - **「BGP weight＝そのルータだけの“絶対優先”属性、デフォルト 0／ローカル 32768」**  
  - 一行パターン:
    - **「おかしな経路選択を見たら、まず show bgp で weight 32769 など ‘盛られた weight’ を疑え」**  
  - セット暗記:
    - **「weight はローカルのみ、有効範囲＝1 台。変な weight はそのルータで消す（no neighbor x weight）」**  

***
QUESTION 668 解説

- 問題文（英語）:  
Users are experiencing slow response times from critical application servers connected to the network. The engineer determined from the console log that there is a DOS attack on the interface GigaEthernet0/0. Which action must the engineer take to resolve the issue?

A. Restrict CoPP traffic on the management interface from 172.17.132.95 and 192.168.1.1.  
B. Configure an access group on GigaEthernet0/0 to block traffic from 192.168.1.1.  
C. Configure QoS on FastEthemet0/0 to restrict bandwidth usage for 172.17.132.95 and 192.168.1.1.  
D. Configure an access group on GigaEthernet0/0 to block traffic from 172.17.132.95.  

Correct Answer: D

- 問題文要約（日本語）:  
重要なアプリケーションサーバへの応答が遅くなっており、コンソールログから G0/0 インターフェースに対する DoS 攻撃が発生していると判明した。  
どの対応でこの問題を解消できるか。  

- 正解: **D**

- 解説（なぜ D が正しいか）

  - 解説によると、コンソールログには以下のような 2 種類のエントリがある。 [file:787]  
    - 受信パケット（rcvd）:  
      - `s=172.17.132.95 (GigabitEthernet0/0), d=192.168.1.1 (FastEthernet0/0)`  
      - 意味: 送信元 172.17.132.95 からのパケットが G0/0 で受信され、宛先はルータの F0/0 インターフェース IP（192.168.1.1）。  
    - 送信パケット（sending）:  
      - `s=192.168.1.1 (local), d=172.17.132.95 (GigabitEthernet0/0)`  
      - 意味: ルータ自身（192.168.1.1）が送信元となり、172.17.132.95 宛ての応答パケットを G0/0 から送出している。  
  - これらから、**DoS 攻撃の発信元は 172.17.132.95 であり、G0/0 から流入している**ことが分かる。 [file:787]  
    - 従って、**G0/0 上で 172.17.132.95 からのトラフィックを ACL でブロックする**のが最も直接的で正しい対策となる。  
  - 他の選択肢が不適切な理由: [file:787]  
    - A: CoPP（Control Plane Policing）は CPU 向け制御トラフィックを制御する機能であり、ここで問題となっているのは G0/0 から入ってくる大量トラフィックであって、管理 IF 向け CoPP 制限では本質的な DoS を止められない。  
    - B: 192.168.1.1 はルータ自身のアドレスであり、**攻撃元ではなくレスポンスの送信元**なので、これをブロックするのは不適切。  
    - C: QoS で帯域制限をかけても、攻撃トラフィックを完全に止めるわけではなく、CPU 負荷やセッション数の観点で DoS を根本的には解決できない。  
      - また、攻撃元は G0/0 側の 172.17.132.95 であり、F0/0 側で QoS をかけても入り口の攻撃を防げない。  

- 試験向けの覚え方フレーズ

  - **「ログの s= が攻撃“元”、d= が攻撃“先”。DoS の元 IP を入り口インターフェースで ACL ブロック」**  
  - 一行パターン:
    - **「DoS 疑い → console log で s=攻撃元 IP＋どの IF から入っているかをチェック」**  
  - セット暗記:
    - **「rcvd: s=攻撃元, d=自分／サーバ → その s を ‘入ってくる IF’ 上の ACL で deny」**  

***
QUESTION 669 解説

- 問題文（英語）:  
An engineer configured summarization for the R1 loopback addresses and failed. Which action permits the successful advertisement of the R1 loopback addresses?

A. Configure EIGRP 100 with a network statement for loopback 0 and redistribute the connected route for loopback 50.  
B. Configure EIGRP 100 with a network statement for loopback 0 and remove the leak map for loopback 50.  
C. Configure 100.1.1.1 permit statement in the prefix list LOOPBACK50 and redistribute the connected route for loopback 50.  
D. Configure EIGRP 100 with a network statement for loopback 0 and configure and redistribute the static route for loopback 50.  

Correct Answer: A

- 問題文要約（日本語）:  
エンジニアが R1 上のループバックアドレスに対して EIGRP サマリを設定したが、期待通りに広告されなかった。  
R1 のループバックアドレスを正しく広告できるようにするには、どの対応が必要か。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 解説によると、R1 のループバックをすべて広告するには次の 2 点が必要になる。 [web:870][web:863]  
    1. **Loopback0 を EIGRP に参加させること**  
       - `router eigrp 100` 配下で `network` コマンド、または `redistribute connected` で Loopback0 を EIGRP に入れる必要がある。  
    2. **設定済みサマリ（例: 10.100.1.0/24）が実際に広告される条件を満たすこと**  
       - EIGRP サマリは、その範囲内に属する少なくとも 1 つの明示的なルート（サブネット）が EIGRP で見えていないと、広告されない。 [web:871]  
       - 本問では leak-map で特定ループバック（lo50）をサマリと一緒にリークする構成なので、**Loopback50 を EIGRP に入れる＝connected を redistribute** してやる必要がある。 [web:870]  
  - 選択肢 A は:  
    - EIGRP 100 に Loopback0 を `network` コマンドで参加させる。  
    - Loopback50 は `redistribute connected` で EIGRP に入れる。  
    → これにより、  
      - Loopback0 自身のプレフィックスが広告され、  
      - Loopback50 がサマリ範囲内の実ルートとして存在するため、設定済みサマリ（10.100.1.0/24）＋リークの動作が成立する。 [web:870][web:863]  
  - 他の選択肢が不適切な理由:
    - B: leak-map を削除すると、サマリ＋特定ループバックのリークという意図した挙動が失われてしまう。問題は leak-map のせいではなく「EIGRP に入れていない loopback の扱い」なので、本質的な解決にならない。  
    - C: prefix-list LOOPBACK50 に特定アドレスを permit しても、**そもそも Loopback50 が EIGRP に redistribute されていなければ意味がない**。  
    - D: Loopback50 のためにわざわざ static route を作って redistribute するのは過剰で、**connected をそのまま redistribute する方がシンプルかつ意図に沿う**。選択肢 A がより正しいアプローチ。  

- 試験向けの覚え方フレーズ

  - **「EIGRP サマリは ‘範囲内の少なくとも 1 ルート’ が EIGRP に存在しないと出ない」**  
  - 一行パターン:
    - **「Loopback サマリ＋leak-map → L0 は network、L50 は redistribute connected で必ず EIGRP に載せる」**  
  - セット暗記:
    - **「サマリが出ないときは：①サマリ対象の実ルートが EIGRP にあるか ②leak-map でリークしたい prefix が EIGRP に入っているか」**  

***
QUESTION 671 解説

- 問題文（英語）:  
An engineer must configure a per VRF for TACACS+ for company A. Which configuration on RTG-1 accomplishes the task?

A.  
aaa new-model  
aaa group server tacacs+ Tacacscluster  
 server-private 172.16.1.3 port 49 key routing  
ip tacacs source-interface GigabitEthernet 0/0  
ip vrf CompanyA  

B.  
aaa new-model  
aaa group server tacacs+ Tacacscluster  
 server-private 172.16.1.1 port 49 key routing  
 ip tacacs source-interface GigabitEthernet 0/0  
 ip vrf forwarding CompanyA  

C.  
aaa new-model  
aaa group server tacacs+ Tacacscluster  
 server-private 172.16.1.3 port 49 key routing  
 ip tacacs source-interface GigabitEthernet 0/1  
 ip vrf forwarding CompanyA  

D.  
aaa new-model  
aaa group server tacacs+ Tacacscluster  
 server-private 172.16.1.1 port 49 key routing  
 ip tacacs source-interface GigabitEthernet 0/1  
 ip vrf CompanyA  

Correct Answer: B

- 問題文要約（日本語）:  
企業 A（CompanyA）向けに、TACACS+ を VRF 単位（per VRF）で利用できるように RTG-1 を設定する必要がある。  
どの設定がその要件を満たすか。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - Per VRF TACACS+ を構成するには、**AAA サーバグループがどの VRF を使うか** と **どのインターフェースのアドレスを TACACS+ の送信元にするか** を明示する必要がある。 [web:892][web:893][web:896]  
  - Cisco の公式ドキュメントでは、TACACS+ サーバグループに対して次の 2 つを設定する例が示されている。 [web:892][web:893]  
    ```text
    aaa group server tacacs+ tacacs1
     server-private 10.1.1.1 port 49 key cisco
     ip vrf forwarding cisco
     ip tacacs source-interface Loopback0
    ```
    - `ip vrf forwarding <VRF名>` で **そのサーバグループが利用する VRF** を指定。  
    - `ip tacacs source-interface <IF名>` で **その VRF に属するインターフェースを送信元**として TACACS+ トラフィックを出す。  
  - 本問の解説文でも、  
    - 「per VRF AAA services 用のコマンドは `ip vrf forwarding <VRF name>`」  
    - 「その VRF に割り当てられているインターフェースを source-interface に使う必要がある」  
    とされている。 [web:890][web:892][web:893]  
  - 問題の前提より、**GigabitEthernet0/0 が VRF CompanyA に属している**ため、  
    - 必要な 2 行は:  
      - `ip tacacs source-interface GigabitEthernet 0/0`  
      - `ip vrf forwarding CompanyA`  
    - これを両方満たしているのが選択肢 B であり、TACACS+ グループ Tacacscluster が VRF CompanyA 上の G0/0 からサーバへ到達できるようになる。 [web:890][web:892]  
  - 他の選択肢が不適切な理由:
    - A / D: `ip vrf CompanyA` という構文は TACACS+ サーバグループへの VRF 紐づけとして不正であり、正しくは `ip vrf forwarding CompanyA` をグループコンテキスト内で用いる必要がある。 [web:892][web:896][web:897]  
    - C: `ip vrf forwarding CompanyA` は正しいが、**source-interface にしている G0/1 が CompanyA VRF に属していない**前提のため、TACACS+ トラフィックが正しい VRF 経路を使えない。  

- 試験向けの覚え方フレーズ

  - **「Per VRF TACACS+：`aaa group` の中で `ip vrf forwarding VRF名`＋`ip tacacs source-interface VRF 内IF`」**  
  - 一行パターン:
    - **「VRF 単位 AAA＝`ip vrf forwarding`（server-group）と ‘その VRF の IF を source-interface’」**  
  - セット暗記:
    - **「ip vrf CompanyA ではなく ip vrf forwarding CompanyA（場所は server-group コンテキスト）」**  

***
QUESTION 672 解説

- 問題文（英語）:  
How many labels are present in an MPLS Layer 3 packet traversing through the network without traffic engineering?

A. 1  
B. 2  
C. 3  
D. 4  

Correct Answer: B

- 問題文要約（日本語）:  
トラフィックエンジニアリング（RSVP-TE など）を使わない通常の MPLS L3VPN で、コアを流れる MPLS パケットにはラベルがいくつ積まれているか。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - MPLS L3VPN では、コア内を流れる VPN トラフィックは **2 つのラベル**を持つ。 [web:911][web:912][web:913][web:918]  
    1. **外側（トップ）ラベル = 送信 PE から受信 PE までの LSP 用トランスポートラベル**  
       - 通常 LDP（または単純な MPLS）で配布され、P ルータはこのラベルだけを見てスワップしながらコア内を転送する。 [web:911][web:912]  
    2. **内側（ボトム）ラベル = VPN ラベル**  
       - MP-BGP により、受信 PE が各 VPN/VRF ごとに配布するラベルで、受信 PE はこのラベルを見て「どの VRF に渡すか」を判断する。 [web:910][web:911][web:918]  
  - Cisco などの解説でも「L3 MPLS VPN が正しく動作するために、入口 PE は各データパケットに 2 つのラベル（LSP ラベルと VPN ラベル）をプッシュする必要がある」と明記されている。 [web:912][web:910]  
  - 問題文は「without traffic engineering」と条件を付けており、  
    - RSVP-TE による追加のトンネルラベルが無い素の L3VPN であることを示しているので、  
    - ラベルスタックは **ちょうど 2 枚**（トランスポート＋VPN）になる。 [web:913][web:912]  
  - 選択肢 A: 素の MPLS（単純 LDP だけの L3 転送）なら 1 ラベルのケースもあるが、**L3VPN** では VRF を識別する VPN ラベルが必須なので 1 では足りない。 [web:913][web:918]  
  - C / D: 3 枚以上になるのは、さらに **RSVP-TE や階層 VPN、Inter-AS などで追加ラベルを積んだ場合**であり、「without traffic engineering」という前提と矛盾する。 [web:909][web:910][web:915]  

- 試験向けの覚え方フレーズ

  - **「L3VPN＝ラベル 2 枚：外＝コアの ‘行き先 PE’、内＝PE 内の ‘どの VRF’」**  
  - 一行パターン:
    - **「TE なし L3VPN → 2 ラベル（LSP＋VPN）、TE ありなら＋1 で 3 もあり得る」**  
  - セット暗記:
    - **「外ラベル＝トランスポート、内ラベル＝VPN／VRF、これで ‘PE まで’＋‘PE 内’ の 2 段階転送」**  

***
QUESTION 673 解説

- 問題文（英語）:  
An engineer configured router R3 to redistribute the prefix 10.10.10.0/24 from OSPF into EIGRP. R1 has no connectivity to the prefix. Which action enables receipt of prefixes on R1?

A. R1 does not have a neighbor relationship with R2. The EIGRP should be clear on R1.  
B. Duplicate router IDs on R1 and R3. R1 should modify its router ID.  
C. R3 is adverting the 10.20.20.0/24 prefix with a TTL of 1. R3 must set the TTL to 2 for this prefix.  
D. R1 is not receiving the next-hop IP address of R3. R2 must enable the network 10.20.20.0/24 within EIGRP.  

Correct Answer: B

- 問題文要約（日本語）:  
R3 は OSPF から 10.10.10.0/24 を EIGRP へ再配布しているが、R1 からそのプレフィックスに到達できない。  
どの対応で R1 がこのプレフィックスを正しく受信できるようになるか。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 解説によると、R2 の `show ip eigrp topology` で 10.10.10.0/24 を見ると、  
    - **originating router（由来ルータ ID）が 10.1.1.1** なのに、  
    - そのルートを **10.20.20.3（= R3 のアドレス）から受信している** ことがわかる。 [file:787]  
  - さらに R1 の `show run` から、R1 の EIGRP ルータ ID も **10.1.1.1** になっている。  
    - つまり、**R1 と R3 のルータ ID が重複（duplicate router ID）している** 状態。 [file:787]  
  - EIGRP では、ルータ ID はトポロジ上の一意な識別子として使われ、  
    - 同一 AS 内でルータ ID が重複すると、**ルートの起点が誤認識されたり、ループ防止アルゴリズム（DUAL）の動作に問題が出る**可能性がある。  
    - 本問では、「本来 R3 起点の再配布ルート」が「R1 起点」と誤認されることで、R1 への経路インストールに不具合が出ている状況を表している。 [file:787]  
  - 従って、**R1 側のルータ ID を R3 と重ならない値に変更し、EIGRP プロセスをクリアする**ことで、  
    - 10.10.10.0/24 の経路が正しく「R3 起点」として扱われ、R1 にも到達できるようになる。  
  - 他の選択肢が不適切な理由:
    - A: R1–R2 間の EIGRP ネイバーや単なるプロセスクリアでは、**ルータ ID の重複という根本原因**を解決できない。  
    - C: TTL についての記述は、パケット到達性の問題（ホップ数制限）に関するものだが、ここで問題になっているのは **EIGRP 経路情報の起点識別** であり、TTL とは無関係。  
    - D: R2 で 10.20.20.0/24 を EIGRP に入れるかどうかは next-hop 解決の話だが、問題文／解説では next-hop 不達ではなく **originating router ID の矛盾**を原因としている。  

- 試験向けの覚え方フレーズ

  - **「EIGRP のトポロジで ‘originating router ID’ と実際の受信元が一致しない → どこかで router-id 重複を疑え」**  
  - 一行パターン:
    - **「R1 と別ルータで同じ router-id を見る → R1 側 router-id を変えて clear ip eigrp」**  
  - セット暗記:
    - **「IGP/BGP 共通：router-id は AS 内ユニーク、重複＝経路起点の誤認・収束不良の元」**  

***
QUESTION 674 解説

- 問題文（英語）:  
The users from subnet 172.16.1.16/28 are experiencing connectivity issues to subnet 172.16.2.32/48. Which configuration resolves the issue?

A.  
R1(config)#route-map REDIST permit 10  
R1(config-route-map)#match ip address 15  
R1(config-route-map)#exit  
R1(config)# access-list 15 permit 172.16.0.0 0.0.255.255  
R1(config-router)#router ospf 1  
R1(config-router)#redistribute eigrp 100 subnets route-map REDIST  

B.  
R1(config-router)#router ospf 1  
R2(config-router)#redistribute eigrp 100 subnets metric 100  

C.  
R1(config-router)#router eigrp 100  
R1(config-router)#redistribute ospf 1 metric 1000000 1 1 1 1  

D.  
R1(config)#route-map REDIST permit 10  
R1(config-route-map)#match ip address 15  
R1(config-route-map)#exit  
R1(config)# access-list 15 permit 172.16.0.0 0.0.255.255  
R1(config-router)#router eigrp 100  
R1(config-router)#redistribute ospf 1 subnets route-map REDIST  

Correct Answer: C  

- 問題文要約（日本語）:  
172.16.1.16/28 のユーザが 172.16.2.32/28（問題文は /48 とあるが誤記）へ疎通できない。  
どの設定を入れればこの疎通問題を解決できるか。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 解説によると状況は次の通り:  
    - 行きのトラフィック（172.16.1.16/28 → 172.16.2.32/28）は、R3 のルーティングテーブルに 172.16.2.32 への経路が存在するため問題なし。  
    - 帰りのトラフィック（172.16.2.32/28 → 172.16.1.16/28）は、R4 のルーティングテーブルに 172.16.1.16/28 の経路が存在せず、ここでルーティングが破綻している。  
  - 172.16.1.16/28 のプレフィックスは OSPF 側に存在し、それを R1 が **EIGRP へ再配布**して R4 側に届ける必要がある。  
    - しかし、R1 の `router eigrp 100` 下にある「OSPF → EIGRP 再配布」の設定では **metric が指定されておらず、EIGRP のデフォルトメトリックも設定されていない**。  
    - EIGRP では、他プロトコルからの再配布時に **5 つのメトリック（帯域, 遅延, 信頼性, 負荷, MTU）** のセットが必須で、これが無いとルートは再配布されない。  
  - 選択肢 C は、R1 の EIGRP プロセスで次のように設定している:  
    ```text
    router eigrp 100
     redistribute ospf 1 metric 1000000 1 1 1 1
    ```  
    - これにより、OSPF ルート（172.16.1.16/28 など）が EIGRP に対して有効なメトリック付きで再配布され、EIGRP ドメイン側（R4 側）にも経路が届くようになる。  
    - 結果として、往復ともにルートが揃い、双方向通信が成立する。  
  - 他の選択肢が不適切な理由:

    - A / B: どちらも **EIGRP → OSPF の再配布側**に関する設定であり、  
      - 本件で問題となっているのは「OSPF 側の 172.16.1.16/28 を EIGRP 側に出せていない」ことなので、向きが逆。  
      - また A の route-map＋ACL 自体は「どのプレフィックスを出すか」の制御だが、メトリック未指定の根本問題は解決していない。  

    - D: OSPF → EIGRP の再配布を route-map＋ACL で制御しているが、  
      - やはり **メトリック指定がなく、EIGRP が受け入れられる形になっていない**。  
      - また 172.16.0.0/16 全体を許可する ACL を使う意図が問題のシナリオと合致せず、解説でも触れられていない。  

- 試験向けの覚え方フレーズ

  - **「EIGRP への再配布でメトリックを入れ忘れると“何も入らない”」**  
  - 一行パターン:
    - **「OSPF → EIGRP, BGP → EIGRP など ‘into EIGRP’ は必ず 5 つの metric をセット」**  
  - セット暗記:
    - **「片方向だけ通るときは ‘戻り経路の再配布’ を疑え → 特に EIGRP の metric 未設定」**  

***
QUESTION 675 解説

- 問題文（英語）:  
SW100 cannot receive routes from R1. Which configuration resolves the issue?

A.  
R1  
 router bgp 100  
  address-family ipv6  
   redistribute connected  
R2  
 router bgp 100  
  address-family ipv6  
   redistribute connected  

B.  
R1  
 router bgp 100  
  address-family ipv6  
   neighbor 2001::2 route-reflector-client  
   neighbor 2001:ABC:AABB:1100:1122:1111:2222:AAA2 route-reflector-client  
R2  
 router bgp 100  
  address-family ipv6  
   neighbor 2001::2  
   neighbor 2001::1 as-override  

C.  
R1  
 router bgp 100  
  address-family ipv6  
   neighbor 2001::2 route-reflector-client  
   neighbor 2001:ABC:AABB:1100:1122:1111:2222:AAA2 route-reflector-client  
R2  
 router bgp 100  
  address-family ipv6  
   neighbor 2001::2  
   neighbor 2001::1 next-hop-self  

D.  
R1  
 router bgp 100  
  address-family ipv6  
   no synchronization  
R2  
 router bgp 100  
  address-family ipv6  
   no synchronization  
SW100  
 router bgp 100  
  address-family ipv6  
   no synchronization  

Correct Answer: C

- 問題文要約（日本語）:  
SW100 が R1 からルートを受け取れていない。  
どの設定を入れれば、この問題を解決できるか。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 解説によると、R1 の `show ip(bg)p`（実際は IPv6）にて、  
    - AS300 起源のルート `2001::4/128` が R2 から学習されているが、  
    - R1 のルーティングテーブルにはインストールされておらず、SW100 にも伝わっていない。  
  - 原因は 2 つある。 [web:919][web:925][web:928]  

    1. **R1 から見て next-hop（2001::4）が到達不能**  
       - BGP は「next-hop への到達性が無い経路」は RIB にインストールしない。 [web:925]  
       - R1 は R2 から `2001::4/128` を学習しているが、next-hop アドレス（2001::4）への IGP 経路が無いため、ルーティングテーブルに載せていない。  
       - これを解決する典型手段が、**R2 側で `neighbor 2001::1 next-hop-self` を設定すること**。  
         - こうすると、R2 は R1 に対して「この経路の next-hop は自分（2001::2）」として広告するため、R1 は自分の IGP で到達可能な next-hop として扱える。 [web:921][web:925]  

    2. **iBGP の「client 間非転送」制約**  
       - BGP の基本ルールとして、**iBGP で学習した経路は別の iBGP ピアへそのまま再広告しない**（フルメッシュか RR が必要）。 [web:921][web:923][web:928]  
       - R1 は R2 から iBGP で `2001::4/128` を受け取る立場であり、SW100 も R1 と iBGP ピアの関係にある。  
       - したがって、R1 を **Route Reflector** にして SW100 を RR クライアントにする必要がある。  
       - 選択肢 C では、R1 で  
         ```text
         neighbor 2001::2 route-reflector-client
         neighbor 2001:ABC:AABB:1100:1122:1111:2222:AAA2 route-reflector-client
         ```  
         としており、R2 と SW100 を RR クライアントにしている。 [web:919][web:928]  

  - 選択肢 C は、  
    - 「R1 を RR にして R2 / SW100 をクライアント化」＋  
    - 「R2 で `neighbor 2001::1 next-hop-self` を設定し、R1 が next-hop に到達できるようにする」  
    の **両方を同時に満たす唯一の選択肢** であり、解説の説明と完全に一致する。 [web:919][web:921][web:925]  

  - 他の選択肢が不適切な理由:

    - A: `redistribute connected` は「自分の直結経路を BGP に入れる」設定であり、  
      - 現状問題になっている `2001::4/128` の next-hop 到達性や iBGP 再広告ルールは解決しない。  

    - B: `as-override` は eBGP で同一 AS を再利用する場面などで使う AS_PATH 操作であり、  
      - 今回の問題（iBGP 内での next-hop 解決と RR の有無）とは無関係。  

    - D: `no synchronization` は古い「IGP 同期」機能を無効化するもので、  
      - そもそも現代の BGP ではデフォルト無効であることが多く、ここでの next-hop 不達／iBGP 非転送問題には効果がない。  

- 試験向けの覚え方フレーズ

  - **「iBGP 経路が RIB に入らないときは ‘next-hop 到達性’ を、iBGP 間で流れないときは ‘RR or フルメッシュ’ を疑え」**  
  - 一行パターン:
    - **「R1 で見えるが SW に届かない → R1 を RR、R2 は next-hop-self で next-hop を自分に」**  
  - セット暗記:
    - **「iBGP: next-hop-self で到達性、route-reflector-client でクライアント間転送」**  

***
QUESTION 676 解説

- 問題文（英語）:  
An engineer examines the BGP update for the IPv6 prefix 2001:db8::1/128, which should have been summarized into a /64 prefix. Which sequence of actions achieves the summarization?

A. R1 is a route reflector with a router ID of 1.1.1.1, and the originator of the prefix is a route reflector client, which has a router ID of 3.3.3.3. Both routers belong to the same AS. Configure an aggregate address on the router with ID 1.1.1.1 for the prefix.  

B. R1 is a route reflector client of a RR with a router ID of 1.1.1.1, and the originator of the prefix has a router ID of 3.3.3.3. Both routers belong to different ASs. The prefix is not advertised to any peer and must be advertised using the network statement on R3.  

C. R1 is a route reflector client of a RR with a router ID of 1.1.1.1, and the originator of the prefix has a router ID of 3.3.3.3. Both routers belong to the same AS. Configure an aggregate address on the router with ID 3.3.3.3 for the prefix.  

D. R1 is a route reflector with a router ID of 3.3.3.3, and the originator of the prefix is a route reflector client, which has a router ID of 3.3.3.3. Both routers belong to the same AS. Configure an aggregate address on the router with ID 1.1.1.1 for the prefix.  

Correct Answer: C  

- 問題文要約（日本語）:  
本来 /64 にサマリされるべき IPv6 プレフィックス 2001:db8::1/128 の BGP Update を確認したところ、/128 のままで流れている。  
どの手順で /64 サマリを正しく実現できるか。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 解説のポイント: [web:935][web:937][web:938]  
    - BGP ルートリフレクタ環境では、  
      - **Cluster list** 属性に **RR（ルートリフレクタ）の Router-ID** が入る。  
      - **Originator** 属性に「その経路を最初に生成した RR クライアントの Router-ID」が入る。  
    - 問題の出力では:  
      - Cluster list に 1.1.1.1 → RR の Router-ID は 1.1.1.1。  
      - Originator に 3.3.3.3 → このプレフィックス 2001:db8::1/128 を生成した RR クライアントは 3.3.3.3。  
    - また、RR とそのクライアントは **同一 AS 内の iBGP** である必要がある。 [web:935][web:942][web:946]  
  - BGP の `aggregate-address` は、**そのルータの BGP テーブル内に存在する「より長いプレフィックス」をまとめてサマリルートを生成する**コマンド。 [web:934][web:937]  
    - したがって、**サマリを行うのに最も適切な位置は「元の /128 を持っているルータ（Originator）」** となる。  
    - このケースでは、Router-ID 3.3.3.3 の RR クライアントが 2001:db8::1/128 を origination しているので、**3.3.3.3 上で `aggregate-address 2001:db8::/64` を設定する**のが正解。 [web:937][web:938]  
  - 選択肢 C は:  
    - R1 は RR クライアント、RR の Router-ID は 1.1.1.1。  
    - プレフィックスの Originator は Router-ID 3.3.3.3。  
    - 両者は同じ AS に属しており、**Router-ID 3.3.3.3（Originator 側）で aggregate address を構成する**と明記している。  
    → RR クライアント上で /64 サマリを生成し、それが RR を通じて他の iBGP ピアへ反映される、という動作になり、解説の説明と一致する。  
  - 他の選択肢が不適切な理由:

    - A: サマリを **RR（1.1.1.1）側で実施**しているが、元ルートは RR クライアント（3.3.3.3）起源。  
      - RR 自身がその具体的な /128 を持っていない場合、期待どおりサマリを生成・抑制できないか、構成が複雑になる。  
    - B: 「両ルータが異なる AS に属している」と記載されているが、RR とクライアントは同一 AS 内で構成する必要があり、この前提が誤り。 [web:935][web:942]  
      - また、問題の焦点はサマリ生成であり、`network` ステートメントでの再広告ではない。  
    - D: Router-ID の役割が入れ替わっており、「RR が 3.3.3.3、クライアントの Router-ID も 3.3.3.3」という矛盾した記述になっている。  
      - Originator と RR は同じ Router-ID にならない（Originator は「クライアント側の RID」）ので、前提からおかしい。  

- 試験向けの覚え方フレーズ

  - **「BGP 集約は“その詳細ルートを持っているルータ”でやる → Originator のルータで aggregate-address」**  
  - 一行パターン:
    - **「RR 環境で ORIGINATOR_ID＝3.3.3.3 と見えたら ‘サマリは 3.3.3.3 上で設定’」**  
  - セット暗記:
    - **「Cluster list＝RR の RID、Originator＝クライアントの RID → サマリ位置は Originator 側」**  

***
QUESTION 677 解説

- 問題文（英語）:  
What are two features of BFD? (Choose two.)

A. scalable  
B. reliable  
C. replaces hello messages  
D. requires routing protocols.  
E. intensive on CPU for Layer 2 links  

Correct Answer: A B  

- 問題文要約（日本語）:  
BFD の特徴として正しいものを 2 つ選べ。  

- 正解: **A, B**

- 解説（なぜ A と B が正しいか）

  - A: **scalable**  
    - BFD は単一の BFD セッションを複数のルーティングプロトコル（OSPF, EIGRP, BGP, IS-IS など）が共有できるため、  
      - 各プロトコルごとに個別の高速 Hello タイマーを設定するよりも **スケーラブルな障害検出メカニズム**になる。 [web:951][web:952][web:954]  
    - さらに、一部プラットフォームでは BFD 処理をラインカード側にオフロードでき、CPU への負荷を抑えたまま多くのセッションを持てる。 [web:952][web:954][web:961]  

  - B: **reliable**（ここでは「信頼性の高い障害検出」という意味）  
    - BFD は非常に短い間隔で軽量なコントロールパケットを交換し、リンク／経路障害をミリ秒単位で検出できるよう設計されている。 [web:951][web:954][web:956]  
    - これにより、従来のルーティングプロトコル Hello のみを用いる場合より **高い可用性・迅速な収束（信頼性の高い障害検出）** を提供する。 [web:951][web:956][web:961]  

  - 他の選択肢が不適切な理由

    - C: **replaces hello messages**  
      - 実際には「Hello を完全に置き換える」のではなく、  
        - OSPF/EIGRP/BGP などのプロトコルが **BFD に登録して、障害検出を BFD に任せる** 形で併用する。 [web:951][web:954][web:956]  
      - そのため、「Hello を置き換える」という表現は言い過ぎであり、選択肢としては不正確。  

    - D: **requires routing protocols**  
      - BFD はルーティングプロトコル非依存であり、**スタティックルートや HSRP, その他の FHRP とも連携可能**。 [web:953][web:955][web:959]  
      - 「ルーティングプロトコルが必須」というのは誤り。  

    - E: **intensive on CPU for Layer 2 links**  
      - Cisco の資料では、BFD は軽量であり、場合によってはデータプレーンにオフロードできるため、  
        - 「Hello タイマーを極端に縮めたルーティングプロトコル」より **CPU 負荷が低い**と説明されている。 [web:952][web:954][web:961]  
      - よって「CPU 集約的」という表現は逆。  

- 試験向けの覚え方フレーズ

  - **「BFD＝高速・軽量な汎用障害検出：スケーラブルで信頼性の高い収束サポート」**  
  - 一行パターン:
    - **「BFD は routing に“寄生するセンサー”＝プロトコル非依存、Hello の補完役」**  
  - セット暗記:
    - **「特徴：scalable＋高速・高信頼な故障検出、不要：専用 routing protocol、誤り：CPU heavy / Hello 完全置き換え」**  

***
QUESTION 678 解説

- 問題文（英語）:  
Which configuration advertises more specific routes to R1 without sending a BGP summary route?

A.  
R2#configure terminal  
R2(config)#router bgp 100  
R2(config-router)#auto-summary  

B.  
R1#configure terminal  
R1(config)#router bgp 100  
R1(config-router)#auto-summary  

C.  
R2#configure terminal  
R2(config)#router bgp 100  
R2(config-router)#no auto-summary  

D.  
R1#configure terminal  
R1(config)#router bgp 100  
R1(config-router)#no auto-summary  

Correct Answer: C  

- 問題文要約（日本語）:  
R1 に対して「サマリルートではなく、より細かいプレフィックス（more specific routes）」を広告したい。  
BGP のサマリルートを送らずに、どの設定を入れればよいか。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - `auto-summary`（自動要約）は、**クラスフル境界でのサマリを自動生成して広告する機能**で、  
    - 有効な場合、10.1.1.0/24 など複数のサブネットをまとめて 10.0.0.0/8 のような **大きなサマリだけ** を広告し、more specific は隠れることがある。 [web:965][web:968]  
  - 逆に **`no auto-summary` を設定すると、自動サマリを行わず、more specific のプレフィックスをそのまま広告**する。 [web:966][web:970]  
  - 問題文は「R1 に more specific route を広告したい」「サマリは送りたくない」と言っているので、  
    - **サマリを生成している側（R2）が `no auto-summary` を持つ必要がある**。  
    - つまり、**広告元の R2 で `router bgp 100` 配下に `no auto-summary` を設定する C** が正解となる。 [web:966][web:967]  
  - 他の選択肢が不適切な理由:

    - A: `auto-summary` を R2 側に設定すると、**サマリルートが作られ、more specific が隠れる方向**の動作になるため、意図と真逆。 [web:965][web:968]  
    - B: R1 側で `auto-summary` を設定しても、「R1 が受信するルートの粒度」には影響しない。  
      - サマリの生成は広告元（R2）側の BGP 設定に依存する。  
    - D: R1 側の `no auto-summary` は、やはり「自分が広告するときどう振る舞うか」に関係し、  
      - 受信側として R1 にどのようなプレフィックスが送られるかは変わらない。  

- 試験向けの覚え方フレーズ

  - **「more specific を送りたいなら ‘no auto-summary’ を“送る側”で設定」**  
  - 一行パターン:
    - **「サマリが勝手に飛んでくる → 広告元ルータで no auto-summary」**  
  - セット暗記:
    - **「auto-summary＝クラスフル要約を自動生成、no auto-summary＝詳細プレフィックスをそのまま広告」**  

***
QUESTION 679 解説

- 問題文（英語）:  
R1 has a route map configured, which results in a loss of partial IPv6 prefixes for the BGP neighbor, resulting in service degradation. How can the full service be restored?

A. The route map requires a deny 20 statement without set conditions, and this will allow additional prefixes to be advertised.  
B. The neighbor requires a soft reconfiguration, and this will clear the policy without resetting the BGP TCP connection.  
C. The route map requires a permit 20 statement without set conditions, and this will allow additional prefixes to be advertised.  
D. The prefix list requires all prefixes that R1 is advertising to be added to it, and this will allow additional prefixes to be advertised.  

Correct Answer: C  

- 問題文要約（日本語）:  
R1 に設定された route-map が原因で、一部の IPv6 プレフィックスだけが BGP ピアに広告されず、サービスが劣化している。  
どの対応をすれば、完全なサービス（全プレフィックスの広告）を復旧できるか。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - route-map は、シーケンス番号の小さい行から順番に評価され、  
    - `permit` 行で条件にマッチしたプレフィックスは、その行で指定された `set` を適用して「許可」される。  
    - `deny` 行でマッチしたプレフィックスは、その行で「**完全に破棄（広告されない）**」される。  
    - **どの行にもマッチしなかったプレフィックスは最後に暗黙の `deny` となり、やはり広告されない。** [web:978][web:979]  
  - 本問の状況（「一部 IPv6 プレフィックスが欠落」）から、  
    - route-map の先頭（たとえば `route-map RM permit 10`）で **一部のプレフィックスだけを許可／変更している**が、  
    - 他のプレフィックスは **どのシーケンスにもマッチせず implicit deny で落ちている**、という典型パターンが推測される。 [web:978][web:979]  
  - これを解決する一般的な方法は、  
    - 先頭のシーケンスで特定プレフィックスだけマッチさせた後、  
    - **次のシーケンスで「条件なしの permit」を追加し、残りのすべてのプレフィックスを通す**こと。  
    - つまり、`route-map RM permit 20`（match も set も無し）を追加する。 [web:978][web:979]  
  - 選択肢 C はまさにこれを言っており、  
    - 「route map に `permit 20` を追加し、set 条件を何も指定しないことで、残りのプレフィックスも広告されるようにする」  
    → これにより、一部だけ通っていた状態から、「必要なプレフィックスはすべて通る」状態に戻る。  

  - 他の選択肢が不適切な理由:

    - A: `deny 20` を追加すると、そのシーケンスにマッチしたプレフィックスは**確実に破棄**されるだけで、  
      - 暗黙の permit にはならないため、欠落プレフィックスは依然として広告されない。  

    - B: soft reconfiguration は「ポリシー変更後に BGP を非破壊で再適用する」ための操作であり、  
      - **誤った route-map ロジックそのもの**を直さない限り、ソフトリセットだけではプレフィックス不足は解消されない。  

    - D: prefix-list に「すべてのプレフィックス」を列挙するのは運用上現実的でなく、  
      - 設問でも route-map ロジックの問題が原因とされているため、根本解決にはならない。  

- 試験向けの覚え方フレーズ

  - **「route-map で一部だけ通したいときは ‘最後に permit 〇〇（条件なし）’ を忘れない」**  
  - 一行パターン:
    - **「BGP フィルタで経路が一部消えたら、implicit deny 回避のための ‘permit 20’ を確認」**  
  - セット暗記:
    - **「deny ＝完全ブロック、permit＋条件無し＝残り全部通す、条件に当たらないと最後は暗黙の deny」**  

***
QUESTION 680 解説

- 問題文（英語）:  
R10 attempts to copy an image file from R11 using TFTP, and the operation is timing out. Which action resolves the issue?

A. R11 requires the TFTP server source-interface to be set to GigabitEthernet0/0.  
B. R10 must be configured to belong to the same VRF TFTP.  
C. Change R10 duplex to auto to resolve the duplex mismatch between routers R10 and R11.  
D. R11 requires the TFTP server command for the image to be set to the VRF TFTP.  

Correct Answer: A  

- 問題文要約（日本語）:  
R10 が R11 から TFTP でイメージファイルをコピーしようとしているが、タイムアウトしてしまう。  
どの対応でこの問題を解決できるか。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 解説によると、このシナリオでは **TFTP 通信用インターフェースが VRF に属している**。  
    - VRF 配下から TFTP を正常に動作させるには、**TFTP のソースインターフェースを、その VRF に所属するインターフェースに明示的に指定する必要がある**。 [web:987][web:988][web:992]  
  - Cisco の実例でも、VRF 内インターフェースをソースに指定して TFTP を成功させている。 [web:987][web:988][web:992]  
    ```text
    ip tftp source-interface GigabitEthernet0/0
    ```
    - このコマンドにより、TFTP パケットの送信元 IP が G0/0 のアドレスとなり、  
      - そのインターフェースが属する VRF のルーティングテーブルを用いて TFTP サーバ（R10 側）に到達できるようになる。 [web:987][web:991][web:992]  
  - 選択肢 A は、まさにこの **TFTP サーバ側（R11）でソースインターフェースを G0/0 に設定する**対応を指しており、  
    - 「VRF TFTP 上のインターフェース G0/0 を TFTP の送信元にする」ことで、タイムアウトを解消できる。  

  - 他の選択肢が不適切な理由:

    - B: R10 を同じ VRF に入れても、**R11 側で TFTP セッションのソースインターフェースが不適切なまま**であれば、依然として到達性に問題が残る。  
      - 問題の本質は「どの VRF／インターフェースから TFTP を発信しているか」であり、R10 の VRF ではない。  

    - C: duplex ミスマッチはスループット低下やエラーフレームの原因にはなり得るが、  
      - この種の問題ではまず IP 到達性・VRF・ソースインターフェース指定を確認すべきであり、解説でも言及がない。  

    - D: IOS の TFTP クライアント／サーバの基本コマンドには「TFTP の VRF を直接指定するオプション」はなく、  
      - VRF を意識させる標準的な方法は **`ip tftp source-interface` で VRF 所属インターフェースを指定すること** とドキュメントに明記されている。 [web:992][web:994]  

- 試験向けの覚え方フレーズ

  - **「VRF 経由の TFTP ＝ ip tftp source-interface で ‘その VRF の IF’ を指定」**  
  - 一行パターン:
    - **「copy tftp が VRF でタイムアウト → まず ip tftp source-interface <VRF内IF>」**  
  - セット暗記:
    - **「TFTP/FTP/SSH など管理系トラフィックの VRF 利用＝source-interface（or source-vrf）で VRF 側 IF を指定」**  

***
QUESTION 681 解説

- 問題文（英語）:  
R1 uses SP1 as the primary path. A network engineer must force all SSH traffic generated from R1 toward SP2. Which configuration accomplishes the task?

A.  
ip access-list extended match_SSH  
 permit tcp any any eq 22  
!  
route-map PBR_SSH permit 10  
 match ip address match_SSH  
 set ip next-hop 10.20.20.1  
!  
interface Gig0/0  
 ip policy route-map PBR_SSH  

B.  
ip access-list extended match_SSH  
 permit tcp any any eq 22  
!  
route-map PBR_SSH permit 10  
 match ip address match_SSH  
 set ip next-hop 10.20.20.1  
!  
ip local policy route-map PBR_SSH  

C.  
ip access-list extended match_SSH  
 permit tcp any any eq 22  
!  
route-map PBR_SSH permit 10  
 match ip address match_SSH  
 set ip next-hop 10.10.10.1  
!  
ip local policy route-map PBR_SSH  

D.  
ip access-list extended match_SSH  
 permit tcp any any eq 22  
!  
route-map PBR_SSH permit 10  
 match ip address match_SSH  
 set ip next-hop 10.20.20.1  
!  
interface Gig0/1  
 ip policy route-map PBR_SSH  

Correct Answer: B  

- 問題文要約（日本語）:  
R1 は通常 SP1 をメインパスとして使っているが、**R1 自身が発生させる SSH トラフィックだけ**は SP2 側（次ホップ 10.20.20.1）へ強制的に流したい。  
どの設定でこれを実現できるか。  

- 正解: **B**

- 解説（なぜ B が正しいか）

  - Policy-Based Routing（PBR）には 2 種類ある。 [web:1002][web:1003][web:1004]  
    - **インターフェース PBR**: `interface Gi0/x` 上で `ip policy route-map XXX` を設定し、  
      - そのインターフェースを通過するトラフィック（**通過トラフィック**）に対してのみ PBR を適用。  
    - **ローカル PBR（Local Policy Routing）**: グローバルで `ip local policy route-map XXX` を設定し、  
      - ルータ自身が生成するトラフィック（**locally generated traffic**）に対して PBR を適用。 [web:1002][web:1004][web:1008][web:1011]  
  - 問題文では「SSH traffic generated from R1」と明記されており、  
    - 対象は R1 自身がソースとなる SSH セッション（管理者が R1 から外部に SSH するなど）。  
    - この種のトラフィックには **ローカル PBR** を使う必要がある。 [web:1002][web:1004][web:1008][web:1011]  
  - 選択肢 B は、  
    - `match_SSH` ACL で「tcp any any eq 22」をマッチ。  
    - `route-map PBR_SSH` でその ACL をマッチさせ、`set ip next-hop 10.20.20.1`（SP2 側の次ホップ）を指定。  
    - そして `ip local policy route-map PBR_SSH` で **R1 ローカル発トラフィックに対してこの PBR を適用**している。 [web:1002][web:1004]  
    → これにより、R1 が発生させる SSH トラフィックは **常に SP2 側 10.20.20.1 を次ホップとして使う**ようになる。  

  - 他の選択肢が不適切な理由:

    - A / D: `ip policy route-map` をインターフェースに適用しているため、  
      - その IF を**通過する**トラフィックにしか PBR が効かず、「R1 自身がソースのトラフィック」には適用されない。 [web:1003][web:1004]  
      - 問題の要件は「generated from R1」なので、Local PBR が必要。  

    - C: 構文は B と同じく Local PBR だが、`set ip next-hop 10.10.10.1` としており、  
      - これは SP1 側（プライマリ）の次ホップであり、「SP2 に流したい」という要件と逆。  

- 試験向けの覚え方フレーズ

  - **「通過トラフィック＝interface で ip policy、ルータ発トラフィック＝ip local policy」**  
  - 一行パターン:
    - **「‘generated from R1’ と来たら即 Local PBR（ip local policy route-map）」**  
  - セット暗記:
    - **「PBR: set ip next-hop <ISP2> ＋ ip local policy → 自ルータ発トラフィックだけ ISP2 経由にできる」**  

***
QUESTION 682 解説

- 問題文（英語）:  
An engineer is troubleshooting suboptimal communication from the 192.168.5.32/28 subnet to the 172.16.3.16/28 segment using the slowest links. Which configuration resolves the suboptimal routing issue?

A.  
R1(config-router)#router eigrp 100  
R1(config-router)#redistribute ospf 1 metric 1000000 1 1 1 1  

B.  
R2(config-router)#router ospf 1  
R2(config-router)#default-metric 1  
R1(config-router)#router ospf 1  
R1(config-router)#default-metric 10  

C.  
R2(config-router)#router eigrp 100  
R2(config-router)#redistribute ospf 1 metric 1000000 1 1 1 1  

D.  
R2(config-router)#router ospf 1  
R2(config-router)#default-metric 10  
R1(config-router)#router ospf 1  
R1(config-router)#default-metric 1  

Correct Answer: C  

- 問題文要約（日本語）:  
192.168.5.32/28 サブネットから 172.16.3.16/28 セグメントへの通信が、帯域の低いリンク（10 Mbps）側を通ってしまうサブオプティマルな経路になっている。  
どの設定で、このサブオプティマルルーティングを解消できるか。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 解説によると、traceroute の結果から、  
    - 192.168.5.32/28 → 172.16.3.16/28 のトラフィックは、**上側の R1 経由（R1–R5 間 10 Mbps リンク）**を通っている。  
      - 192.168.250.6 は R1–R5 間 192.168.250.4/30 のアドレス。  
  - 172.16.3.16/28 は OSPF 側にあり、それを R1 と R2 から EIGRP へ再配布している構成。  
    - しかし、R1 / R2 両方で `redistribute ospf 1 metric 1 1 1 1 1`（実際は 1kbps）といった、**非常に小さい帯域メトリック**を使っている。 [web:1017][web:1018][web:1020]  
    - この 1 kbps が EIGRP メトリック計算で「最小帯域」として使われるため、  
      - R1 経由ルート・R2 経由ルートともに **ほぼ同じメトリック（等コスト）** に見えてしまい、  
      - R6 などのルータが、時に 10 Mbps の遅いリンク側（R1 経由）を選んでしまう。  
  - サブオプティマルを解消するには、**R2 から再配布される経路の方が「速いパス」を反映したメトリックになるように、R2 の再配布メトリックを調整する**必要がある。  
    - 具体的には、R2 で `redistribute ospf 1 metric 1000000 1 1 1 1` のように、  
      - 帯域メトリックを 1,000,000 kbps（= 1 Gbps）など、実際の高速リンクに近い値に設定し直す。 [web:1017][web:1018]  
    - これにより、R6 から見た「R2 経由の 172.16.3.16/28 への FD（Feasible Distance）」が R1 経由より小さくなり、  
      - トラフィックは優先的に **高速リンク側（R2 経由）** を選ぶようになる。  
  - 選択肢 C は、まさに R2 で OSPF → EIGRP 再配布のメトリックを `1000000 1 1 1 1` に変更している。  
    - R1 側の再配布設定はそのままでも、R2 側メトリックをより「速いパス」にふさわしい値にすることで、  
      - R2 経由が最適パスとして選ばれ、サブオプティマルルーティングが解消される。  

  - 他の選択肢が不適切な理由:

    - A: R1 側で OSPF → EIGRP 再配布メトリックを変更しても、  
      - 問題は「R2 経由の方をより魅力的にしたい」点なので、逆効果になる可能性がある。  

    - B / D: どちらも OSPF 側の `default-metric` を調整しているが、  
      - 問題のボトルネックは「OSPF → EIGRP 再配布時の EIGRP メトリックが不適切（両側 1kbps）」であり、  
      - OSPF デフォルトメトリックをいじっても、EIGRP 内でのパス選択問題は解決しない。  

- 試験向けの覚え方フレーズ

  - **「EIGRP 再配布メトリックを ‘小さすぎる値で共通’ にすると、速いリンクも遅いリンクも同じに見える」**  
  - 一行パターン:
    - **「サブオプティマル経路＋再配布あり → どのルータで何のプロトコルを ‘どのメトリック値’ で再配布しているか確認」**  
  - セット暗記:
    - **「OSPF→EIGRP: `redistribute ospf 1 metric BW DELAY RELI LOAD MTU`、BW の設定次第で最適パスが変わる」**  

***
QUESTION 683 解説

- 問題文（英語）:  
The enterprise users fail to authenticate with the TACACS server when a direct fiber link fails between RB and RD. The NOC team observes:  
- Users connected on AS65201 fail to authenticate with TACACS server 192.168.1.1.  
- Users connected on AS65101 successfully authenticate with TACACS server 192.168.1.1.  
- All AS65101 and AS65201 users are configured to authenticate with the TACACS server.  
Which configuration resolves the issue?

A. RF(config)#no ip prefix-list Customer seq 5 deny 192.168.1.1/32  
B. RF(config)#router bgp 65201  
   RF(config-router)# neighbor 10.0.0.17 prefix-list Customer out  
C. RC(config)#ip prefix-list Customer seq 5 permit 192.168.30.1/32  
D. RC(config)#router bgp 65101  
   RC(config-router)# neighbor 10.0.0.18 prefix-list Customer in  

Correct Answer: A  

- 問題文要約（日本語）:  
RB–RD 間のダイレクトファイバがダウンすると、AS65201 側のユーザだけ TACACS サーバ 192.168.1.1 に認証できなくなるが、AS65101 側は問題なく認証できる。  
両方の AS のユーザは TACACS 192.168.1.1 を使うよう設定済み。どの設定変更でこの問題を解決できるか。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 提示されている prefix-list `Customer` は、以下のような形と説明されている。 [web:891]  
    ```text
    ip prefix-list Customer: count: 2, range entries: 1, sequences: 5 - 10
     seq 5 deny 192.168.1.1/32        (hit count: 5)
     seq 10 permit 0.0.0.0/0 le 32    (hit count: 26)
    ```
    - `seq 5 deny 192.168.1.1/32` が **TACACS サーバ 192.168.1.1 への経路を明示的にブロック**している。  
    - `seq 10 permit 0.0.0.0/0 le 32` で、それ以外のルートは許可。  
  - 解説の仮定: [web:891]  
    - `Customer` prefix-list は、  
      - RE（問題文では RF と誤記）の BGP 設定で **RC とのピアに対する受信 or 送信フィルタ**として適用されていて、  
      - その結果、**AS65201 側に 192.168.1.1/32 の経路が届かない**ため、AS65201 ユーザは TACACS サーバへのルーティングが成立しない。  
    - 一方、AS65101 側経路は別パスで 192.168.1.1 に到達できるため、そちらのユーザは認証できている。  
  - この状況を解消するには、  
    - prefix-list `Customer` 内の `seq 5 deny 192.168.1.1/32` を削除し、  
    - 192.168.1.1/32 も `seq 10 permit 0.0.0.0/0 le 32` で **許可されるようにする**必要がある。 [web:1030][web:1031][web:1033]  
  - 選択肢 A の `no ip prefix-list Customer seq 5 deny 192.168.1.1/32` は、  
    - まさにこの deny エントリを削除するコマンドであり、  
    - これにより AS65201 側にも TACACS サーバ 192.168.1.1 への経路が広告され、認証が復旧する。 [web:891][web:1030]  

  - 解説上の補足（命名の紛らわしさ）: [web:891]  
    - 元の解説では、実際に prefix-list を持っているのは RE だが、選択肢では RF と表記されている。  
      - しかし他の選択肢（B〜D）は、いずれも問題の `deny 192.168.1.1/32` を解除しないため、  
      - 実質的に「RF」は「RE」の誤記とみなして A を選ぶべき、とされている。  

  - 他の選択肢が不適切な理由:

    - B: `neighbor 10.0.0.17 prefix-list Customer out` を設定しても、  
      - `Customer` 内の deny エントリが残ったままなので、192.168.1.1/32 のブロックは続く。  

    - C: `Customer seq 5 permit 192.168.30.1/32` は別プレフィックスを許可するだけで、  
      - 問題となっている 192.168.1.1/32 の deny を解除していない。  

    - D: `neighbor 10.0.0.18 prefix-list Customer in` は BGP ピア側に prefix-list を適用するだけで、  
      - deny 行そのものを削除しない限り、192.168.1.1/32 の経路はフィルタされ続ける。  

- 試験向けの覚え方フレーズ

  - **「prefix-list の ‘deny 1 行’ が重要経路を殺していないかを必ず確認」**  
  - 一行パターン:
    - **「TACACS 到達不可＋BGP あり → ‘192.168.1.1/32 deny’ みたいなピンポイント deny を最初に疑え」**  
  - セット暗記:
    - **「seq 5 deny X/32 ＋ seq 10 permit 0.0.0.0/0 le 32 → X だけ死ぬ構成、問題のときはまず seq 5 を消す」**  

***
QUESTION 684 解説

- 問題文（英語）:  
A NOC team receives a ticket that data traffic from RA to RF is not forwarded when the link between the RC-RE path goes down. All routers learn loopback IP through the IGP protocol. Which configuration resolves the issue?

A.  
RD(config)#router bgp 65201  
RD(config-router)#neighbor 10.10.10.2 ebgp-multihop 3  

B.  
RB(config)#router bgp 65101  
RB(config-router)#neighbor 10.10.10.3 update-source loopback 0  

C.  
RB(config)#router bgp 65101  
RB(config-router)#neighbor 10.10.10.3 ebgp-multihop 3  

D.  
RD(config)#router bgp 65201  
RD(config-router)#neighbor 10.10.10.2 update-source loopback 0  

Correct Answer: C  

- 問題文要約（日本語）:  
RC–RE 間のリンクがダウンすると、RA から RF へのデータトラフィックが RB–RD 経路でフォワードされなくなる。  
すべてのルータは Loopback IP を IGP で学習している。どの設定でこの問題を解決できるか。  

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 解説によると、RC–RE パスがダウンした後、本来は迂回経路として RB–RD 間のパスが使われるべきだが、  
    - 実際には **RB–RD 間の eBGP ピアリングが問題**を起こしており、  
    - その結果、RA→RF のトラフィックがこのバックアップパスを通れなくなっている。  
  - RD 側の `show ip bgp neighbors` 出力には、  
    - 「External BGP neighbor may be up to 3 hops away」の行が確認できる。  
    - これは Cisco IOS の `show ip bgp neighbors` で、`neighbor x.x.x.x ebgp-multihop 3` が設定されているときに出るメッセージであり、  
      - RD では既に `neighbor 10.10.10.2 ebgp-multihop 3` が設定されていることを示す。 [web:1043][web:1044][web:1046][web:1049]  
  - 一方、RB 側では同様の `ebgp-multihop` 設定がないため、  
    - RB–RD 間の eBGP セッションは「Loopback 同士＋中継ルータあり」の構成で **片側のみ multihop** の状態になっており、  
    - TTL=1 のデフォルト eBGP 仕様により、RB から RD への BGP パケットが途中で TTL 0 となって破棄されている。 [web:1044][web:1046][web:1048]  
  - この問題を解決するには、RB でも RD と同様に **`neighbor 10.10.10.3 ebgp-multihop 3`** を設定し、  
    - 双方で TTL を 3 に引き上げて、**Loopback 間のマルチホップ eBGP セッション**を成立させる必要がある。 [web:1044][web:1046][web:1049]  
  - 選択肢 C は、まさに RB 側の BGP プロセスで `neighbor 10.10.10.3 ebgp-multihop 3` を設定しており、  
    - RD 側に既に存在する `ebgp-multihop 3` と対になる形で eBGP セッションを確立できる。  

  - 他の選択肢が不適切な理由:

    - A: RD 側に `ebgp-multihop 3` を再設定しても、既に RD にはその設定が入っており、問題は解決しない。  

    - B / D: `update-source loopback 0` は「BGP セッションで使用するローカル送信元インターフェース」を Loopback0 にする設定であり、  
      - それ自体は「Loopback 間で eBGP セッションを張る」ための必要条件の一部だが、  
      - **マルチホップ中継が入る場合には、さらに `ebgp-multihop` が必要**である。 [web:1044][web:1039]  
      - 本問での根本原因は「片側だけ ebgp-multihop が設定されている」ことなので、update-source を変えても改善しない。  

- 試験向けの覚え方フレーズ

  - **「Loopback 同士の eBGP（間に L3 ホップあり）＝両側で `update-source Loopback`＋`ebgp-multihop` セット」**  
  - 一行パターン:
    - **「show ip bgp neighbors に ‘External BGP neighbor may be up to N hops away’ が片側だけ出てたら、反対側の ebgp-multihop 忘れ」**  
  - セット暗記:
    - **「eBGP: デフォルト TTL=1、1ホップ超えたら必ず ebgp-multihop が両端に必要」**  

***
QUESTION 685 解説

- 問題文（英語）:  
A network engineer wants to redirect HTTP traffic originating from subnet 192.168.3.1/24 to the next-hop IP address of 10.2.3.1. This configuration was implemented:  
Which command completes the configuration?

A. Router(config)#access-list 100 permit tcp 192.168.3.0 0.0.0.255 any eq 80  
B. Router(config)#access-list 100 permit tcp 192.168.3.0 0.0.0.255 eq any 80  
C. Router(config)#access-list 100 permit tcp any 192.168.3.0 0.0.0.255 eq 80  
D. Router(config)#access-list 100 permit tcp any eq 80 192.168.3.0 0.0.0.255  

Correct Answer: A  

- 問題文要約（日本語）:  
192.168.3.0/24 サブネット「から発信される」HTTP トラフィック（ポート 80）を、次ホップ 10.2.3.1 に PBR でリダイレクトしたい。  
この PBR 構成を完成させる ACL 行として正しいのはどれか。  

- 正解: **A**

- 解説（なぜ A が正しいか）

  - PBR では、`route-map` 内の `match ip address 100` などに紐づく **拡張 ACL** で、  
    - 「ソース」「宛先」「プロトコル」「ポート（サービス）」を指定してマッチさせる。 [web:1064][web:1058][web:1060]  
  - 今回の要件は「**192.168.3.0/24 から発信される HTTP トラフィック**」なので、  
    - ソース: 192.168.3.0/24  
    - 宛先: 任意（any）  
    - プロトコル: tcp  
    - 宛先ポート: 80（HTTP）  
    を指定する必要がある。 [web:1064][web:1060]  
  - Cisco 拡張 ACL の一般形式は:  
    ```text
    access-list 100 permit tcp <src_ip> <src_wildcard> <dst_ip> <dst_wildcard> eq <dst_port>
    ```  
    - よって、要件に合う正しい文は  
      - `access-list 100 permit tcp 192.168.3.0 0.0.0.255 any eq 80`  
    - これにより、「192.168.3.0/24 から任意の宛先への HTTP トラフィック」が PBR の対象となる。  

  - 他の選択肢が不適切な理由:

    - B: `192.168.3.0 0.0.0.255 eq any 80` の構文は誤りで、  
      - `eq` は IP アドレスの直後ではなく、宛先 IP／ワイルドカードの後に来る。  

    - C: `any 192.168.3.0 0.0.0.255 eq 80` は、  
      - 「任意のソースから 192.168.3.0/24 宛ての HTTP トラフィック」を意味し、方向が逆（宛先が 192.168.3.0/24）になってしまう。  

    - D: `any eq 80 192.168.3.0 0.0.0.255` は、  
      - 「ソースポート 80 から 192.168.3.0/24 宛て」のトラフィックを意味し、HTTP サーバ側ではなくクライアント側トラフィックとしては不適切。  

- 試験向けの覚え方フレーズ

  - **「ACL の並び：src IP → src wildcard → dst IP → dst wildcard → eq <dst port>」**  
  - 一行パターン:
    - **「『○○からの HTTP』＝`permit tcp <src_subnet> <wildcard> any eq 80`」**  
  - セット暗記:
    - **「PBR で ‘from subnet X の HTTP’ を redirect → 拡張 ACL で source=X, dst=any, eq 80」**  

***