***
### QUESTION 300 解説

- 問題文:
Refer to the exhibit.  
An OSPF neighbor relationship between R2 and R3 is stuck in EXCHANGE/EXSTART state. The neighbor is established between R1 and R2. The network engineer can ping from R2 to R3 and vice versa, but the neighbor is still down. Which action resolves the issue?

- 問題文要約（日本語）:  
R2 と R3 の間で OSPF ネイバーを張ろうとしているが、`show ip ospf neighbor` では  
- R2 側: neighbor 192.168.99.2 state **EXCHANGE/**  
- R3 側: neighbor 192.168.99.1 state **EXSTART/**  
の状態で止まっており、Full まで上がらない。[file:61]  
ICMP では相互に ping は成功するため L3 到達性はあるが、OSPF の隣接関係だけが確立しない。どの対応がこの問題を解決するかを問う。

- 選択肢:
A. Restore the Layer 2/Layer 3 connectivity issue in the ISP network.  
B. Match MTU on both router interfaces or ignore MTU.  
C. Administrative “shut then no shut” both router interfaces.  
D. Enable OSPF on the interface, which is required.  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - OSPF 隣接が **EXSTART / EXCHANGE** 状態で止まり、ping は通る、という典型的な原因は「両端インターフェイスの MTU 不一致」。  
      OSPF は DBD（Database Description）パケットのやり取りの際に MTU をチェックし、値が異なるとネイバーを Full まで上げずに EXSTART/EXCHANGE で固まる挙動を取る。[file:24]
    - したがって、この問題は  
      - インターフェイスの `ip mtu` / `mtu` 値を両端で一致させる  
      - もしくはインターフェイス側に `ip ospf mtu-ignore` を設定して、OSPF による MTU チェックを無効化する  
      のいずれかで解決できる。  
      選択肢 B の「Match MTU on both router interfaces or ignore MTU」は上記の対策をそのまま表しているため正解。

- 他の選択肢がダメな理由
    - A: ping が成功していることから、L2/L3 の基本的な到達性には問題がない。ISP 側の接続を「復旧」するのは根本原因ではない。
    - C: インターフェイスを shut/no shut すると一時的に状態はリセットされるが、MTU 不一致が残っていれば再び EXSTART/EXCHANGE で止まるため、恒久的な解決にはならない。
    - D: 出力には既に `show ip ospf neighbor` でネイバーが見えているので、そのインターフェイスで OSPF は有効化されている。  
      有効化されていないならそもそも INIT/2-WAY すら表示されないため、この回答は状況と矛盾する。

***
### QUESTION 301 解説

- 問題文:
Refer to the exhibit.  
R1 and R2 have been configured with the following settings for forming a BGP neighbor relationship over 172.16.1.0/24:

R1 (AS 100)  
router bgp 100  
 neighbor 172.16.1.2 remote-as 200  
 neighbor 172.16.1.1 password cisco123  

R2 (AS 200)  
router bgp 200  
 neighbor 172.16.1.1 remote-as 100  
 neighbor 172.16.1.1 password cisco  

The neighbor relationship is not coming up. Which configuration resolves the issue?

- 問題文要約（日本語）:  
R1（AS100）と R2（AS200）は eBGP でピアを張ろうとしている。  
物理リンクは 172.16.1.1（R1 e0/0）– 172.16.1.2（R2 e0/0）。[file:62]  
しかし、BGP 設定を見ると、R1 側の `neighbor` と `password` 行の IP が一致していなかったり、両者で設定しているパスワード文字列も異なっており、その結果 BGP ネイバーが up しない。  
どの設定に変更すれば、この隣接関係を確立できるか。

- 選択肢:
A. R1  
   router bgp 100  
    neighbor 172.16.1.2 password cisco  

B. R1  
   router bgp 100  
    neighbor 172.16.1.2 password MD5 cisco  
   R2  
   router bgp 200  
    neighbor 172.16.1.1 password MD5 cisco  

C. R2  
   router bgp 200  
    neighbor 172.16.1.1 password cisco123  

D. R1  
   router bgp 100  
    neighbor 172.16.1.2 password MD5 cisco123  
   R2  
   router bgp 200  
    neighbor 172.16.1.1 password MD5 cisco123  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 現状の問題点は 2 つ:
        1. R1 でパスワードを設定している neighbor IP が誤り  
           - R1 は実際には `neighbor 172.16.1.2 remote-as 200` で R2 とピアを張ろうとしているのに、パスワード行は `neighbor 172.16.1.1 password ...` となっており、**自分自身の IP を指定してしまっている**。  
        2. R1 と R2 のパスワード文字列が不一致  
           - R1: `cisco123`、R2: `cisco`。BGP MD5 認証では両端で **完全一致したパスワード** が必要。  
    - 選択肢 A は R1 側で  
      `neighbor 172.16.1.2 password cisco`  
      と設定し、  
      - neighbor IP を正しく相手 (172.16.1.2) に修正  
      - パスワード文字列を R2 側の `cisco` と一致させる  
      ため、この一行で両方の問題が解消される。[file:24]
    - BGP のパスワードは明示的に `MD5` と書かなくても、`neighbor … password` でデフォルトが MD5 認証になるため、A のような設定で十分。[file:24]

- 他の選択肢がダメな理由
    - B: パスワード文字列は R1・R2 とも `cisco` で一致しているが、**R1 の元々の誤った neighbor IP（172.16.1.1）を修正する記述がない**。  
      問題文の設定のままに `MD5` を追加しただけなら、依然として R1 は自分自身に対してパスワードを設定していることになり、ピアは上がらない。
    - C: R2 側のパスワードを `cisco123` に変えるだけで、**R1 側の neighbor IP の誤りが残ったまま**。  
      そのため、R1 が本当にピアを結びたい 172.16.1.2 にはパスワードが設定されず、認証ミスマッチでネイバーは確立されない。
    - D: 両端でパスワード文字列は一致するが、B と同様に「R1 の neighbor IP の誤りを直す記述がない」点は変わらない。  
      `MD5` を明示しても、対象 neighbor の IP が正しくなければ意味がない。

***
### QUESTION 301 解説

- 問題文:
Refer to the exhibit.  
R1 and R2 have been configured with the following settings for forming a BGP neighbor relationship over 172.16.1.0/24:

R1 (AS 100)  
router bgp 100  
 neighbor 172.16.1.2 remote-as 200  
 neighbor 172.16.1.1 password cisco123  

R2 (AS 200)  
router bgp 200  
 neighbor 172.16.1.1 remote-as 100  
 neighbor 172.16.1.1 password cisco  

The neighbor relationship is not coming up. Which configuration resolves the issue?

- 問題文要約（日本語）:  
R1（AS100）と R2（AS200）は eBGP でピアを張ろうとしている。  
物理リンクは 172.16.1.1（R1 e0/0）– 172.16.1.2（R2 e0/0）。[file:62]  
しかし、BGP 設定を見ると、R1 側の `neighbor` と `password` 行の IP が一致していなかったり、両者で設定しているパスワード文字列も異なっており、その結果 BGP ネイバーが up しない。  
どの設定に変更すれば、この隣接関係を確立できるか。

- 選択肢:
A. R1  
   router bgp 100  
    neighbor 172.16.1.2 password cisco  

B. R1  
   router bgp 100  
    neighbor 172.16.1.2 password MD5 cisco  
   R2  
   router bgp 200  
    neighbor 172.16.1.1 password MD5 cisco  

C. R2  
   router bgp 200  
    neighbor 172.16.1.1 password cisco123  

D. R1  
   router bgp 100  
    neighbor 172.16.1.2 password MD5 cisco123  
   R2  
   router bgp 200  
    neighbor 172.16.1.1 password MD5 cisco123  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 現状の問題点は 2 つ:
        1. R1 でパスワードを設定している neighbor IP が誤り  
           - R1 は実際には `neighbor 172.16.1.2 remote-as 200` で R2 とピアを張ろうとしているのに、パスワード行は `neighbor 172.16.1.1 password ...` となっており、**自分自身の IP を指定してしまっている**。  
        2. R1 と R2 のパスワード文字列が不一致  
           - R1: `cisco123`、R2: `cisco`。BGP MD5 認証では両端で **完全一致したパスワード** が必要。  
    - 選択肢 A は R1 側で  
      `neighbor 172.16.1.2 password cisco`  
      と設定し、  
      - neighbor IP を正しく相手 (172.16.1.2) に修正  
      - パスワード文字列を R2 側の `cisco` と一致させる  
      ため、この一行で両方の問題が解消される。[file:24]
    - BGP のパスワードは明示的に `MD5` と書かなくても、`neighbor … password` でデフォルトが MD5 認証になるため、A のような設定で十分。[file:24]

- 他の選択肢がダメな理由
    - B: パスワード文字列は R1・R2 とも `cisco` で一致しているが、**R1 の元々の誤った neighbor IP（172.16.1.1）を修正する記述がない**。  
      問題文の設定のままに `MD5` を追加しただけなら、依然として R1 は自分自身に対してパスワードを設定していることになり、ピアは上がらない。
    - C: R2 側のパスワードを `cisco123` に変えるだけで、**R1 側の neighbor IP の誤りが残ったまま**。  
      そのため、R1 が本当にピアを結びたい 172.16.1.2 にはパスワードが設定されず、認証ミスマッチでネイバーは確立されない。
    - D: 両端でパスワード文字列は一致するが、B と同様に「R1 の neighbor IP の誤りを直す記述がない」点は変わらない。  
      `MD5` を明示しても、対象 neighbor の IP が正しくなければ意味がない。

***
### QUESTION XXX 解説

- 問題文:
Refer to the exhibit.  
The diagram shows that R1 has the OSPF router ID 10.255.255.1, and a message similar to the following is seen:  
`%OSPF-4-FLOOD_WAR: ... type-5 adv-rtr 10.255.255.1 ...`  
After configuring OSPF on R1, some external destinations in the network became unreachable. Which action resolves the issue?

- 問題文要約（日本語）:  
R1 に OSPF を設定したところ、ネットワーク上のいくつかの外部宛先が到達不能になった。  
ログには「`%OSPF-4-FLOOD_WAR` type-5 adv-rtr 10.255.255.1」といったメッセージが出ており、R1 の OSPF Router ID は 10.255.255.1 になっている。  
これは OSPF の外部 LSA（Type 5）に関するフラッディング警告であり、どの対処を行えば問題を解消できるかを問う問題。[file:24]

- 選択肢:
A. Change the R1 router ID from 10.255.255.1 to a unique value and clear the process.  
B. Increase the SPF delay interval on R1 to synchronize routes.  
C. Disconnect the router with the OSPF router ID 0.0.0.0 from the network.  
D. Clear the OSPF process on R1 to flush stale LSAs sent by other routers.  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - `%OSPF-4-FLOOD_WAR` は、「ルータが**自分自身が生成した LSA を、別ルータ経由で再度受け取り続けている**」ときに出る警告で、  
      よくある原因は「同じ OSPF Router ID を持つルータが複数存在すること」。特に Type-5 LSA（外部経路）については、**異なるエリアで同じ Router ID を使って ASBR が存在**すると、このような状況が起こる。[file:24]
    - 今回、ログの `adv-rtr 10.255.255.1` は「LSA を広告したルータ ID」が 10.255.255.1 であることを示しており、R1 も Router ID 10.255.255.1 を使っている。  
      つまり **他の ASBR も同じ Router ID を使っている** 可能性が高く、外部 LSA が衝突して一部の外部宛先が不達になっている。
    - この問題を根本的に解決するには、**R1 の Router ID をネットワーク内で一意な値に変更し、その後 `clear ip ospf process` などで OSPF プロセスを再起動して新しい LSAs を再配布させる**必要がある。  
      選択肢 A はこの手順をそのまま表しているため正解。

- 他の選択肢がダメな理由
    - B: SPF delay（SPF 計算のディレイタイマ）を増やしても、**Router ID の重複という根本原因は解決しない**。LSA のループ／フラッディング自体は続き、外部経路の不整合も残る。
    - C: Router ID 0.0.0.0 は通常一時的な状態で、ここでの問題は明示的に 10.255.255.1 の Type-5 LSA が繰り返し見えていること。  
      0.0.0.0 のルータを切断しても、この FLOOD_WAR の原因には関係しない。
    - D: OSPF プロセスをクリアすると、一時的には LSDB がリセットされて状況が改善したように見えるかもしれないが、**Router ID が重複したままなら、再び同じ問題が発生する**。  
      したがって、単純なプロセスクリアだけでは恒久的な解決にはならない。

***
### QUESTION 302 解説

- 問題文:
Refer to the exhibit.  
The diagram shows that R1 has the OSPF router ID 10.255.255.1, and a message similar to the following is seen:  
`%OSPF-4-FLOOD_WAR: ... type-5 adv-rtr 10.255.255.1 ...`  
After configuring OSPF on R1, some external destinations in the network became unreachable. Which action resolves the issue?

- 問題文要約（日本語）:  
R1 に OSPF を設定したところ、ネットワーク上のいくつかの外部宛先が到達不能になった。  
ログには「`%OSPF-4-FLOOD_WAR` type-5 adv-rtr 10.255.255.1」といったメッセージが出ており、R1 の OSPF Router ID は 10.255.255.1 になっている。  
これは OSPF の外部 LSA（Type 5）に関するフラッディング警告であり、どの対処を行えば問題を解消できるかを問う問題。[file:24]

- 選択肢:
A. Change the R1 router ID from 10.255.255.1 to a unique value and clear the process.  
B. Increase the SPF delay interval on R1 to synchronize routes.  
C. Disconnect the router with the OSPF router ID 0.0.0.0 from the network.  
D. Clear the OSPF process on R1 to flush stale LSAs sent by other routers.  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - `%OSPF-4-FLOOD_WAR` は、「ルータが**自分自身が生成した LSA を、別ルータ経由で再度受け取り続けている**」ときに出る警告で、  
      よくある原因は「同じ OSPF Router ID を持つルータが複数存在すること」。特に Type-5 LSA（外部経路）については、**異なるエリアで同じ Router ID を使って ASBR が存在**すると、このような状況が起こる。[file:24]
    - 今回、ログの `adv-rtr 10.255.255.1` は「LSA を広告したルータ ID」が 10.255.255.1 であることを示しており、R1 も Router ID 10.255.255.1 を使っている。  
      つまり **他の ASBR も同じ Router ID を使っている** 可能性が高く、外部 LSA が衝突して一部の外部宛先が不達になっている。
    - この問題を根本的に解決するには、**R1 の Router ID をネットワーク内で一意な値に変更し、その後 `clear ip ospf process` などで OSPF プロセスを再起動して新しい LSAs を再配布させる**必要がある。  
      選択肢 A はこの手順をそのまま表しているため正解。

- 他の選択肢がダメな理由
    - B: SPF delay（SPF 計算のディレイタイマ）を増やしても、**Router ID の重複という根本原因は解決しない**。LSA のループ／フラッディング自体は続き、外部経路の不整合も残る。
    - C: Router ID 0.0.0.0 は通常一時的な状態で、ここでの問題は明示的に 10.255.255.1 の Type-5 LSA が繰り返し見えていること。  
      0.0.0.0 のルータを切断しても、この FLOOD_WAR の原因には関係しない。
    - D: OSPF プロセスをクリアすると、一時的には LSDB がリセットされて状況が改善したように見えるかもしれないが、**Router ID が重複したままなら、再び同じ問題が発生する**。  
      したがって、単純なプロセスクリアだけでは恒久的な解決にはならない。

***
### QUESTION 303 解説

- 問題文:
Which two solutions are used to overcome a flapping link that causes a frequent label binding exchange between MPLS routers? (Choose two)

- 問題文要約（日本語）:  
MPLS ネットワークでリンクがフラップすると、LDP セッションが頻繁に張り直され、そのたびにラベルバインディングの再交換が発生して不安定になる。  
この「リンクの一時的なダウン・フラップ」に対して、LDP セッション／ラベル情報を保護し、頻繁な再ネゴシエーションを防ぐ手段を 2 つ選ぶ問題。[file:24]

- 選択肢:
A. create targeted hellos to protect the session  
B. increase a hold-timer to protect the session  
C. increase input queue on links to protect the session  
D. create link dampening on links to protect the session  
E. increase a session delay to protect the session.  

- 正解: **A, B**

- 解説（なぜ A・B が正しいか）
    - **A. create targeted hellos to protect the session**  
      - MPLS LDP Session Protection 機能では、**LDP Targeted Hellos** を用いて、物理リンクがダウンしても LDP セッションを別インターフェイス経由で維持する。  
      - これにより、リンクフラップがあっても LDP セッションやラベルバインディングを再確立する必要が減り、頻繁なラベル交換を防げる。[file:24]
    - **B. increase a hold-timer to protect the session**  
      - `mpls ldp session holdtime` を長めに設定すると、**一定時間 LDP メッセージが途絶えてもセッションをすぐには落とさない**。  
      - 瞬間的なリンクフラップや短時間のパケットロスでセッションがダウンしなくなり、ラベル再配布の頻度を抑えられる。[file:24]

- 他の選択肢がダメな理由
    - C: 入力キューを増やすと輻輳時のドロップは多少減るかもしれないが、**リンクの物理的なフラップによるセッション切断**には直接効かない。LDP セッション保護の手段としては不適切。
    - D: 「リンクダンピング（dampening）」は一般的に BGP ルートフラップ抑制などで使う概念であり、ここでは **LDP セッションそのものを保護するメカニズムではない**。  
    - E: セッションディレイは「セッションの確立を遅らせる」機能であり、問題文のような **既存セッションの頻繁な切断・再確立** を防ぐものではない。[file:24]

***
### QUESTION 304 解説

- 問題文:
Refer to the exhibit.  
A command `aaa authentication login default local` is entered.  
A network engineer must configure a login authentication method at line console 0 on a Cisco ISR router. During configuration, the engineer got a call and the router console session timed out. When the call was completed, the engineer pressed Enter on the console. The engineer was asked for a username to log in to the router even though no username and password were previously configured on the router. What action resolves the issue?

- 問題文要約（日本語）:  
エンジニアが ISR ルータのコンソールで作業中に、`aaa authentication login default local` を入力した直後に電話対応で離席し、その間にコンソールがタイムアウトした。  
戻って Enter を押すと、コンソールログインに「username」が要求されるが、ルータには **まだローカルユーザ名／パスワードを一切設定していない**。  
どうすれば再度ルータにアクセスして設定をやり直せるか、という問題。

- 選択肢:
A. Telnet to the router to log in  
B. Power cycle to reload the router and reconfigure it  
C. Configure the user and password in the RADIUS server  
D. Use CTRL+C to bypass authentication in the router.  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - `aaa authentication login default local` を入れると、「すべてのログイン（コンソール・VTY など）は **ローカルユーザデータベース** で認証する」という意味になる。[file:24]
    - ところが、この時点で `username xxx secret yyy` などのローカルユーザがまだ一切設定されていないため、**認証に使えるアカウントが存在しない**。  
      結果として、コンソールにユーザ名を入れても認証できず、完全にロックアウトされる。
    - さらに、コンソールでの設定変更は running-config 上には反映されているが、まだ `write` していない前提は問題文から明確ではないものの、  
      多くの試験文脈では「保存前なら電源再投入すれば元の設定に戻る」ことを意図している。[file:24]
    - よって、**電源再投入（Power cycle）でルータを再起動し、startup-config（AAA 設定を入れる前の状態）からやり直す**のが現実的な解決策となるため、B が正解。

- 他の選択肢がダメな理由
    - A: Telnet も `aaa authentication login default local` の対象であり、**コンソール同様にローカルユーザ認証を要求**される。  
      ローカルユーザが存在しない限り、Telnet でもログインできないため解決にならない。
    - C: すでに `default local` としているため、認証は RADIUS ではなく「ルータ上のローカルデータベース」を参照する。  
      RADIUS サーバにユーザを作っても、AAA メソッドリストを `group radius` に変更しない限り使われない。
    - D: Cisco IOS のログインプロンプトで **Ctrl+C で認証をバイパスする方法は存在しない**。  
      Ctrl+C はコンフィグモード内でのコマンド中断などに使われるキーであり、ログイン要求そのものをスキップすることはできない。

***
### QUESTION 305 解説

- 問題文:
An engineer creates a default static route on a router with a next hop of 10.1.1.1. On inspection, the engineer finds the router has two VRFs, Red and Blue. The next hop is valid for both VRFs and exists in each assigned VRF. Which configuration achieves connectivity?

- 問題文要約（日本語）:  
ルータには VRF Red と VRF Blue の 2 つがあり、どちらの VRF からも next-hop 10.1.1.1 に到達できる（各 VRF 内に 10.1.1.1 への経路がある）。  
それぞれの VRF からインターネットなど外部へのデフォルトルートを、next-hop 10.1.1.1 で設定したい。  
どの `ip route` 設定を入れれば、Red / Blue 両方の VRF でデフォルトルートを正しく持てるか、という問題。[file:24]

- 選択肢:
A.  
   ip route vrf Red 0.0.0.0 255.255.255.255 10.1.1.1  
   ip route vrf Blue 0.0.0.0 255.255.255.255 10.1.1.1  

B.  
   ip route 0.0.0.0 0.0.0.0 10.1.1.1  

C.  
   ip route vrf Red 0.0.0.0 0.0.0.0 10.1.1.1  
   ip route vrf Blue 0.0.0.0 0.0.0.0 10.1.1.1  

D.  
   ip route vrf Blue 0.0.0.0 255.255.255.255 10.1.1.1  
   ip route vrf Red 0.0.0.0 255.255.255.255 10.1.1.1  

- 正解: **C**

- 解説（なぜ C が正しいか）
    - VRF ごとに別々のルーティングテーブルがあり、**各 VRF 内でデフォルトルートを定義する必要がある**。  
    - デフォルトルートはプレフィックス `/0` なので、Cisco IOS では  
      `ip route vrf <VRF名> 0.0.0.0 0.0.0.0 <next-hop>`  
      の形式を使う。[file:24]
    - 選択肢 C は、Red・Blue の両方に対して  
      - `ip route vrf Red  0.0.0.0 0.0.0.0 10.1.1.1`  
      - `ip route vrf Blue 0.0.0.0 0.0.0.0 10.1.1.1`  
      を設定しており、**両 VRF のルーティングテーブルにデフォルトルートを正しく追加**しているため、要件を満たす。

- 他の選択肢がダメな理由
    - A / D: マスクに `255.255.255.255` を指定しており、これは「/32 のホストルート」であってデフォルトルートではない。  
      宛先 0.0.0.0/32 へのみ有効で、通常の「どこ宛でも 10.1.1.1 へ」というデフォルト動作にならない。
    - B: VRF を指定しない `ip route 0.0.0.0 0.0.0.0 10.1.1.1` は **グローバルルーティングテーブル** のデフォルトルートを作るだけであり、VRF Red/Blue のテーブルには反映されない。  
      そのため、Red / Blue VRF 内のトラフィックはこのデフォルトルートを利用できない。

***
### QUESTION 306 解説

- 問題文:
What are two characteristics of IPv6 Source Guard? (Choose two)

- 問題文要約（日本語）:  
IPv6 Source Guard 機能の性質について正しいものを 2 つ選ぶ問題。  
Source Guard は、IPv6 Snooping などで作られた「バインディングテーブル」に基づいて、ポート上の送信元 IPv6 アドレスを検証し、不正なトラフィックを遮断する Layer2 セキュリティ機能。[file:24]

- 選択肢:
A. requires the user to configure a static binding  
B. recovers missing binding table entries  
C. used in service provider deployments to protect DDoS attacks  
D. requires that validate prefix be enabled  
E. requires IPv6 snooping on Layer 2 access or trunk ports  

- 正解: **C, E**

- 解説（なぜ C・E が正しいか）
    - **E. requires IPv6 snooping on Layer 2 access or trunk ports**  
      - IPv6 Source Guard は、**IPv6 Snooping / ND inspection / DHCPv6 gleaning** などで作成される「IPv6 ファーストホップ・セキュリティのバインディングテーブル」に依存して動作する。[file:24]  
      - このテーブルは、MAC・VLAN・ポート・IPv6 アドレス・リンクローカルアドレスなどを L2 ポート上で学習するため、**L2 アクセス／トランクポートで IPv6 snooping 系機能が有効であることが前提条件**となる。
    - **C. used in service provider deployments to protect DDoS attacks**  
      - IPv6 Source Guard 自体は「既知ホストのみ許可」という性質があり、エンタープライズ側のアクセスエッジ向けが主用途だが、  
        その内部機能である **IPv6 Prefix Guard** を用いて、サービスプロバイダ環境で「許可されたプレフィックス以外からの不正トラフィック（DDoS など）」を遮断する使い方ができる。[file:24]  
      - 問題集の想定解では、Source Guard 機能セットの一部として Prefix Guard を含めた全体を見ており、「SP で DDoS 攻撃から保護するのに用いられる」という説明に合致するため C を正解としている。

- 他の選択肢がダメな理由
    - **A: requires the user to configure a static binding**  
      - Source Guard は「既に存在するバインディングテーブルを参照してフィルタする」機能であって、静的バインディングの設定を必須とはしていない。  
        バインディングテーブルは通常 ND inspection や IPv6 Snooping、DHCP gleaning などで自動生成される。[file:24]
    - **B: recovers missing binding table entries**  
      - 欠落しているエントリを ND や DHCPv6 から再学習するのは、**Binding Table Recovery** 機能の役割であり、Source Guard 単体の動作ではない。  
        Source Guard はあくまで「テーブルにない送信元アドレスのトラフィックを拒否する」側。[file:24]
    - **D: requires that validate prefix be enabled**  
      - `validate prefix` は IPv6 Prefix Guard 機能のためのオプションであり、Source Guard を基本的に動かすだけなら必須ではない。  
        Prefix Guard を使わないなら、このコマンドは不要なので「必須」という表現は誤り。

***
### QUESTION 307 解説

- 問題文:
An engineer is creating a policy that overrides normal routing behavior. If the route to a destination of 10.100.100.0/24 is withdrawn from the routing table, the policy must direct traffic to a next-hop of 10.1.1.1. If the route is present in the routing table, then normal forwarding must occur. Which configuration meets the requirements?

- 問題文要約（日本語）:  
宛先 10.100.100.0/24 へのパケットに対して、  
- ルーティングテーブルに 10.100.100.0/24 の経路が **存在する間は通常ルーティング** を行う。  
- その経路が **消えたときだけ**、PBR で next-hop 10.1.1.1 に転送させたい。  
この条件付き PBR を実現する `route-map` / ACL の組み合わせを選ぶ問題。

- 選択肢（要旨）:
A. ACL 100: `permit ip any any`、route-map で `set ip next-hop recursive 10.1.1.1`  
B. ACL 100: 宛先 10.100.100.0/24、route-map で `set ip next-hop recursive 10.1.1.1`  
C. ACL 100: 宛先 10.100.100.0/24、route-map で `set ip next-hop 10.1.1.1`  
D. ACL 100: 宛先 10.100.100.0/24、route-map で `set ip default next-hop 10.1.1.1`  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - PBR の `set` 動作:
        - `set ip next-hop <IP>`  
          → 該当パケットに対し **常に** 指定 next-hop を使う（ルーティングテーブルに経路があっても上書き）。  
        - `set ip default next-hop <IP>`  
          → **宛先へのルートがルーティングテーブルに無いときだけ** 指定 next-hop を使い、  
            経路が存在するときは通常のルーティングテーブルに従う。[file:24]
    - 問題の要件は「10.100.100.0/24 のルートがある間は通常ルーティング／無くなったら 10.1.1.1 を使う」なので、  
      - 該当宛先をマッチさせる ACL（10.100.100.0/24）  
      - ルートが無いときだけ next-hop を指定する `set ip default next-hop 10.1.1.1`  
      の組み合わせが必要。  
      これを満たすのが選択肢 D であり、要件通りの「条件付き PBR」を実現できる。

- 他の選択肢がダメな理由
    - A: ACL が `any any` なので、**すべてのトラフィック** に対して常に `set ip next-hop recursive 10.1.1.1` を適用してしまい、  
      - 宛先が 10.100.100.0/24 以外も含めて通常ルーティングが上書きされる。  
      - さらに `set ip next-hop` 系はルートが存在しても常に適用されるため、要件と逆。
    - B: 宛先マッチは正しいが、`set ip next-hop recursive` を使っており、**経路の有無に関係なく** 常に 10.1.1.1 を next-hop として使う。  
      経路が存在するときも PBR が優先されるため、通常ルーティングにならない。
    - C: B と同様に `set ip next-hop` を使っているため、ルートがある場合も必ず 10.1.1.1 へ転送してしまう。  
      「ルートがあるときは通常転送」という条件を満たさない。

***
### QUESTION 308 解説

- 問題文:
Refer to the exhibit.  
A diagram shows that R1 has three paths to reach network 5.5.5.0/24. EIGRP is used as the routing protocol, but only two EIGRP routes for 5.5.5.0/24 are installed in the routing table. An engineer finds that traffic destined for 5.5.5.0/24 does not use all available paths. Which action resolves the issue?

- 問題文要約（日本語）:  
R1 から 5.5.5.0/24 へ到達する 3 本の EIGRP パスが存在するが、`show ip route` ではそのうち 2 本しかルーティングテーブルに載っておらず、  
実際のトラフィックも 2 本のパスでしかロードバランシングされていない。  
3 本すべてを EIGRP の等価 / 不等価ロードバランシングの対象に含めたい。どの設定変更が必要か、という問題。[file:24]

- 選択肢:
A. Decrease the variance value in EIGRP.  
B. Stop advertising 192.168.13.0/24 in EIGRP.  
C. Remove the adjacency of R3 from EIGRP.  
D. Increase the variance value in EIGRP.  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - EIGRP では、**FD（Feasible Distance）が最小のパスを SFD とし、他の候補パスのメトリックが「SFD × variance 以下」なら、  
      そのパスもルーティングテーブルに載せてロードバランシングに参加**させる。[file:24]
    - 現状、3 本のうち 2 本だけが「SFD × 現在の variance」以内に収まっており、3 本目のパスはメトリックが高すぎて条件を満たしていないため除外されている。  
      → **variance を大きくすれば、メトリックの高い 3 本目も許容範囲に入り、ルーティングテーブルにインストールされる**。
    - したがって、「Increase the variance value in EIGRP.」である D を選ぶと、3 本すべてのパスを EIGRP が利用できるようになる。

- 他の選択肢がダメな理由
    - A: variance を減らすと、「許容されるメトリック差」が小さくなり、**さらに多くのパスが除外される** 可能性がある。3 本すべてを使いたい要件とは逆。
    - B: 192.168.13.0/24 の広告を止めると、そもそもその経路を通るパスが消えるため、  
      「すべてのパスを利用したい」という目的に反して経路数を減らしてしまうだけ。
    - C: R3 の隣接を削除すると、R3 経由のパスが完全に消えるため、パスを増やすどころか減らしてしまい、問題の趣旨と逆。

***
### QUESTION 309 解説

- 問題文:
Refer to the exhibit.  
A network administrator has developed a Python script on the local Linux machine and is trying to transfer it to the router using SCP, but the transfer fails with:

Password:  
Administratively disabled.  
Connection to 198.51.100.64 closed by remote host. [file:63]

Which action resolves this issue?

- 問題文要約（日本語）:  
Linux 端末から `scp script.py admin@198.51.100.64:script.py` を実行すると、パスワード入力後に「Administratively disabled.」と表示されて接続が切断される。[file:63]  
SCP で IOS ルータにファイルをコピーしたいが、どの設定を行えばよいかを問う問題。

- 選択肢:
A. The SSH service must be enabled with the crypto key generate rsa command.  
B. The SCP service must be enabled with the ip scp server enable command.  
C. The Python interpreter must first be enabled with the guestshell enable command.  
D. The SSH access must be allowed on the VTY lines using the transport input ssh command.  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - メッセージ「Administratively disabled」は、**SSH 接続自体はできるが、SCP サーバ機能が無効化されている** IOS デバイスに対して SCP を試みたときに表示される典型的な出力。[file:24]
    - Cisco IOS で SCP サーバ機能を有効にするには、グローバルコンフィグで  
      `ip scp server enable`  
      を設定する必要がある。  
      これにより、既存の SSH サービス上で SCP 転送が許可され、Linux クライアントからの `scp` が成功するようになる。

- 他の選択肢がダメな理由
    - A: もし RSA キーが生成されておらず SSH 自体が無効なら、**そもそもパスワードプロンプトすら出ず** 接続ができない。  
      今回はパスワード入力後に「Administratively disabled」と出ているため、SSH はすでに有効。
    - C: guestshell（Python インタプリタ）はルータ上でスクリプトを実行するための機能であり、**SCP 転送の成否には関係しない**。
    - D: `transport input ssh` が未設定で SSH が禁止されていれば、A と同様に接続自体に失敗し、「Administratively disabled」ではなく接続拒否やタイムアウトになる。  
      したがって、本ケースの症状とは一致しない。

### QUESTION 310 解説

- 問題文:
Refer to the exhibit.  
A junior engineer updated a branch router configuration. Immediately after the change, the help desk reports that branch users cannot reach any network destinations. Which configuration restores service and continues to block 10.1.1.100/32?

- 問題文要約（日本語）:  
BRANCH-RTR では EIGRP 100 を動かしており、FastEthernet0/0 に対して受信ルートをフィルタするための distribute-list が設定されている。

現在の設定（抜粋）:  
router eigrp 100  
 network 10.4.31.0 0.0.0.7  
 network 10.100.100.1 0.0.0.0  
 distribute-list route-map FILTER-IN in FastEthernet0/0  
 eigrp router-id 10.100.100.1  

ip prefix-list 102 seq 10 permit 10.1.1.100/32  

route-map FILTER-IN deny 10  
 match ip address prefix-list 102  [file:64]

この変更後、ブランチからどの宛先にも到達できなくなった。10.1.1.100/32 だけをブロックしつつ、他の経路は受け入れて通信を復旧させるには、どの設定を追加／修正すべきか。

- 選択肢:
A. route-map FILTER-IN deny 5  
B. ip prefix-list 102 seq 15 permit 0.0.0.0/32 le 32  
C. ip prefix-list 102 seq 5 permit 0.0.0.0/32 le 32  
D. route-map FILTER-IN permit 20  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - 現状の route-map `FILTER-IN` は、シーケンス 10 で  
      `deny` + `match ip address prefix-list 102`  
      のみが定義されている。[file:64]
    - route-map には **暗黙の deny** があり、どのシーケンスにもマッチしなかったルートはすべて拒否される。  
      つまり今の設定だと  
      - 10.1.1.100/32 にマッチ → seq 10 の `deny` で拒否（これは狙い通り）  
      - それ以外のプレフィックス → どのシーケンスにもマッチせず暗黙の `deny` → **全部拒否される**  
      ため、ブランチからどこにも行けなくなっている。
    - 解決には、「10.1.1.100/32 以外はすべて許可」するための **permit 行** を route-map の後ろに追加する必要がある。  
      選択肢 D の `route-map FILTER-IN permit 20` を追加すれば、  
      - seq 10: 10.1.1.100/32 を `deny`  
      - seq 20: その他のプレフィックスは条件無しで `permit`  
      となり、10.1.1.100 だけをブロックしつつ、その他の EIGRP ルートは通常通り受け入れられる。

- 他の選択肢がダメな理由
    - A: `deny 5` を追加しても、結局 seq 5 と seq 10 のどちらかで `deny` されるだけであり、「permit するシーケンス」が存在しない点は変わらない。
    - B / C: prefix-list 102 に「0.0.0.0/0〜/32 を permit」するエントリを足すと、  
      そのプレフィックスも seq 10 の `deny` にマッチしてしまい、**むしろ全プレフィックスが deny 対象になりかねない**。  
      いずれにせよ route-map に `permit` シーケンスが無い限り、暗黙の deny によりルートは受け入れられない。

***
### QUESTION 311 解説

- 問題文:
Refer to the exhibit.  
An engineer cannot copy the IOS.bin image from an FTP server to the switch. Which action resolves the issue?

- 問題文要約（日本語）:  
FTP サーバ (10.0.0.2/24) に IOS.bin ファイルがあり、ユーザ名／パスワードは `cisco / cisco` と表示されている。[file:65]  
スイッチ側では VLAN2 に 10.0.0.1/24 を設定し、`ip ftp source-interface vlan 2` も設定済み。  
PC から 10.0.0.1 への ping は成功しているので、L3 接続は問題ない。  
しかしスイッチで  
`copy ftp://cisco:cisco@10.0.0.2/IOS.bin flash:/`  
を実行すると、  
`%Error opening ftp://cisco:cisco@10.0.0.2/IOS.bin (No such file or directory)`  
と表示されて失敗する。[file:65]  
どの対応を行えば IOS.bin を正常にコピーできるか。

- 選択肢:
A. Allow file permissions to download the file from the FTP server.  
B. Add the IOS.bin file, which does not exist on FTP server.  
C. Make memory space on the switch flash or USB drive to download the file.  
D. Use the copy flash:/ ftp://cisco@10.0.0.2/IOS.bin command.  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 図中の FTP サーバのメモには「File to download: IOS.bin」と記載されており、**IOS.bin ファイル自体はサーバ上に既に存在する前提** になっている。[file:65]
    - にもかかわらず `No such file or directory` が表示されるのは、  
      - FTP サーバ側で、そのファイル（またはディレクトリ）への **読み取り権限が付与されていない**  
      - FTP のホームディレクトリ / パス設定の関係で、匿名または `cisco` ユーザからそのファイルにアクセスできない  
      といった **ファイル権限・アクセス権の問題** の可能性が高い。
    - したがって、FTP サーバ側で `IOS.bin` に対し「ダウンロード（読み取り）を許可するファイル権限」を設定し直せば、同じコマンドでコピーが成功する。  
      これが選択肢 A の内容であり、最も妥当な解決策。

- 他の選択肢がダメな理由
    - B: 図の説明から、`IOS.bin` は「ダウンロードすべきファイル」として既に存在している前提なので、「ファイルを追加する」が正解ではない。  
      もし存在しなければ、その旨の記載があるはず。
    - C: フラッシュ容量不足であれば、エラーは「No space left on device」など容量関連のメッセージになり、「No such file or directory」にはならない。
    - D: `copy flash:/ ftp://...` は「スイッチから FTP サーバへアップロード」するコマンドであり、問題は「サーバからスイッチへダウンロード」なので方向が逆。  
      また現在のエラーはダウンロード時のファイル参照問題であり、コピー方向を変えても根本原因は解決しない。

***
### QUESTION 312 解説

- 問題文:
What is an advantage of implementing BFD?

- 問題文要約（日本語）:  
BFD（Bidirectional Forwarding Detection）をネットワークに導入したときのメリットとして、正しいものを 1 つ選ぶ問題。

- 選択肢:
A. BFD provides faster updates for any flapping route.  
B. BFD provides millisecond failure detection  
C. BFD is deployed without the need to run any routing protocol  
D. BFD provides better capabilities to maintain the routing table  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - BFD は、2 台のルータ間で**非常に短い間隔（ミリ秒オーダー）で Hello メッセージをやり取りしてリンク障害を検出する軽量プロトコル**。[file:24]
    - 通常の OSPF / EIGRP / BGP などのデッドタイマよりもはるかに短い時間で障害検出ができ、**ミリ秒単位の高速フェイルオーバー**を実現するのが最大の利点なので、B が正解。

- 他の選択肢がダメな理由
    - A: BFD 自体は「ルートアップデート」を送るわけではなく、**リンク障害の検出**だけを担当する。ルーティング更新は各プロトコル（OSPF, BGP など）が行う。
    - C: BFD はしばしば OSPF / EIGRP / BGP など「既存のルーティングプロトコルと連携して」使われるものであり、それら無しに単独で運用するのが目的ではない。
    - D: ルーティングテーブルの管理そのものを強化する機能ではなく、あくまで障害検出を速くして、**上位プロトコルが早く経路切り替えできるよう補助する**だけ。

***
### QUESTION 313 解説

- 問題文:
What is the purpose of the DHCPv6 Guard?

- 問題文要約（日本語）:  
DHCPv6 Guard 機能の役割について正しい説明を 1 つ選ぶ問題。  
DHCPv6 Guard は、スイッチのポートで「どのポートが正規の DHCPv6 サーバ／リレーからの応答を流せるか」を制御し、不正（rogue）サーバからの配布を防ぐ仕組み。[file:24]

- 選択肢:
A. It filters messages between a DHCPv6 server and a DHCPv6 client (or relay agent).  
B. It shows that clients of a DHCPv5 server are affected.  
C. It block DHCPv6 messages from relay agents to a DHCPv6 server.  
D. It allows DHCPv6 reply and advertisements from (rouge) DHCPv6 servers.  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - DHCPv6 Guard は、**スイッチが「トラストポート」と「アンテラストポート」を区別し、不正なポートから送られてくる DHCPv6 サーバメッセージ（Reply, Advertise など）をブロックする機能**。[file:24]
    - つまり「DHCPv6 サーバとクライアント（またはリレー）の間を通過する DHCPv6 メッセージをフィルタし、許可されていない方向の応答を遮断する」機能なので、  
      選択肢 A の説明が最も近い。

- 他の選択肢がダメな理由
    - B: DHCPv5 というバージョンは存在せず、DHCPv6 Guard の動作とも無関係。
    - C: DHCPv6 Guard は主に **サーバからクライアントへの Reply / Advertise などをブロック**する機能であり、リレーからサーバへのメッセージをブロックするものではない。[file:24]
    - D: 不正サーバからの応答を「許可する」のではなく、**防ぐ（防止する）** のが目的なので、方向が逆。  
      説明文にも「blocks DHCP reply and advertisement messages that originate from unauthorized DHCP servers」とあり、D の内容とは反対である。[file:24]

***
### QUESTION 314 解説

- 問題文:
Which function does LDP provide in an MPLS topology?

- 問題文要約（日本語）:  
MPLS トポロジにおいて、LDP（Label Distribution Protocol）が果たす役割として正しいものを 1 つ選ぶ問題。

- 選択肢:
A. It enables a MPLS topology to connect multiple VPNs to P routers.  
B. It provides hop-by-hop forwarding in an MPLS topology for LSRs.  
C. It exchanges routes for MPLS VPNs across different VRFs.  
D. It provides a means for LSRs to exchange IP routes.  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - LDP は、**IGP が決めた IP 経路に従って、ラベルスイッチングルータ（LSR）同士がラベルを割り当てて配布するプロトコル**。[file:24]
    - こうして各 LSR が隣接ノードとラベル情報を交換することで、MPLS ネットワーク内で **ホップ・バイ・ホップのラベル転送パス（LSP）** が構築される。  
      したがって、「hop-by-hop forwarding in an MPLS topology for LSRs」を提供するという B の説明が正しい。

- 他の選択肢がダメな理由
    - A: 複数 VPN を P ルータに接続するのは主に **MP-BGP と VRF 設定** の役割であり、LDP は単にラベル配送を行うだけで VPN 間接続を直接制御しない。
    - C: MPLS VPN のルート交換は **MP-BGP** で行われる。LDP は VRF 間のルート交換機能を持たない。
    - D: LDP が交換するのは **IP ルートそのものではなく、それに対応するラベル**。IP ルートは IGP（OSPF、IS-IS など）によって配布される。[file:24]

***
### QUESTION 315 解説

- 問題文:
Refer to the exhibit.  
The administrator noticed that the connection was flapping between the two ISPs instead of switching to ISP2 when ISP1 failed. Which action resolves the issue?

- 問題文要約（日本語）:  
IP SLA で 8.8.8.8 への ICMP Echo を監視し、その結果を track 1 で監視している。

設定抜粋:  
ip sla 1  
 icmp-echo 8.8.8.8  
 threshold 1000  
 timeout 2000  
 frequency 5  
ip sla schedule 1 life forever start-time now  
track 1 ip sla 1  

ip route 0.0.0.0 0.0.0.0 203.0.113.1  name ISP1 track 1  
ip route 0.0.0.0 0.0.0.0 198.51.100.1 2 name ISP2  [file:66]

ISP1 を優先し、IP SLA (track 1) がダウンしたら ISP2 のデフォルトルートに切り替える設計だが、実際には ISP1 と ISP2 の間でデフォルトルートがフラップしてしまう。  
どの対処を行えば安定して ISP2 へ切り替わるか。

- 選択肢:
A. Include a valid source-interface keyword in the icmp-echo statement.  
B. Reference the track object 1 on the default route through ISP2 instead of ISP1.  
C. Modify the static routes to refer both to the next hop and the outgoing interface.  
D. Modify the threshold to match the administrative distance of the ISP2 route.  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 現状の `icmp-echo 8.8.8.8` では **送信元インターフェイスが指定されておらず、ルーティングテーブルに基づいて送信元が決まる**。[file:24]
    - ISP1 がダウンすると、デフォルトルートが ISP2（198.51.100.1）に切り替わり、その回線から 8.8.8.8 への ping が成功する。  
      すると IP SLA は「成功」と判断して track 1 を再び up にし、ISP1 へのデフォルトルートが復活 → しかし ISP1 は実際にはまだ不通 → 再び SLA 失敗 → ISP2 に切り替え…  
      というループが発生し、**ISP1/ISP2 間でフラップ**する。
    - 解決するには、IP SLA の ICMP Echo が **常に ISP1 側のインターフェイス（例: `GigabitEthernet0/0`）を送信元として使うよう指定**する必要がある。  
      具体的には:  
      `ip sla 1`  
      ` icmp-echo 8.8.8.8 source-interface GigabitEthernet0/0`  
      のように `source-interface` を追加することで、ISP1 経由で通信できなくなった時点で SLA が確実に down となり、その後は ISP2 経由に切り替わったまま安定する。[file:24]

- 他の選択肢がダメな理由
    - B: track 1 を ISP2 のルートに付け替えると、「SLA が up のとき ISP2、down のとき ISP1」という逆の動作になり、優先ルートが変わってしまうだけでフラップの根本原因は解決しない。
    - C: 次ホップと出口インターフェイスの両方を指定しても、IP SLA の送信元が ISP2 側に変わってしまう問題には関係しない。
    - D: `threshold` は RTT などの応答時間のしきい値であり、**AD（管理距離）とは無関係**。しきい値を AD に合わせても、SLA の送信元問題によるフラップは続く。

***
### QUESTION 316 解説

- 問題文:
Refer to the exhibit.  
An engineer must establish a point-to-point GRE VPN between R1 and the remote site. Which configuration accomplishes the task for the remote site?

- 問題文要約（日本語）:  
R1 側のトンネル情報には、次のように表示されている。

- Tunnel source 199.1.1.1  
- Tunnel destination 200.1.1.3  
- Tunnel protocol GRE/IP  
- Tunnel transport MTU 1476 bytes など [file:67]

R1 のトンネルインターフェイスの IP アドレスは 192.168.1.1/24（図の欠落部分）だと仮定される。  
この R1 と GRE P2P トンネルを張る「リモートサイト側」の Tunnel1 設定として正しいものを選ぶ問題。

- 選択肢（要旨）:
A. Tunnel1: source 199.1.1.1, destination 200.1.1.3, ip 192.168.1.3/24  
B. Tunnel1: source 200.1.1.3, destination 199.1.1.1, ip 192.168.1.1/24  
C. Tunnel1: source 200.1.1.3, destination 199.1.1.1, ip 192.168.1.3/24  
D. Tunnel1: source 199.1.1.1, destination 200.1.1.3, ip 192.168.1.1/24  

- 正解: **C**

- 解説（なぜ C が正しいか）
    - P2P GRE トンネルでは、両端で次の条件を満たす必要がある。
        1. 一方の `tunnel source` が、もう一方の `tunnel destination` になる（＝互いに向き合う）。  
        2. トンネルインターフェイスの IP は同一サブネット上で、**異なるホストアドレス** を割り当てる。[file:24]
    - R1 側が  
      - `tunnel source 199.1.1.1`  
      - `tunnel destination 200.1.1.3`  
      と設定されているので、リモート側は **逆向き** に  
      - `tunnel source 200.1.1.3`  
      - `tunnel destination 199.1.1.1`  
      とする必要がある。
    - また、R1 のトンネル IP を 192.168.1.1/24 とすると、リモート側は同じ /24 サブネット内で別アドレス、例: 192.168.1.3/24 を使うのが自然。  
      選択肢 C はこの条件を満たしているため正解。

- 他の選択肢がダメな理由
    - A / D: どちらも `tunnel source 199.1.1.1` / `destination 200.1.1.3` と、**R1 と同じ向き** に設定しており、エンド間で向き合う構成にならない。
    - B: source/destination の向きは正しいが、トンネル IP に 192.168.1.1/24 を使っており、R1 と同じアドレスとなってしまう。  
      P2P トンネル両端で同一 IP を設定することはできないため不正。

***
