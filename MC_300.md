***
### QUESTION 300 解説

- 問題文:
Refer to the exhibit.  
An OSPF neighbor relationship between R2 and R3 is stuck in EXCHANGE/EXSTART state. The neighbor is established between R1 and R2. The network engineer can ping from R2 to R3 and vice versa, but the neighbor is still down. Which action resolves the issue?

- 問題文要約（日本語）:  
R2 と R3 の間で OSPF ネイバーを張ろうとしているが、`show ip ospf neighbor` では  
- R2 側: neighbor 192.168.99.2 state **EXCHANGE/**  
- R3 側: neighbor 192.168.99.1 state **EXSTART/**  
の状態で止まっており、Full まで上がらない。[file:61]  
ICMP では相互に ping は成功するため L3 到達性はあるが、OSPF の隣接関係だけが確立しない。どの対応がこの問題を解決するかを問う。

- 選択肢:
A. Restore the Layer 2/Layer 3 connectivity issue in the ISP network.  
B. Match MTU on both router interfaces or ignore MTU.  
C. Administrative “shut then no shut” both router interfaces.  
D. Enable OSPF on the interface, which is required.  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - OSPF 隣接が **EXSTART / EXCHANGE** 状態で止まり、ping は通る、という典型的な原因は「両端インターフェイスの MTU 不一致」。  
      OSPF は DBD（Database Description）パケットのやり取りの際に MTU をチェックし、値が異なるとネイバーを Full まで上げずに EXSTART/EXCHANGE で固まる挙動を取る。[file:24]
    - したがって、この問題は  
      - インターフェイスの `ip mtu` / `mtu` 値を両端で一致させる  
      - もしくはインターフェイス側に `ip ospf mtu-ignore` を設定して、OSPF による MTU チェックを無効化する  
      のいずれかで解決できる。  
      選択肢 B の「Match MTU on both router interfaces or ignore MTU」は上記の対策をそのまま表しているため正解。

- 他の選択肢がダメな理由
    - A: ping が成功していることから、L2/L3 の基本的な到達性には問題がない。ISP 側の接続を「復旧」するのは根本原因ではない。
    - C: インターフェイスを shut/no shut すると一時的に状態はリセットされるが、MTU 不一致が残っていれば再び EXSTART/EXCHANGE で止まるため、恒久的な解決にはならない。
    - D: 出力には既に `show ip ospf neighbor` でネイバーが見えているので、そのインターフェイスで OSPF は有効化されている。  
      有効化されていないならそもそも INIT/2-WAY すら表示されないため、この回答は状況と矛盾する。

***
### QUESTION 301 解説

- 問題文:
Refer to the exhibit.  
R1 and R2 have been configured with the following settings for forming a BGP neighbor relationship over 172.16.1.0/24:

R1 (AS 100)  
router bgp 100  
 neighbor 172.16.1.2 remote-as 200  
 neighbor 172.16.1.1 password cisco123  

R2 (AS 200)  
router bgp 200  
 neighbor 172.16.1.1 remote-as 100  
 neighbor 172.16.1.1 password cisco  

The neighbor relationship is not coming up. Which configuration resolves the issue?

- 問題文要約（日本語）:  
R1（AS100）と R2（AS200）は eBGP でピアを張ろうとしている。  
物理リンクは 172.16.1.1（R1 e0/0）– 172.16.1.2（R2 e0/0）。[file:62]  
しかし、BGP 設定を見ると、R1 側の `neighbor` と `password` 行の IP が一致していなかったり、両者で設定しているパスワード文字列も異なっており、その結果 BGP ネイバーが up しない。  
どの設定に変更すれば、この隣接関係を確立できるか。

- 選択肢:
A. R1  
   router bgp 100  
    neighbor 172.16.1.2 password cisco  

B. R1  
   router bgp 100  
    neighbor 172.16.1.2 password MD5 cisco  
   R2  
   router bgp 200  
    neighbor 172.16.1.1 password MD5 cisco  

C. R2  
   router bgp 200  
    neighbor 172.16.1.1 password cisco123  

D. R1  
   router bgp 100  
    neighbor 172.16.1.2 password MD5 cisco123  
   R2  
   router bgp 200  
    neighbor 172.16.1.1 password MD5 cisco123  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 現状の問題点は 2 つ:
        1. R1 でパスワードを設定している neighbor IP が誤り  
           - R1 は実際には `neighbor 172.16.1.2 remote-as 200` で R2 とピアを張ろうとしているのに、パスワード行は `neighbor 172.16.1.1 password ...` となっており、**自分自身の IP を指定してしまっている**。  
        2. R1 と R2 のパスワード文字列が不一致  
           - R1: `cisco123`、R2: `cisco`。BGP MD5 認証では両端で **完全一致したパスワード** が必要。  
    - 選択肢 A は R1 側で  
      `neighbor 172.16.1.2 password cisco`  
      と設定し、  
      - neighbor IP を正しく相手 (172.16.1.2) に修正  
      - パスワード文字列を R2 側の `cisco` と一致させる  
      ため、この一行で両方の問題が解消される。[file:24]
    - BGP のパスワードは明示的に `MD5` と書かなくても、`neighbor … password` でデフォルトが MD5 認証になるため、A のような設定で十分。[file:24]

- 他の選択肢がダメな理由
    - B: パスワード文字列は R1・R2 とも `cisco` で一致しているが、**R1 の元々の誤った neighbor IP（172.16.1.1）を修正する記述がない**。  
      問題文の設定のままに `MD5` を追加しただけなら、依然として R1 は自分自身に対してパスワードを設定していることになり、ピアは上がらない。
    - C: R2 側のパスワードを `cisco123` に変えるだけで、**R1 側の neighbor IP の誤りが残ったまま**。  
      そのため、R1 が本当にピアを結びたい 172.16.1.2 にはパスワードが設定されず、認証ミスマッチでネイバーは確立されない。
    - D: 両端でパスワード文字列は一致するが、B と同様に「R1 の neighbor IP の誤りを直す記述がない」点は変わらない。  
      `MD5` を明示しても、対象 neighbor の IP が正しくなければ意味がない。

***
### QUESTION 301 解説

- 問題文:
Refer to the exhibit.  
R1 and R2 have been configured with the following settings for forming a BGP neighbor relationship over 172.16.1.0/24:

R1 (AS 100)  
router bgp 100  
 neighbor 172.16.1.2 remote-as 200  
 neighbor 172.16.1.1 password cisco123  

R2 (AS 200)  
router bgp 200  
 neighbor 172.16.1.1 remote-as 100  
 neighbor 172.16.1.1 password cisco  

The neighbor relationship is not coming up. Which configuration resolves the issue?

- 問題文要約（日本語）:  
R1（AS100）と R2（AS200）は eBGP でピアを張ろうとしている。  
物理リンクは 172.16.1.1（R1 e0/0）– 172.16.1.2（R2 e0/0）。[file:62]  
しかし、BGP 設定を見ると、R1 側の `neighbor` と `password` 行の IP が一致していなかったり、両者で設定しているパスワード文字列も異なっており、その結果 BGP ネイバーが up しない。  
どの設定に変更すれば、この隣接関係を確立できるか。

- 選択肢:
A. R1  
   router bgp 100  
    neighbor 172.16.1.2 password cisco  

B. R1  
   router bgp 100  
    neighbor 172.16.1.2 password MD5 cisco  
   R2  
   router bgp 200  
    neighbor 172.16.1.1 password MD5 cisco  

C. R2  
   router bgp 200  
    neighbor 172.16.1.1 password cisco123  

D. R1  
   router bgp 100  
    neighbor 172.16.1.2 password MD5 cisco123  
   R2  
   router bgp 200  
    neighbor 172.16.1.1 password MD5 cisco123  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 現状の問題点は 2 つ:
        1. R1 でパスワードを設定している neighbor IP が誤り  
           - R1 は実際には `neighbor 172.16.1.2 remote-as 200` で R2 とピアを張ろうとしているのに、パスワード行は `neighbor 172.16.1.1 password ...` となっており、**自分自身の IP を指定してしまっている**。  
        2. R1 と R2 のパスワード文字列が不一致  
           - R1: `cisco123`、R2: `cisco`。BGP MD5 認証では両端で **完全一致したパスワード** が必要。  
    - 選択肢 A は R1 側で  
      `neighbor 172.16.1.2 password cisco`  
      と設定し、  
      - neighbor IP を正しく相手 (172.16.1.2) に修正  
      - パスワード文字列を R2 側の `cisco` と一致させる  
      ため、この一行で両方の問題が解消される。[file:24]
    - BGP のパスワードは明示的に `MD5` と書かなくても、`neighbor … password` でデフォルトが MD5 認証になるため、A のような設定で十分。[file:24]

- 他の選択肢がダメな理由
    - B: パスワード文字列は R1・R2 とも `cisco` で一致しているが、**R1 の元々の誤った neighbor IP（172.16.1.1）を修正する記述がない**。  
      問題文の設定のままに `MD5` を追加しただけなら、依然として R1 は自分自身に対してパスワードを設定していることになり、ピアは上がらない。
    - C: R2 側のパスワードを `cisco123` に変えるだけで、**R1 側の neighbor IP の誤りが残ったまま**。  
      そのため、R1 が本当にピアを結びたい 172.16.1.2 にはパスワードが設定されず、認証ミスマッチでネイバーは確立されない。
    - D: 両端でパスワード文字列は一致するが、B と同様に「R1 の neighbor IP の誤りを直す記述がない」点は変わらない。  
      `MD5` を明示しても、対象 neighbor の IP が正しくなければ意味がない。

***
### QUESTION XXX 解説

- 問題文:
Refer to the exhibit.  
The diagram shows that R1 has the OSPF router ID 10.255.255.1, and a message similar to the following is seen:  
`%OSPF-4-FLOOD_WAR: ... type-5 adv-rtr 10.255.255.1 ...`  
After configuring OSPF on R1, some external destinations in the network became unreachable. Which action resolves the issue?

- 問題文要約（日本語）:  
R1 に OSPF を設定したところ、ネットワーク上のいくつかの外部宛先が到達不能になった。  
ログには「`%OSPF-4-FLOOD_WAR` type-5 adv-rtr 10.255.255.1」といったメッセージが出ており、R1 の OSPF Router ID は 10.255.255.1 になっている。  
これは OSPF の外部 LSA（Type 5）に関するフラッディング警告であり、どの対処を行えば問題を解消できるかを問う問題。[file:24]

- 選択肢:
A. Change the R1 router ID from 10.255.255.1 to a unique value and clear the process.  
B. Increase the SPF delay interval on R1 to synchronize routes.  
C. Disconnect the router with the OSPF router ID 0.0.0.0 from the network.  
D. Clear the OSPF process on R1 to flush stale LSAs sent by other routers.  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - `%OSPF-4-FLOOD_WAR` は、「ルータが**自分自身が生成した LSA を、別ルータ経由で再度受け取り続けている**」ときに出る警告で、  
      よくある原因は「同じ OSPF Router ID を持つルータが複数存在すること」。特に Type-5 LSA（外部経路）については、**異なるエリアで同じ Router ID を使って ASBR が存在**すると、このような状況が起こる。[file:24]
    - 今回、ログの `adv-rtr 10.255.255.1` は「LSA を広告したルータ ID」が 10.255.255.1 であることを示しており、R1 も Router ID 10.255.255.1 を使っている。  
      つまり **他の ASBR も同じ Router ID を使っている** 可能性が高く、外部 LSA が衝突して一部の外部宛先が不達になっている。
    - この問題を根本的に解決するには、**R1 の Router ID をネットワーク内で一意な値に変更し、その後 `clear ip ospf process` などで OSPF プロセスを再起動して新しい LSAs を再配布させる**必要がある。  
      選択肢 A はこの手順をそのまま表しているため正解。

- 他の選択肢がダメな理由
    - B: SPF delay（SPF 計算のディレイタイマ）を増やしても、**Router ID の重複という根本原因は解決しない**。LSA のループ／フラッディング自体は続き、外部経路の不整合も残る。
    - C: Router ID 0.0.0.0 は通常一時的な状態で、ここでの問題は明示的に 10.255.255.1 の Type-5 LSA が繰り返し見えていること。  
      0.0.0.0 のルータを切断しても、この FLOOD_WAR の原因には関係しない。
    - D: OSPF プロセスをクリアすると、一時的には LSDB がリセットされて状況が改善したように見えるかもしれないが、**Router ID が重複したままなら、再び同じ問題が発生する**。  
      したがって、単純なプロセスクリアだけでは恒久的な解決にはならない。

***
### QUESTION 302 解説

- 問題文:
Refer to the exhibit.  
The diagram shows that R1 has the OSPF router ID 10.255.255.1, and a message similar to the following is seen:  
`%OSPF-4-FLOOD_WAR: ... type-5 adv-rtr 10.255.255.1 ...`  
After configuring OSPF on R1, some external destinations in the network became unreachable. Which action resolves the issue?

- 問題文要約（日本語）:  
R1 に OSPF を設定したところ、ネットワーク上のいくつかの外部宛先が到達不能になった。  
ログには「`%OSPF-4-FLOOD_WAR` type-5 adv-rtr 10.255.255.1」といったメッセージが出ており、R1 の OSPF Router ID は 10.255.255.1 になっている。  
これは OSPF の外部 LSA（Type 5）に関するフラッディング警告であり、どの対処を行えば問題を解消できるかを問う問題。[file:24]

- 選択肢:
A. Change the R1 router ID from 10.255.255.1 to a unique value and clear the process.  
B. Increase the SPF delay interval on R1 to synchronize routes.  
C. Disconnect the router with the OSPF router ID 0.0.0.0 from the network.  
D. Clear the OSPF process on R1 to flush stale LSAs sent by other routers.  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - `%OSPF-4-FLOOD_WAR` は、「ルータが**自分自身が生成した LSA を、別ルータ経由で再度受け取り続けている**」ときに出る警告で、  
      よくある原因は「同じ OSPF Router ID を持つルータが複数存在すること」。特に Type-5 LSA（外部経路）については、**異なるエリアで同じ Router ID を使って ASBR が存在**すると、このような状況が起こる。[file:24]
    - 今回、ログの `adv-rtr 10.255.255.1` は「LSA を広告したルータ ID」が 10.255.255.1 であることを示しており、R1 も Router ID 10.255.255.1 を使っている。  
      つまり **他の ASBR も同じ Router ID を使っている** 可能性が高く、外部 LSA が衝突して一部の外部宛先が不達になっている。
    - この問題を根本的に解決するには、**R1 の Router ID をネットワーク内で一意な値に変更し、その後 `clear ip ospf process` などで OSPF プロセスを再起動して新しい LSAs を再配布させる**必要がある。  
      選択肢 A はこの手順をそのまま表しているため正解。

- 他の選択肢がダメな理由
    - B: SPF delay（SPF 計算のディレイタイマ）を増やしても、**Router ID の重複という根本原因は解決しない**。LSA のループ／フラッディング自体は続き、外部経路の不整合も残る。
    - C: Router ID 0.0.0.0 は通常一時的な状態で、ここでの問題は明示的に 10.255.255.1 の Type-5 LSA が繰り返し見えていること。  
      0.0.0.0 のルータを切断しても、この FLOOD_WAR の原因には関係しない。
    - D: OSPF プロセスをクリアすると、一時的には LSDB がリセットされて状況が改善したように見えるかもしれないが、**Router ID が重複したままなら、再び同じ問題が発生する**。  
      したがって、単純なプロセスクリアだけでは恒久的な解決にはならない。

***
### QUESTION 303 解説

- 問題文:
Which two solutions are used to overcome a flapping link that causes a frequent label binding exchange between MPLS routers? (Choose two)

- 問題文要約（日本語）:  
MPLS ネットワークでリンクがフラップすると、LDP セッションが頻繁に張り直され、そのたびにラベルバインディングの再交換が発生して不安定になる。  
この「リンクの一時的なダウン・フラップ」に対して、LDP セッション／ラベル情報を保護し、頻繁な再ネゴシエーションを防ぐ手段を 2 つ選ぶ問題。[file:24]

- 選択肢:
A. create targeted hellos to protect the session  
B. increase a hold-timer to protect the session  
C. increase input queue on links to protect the session  
D. create link dampening on links to protect the session  
E. increase a session delay to protect the session.  

- 正解: **A, B**

- 解説（なぜ A・B が正しいか）
    - **A. create targeted hellos to protect the session**  
      - MPLS LDP Session Protection 機能では、**LDP Targeted Hellos** を用いて、物理リンクがダウンしても LDP セッションを別インターフェイス経由で維持する。  
      - これにより、リンクフラップがあっても LDP セッションやラベルバインディングを再確立する必要が減り、頻繁なラベル交換を防げる。[file:24]
    - **B. increase a hold-timer to protect the session**  
      - `mpls ldp session holdtime` を長めに設定すると、**一定時間 LDP メッセージが途絶えてもセッションをすぐには落とさない**。  
      - 瞬間的なリンクフラップや短時間のパケットロスでセッションがダウンしなくなり、ラベル再配布の頻度を抑えられる。[file:24]

- 他の選択肢がダメな理由
    - C: 入力キューを増やすと輻輳時のドロップは多少減るかもしれないが、**リンクの物理的なフラップによるセッション切断**には直接効かない。LDP セッション保護の手段としては不適切。
    - D: 「リンクダンピング（dampening）」は一般的に BGP ルートフラップ抑制などで使う概念であり、ここでは **LDP セッションそのものを保護するメカニズムではない**。  
    - E: セッションディレイは「セッションの確立を遅らせる」機能であり、問題文のような **既存セッションの頻繁な切断・再確立** を防ぐものではない。[file:24]

***
### QUESTION 304 解説

- 問題文:
Refer to the exhibit.  
A command `aaa authentication login default local` is entered.  
A network engineer must configure a login authentication method at line console 0 on a Cisco ISR router. During configuration, the engineer got a call and the router console session timed out. When the call was completed, the engineer pressed Enter on the console. The engineer was asked for a username to log in to the router even though no username and password were previously configured on the router. What action resolves the issue?

- 問題文要約（日本語）:  
エンジニアが ISR ルータのコンソールで作業中に、`aaa authentication login default local` を入力した直後に電話対応で離席し、その間にコンソールがタイムアウトした。  
戻って Enter を押すと、コンソールログインに「username」が要求されるが、ルータには **まだローカルユーザ名／パスワードを一切設定していない**。  
どうすれば再度ルータにアクセスして設定をやり直せるか、という問題。

- 選択肢:
A. Telnet to the router to log in  
B. Power cycle to reload the router and reconfigure it  
C. Configure the user and password in the RADIUS server  
D. Use CTRL+C to bypass authentication in the router.  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - `aaa authentication login default local` を入れると、「すべてのログイン（コンソール・VTY など）は **ローカルユーザデータベース** で認証する」という意味になる。[file:24]
    - ところが、この時点で `username xxx secret yyy` などのローカルユーザがまだ一切設定されていないため、**認証に使えるアカウントが存在しない**。  
      結果として、コンソールにユーザ名を入れても認証できず、完全にロックアウトされる。
    - さらに、コンソールでの設定変更は running-config 上には反映されているが、まだ `write` していない前提は問題文から明確ではないものの、  
      多くの試験文脈では「保存前なら電源再投入すれば元の設定に戻る」ことを意図している。[file:24]
    - よって、**電源再投入（Power cycle）でルータを再起動し、startup-config（AAA 設定を入れる前の状態）からやり直す**のが現実的な解決策となるため、B が正解。

- 他の選択肢がダメな理由
    - A: Telnet も `aaa authentication login default local` の対象であり、**コンソール同様にローカルユーザ認証を要求**される。  
      ローカルユーザが存在しない限り、Telnet でもログインできないため解決にならない。
    - C: すでに `default local` としているため、認証は RADIUS ではなく「ルータ上のローカルデータベース」を参照する。  
      RADIUS サーバにユーザを作っても、AAA メソッドリストを `group radius` に変更しない限り使われない。
    - D: Cisco IOS のログインプロンプトで **Ctrl+C で認証をバイパスする方法は存在しない**。  
      Ctrl+C はコンフィグモード内でのコマンド中断などに使われるキーであり、ログイン要求そのものをスキップすることはできない。

***
### QUESTION 305 解説

- 問題文:
An engineer creates a default static route on a router with a next hop of 10.1.1.1. On inspection, the engineer finds the router has two VRFs, Red and Blue. The next hop is valid for both VRFs and exists in each assigned VRF. Which configuration achieves connectivity?

- 問題文要約（日本語）:  
ルータには VRF Red と VRF Blue の 2 つがあり、どちらの VRF からも next-hop 10.1.1.1 に到達できる（各 VRF 内に 10.1.1.1 への経路がある）。  
それぞれの VRF からインターネットなど外部へのデフォルトルートを、next-hop 10.1.1.1 で設定したい。  
どの `ip route` 設定を入れれば、Red / Blue 両方の VRF でデフォルトルートを正しく持てるか、という問題。[file:24]

- 選択肢:
A.  
   ip route vrf Red 0.0.0.0 255.255.255.255 10.1.1.1  
   ip route vrf Blue 0.0.0.0 255.255.255.255 10.1.1.1  

B.  
   ip route 0.0.0.0 0.0.0.0 10.1.1.1  

C.  
   ip route vrf Red 0.0.0.0 0.0.0.0 10.1.1.1  
   ip route vrf Blue 0.0.0.0 0.0.0.0 10.1.1.1  

D.  
   ip route vrf Blue 0.0.0.0 255.255.255.255 10.1.1.1  
   ip route vrf Red 0.0.0.0 255.255.255.255 10.1.1.1  

- 正解: **C**

- 解説（なぜ C が正しいか）
    - VRF ごとに別々のルーティングテーブルがあり、**各 VRF 内でデフォルトルートを定義する必要がある**。  
    - デフォルトルートはプレフィックス `/0` なので、Cisco IOS では  
      `ip route vrf <VRF名> 0.0.0.0 0.0.0.0 <next-hop>`  
      の形式を使う。[file:24]
    - 選択肢 C は、Red・Blue の両方に対して  
      - `ip route vrf Red  0.0.0.0 0.0.0.0 10.1.1.1`  
      - `ip route vrf Blue 0.0.0.0 0.0.0.0 10.1.1.1`  
      を設定しており、**両 VRF のルーティングテーブルにデフォルトルートを正しく追加**しているため、要件を満たす。

- 他の選択肢がダメな理由
    - A / D: マスクに `255.255.255.255` を指定しており、これは「/32 のホストルート」であってデフォルトルートではない。  
      宛先 0.0.0.0/32 へのみ有効で、通常の「どこ宛でも 10.1.1.1 へ」というデフォルト動作にならない。
    - B: VRF を指定しない `ip route 0.0.0.0 0.0.0.0 10.1.1.1` は **グローバルルーティングテーブル** のデフォルトルートを作るだけであり、VRF Red/Blue のテーブルには反映されない。  
      そのため、Red / Blue VRF 内のトラフィックはこのデフォルトルートを利用できない。

***
### QUESTION 306 解説

- 問題文:
What are two characteristics of IPv6 Source Guard? (Choose two)

- 問題文要約（日本語）:  
IPv6 Source Guard 機能の性質について正しいものを 2 つ選ぶ問題。  
Source Guard は、IPv6 Snooping などで作られた「バインディングテーブル」に基づいて、ポート上の送信元 IPv6 アドレスを検証し、不正なトラフィックを遮断する Layer2 セキュリティ機能。[file:24]

- 選択肢:
A. requires the user to configure a static binding  
B. recovers missing binding table entries  
C. used in service provider deployments to protect DDoS attacks  
D. requires that validate prefix be enabled  
E. requires IPv6 snooping on Layer 2 access or trunk ports  

- 正解: **C, E**

- 解説（なぜ C・E が正しいか）
    - **E. requires IPv6 snooping on Layer 2 access or trunk ports**  
      - IPv6 Source Guard は、**IPv6 Snooping / ND inspection / DHCPv6 gleaning** などで作成される「IPv6 ファーストホップ・セキュリティのバインディングテーブル」に依存して動作する。[file:24]  
      - このテーブルは、MAC・VLAN・ポート・IPv6 アドレス・リンクローカルアドレスなどを L2 ポート上で学習するため、**L2 アクセス／トランクポートで IPv6 snooping 系機能が有効であることが前提条件**となる。
    - **C. used in service provider deployments to protect DDoS attacks**  
      - IPv6 Source Guard 自体は「既知ホストのみ許可」という性質があり、エンタープライズ側のアクセスエッジ向けが主用途だが、  
        その内部機能である **IPv6 Prefix Guard** を用いて、サービスプロバイダ環境で「許可されたプレフィックス以外からの不正トラフィック（DDoS など）」を遮断する使い方ができる。[file:24]  
      - 問題集の想定解では、Source Guard 機能セットの一部として Prefix Guard を含めた全体を見ており、「SP で DDoS 攻撃から保護するのに用いられる」という説明に合致するため C を正解としている。

- 他の選択肢がダメな理由
    - **A: requires the user to configure a static binding**  
      - Source Guard は「既に存在するバインディングテーブルを参照してフィルタする」機能であって、静的バインディングの設定を必須とはしていない。  
        バインディングテーブルは通常 ND inspection や IPv6 Snooping、DHCP gleaning などで自動生成される。[file:24]
    - **B: recovers missing binding table entries**  
      - 欠落しているエントリを ND や DHCPv6 から再学習するのは、**Binding Table Recovery** 機能の役割であり、Source Guard 単体の動作ではない。  
        Source Guard はあくまで「テーブルにない送信元アドレスのトラフィックを拒否する」側。[file:24]
    - **D: requires that validate prefix be enabled**  
      - `validate prefix` は IPv6 Prefix Guard 機能のためのオプションであり、Source Guard を基本的に動かすだけなら必須ではない。  
        Prefix Guard を使わないなら、このコマンドは不要なので「必須」という表現は誤り。

***
### QUESTION 307 解説

- 問題文:
An engineer is creating a policy that overrides normal routing behavior. If the route to a destination of 10.100.100.0/24 is withdrawn from the routing table, the policy must direct traffic to a next-hop of 10.1.1.1. If the route is present in the routing table, then normal forwarding must occur. Which configuration meets the requirements?

- 問題文要約（日本語）:  
宛先 10.100.100.0/24 へのパケットに対して、  
- ルーティングテーブルに 10.100.100.0/24 の経路が **存在する間は通常ルーティング** を行う。  
- その経路が **消えたときだけ**、PBR で next-hop 10.1.1.1 に転送させたい。  
この条件付き PBR を実現する `route-map` / ACL の組み合わせを選ぶ問題。

- 選択肢（要旨）:
A. ACL 100: `permit ip any any`、route-map で `set ip next-hop recursive 10.1.1.1`  
B. ACL 100: 宛先 10.100.100.0/24、route-map で `set ip next-hop recursive 10.1.1.1`  
C. ACL 100: 宛先 10.100.100.0/24、route-map で `set ip next-hop 10.1.1.1`  
D. ACL 100: 宛先 10.100.100.0/24、route-map で `set ip default next-hop 10.1.1.1`  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - PBR の `set` 動作:
        - `set ip next-hop <IP>`  
          → 該当パケットに対し **常に** 指定 next-hop を使う（ルーティングテーブルに経路があっても上書き）。  
        - `set ip default next-hop <IP>`  
          → **宛先へのルートがルーティングテーブルに無いときだけ** 指定 next-hop を使い、  
            経路が存在するときは通常のルーティングテーブルに従う。[file:24]
    - 問題の要件は「10.100.100.0/24 のルートがある間は通常ルーティング／無くなったら 10.1.1.1 を使う」なので、  
      - 該当宛先をマッチさせる ACL（10.100.100.0/24）  
      - ルートが無いときだけ next-hop を指定する `set ip default next-hop 10.1.1.1`  
      の組み合わせが必要。  
      これを満たすのが選択肢 D であり、要件通りの「条件付き PBR」を実現できる。

- 他の選択肢がダメな理由
    - A: ACL が `any any` なので、**すべてのトラフィック** に対して常に `set ip next-hop recursive 10.1.1.1` を適用してしまい、  
      - 宛先が 10.100.100.0/24 以外も含めて通常ルーティングが上書きされる。  
      - さらに `set ip next-hop` 系はルートが存在しても常に適用されるため、要件と逆。
    - B: 宛先マッチは正しいが、`set ip next-hop recursive` を使っており、**経路の有無に関係なく** 常に 10.1.1.1 を next-hop として使う。  
      経路が存在するときも PBR が優先されるため、通常ルーティングにならない。
    - C: B と同様に `set ip next-hop` を使っているため、ルートがある場合も必ず 10.1.1.1 へ転送してしまう。  
      「ルートがあるときは通常転送」という条件を満たさない。

***
### QUESTION 308 解説

- 問題文:
Refer to the exhibit.  
A diagram shows that R1 has three paths to reach network 5.5.5.0/24. EIGRP is used as the routing protocol, but only two EIGRP routes for 5.5.5.0/24 are installed in the routing table. An engineer finds that traffic destined for 5.5.5.0/24 does not use all available paths. Which action resolves the issue?

- 問題文要約（日本語）:  
R1 から 5.5.5.0/24 へ到達する 3 本の EIGRP パスが存在するが、`show ip route` ではそのうち 2 本しかルーティングテーブルに載っておらず、  
実際のトラフィックも 2 本のパスでしかロードバランシングされていない。  
3 本すべてを EIGRP の等価 / 不等価ロードバランシングの対象に含めたい。どの設定変更が必要か、という問題。[file:24]

- 選択肢:
A. Decrease the variance value in EIGRP.  
B. Stop advertising 192.168.13.0/24 in EIGRP.  
C. Remove the adjacency of R3 from EIGRP.  
D. Increase the variance value in EIGRP.  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - EIGRP では、**FD（Feasible Distance）が最小のパスを SFD とし、他の候補パスのメトリックが「SFD × variance 以下」なら、  
      そのパスもルーティングテーブルに載せてロードバランシングに参加**させる。[file:24]
    - 現状、3 本のうち 2 本だけが「SFD × 現在の variance」以内に収まっており、3 本目のパスはメトリックが高すぎて条件を満たしていないため除外されている。  
      → **variance を大きくすれば、メトリックの高い 3 本目も許容範囲に入り、ルーティングテーブルにインストールされる**。
    - したがって、「Increase the variance value in EIGRP.」である D を選ぶと、3 本すべてのパスを EIGRP が利用できるようになる。

- 他の選択肢がダメな理由
    - A: variance を減らすと、「許容されるメトリック差」が小さくなり、**さらに多くのパスが除外される** 可能性がある。3 本すべてを使いたい要件とは逆。
    - B: 192.168.13.0/24 の広告を止めると、そもそもその経路を通るパスが消えるため、  
      「すべてのパスを利用したい」という目的に反して経路数を減らしてしまうだけ。
    - C: R3 の隣接を削除すると、R3 経由のパスが完全に消えるため、パスを増やすどころか減らしてしまい、問題の趣旨と逆。

***
### QUESTION 309 解説

- 問題文:
Refer to the exhibit.  
A network administrator has developed a Python script on the local Linux machine and is trying to transfer it to the router using SCP, but the transfer fails with:

Password:  
Administratively disabled.  
Connection to 198.51.100.64 closed by remote host. [file:63]

Which action resolves this issue?

- 問題文要約（日本語）:  
Linux 端末から `scp script.py admin@198.51.100.64:script.py` を実行すると、パスワード入力後に「Administratively disabled.」と表示されて接続が切断される。[file:63]  
SCP で IOS ルータにファイルをコピーしたいが、どの設定を行えばよいかを問う問題。

- 選択肢:
A. The SSH service must be enabled with the crypto key generate rsa command.  
B. The SCP service must be enabled with the ip scp server enable command.  
C. The Python interpreter must first be enabled with the guestshell enable command.  
D. The SSH access must be allowed on the VTY lines using the transport input ssh command.  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - メッセージ「Administratively disabled」は、**SSH 接続自体はできるが、SCP サーバ機能が無効化されている** IOS デバイスに対して SCP を試みたときに表示される典型的な出力。[file:24]
    - Cisco IOS で SCP サーバ機能を有効にするには、グローバルコンフィグで  
      `ip scp server enable`  
      を設定する必要がある。  
      これにより、既存の SSH サービス上で SCP 転送が許可され、Linux クライアントからの `scp` が成功するようになる。

- 他の選択肢がダメな理由
    - A: もし RSA キーが生成されておらず SSH 自体が無効なら、**そもそもパスワードプロンプトすら出ず** 接続ができない。  
      今回はパスワード入力後に「Administratively disabled」と出ているため、SSH はすでに有効。
    - C: guestshell（Python インタプリタ）はルータ上でスクリプトを実行するための機能であり、**SCP 転送の成否には関係しない**。
    - D: `transport input ssh` が未設定で SSH が禁止されていれば、A と同様に接続自体に失敗し、「Administratively disabled」ではなく接続拒否やタイムアウトになる。  
      したがって、本ケースの症状とは一致しない。

### QUESTION 310 解説

- 問題文:
Refer to the exhibit.  
A junior engineer updated a branch router configuration. Immediately after the change, the help desk reports that branch users cannot reach any network destinations. Which configuration restores service and continues to block 10.1.1.100/32?

- 問題文要約（日本語）:  
BRANCH-RTR では EIGRP 100 を動かしており、FastEthernet0/0 に対して受信ルートをフィルタするための distribute-list が設定されている。

現在の設定（抜粋）:  
router eigrp 100  
 network 10.4.31.0 0.0.0.7  
 network 10.100.100.1 0.0.0.0  
 distribute-list route-map FILTER-IN in FastEthernet0/0  
 eigrp router-id 10.100.100.1  

ip prefix-list 102 seq 10 permit 10.1.1.100/32  

route-map FILTER-IN deny 10  
 match ip address prefix-list 102  [file:64]

この変更後、ブランチからどの宛先にも到達できなくなった。10.1.1.100/32 だけをブロックしつつ、他の経路は受け入れて通信を復旧させるには、どの設定を追加／修正すべきか。

- 選択肢:
A. route-map FILTER-IN deny 5  
B. ip prefix-list 102 seq 15 permit 0.0.0.0/32 le 32  
C. ip prefix-list 102 seq 5 permit 0.0.0.0/32 le 32  
D. route-map FILTER-IN permit 20  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - 現状の route-map `FILTER-IN` は、シーケンス 10 で  
      `deny` + `match ip address prefix-list 102`  
      のみが定義されている。[file:64]
    - route-map には **暗黙の deny** があり、どのシーケンスにもマッチしなかったルートはすべて拒否される。  
      つまり今の設定だと  
      - 10.1.1.100/32 にマッチ → seq 10 の `deny` で拒否（これは狙い通り）  
      - それ以外のプレフィックス → どのシーケンスにもマッチせず暗黙の `deny` → **全部拒否される**  
      ため、ブランチからどこにも行けなくなっている。
    - 解決には、「10.1.1.100/32 以外はすべて許可」するための **permit 行** を route-map の後ろに追加する必要がある。  
      選択肢 D の `route-map FILTER-IN permit 20` を追加すれば、  
      - seq 10: 10.1.1.100/32 を `deny`  
      - seq 20: その他のプレフィックスは条件無しで `permit`  
      となり、10.1.1.100 だけをブロックしつつ、その他の EIGRP ルートは通常通り受け入れられる。

- 他の選択肢がダメな理由
    - A: `deny 5` を追加しても、結局 seq 5 と seq 10 のどちらかで `deny` されるだけであり、「permit するシーケンス」が存在しない点は変わらない。
    - B / C: prefix-list 102 に「0.0.0.0/0〜/32 を permit」するエントリを足すと、  
      そのプレフィックスも seq 10 の `deny` にマッチしてしまい、**むしろ全プレフィックスが deny 対象になりかねない**。  
      いずれにせよ route-map に `permit` シーケンスが無い限り、暗黙の deny によりルートは受け入れられない。

***
### QUESTION 311 解説

- 問題文:
Refer to the exhibit.  
An engineer cannot copy the IOS.bin image from an FTP server to the switch. Which action resolves the issue?

- 問題文要約（日本語）:  
FTP サーバ (10.0.0.2/24) に IOS.bin ファイルがあり、ユーザ名／パスワードは `cisco / cisco` と表示されている。[file:65]  
スイッチ側では VLAN2 に 10.0.0.1/24 を設定し、`ip ftp source-interface vlan 2` も設定済み。  
PC から 10.0.0.1 への ping は成功しているので、L3 接続は問題ない。  
しかしスイッチで  
`copy ftp://cisco:cisco@10.0.0.2/IOS.bin flash:/`  
を実行すると、  
`%Error opening ftp://cisco:cisco@10.0.0.2/IOS.bin (No such file or directory)`  
と表示されて失敗する。[file:65]  
どの対応を行えば IOS.bin を正常にコピーできるか。

- 選択肢:
A. Allow file permissions to download the file from the FTP server.  
B. Add the IOS.bin file, which does not exist on FTP server.  
C. Make memory space on the switch flash or USB drive to download the file.  
D. Use the copy flash:/ ftp://cisco@10.0.0.2/IOS.bin command.  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 図中の FTP サーバのメモには「File to download: IOS.bin」と記載されており、**IOS.bin ファイル自体はサーバ上に既に存在する前提** になっている。[file:65]
    - にもかかわらず `No such file or directory` が表示されるのは、  
      - FTP サーバ側で、そのファイル（またはディレクトリ）への **読み取り権限が付与されていない**  
      - FTP のホームディレクトリ / パス設定の関係で、匿名または `cisco` ユーザからそのファイルにアクセスできない  
      といった **ファイル権限・アクセス権の問題** の可能性が高い。
    - したがって、FTP サーバ側で `IOS.bin` に対し「ダウンロード（読み取り）を許可するファイル権限」を設定し直せば、同じコマンドでコピーが成功する。  
      これが選択肢 A の内容であり、最も妥当な解決策。

- 他の選択肢がダメな理由
    - B: 図の説明から、`IOS.bin` は「ダウンロードすべきファイル」として既に存在している前提なので、「ファイルを追加する」が正解ではない。  
      もし存在しなければ、その旨の記載があるはず。
    - C: フラッシュ容量不足であれば、エラーは「No space left on device」など容量関連のメッセージになり、「No such file or directory」にはならない。
    - D: `copy flash:/ ftp://...` は「スイッチから FTP サーバへアップロード」するコマンドであり、問題は「サーバからスイッチへダウンロード」なので方向が逆。  
      また現在のエラーはダウンロード時のファイル参照問題であり、コピー方向を変えても根本原因は解決しない。

***
### QUESTION 312 解説

- 問題文:
What is an advantage of implementing BFD?

- 問題文要約（日本語）:  
BFD（Bidirectional Forwarding Detection）をネットワークに導入したときのメリットとして、正しいものを 1 つ選ぶ問題。

- 選択肢:
A. BFD provides faster updates for any flapping route.  
B. BFD provides millisecond failure detection  
C. BFD is deployed without the need to run any routing protocol  
D. BFD provides better capabilities to maintain the routing table  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - BFD は、2 台のルータ間で**非常に短い間隔（ミリ秒オーダー）で Hello メッセージをやり取りしてリンク障害を検出する軽量プロトコル**。[file:24]
    - 通常の OSPF / EIGRP / BGP などのデッドタイマよりもはるかに短い時間で障害検出ができ、**ミリ秒単位の高速フェイルオーバー**を実現するのが最大の利点なので、B が正解。

- 他の選択肢がダメな理由
    - A: BFD 自体は「ルートアップデート」を送るわけではなく、**リンク障害の検出**だけを担当する。ルーティング更新は各プロトコル（OSPF, BGP など）が行う。
    - C: BFD はしばしば OSPF / EIGRP / BGP など「既存のルーティングプロトコルと連携して」使われるものであり、それら無しに単独で運用するのが目的ではない。
    - D: ルーティングテーブルの管理そのものを強化する機能ではなく、あくまで障害検出を速くして、**上位プロトコルが早く経路切り替えできるよう補助する**だけ。

***
### QUESTION 313 解説

- 問題文:
What is the purpose of the DHCPv6 Guard?

- 問題文要約（日本語）:  
DHCPv6 Guard 機能の役割について正しい説明を 1 つ選ぶ問題。  
DHCPv6 Guard は、スイッチのポートで「どのポートが正規の DHCPv6 サーバ／リレーからの応答を流せるか」を制御し、不正（rogue）サーバからの配布を防ぐ仕組み。[file:24]

- 選択肢:
A. It filters messages between a DHCPv6 server and a DHCPv6 client (or relay agent).  
B. It shows that clients of a DHCPv5 server are affected.  
C. It block DHCPv6 messages from relay agents to a DHCPv6 server.  
D. It allows DHCPv6 reply and advertisements from (rouge) DHCPv6 servers.  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - DHCPv6 Guard は、**スイッチが「トラストポート」と「アンテラストポート」を区別し、不正なポートから送られてくる DHCPv6 サーバメッセージ（Reply, Advertise など）をブロックする機能**。[file:24]
    - つまり「DHCPv6 サーバとクライアント（またはリレー）の間を通過する DHCPv6 メッセージをフィルタし、許可されていない方向の応答を遮断する」機能なので、  
      選択肢 A の説明が最も近い。

- 他の選択肢がダメな理由
    - B: DHCPv5 というバージョンは存在せず、DHCPv6 Guard の動作とも無関係。
    - C: DHCPv6 Guard は主に **サーバからクライアントへの Reply / Advertise などをブロック**する機能であり、リレーからサーバへのメッセージをブロックするものではない。[file:24]
    - D: 不正サーバからの応答を「許可する」のではなく、**防ぐ（防止する）** のが目的なので、方向が逆。  
      説明文にも「blocks DHCP reply and advertisement messages that originate from unauthorized DHCP servers」とあり、D の内容とは反対である。[file:24]

***
### QUESTION 314 解説

- 問題文:
Which function does LDP provide in an MPLS topology?

- 問題文要約（日本語）:  
MPLS トポロジにおいて、LDP（Label Distribution Protocol）が果たす役割として正しいものを 1 つ選ぶ問題。

- 選択肢:
A. It enables a MPLS topology to connect multiple VPNs to P routers.  
B. It provides hop-by-hop forwarding in an MPLS topology for LSRs.  
C. It exchanges routes for MPLS VPNs across different VRFs.  
D. It provides a means for LSRs to exchange IP routes.  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - LDP は、**IGP が決めた IP 経路に従って、ラベルスイッチングルータ（LSR）同士がラベルを割り当てて配布するプロトコル**。[file:24]
    - こうして各 LSR が隣接ノードとラベル情報を交換することで、MPLS ネットワーク内で **ホップ・バイ・ホップのラベル転送パス（LSP）** が構築される。  
      したがって、「hop-by-hop forwarding in an MPLS topology for LSRs」を提供するという B の説明が正しい。

- 他の選択肢がダメな理由
    - A: 複数 VPN を P ルータに接続するのは主に **MP-BGP と VRF 設定** の役割であり、LDP は単にラベル配送を行うだけで VPN 間接続を直接制御しない。
    - C: MPLS VPN のルート交換は **MP-BGP** で行われる。LDP は VRF 間のルート交換機能を持たない。
    - D: LDP が交換するのは **IP ルートそのものではなく、それに対応するラベル**。IP ルートは IGP（OSPF、IS-IS など）によって配布される。[file:24]

***
### QUESTION 315 解説

- 問題文:
Refer to the exhibit.  
The administrator noticed that the connection was flapping between the two ISPs instead of switching to ISP2 when ISP1 failed. Which action resolves the issue?

- 問題文要約（日本語）:  
IP SLA で 8.8.8.8 への ICMP Echo を監視し、その結果を track 1 で監視している。

設定抜粋:  
ip sla 1  
 icmp-echo 8.8.8.8  
 threshold 1000  
 timeout 2000  
 frequency 5  
ip sla schedule 1 life forever start-time now  
track 1 ip sla 1  

ip route 0.0.0.0 0.0.0.0 203.0.113.1  name ISP1 track 1  
ip route 0.0.0.0 0.0.0.0 198.51.100.1 2 name ISP2  [file:66]

ISP1 を優先し、IP SLA (track 1) がダウンしたら ISP2 のデフォルトルートに切り替える設計だが、実際には ISP1 と ISP2 の間でデフォルトルートがフラップしてしまう。  
どの対処を行えば安定して ISP2 へ切り替わるか。

- 選択肢:
A. Include a valid source-interface keyword in the icmp-echo statement.  
B. Reference the track object 1 on the default route through ISP2 instead of ISP1.  
C. Modify the static routes to refer both to the next hop and the outgoing interface.  
D. Modify the threshold to match the administrative distance of the ISP2 route.  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 現状の `icmp-echo 8.8.8.8` では **送信元インターフェイスが指定されておらず、ルーティングテーブルに基づいて送信元が決まる**。[file:24]
    - ISP1 がダウンすると、デフォルトルートが ISP2（198.51.100.1）に切り替わり、その回線から 8.8.8.8 への ping が成功する。  
      すると IP SLA は「成功」と判断して track 1 を再び up にし、ISP1 へのデフォルトルートが復活 → しかし ISP1 は実際にはまだ不通 → 再び SLA 失敗 → ISP2 に切り替え…  
      というループが発生し、**ISP1/ISP2 間でフラップ**する。
    - 解決するには、IP SLA の ICMP Echo が **常に ISP1 側のインターフェイス（例: `GigabitEthernet0/0`）を送信元として使うよう指定**する必要がある。  
      具体的には:  
      `ip sla 1`  
      ` icmp-echo 8.8.8.8 source-interface GigabitEthernet0/0`  
      のように `source-interface` を追加することで、ISP1 経由で通信できなくなった時点で SLA が確実に down となり、その後は ISP2 経由に切り替わったまま安定する。[file:24]

- 他の選択肢がダメな理由
    - B: track 1 を ISP2 のルートに付け替えると、「SLA が up のとき ISP2、down のとき ISP1」という逆の動作になり、優先ルートが変わってしまうだけでフラップの根本原因は解決しない。
    - C: 次ホップと出口インターフェイスの両方を指定しても、IP SLA の送信元が ISP2 側に変わってしまう問題には関係しない。
    - D: `threshold` は RTT などの応答時間のしきい値であり、**AD（管理距離）とは無関係**。しきい値を AD に合わせても、SLA の送信元問題によるフラップは続く。

***
### QUESTION 316 解説

- 問題文:
Refer to the exhibit.  
An engineer must establish a point-to-point GRE VPN between R1 and the remote site. Which configuration accomplishes the task for the remote site?

- 問題文要約（日本語）:  
R1 側のトンネル情報には、次のように表示されている。

- Tunnel source 199.1.1.1  
- Tunnel destination 200.1.1.3  
- Tunnel protocol GRE/IP  
- Tunnel transport MTU 1476 bytes など [file:67]

R1 のトンネルインターフェイスの IP アドレスは 192.168.1.1/24（図の欠落部分）だと仮定される。  
この R1 と GRE P2P トンネルを張る「リモートサイト側」の Tunnel1 設定として正しいものを選ぶ問題。

- 選択肢（要旨）:
A. Tunnel1: source 199.1.1.1, destination 200.1.1.3, ip 192.168.1.3/24  
B. Tunnel1: source 200.1.1.3, destination 199.1.1.1, ip 192.168.1.1/24  
C. Tunnel1: source 200.1.1.3, destination 199.1.1.1, ip 192.168.1.3/24  
D. Tunnel1: source 199.1.1.1, destination 200.1.1.3, ip 192.168.1.1/24  

- 正解: **C**

- 解説（なぜ C が正しいか）
    - P2P GRE トンネルでは、両端で次の条件を満たす必要がある。
        1. 一方の `tunnel source` が、もう一方の `tunnel destination` になる（＝互いに向き合う）。  
        2. トンネルインターフェイスの IP は同一サブネット上で、**異なるホストアドレス** を割り当てる。[file:24]
    - R1 側が  
      - `tunnel source 199.1.1.1`  
      - `tunnel destination 200.1.1.3`  
      と設定されているので、リモート側は **逆向き** に  
      - `tunnel source 200.1.1.3`  
      - `tunnel destination 199.1.1.1`  
      とする必要がある。
    - また、R1 のトンネル IP を 192.168.1.1/24 とすると、リモート側は同じ /24 サブネット内で別アドレス、例: 192.168.1.3/24 を使うのが自然。  
      選択肢 C はこの条件を満たしているため正解。

- 他の選択肢がダメな理由
    - A / D: どちらも `tunnel source 199.1.1.1` / `destination 200.1.1.3` と、**R1 と同じ向き** に設定しており、エンド間で向き合う構成にならない。
    - B: source/destination の向きは正しいが、トンネル IP に 192.168.1.1/24 を使っており、R1 と同じアドレスとなってしまう。  
      P2P トンネル両端で同一 IP を設定することはできないため不正。

***
### QUESTION 317 解説

- 問題文:
How is a VPN forwarding table used in an MPLS network?

- 問題文要約（日本語）:  
MPLS L3 VPN で使われる「VPN forwarding table」（＝VRF のルーティング＋ラベル情報を持つテーブル）が、どのような役割を持つのかを問う問題。[file:24]

- 選択肢:
A. learns VPN labels through LDP or RSVP  
B. learns PE route next hops through eBGP  
C. learns customer routes through MP-BGP from the connected CE  
D. stores VPN routes with associated labels  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - MPLS L3 VPN では、PE ルータ上に VRF ごとの **VPN forwarding table** が作られ、そこに  
      - CE から学習した各 VPN のルート情報  
      - それに対応する MPLS ラベル情報（仮想的な「出力インターフェイス＋ラベル」）  
      が紐付けられて保存される。[file:24]
    - このテーブルを使って、PE は VPN ごとに適切なラベルを付け替えつつパケットを転送するため、「VPN routes とそれに対応するラベルを格納する」という D の説明が最も正確。

- 他の選択肢がダメな理由
    - A: LDP や RSVP は主に **コア（P/PE 間）のラベル配布や LSP 設定** を行うプロトコルであり、「VPN forwarding table 自体がラベルを学習する」のではない。
    - B: PE 間の VPNv4 ルート交換には **MP-BGP (iBGP)** が使われ、eBGP ではない。[file:24]
    - C: CE からのルートは通常 CE–PE 間の eBGP や IGP（OSPF/EIGRP など）で学習し、**MP-BGP は PE 間で VPNv4 ルートを運ぶ** 役割。  
      「MP-BGP が CE から直接ルートを学習する」という記述は誤り。

***
### QUESTION 318 解説

- 問題文:
Drag and drop the IPv6 first hop security device roles from the left onto the corresponding descriptions on the right.

- 問題文要約（日本語）:  
IPv6 First-Hop Security では、ポートごとに「router / host / monitor / switch」といったロールを設定して、  
RA や RS などのメッセージをどのように扱うか制御する。  
それぞれのロールを、右側の説明文と正しく対応させるドラッグ＆ドロップ問題。[file:24][file:68][file:69]

- 対応付け（答え）:
1. **host**  
   → 「Receives router advertisements from valid routers, and no router solicitation are received.」  
   （有効なルータからの RA だけを受信し、自身からは RS を送らないホストポート）

2. **router**  
   → 「Receives router solicitation and sends router advertisements.」  
   （ホストからの RS を受け取り、RA を送信するルータポート）

3. **monitor**  
   → 「Receives valid and rogue router advertisements and all router solicitation.」  
   （正規・不正どちらの RA も RS もすべて受信して監視するポート）

4. **switch**  
   → 「Received router advertisements are trusted and are flooded to synchronize states.」  
   （受信した RA を信頼し、スイッチ内で状態同期のためにフラッディングするポート）

***
### QUESTION 319 解説

- 問題文:
Refer to the exhibit.  
PC2 is directly connected to R1. A user at PC2 cannot Telnet to 2001:db8:a:b::10. The user can ping 2001:db8:a:b::10 and receive DHCP-related information from the DHCP server. Which action resolves the issue?

- ACL 設定（抜粋）:
R1# sh ipv6 access-list GUARD  

deny tcp any host 2001:DB8:A:B::10 eq telnet       sequence 10  
permit tcp host 2001:DB8:A:A::20 host 2001:DB8:A:B::10 eq telnet  sequence 20  
permit tcp host 2001:DB8:A:A::2  host 2001:DB8:A:D::1 eq www      sequence 30  
permit ipv6 2001:DB8:A:A::/64 any                               sequence 40  [file:70]

- 問題文要約（日本語）:  
PC2 は R1 に L2 直結しており、IPv6 的には 2001:db8:a:a::/64 のホスト。  
PC2 から 2001:db8:a:b::10 へ ping や DHCP は通るが、Telnet だけができない。  
上記 IPv6 ACL「GUARD」がどこかのインターフェイスに適用されている状態で、どのシーケンス番号のエントリをどこに挿入し直せば Telnet が通るかを問う問題。

- 選択肢:
A. Remove sequence 10 and put it back as sequence 25.  
B. Remove sequence 20 and put it back as sequence 45.  
C. Remove sequence 30 and put it back as sequence 5.  
D. Remove sequence 40 and put it back as sequence 15.  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - ACL の評価は **上から順に** 行われ、最初にマッチしたエントリの動作が適用される。  
    - 現状:
        - seq 10: `deny tcp any host 2001:DB8:A:B::10 eq telnet`  
          → 宛先が 2001:db8:a:b::10 で宛先ポートが telnet のすべての TCP を拒否する。  
          PC2 の送信元も `any` に含まれるため、**Telnet はここでブロックされる**。  
        - seq 20 では PC2（2001:db8:a:a::20）からの Telnet を許可しているが、seq 10 で既に deny されているので到達しない。[file:24]
    - したがって、PC2 の Telnet を許可するには、
        1. まず PC2 からの Telnet を permit でマッチさせる（seq 20）。  
        2. その後に「その他の any からの Telnet を deny」する（seq 10 相当）  
      という順序にする必要がある。
    - 選択肢 A のように、**seq 10（deny）を一度削除し、seq 25 として seq 20 より後ろに再作成**すれば、  
      - 先に seq 20 で PC2 からの Telnet を許可  
      - その後、他の any からの Telnet を seq 25 の deny で拒否  
      となり、要件を満たす。

- 他の選択肢がダメな理由
    - B: seq 20（PC2 からの permit）を最後尾に移すと、先に seq 10 の deny にマッチしてしまい、依然として Telnet は拒否される。
    - C: seq 30（www トラフィック）を先頭に動かしても、Telnet に関する条件には影響しない。
    - D: seq 40（`permit ipv6 2001:DB8:A:A::/64 any`）を seq 15 にすると、Telnet を含むすべてのトラフィックが seq 15 で許可され、  
      その下の telnet deny/permit ロジックが無意味になる。問題は「PC2 以外からの Telnet は拒否したままにする」ことなので、D は意図と合わない。

***
### QUESTION 320 解説

- 問題文:
Refer to the exhibit.  
The remote server is failing to receive the NetFlow data. Which action resolves the issue?

- 設定抜粋:
flow record v4_r1  
 match ipv4 tos  
 match ipv4 protocol  
 match ipv4 source address  
 match ipv4 destination address  
 match transport source-port  
 match transport destination-port  
 collect counter bytes long  
 collect counter packets long  

flow exporter EXPORTER-1  
 destination 172.16.10.2  
 transport udp 2055  

flow monitor FLOW-MONITOR-1  
 exporter EXPORTER-1  
 record v4_r1  

!  

flow monitor v4_r1  

interface Ethernet0/0.1  
 ip address 172.16.6.2 255.255.255.0  
 ip flow monitor v4_r1 input  [file:71]

- 問題文要約（日本語）:  
NetFlow v9/Flexible NetFlow を設定し、`flow record v4_r1` と `flow exporter EXPORTER-1` を使ったモニタ `FLOW-MONITOR-1` を定義している。  
しかし、インターフェイス Ethernet0/0.1 には `ip flow monitor v4_r1 input` が適用されており、リモートのコレクタ (172.16.10.2) はフローを受信していない。  
どの修正を行えば NetFlow データがコレクタに送信されるか。

- 選択肢:
A. Modify the flow transport command `transport udp 2055` to move under flow monitor profile.  
B. Modify the interface command to `ip flow monitor FLOW-MONITOR-1 input`.  
C. Modify the udp port under flow exporter profile to `ip transport udp 4739`.  
D. Modify the flow record command `record v4_r1` to move under flow exporter profile.  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - Flexible NetFlow では、フローの生成・送信には 3 つの要素が必要。
        1. **flow record**: どのフィールドをマッチ／コレクトするか  
        2. **flow exporter**: どの宛先 IP・UDP ポートへ送るか  
        3. **flow monitor**: record と exporter を紐付けたプロファイル  
      そしてインターフェイスには「monitor」を適用する必要がある。[file:24]
    - 設定を見ると、
        - `FLOW-MONITOR-1` は `record v4_r1` と `exporter EXPORTER-1` を正しく参照している。  
        - しかし、インターフェイスには **別名の `flow monitor v4_r1`** が適用されており、こちらにはレコードもエクスポータも定義されていない（空のモニタ）。  
          そのため、コレクタへは何も送られない。
    - 解決策は、インターフェイス側で **正しいモニタ名 `FLOW-MONITOR-1` を指定すること**。  
      つまり `ip flow monitor FLOW-MONITOR-1 input` に修正すれば、レコードとエクスポータが紐付いたモニタが有効になり、リモートサーバへ NetFlow データが送信される。

- 他の選択肢がダメな理由
    - A / D: `transport udp` や `record` の設定位置は既に正しく、エクスポータ／レコードの文法も問題ない。モニタ名のミスマッチが根本原因であり、これらを動かしても状況は変わらない。
    - C: UDP 4739 は IPFIX でよく用いられるポートだが、問題文からはコレクタ側が 4739 を使うという情報はない。  
      現在の 2055 も一般的な NetFlow ポートであり、ポート変更は不要。

***
### QUESTION 321 解説

- 問題文:
What is a function of an end device configured with DHCPv6 guard?

- 問題文要約（日本語）:  
DHCPv6 Guard 機能で「エンドデバイス（クライアント側）」としてロールを設定したポートは、どのような動作をするかを問う問題。[file:24]

- 選択肢:
A. If it is configured as a server, only prefix assignments are permitted.  
B. If it is configured as a relay agent, only prefix assignments are permitted.  
C. If it is configured as a client, messages are switched regardless of the assigned role.  
D. If it is configured as a client, only DHCP requests are permitted.  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - DHCPv6 Guard は、スイッチポートのロール（server / relay / client）に応じて、**DHCPv6 メッセージの方向をフィルタする機能**。[file:24]
    - ポートが **client ロール** の場合、そのポートは「エンドホストが接続されている」とみなされるため、  
      - ホストから上流への **Solicit / Request などのクライアントメッセージは許可**  
      - しかし、そのポートから **Server/Relay 由来の Reply / Advertise などが出ていくことは想定されない** ためブロックされる。  
      → つまり「クライアントからの DHCP 要求だけを通す」動作になり、D の説明と一致する。

- 他の選択肢がダメな理由
    - A / B: server や relay ロールのときに「prefix assignment のみ許可」というような限定は行わない。サーバ／リレー用ポートでは、通常のサーバ／リレーメッセージ（Reply, Advertise など）を許可する。
    - C: client ロールではすべての DHCPv6 メッセージをスイッチするのではなく、**不正なサーバ／リレーメッセージを遮断する** のが目的なので誤り。

***
### QUESTION 323 解説

- 問題文:
Refer to the exhibit.  
An engineer must configure a LAN-to-LAN IPsec VPN between R1 and the remote router. Assume that the remote router is using SHA. Which IPsec Phase 1 configuration must the engineer use on the local router (R1)?

- 問題文要約（日本語）:  
R1（左側）は SP 経由でリモートルータ（R3）と IPsec site-to-site VPN を張る。  
- R1 の外側インターフェイス: 199.1.1.1/24（toward SP）  
- R3 の外側インターフェイス: 200.1.1.3/24（toward SP）[file:72]  
リモート側は ISAKMP Phase 1 でハッシュに SHA を使用している前提。  
ローカル側 R1 の `crypto isakmp policy` / `crypto isakmp key` をどう書くべきかを問う問題。

- 選択肢（要旨）:
A. hash sha, key for address 200.1.1.3  
B. hash md5, key for address 200.1.1.3  
C. hash md5, key for address 199.1.1.1  
D. hash md5, key for address 199.1.1.1（末尾に余分な !）  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - IKE Phase 1 の双方で一致させる必要がある主なパラメータ:
        - 暗号化アルゴリズム (encryption)  
        - ハッシュアルゴリズム (hash)  
        - 認証方式 (authentication)  
        - DH グループ (group)  
        - ライフタイム (未指定ならデフォルト同士で一致)  
      ここでは「リモートは SHA を使用」と明記されているため、ローカルも `hash sha` にする必要がある。[file:24]
    - また、`crypto isakmp key cisco123 address <peer-IP>` の `<peer-IP>` には **対向ピア（リモートルータ）の外側アドレス** を指定する。  
      R1 から見たピアは 200.1.1.3 なので、この IP を指定しなければならない。[file:72]
    - 選択肢 A は  
      - `hash sha`  
      - `crypto isakmp key cisco123 address 200.1.1.3`  
      と、ハッシュとピア IP の両方が正しく設定されているため、要求を満たす。

- 他の選択肢がダメな理由
    - B: ピアアドレスは正しいが `hash md5` となっており、リモートが SHA を使っているため Phase 1 が確立しない。
    - C / D: いずれも `hash md5` で誤りなうえ、`address 199.1.1.1` とローカル自身の IP を指定してしまっている。  
      ISAKMP キーの address に自分の IP を指定しても、リモートとのピア関係には使用されない。

***
### QUESTION 324 解説

- 問題文:
A CoPP policy is applied for receiving SSH traffic from the WAN interface on a Cisco ISR4321 router. However, the SSH response from the router is abnormal and stuck during high link utilization. The problem is identified as SSH traffic does not match in the ACL. Which action resolves the issue?

- 問題文要約（日本語）:  
ISR4321 で、WAN から入ってくる SSH を守るために CoPP（Control Plane Policing）を設定している。  
ところがリンク使用率が高いとき、ルータからの SSH 応答が異常に遅くなり、原因を調べると「SSH トラフィックが ACL にマッチせず、CoPP で保護されていない」ことが分かった。  
どの対応でこの問題を解決できるかを問う。

- 選択肢:
A. Rate-limit SSH traffic to ensure dedicated bandwidth.  
B. Apply CoPP on the control plane interface.  
C. Increase the IP precedence value of SSH traffic to 6.  
D. Apply CoPP on the WAN interface inbound direction.  

- 正解: **B**

- 解説（なぜ B が正しいか）
    - CoPP は名前の通り **「コントロールプレーン（ルータ自身が終端するトラフィック）」に対するポリシング** であり、  
      物理インターフェイスではなく `control-plane` コンテキストに `service-policy` を適用するのが正しい。[file:24]
    - 問題文では「WAN インターフェイスで受信する SSH 用に CoPP を適用した」とあり、  
      おそらくインターフェイス側にポリシーを適用しているため、**実際に CPU へ届く SSH パケットは CoPP ポリシーの対象外** になっている。  
      その結果、高負荷時に他のトラフィックに押し流され、SSH 応答が詰まっている。
    - `control-plane` に CoPP を適用すれば、**すべての入力経路からコントロールプレーンへ届く SSH トラフィックが ACL でマッチし、適切に保護・優先制御される** ため、B が正解。

- 他の選択肢がダメな理由
    - A: レートリミット（帯域制限）をかけるだけでは、「SSH が CoPP にマッチしていない」という根本問題は解決しない。
    - C: IP precedence を 6 に上げても、CoPP の ACL マッチ条件や適用場所が誤っていれば効果はない。
    - D: すでに「WAN インターフェイスで受信する SSH 用に CoPP を適用した」とあり、そのやり方が問題の原因。  
      CoPP はインターフェイスではなく control-plane に適用すべきなので、D は誤り。

***
### QUESTION 325 解説

- 問題文:
How are MPLS Layer 3 VPN services deployed?

- 問題文要約（日本語）:  
MPLS L3 VPN サービスを成立させるために、サービスプロバイダ側で必須となる要件はどれかを問う問題。[file:24]

- 選択肢:
A. The RD and RT values must match under the VRF  
B. The RD and RT values under a VRF must match on the remote PE router  
C. The import and export RT values under a VRF must always be the same.  
D. The label switch path must be available between the local and remote PE routers.  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - MPLS Layer 3 VPN では、**VPNv4 ルートのやり取り (MP-BGP)** と、それを実際に転送するための **MPLS ラベルスイッチパス (LSP)** の両方が必要になる。[file:24]
    - CE 間のユーザトラフィックは、PE からコアの P ルータ群を MPLS ラベル付きで通過し、対向 PE まで届く必要があるため、**ローカル PE とリモート PE の間に LSP が張られていること**が前提条件となる。  
      これを満たすのが D の記述。

- 他の選択肢がダメな理由
    - A: RD と RT は役割が異なり、**一致させる必要はない**。  
      - RD（Route Distinguisher）は VPNv4 アドレスを一意にするための識別子。  
      - RT（Route Target）は「どの VRF にルートをインポート／エクスポートするか」を決めるタグ。
    - B: RD/RT の値がリモート PE と完全一致している必要はない。  
      同一 VPN に属する VRF では「インポート／エクスポート RT の関係」が重要であり、RD の一致は必須条件ではない。
    - C: 一つの VRF 内でも **import RT と export RT をあえて変える**ことで、ハブ＆スポーク型などさまざまなトポロジを実現できる。[file:24]  
      常に同じでなければならないわけではない。

***
### QUESTION 326 解説

- 問題文:
Which feature minimizes DoS attacks on an IPv6 network?

- 問題文要約（日本語）:  
IPv6 ネットワークに対する DoS（Denial of Service）攻撃を最小化するのに役立つ IPv6 First-Hop Security 機能はどれかを問う問題。[file:24]

- 選択肢:
A. IPv6 Binding Security Table  
B. IPv6 Router Advertisement Guard  
C. IPv6 Prefix Guard  
D. IPv6 Destination Guard  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - **IPv6 Destination Guard** は、IPv6 FHS バインディングテーブルを利用して「有効な宛先アドレス」だけに対して NDP 解決を行い、それ以外の宛先あてトラフィックをブロックする機能。[file:24]
    - これにより、存在しない多量の宛先アドレスへのトラフィックや、不正な宛先に対する Neighbor Solicitation を抑制できるため、**NDP を悪用した DoS 攻撃を大幅に軽減**できる。
    - Destination Guard は「宛先アドレスベース」でフィルタするため、攻撃トラフィックがスイッチやルータの制御プレーンに到達する前に落とせる点で、DoS 緩和に特化している。

- 他の選択肢がダメな理由
    - A: Binding Table 自体は FHS 機能が参照するデータベースであり、それ単体が DoS 対策機能というわけではない。
    - B: **RA Guard** は不正な Router Advertisement をブロックして、偽ルータによる攻撃を防ぐ機能であり、NDP 宛先全般への DoS を最小化する主目的ではない。
    - C: **Prefix Guard** は不正・誤ったプレフィックスからのトラフィックを防ぐ機能で、主にトポロジ的に正しくないアドレスの利用を防止する。[file:24]  
      DoS の緩和には寄与するが、Destination Guard ほど「DoS を最小化する機能」として直接的に説明されていない。

***
### QUESTION 328 解説

- 問題文:
Refer to the exhibit. Mutual redistribution is enabled between RIP and EIGRP on R2 and R5. Which configuration resolves the routing loop for the 192.168.1.0/24 network?

- トポロジ要約:
- R1 の LAN: 192.168.1.0/24（R1–R2 間は 178.1.10.4/30）  
- R2–R5 間: 178.1.10.0/30（RIP）  
- R2–R3–R4–R5 間: 181.16.x.x/30（EIGRP）[file:74]  
R2 と R5 は、RIP と EIGRP を相互再配布している。

- ループが起きる理由（日本語要約）:
    - 192.168.1.0/24 は本来 R1 側の RIP ドメインに属するネットワーク。  
    - R2 は 192.168.1.0/24 を RIP から受け取り、EIGRP に再配布する。  
    - R5 はその EIGRP ルートを再び RIP に再配布し、R2 側から 192.168.1.0/24 を「R5 経由」として学習してしまう。  
    - 結果として、R2 と R5 間で 192.168.1.0/24 をお互いに再配布し合い、ルーティングループや不安定さが発生する。[file:24]

- 正解: **B**

- 解説（なぜ B が正しいか）
    - ループを防ぐには、「R1 側の 192.168.1.0/24 ネットワークが EIGRP ドメインに“戻ってくる”のを防ぐ」必要がある。  
    - 選択肢 B では、R2 と R5 の両方で  
      - EIGRP からの学習ルートに対して、インターフェイス s1 入口で  
      - `access-list 1 deny 192.168.1.0` を用いた `distribute-list 1 in`  
      を設定している。[file:24]
    - s1 はそれぞれ EIGRP 側（R3/R4 方面）につながるインターフェイスなので、  
      **EIGRP から 192.168.1.0/24 を受け取らない** ようにすることで、ルートが RIP ドメインに逆流せず、ループが解消される。
    - また、問題文では EIGRP の AS 番号について特に制約が出ていないため、R2/R5 が同じ AS を使っていれば AS 番号 7 である点も問題にならない。

- 他の選択肢がダメな理由
    - A / C / D: distribute-list の適用インターフェイスが不適切だったり、一方のルータだけ／別方向でフィルタしているため、  
      いずれかの経路で 192.168.1.0/24 が EIGRP から RIP に再流入し、ループの可能性が残る。

***
### QUESTION 329 解説

- 問題文:
access-list 1 permit 219.65.100.15  
access-list 2 permit 219.65.100.16  

interface Ethernet1  
 ip policy route-map Texas  

route-map Texas permit 10  
 match ip address 1  
 set ip precedence priority  
 set ip next-hop 219.65.100.17  

route-map Texas permit 20  
 match ip address 2  
 set ip next-hop 219.65.100.18  

Packets arriving from source 219.65.100.15 must be sent with precedence bit 1, and packets from 219.65.100.16 with precedence bit 5. Which action resolves the issue?

- 問題文要約（日本語）:  
PBR（Policy-Based Routing）で、  
- ACL 1: 送信元 219.65.100.15  
- ACL 2: 送信元 219.65.100.16  
をマッチさせ、  
- 219.65.100.15 のパケットは Precedence=1  
- 219.65.100.16 のパケットは Precedence=5  
になるようにしたい。  
既存設定では、両方の `set ip precedence` が 1（priority）になっていないかを確認し、どこを直すかを問う問題。[file:24]

- IP Precedence 名称と値:
  - routine = 0  
  - priority = 1  
  - immediate = 2  
  - flash = 3  
  - flash-override = 4  
  - critical = 5  
  - internet = 6  
  - network = 7  [file:24]

- 現状の設定と要件のギャップ
    - route-map Texas permit 10（ACL1: 219.65.100.15）  
      `set ip precedence priority` → priority=1 なので要件どおり。  
    - route-map Texas permit 20（ACL2: 219.65.100.16）  
      Precedence の設定がなく、デフォルトの 0（routine）のまま。  
      本来は 5（critical）にしたい。

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 219.65.100.15 については既に `set ip precedence priority` で 1 をセットできているので変更不要。  
    - 219.65.100.16 については、permit 20 に  
      `set ip precedence critical`  
      を追加すれば Precedence=5（critical）に設定できる。[file:24]
    - 選択肢 A の  
      > set ip precedence critical in route-map Texas permit 20  
      がちょうどこの修正に該当するため正解。

- 他の選択肢がダメな理由
    - B: permit 10 を immediate(2) に変えてしまい、219.65.100.15 の要件「1」を満たさなくなる。
    - C: permit 10 を critical(5) に変えてしまい、こちらも 219.65.100.15 の要件「1」と不一致。
    - D: permit 20 を priority(1) にすると、219.65.100.16 も Precedence=1 となり、必要な「5」にならない。

***
### QUESTION 330 解説

- 問題文:
R2 のルーティングエントリ（一部）が次のように表示されている:

C 192.168.10.0/24 is directly connected, Serial1/1e  
172.16.0.0/16 is variably subnetted, 5 subnets, 2 masks  
C 172.16.160.0/19 is directly connected, Loopback1  
C 172.16.128.0/19 is directly connected, Loopback2  
C 172.16.224.0/19 is directly connected, Loopback3  
C 172.16.192.0/19 is directly connected, Loopback4  
D 172.16.0.0/16 is a summary, 00:01:23, Null0  

R1 と R2 の間で EIGRP を構成する際、「要約ルート 172.16.0.0/16 を広告させたくない」。  
どの設定を行えばよいか。[file:24]

- 状況のポイント（日本語）:
    - R2 には 172.16.128.0/19 ～ 172.16.224.0/19 など複数のサブネットとともに、  
      `D 172.16.0.0/16 is a summary, Null0` という **EIGRP の自動サマリールート** が入っている。  
    - このサマリは「EIGRP auto-summary」が有効なときに生成される典型的な形で、  
      R2 から R1 へ 172.16.0.0/16 が広報されてしまう。[file:24]
    - 要件は「R1 と R2 の間の EIGRP で、このサマリを使わないこと」。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - 自動サマリは、**そのルートを持っているルータ側の EIGRP プロセス** で制御される。  
      今回サマリ 172.16.0.0/16 を持っているのは R2 なので、R2 側で `no auto-summary` を入れる必要がある。
    - 選択肢 B の  
      - `R2(config)# router eigrp 1`  
      - `R2(config-router)# no auto-summary`  
      により、R2 は 172.16.0.0/16 の自動サマリを生成・広告しなくなり、R1 には個別の /19 ルートだけが配布される。

- 他の選択肢がダメな理由
    - A: R1 に `no auto-summary` を入れても、R2 から送られてくる 172.16.0.0/16 のサマリルートの生成源は R2 のままなので解決しない。
    - C / D: `auto-summary` を有効化する設定であり、逆にサマリルートの生成を促進してしまう。

***
### QUESTION 331 解説

- 問題文:
Refer to the exhibit.  
R1 と R2 は EIGRP ピアを結ぶ際に認証を使うよう設定されているが、ネイバー関係が不安定で down/up を繰り返している。  
どの対応で問題を解決できるか。

- 設定要約:
R1:

key chain cisco  
 key 2  
  key-string abc  

interface Serial1/0  
 ip address 192.168.10.1 255.255.255.0  
 ip authentication mode eigrp 100 md5  
 ip authentication key-chain eigrp 100 cisco  

router eigrp 100  
 network 10.10.1.0 0.0.0.255  
 network 192.168.10.0  
 no auto-summary  

R2:

key chain cisco  
 key 1  
  key-string 123  
 key 2  
  key-string abc  

interface Serial1/0  
 ip address 192.168.10.2 255.255.255.0  
 ip authentication mode eigrp 100 md5  
 ip authentication key-chain eigrp 100 cisco  [file:75]

`show ip eigrp neighbors` では、R2 から見ると隣接が一度 up した後すぐ retry limit exceeded で down になり、再度 up する、を繰り返している。[file:76]

- 問題文要約（日本語）:  
両ルータとも key chain 名は「cisco」、EIGRP 認証モードは MD5 で一致しているのに、EIGRP 隣接が安定しない。  
どの設定変更が必要か。

- 正解: **D**

- 解説（なぜ D が正しいか）
    - EIGRP の MD5 認証では、**key-chain 内の「最も小さい key-id」を送信時に使用**する。[file:24]
    - R1 の key chain:
        - key 2 のみ（string "abc"）  
        → R1 は送信時に key-id 2 を使う。
    - R2 の key chain:
        - key 1 ("123") と key 2 ("abc")  
        → R2 は送信時に **key-id 1** を使う。
    - パケット受信時の挙動:
        - R2 が R1 から受信: R1 は key 2 / "abc" を使う。  
          R2 も key 2 を持っているため検証に成功し、この方向の認証は問題ない。
        - R1 が R2 から受信: R2 は key 1 / "123" を使う。  
          しかし R1 側には key-id 1 が存在しないため、R1 はそのパケットを正しく検証できず拒否する。  
          これが隣接 flapping の原因。
    - 従って、**両ルータで「使用される最小 key-id を揃える」**必要がある。  
      例えば、R1 側にも key 1 を追加するか、R2 側で key 1 を削除して key 2 のみを残すなど。  
      この考え方を述べているのが選択肢 D の  
      > Configure a matching lowest key-id on both routers.  
      であり、これが正解となる。

- 他の選択肢がダメな理由
    - A: 認証タイプ（MD5）は既に `ip authentication mode eigrp 100 md5` で一致している。
    - B: key-chain 名も両方とも `cisco` で一致している。
    - C: 「どれか 1 つの key-id を合わせればよい」ではなく、「送信に使われる最小 key-id」が一致していないことが問題なので不十分。

***
### QUESTION 332 解説

- 問題文:
Which method provides failure detection in BFD?

- 問題文要約（日本語）:  
BFD がフォワーディングパスの障害検出を行うとき、その特徴を最もよく表している組み合わせはどれか。[file:24]

- 選択肢:
A. short duration, low overhead  
B. long duration, low overhead  
C. long duration, high overhead  
D. short duration, high overhead  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - BFD（Bidirectional Forwarding Detection）は、隣接ルータ間のフォワーディングパス（インターフェイス、リンク、転送プレーン）の障害を、**非常に短い間隔で監視する軽量な仕組み**。[file:24]
    - 特徴は:
        - **short duration**: ミリ秒単位の短い検出タイマで素早くダウンを検出できる。  
        - **low overhead**: 単純な制御パケットを周期的に送るだけなので、CPU や帯域への負荷が小さい。  
    - この 2 点の組み合わせは選択肢 A の  
      > short duration, low overhead  
      と一致する。

- 他の選択肢がダメな理由
    - B / C: long duration は BFD の「高速検出」という目的と矛盾する。
    - D: high overhead では BFD の「軽量」という設計方針と合わない。

***
### QUESTION 333 解説

- 問題文:
Refer to the exhibit.  
TCP トラフィックは、ホスト 10.10.10.10/24 に対して左側の R2 経由で到達させたい。  
どの操作を行えばよいか。

- 設定要約（R1）:
ip access-list extended UDP-ACL
 permit udp any any
!
route-map VIA-R2 permit 10
 match ip address UDP-ACL
 set ip next-hop 10.10.11.2
!
interface Gi0/1
 ip policy route-map VIA-R2  [file:77]

- 問題文要約（日本語）:  
R1 の PBR では UDP-ACL にマッチしたトラフィックだけを next-hop 10.10.11.2（R2 側）に送るよう設定されている。  
しかし、TCP トラフィックも R2 経由にしたい。

- 選択肢:
A. 変更なしでも TCP は R2 経由になる。  
B. route-map permit 10 の set ip next-hop を 10.10.12.2 に変更する。  
C. ACL に TCP を許可するエントリを追加し、route-map はそのままにする。  
D. route-map に permit 20 ステートメントを追加して TCP を許可する。  

- 正解: **C**

- 解説（なぜ C が正しいか）
    - 現在の拡張 ACL `UDP-ACL` は `permit udp any any` のみであり、**UDP トラフィックだけ**が PBR にマッチして R2（10.10.11.2）に送られる。[file:77]
    - TCP トラフィックは ACL にマッチしないため、PBR の対象外となり、通常のルーティング（＝右側 R4 経由など）で転送される。
    - したがって、**ACL に TCP を含めるだけでよい**。  
      例:  
      - `permit udp any any`  
      - `permit tcp any any`  
      とすれば、UDP と TCP の両方が同じ route-map エントリ（permit 10）にマッチし、`set ip next-hop 10.10.11.2` により R2 経由となる。
    - この操作内容を示しているのが選択肢 C の  
      > Allow TCP in the access list with no changes to the route map.  
      であり、正解となる。

- 他の選択肢がダメな理由
    - A: 現状 ACL が UDP のみ許可のため、TCP は PBR 適用外で R2 経由にはならない。
    - B: next-hop アドレスを変えても、そもそも TCP が route-map にマッチしていないので意味がない。
    - D: permit 20 を追加しても、そこにマッチさせるための ACL や条件がないと TCP は依然として PBR されない。

***
### QUESTION 334 解説

- 問題文:
Refer to the exhibit.  
R1, R2, R3, R4 が EIGRP AS 100 で接続されている。  
R1–R2–R3 の三角形部分で、poison reverse が有効になっているにもかかわらずループが発生している。  
R1, R2, R3 間のループを防ぐにはどの対応を行うべきか。[file:78]

- 選択肢:
A. Configure route filtering  
B. Configure route tagging  
C. Configure R3 as stub receive-only.  
D. Enable split horizon  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 図では R1–R2–R3 の 3 台がフルメッシュではなく、「ある宛先に対して複数の経路情報がグルグル回ってしまう」状況が想定されている。[file:24]
    - EIGRP では、poison reverse や split horizon で多くのループを防げるが、  
      特定のトポロジでは依然として「あるルータから学習した経路を別方向で再度広告してしまう」ことでループが生じうる。
    - このようなときに有効なのが **route filtering** で、  
      - 例えば R2 が R3 経由で学習した特定プレフィックスを R1 に広告しないようにする  
      - または R3 が R1 から学習した経路を R2 へ再広告しないようにする  
      といった制御をかけることで、ループの経路そのものを遮断できる。[file:24]
    - 選択肢 A はその対策を表しており、正解となる。

- 他の選択肢がダメな理由
    - B: route tagging は主に「プロトコル間の再配送（redistribution）」でループを防ぐために使われる。  
      今回は EIGRP 単一ドメイン内の話であり、前提に redistribution がないので不適切。
    - C: R3 を stub receive-only にすると、R3 はルートを他へ広告しなくなるため、R3 の下流ネットワークを他ルータが学習できなくなるなど、要件を満たさない。
    - D: split horizon は「受信したインターフェイスから同じ経路を送り返さない」機能で、主にハブ&スポークのような部分メッシュ環境で有効。  
      問題文では既に poison reverse が有効であり、split horizon だけでこの三角形内のループは解消できない。

***
### QUESTION 335 解説

- 問題文:
A network administrator added a new spoke site with dynamic IP on the DMVPN network.  
Which configuration command passes traffic on the DMVPN tunnel from the spoke router?

- 問題文要約（日本語）:  
DMVPN ネットワークに「動的 IP（DHCP など）」を使う新しいスポークを追加した。  
スポーク側の物理（NBMA）アドレスが変わっても、ハブ側 NHRP サーバがそのスポークからのトラフィックを受け付けられるようにするには、どのコマンドが必要か。[file:79]

- 選択肢:
A. ip nhrp registration no-unique  
B. ip nhrp registration no-registration  
C. ip nhrp registration ignore  
D. ip nhrp registration dynamic  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - DMVPN では、スポークは NHRP を使って「トンネル IP ↔ NBMA（物理）IP」をハブに登録する。[file:79]
    - 通常は、NHS（ハブ）は「同じトンネル IP には 1 つの NBMA アドレスだけ」を許可する（ユニーク制約）。  
      しかし、スポークが動的 IP を使う場合、再接続のたびに NBMA アドレスが変わる。
    - `ip nhrp registration no-unique` をスポークで設定すると、  
      **ハブ側 NHS が同じトンネル IP からの新しい NBMA アドレスの登録を受け付ける** ようになり、  
      IP 変更後もトンネル経由でトラフィックが通るようになる。[file:79]
    - したがって、動的 IP スポークでトラフィックを通すために必要なコマンドは選択肢 A となる。

- 他の選択肢がダメな理由
    - B: `no-registration` のようなキーワードは NHRP で使用されない。
    - C: `registration ignore` は、ハブ側が特定の登録要求を無視する文脈で使われるもので、スポーク側で動的 IP を許可する目的とは合致しない。
    - D: `registration dynamic` というコマンドは存在せず、動的 IP 許可の機能とも一致しない。

***
### QUESTION 336 解説

- 問題文:
Refer to the exhibit.  
R4 から、R2 の向こう側にある 172.16.2.7 へトラフィックを送るとパケットドロップが発生している。  
どの対応を行えば問題を解決できるか。[file:124]

- トポロジ概要:
    - R1 側 LAN: 172.16.1.0/24  
    - R3 側 LAN: 172.16.2.0/24  
    - R1–R2 間: 10.1.1.0/24  
    - R2–R3 間: 10.1.2.0/24  
    - 3 台とも EIGRP AS 100 を実行。  
    - R1, R3 は auto-summary 有効のため、R2 へ 172.16.0.0/16 として広告している。[file:79]

- 選択肢:
A. Enable auto summarization on all three routers R1, R2, and R3.  
B. Insert a /16 floating static route on R2 toward R3 with metric 254.  
C. Disable auto summarization on R2.  
D. Insert a /24 floating static route on R2 toward R3 with metric 254.  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - R1 と R3 から見た 172.16.0.0/16 の EIGRP ルートが、R2 上で **左右の両インターフェイス（Fa0/0, Fa0/1）経由の等コストルート** として存在するため、R2 は 172.16.2.7 へのパケットをどちら側にも転送しうる。[file:79]
    - 172.16.2.7 へ到達すべき正しい経路は **R3 側（10.1.2.0/24）** だが、要約ルートだけでは R2 が 172.16.1.0/24 と 172.16.2.0/24 を区別できないため、R1 側へ誤転送し、結果としてループやドロップが発生する。
    - これを避けるには、R2 に「172.16.2.0/24 は必ず R3 側へ」という **より具体的なスタティックルート** を入れておけばよい。  
      - 例: `ip route 172.16.2.0 255.255.255.0 10.1.2.x 254`  
      - /24 ルートは /16 要約よりも「長いプレフィックス（より具体的）」なので、常に EIGRP の 172.16.0.0/16 より優先される。
    - この考え方を示しているのが選択肢 D の  
      > Insert a /24 floating static route on R2 toward R3 with metric 254.  
      であり、正解となる。

- 他の選択肢がダメな理由
    - A: R2 でも auto-summary を有効にしても、すでに要約ルートの問題が起きているので根本解決にならない。
    - B: /16 のスタティックルートは EIGRP の /16 と同じプレフィックス長であり、AD を 254 にしても優先順位の制御が難しく、誤転送を防げない。
    - C: R2 の auto-summary を無効にしても、R1 と R3 から 172.16.0.0/16 が送られてきている限り、R2 のルーティングテーブルに 2 本の /16 が残りうるため解決しない。

***
### QUESTION 337 解説

- 問題文:
Refer to the exhibit.  
DHCPv6 を用いて展開されたネットワークで、サーバ間の到達性が不安定になっている。  
DHCPv6 が正常動作するようにするには、どのコマンドを設定から削除する必要があるか。[file:125]

- 設定要約:
ipv6 dhcp pool DHCPPOOL  
 address prefix 2001:0:1:4::/64 lifetime infinite infinite  

interface FastEthernet0/0  
 ip address 10.0.0.1 255.255.255.240  
 ipv6 address 2001:0:1:4::1/64  
 ipv6 enable  
 ipv6 nd ra suppress  
 ipv6 ospf 1 area 1  
 ipv6 dhcp server DHCPPOOL  [file:125]

- 選択肢:
A. ipv6 address 2001:0:1:4::1/64  
B. ipv6 dhcp server DHCPPOOL  
C. ipv6 nd ra suppress  
D. address prefix 2001:0:1:4::/64 lifetime infinite infinite  

- 正解: **C**

- 解説（なぜ C が正しいか）
    - DHCPv6 でアドレスを配布する場合、多くのホストは **RA（Router Advertisement）の M フラグ** を見て、「DHCPv6 を使うかどうか」を判断する。[file:79]
    - インターフェイスに `ipv6 nd ra suppress` を設定すると、そのインターフェイスでは **RA 自体が送信されなくなる**。  
      するとホストは M フラグを受け取れず、DHCPv6 を使うか SLAAC にするかを適切に判断できないため、アドレス取得やデフォルトゲートウェイ学習が不安定になる。
    - したがって、DHCPv6 を安定動作させるには **RA を抑止している `ipv6 nd ra suppress` を削除**すべきである。  
      RA を送信しつつ M フラグを 1 に設定すれば、ホストは「アドレス情報は DHCPv6 から取得すべき」と判断できる。
    - 他の行（DHCP プール定義、インターフェイスの IPv6 アドレス、DHCPv6 サーバ指定）は DHCPv6 に必要な設定であり、削除すると逆に機能しなくなる。

***
### QUESTION 338 解説

- 問題文:
An engineer failed to run diagnostic commands on devices using Cisco DNA Center.  
Which action in Cisco DNA Center resolves the issue?[file:79]

- 選択肢:
A. Enable APIs  
B. Enable Secure Shell  
C. Enable CDP.  
D. Enable Command Runner.  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - Cisco DNA Center には、管理対象デバイスに対して CLI ベースの診断コマンドを一括実行し、その出力を収集する機能 **Command Runner** が用意されている。[file:79]
    - エンジニアが Cisco DNA Center から診断コマンドを実行できない場合、対象デバイスに対してこの Command Runner 機能が有効になっていない（もしくはワークフローで使用していない）ことが原因となる。  
    - Command Runner を有効化・使用することで、選択した複数デバイスに対し show 系などの診断 CLI を実行し、その結果を画面やレポートで確認できるようになるため、設問の問題が解消される。  

- 他の選択肢がダメな理由
    - A: API の有効化は DNA Center と外部システム連携用であり、デバイスへ CLI 診断を投げる機能とは直接関係しない。
    - B: SSH はデバイス側での管理アクセスプロトコルであり、DNA Center 側で「Enable Secure Shell」という操作ではない。
    - C: CDP は隣接情報の検出に使うプロトコルで、診断コマンド実行可否には影響しない。

***

### QUESTION 339 解説

- 問題文:
Refer to the exhibit.  
エンジニアは、ルータのコンフィグ上に平文で見えないように、暗号化されたユーザパスワードを設定しようとしている。  
どの 2 つのコマンドを使えばよいか。（2 つ選択）[file:126]

- 状況:
R1(config)# username Admin password 7 Cisco@123  
Invalid encrypted password: Cisco@123  

`password 7` は「既に暗号化された文字列」を入力する形式であり、`Cisco@123` のような平文を直接指定するとエラーになる。[file:79]

- 選択肢:
A. username Admin password 5 Cisco@123  
B. username Admin password Cisco@123  
C. password encryption aes  
D. username Admin secret Cisco@123  
E. no service password-encryption  
F. service password-encryption  

- 正解: **D, F**

- 解説（なぜ D, F が正しいか）
    - `username Admin secret Cisco@123` を使うと、パスワードは **Type 5/Type 9 のハッシュ（MD5 など）** として保存され、コンフィグ上に平文で表示されない。[file:79]  
      → これが選択肢 **D**。
    - さらに `service password-encryption` を有効化すると、`password` 形式で保存される他のパスワード（line vty, console, enable password など）も **Type 7 で暗号化されて表示**される。[file:79]  
      → これが選択肢 **F**。
    - この 2 つを組み合わせることで、「ユーザのパスワードも含め、コンフィグ上に平文パスワードを残さない」という要件を満たせる。

- 他の選択肢がダメな理由
    - A: `password 5` も「暗号化済み文字列」を期待する形式で、平文 `Cisco@123` を直接入れると無効。
    - B: `password` だけだと平文で保存され、`service password-encryption` がなければ丸見えになる。
    - C: `password encryption aes` というグローバルコマンドは存在しない（ASA など別機器の文法と混同しやすい）。
    - E: `no service password-encryption` は逆に平文表示を許してしまう。

***
### QUESTION 340 解説

- 問題文:
Refer to the exhibit.  
R6 から R1 へのトラフィックは、R6 → R5 → R2 → R1 のパスを通るべきである。  
どの操作を行えばよいか。[file:127]

- トポロジ概要:
    - 3 つのエリア:
        - Area 1: R1–R2–R3  
        - Area 0: R2–R3–R4–R5  
        - Area 2: R4–R5–R6  
    - 各リンクには OSPF コストが表示されている（20, 40, 80 など）。[file:127]

- 目的の経路:
    - R6 から R1 への希望パスは  
      R6 → R5 → R2 → R1

- なぜ調整が必要か
    - 現状では、R6 から見た R1 への最短コストが  
      R6 → R5 → R2 → R3 → R1  
      のように R3 経由になってしまっている。  
    - つまり、R2–R3 間が相対的に安く、R2–R1 への直接パスより R3 を経由したパスの合計コストが小さい状態。

- 正解: **C**

- 解説（なぜ C が正しいか）
    - R2–R3 間のコストを **61** に上げると、R2 → R3 → R1 方向のパス全体のコストが大きくなり、  
      R2 → R1 への直接パス（80 コスト）を経由する方が相対的に安くなる。
    - その結果、Area 0 内・全体トポロジで計算される SPF の結果として、  
      R6 から R1 への最短経路が **R6 → R5 → R2 → R1** となる。
    - 選択肢 C の  
      > Increase the cost to 61 between R2 and R3  
      はまさにこの調整を指しており、希望するパス選択を実現する。

- 他の選択肢がダメな理由
    - A: R6–R5–R2 間のコストを下げても、R2 から先で R3 経由が依然として安ければ、R3 経由が選ばれる可能性が残る。
    - B: 「R2–R3–R1 間すべてを 61 にする」という表現はあいまいで、実際に必要なのは R2–R3 リンク単体のコスト調整である。
    - D: R2–R1 間のコストを 41 に下げるとパスは改善される可能性はあるが、問題の意図は「R2–R3 間を高くして R3 経由パスを不利にすること」であり、公式解としては C が選ばれている。

***
### QUESTION 341 解説

- 問題文（英語）:
Refer to the exhibit. A network engineer applied a filter for LSA traffic on OSPFv3 interarea routes on the area 5 ABR to protect advertising the internal routes of area 5 to the business partner network. All other areas should receive the area 5 internal routes. After the respective route filtering configuration is applied on the ABR, area 5 routes are not visible on any of the areas. How must the filter list be applied on the ABR to resolve this issue? [file:130][file:131]

- 問題文要約（日本語）:  
OSPFv3 ネットワークで、Area 5 の ABR に「interarea ルート用の LSA フィルタ」を設定し、  
Area 20（ビジネスパートナ）には Area 5 の内部ルートを広告しないようにしたい。  
しかし適用後、Area 5 のルートがどのエリアからも見えなくなってしまった。  
この要件を満たしつつ、他のエリアからは Area 5 ルートが見えるようにするには、フィルタリストを ABR にどう適用すべきか。

- 選択肢:
A. in the "out" direction for area 20 on router R2  
B. in the "out" direction for area 5 on router R1  
C. in the "in" direction for area 20 on router R2  
D. in the "in" direction for area 5 on router R1  

- 正解: **C**

- 解説（なぜ C が正しいか）
    - 要件は「Area 5 のルートを、Area 20（ビジネスパートナ）にだけ見せない」ことであり、Area 0 や Area 10 には普通に広告したい。[file:131]
    - Area 5 と Area 0 を接続する ABR は R1。R1 で Area 5 の LSA を **out 方向でフィルタ**すると、Area 0 自体が Area 5 ルートを学習できず、その先の Area 10/20 にも一切届かなくなる（今起きている症状）。
    - そこで、Area 5 → Area 0 の広告はそのまま通し、**Area 0 → Area 20 に入る時だけフィルタ**すればよい。  
      その位置にいるのが、Area 0・10・20 の ABR である R2。
    - R2 で Area 20 に対する LSAs を **in 方向でフィルタ**すると、  
        - Area 0・Area 10 は Area 5 ルートを受け取る  
        - Area 20 のみ Area 5 ルートが入ってこない  
      という動作になり、要件を満たす。
    - これを表しているのが選択肢 C の  
      > in the "in" direction for area 20 on router R2  
      なので、正解となる。

- 他の選択肢がダメな理由
    - A: Area 20 へ出ていく「out 方向」でフィルタすると、一見よさそうに見えるが、ABR の実装上 interarea LSA の扱いが意図通りにならず、エリア間での汎用的な解決策としては不適切とされている（公式解は C）。  
    - B / D: R1 に対して in/out 方向でフィルタをかけると、Area 0 自体に Area 5 ルートが流れず、結果としてどのエリアからも Area 5 ルートが見えなくなる。

***
### QUESTION 342 解説

- 問題文（英語）:
ip sla 10  
 icmp-echo 10.1.1.10  
 timeout 2000  
 threshold 2000  
 frequency 40  
ip sla schedule 10 life forever start-time now  
!  
track 1 ip sla 10 reachability  

Refer to the exhibit. An engineer configured IP SLA to monitor a next hop on a router for reachability. When the next hop is unreachable, the router is executing tracking and failing over another route, but packet loss is experienced because the reachability is flapping. Which action resolves the issue?

- 問題文要約（日本語）:  
上記のとおり IP SLA（ICMP echo）で次ホップ 10.1.1.10 を監視し、`track 1 ip sla 10 reachability` で到達不能になったら別ルートへフェイルオーバーする設定になっている。  
しかし、到達性が「上がったり下がったり」を頻繁に繰り返し、そのたびに経路が切り替わるためパケットロスが発生している。  
このフラッピングを抑えて安定させるにはどうすればよいか。

- 選択肢:
A. Increase the frequency of the SLA probe to 60.  
B. Increase the timeout of the SLA probe to 6000.  
C. Append `delay up 0 down 0` to the track command.  
D. Append `delay up 60 down 60` to the track command.  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - `track 1 ip sla 10 reachability` だけだと、IP SLA の成功/失敗が起きるたびに即座にトラッキング状態が変わり、経路もすぐ切り替わるため、軽い瞬断でもルートがフラップしてしまう。[file:79]
    - `delay up <sec> down <sec>` を付与すると、  
        - `up`：SLA が成功し続けてからトラックを Up にするまで待つ時間  
        - `down`：SLA が失敗し続けてからトラックを Down にするまで待つ時間  
      を指定でき、**ヒステリシス** を持たせてフラッピングを防げる。
    - `delay up 60 down 60` を付けると、「連続 60 秒間成功 or 失敗が続いた場合のみ状態を切り替える」ようになり、短時間の到達性ブレではルート切り替えが起きなくなる。
    - したがって、選択肢 D  
      > Append delay up 60 down 60 to the track command.  
      が、フラッピングによるパケットロスを抑える正しい対処となる。

- 他の選択肢がダメな理由
    - A: frequency を 40→60 にしても、依然として「1 回の失敗で即 Down」なのでフラッピングは防げない。
    - B: timeout を 2000→6000 にしても、応答が遅い場合の扱いが変わるだけで、連続状態を見ているわけではない。
    - C: `delay up 0 down 0` はデフォルトと同じで、全くヒステリシスが付かないため問題は解決しない。

***
### QUESTION 343 解説

- 問題文（英語）:
Refer to the exhibit. The R2 loopback interface is advertised with RIP and EIGRP using default values. Which configuration changes make R1 reach the R2 loopback using RIP? [file:132]

- 問題文要約（日本語）:  
R2 のループバック 192.168.1.100/32 は、RIP と EIGRP の両方で R1 に広告されている（どちらもデフォルト設定）。  
現在 R1 は、より優先度の高い EIGRP 経路を使って R2 ループバックへ到達している。  
このループバックへ **RIP 経路を使って** 到達させるには、R1 でどの設定変更を行うべきか。

- 選択肢:
A.  
   R1(config)# router eigrp 1  
   R1(config-router)# distance eigrp 120 120  

B.  
   R1(config)# router rip  
   R1(config-router)# distance 100  

C.  
   R1(config)# router eigrp 1  
   R1(config-router)# distance eigrp 130 120  

D.  
   R1(config)# router rip  
   R1(config-router)# distance 90  

- 正解: **C**

- 解説（なぜ C が正しいか）
    - デフォルトの管理距離 (AD):
        - EIGRP 内部: 90  
        - RIP: 120  
      そのため、同じ宛先プレフィックスが RIP と EIGRP の両方から来た場合、**AD が小さい EIGRP (90)** が選ばれる。[file:79]
    - 目的は「RIP を使わせること」なので、R1 では **EIGRP の AD を RIP より大きく** しなければならない。
    - 選択肢 C は  
      `distance eigrp 130 120`  
      と設定し、  
        - EIGRP 内部ルートの AD = 130  
        - EIGRP 外部ルートの AD = 120  
      としている。  
      これにより、RIP の AD 120 より **EIGRP 内部 (130) が大きくなる** ため、同一宛先に対して RIP 経路が優先されるようになる。
    - こうして R1 は、R2 ループバックへの到達経路として RIP を選択する。

- 他の選択肢がダメな理由
    - A: EIGRP の AD を 120/120 にすると、RIP 120 と同値になり、どちらが選ばれるかは実装依存で RIP を確実に優先できない。
    - B: RIP の AD を 100 に下げれば、EIGRP 90 より依然として大きくはないが、**本来は 100 > 90 なので EIGRP が優先**されたまま。
    - D: RIP の AD を 90 に下げると EIGRP 内部 (90) と同値になり、やはり RIP を必ずしも優先できない（しかも問題の意図は EIGRP 側の AD を上げること）。

***
### QUESTION 344 解説

- 問題文（英語）:
Refer to the exhibit. An engineer must advertise routes into IPv6 MP-BGP and failed. Which configuration resolves the issue on R1? [file:133]

- 問題文要約（日本語）:  
図では、R1 が左側 LAN プレフィックス `2001:DB8::/64` を持ち、R1–R2 間も IPv6 で接続されている。  
R1 からこの `2001:DB8::/64` を **IPv6 MP-BGP（IPv6 unicast アドレスファミリ）** に広告したいが、うまく広告されていない。  
どの BGP 設定が R1 上で必要か。

- 選択肢:
A.  
   router bgp 64900  
    no bgp default ipv4-unicast  
    address-family ipv6 unicast  
     redistribute ospf  
     network 2001:DB9::/64  

B.  
   router bgp 65000  
    no bgp default ipv4-unicast  
    address-family ipv6 multicast  
     network 2001:DB8::/64  

C.  
   router bgp 65000  
    no bgp default ipv4-unicast  
    address-family ipv6 unicast  
     network 2001:DB8::/64  

D.  
   router bgp 64900  
    no bgp default ipv4-unicast  
    address-family ipv6 multicast  
     neighbor 2001:DB8:7000::2 translate-update ipv6 multicast  

- 正解: **C**

- 解説（なぜ C が正しいか）
    - IPv6 MP-BGP で通常のユニキャストルートを広告するには、  
      - 正しい AS 番号で `router bgp <ASN>` を定義  
      - `no bgp default ipv4-unicast` で IPv4 AF のデフォルト有効を無効化（IOS XE などの動作）  
      - `address-family ipv6 unicast` を有効にし、その中で `network` や `redistribute` を設定  
      する必要がある。[file:79]
    - トポロジ上、R1/R2 は AS 65000 内の iBGP であるため、ASN は 65000 であるべき。  
      また、広告したいのはユニキャストプレフィックス `2001:DB8::/64`。
    - 選択肢 C は  
      - BGP AS を 65000 に設定  
      - IPv6 **unicast** アドレスファミリを使用  
      - 正しいプレフィックス `2001:DB8::/64` を `network` で指定  
      しており、要件に一致する。

- 他の選択肢がダメな理由
    - A: ASN が 64900 と誤っており、さらに network が `2001:DB9::/64` と違うプレフィックスになっている。
    - B: `address-family ipv6 multicast` になっており、ユニキャストルートを広告する用途に合わない。
    - D: こちらも ASN が誤り、かつ IPv6 multicast AF と `translate-update` でマルチキャスト関連の設定になっており、要求と異なる。

***
### QUESTION 345 解説

- 問題文（英語）:
Refer to the exhibit. A network administrator must configure DMVPN tunnels between the hub and spoke with dynamic spoke-to-spoke tunnel capabilities using EIGRP. Which tunnel interface command must the network administrator configure to establish an EIGRP peer? [file:79]

- 問題文要約（日本語）:  
DMVPN（mGRE＋NHRP）でハブ＆スポーク構成を作成しており、スポーク同士も動的にトンネル（spoke-to-spoke）を張れるようにしたい。  
ルーティングには EIGRP を使用しているが、トンネル上で EIGRP ピアが正しく張れず、特に spoke-to-spoke の経路で問題が出ている。  
EIGRP ピア確立と spoke-to-spoke 通信を成立させるには、トンネルインターフェイス上でどのコマンドを設定すべきか。

- 選択肢:
A. `no ip next-hop-self eigrp 1`  
B. `no ip nhrp next-hop-self`  
C. `ip nhrp next-hop-self`  
D. `ip next-hop-self eigrp 1`  

- 正解: **A**

- 解説（なぜ A が正しいか）
    - DMVPN フェーズ 3 などで EIGRP を使う場合、ハブがスポーク宛てルートの **next-hop を自分自身に書き換えてしまう** と、スポーク同士が直接トンネルを張るための「本来の次ホップ（相手スポーク）」情報が失われる。[file:79]
    - その結果、spoke-to-spoke トンネルではなく、常に hub 経由の転送になりやすく、EIGRP のネイバー関係や経路選択に問題が出る。
    - `no ip next-hop-self eigrp 1` をトンネルインターフェイスで指定すると、ハブは EIGRP で再広告する際に next-hop を自分に書き換えなくなり、スポークは互いのルートの next-hop に **相手スポークのトンネル IP** を保持できる。  
      これにより、NHRP と組み合わせて spoke-to-spoke トンネルが確立し、EIGRP ピアリングとトラフィックの直接転送が可能になる。

- 他の選択肢がダメな理由
    - B/C: `ip nhrp next-hop-self`／`no ip nhrp next-hop-self` は NHRP のマッピング挙動に関係するが、問題のポイントは **EIGRP の next-hop-self** の抑制であり、NHRP 側ではない。
    - D: `ip next-hop-self eigrp 1` はその逆で、ハブが積極的に next-hop を自分に書き換えてしまうため、spoke-to-spoke トンネルをさらに阻害してしまう。

***
### QUESTION 346 解説

- 問題文（英語）:
access-list 120 permit tcp any gt 1024 eq bgp log  
access-list 120 permit tcp any eq bgp gt 1024 established  
access-list 120 permit tcp any gt 1024 eq 639  
access-list 120 permit tcp any eq 639 gt 1024 established  
access-list 120 permit tcp any eq 646  
access-list 120 permit udp any eq 646  
access-list 120 permit ospf any  
access-list 120 permit ospf any host 224.0.0.5  
access-list 120 permit ospf any host 224.0.0.6  
access-list 120 permit eigrp any  
access-list 120 permit eigrp any host 224.0.0.10  
access-list 120 permit udp any any eq pim-auto-rp  

Refer to the exhibit. The control plane is heavily impacted after the above CoPP configuration is applied to the router. Which command removal lessens the impact on the control plane? [file:79]

- 問題文要約（日本語）:  
上記の ACL 120 を CoPP（Control Plane Policing）で使用したところ、コントロールプレーンへのトラフィック負荷が大きくなってしまった。  
ACL のどのエントリを削除すれば、コントロールプレーンへの影響をもっとも軽減できるか。

- 選択肢:
A. `access-list 120 permit udp any any eq pim-auto-rp`  
B. `access-list 120 permit tcp any gt 1024 eq bgp log`  
C. `access-list 120 permit ospf any`  
D. `access-list 120 permit eigrp any host 224.0.0.10`  

- 正解: **D**

- 解説（なぜ D が正しいか）
    - 前提として、ACL 中で宛先が省略されている行は `any` 宛てとみなされる。したがって、  
      - `access-list 120 permit ospf any` は「OSPF パケットを任意宛先に対して許可」  
      - `access-list 120 permit eigrp any` は「EIGRP パケットを任意宛先に対して許可」  
      となる。[file:79]
    - そのうえで  
      - `access-list 120 permit ospf any host 224.0.0.5`  
      - `access-list 120 permit ospf any host 224.0.0.6`  
      - `access-list 120 permit eigrp any host 224.0.0.10`  
      は、すでにより広い条件で許可されているトラフィックの**サブセット**であり、完全に冗長なエントリとなる。
    - CoPP では ACL のエントリが多いほどマッチ処理のオーバーヘッドが増え、コントロールプレーンに余計な負荷がかかる。  
      冗長なエントリを削除することで、マッチ処理を減らし影響を軽減できる。
    - 選択肢の中で削除候補として挙がっている冗長エントリは  
      `access-list 120 permit eigrp any host 224.0.0.10`  
      のみなので、D が正解となる。

- 他の選択肢がダメな理由
    - A: PIM Auto-RP 用の UDP トラフィックはマルチキャスト制御に必要で、冗長ではない。
    - B: BGP トラフィックをログ付きで許可する重要なエントリであり、これを消すと BGP の制御トラフィックが制限されてしまう。
    - C: `access-list 120 permit ospf any` を消すと、OSPF の制御トラフィック自体が CoPP で落とされる可能性があり、ルーティングに影響する。

***
### QUESTION 347 解説

- 問題文（英語）:
Which two components are required for MPLS Layer 3 VPN configuration? (Choose two.) [file:79]

A. Use a unique RD per customer VRF  
B. Use OSPF between PE and CE  
C. Use MP-BGP for customer routes  
D. Use pseudowire for Layer 2 routes  
E. Use LDP for customer routes  

- 問題文要約（日本語）:  
MPLS L3 VPN を構成するうえで、「必須」となるコンポーネントを 2 つ選びなさい、という問題。

- 正解: **A, C**

- 解説（なぜ A と C が正しいか）
    - MPLS L3 VPN では、**各顧客ごとに VRF を分離**し、その VRF 内の経路を MPLS コアで運ぶために **VPNv4 アドレス（32bit IPv4 + 64bit RD）** を使う。  
      そのため、通常は「顧客ごと（あるいは VRF ごと）に一意な RD (Route Distinguisher)」を設定する必要がある。[file:79]
    - PE 間で各 VRF の経路をやり取りする際には、**MP-BGP (Multiprotocol BGP)** の VPNv4 アドレスファミリを使って VRF ルートを広告する。  
      これにより、顧客同士で重複した IPv4 アドレスを持っていても、RD により区別され、正しく PE 間で配送される。[file:79]
    - よって  
      - A: 「Use a unique RD per customer VRF」  
      - C: 「Use MP-BGP for customer routes」  
      の 2 つが、MPLS L3 VPN を成り立たせるための必須要素となる。

- 他の選択肢がダメな理由
    - B: PE–CE 間で使う IGP/EGP は EIGRP/OSPF/BGP/静的ルートなど自由に選べるため、OSPF 固定ではない。
    - D: Pseudowire は L2VPN（VPWS, VPLS など）の技術であり、L3 VPN の必須要素ではない。
    - E: LDP は「ラベル配布」で MPLS コアの LSP を作る用途に使うが、「customer routes」を運ぶためのプロトコルではなく、MP-BGP が担当する。

***
### QUESTION 348 解説

- 問題文（英語）:
A customer reports that traffic is not passing on an EIGRP enabled multipoint interface on a router configured as below:

interface Serial0/0/0  
 no ip address  
interface Serial0/0/0.9 multipoint  
 ip address 10.1.1.1 255.255.255.248  
 ip split-horizon eigrp 1  

Which action resolves the issue?

A. Disable poison reverse.  
B. Enable split horizon  
C. Enable poison reverse  
D. Disable split horizon  

- 問題文要約（日本語）:  
上記のように EIGRP を有効にしたマルチポイントサブインターフェイス上で、トラフィックが通らないと報告されている。  
`ip split-horizon eigrp 1` が設定されている状態で、どの操作をすればトラフィックが流れるようになるか。

- 正解: **D**

- 解説（なぜ D が正しいか）
    - フレームリレーや NBMA 環境で「マルチポイント」インターフェイスを使い、そこに複数の EIGRP ネイバー（疑似ハブ＆スポーク）をぶら下げる場合、**split horizon を有効にしていると、学習したルートを同じインターフェイスから再広告できない。**  
      そのため、スポーク間の到達性が失われ、結果として「トラフィックが通らない」状態になる。
    - このときの対処は、マルチポイントインターフェイスで **split horizon を無効化** すること。  
      コマンドとしてはインターフェイス上で  
      `no ip split-horizon eigrp 1`  
      を設定する。
    - 選択肢 D「Disable split horizon」がこれを意味しており、EIGRP が同じマルチポイントインターフェイス上の他の隣接へルートを再広告できるようになり、トラフィックが通るようになる。

- 他の選択肢がダメな理由
    - A / C: poison reverse の有効・無効は、このようなマルチポイント上の再広告制限の問題とは関係がない。
    - B: すでに `ip split-horizon eigrp 1` が設定されているので、有効化ではなく **無効化** が必要。

***
### QUESTION 349 解説

- 問題文（英語）:
Refer to the exhibit. A network administrator notices these console messages from host 10.11.110.12 originating from interface E1/0. The administrator considers this an unauthorized attempt to access SNMP on R1. Which action prevents the attempts to reach R1 E1/0?

A. Configure IOS control plane protection using ACL 90 on interface E1/0  
B. Add a permit statement including the host 10.11.110.12 into ACL 90  
C. Configure IOS management plane protection using ACL 90 on interface E1/0.  
D. Create an inbound ACL on interface E1/0 to deny SNMP from host 10.11.110.12  

- 問題文要約（日本語）:  
ホスト 10.11.110.12 から、R1 のインターフェイス E1/0 宛てに SNMP アクセスの試行があり、コンソールにメッセージが出ている。  
これを「不正な SNMP アクセス」とみなし、E1/0 宛てのその試行を止めたい。どの設定を行うべきか。

- 正解: **D**

- 解説（なぜ D が正しいか）
    - 目的は「特定ホスト 10.11.110.12 からの SNMP（UDP 161/162）アクセスを R1 の E1/0 で止める」こと。  
    - もっともシンプルで確実な方法は、**インターフェイス E1/0 に着信 ACL（inbound ACL）を適用し、そのホストからの SNMP を deny する**こと。  
      例:  
      `access-list 101 deny udp host 10.11.110.12 any eq snmp`  
      `access-list 101 permit ip any any`  
      `interface E1/0`  
      ` ip access-group 101 in`
    - これにより、E1/0 に入ってくる 10.11.110.12 からの SNMP パケットはドロップされ、R1 の SNMP エージェントやコントロールプレーンに届かなくなる。
    - 選択肢 D の  
      > Create an inbound ACL on interface E1/0 to deny SNMP from host 10.11.110.12  
      はこの方針をそのまま表しており、要件に合致する。

- 他の選択肢がダメな理由
    - A / C: コントロールプレーン／マネジメントプレーン保護 (CoPP/MPP) は `control-plane` コンテキストで設定するものであり、単に「ACL 90 をインターフェイスに適用」ではないため、表現として不正確。
    - B: 「permit 10.11.110.12」を ACL に追加すると、むしろそのホストからの SNMP アクセスを許可してしまうので、要件と逆になる。

***
### QUESTION 350 解説

- 問題文（英語）:
Refer to the exhibit. During ISP router maintenance, the network produced many alerts because of the flapping interface. Which configuration on R1 resolves the issue? [file:79]

A. snmp trap ip verify drop-rate  
B. ip verify drop-rate notify hold-down 60  
C. no snmp trap link-status  
D. snmp trap link-status down  

- 問題文要約（日本語）:  
ISP 側ルータのメンテナンス中、R1 から接続されているインターフェイスが頻繁に up/down を繰り返し、そのたびに多数の SNMP アラートが発生した。  
この「インターフェイスのフラッピングによる大量のリンクアップ／ダウン Trap」を抑えたい。R1 ではどの設定を行うべきか。

- 正解: **C**

- 解説（なぜ C が正しいか）
    - SNMP でリンクアップ／ダウンの Trap を送信する動作は、`snmp trap link-status` によって有効になっている。[file:79]
    - これを抑制するには、その逆のコマンドである  
      `no snmp trap link-status`  
      を設定し、「インターフェイスのリンク状態変化に関する SNMP Trap の送信自体を無効化」すればよい。
    - これにより、ISP 側でインターフェイスが何度フラップしても、R1 は SNMP Trap を送信せず、監視システム側のアラートが激減する。

- 他の選択肢がダメな理由
    - A / B: `ip verify drop-rate` は IP Source Guard/ACL drop-rate など別の機能に関連しており、インターフェイスのリンク状態 Trap には関係しない。
    - D: `snmp trap link-status down` は「ダウン時の Trap 送信を明示的に有効」にする方向であり、アラートを減らしたいという目的と逆になる。

***
### QUESTION 351 解説

- 問題文（英語）:
Refer to the exhibit. The administrator is trying to overwrite an existing file on the TFTP server that was previously uploaded by another router. However, the attempt to update the file fails. Which action resolves this issue? [file:134]

A. Make the package.conf file executable by all on the TFTP server.  
B. Make sure to run the TFTP service on the TFTP server.  
C. Make the TFTP folder writable by all on the TFTP server.  
D. Make the package.conf file writable by all on the TFTP server.  

- 問題文要約（日本語）:  
すでに別のルータが TFTP サーバにアップロードした `packages.conf` ファイルを、CPE ルータから上書きしようとしたところ失敗した。  
この既存ファイルを正常に上書きできるようにするには、TFTP サーバ側でどの対応が必要か。

- 正解: **D**

- 解説（なぜ D が正しいか）
    - 端末からは `ping 10.0.2.4` が成功しており、TFTP サーバ自体への IP 到達性には問題がない。[file:134]
    - `copy flash:/packages.conf tftp://10.0.2.4/` を実行すると `Error opening tftp://10.0.2.4/packages.conf (Undefined error)` となっているが、問題文では「すでに存在するファイルを**上書き**しようとしている」と明示されている。[file:79]
    - 既存ファイルを上書きできない典型的な原因は、そのファイル自身に書き込み権限がないこと。  
      この場合、TFTP デーモンはフォルダには書き込めても、該当ファイルを開いて書き換えることができない。
    - 従って、TFTP サーバ側で `packages.conf` を「すべてのユーザが書き込み可能 (writable by all)」とすることで、ルータが TFTP を通じてファイルを上書きできるようになる。
    - 選択肢 D の  
      > Make the package.conf file writable by all on the TFTP server.  
      が、この対処をそのまま表している。

- 他の選択肢がダメな理由
    - A: 実行権（executable）は TFTP の読み書きには不要であり、上書き失敗の原因ではない。
    - B: TFTP サービスが動作していなければ「タイムアウト」エラーになるのが一般的で、ここでの「Undefined error」とは異なる。
    - C: 新規ファイルを作る場合はフォルダの書き込み権限が必要だが、「既に存在するファイルの上書き」の場合、フォルダではなく**ファイル自身の書き込み権限**が問題になる。

***
### QUESTION 352 解説

- 問題文（英語）:
A newly installed spoke router is configured for DMVPN with the `ip mtu 1400` command. Which configuration allows the spoke to use fragmentation with the maximum negotiated TCP MTU over GRE?

A. `ip tcp payload-mtu 1360`  
   `crypto ipsec fragmentation after-encryption`  

B. `ip tcp adjust-mtu 1360`  
   `crypto ipsec fragmentation mtu-discovery`  

C. `ip tcp payload-mtu 1360`  
   `crypto ipsec fragmentation mtu-discovery`  

D. `ip tcp adjust-mtu 1360`  
   `crypto ipsec fragmentation after-encryption`  

- 問題文要約（日本語）:  
新しく導入した DMVPN スポークルータで `ip mtu 1400` を設定している。  
GRE＋IPsec のオーバーヘッドを考慮しつつ、**TCP の最大 MTU を調整し、フラグメンテーションを適切に行いたい**。  
どの組み合わせの設定が正しいか。

- 正解: **D**

- 解説（なぜ D が正しいか）
    - GRE＋IPsec ではヘッダ分だけ実効 MTU が下がるため、その分を見越して **TCP の MSS/MTU を調整**しないとフラグメントやブラックホールが起こる。[file:79]
    - `ip tcp adjust-mtu <value>` は、インターフェイスで通過する TCP セッションに対し、パス MTU を考慮した MSS 調整を行うための一般的なコマンドで、DMVPN 構成でよく使われる。
    - DMVPN で IPsec フラグメントを扱う際は、`crypto ipsec fragmentation after-encryption` により **暗号化後フラグメント**を許可しておくと、トンネル MTU を超えるパケットも正しく送出できる。
    - 選択肢 D の  
      - `ip tcp adjust-mtu 1360`（MSS 調整）  
      - `crypto ipsec fragmentation after-encryption`（暗号化後のフラグメント許可）  
      の組み合わせが、GRE 上で交渉された最大 TCP MTU を保ちつつ、必要時にはフラグメントを許容する正しい設定となる。

- 他の選択肢がダメな理由
    - A / C: `ip tcp payload-mtu` は有効な IOS コマンドではなく、実際の設定として不適切。
    - B: `crypto ipsec fragmentation mtu-discovery` という組み合わせは一般的な IPsec フラグメント制御のコマンドとして正しくなく、意図通りに動作しない。

***
### QUESTION 353 解説

- 問題文（英語）:
Refer to the exhibit. Which configuration allows Router1 to reach `2005:DEAD:BEEF:DB2::/64`? [file:79]

A. Router1  
   route-map Router1V6 permit 10  
    set ipv6 next-hop 2006:DEAD:BEEF:DB1::1  
    exit  
   !  
   router bgp 65001  
    address-family ipv6  
     network 2005:DEAD:BEEF:DB1::/64  
     neighbor 10.12.12.2 activate  
     neighbor 10.12.12.2 route-map Router1V6 out  
    exit-address-family  

B. Router2  
   route-map Router2V6 permit 10  
    set ipv6 next-hop 2006:DEAD:BEEF:DB8::2  
    exit  
   !  
   router bgp 65002  
    address-family ipv6  
     network 2005:DEAD:BEEF:DB2::/64  
     neighbor 10.12.12.2 activate  
     neighbor 10.12.12.2 route-map Router2V6 out  
    exit-address-family  

C. Router2  
   route-map Router2V6 permit 10  
    set ipv6 next-hop 2006:DEAD:BEEF:DB8::2  
    exit  
   !  
   router bgp 65002  
    address-family ipv6  
     network 2005:DEAD:BEEF:DB2::/64  
     neighbor 10.12.12.2 activate  
     neighbor 10.12.12.2 route-map Router out  
    exit-address-family  

D. Router1  
   route-map Router1V6 permit 10  
    set ipv6 next-hop 2006:DEAD:BEEF:DB8::2  
    exit  
   !  
   router bgp 65001  
    address-family ipv6  
     network 2005:DEAD:BEEF:DB1::/64  
     neighbor 10.12.12.2 activate  
     neighbor 10.12.12.2 route-map Router1V6 out  
    exit-address-family  

- 問題文要約（日本語）:  
図では、Router1 (AS 65001) と Router2 (AS 65002) が IPv4 アドレス 10.12.12.0/30 で eBGP ピアになっており、  
- Router1: `2005:DEAD:BEEF:DB1::/64`（自ルータの IPv6 プレフィックス）  
- Router2: `2005:DEAD:BEEF:DB2::/64`（R2 の Loopback0）  
を持っている。[file:79]  
Router1 から `2005:DEAD:BEEF:DB2::/64` へ到達できるようにするには、どの設定が必要か。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - `2005:DEAD:BEEF:DB2::/64` は Router2 のループバックに属するアドレスであり、このプレフィックスを BGP で広告するのは Router2 の役割。[file:79]
    - eBGP では、デフォルトで **next-hop は自ピア（広告元）** になるが、IPv6 では特に next-hop の扱いに注意が必要で、図の構成では R1 が受け取る経路の next-hop を正しく解決できていない前提になっている。
    - そのため、Router2 側で `route-map` を用いて、R1 へ送る IPv6 プレフィックス `2005:DEAD:BEEF:DB2::/64` の **next-hop を Router2 自身の到達可能な IPv6 アドレス（2006:DEAD:BEEF:DB8::2）に書き換えて** 広告する必要がある。
    - 選択肢 B は Router2 において  
      - `route-map Router2V6` で `set ipv6 next-hop 2006:DEAD:BEEF:DB8::2` を指定  
      - その route-map を `neighbor 10.12.12.2 route-map Router2V6 out` で eBGP 隣接に適用  
      - かつ `network 2005:DEAD:BEEF:DB2::/64` を広告  
      しており、R1 から見て「到達可能な next-hop つきで DB2 プレフィックスを受け取れる」状態となる。

- 他の選択肢がダメな理由
    - A / D: Router1 側で route-map を out 方向に適用しているが、R1 は `2005:DEAD:BEEF:DB2::/64` の起点ではないため、ここで next-hop をいじっても R2 から DB2 プレフィックスが正しく来なければ意味がない。
    - C: ルートマップ名として `Router2V6` を定義しているのに、隣接には `route-map Router out` を適用しており、名前が一致していないため設定ミスとなる。[file:79]

***
### QUESTION 354 解説

- 問題文（英語）:
What are the two goals of micro BFD sessions? (Choose two.) [file:79]

A. Each member link of a link aggregation group must run BFD  
B. Any member link on a link aggregation group must run BFD  
C. The high bandwidth member link of a link aggregation group must run BFD  
D. Continuity for each member link of a link aggregation group must be verified  
E. Run the BFD session with 3x3 ms hello timer.  

- 問題文要約（日本語）:  
ポートチャネル（LAG）で使われる **micro BFD セッション** の目的として正しいものを 2 つ選べ、という問題。

- 正解: **A, D**

- 解説（なぜ A と D が正しいか）
    - micro BFD は、ポートチャネル単位ではなく **各メンバリンク単位で BFD セッションを張る** 仕組みで、LACP/非LACP いずれの port-channel でも利用できる。[file:79]
    - 各メンバリンクごとに独立した BFD セッションを持ち、リンク故障を検出したらそのメンバを LAG の転送テーブルから外すことで、より高速かつきめ細かい障害検出を実現する。[file:79]
    - したがって目的は:
        - A: 「各メンバリンクごとに BFD セッションを動作させる」  
        - D: 「各メンバリンクの連続性（リンク状態）を検証する」  
      の 2 つが正しい。

- 他の選択肢がダメな理由
    - B: 「任意のメンバリンクだけで BFD を動かす」では、micro BFD の“全メンバ監視”という目的を満たさない。
    - C: 帯域の高いリンクだけを対象にする、という仕様はない。
    - E: 3×3ms という特定のタイマー値は問題文にはなく、micro BFD の一般的な“目的”ではなく単なる設定例に過ぎない。

***
### QUESTION 355 解説

- 問題文（英語）:
What action is performed for untagged outgoing labels in an MPLS router? [file:79]

A. Convert the incoming MPLS packet to an untagged packet and then do a FIB lookup.  
B. Convert the untagged packet to a labeled packet and forward it to the next router.  
C. Convert the incoming MPLS packet to an IP packet and forward it to the next router.  
D. Convert the incoming MPLS packet to an untagged packet and then do a RIB lookup.  

- 問題文要約（日本語）:  
MPLS ルータで「outgoing label が Untagged の場合」、受信した MPLS パケットに対してどのような処理が行われるか。

- 正解: **C**

- 解説（なぜ C が正しいか）
    - MPLS の LFIB で「outgoing label = Untagged」となっているエントリは、**MPLS ドメインから IP ドメインへの境界**で使われる。[file:79]
    - この場合、ルータは「受信した MPLS パケットからラベルスタックをすべて外し、純粋な IP パケットとして次ホップへ転送」する。  
      つまり「MPLS → IP 変換をして、そのままフォワーディング」する動作であり、選択肢 C の  
      > Convert the incoming MPLS packet to an IP packet and forward it to the next router.  
      に相当する。

- 他の選択肢がダメな理由
    - A/D: 「untagged packet」という表現と FIB/RIB ルックアップを分けて書いているが、Untagged の定義上、**MPLS を剥がして IP として転送する**ことがポイントで、余計な区別をしていない C が正しい。
    - B: ここで扱うのは「incoming MPLS → outgoing untagged」のケースであり、「untagged → labeled」ではない。

***
### QUESTION 356 解説

- 問題文（英語）:
What is a characteristic of an MPLS LSP tunnel? [file:79]

A. bidirectional tunnel  
B. unidirectional tunnel  
C. hop-by-hop tunnel  
D. secured bidirectional tunnel  

- 問題文要約（日本語）:  
MPLS の LSP (Label Switched Path) トンネルの特徴として正しいものを 1 つ選べ、という問題。

- 正解: **C**

- 解説（なぜ C が正しいか）
    - MPLS の LSP は、**各ホップのラベルスイッチルータ (LSR) が、LFIB に基づいて「ラベルを見て次のホップへ転送」する**ことで成り立っている。[file:79]
    - このため、パケットは IP のように宛先アドレスに基づくルックアップではなく、「各ホップごと (hop-by-hop) にラベルに基づき転送されるトンネル」のように振る舞う。  
      この性質から、LSP はしばしば「MPLS トンネル」とも呼ばれる。
    - 以上より、  
      > hop-by-hop tunnel  
      を選んだ C が正解となる。

- 他の選択肢がダメな理由
    - A/B/D: LSP 自体は **方向性**としては片方向のパス（unidirectional）だが、選択肢 C ほど正確に特徴を表していないうえ、D のような「secured」などの性質は LSP に必須ではない（IPsec など別機能が必要）。

***
### QUESTION 357 解説

- 問題文（英語）:
Refer to the exhibit. Routers R1, R2, R3, and R4 use EIGRP. However, traffic always prefers R1–R5 backup links in non‑failure scenarios. Which configuration resolves the issue? [file:79]

A.  
   R1(config)# interface GigabitEthernet0/0/0  
   R1(config-if)# bandwidth 10000000  

B.  
   R1(config)# no ip route 10.40.10.0 255.255.255.252 10.10.20.2  
   R1(config)# ip route 10.40.10.0 255.255.255.252 10.10.20.2 115  

C.  
   R1(config)# no ip route 10.40.10.0 255.255.255.252 10.10.20.2  
   R1(config)# ip route 0.0.0.0 0.0.0.0 10.10.10.2  

D.  
   R1(config)# interface GigabitEthernet0/0/0  
   R1(config-if)# bandwidth 10000  

- 問題文要約（日本語）:  
R1〜R4 は EIGRP を実行しているが、本来は R1–R2–R3–R4 側がメイン、R1–R5 側がバックアップであるべきところ、障害がない平常時でもトラフィックが R1–R5 側のバックアップリンクを優先してしまっている。  
この挙動を修正して、R5 経由は「バックアップ専用」にするには、どの設定を R1 に入れるべきか。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - ルーティングテーブル上、R1 にはネットワーク `10.40.10.0/30`（R4-R5 セグメント）向けの **スタティックルート** が  
      `ip route 10.40.10.0 255.255.255.252 10.10.20.2`  
      のように設定されており、AD はデフォルト 1 となる。[file:79]
    - 一方、EIGRP から学習する同一宛先の経路は AD 90（外部なら 170）のため、**AD 1 のスタティックルートが常に優先**され、結果として平常時でも R5 経由が使われてしまう。
    - このスタティックルートを「バックアップ用」にするには、EIGRP（90）や OSPF（110）より **大きな AD** を設定して「フローティングスタティック」にすればよい。  
      例として AD 115 を指定すると、  
      - EIGRP/OSPF 経路があるとき: それらが優先され、R5 へのスタティックはバックアップに退く  
      - EIGRP/OSPF 経路が落ちたとき: AD 115 のスタティックが有効になり、R5 経由にフェイルオーバー  
      という動作になる。
    - 選択肢 B は  
      - 既存スタティックを削除し (`no ip route ...`)  
      - 同じ宛先・ネクストホップで AD 115 を設定し直す  
      構成になっており、まさに意図通りの「フローティングスタティック」化を行っている。

- 他の選択肢がダメな理由
    - A / D: `bandwidth` コマンドでインターフェイス帯域を変えても、ここで優先されているのは「スタティックルート vs EIGRP」の AD 差であり、EIGRP のメトリックではないため根本原因を解決しない。
    - C: もとのスタティックを消してデフォルトルートを作っているが、バックアップリンクとして `10.40.10.0/30` 向けの明示的な経路を残していないため、設計意図と異なる。

***
### QUESTION 358 解説

- 問題文（英語）:
Refer to the exhibit. An engineer cannot determine the time of the problem on R1 due to a mismatch between the router local clock and logs. Which command synchronizes the time between new log entries and the local clock on R1? [file:79]

A. service timestamps log datetime msec show-timezone  
B. service timestamps debug datetime localtime msec  
C. service timestamps debug datetime msec show-timezone  
D. service timestamps log datetime localtime msec  

- 問題文要約（日本語）:  
R1 のローカルクロックの時刻と、ログに表示される時刻が一致しておらず、いつ問題が起きたのか判断できない。  
exhibit に表示されているのは「debug コマンドの出力」であり、そのタイムスタンプをローカルクロックに合わせたい。  
どのコマンドを入れれば、以後のログの時刻とローカルクロックが同期するか。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - 画面には syslog レベル付きの通常ログではなく、`debug ...` の出力だけが表示されているため、「調整したいのは debug メッセージのタイムスタンプ」である。[file:79]
    - `service timestamps debug datetime localtime msec` は、  
        - debug メッセージに対して  
        - ルータのローカルタイム（タイムゾーン・夏時間などを反映）を使い  
        - ミリ秒単位の datetime 形式で  
      タイムスタンプを付与するコマンド。
    - これにより、今後出力される debug ログの時刻と、`show clock` などで確認できるローカルクロックの表示が一致するようになるため、問題の発生時間を正確に突き合わせられる。

- 他の選択肢がダメな理由
    - A / D: `service timestamps log ...` は **syslog メッセージ**用であり、debug 出力には効かない。
    - C: debug 用ではあるが `localtime` を付けていないため、タイムゾーンを考慮したローカル時間と完全には一致しない。

***
### QUESTION 359 解説

- 問題文（英語）:
Refer to the exhibit. Which action installs routes into the routing table of R1 and R3? [file:79]

A. BGP Synchronization has been enabled. Disable synchronization.  
B. BGP weight is set to 0. Configure a higher weight for these routes.  
C. IGP does not have a route to R4. Rectify route reachability.  
D. BGP has not been redistributed into the IGP. Redistribute BGP.  

- 問題文要約（日本語）:  
R1 と R3 では BGP の経路は受信できているが、その経路がルーティングテーブルにインストールされていない。  
この状況を修正し、R1/R3 のルーティングテーブルに BGP ルートを載せるには、どの対応が必要か。

- 正解: **C**

- 解説（なぜ C が正しいか）
    - exhibit では、R3 の BGP テーブルにはプレフィックスが見えているものの、その next-hop が **10.11.11.1（R4 側アドレス）** になっている。[file:79]
    - ところが、R3 のルーティングテーブルには 10.11.11.1 への到達経路が存在せず、**next-hop 未到達のため BGP ルートが RIB にインストールされない** 状態になっている。
    - BGP ルートが RIB に入る条件の 1 つは「next-hop が IGP などで到達可能であること」なので、まず IGP などで R3 から R4（10.11.11.1）までの経路を学習させる必要がある。
    - よって、選択肢 C の  
      > IGP does not have a route to R4. Rectify route reachability.  
      を行い、IGP に R4 へのルートを追加すれば、BGP ルートが R1/R3 のルーティングテーブルにインストールされる。

- 他の選択肢がダメな理由
    - A: 近年の IOS では BGP synchronization はデフォルト無効であり、このケースで「BGP テーブルにはあるが RIB に載らない」主因ではない。
    - B: weight は「複数 BGP 経路間の優先度」を決める属性であり、ルートが RIB に入る／入らない条件とは無関係。
    - D: BGP を IGP に再配送していないことが問題なのではなく、「BGP next-hop の到達性」が欠けていることが原因。

***
### QUESTION 360 解説

- 問題文（英語）:
Refer to the exhibit. Router R2 should be learning the route for 10.123.187.0/24 via EIGRP. Which action resolves the issue without introducing more issues? [file:79]

A. Use distribute-list to filter the external routes in OSPF.  
B. Redistribute the route in EIGRP with metric, delay, and reliability.  
C. Remove route redistribution in R2 for this route in OSPF.  
D. Use distribute-list to modify the route as an internal EIGRP route.  

- 問題文要約（日本語）:  
R2 はネットワーク 10.123.187.0/24 を **EIGRP 経由で学習してほしい** が、実際には OSPF の経路がルーティングテーブルに入っており、EIGRP ルートはトポロジテーブルにだけ存在して RIB にインストールされていない。  
他に悪影響を出さずに、この経路を EIGRP から学習させるにはどうすべきか。

- 状況整理:
    - R2 の **ルーティングテーブル** には 10.123.187.0/24 の **OSPF external ルート** が AD 110 でインストールされている。  
    - **EIGRP トポロジテーブル** には同じプレフィックスがあるが、`0 successors, FD is Inaccessible` となっており、ルーティングテーブルには採用されていない。[file:79]
    - EIGRP external の AD は 170 のため、OSPF external (110) に負けているのが原因と考えられる。

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 求めたい状態は「R2 では 10.123.187.0/24 を EIGRP から学習し、OSPF 版はローカル RIB に入れないこと」。  
    - ルーティングテーブルに入る経路は「同一プレフィックスに対して AD が最も小さいもの」なので、OSPF external を **R2 の RIB に入らないようにする**必要がある。
    - ここで有効なのが、**OSPF プロセス側で distribute-list を使って特定ルートをフィルタし、ローカルルータの RIB へのインストールのみ止める**方法。  
      distribute-list は LSA の生成・受信自体には影響を与えず、そのルータのルーティングテーブルへの反映だけを制御できる。[file:79]
    - 選択肢 A の  
      > Use distribute-list to filter the external routes in OSPF.  
      により、R2 は OSPF 外部ルート 10.123.187.0/24 を RIB に持たなくなり、代わりに EIGRP external（AD 170）がルーティングテーブルへインストールされる。

- 他の選択肢がダメな理由
    - B: EIGRP のメトリック値を変えても、AD 170 という事実は変わらないため、OSPF external (AD 110) に勝てない。
    - C: 「R2 での OSPF への再配送をやめる」だけでは、すでに他ルータから来ている OSPF external 10.123.187.0/24 を止められない。さらに他プレフィックスへの影響も読みにくい。
    - D: distribute-list で「EIGRP external を internal に書き換える」ような動作はできず、AD の違いという根本問題も解決しない。

***
### QUESTION 361 解説

- 問題文（英語）:
While examining log messages from a router, an administrator sees this error message:  

`%DUAL-5-NBRCHANGE: IP-EIGRP(0) 1: Neighbor 10.4.1.5 (Ethernet1/0) is down: K-value mismatch`  

Both routers are using default values. Which action resolves this issue?

A. Configure metric weights between the peers to calculate the valid K-value.  
B. Resolve metric differences between the peers to calculate the valid K-value.  
C. Upgrade the peer device IOS to support the goodbye message that establishes adjacency.  
D. Match authentication parameters to support the goodbye message that establishes adjacency.  

- 問題文要約（日本語）:  
ログに「Neighbor is down: K-value mismatch」と出ているが、両ルータとも EIGRP の K 値はデフォルトのままのはず。  
この場合、どの対応を行えば問題が解決するか。

- 正解: **C**

- 解説（なぜ C が正しいか）
    - 通常「K-value mismatch」が出るのは、EIGRP メトリック計算に使う K 値 (K1〜K5) が両ルータで異なるとき。[file:79]
    - しかし **両方デフォルト** にもかかわらずこのメッセージが出る場合、原因としてよくあるのが **EIGRP graceful shutdown（Goodbye メッセージ）機能**。  
      この機能では、ルータがシャットダウンする際に「K 値を 255 にセットした Hello」を送り、隣接ルータに「意図的に K‑value mismatch を起こして adjacency を落とす」ことで、きれいに終了を知らせる。[file:79]
    - 新しい IOS では受信側もこの Goodbye メッセージを理解し、  
      `... is down: Interface Goodbye received`  
      のようなログが出るが、**古い IOS ではこれを単なる K 値不一致と誤解**し、  
      `K-value mismatch`  
      として隣接を落としてしまう。
    - したがって、問題を根本的に解消するには、古い側のルータをアップグレードして Goodbye メッセージをサポートさせるのが正解となる。  
      これを述べているのが C の  
      > Upgrade the peer device IOS to support the goodbye message that establishes adjacency.  

- 他の選択肢がダメな理由
    - A / B: 実際には K 値は両者ともデフォルトで一致しており、メトリック設定をいじる必要はない。  
    - D: 問題は認証パラメータではなく、Goodbye メッセージを理解できない古い IOS に起因している。

***
### QUESTION 362 解説

- 問題文（英語）:
Which IPv6 security feature blocks all traffic from an IPv6 host when initially connecting to a switch port except for traffic to gain an IPv6 address and source IPv6 neighbors? [file:79]

A. IPv6 RA Guard  
B. IPv6 Source Guard  
C. IPv6 Destination Guard  
D. IPv6 DHCP Guard  

- 問題文要約（日本語）:  
ホストがスイッチポートに最初に接続したとき、IPv6 アドレス取得や近隣情報取得に必要なトラフィック（DHCPv6 / NDP）以外の IPv6 トラフィックをすべてブロックする機能はどれか。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - **IPv6 Source Guard** は、スイッチポートに流入する IPv6 パケットを、IPv6 スヌーピング（DHCPv6・NDP）で学習した **IPv6 バインディングテーブル** と照合してチェックする「入力方向のフィルタ機能」。[file:79]
    - ホストが接続した直後は、まだその MAC / IP / ポート情報がバインディングテーブルに登録されていないため、Source Guard は  
      - DHCPv6 や NDP など、バインディングテーブル作成に必要なパケットのみ許可し  
      - それ以外の IPv6 トラフィックはすべてブロックする。  
      これが設問にある動作と一致する。

- 他の選択肢がダメな理由
    - A: IPv6 RA Guard は不正な Router Advertisement をブロックする機能で、ホストからの全トラフィックを初期に遮断するものではない。
    - C: IPv6 Destination Guard は「宛先 IPv6 アドレスがバインディングテーブルに存在しない場合の NDP 解決をブロック」する機能で、送信元側の初期トラフィック制御とは異なる。
    - D: IPv6 DHCP Guard は不正な DHCPv6 サーバからの応答をブロックする機能であり、ホストからの一般トラフィックを全面ブロックするものではない。

***
### QUESTION 363 解説

- 問題文（英語）:
ip prefix-list DMZ-STATIC seq 5 permit 10.1.1.0/24  
!  
route-map DMZ permit 10  
 match ip address prefix-list DMZ-STATIC  
!  
router ospf 1  
 network 0.0.0.0 0.0.0.0 area 0  
 redistribute static route-map DMZ  
!  
ip route 10.1.1.0 255.255.255.0 10.20.20.1  

Refer to the exhibit. The static route is not present in the routing table of an adjacent OSPF neighbor router. Which action resolves the issue? [file:79]

A. Configure the subnets keyword in the redistribution command.  
B. Configure a permit 20 statement to the route map to redistribute the static route.  
C. Configure the next-hop interface at the end of the static route for it to get redistributed.  
D. Configure the next hop of 10.20.20.1 in the prefix list DMZ-STATIC.  

- 問題文要約（日本語）:  
上記のように、プレフィックスリストとルートマップを使って静的ルート `10.1.1.0/24` を OSPF に再配布しているが、隣接の OSPF ルータのルーティングテーブルにこの経路が現れない。  
どの設定を追加すれば、この静的ルートが OSPF へ正常に再配布されるか。

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 静的ルート:  
      `ip route 10.1.1.0 255.255.255.0 10.20.20.1` はクラスフル的には 10.0.0.0/8 の「サブネット」にあたる。  
    - 古い IOS では `redistribute static` だけだと「クラスフルネットワークのみ」がデフォルトで再配布され、**クラスフル範囲内のサブネット（/24 など）は除外される**。[file:79]
    - これを明示的に含めるには、OSPF の `redistribute` に `subnets` を付けて  
      `redistribute static subnets route-map DMZ`  
      のように指定する必要がある。
    - したがって、選択肢 A の  
      > Configure the subnets keyword in the redistribution command.  
      が、静的ルート `10.1.1.0/24` を OSPF に広告させる正しい修正となる。

- 他の選択肢がダメな理由
    - B: すでに `route-map DMZ permit 10` で対象プレフィックスを許可しており、追加の seq 20 は不要。
    - C: 再配布の可否は「スタティックであること」と「ルートマップでマッチすること」に依存し、次ホップが IP かインターフェイスかは関係ない。
    - D: プレフィックスリストは宛先プレフィックスをマッチするものであり、ネクストホップアドレス 10.20.20.1 を書いても意味がない。

***
### QUESTION 364 解説

- 問題文（英語）:
R1#
 router ospf 1
  redistribute rip subnets
  network 132.108.1.0 0.0.0.255 area 2
  network 132.108.2.0 0.0.0.255 area 2
  distribute-list 1 out
 !
 access-list 1 permit 132.108.4.0 0.0.0.255

Refer to the exhibit. The R1 OSPF neighbor is not receiving Type 5 external LSAs for 132.108.2.0/24 and 132.108.3.0/24 networks. Which configuration command resolves the issue? [file:79]

A. access-list 1 permit 132.108.0.0 0.0.3.255  
B. access-list 1 permit 132.108.0.0 0.0.1.255  
C. access-list 1 permit 132.108.4.0 0.0.3.255  
D. access-list 1 permit 132.108.2.0 0.0.0.255  

- 問題文要約（日本語）:  
R1 は RIP を OSPF に `redistribute rip subnets` で再配布しており、`distribute-list 1 out` によって外部ルートの再配布を ACL 1 で制御している。  
しかし OSPF の隣接ルータには、RIP ネットワーク 132.108.2.0/24 と 132.108.3.0/24 の Type 5 LSA が届いていない。  
どの ACL 設定に変えれば、この 2 つのネットワークも再配布されるか。

- 正解: **A**

- 解説（なぜ A が正しいか）
    - `distribute-list 1 out` は「OSPF に再配布する外部ルートを、ACL 1 でフィルタする」ために使われる。[file:79]
    - 現在の ACL は  
      `access-list 1 permit 132.108.4.0 0.0.0.255`  
      だけなので、132.108.4.0/24 以外の RIP ルート（132.108.2.0/24 と 132.108.3.0/24）は **再配布時に deny されている**。
    - 132.108.0.0〜132.108.3.255 をまとめて許可すれば、/24 サブネット 4 つ
        - 132.108.0.0/24  
        - 132.108.1.0/24  
        - 132.108.2.0/24  
        - 132.108.3.0/24  
      をすべてマッチできる。
    - それを実現するワイルドカードが  
      `132.108.0.0 0.0.3.255`  
      であり、選択肢 A がこれを指定している。

- 他の選択肢がダメな理由
    - B: `0.0.1.255` では 132.108.0.0/24 と 132.108.1.0/24 しか含められず、問題の 2.0/24 と 3.0/24 が依然として除外される。
    - C: 132.108.4.0〜7.255 を許可するだけで、やはり 2.0/24 と 3.0/24 は含まれない。
    - D: 132.108.2.0/24 だけを許可しており、132.108.3.0/24 が依然として再配布されない。

***
### QUESTION 365 解説

- 問題文（英語）:
Which two functions are the functions of LDP? (Choose two.) [file:79]

A. uses TCP for targeted LDP session  
B. uses P2P VPWS services  
C. uses pseudowire signaling  
D. uses MP2MP VPLS services  
E. used for MPLS-TE Fast Reroute  

- 問題文要約（日本語）:  
LDP (Label Distribution Protocol) の機能として正しいものを 2 つ選べ。

- 正解: **A, C**

- 解説（なぜ A と C が正しいか）
    - LDP は MPLS ネットワークで **ラベルを配布するためのコントロールプレーン・プロトコル** で、TCP/UDP 646 を使用する。[file:79]
        - 通常の隣接 LSR 間の LDP セッションに加え、ルータ同士が IP で離れている場合に **targeted LDP セッション** を張ることができ、このときも TCP を使用する。  
          → A「uses TCP for targeted LDP session」は正しい。
    - また、**MPLS Layer 2 VPN（pseudowire）** では、VC ラベルや PW ステータスをやり取りする「pseudowire signaling」に LDP (tLDP) が使われる。  
      → C「uses pseudowire signaling」も正しい。[file:79]

- 他の選択肢がダメな理由
    - B: P2P VPWS（point-to-point L2VPN）というサービス自体は LDP ベースの pseudowire で実現されることが多いが、「LDP の機能」というよりサービス種別であり、設問の文脈では C の方が正確。
    - D: MP2MP VPLS など VPLS の制御には BGP や LDP など複数方式があるが、「LDP の主な機能」として問われているのは label/pseudowire signaling で、C が代表。
    - E: MPLS-TE Fast Reroute のシグナリングには RSVP-TE が使われ、LDP は直接関与しない。

***
### QUESTION 366 解説

- 問題文（英語）:
A customer requested a GRE tunnel through the provider network between two customer sites using loopback to hide internal networks. Which configuration on R2 establishes the tunnel with R1? [file:79]

A.  
 R2(config)# interface Tunnel1  
 R2(config-if)# ip address 172.20.1.2 255.255.255.0  
 R2(config-if)# ip mtu 1500  
 R2(config-if)# ip tcp adjust-mss 1360  
 R2(config-if)# tunnel source 192.168.20.1  
 R2(config-if)# tunnel destination 10.10.1.1  

B.  
 R2(config)# interface Tunnel1  
 R2(config-if)# ip address 172.20.1.2 255.255.255.0  
 R2(config-if)# ip mtu 1400  
 R2(config-if)# ip tcp adjust-mss 1360  
 R2(config-if)# tunnel source 192.168.20.1  
 R2(config-if)# tunnel destination 192.168.10.1  

C.  
 R2(config)# interface Tunnel1  
 R2(config-if)# ip address 172.20.1.2 255.255.255.0  
 R2(config-if)# ip mtu 1400  
 R2(config-if)# ip tcp adjust-mss 1360  
 R2(config-if)# tunnel source 10.10.2.2  
 R2(config-if)# tunnel destination 10.10.1.1  

D.  
 R2(config)# interface Tunnel1  
 R2(config-if)# ip address 172.20.1.2 255.255.255.0  
 R2(config-if)# ip mtu 1500  
 R2(config-if)# ip tcp adjust-mss 1360  
 R2(config-if)# tunnel source 10.10.2.2  
 R2(config-if)# tunnel destination 10.10.1.1  

- 問題文要約（日本語）:  
プロバイダ網をまたいで、2 拠点間に GRE トンネルを張りたい。内部ネットワークを隠すために loopback アドレスをトンネルの終端に使う設計になっている。  
R1 側はループバック `192.168.10.1` を、R2 側は `192.168.20.1` をトンネルの source/destination として使う想定。  
R1 とトンネルを張るために、R2 ではどの設定を入れるべきか。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - GRE トンネルは  
      - `tunnel source` と `tunnel destination` に **到達可能な IP アドレス**（物理 IF や loopback）を指定する必要がある。
    - 要件では「loopback を使って内部ネットワークを隠す」とあるため、トンネル終端は  
      - R1: `192.168.10.1`（Loopback）  
      - R2: `192.168.20.1`（Loopback）  
      の組み合わせが自然となる。[file:79]
    - 選択肢を見比べると:
        - A: destination が 10.10.1.1（おそらく物理アドレス）で、要件の「loopback 同士で張る」に反する。
        - C / D: source/destination とも 10.10.x.x で、これも loopback を使っていない。
        - B: `tunnel source 192.168.20.1` と `tunnel destination 192.168.10.1` の組み合わせで、要件通り loopback を使用。
    - さらに、GRE＋IPsec などを想定すると、カプセル化のオーバーヘッドを考慮して **トンネル側 MTU を 1400** にしておくのが一般的で、`ip tcp adjust-mss 1360` との組み合わせも妥当。[file:79]
    - 以上から、B が唯一、設計要件と運用上のベストプラクティスの両方を満たす構成になる。

- 他の選択肢がダメな理由
    - A: destination が 10.10.1.1 で、R1 の loopback を使っていないため要件不一致。
    - C / D: 双方とも物理 IP（10.10.2.2, 10.10.1.1）を使っており、「loopback を使って内部アドレスを隠す」という要件を満たさない。さらに D は MTU 1500 のままで、GRE/IPsec のオーバーヘッドを考慮していない。

***
### QUESTION 367 解説

- 問題文（英語）:
In a DMVPN network, the Spoke1 user observed that the voice traffic is coming to Spoke2 users via the hub router. Which command is required on both spoke routers to communicate directly to one another? [file:79]

A. ip nhrp shortcut  
B. ip nhrp redirect  
C. ip nhrp nhs multicast  
D. ip nhrp map dynamic  

- 問題文要約（日本語）:  
DMVPN 環境で、本来は Spoke1 ↔ Spoke2 間をダイレクトに通信させたいのに、音声トラフィックが常に Hub 経由になっている。  
スポーク同士が直接トンネルを張って通信できるようにするには、スポーク両方にどのコマンドを入れる必要があるか。

- 正解: **A**

- 解説（なぜ A が正しいか）
    - DMVPN Phase 3 では、次の 2 つのコマンドを組み合わせて **spoke-to-spoke ダイレクトトンネル**を実現する。[file:79]
        - Hub: `ip nhrp redirect`  
          → ハブが「よりよいネクストホップ（別のスポーク）」を示す NHRP Redirect メッセージをスポークに送れるようにする。
        - Spoke: `ip nhrp shortcut`  
          → スポークが、その Redirect 情報を基に「宛先スポークへのショートカットルート＋ダイレクトトンネル」を動的に作成できるようにする。
    - 問題文では「どのコマンドを両スポークに必要か」と聞いているため、**Spoke 側に入れるべき `ip nhrp shortcut`** が正解となる。

- 他の選択肢がダメな理由
    - B: `ip nhrp redirect` は **ハブ側** に入れるコマンドであり、両スポークには不要。
    - C: `ip nhrp nhs multicast`（正しくは `ip nhrp map multicast` など）はマルチキャストの NHRP マッピング関連で、spoke-to-spoke ショートカットとは別の目的。
    - D: `ip nhrp map dynamic` は動的マッピング関連だが、Phase 3 のショートカットルートを有効にする本質的なコマンドではない。

***
### QUESTION 368 解説

- 問題文（英語）:
Refer to the exhibit. An engineer configured NetFlow on R1, but the flows do not reach the NMS server from R1. Which configuration resolves this issue? [file:79]

A.  
 R1(config)# interface Ethernet0/1  
 R1(config-if)# ip flow monitor FlowMonitor1 input  
 R1(config-if)# ip flow monitor FlowMonitor1 output  

B.  
 R1(config)# interface Ethernet0/0  
 R1(config-if)# ip flow monitor FlowMonitor1 input  
 R1(config-if)# ip flow monitor FlowMonitor1 output  

C.  
 R1(config)# flow monitor FlowMonitor1  
 R1(config-flow-monitor)# destination 10.66.66.66  

D.  
 R1(config)# flow exporter FlowExporter1  
 R1(config-flow-exporter)# destination 10.66.66.66  

- 問題文要約（日本語）:  
R1 に NetFlow（Flexible NetFlow）が設定されているが、NMS サーバにフローが届いていない。  
exhibit の `show flow exporter` の出力を見ると、エクスポータの送信先 IP が誤っている。  
どの設定を追加・修正すれば、R1 から NMS サーバ 10.66.66.66 にフローが届くようになるか。

- 正解: **D**

- 解説（なぜ D が正しいか）
    - Flexible NetFlow では  
        - `flow record` で項目定義  
        - `flow monitor` で record＋exporter を関連付け  
        - `flow exporter` で **NMS サーバの宛先 IP/ポート** を設定  
      する。[file:79]
    - exhibit では `show flow exporter` の出力に、destination が **10.60.66.66** のように誤って設定されているため、パケットが正しい NMS (10.66.66.66) に届いていない。[file:79]
    - 直すべきは exporter の宛先 IPであり、正しいコマンドは  
      `flow exporter FlowExporter1` のサブモードで  
      `destination 10.66.66.66`  
      を設定し直すこと。
    - 選択肢 D はその修正を明示しているため、フローが正しい NMS サーバに送出されるようになり、問題が解決する。

- 他の選択肢がダメな理由
    - A / B: すでにフローモニタはインターフェイスに適用されており、問題は「どの IF に適用しているか」ではなく「exporter の宛先 IP の誤り」。
    - C: `destination` を設定できるのは `flow exporter` であり、`flow monitor` コンテキストではない（構文として不正）。

***
### QUESTION 369 解説

- 問題文（英語）:
What is an MPLS LDP targeted session?

A. LDP session established by exchanging multicast hello packets  
B. label distribution session between non-directly connected neighbors  
C. session between neighbors that are connected no more than one hop away  
D. LDP session established between LSRs by exchanging TCP hello packets  

- 問題文要約（日本語）:  
MPLS の LDP「ターゲットセッション (targeted LDP session)」とはどのようなものか。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - 通常の LDP セッションは、直結インターフェイス上で **マルチキャスト Hello（UDP 646）** をやり取りして自動検出した隣接 LSR 間で確立される。
    - 一方 **targeted LDP セッション** は、直結していないルータ同士でラベルをやり取りしたいときに、特定のリモート IP を「ターゲット」として指定して確立する **非直結ノード間の LDP セッション** である。[file:79]
    - よって  
      > label distribution session between non-directly connected neighbors  
      を述べる B が正解。

- 他の選択肢がダメな理由
    - A: これは通常の「リンクローカル LDP セッション」の説明で、targeted ではない。
    - C: 「1 ホップ以内」に限定されており、「非直結でもよい」という targeted の定義と矛盾する。
    - D: LDP の制御セッション自体は TCP 646 を使うが、「TCP Hello を交換して確立」という表現は不正確で、targeted セッションの特徴を表していない。

***
### QUESTION 370 解説

- 問題文（英語）:
What is the purpose of an OSPF sham-link?

A. to correct OSPF backdoor routing when OSPF is used as the PE-PE connection protocol in an MPLS VPN network  
B. to correct OSPF backdoor routing when OSPF is used as the PE-CE connection protocol in an MPLS VPN network  
C. to allow inter-area routing when OSPF is used as the PE-CE connection protocol in a MPLS VPN network  
D. to allow intra-area routing when OSPF is used as the PE-CE connection protocol in an MPLS VPN network  

- 問題文要約（日本語）:  
MPLS L3 VPN 環境で OSPF を使うとき、「OSPF sham-link」の目的は何か。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - MPLS L3 VPN で PE–CE 間に OSPF を使う場合、同一サイト間に  
        - MPLS VPN 経路（PE 経由）  
        - 直接のバックドアリンク（CE 間など）  
      の両方が存在すると、OSPF のコスト計算の都合で **バックドアリンクの方が優先され、MPLS VPN を迂回する** ことがある。
    - **OSPF sham-link** は、PE 間を仮想的に同一 OSPF エリア内の「プライベートリンク」として見せることで、VPN 経路のコストをバックドア経路より有利にし、**“backdoor 逆転現象”を修正するための仕組み**。[file:79]
    - これはあくまで **PE–CE 間で OSPF を使っている MPLS VPN** における話なので、選択肢 B が定義通りとなる。

- 他の選択肢がダメな理由
    - A: sham-link は PE–CE 間 OSPF 用であり、PE–PE 間プロトコル（通常は BGP/IGP）を対象としない。
    - C / D: inter-area / intra-area ルーティングを可能にする一般機能ではなく、「backdoor を修正して、VPN 経路を同一エリア内の優先パスに見せる」ことが目的。

***
### QUESTION 371 解説

- 問題文（英語）:
The network administrator configured CoPP so that all routing protocol traffic toward the router CPU is limited to 1 Mbps. All traffic that exceeds this limit must be dropped. The router is running BGP and OSPF. Management traffic for Telnet and SSH must be limited to 500 kbps.


No traffic is filtering through CoPP, which is resulting in high CPU utilization. Which configuration resolves the issue? [file:79]

A. （ACL を分割して CM-MGMT に ACL101 を使う）  
B. （service-policy を input に適用）  
C. A の修正＋B の修正を両方反映  
D. ACL100 に Telnet/SSH/OSPF すべてを追加  

- 問題文要約（日本語）:  
CoPP で  
- ルーティングプロトコル（BGP 179, OSPF）を 1 Mbps  
- 管理トラフィック（Telnet/SSH）を 500 kbps  
に制限したいが、現在どのトラフィックも CoPP で制御されておらず CPU が高負荷になっている。  
どの構成変更で問題が解決するか。

- 正解: **C**

- 解説（なぜ C が正しいか）

  現状の問題は 2 つある。[file:79]

  1. **同じ ACL100 を 2 つの class-map でマッチしている**  
     - CM-ROUTING も CM-MGMT も ACL100 を参照しているため、ルーティングと管理トラフィックを区別できない。  
     - Telnet/SSH を別 ACL（例: 101）に分け、CM-MGMT では ACL101 をマッチさせる必要がある。
  
  2. **CoPP ポリシーを control-plane の output に適用している**  
     - CoPP は「CPU へ入ってくるトラフィック」を制御するので、`control-plane service-policy input PM-COPP` と **input 方向**に適用しなければならない。

  選択肢 C はこの 2 点を両方まとめて修正している。

  - ACL を分割:
    ```text
    no access-list 100
    access-list 100 permit tcp any any eq 179
    access-list 100 permit ospf any any
    access-list 101 permit tcp any any range 22 23
    !
    class-map CM-MGMT
     no match access-group 100
     match access-group 101
    ```
  - CoPP を input に適用:
    ```text
    control-plane
     no service-policy output PM-COPP
     service-policy input PM-COPP
    ```

  これにより、  
  - BGP/OSPF は CM-ROUTING で 1 Mbps にポリシング  
  - Telnet/SSH は CM-MGMT で 500 kbps にポリシング  
  - いずれも CPU に到達する **入力トラフィック** として正しく制御されるようになる。

- 他の選択肢がダメな理由
  - A: ACL 分割だけを修正しており、CoPP を依然として `output` に適用しているため CPU へ入るトラフィックは制御されない。
  - B: ポリシーを `input` に変えるだけで、ルーティングと管理トラフィックを同じ ACL100 でマッチしたままなのでクラスが分離されず、意図通りの帯域制御にならない。
  - D: 単に ACL100 に条件を追加するだけで、2 つの class-map が同じ ACL を共有するという問題が解消されない。

***
### QUESTION 372 解説

- 問題文（英語）:
Refer to the exhibit. A network administrator is troubleshooting an OSPF adjacency issue by going through the console logs in the router, but due to an overwhelming log message stream, it is impossible to capture the problem. Which two commands reduce console log messages to relevant OSPF neighbor problem details so that the issue can be resolved? (Choose two.) [file:79]

A. debug condition ip  
B. debug condition all  
C. debug condition ospf neighbor  
D. debug condition interface  
E. debug condition session-id ADJCHG  

- 問題文要約（日本語）:  
OSPF 隣接問題を debug で調べたいが、コンソールに大量のログが流れてしまい、肝心の情報が追えない。  
`debug condition` を使って、OSPF 隣接に関係するメッセージだけに絞り込みたい。どの 2 つのコマンドを使うべきか。

- 正解: **A, D**

- 解説（なぜ A と D が正しいか）
    - `debug condition` で指定できる項目は限られており、実際に IOS で `debug condition ?` とすると次のような候補が出る（抜粋）。[file:79]
      - `interface`  
      - `ip`  
      - `mac-address`  
      - `vrf`  
      など。
    - 選択肢の中で **実在する条件指定** は  
      - A: `debug condition ip`（特定 IP に関係する debug に絞る）  
      - D: `debug condition interface`（特定インターフェイス関連の debug に絞る）  
      の 2 つのみ。
    - 例えば  
      ```text
      debug condition interface Gi0/0
      debug condition ip <neighbor-IP>
      debug ip ospf adj
      ```  
      のように組み合わせれば、「特定インターフェイス・特定ネイバー IP に関係する OSPF debug」だけが出力され、膨大なログから隣接問題に関わるメッセージだけを見られるようになる。

- 他の選択肢がダメな理由
    - B: `debug condition all` という条件は存在せず、実際の IOS の候補にも出てこない。
    - C: `debug condition ospf neighbor` という条件も存在しない（OSPF neighbor は debug コマンドの種類であって、condition ではない）。
    - E: `debug condition session-id ADJCHG` という構文もサポートされていない。

***
### QUESTION 373 解説

- 問題文（英語）:
An engineer noticed that the router log messages do not have any information about when the event occurred. Which action should the engineer take when enabling service time stamps to improve the logging functionality at a granular level?

A. Configure the debug uptime option.  
B. Configure the msec option.  
C. Configure the timezone option.  
D. Configure the log uptime option.  

- 問題文要約（日本語）:  
ログに「いつ起きたか」の情報が出ていない。`service timestamps` を有効化する際に、より細かい（粒度の細かい）時刻情報をログに付けたい。どのオプションを付けるべきか。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - `service timestamps log|debug datetime` に対して **`msec` オプション** を付けると、ログのタイムスタンプに「ミリ秒」まで表示されるようになり、非常に細かい時間解像度でイベントを追跡できる。[file:79]
      例:  
      `service timestamps log datetime localtime msec`  
    - これにより、短時間に多発するイベントや、複数ログの前後関係をより正確に確認できるため、「granular level」での解析が可能になる。

- 他の選択肢がダメな理由
    - A/D: `log uptime` や `debug uptime` は「装置起動からの経過時間」を表示するオプションで、粒度を上げる主目的には合わない。
    - C: `timezone` を設定するとローカルタイムゾーンが反映されるが、ミリ秒レベルの“細かさ”を追加するわけではない。

***
### QUESTION 374 解説

- 問題文（英語）:
interface GigabitEthernet2  
 no ip address  
 ip helper-address 192.168.255.3  
 no shutdown  
!  
interface GigabitEthernet2.10  
 encapsulation dot1Q 210  
 ip address 192.168.210.1 255.255.255.0  
 ip ospf 1 area 0  
 no shutdown  

Refer to the exhibit. With the partial configuration of a router-on-a-stick, clients in VLAN 10 on Gi2 cannot obtain IP configuration from the central DHCP server. The DHCP server is reachable by a successful ping from the router. Which action resolves the issue?

A. Configure a valid IP address on the Gi2 interface so that DHCP requests can be forwarded.  
B. Configure the `ip dhcp pool 1` and `network 192.168.210.0 255.255.255.0` commands.  
C. Configure the `ip helper-address 192.168.255.3` command on the Gi2.10 subinterface.  
D. Configure the `ip dhcp excluded-address 192.168.255.3` command on the Gi2.10 subinterface.  

- 問題文要約（日本語）:  
Router-on-a-stick 構成で VLAN10（サブIF Gi2.10, 192.168.210.0/24）のクライアントが、中央 DHCP サーバ 192.168.255.3 から IP を取得できない。  
ルータからは DHCP サーバへ ping が通る。  
どの設定を入れれば VLAN10 クライアントが IP を取得できるようになるか。

- 正解: **C**

- 解説（なぜ C が正しいか）
    - DHCP ブロードキャスト (DISCOVER) はクライアント VLAN 内で送信され、**デフォルトゲートウェイの L3 インターフェイス** で `ip helper-address` が設定されていると、そのアドレス宛ての **ユニキャスト DHCP リレー** に変換される。
    - クライアントが属するのは VLAN10 (192.168.210.0/24) であり、その L3 インターフェイスは `GigabitEthernet2.10`。  
      ここには `ip helper-address` が設定されていないため、DHCP ブロードキャストがリレーされていない。
    - 逆に、物理 IF Gi2 に設定されている `ip helper-address` は、L2 トランクポートであって SVI ではないので、クライアントのブロードキャストを受け取る L3 インターフェイスとして機能しない。
    - よって、サブインターフェイス `Gi2.10` 上に  
      `ip helper-address 192.168.255.3`  
      を設定する必要があり、これを述べる選択肢 C が正解となる。

- 他の選択肢がダメな理由
    - A: Router-on-a-stick ではサブIF に IP を持たせるのが正しく、物理 IF Gi2 に IP を付けるべきではない。
    - B: 問題の DHCP は「中央サーバ」で提供されており、ルータ自身を DHCP サーバにする必要はない。
    - D: `ip dhcp excluded-address` はローカル DHCP プールからの配布除外に使うコマンドであり、リレー動作には無関係。

***
### QUESTION 375 解説

- 問題文（英語）:
An engineer configured routing between multiple OSPF domains and introduced a routing loop that caused network instability. Which action resolves the problem?

A. Set a tag using the redistribute command toward a domain and deny inbound in the other domain by a matching tag  
B. Set a tag using the redistribute command toward a different domain and deny the matching tag when exiting from that domain  
C. Set a tag using the network command in a domain and use the route-map command to deny the matching tag when exiting toward a different domain.  
D. Set a tag using the network command in a domain and use the route-map command to deny the matching tag when entering into a different domain.  

- 問題文要約（日本語）:  
複数の OSPF ドメイン間で相互再配布をした結果、ループが発生してネットワークが不安定になっている。  
どの方法でループを防げるか。

- 正解: **A**

- 解説（なぜ A が正しいか）
    - OSPF ドメイン間で相互に `redistribute ospf x` を行うと、「ドメイン 1 → ドメイン 2 → 再びドメイン 1」というように、同じ経路が行き来して **ルーティングループ** を起こす危険がある。[file:79]
    - これを防ぐ代表的な方法が **ルートタグ (tag)** の利用。  
      手順のイメージ:
        1. ドメイン 1 からドメイン 2 へ再配布するとき、`redistribute ospf 1 subnets tag 1` のように **特定のタグ (1)** を付与する。
        2. ドメイン 2 側では、OSPF 再配布の「受信（in）」時にルートマップ／distribute-list で **tag 1 を持つルートを deny** し、自分側から再配布された経路が戻ってこないようにする。
        3. 逆方向（ドメイン 2 → 1）も tag 2 を付与し、ドメイン 1 側で tag 2 を持つルートを in で deny する。
    - 選択肢 A の  
      > Set a tag using the redistribute command toward a domain and deny inbound in the other domain by a matching tag  
      はこのやり方を正しく表現しているため、ループ防止策として適切。

- 他の選択肢がダメな理由
    - B: 「exit（出ていく）」方向で deny してしまうと、そのドメイン内でループした経路を防ぎきれないうえ、設計上も一般的ではない。
    - C / D: OSPF の `network` コマンドではタグを付けられず、タグ付与は `redistribute` で行うのが正しい。したがって前提から誤り。

***
### QUESTION 376 解説

- 問題文（英語）:
Refer to the exhibit. An administrator must configure the router with OSPF for IPv4 and IPv6 networks under a single process. The OSPF adjacencies are not established and did not meet the requirement. Which action resolves the issue?

A. Replace OSPF process 10 on the interfaces with OSPF process 1 for the IPv4 address, and remove process 10 from the global configuration.  
B. Replace OSPF process 10 on the interfaces with OSPF process 1, and remove process 10 from the global configuration.  
C. Replace OSPF process 10 on the interfaces with OSPF process 1 for the IPv6 address, and remove process 10 from the global configuration.  
D. Replace OSPF process 10 on the interfaces with OSPF process 1, and configure an additional router ID with IPv6 address.  

- 設定抜粋:
  - グローバル:
    ```text
    router ospfv3 1
     router-id 10.1.1.1
     address-family ipv4 unicast
      passive-interface Loopback0
     exit-address-family
     address-family ipv6 unicast
      passive-interface Loopback0
     exit-address-family
    ```
  - インターフェイス側:
    ```text
    interface Loopback0
     ospfv3 10 ipv4 area 10
     ospfv3 10 ipv6 area 0
    interface GigabitEthernet2
     ospfv3 10 ipv4 area 10
     ospfv3 10 ipv6 area 0
    ``` [file:79]

- 問題文要約（日本語）:  
1 つの OSPFv3 プロセスで IPv4 と IPv6 の両方を動かしたいが、グローバルでは process 1、インターフェイスでは process 10 を使っていて、隣接が張れていない。  
要件どおり「単一プロセス」で動作させるには、どう修正すべきか。

- 正解: **B**

- 解説（なぜ B が正しいか）
    - OSPFv3 では「プロセス IDごとに address-family ipv4/ipv6 をぶら下げる」形で、**1 つの OSPFv3 プロセス内で IPv4/IPv6 を扱う**ことができる。[file:79]
    - 現在の設定では:
        - `router ospfv3 1` には IPv4/IPv6 の address-family と router-id が定義されている。
        - しかし、インターフェイスでは `ospfv3 10 ...` が指定されており、process 10 の下にぶら下がっている。
        - その結果、実際に動作しているのは「プロセス 10」であり、「単一プロセスに統一する」という要件に反している。
    - 対応すべきこと:
        1. すべてのインターフェイスの `ospfv3 10 ...` を **`ospfv3 1 ...` に変更**し、IPv4/IPv6 両方とも process 1 配下に参加させる。
        2. もう使わない `router ospfv3 10` のグローバル設定を `no router ospfv3 10` で削除する。
    - 選択肢 B は「インターフェイスの OSPF process を 1 に置き換え、さらにグローバルから process 10 を削除する」と述べており、上記 2 点を正しく行う構成になっている。

- 他の選択肢がダメな理由
    - A / C: IPv4 だけ、あるいは IPv6 だけを process 1 に変えると、IPv4/IPv6 で別プロセスになってしまい「単一プロセス」という要件を満たさない。
    - D: 既に router-id 10.1.1.1 が設定されており、IPv6 アドレスを用いた router-id は不要。問題は router-id ではなく「プロセス ID の不一致」。

***
### QUESTION 377 解説

- 問題文（英語）:
Which failure detection mechanism is used for BFD?

A. consistent rate  
B. Layer 2 protocol failure  
C. variable rate  
D. routing protocol failure  

- 問題文要約（日本語）:  
BFD が提供する障害検出の仕組みとして、どの特徴が正しいか。

- 正解: **A**

- 解説（なぜ A が正しいか）
    - BFD は、リンクやフォワーディングパスの障害を **一定・均一な間隔（uniform / consistent rate）** の高速 Hello によって検出するプロトコル。[file:79]
    - これにより、OSPF/EIGRP/BGP など各ルーティングプロトコル固有の Hello タイマに依存せず、**常に同じ検出時間** で障害検出・再収束が行えるため、動作が予測しやすい。
    - 選択肢 A の「consistent rate」は、この BFD の“均一なレート”での故障検出を指しており、説明と一致する。

- 他の選択肢がダメな理由
    - B: BFD は L2 プロトコル自体の失敗検出ではなく、「L2/L3 を含むフォワーディングパス全体」を監視する。
    - C: “variable rate” は、プロトコルごとに異なる Hello 間隔を使う従来のやり方であり、BFD はそれを解消するために「一定レート」で動作する。
    - D: BFD 自体は汎用のフォワーディングパス監視であり、特定の「ルーティングプロトコルの失敗」を見ているわけではない（ルーティングプロトコルは BFD 結果をトリガとして利用する）。

***
### QUESTION 378 解説

- 問題文（英語）:
The IPv6 network is under attack by an unknown source that is neither in the binding table nor learned through neighbor discovery. Which feature helps prevent the attack?

A. IPv6 Prefix Guard  
B. IPv6 Snooping  
C. IPv6 Router Advertisement Guard  
D. IPv6 Destination Guard  

- 問題文要約（日本語）:  
IPv6 ネットワークが、バインディングテーブルにも載っておらず、NDP でも学習されていない“不明なアドレス”から攻撃を受けている。  
このような「トポロジ的に正しくない IPv6 アドレス」からのトラフィックを防ぐのに役立つ機能はどれか。

- 正解: **A**

- 解説（なぜ A が正しいか）
    - 基本となるのは **IPv6 Source Guard** で、これはバインディングテーブル（IPv6 Snooping で学習した IPv6/MAC/ポート情報）にない送信元アドレスのトラフィックをブロックする機能。[file:79]
    - **IPv6 Prefix Guard** は Source Guard の中で動作し、さらに一歩進んで  
      - トポロジ的に正しくないプレフィックス  
      - DHCPv6 PD などで委譲されていないプレフィックス  
      からのトラフィックを拒否することで、未知のソースからの攻撃を防ぐ役割を持つ。[file:79]
    - 設問の「バインディングテーブルにも NDP にも載っていない未知のアドレス」という条件に最も合うのが、この Prefix Guard であり、攻撃元を遮断できる。

- 他の選択肢がダメな理由
    - B: IPv6 Snooping はバインディングテーブルを“作る”機能であり、それ単体ではトラフィックをブロックしない（Source Guard/Prefix Guard の土台）。
    - C: IPv6 RA Guard は、不正な Router Advertisement をブロックする機能で、一般的な「未知ソースアドレス」からの全トラフィックを止めるものではない。
    - D: IPv6 Destination Guard は「宛先アドレス側」を検証して不正な宛先への NDP 解決を防ぐ機能であり、ここで問われている“未知ソースからの攻撃”には直接対応しない。

***

### QUESTION 379 解説

- 問題文（英語）:
Refer to the exhibit. The network administrator configured BGP as the backup route for 10.0.0.0/8 and it should work only when EIGRP 10.0.0.0/8 failed to install for the site. Which configuration resolves the issue? [file:79]

A. 
 configure terminal  
 !  
 ip route 10.0.0.0 255.0.0.0 192.168.90.2  

B. 
 configure terminal  
 !  
 router eigrp 1  
  distance eigrp 10 170  

C. 
 configure terminal  
 !  
 router eigrp 1  
  distance eigrp 90 170  

D. 
 configure terminal  
 !  
 router eigrp 1  
  redistribute bgp metric 10000 1 255 1 1500  

- 問題文要約（日本語）:  
10.0.0.0/8 への経路は **EIGRP をメイン**、BGP は「EIGRP が入らないときだけ使うバックアップ」にしたい。  
しかし現状では BGP 経路が優先されてしまっている。  
どの設定で「EIGRP を優先・BGP をバックアップ」にできるか。

- 状況整理:
    - eBGP から学習した 10.0.0.0/8 の AD は 20。  
    - EIGRP 内部ルートの AD はデフォルト 90。  
    - よって、**AD 20 の BGP が常に EIGRP (90) より優先**され、バックアップではなくメインとして使われてしまう。[file:79]

- 正解: **B**

- 解説（なぜ B が正しいか）
    - 目的は「EIGRP 10.0.0.0/8 をメイン」にすることなので、**EIGRP の AD を BGP (20) より小さく**すればよい。
    - `distance eigrp <internal> <external>` で、EIGRP の管理距離を変更できる。  
      選択肢 B では:
        - 内部 EIGRP AD = 10  
        - 外部 EIGRP AD = 170  
      と設定しており、10.0.0.0/8 が内部 EIGRP ルートなら AD 10 となって eBGP の 20 より優先される。  
      EIGRP 経路が消えた場合のみ、AD 20 の BGP 経路がルーティングテーブルへ入り、バックアップとして機能する。

- 他の選択肢がダメな理由
    - A: スタティックルート (AD 1) を追加すると、EIGRP/BGP よりさらに優先されてしまい、バックアップどころか常にこのスタティックが使われる。
    - C: `distance eigrp 90 170` はデフォルト値と同じで、AD を変えていないため問題は解決しない。
    - D: BGP 経路を EIGRP へ再配布する設定であり、AD の関係は変わらないうえ、ループや経路設計を複雑にするだけで逆効果。

***
### QUESTION 380 解説

- 問題文（英語）:
Switch(config)# ip vrf 70  
Switch(config-vrf)# rd 70:1  
Switch(config-vrf)# route-target export 70:1  
Switch(config-vrf)# route-target import 70:1  
Switch(config-vrf)# exit  
Switch(config)# ip vrf 80  
Switch(config-vrf)# rd 80:1  
Switch(config-vrf)# route-target export 80:1  
Switch(config-vrf)# route-target import 80:1  

Refer to the exhibit. An engineer must extend VRF-Lite over a trunk to another switch for VLAN 70 (10.70.70.0/24) on port GigabitEthernet0/0 and VLAN 80 (10.80.80.0/24) on port GigabitEthernet0/1. Which configuration accomplishes this objective?

A. Use routed ports with `ip vrf forwarding` on Gi0/0 and Gi0/1  
B. Make Gi0/0 and Gi0/1 access ports with `ip vrf forwarding`  
C. Make Gi0/0 and Gi0/1 access ports only  
D. Make Gi0/0 and Gi0/1 802.1Q trunks, allowing VLAN 70 and VLAN 80 respectively  

- 問題文要約（日本語）:  
スイッチ上で VRF 70 と VRF 80 が作成されている。  
別のスイッチに VRF-Lite を**トランク越しに拡張**するために、  
- Gi0/0 で VLAN70（10.70.70.0/24）  
- Gi0/1 で VLAN80（10.80.80.0/24）  
を流したい。どのインターフェイス設定が適切か。

- 正解: **D**

- 解説（なぜ D が正しいか）
    - VRF-Lite を「スイッチ間の VLAN トランクで延伸」する場合、通常は  
      - 物理ポートは L2 の **802.1Q トランクポート** として設定  
      - VLAN ごとに SVI（`interface Vlan70`, `interface Vlan80` など）で `ip vrf forwarding` と IP アドレスを割り当てる  
      という構成をとる。
    - 問題文でも「over a trunk to another switch」と明示されているので、Gi0/0 と Gi0/1 は **トランクポート** にする必要があり、`switchport mode trunk` と `switchport trunk allowed vlan ...` が必須となる。
    - 選択肢 D は Gi0/0, Gi0/1 をそれぞれ  
      ```text
      switchport trunk encapsulation dot1q
      switchport mode trunk
      switchport trunk allowed vlan 70 / 80
      ```  
      と設定しており、VLAN70 と 80 をトランクで別スイッチへ運ぶ要件に一致する。

- 他の選択肢がダメな理由
    - A: `no switchport` にして L3 ポートにすると、「トランクで VLAN を延伸する」という要件を満たさない（ただの L3 ポイントツーポイントになる）。
    - B: `switchport mode access` かつ `ip vrf forwarding` を物理ポートに同時適用する構成は一般的でなく、トランクでもない。
    - C: 単にアクセス VLAN を設定するだけで、VRF との関連付けもトランクでの延伸も行われない。

***
### QUESTION 381 解説

- 問題文（英語）:
The network administrator must configure R1 to authenticate Telnet connections based on Cisco ISE using RADIUS. ISE = 192.168.1.5, R1 = 192.168.1.1, shared secret = Cisco123.  
R1 設定:

aaa new-model
!
radius server ISE1
 address ipv4 192.168.1.5
 key Cisco123
!
aaa group server tacas+ RAD-SERV
 server name ISE1
!
aaa authentication login default group RAD-SERV

The network administrator cannot authenticate to access R1 based on ISE. Which set of configurations fixes the issue?

A. aaa group server radius RAD-SERV  
   server name ISE1  

B. aaa group server tacacs+ ISE1  
   server name RAD-SERV  

C. line vty 0 4  
   login authentication RAD-SERV  

D. line vty 0 4  
   login authentication default  

- 問題文要約（日本語）:  
Telnet ログインを ISE（RADIUS）で認証したいが、ログインできない。  
RADIUS サーバ自体は定義しているが、サーバグループ定義で TACACS+ を指定してしまっている。  
どの設定で修正すべきか。

- 正解: **A**

- 解説（なぜ A が正しいか）
    - ISE との通信には **RADIUS** を使う前提なのに、グループ定義が  
      `aaa group server tacas+ RAD-SERV`  
      となっており、**TACACS+ グループ**として登録している。[file:79]
    - その結果、`aaa authentication login default group RAD-SERV` では、R1 は TACACS+ で ISE に話そうとして失敗する。
    - 正しくは RADIUS グループとして  
      ```text
      aaa group server radius RAD-SERV
       server name ISE1
      ```  
      を定義し直す必要がある。
    - これを示しているのが選択肢 A であり、既存の `aaa authentication login default group RAD-SERV` はそのまま使えるので、これで ISE/RADIUS 認証が機能する。

- 他の選択肢がダメな理由
    - B: グループ種別を tacacs+ のままにしており、問題を解決しない。
    - C: line vty の認証リスト名を `RAD-SERV` に変えても、そもそもグループ定義が TACACS+ なので根本原因が残る。
    - D: `login authentication default` はすでにデフォルトで使っている設定であり、変更にならない（`aaa authentication login default ...` が既に定義済み）。

***
### QUESTION 382 解説

- 問題文（英語）:
Network operations report issues with receiving too many external routes, which caused CPU spike on routers with smaller memories. Which action resolves the issue?

A. Configure the area range command when redistributing on ASBR  
B. Configure the summary-address command when redistributing on ABR  
C. Configure the area range command when redistributing on ABR  
D. Configure the summary-address command when redistributing on ASBR  

- 問題文要約（日本語）:  
外部ルート（OSPF 外部 LSA）が多すぎて、小規模ルータで CPU スパイクが発生している。  
どの設定で、外部ルート数を減らして負荷を下げられるか。

- 正解: **D**

- 解説（なぜ D が正しいか）
    - OSPF で「外部ルート（Type 5/Type 7 LSA）」を集約・要約するのは **ASBR 側の `summary-address` コマンド** の役割。[file:79]
        - 例:  
          `router ospf 1`  
          ` summary-address 192.168.0.0 255.255.252.0`  
        - こうすると、ASBR が多くの外部プレフィックスを 1 つの大きなサマリプレフィックスとして生成し、他ルータにはそのサマリだけを配布できる。
    - これにより、ネットワーク全体に流れる外部 LSA の数が大幅に減り、LSDB・計算負荷・CPU 使用率の抑制につながる。
    - 選択肢 D の  
      > Configure the summary-address command when redistributing on ASBR  
      がまさにこの操作であり、外部ルート数削減という目的に合致する。

- 他の選択肢がダメな理由
    - A / C: `area range` は **ABR でエリア内部ルート (intra-area / inter-area)** をサマリするためのコマンドであり、外部ルートを対象としない。
    - B: `summary-address` を ABR に設定するのは誤りで、外部ルートは ASBR が起点となるため、サマリは ASBR で行う必要がある。

***
QUESTION 383 解説

- 問題文（英語）:
Refer to the exhibit. An engineer must configure a LAN-to-LAN IPsec VPN between R1 and the remote router. Which IPsec Phase 1 configuration must the engineer use for the local router? [file:1]

A. crypto isakmp policy 5
   authentication pre-share
   encryption 3des
   hash md5
   group 2
   !
   crypto isakmp key cisco123 address 199.1.1.1

B. crypto isakmp policy 5
   authentication pre-share
   encryption 3des
   hash md5
   group 2
   !
   crypto isakmp key cisco123 address 200.1.1.3

C. crypto isakmp policy 5
   authentication pre-share
   encryption 3des
   hash md5
   group 2
   !
   crypto isakrnp key cisco123! address 199.1.1.1

D. crypto isakmp policy 5
   authentication pre-share
   encryption 3des
   hash sha
   group 2
   !
   crypto isakmp key cisco123 address 200.1.1.3 [file:1]

- 問題文要約（日本語）:  
R1（ローカル）とリモートルータ R3 の間で LAN-to-LAN IPsec VPN を張る。  
図では R1 側が 199.1.1.0/24、リモート側 R3 は 200.1.1.0/24 上の 200.1.1.3。  
R1 で設定すべき Phase 1（ISAKMP）設定を選ぶ問題。[file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）
  - ローカルは R1、リモートは R3 なので、R1 から見る「相手の IP」は R3 の 200.1.1.3。  
    したがって `crypto isakmp key ... address 200.1.1.3` が正しい。199.1.1.1 は自分側なので NG。[file:1]
  - Phase 1 のポリシー自体は、どれも pre-shared・3DES・DH group 2 などで大きな違いはないが、ハッシュに SHA を使う方が MD5 よりセキュア。  
    選択肢 D は、正しいリモート IP（200.1.1.3）とより安全な `hash sha` を組み合わせているのでベスト。[file:1]
  - 問題文中の解説でも「R1 から R3 へ認証するには R3 の IP（200.1.1.0/24 内）を指定する必要がある」「200.1.1.3 の選択肢どちらでも動作するが、SHA の方が望ましい」と説明されている。[file:1]

- 他の選択肢がダメな理由
  - A / C: `address 199.1.1.1` は R1 自身の IP なので、相手のピア指定として誤り。[file:1]
  - C: さらにキー文字列に余計な「!」が含まれておりタイプミス（`cisco123!`）になっている。[file:1]
  - B: リモート IP は正しいが、ハッシュが MD5 で、D に比べてセキュリティ面で劣る。[file:1]

***
QUESTION 384 解説

- 問題文（英語）:
Refer to the exhibit. A network engineer received a call from the vendor for a failed attempt to remotely log in to their managed router loopback interface from 192.168.40.15. Which action must the network engineer take to resolve the issue? [file:1]

A. The source IP summarization must be updated to include the vendor source IP address.  
B. The IP access list VENDOR must be applied to interface loopback 100.  
C. The time-range configuration must be changed to use absolute instead of periodic.  
D. The EIGRP configuration must be updated to include a network statement for loopback 100. [file:1]

- 問題文要約（日本語）:  
ベンダが自分たち管理用ルータの Loopback100 へリモートログインしようとしたが失敗した。  
ベンダ側の送信元 IP は 192.168.40.15。  
図の ACL「VENDOR」では、192.168.32.0 0.0.7.255 というサマリで許可している。  
どの対応で問題が解決できるか。[file:1]

- 正解: **A**

- 解説（なぜ A が正しいか）
  - ACL では `192.168.32.0 0.0.7.255` が許可されているが、これはワイルドカード 0.0.7.255 なので  
    192.168.32.0〜192.168.39.255（192.168.32.x〜39.x）を意味する。[file:1]
  - ベンダの IP 192.168.40.15 はこの範囲外（40.x）なので、ACL で「許可されていない＝拒否」される。  
    そのためリモートログインが失敗している。[file:1]
  - よって、ベンダのアドレス 192.168.40.15 も含むように「送信元 IP のサマリ範囲（サマリゼーション）」を修正する必要がある。  
    これを示しているのが選択肢 A。ACL のサマリを 192.168.32.0 0.0.15.255 などに広げれば 32〜47.x まで含められる。[file:1]

- 他の選択肢がダメな理由
  - B: ループバックに ACL がすでに適用済みかどうかは関係なく、根本原因は「サマリ範囲に 192.168.40.15 が含まれていない」こと。[file:1]
  - C: 時間帯（time-range）はこの問題の説明には出てこず、ACL 範囲の不備が原因と説明されているので無関係。[file:1]
  - D: EIGRP の network 文はルーティングの問題であり、ACL によるリモートアクセス制御の話とは別問題。[file:1]

***
QUESTION 386 解説

- 問題文（英語）:
Drag and drop the ICMPv6 neighbor discovery messages from the left onto the correct packet types on the right. Select and Place. [file:1]

- 問題文要約（日本語）:  
ICMPv6 Neighbor Discovery で使われる各種メッセージの「タイプ番号」と「名前（役割）」を正しく対応させるドラッグ&ドロップ問題。[file:1]

- 正解（対応関係）:
  - Type 133 → Router Solicitation (RS) [file:1]  
  - Type 134 → Router Advertisement (RA) [file:1]  
  - Type 135 → Neighbor Solicitation (NS) [file:1]  
  - Type 136 → Neighbor Advertisement (NA) [file:1]  
  - Type 137 → Redirect Message [file:1]

- 解説（なぜこうなるか）
  - 133/134 は「ルータ検出用」メッセージで、ホストからの問い合わせが RS、ルータからの応答が RA。 [file:1]
  - 135/136 は「近隣ノードの MAC 解決・重複確認」などに使うメッセージで、問い合わせが NS、応答が NA。 [file:1]
  - 137 は「より良い経路を教える」ための Redirect。 [file:1]

***
QUESTION 387 解説

- 問題文（英語）:
Refer to the exhibit. An engineer configured SNMP communities on UserSW2 switch, but the SNMP server cannot upload modified configurations to the switch. Which configuration resolves this issue? [file:1]

A. snmp-server community Ciscowruser RW 11  
B. snmp-server group NETVIEW v2c priv read NETVIEW access 11  
C. snmp-server community Cisc0Us3r RW 11  
D. snmp-server group NETADMIN v3 priv read NETVIEW write NETADMIN access 22 [file:1]

- 問題文要約（日本語）:  
ユーザスイッチ UserSW2 で SNMP コミュニティを設定しているが、NMS 側から設定変更（書き込み）ができない。  
トポロジ図では「書き込み用コミュニティ名」は Ciscowruser になっているのに、スイッチの設定では綴りが食い違っている。  
どの設定を入れれば SNMP サーバが設定を書き込めるようになるか。[file:1]

- 正解: **A**

- 解説（なぜ A が正しいか）
  - トポロジ上の「RW（read-write）コミュニティ名」は `Ciscowruser` となっているが、現在のスイッチ設定では `Cisc0wrus3r` のように 0 と o／3 と e が混ざった誤綴りになっている。 [file:1]
  - SNMPv2c の認証は「コミュニティ文字列」が一致しているかどうかだけで判定されるため、文字が 1 文字でも違えば別物として扱われ、RW 権限での書き込みは許可されない。 [file:1]
  - 選択肢 A の `snmp-server community Ciscowruser RW 11` により、  
    トポロジで定義された正しい RW コミュニティ名とアクセスリスト 11 を使った read-write コミュニティが設定され、  
    NMS からのコンフィグ書き込みが成功するようになる。 [file:1]

- 他の選択肢がダメな理由
  - B / D: SNMP グループ定義（v2c/v3）の話であり、問題の核心である「コミュニティ文字列のスペルミス」を修正していない。 [file:1]
  - C: `Cisc0Us3r` は読み取り専用コミュニティ（RO）の名前として使われている文字列であり、RW 用コミュニティの綴り修正にはなっていない。 [file:1]

***
QUESTION 388 解説

- 問題文（英語）:
How is a preshared key "Test" for all the remote VPN routers configured in a DMVPN using GRE over IPsec set up? [file:1]

A. authentication pre-share Test address 0.0.0.0 0.0.0.0  
B. set pre-share Test address 0.0.0.0 0.0.0.0  
C. crypto ipsec key Test address 0.0.0.0 0.0.0.0  
D. crypto isakmp key Test address 0.0.0.0 0.0.0.0 [file:1]

- 問題文要約（日本語）:  
GRE over IPsec を使った DMVPN で、「すべてのリモート VPN ルータ」と共有する事前共有鍵 Test を設定したい。  
このとき、どのコマンドで“全リモートピア（動的なピアを含む）”向けの事前共有鍵を設定するかを問う問題。[file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）
  - IPsec のフェーズ1（IKE/ISAKMP）の事前共有鍵は `crypto isakmp key <key> address <peer-ip>` で設定する。  
    ここで `<key>` が "Test"、`address 0.0.0.0 0.0.0.0` は「どのピアアドレスでもこの鍵を使う」という意味になる。 [file:1]
  - DMVPN では、ハブ側はスポークのピア IP が動的なことが多いため、すべてのスポークに共通の PSK を 0.0.0.0/0 で定義するのが典型パターン。 [file:1]
  - 選択肢 A / B は正しい Cisco IOS の構文ではなく、C の `crypto ipsec key` というコマンドも存在しないため誤りとなる。 [file:1]

- 補足（イメージ）
  - 実際の設定イメージは次のような形になる：  
    `crypto isakmp key Test address 0.0.0.0 0.0.0.0`  
    これにより、このハブに対して IKE を張ってくる任意のリモートルータは、PSK "Test" を使って認証される。 [file:1]

***
DMVPN の設計で「全スポーク同じ PSK」にする場合と、「スポークごとに別 PSK」にする場合、それぞれどんなメリット・デメリットがありそうか、ざっくりイメージできますか？

QUESTION 389 解説

- 問題文（英語）:
Refer to the exhibit. A shoe retail company implemented the uRPF solution for an antispoofing attack. A network engineer received the call that the branch A server is under an IP spoofing attack. Which configuration must be implemented to resolve the attack? [file:1]

A. R4  
   interface ethernet0/1  
   ip unicast RPF check reachable-via any allow-default allow-self-ping  

B. R4  
   interface ethernet0/1  
   ip verify unicast source reachable-via any allow-default allow-self-ping  

C. R3  
   interface ethernet0/1  
   ip unicast RPF check reachable-via any allow-default allow-self-ping  

D. R3  
   interface ethernet0/1  
   ip verify unicast source reachable-via any allow-default allow-self-ping [file:1]

- 問題文要約（日本語）:  
シューズ小売り会社が IP スプーフィング対策として uRPF（Unicast Reverse Path Forwarding）を導入した。  
Branch A のサーバに対して IP スプーフィング攻撃が発生したとの連絡を受けた。  
図の出力から、R4 では uRPF が有効だが、R3 ではまだ有効化されていないことが分かる。  
どの設定を入れれば攻撃を防げるか。[file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）
  - Cisco IOS で uRPF を有効にする正式なコマンドは `ip verify unicast source reachable-via ...` であり、`ip unicast RPF check ...` というコマンドは実在しない。  
    よって A と C は構文として不正。 [file:1]
  - 攻撃は Branch A サーバに向かうトラフィックなので、そのサーバに近いルータ（図の構成では R3 側）で uRPF を有効化するのが効果的。  
    出力でも「R3 は IP unicast RPF を未設定、R4 は設定済み」と説明されている。 [file:1]
  - 選択肢 D は、R3 の ethernet0/1 インターフェイスで  
    `ip verify unicast source reachable-via any allow-default allow-self-ping`  
    を設定しており、ルーティングテーブルに照らして“あり得ない送信元”からのパケットを破棄できる。 [file:1]
  - `reachable-via any` を使っているのでルーズモードの uRPF となり、非対称ルーティングでも動作しつつ、スプーフィングパケットはドロップできる。 [file:1]

- 他の選択肢がダメな理由
  - A / C: コマンド構文が誤りで、実際の IOS では受け付けられない。 [file:1]
  - B: R4 側で uRPF を強化しても、既に R4 には uRPF が有効であり、問題は「まだガードが効いていない R3 側」にあるため根本解決にならない。 [file:1]

***
QUESTION 390 解説

- 問題文（英語）:
Refer to the exhibit. An administrator must harden a router, but the administrator failed to test the SSH access successfully to the router. Which action resolves the issue? [file:1]

A. SSH syntax must be ssh -l user ip to log in to the remote device  
B. Configure enable secret to log in to the device  
C. Configure SSH on the remote device to log in using SSH  
D. SSH must be allowed with the transport output ssh command [file:1]

- 問題文要約（日本語）:  
管理者がルータのセキュリティ強化設定を行い、SSH 用のユーザ（例: `csadmin`）を新たに作成したが、自分の端末から SSH 接続テストに失敗してしまった。  
図の状態では、コンソールや Telnet などでログイン中のセッションに「ユーザ名」は紐づいておらず、その状態から SSH コマンドを打っても正しく認証されない。  
どの操作をすれば SSH ログインに成功できるかを問う問題。[file:1]

- 正解: **A**

- 解説（なぜ A が正しいか）
  - 現在の exec セッションは、おそらく「コンソール or 単純なパスワード認証の Telnet」で入っており、ユーザ名 `csadmin` ではログインしていない。 [file:1]
  - Cisco IOS では、SSH クライアントとしてルータから別機器へ接続するとき、`ssh -l <username> <ip>` 形式で「SSH で使うユーザ名」を明示する必要がある。  
    この問題では `ssh -l csadmin 10.0.0.1` のように実行することで、作成したユーザ名／パスワードでのログインが可能になる。 [file:1]
  - B: `enable secret` は特権モード用パスワードであり、SSH ログイン失敗の直接原因ではない。 [file:1]
  - C: 「リモート側で SSH 有効化」がされていないケースならあり得るが、問題の説明と解説では、すでに SSH サービスは有効で、クライアント側のコマンド指定が誤っている状況とされている。 [file:1]
  - D: `transport output ssh` はルータから外に SSH を出す方向ではなく、通常は vty での受け入れ側に `transport input ssh` を設定する話であり、ここでは論点がずれている。 [file:1]

***
QUESTION 391 解説

- 問題文（英語）:
The network administrator configured the router for Control Plane Policing so that inbound SSH traffic is policed to 500 kbps. This policy must apply to traffic coming in from 10.10.10.0/24 and 192.168.10.0/24 networks.  
… (ACL 100・class-map・policy-map・service-policy 設定省略)  
The Control Plane Policing is not applied to SSH traffic and SSH is open to use any bandwidth available. Which configuration resolves this issue? [file:1]

- 問題文要約（日本語）:  
管理者は「コントロールプレーンポリシング(CoPP)で SSH トラフィックを 500 kbps に制限」したい。対象は 10.10.10.0/24 と 192.168.10.0/24 から来る SSH。  
しかし現在の設定では  
1) ACL 100 が “10.10.10.0/24 からの全 IP” と “192.168.10.0/24 からの Telnet(23番)” をマッチしており、SSH を正しく絞れていない  
2) policy-map が `interface E0/0` と `E0/1` に input で適用されていて、「control-plane」には適用されていないため、本来の CoPP になっていない  
この2つの問題を同時に直すコンフィグはどれか、という問題。[file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  1. ACL の修正（SSH トラフィックだけをマッチ）
     - 元の ACL 100:  
       `permit ip 10.10.10.0 0.0.0.255 any` → 10.10.10.0/24 からの全トラフィック  
       `permit tcp 192.168.10.0 0.0.0.255 any eq 23` → 192.168.10.0/24 からの Telnet のみ [file:1]  
     - これでは「SSH だけを 500 kbps で制限する」という要件を満たせない。  
     - C では、ACL 100 を  
       - `permit tcp 10.10.10.0 0.0.0.255 any eq 22`  
       - `permit tcp 192.168.10.0 0.0.0.255 any eq 22`  
       に変更しており、両セグメントからの SSH（TCP/22）のみにマッチさせている。 [file:1]

  2. 真の CoPP にする（適用先を control-plane に変更）
     - CoPP は本来 `control-plane` コンテキストで `service-policy input <policy>` として CPU 向きトラフィックに適用する機能。 [file:1]
     - 元の設定は `interface E0/0` / `E0/1` に対して `service-policy input PM-COPP` を設定しており、これはただのインターフェイス QoS であって CoPP ではない。 [file:1]
     - C は E0/0・E0/1 からサービスポリシーを削除し、代わりに:
       - `control-plane`
       - `service-policy input PM-COPP`  
       を設定して、正しく CoPP として動作させている。 [file:1]

  3. 他の選択肢が足りない点
     - A / B: ACL 側は SSH のみをマッチするよう直しているが、policy の適用先が「インターフェイスのまま」なので CoPP になっていない。 [file:1]
     - D: control-plane に適用する点は正しいが、ACL 100 は相変わらず SSH 以外もマッチしているため「SSH だけを 500 kbps に制限」という要件を満たさない。 [file:1]

***
QUESTION 392 解説

- 問題文（英語）:
Refer to the exhibit. An engineer configures two ASBRs, 10.4.17.6 and 10.4.15.5, in an OSPF network to redistribute routes from EIGRP. However, both ASBRs show the EIGRP routes as equal costs even though the next-hop router 10.4.17.6 is closer to R1. How should the network traffic to the EIGRP prefixes be sent via 10.4.17.6? [file:1]

A. The administrative distance should be raised to 120 from the ASBR 10.4.15.5.  
B. The ASBR 10.4.17.6 should assign a tag to match and assign a lower metric on R1.  
C. The administrative distance should be raised to 120 from the ASBR 10.4.17.6.  
D. The redistributed prefixes should be advertised as Type 1. [file:1]

- 問題文要約（日本語）:  
2 台の ASBR（10.4.17.6 と 10.4.15.5）が EIGRP から OSPF へ再配布を行っている。  
R1 から見ると、どちらの ASBR から学ぶ EIGRP 経路も OSPF 上で「同じコスト」の外部経路として見えてしまい、R1 から見て物理的に近い 10.4.17.6 経由を優先してくれない。  
EIGRP プレフィックスへのトラフィックを 10.4.17.6 経由にさせるにはどうすべきか、という問題。[file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - OSPF 外部経路のタイプには  
    - Type 2 (E2): デフォルト。外部メトリックのみで経路選択し、AS 内のコストは無視。  
    - Type 1 (E1): 外部メトリックに「AS 内のコスト」も加算して比較する。  
    がある。 [file:1]
  - 現状は両 ASBR が E2 として同じメトリックで再配布しているため、R1 から見ると「どの ASBR 経由でも同じコストの外部経路」に見え、内部距離の違い（10.4.17.6 の方が近い）が反映されていない。 [file:1]
  - 再配布時に E1（Type 1）で再配布するよう設定すれば、  
    「ASBR までの内部 OSPF コスト ＋ 外部メトリック」を合計した値で比較されるため、R1 に近い ASBR（10.4.17.6）経由の経路が自然と優先される。 [file:1]
  - 行いたいのは「2 本の外部経路のうち、近い方を選ばせる」ことなので、AD を変えて片方の経路を“全部”不利にする A / C や、タグでのフィルタリング B より、E1 を使って内部コスト差を反映させる D がシンプルかつ意図に合致している。 [file:1]

***
QUESTION 393 解説

- 問題文（英語）:
Refer to the exhibit. The Customer Edge router (AS 65500) wants to use AS 100 as the preferred ISP for all external routes.

Customer Edge
route-map SETLP
 set local-preference 111
!
router bgp 65500
 neighbor 192.168.111.1 remote-as 100
 neighbor 192.168.111.1 route-map SETLP out
 neighbor 192.168.112.2 remote-as 200

This configuration failed to send routes to AS 100 as the preferred path. Which set of configurations resolves the issue? [file:1]

- 問題文要約（日本語）:  
CE ルータ（AS 65500）は、インターネット向けトラフィックで ISP1（AS 100）を優先的に使いたい。  
そのために local-preference を 111 に上げる route-map を設定したが、設定方向が誤っており、受信した BGP 経路の優先度に反映されていない。  
どの設定にすれば、AS 100 から学んだ経路の local-preference を 111 にできるか、という問題。[file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - 「AS 100 を優先 ISP にしたい」ということは、  
    CE ルータ内部で、AS 100 から**受信した**外部経路の local-preference を他より高く設定する必要がある。  
    local-preference は**自 AS 内部での経路選択**に使う属性なので、「CE が受信する側」で書き換えないと意味がない。 [file:1]
  - 元の設定は `neighbor 192.168.111.1 route-map SETLP out` となっており、  
    これは「CE が AS 100 に広告するルート」に対して local-preference を設定しているだけで、  
    CE が AS 100 から**受信する経路**には何も影響しない。 [file:1]
  - 選択肢 B では、`neighbor 192.168.111.1 route-map SETLP in` となっており、  
    AS 100 から受信する経路に対して `set local-preference 111` が適用される。  
    その結果、AS 200 から学んだ経路よりも AS 100 からの経路が優先されるようになる。 [file:1]
  - C / D の AS-path prepend は「経路を外に出すときに AS パスを長くして他から見て“劣後させる”」用途であり、  
    ここでは「CE 内で AS 100 を優先したい」という目的に合わない。 [file:1]

***
QUESTION 394 解説

- 問題文（英語）:
Refer to the exhibit. A network engineer is troubleshooting a failed link between R2 and R3. No traffic loss is reported from router R5 to HQ. Which command fixes the separated backbone? [file:1]

A. R3(config-router)#area 21 virtual-link 192.168.125.5  
B. R3(config-router)#no area 21 stub  
C. R2(config-router)#area 21 virtual-link 192.168.125.5  
D. R2(config-router)#no area 21 stub [file:1]

- 問題文要約（日本語）:  
OSPF でバックボーンエリア（area 0）が R2–R3 間リンク障害により“分断”されている。  
R2–R3 間の非バックボーンエリア（area 21）をトランジットにして Virtual Link で backbone をつなごうとしているが、うまく動作しておらず、backbone が論理的に分離した状態になっている。  
原因は、そのトランジットエリア 21 が Stub エリアとして設定されていること。Stub エリアは Virtual Link のトランジットに使用できないため、この設定をどこで外すべきかを問う問題。[file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - Virtual Link は「backbone が物理的に分断されたときに、非バックボーンエリアを経由して area 0 同士を論理的につなぐ仕組み」。  
    この“経由するエリア”は **Stub エリアにはできない** という制約がある。 [file:1]
  - 本問では、R2–R3 間のエリア 21 をトランジットエリアとして Virtual Link を張ろうとしているが、既に `area 21 stub` が設定されているため、そのエリアを通る Virtual Link が成立しない。 [file:1]
  - 解決策は「エリア 21 の stub 設定を外す」こと。  
    正解 B の `R3(config-router)#no area 21 stub` によって、R3 側で Stub 設定が削除され、Virtual Link が正常に動作し、area 0 が再び一つのバックボーンとして認識される。 [file:1]
  - Virtual Link 自体はすでに R2・R3 双方で設定済みであり、問題は“Stub であること”なので、追加で `area 21 virtual-link ...` を入れる A / C は本質的な修正にならない。D は R2 側のみ Stub を外す案だが、解説では R3 側の設定を外す前提で説明されている。 [file:1]

***
QUESTION 395 解説

- 問題文（英語）:
Refer to the exhibit. An engineer must configure OSPF with R9 and R10 and configure redistribution between OSPF and RIP, causing routing loop. Which configuration on R9 and R10 meets this objective? [file:1]

A. router ospf 1  
   redistribute rip subnets tag 20  
   !  
   route-map deny_tag20 deny 10  
    match tag 20  
   route-map deny_tag20 permit 20  
   !  
   router ospf 1  
    distribute-list route-map deny_tag20 in  

B. router ospf 1  
   redistribute rip subnets tag 20  
   !  
   route-map deny_tag20 deny 10  
    match tag 20  
   route-map deny_tag20 permit 20  
   !  
   router rip 1  
    distribute-list route-map deny_tag20 in  

C. router ospf 1  
   redistribute rip subnets tag 20  
   !  
   route-map deny_tag20 permit 10  
    match tag 20  
   route-map deny_tag20 permit 20  
   !  
   router ospf 1  
    distribute-list route-map deny_tag20 in  

D. router ospf 1  
   redistribute rip subnets tag 20  
   !  
   route-map deny_tag20 deny 10  
    match tag 20  
   route-map deny_tag20 deny 20  
   !  
   router ospf 1  
    distribute-list route-map deny_tag20 in [file:1]

- 問題文要約（日本語）:  
R9／R10 間で OSPF と RIP を相互再配布しており、RIP で学んだ経路が OSPF に入り、さらに RIP 側へ戻されることでループが発生している。  
解決したいことは：  
1) RIP → OSPF へ再配布する際に tag 20 を付与する。  
2) その後、OSPF → RIP 側に戻すとき、「もともと RIP から来た経路（tag 20）」を RIP へ再配布しないようにフィルタする。  
これを正しく実現する設定はどれか、という問題。[file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  1. RIP → OSPF 方向でタグ付け
     - `router ospf 1`  
       `redistribute rip subnets tag 20`  
       により、「RIP から OSPF に入るすべての再配布経路」に tag 20 が付与される。  
       このタグが「元の出どころは RIP」という印になる。 [file:1]

  2. OSPF → RIP 方向で“タグ 20”を拒否
     - `route-map deny_tag20` では  
       - `deny 10` で `match tag 20` を指定 → tag 20 の経路を拒否  
       - `permit 20` でそれ以外の経路を許可  
       という構成になっている。 [file:1]
     - この route-map を OSPF ではなく **RIP プロセス側** で入力フィルタとして適用するのがポイント。  
       B の `router rip 1` → `distribute-list route-map deny_tag20 in` によって、  
       OSPF から RIP に“入り込んでくる”経路のうち、tag 20 が付いているもの（＝元々 RIP だった経路）を RIP に戻さない。 [file:1]
     - こうすることで、「RIP で学んだ経路 → OSPF → 再び RIP」というループ経路が防止される。 [file:1]

  3. 他の選択肢がダメな理由
     - A / C / D は route-map を OSPF 側に適用しており、「OSPF に入ってくる経路」のフィルタになってしまう。  
       しかしタグ 20 は“RIP から OSPF に再配布するときに付く”ため、「OSPF → RIP に戻るとき」にフィルタする必要がある。 [file:1]
     - C は tag 20 も permit してしまうのでループ防止にならない。D は全 deny で、OSPF 側の経路受け取り自体を止めてしまう。 [file:1]

***
QUESTION 396 解説

- 問題文（英語）:
Refer to the exhibits. London must reach Rome using a faster path via EIGRP if all the links are up, but it failed to take this path. Which action resolves the issue? [file:1]

A. Use the network statement on London to inject the 172.16.X.0/24 networks into EIGRP.  
B. Increase the bandwidth of the link between London and Barcelona  
C. Use the network statement on Rome to inject the 172.16.X.0/24 networks into EIGRP.  
D. Change the administrative distance of RIP to 150. [file:1]

- 問題文要約（日本語）:  
London から 172.16.x.0/24（Rome 側のネットワーク）へ到達する際、本来は EIGRP 経由で「London → Barcelona → Rome」という高速パス（1Gbps×2 本）を使わせたい。  
しかし現状では、London は Rome 直結リンク（100Mbps）を使ってしまい、高速パスを選んでくれない。  
どの設定を変更すると、London が EIGRP 経由の高速パスを選ぶようになるか、という問題。[file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - 現状、London は 172.16.x.0/24 を  
    - EIGRP external（`redistribute connected` など）として AD 170  
    - RIP 経路として AD 120  
    の 2 つで学習しており、AD の小さい RIP が常に優先されている。[file:1]
  - RIP はホップ数ベースなので、「London–Rome 直結 100Mbps（1 hop）」と「London–Barcelona–Rome（2 hop）」を比べると、直結 100Mbps を選んでしまう。[file:1]
  - そこで Rome 側で `network 172.16.X.0 0.0.0.255` のように EIGRP の network ステートメントを追加し、そのネットワークを「EIGRP 内部経路（internal）」として広告させる。  
    その結果、London は 172.16.x.0/24 を  
    - EIGRP internal（AD 90）  
    - RIP（AD 120）  
    の 2 種類で学び、AD 90 の EIGRP internal を優先するようになる。[file:1]
  - EIGRP internal どうしであれば、帯域・遅延から計算される metric が使われるため、最終的に 1Gbps×2 本を通る London → Barcelona → Rome の経路が選ばれ、「速いパスを優先」という要件を満たす。[file:1]
  - A は London 側で network コマンドを打っても、172.16.x.0/24 は London 直下のネットワークではないため意味がない。  
    D の AD 変更も方針としては RIP を不利にできるが、この問題の意図は「172.16.x.0/24 を EIGRP internal にする」ことにある。 [file:1]

***
QUESTION 397 解説

- 問題文（英語）:
Refer to the exhibit. Bangkok is using ECMP to reach 172.20.2.0/24 network. The network administrator must configure it in such a way that traffic from 172.16.2.0/24 network uses the Singapore router as the preferred route. Which set of configurations accomplishes this task? [file:1]

A. Bangkok  
   access-list 101 permit ip 172.16.2.0 0.0.0.255 172.20.2.0 0.0.0.255  
   !  
   route-map PBR1 permit 10  
    match ip address 101  
    set ip next-hop 172.19.1.2  
   !  
   interface Ethernet0/2  
    ip policy route-map PBR1  

B. Dubai  
   access-list 101 permit ip 172.16.2.0 0.0.0.255 172.20.2.0 0.0.0.255  
   !  
   route-map PBR1 permit 10  
    match ip address 101  
    set ip next-hop peer-address  
   !  
   interface Ethernet0/0  
    ip policy route-map PBR1  

C. Dubai  
   access-list 101 permit ip 172.16.2.0 0.0.0.255 172.20.2.0 0.0.0.255  
   !  
   route-map PBR1 permit 10  
    match ip address 101  
    set ip next-hop 172.19.1.2  
   !  
   interface Ethernet0/0  
    ip policy route-map PBR1  

D. Bangkok  
   access-list 101 permit ip 172.16.2.0 0.0.0.255 172.20.2.0 0.0.0.255  
   !  
   route-map PBR1 permit 10  
    match ip address 101  
    set ip next-hop 172.19.1.2  
   !  
   interface Ethernet0/1  
    ip policy route-map PBR1 [file:1]

- 問題文要約（日本語）:  
Bangkok ルータは、172.20.2.0/24 宛トラフィックに対して ECMP（Equal-Cost Multi-Path）で複数経路を持っており、通常は負荷分散する。  
しかし、**送信元が 172.16.2.0/24 のトラフィックだけは、必ず Singapore ルータ（次ホップ 172.19.1.2）経由で送りたい**。  
この「送信元条件つきの優先経路」を実現できる設定はどれか、という問題。[file:1]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - 「送信元サブネット（172.16.2.0/24）＋宛先サブネット（172.20.2.0/24）で経路を変えたい」という要件なので、通常のルーティング（宛先のみで決定）ではなく **PBR（Policy-Based Routing）** が必要。 [file:1]
  - PBR は「パケットが**入ってくるインターフェイス**」に `ip policy route-map XXX` を適用して動く。  
    172.16.2.0/24 からのトラフィックは、図から Bangkok のインターフェイス Ethernet0/2 に入ってくるので、このインターフェイスに PBR を適用する必要がある。 [file:1]
  - 選択肢 A は Bangkok で以下を実施している:
    - ACL 101 で  
      `source: 172.16.2.0/24, destination: 172.20.2.0/24` をマッチ  
    - route-map PBR1 で ACL 101 に一致するトラフィックを  
      `set ip next-hop 172.19.1.2`（Singapore ルータ）へ強制送出  
    - これを **Ethernet0/2（172.16.2.0/24 からのトラフィックが入る口）** に `ip policy route-map PBR1` で適用  
    → 要件どおり、「その送信元のトラフィックだけ Singapore 経由」にできる。 [file:1]
  - B / C は Dubai 側で PBR をかけており、Bangkok での ECMP でどの出口を選ぶかという問題の解決にはならない。  
    D はルータは Bangkok だが、PBR を適用するインターフェイスが Ethernet0/1 で、これは 172.16.2.0/24 からのトラフィック流入インターフェイスではないため、マッチしてほしいトラフィックに PBR が効かない。 [file:1]

***
QUESTION 398 解説

- 問題文（英語）:
Drag and drop the descriptions from the left onto the corresponding MPLS components on the right. Select and Place. [file:1]

- 問題文要約（日本語）:  
MPLS の主要コンポーネント（例: PE, P, CE, VRF, LSP, ラベルなど）に対して、それぞれの役割・説明文を対応づけるドラッグ＆ドロップ問題。  
受験者は「どの役割がどのコンポーネント名に対応するか」を理解している必要がある。[file:1]

- 代表的な対応づけイメージ（学習用）:
  - 「顧客ネットワークと MPLS コアを接続し、VRF を持つエッジルータ」  
    → Provider Edge（PE）[file:1]
  - 「MPLS コア内部でラベルをスワップして転送するルータ」  
    → Provider（P）router[file:1]
  - 「MPLS VPN への出入り口として PE に接続される顧客側ルータ」  
    → Customer Edge（CE）router[file:1]
  - 「顧客ごとに分離されたルーティングテーブル」  
    → VRF（VPN Routing and Forwarding）[file:1]
  - 「ラベル付きパケットがコア内を通る一方向の論理パス」  
    → LSP（Label Switched Path）[file:1]
  - 「20bit のラベル値＋EXP＋Bottom-of-Stack＋TTL で構成されるヘッダ情報」  
    → MPLS label[file:1]

***
QUESTION 399 解説

- 問題文（英語）:
Routing table in Cape Town  
Refer to the exhibit. The network administrator must configure Cape Town to reach Dubai via Tokyo based on the speeds provided by the service provider. It was noticed that Cape Town is reaching Dubai directly and failed to meet the requirement. Which configuration fixes the issue? [file:1]

A. Cape Town  
   router eigrp 100  
    variance 2  

B. Cape Town  
   interface E0/0  
    bandwidth 5000  
   interface E0/1  
    bandwidth 10000  

   Dubai  
   interface E0/0  
    bandwidth 50000  
   interface E0/1  
    bandwidth 5000  

   Tokyo  
   interface E0/0  
    bandwidth 50000  
   interface E0/1  
    bandwidth 10000  

C. Dubai  
   router eigrp 100  
    variance 2  

D. Cape Town  
   interface E0/0  
    bandwidth 5000  
   interface E0/1  
    bandwidth 10000 [file:1]

- 問題文要約（日本語）:  
Cape Town から Dubai へは、本来「Cape Town → Tokyo → Dubai」という“速い経路”を使わせたいが、現状は Cape Town から Dubai 直結リンクを優先してしまっている。  
EIGRP のルーティングテーブルでは、Cape Town から見た Dubai 側のネットワークへのメトリックが、いずれも同じ値（409600）で表示されており、インタフェース帯域の違いが反映されていない。  
サービスプロバイダから提示された回線速度に基づいて EIGRP のパス選択をさせるには、どの設定が必要かを問う問題。[file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - Cape Town のルーティングテーブルでは、Dubai・Tokyo 側の 192.168.x.0/24 いくつかのネットワークに対して、すべて EIGRP メトリックが 409600 と同一値になっている。  
    これは、Cape Town 側の 2 本のインタフェース（E0/0 と E0/1）の `bandwidth` 設定が同じ（おそらく Ethernet のデフォルト帯域）だから、EIGRP がどちらも“同じ速さ”とみなしていることを意味する。[file:1]
  - EIGRP のメトリックは主に「帯域と遅延」に依存するため、サービスプロバイダが提示するリンク速度に合わせて、**ルートを学習する各ルータのインタフェース帯域**を正しく設定してやる必要がある。[file:1]
  - 選択肢 B は、Cape Town・Dubai・Tokyo のそれぞれのインタフェースに対して、SP が提供するリンク速度に応じた `bandwidth` 値を設定している。  
    これにより、Cape Town から見た  
    - 直結の Dubai 経路  
    - Tokyo 経由の Dubai 経路  
    で、メトリックに差が付き、「Tokyo 経由の方が速い」と評価されるようになる。[file:1]
  - D は Cape Town のインタフェースだけ帯域を変えているが、Dubai や Tokyo 側のインタフェース帯域がデフォルトのままだと、ネットワーク全体で正しいメトリックにならない。  
    A / C の `variance` は“負荷分散の幅”を変えるものであり、今回必要なのは「メトリックの正しい計算」であって、ECMP 幅の調整ではない。 [file:1]

***
QUESTION 400 解説

- 問題文（英語）:
Refer to the exhibit. An engineer configured NetFlow but cannot receive the flows from R1. Which two configurations resolve the issue? (Choose two.) [file:1]

A. R3(config)#ip access-list extended DDOS  
   R3(config-ext-nacl)#5 permit udp any host 10.66.66.66 eq 1090  

B. R4(config)#ip access-list extended DDOS  
   R4(config-ext-nacl)#5 permit udp any host 10.66.66.66 eq 1090  

C. R1(config)#flow exporter FlowExporter1  
   R1(config-flow-exporter)#destination 10.66.60.66  

D. R3(config)#flow exporter FlowExporter1  
   R3(config-flow-exporter)#destination 10.66.66.66  

E. R4(config)#flow exporter FlowExporter1  
   R4(config-flow-exporter)#destination 10.66.66.66 [file:1]

- 問題文要約（日本語）:  
R1 で NetFlow を設定したが、コレクタ（10.66.66.66:1090）にフローが届かない。  
R1 では exporter/monitor/インタフェース適用は正しく設定済みだが、途中経路の R3 と R4 には DoS 対策用 ACL「DDOS」が適用されており、NetFlow の UDP 1090 パケットがブロックされている。  
どの設定を追加すれば、R1 からの NetFlow フローがコレクタに届くようになるかを問う問題。[file:1]

- 正解: **A, B**

- 解説（なぜ A と B が正しいか）

  - 展示の設定では、R1 側の NetFlow exporter などは既に正しく  
    `destination 10.66.66.66` と設定されているため、C のような「宛先を 10.66.60.66 にする」設定は逆に誤りとなる。[file:1]
  - コレクタに届かない原因は、経路途中の R3・R4 のインタフェースに適用されている ACL「DDOS」が、  
    UDP 1090 宛てトラフィックを許可していないためであり、NetFlow の UDP パケットがドロップされている。 [file:1]
  - したがって、R3 と R4 の両方で ACL DDOS に  
    `permit udp any host 10.66.66.66 eq 1090`  
    を追加し、NetFlow フロー（UDP 1090 → 10.66.66.66）だけを通過させる必要がある。  
    これが選択肢 A と B の内容であり、2 箇所とも修正しないとどちらかの経路で依然ブロックされる。 [file:1]
  - D / E は exporter を R3/R4 側にも作る設定だが、問題は「R1 からのフローが途中で ACL にブロックされていること」であり、R3/R4 を exporter にすることでは解決しない。 [file:1]

***
QUESTION 401 解説

- 問題文（英語）:
Refer to the exhibit. The Customer Edge router wants to use AS 100 as the preferred ISP for all external routes and ISP-2 as a backup.

Customer-Edge  
route-map SETAS  
 set as-path prepend 111  
!  
router bgp 64555  
 neighbor 192.168.1.1 remote-as 100  
 neighbor 192.168.2.2 remote-as 200  
 neighbor 192.168.2.2 route-map SETAS in  

After this configuration, all the backup routes have disappeared from the BGP table on the Customer Edge router. Which set of configurations solves the issue on the Customer Edge router? [file:1]

A. route-map SETAS  
    set as-path prepend 111  
   router bgp 64555  
    neighbor 192.168.1.1 remote-as 100  
    neighbor 192.168.2.2 remote-as 200  
    neighbor 192.168.2.2 route-map SETAS out  

B. route-map SETAS  
    set as-path prepend 200  
   router bgp 64555  
    neighbor 192.168.1.1 remote-as 100  
    neighbor 192.168.2.2 remote-as 200  
    neighbor 192.168.2.2 route-map SETAS in  

C. route-map SETAS  
    set as-path prepend 111  
   router bgp 64555  
    neighbor 192.168.2.2 remote-as 100  
    neighbor 192.168.1.1 remote-as 200  
    neighbor 192.168.2.2 route-map SETAS in  

D. route-map SETAS  
    set as-path prepend 200  
   router bgp 64555  
    neighbor 192.168.1.1 remote-as 100  
    neighbor 192.168.2.2 remote-as 200  
    neighbor 192.168.2.2 route-map SETAS out [file:1]

- 問題文要約（日本語）:  
Customer Edge ルータ（AS 64555）は、外部プレフィックスに対して「ISP-1（AS 100）を優先」「ISP-2（AS 200）はバックアップ」として使いたい。  
現状、CE は ISP-1 と ISP-2 の両方と eBGP ピアを張っているが、ISP-2 から受け取る経路に対して AS-path prepend を行うルートマップを **inbound** で適用したところ、「バックアップ用に残っていてほしい ISP-2 経路が BGP テーブルから消えてしまった」という状況になっている。  
どの設定にすれば、ISP-1 を優先しつつ、ISP-2 経路もバックアップとして BGP テーブルに残せるか、を問う問題。[file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - CE ルータ視点で「どちらの ISP 経路を優先するか」を決める属性の一つが **AS-path 長**。  
    自分が ISP から**受信する**経路に対して AS-path を長く（＝劣後）したい場合は、**in 方向の route-map** で prepend する。 [file:1]
  - ここで prepend すべき AS 番号は、通常「**隣接 ISP の AS 番号**」を使うことが推奨される。  
    このケースでは ISP-2 の AS は 200 なので、`set as-path prepend 200` とするのが自然。 [file:1]
  - よって、正しい構成は:
    - ISP-2（192.168.2.2, AS 200）から**受信するルート**に対し
    - `set as-path prepend 200` を **inbound** で適用する  
    → これが選択肢 B の設定であり、ISP-1 からの経路より AS-path が長くなり CE では ISP-1 経路が優先される一方、ISP-2 経路もバックアップとして BGP テーブルに残る。 [file:1]
  - A / D のように route-map を **outbound** に適用すると、「CE から ISP-2 へ広告する経路の AS-path」を変えるだけで、CE 自身の経路選択には効かない。  
    C は remote-as の組み合わせが誤りで、ISP の AS 番号が入れ替わってしまっているため、要件を満たさない。 [file:1]

***
QUESTION 402 解説

- 問題文（英語）:  
A network administrator is troubleshooting a failed AAA login issue on a Cisco Catalyst c3560 switch. When the network administrator tries to log in through SSH using TACACS+ username and password credentials, the switch is no longer authenticating and is failing back to the local account. Which action resolves this issue? [web:54]

A. Configure ip tacacs-server source-interface GigabitEthernet 1/1.  
B. Configure ip tacacs source-ip 192.168.100.55.  
C. Configure ip tacacs-server source-ip 192.168.100.55.  
D. Configure ip tacacs source-interface GigabitEthernet 1/1.  

- 問題文要約（日本語）:  
Catalyst 3560 スイッチで、TACACS+ を使った SSH ログイン AAA 認証がうまくいかず、TACACS+ ではなくローカルアカウントで認証されてしまっている。  
TACACS+ サーバには到達できる前提で、「どのコマンドを入れればこの問題を直せるか」を聞いている。 [web:54]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - Catalyst 3560 では、TACACS+ パケットの送信元インタフェースを指定する正しい構文は  
    `ip tacacs source-interface <interface>`  
    であり、`ip tacacs-server ...` や `source-ip` という構文はサポートされていない。 [web:54][web:56]
  - 試験の解説どおり、`ip tacacs ?` と打つと `source-interface` だけが補完候補として出るため、  
    有効なコマンドは D の  
    `ip tacacs source-interface GigabitEthernet1/1`  
    だけである。 [web:56]
  - TACACS+ サーバ側で「どの IP からのリクエストを受けるか」を ACL やクライアント設定で絞っている場合、  
    スイッチの送信元 IP が想定と違うと認証要求自体が受け付けられない。  
    送信元インタフェースを正しく指定することで、サーバ側設定と一致し、AAA 認証が正常に通るようになる。 [web:60]

***
QUESTION 403 解説

- 問題文（英語）:  
A network engineer must configure a DMVPN network so that a spoke establishes a direct path to another spoke if the two must send traffic to each other. A spoke must send traffic directly to the hub if required. Which configuration meets this requirement? [web:72][web:74]

A. （hub に nhs, spokes に redirect）  
B. （nhs / map / multicast の組み合わせが不正）  
C. At the hub router:  
   interface tunnel10  
    ip nhrp map multicast dynamic  
    ip nhrp redirect  
    tunnel mode gre multipoint  
   On the spokes router:  
   interface tunnel10  
    ip nhrp map multicast dynamic  
    ip nhrp shortcut  
    tunnel mode gre multipoint  

D. （mode gre multicast などの誤りを含む）

- 問題文要約（日本語）:  
DMVPN で「基本的にはハブ経由だが、スポーク同士が通信を開始したらスポーク間で直接トンネル（ショートカット）を張る」動きをさせたい、つまり DMVPN Phase 3 の挙動を実現したい。  
どの構成なら、最初はハブ経由でルーティングしつつ、必要に応じて spoke-to-spoke のダイレクトパスが自動で張られるかを問う。 [web:72][web:74]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - DMVPN Phase 3 のポイントは次の 2 つ。 [web:72][web:74][web:76]
    - ハブ側トンネル: `ip nhrp redirect`  
      → ハブが「この宛先はあのスポークに直接行った方がいいよ」という **NHRP redirect** を送る。
    - スポーク側トンネル: `ip nhrp shortcut`  
      → スポークが hub からの redirect を受け入れ、宛先スポークへの **ショートカットトンネル** を張る。
  - さらに、`ip nhrp map multicast dynamic` により、NHRP がマルチキャストを含むマッピングを自動生成し、mGRE 上で複数スポークを柔軟に扱えるようにする。 [web:70][web:78]
  - 選択肢 C は:
    - Hub: `ip nhrp map multicast dynamic` と `ip nhrp redirect` を設定し、Phase 3 のハブ要件を満たす。
    - Spoke: `ip nhrp map multicast dynamic` と `ip nhrp shortcut` を設定し、redirect を受けて spoke 間トンネルを張れるようにする。
    - どちらも `tunnel mode gre multipoint`（mGRE）で DMVPN に適したモードになっている。 [web:74][web:76]
  - 他の選択肢は、`ip nhrp nhs` と `redirect/shortcut` の場所が逆だったり、`tunnel mode gre multicast` という不正なモード指定など、コマンドや役割の組み合わせがおかしいため、要件と IOS の仕様を同時に満たさない。 [web:69][web:79]

***
QUESTION 404 解説

- 問題文（英語）:  
Refer to the exhibit. The network engineer configured the summarization of the RIP routes into the OSPF domain on R5 but still sees four different 172.16.0.0/24 networks on R4. Which action resolves the issue? [file:1]

A. R4(config)#router ospf 1  
   R4(config-router)#no area  
   R4(config-router)#summary-address 172.16.0.0 255.255.252.0  

B. R4(config)#router ospf 99  
   R4(config-router)#network 172.16.0.0 0.255.255.255 area 56  
   R4(config-router)#summary-address 172.16.0.0 255.255.252.0  

C. R5(config)#router ospf 1  
   R5(config-router)#no area  
   R5(config-router)#summary-address 172.16.0.0 255.255.252.0  

D. R5(config)#router ospf 99  
   R5(config-router)#network 172.16.0.0 0.255.255.255 area 56  
   R5(config-router)#summary-address 172.16.0.0 255.255.252.0 [file:1]

- 問題文要約（日本語）:  
R5 は RIP ルートを OSPF ドメインに再配布している ASBR。  
R5 で 172.16.0.0/16 のサマリを設定したつもりだが、R4 からは依然として 172.16.0.0/24 が 4 本（/24×4）見えており、期待する 172.16.0.0/22 のサマリ 1 本になっていない。  
どこで、どのコマンドを使ってサマリを行えば、R4 から 172.16.0.0/22 だけが見えるようになるかを問う問題。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - OSPF でのサマリには大きく 2 種類ある。 [file:1]
    - ABR でエリア間ルートをまとめる `area X range`  
    - ASBR で**外部ルート（redistribute されたルート）**をまとめる `summary-address`
  - 問題の 172.16.0.0/24×4 は「RIP から OSPF に再配布された外部ルート」なので、  
    **再配布している ASBR（R5）で `summary-address` を使ってまとめる必要がある**。 [file:1]
  - しかし、R5 には `area ... range ...` が設定されており、これは「あるエリア内の OSPF ルート」をまとめるコマンドであって、「RIP から再配布された外部ルート」には効かない。  
    そのため、R4 には依然として 4 つの /24 が見えてしまう。 [file:1]
  - 適切な対応は:
    - R5 の OSPF プロセスで、既存の `area ... range` を削除した上で  
      `summary-address 172.16.0.0 255.255.252.0`  
      を設定すること。  
    → これが選択肢 C の内容であり、ASBR で外部ルートを 172.16.0.0/22 に要約し、R4 にはそのサマリ 1 本だけが見えるようになる。 [file:1]
  - A・B・D は R4 側でサマリしようとしたり、OSPF プロセス番号やエリアの前提が問題文と合っていないため、外部ルートを正しく要約できない。 [file:1]

***
QUESTION 405 解説

- 問題文（英語）:  
Refer to the exhibit. An engineer configured route exchange between two different companies for a migration project. EIGRP routes were learned in router C, but no OSPF routes were learned in router A. Which configuration allows router A to receive OSPF routes? [file:1]

A. (config-router-af-topology)#redistribute connected  
B. (config-router-af-topology)#redistribute ospf 10 metric 1000000 10 255 1 1500  
C. (config-router-af-topology)#no redistribute ospf 10 match external 1 external 2 metric 1000000 10 255 1 1500  
D. (config-router-af-topology)#redistribute ospf 10 1000000 10 255 1 1500  

- 問題文要約（日本語）:  
2 社間のマイグレーション用に、ルータ B で OSPF と EIGRP 間の経路再配布を設定した。  
Router C では EIGRP 経路は学習できているが、Router A 側（EIGRP ドメイン側）で OSPF の経路が一切学習できていない。  
EIGRP address-family topology モードで、どの再配布コマンドを入れれば Router A が OSPF 経路を受け取れるようになるか、という問題。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - Router B は「OSPF（プロセス 10）から EIGRP への再配布」を行う必要があるが、**現在の再配布設定では OSPF 内部ルートが EIGRP に出ていない**。 [file:1]
  - EIGRP named/af-topology で OSPF を再配布するときは、`redistribute ospf 10 metric <bw> <delay> <reliability> <load> <mtu>` のように、**metric キーワードと 5 つのメトリック値**を明示しないとルートが再配布されない。 [file:1]
  - 選択肢 B はこの形式を満たしており、  
    `redistribute ospf 10 metric 1000000 10 255 1 1500`  
    で OSPF ルートを適切な EIGRP メトリックを伴って再配布できるため、Router A に OSPF 経路が届くようになる。 [file:1]
  - A の connected 再配布では OSPF ルートが EIGRP に渡らない。  
    C は `no redistribute ...` なので、逆に OSPF 再配布を無効化してしまう。  
    D は metric キーワードが抜けた誤った構文であり、IOS に受け付けられない。 [file:1]

***
QUESTION 406 解説

- 問題文（英語）:  
Refer to the exhibit. The network administrator configures the Chicago router to mutually redistribute the LA and NewYork routes with EIGRP routes to be summarized as a single route in OSPF:

router eigrp 100  
 redistribute ospf 1 metric 10 10 10 10 10  
router ospf 1  
 redistribute eigrp 100 subnets  
 area 0 range 192.168.4.0 255.255.252.0  

After the configuration, the LA router receives all the specific NewYork routes except the summary route. Which set of configurations resolve the issue on the Chicago router? [file:1]

A. router ospf 1  
   summary-address 192.168.4.0 255.255.252.0  

B. router ospf 1  
   area 0 range 192.168.4.0 255.255.0.0  

C. interface E 0/0  
   summary-address 192.168.4.0 255.255.252.0  

D. interface E 0/0  
   ip summary-address eigrp 100 192.168.4.0 255.255.252.0  

- 問題文要約（日本語）:  
Chicago ルータで LA 側（EIGRP）と NewYork 側（OSPF）を相互再配布している。  
Chicago では OSPF 側で `area 0 range 192.168.4.0 255.255.252.0` を設定し、「EIGRP から再配布されたルートを 192.168.4.0/22 の 1 本に要約したつもり」だが、LA ルータでは NewYork の個別ルートだけが届き、サマリルートが見えない。  
ASBR（Chicago）でどのコマンドを入れれば、LA 側に 192.168.4.0/22 のサマリ OSPF ルートが届くかを問う問題。 [file:1]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - OSPF のサマリには 2 種類ある。 [file:1]
    - ABR でエリア間ルート（inter-area）をまとめる `area X range`  
    - ASBR で**外部ルート（external：他プロトコルから再配布されたルート）**をまとめる `summary-address`
  - 今回 Chicago は「EIGRP を OSPF に再配布している ASBR」であり、192.168.4.x のルートは OSPF 外部ルート（Type 5）として扱われる。  
    → これらをサマリするには **ASBR 上で `summary-address` を使う必要があり、`area 0 range` では外部ルートは要約されない。** [file:1]
  - したがって、Chicago の OSPF プロセスに次を追加するのが正解。  
    `router ospf 1`  
    ` summary-address 192.168.4.0 255.255.252.0`  
    これにより、EIGRP から再配布された外部ルート群が 192.168.4.0/22 として LA 側に広告される。 [file:1]
  - B はマスクもおかしく、かつ `area range` なので依然として外部ルートには効かない。  
    C は OSPF の構文として不正。  
    D は EIGRP 側のインターフェイスサマリであり、「OSPF での外部ルート要約」という要件を満たさない。 [file:1]

***
QUESTION 407 解説

- 問題文（英語）:  
Refer to the exhibit. R1 cannot authenticate via TACACS. Which configuration resolves the issue? [file:1]

A. tacacs server DC1_TACACS  
   address ipv4 10.60.66.66  
   key D@t@c3nter1TACACS  

B. aaa group server tacacs+ DC1_TACACS  
   server name DC_TACACS  

C. tacacs server DC1_TACACS  
   address ipv4 10.66.66.66  
   key D@t@c3nter1TACACS  

D. aaa group server tacacs+ DC_TACACS  
   server name DC_TACACS  

- 問題文要約（日本語）:  
R1 から TACACS+ 認証をしようとしても失敗している。  
ログとコンフィグ（exhibit）から、TACACS サーバのホスト名やキーワードは合っているが、R1 上の `tacacs server DC1_TACACS` の定義に TACACS サーバの正しい IP アドレスが入っていない。  
どの設定を入れれば、R1 が正しい TACACS サーバ 10.66.66.66 に到達できるようになり、認証が成功するかを問う。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - exhibit 上の R1 の設定では、TACACS サーバ名 `DC1_TACACS` は定義されているが、**`address ipv4 10.66.66.66` が設定されておらず、サーバ IP が不完全**になっている。  
    その結果、R1 は実際の TACACS サーバ IP アドレス（10.66.66.66）にパケットを送れていない。 [file:1]
  - 選択肢 C は、サーバ名 `DC1_TACACS` に対して正しい IP アドレス 10.66.66.66 と既存のキー `D@t@c3nter1TACACS` を関連付けているため、R1 から TACACS サーバへの到達性が確立され、認証が動作する。 [file:1]
  - A は IP アドレスが 10.60.66.66 で誤っている。  
    B と D は AAA server-group 側の設定だが、**根本原因はサーバ定義の IP 欠落**なので、グループ設定だけを触っても TACACS サーバには届かない。 [file:1]

***
QUESTION 408 解説

- 問題文（英語）:  
What must be configured by the network engineer to circumvent AS_PATH loop prevention mechanism in IP/VPN Hub and Spoke deployment scenarios? [web:83][web:89]

A. Use allowas-in and as-override at all PEs  
B. Use allowas-in and as-override at the PE_Hub  
C. Use allowas at the PE_Hub  
D. Use as-override at the PE_Hub  

- 問題文要約（日本語）:  
IP VPN の Hub&Spoke 構成で、複数スポーク CE が同じカスタマ AS を使っていると、CE 間で経路を流すときに AS_PATH に自分の AS が現れて BGP のループ防止に引っかかる。  
この「AS_PATH ループ防止」を回避して、Hub 経由でスポーク同士の経路をちゃんと届けるには、どこで allowas-in と as-override を組み合わせればよいか、という問題。 [web:89][web:90]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - ループ防止に引っかかる典型パターン:  
    - Spoke CE1(AS 65000) → PE_Hub → Spoke CE2(AS 65000)  
    - CE2 から見ると、AS_PATH に自分の AS 65000 が含まれているので、通常はルートを破棄する。 [web:89]
  - 回避する代表的な 2 つの仕組み: [web:89]
    - **as-override（PE 側）**: PE が CE の AS を自分の Provider AS に書き換えて広告し、CE から見て「自 AS が AS_PATH に出てこない」ようにする。  
    - **allowas-in（CE 側）**: CE が「AS_PATH 内に自分の AS があっても許可して受け入れる」ようにする。
  - Hub&Spoke の IP VPN では、Hub PE で両方を組み合わせることで、Hub 経由のスポーク間経路が AS_PATH ループ防止に引っかからないようにできる、という想定で出題されている。  
    → そのため、「Hub の PE で allowas-in と as-override の両方を使う」B が問題の意図に最も沿った答えとして扱われている。 [web:88][web:91]
  - A（全 PE）だと要件以上に広く設定する形になり、試験の選択肢としては過剰。  
    C は allowas-in だけで、PE 側で AS の書き換えが行われない。  
    D は as-override だけで、「自 AS が AS_PATH に残るケースを許可する」という観点が欠けている。 [web:89]

***
QUESTION 409 解説

- 問題文（英語）:  
Refer to the exhibit. An administrator can log in to the device using Telnet, but the attempts to log in to the same device using SSH with the same credentials fail. Which action resolves this issue? [file:1]

A. Configure transport input all on the VTY lines to allow SSH.  
B. Configure the VTY lines with login local.  
C. Configure to use the Telnet user database for SSH as well.  
D. Configure SSH service on the router.  

- 問題文要約（日本語）:  
管理者は Telnet ではログインできるが、同じユーザ名・パスワードで SSH ログインしようとすると失敗する。  
コンソール出力（exhibit）から、SSH 自体は有効化されているが、VTY での認証方法が「パスワードのみ前提」になっており、**SSH が必須とする “ユーザ名＋パスワード” の認証と噛み合っていない**状況。  
どの設定を入れれば SSH でも正しくログインできるかを問う。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - Telnet は `line vty` に `password xxx` と `login` があれば、「ユーザ名なし・パスワードのみ」でログイン可能。  
    そのため、現状でも Telnet は成功している。 [file:1]
  - SSH は必ず「ユーザ名＋パスワード」の組み合わせでログインするため、  
    VTY ライン側で「どのユーザ DB を使うか」を明示的に指定してやる必要がある。  
    もっともシンプルなのが `login local` で、ローカルユーザデータベース（`username xxx secret yyy` など）を使って VTY ログインさせる方法。 [file:1]
  - 選択肢 B の `login local` を VTY ラインに設定すれば、Telnet/SSH ともに **同じローカルユーザ名・パスワード**で認証されるようになり、SSH ログインも成功する。 [file:1]
  - A はプロトコル（telnet/ssh）の許可だけで、ユーザ名/パスワード DB の問題を解決しない。  
    C は抽象的で、具体的な IOS コマンドとして成立していない。  
    D は既に SSH が動作している前提（鍵生成済みなど）なので、根本原因ではない。 [file:1]

***
QUESTION 410 解説

- 問題文（英語）:  
An engineer must override the normal routing behavior of a router. The engineer must send HTTP traffic that is destined to 10.100.100.100 from 10.1.1.0/24 via a next hop of 10.2.2.2, two hops away from the router that is connected to the 10.1.1.0/24 subnet. Which configuration reroutes traffic according to this requirement?

A. access-list 100 permit tcp 10.1.1.0 0.0.0.255 host 10.100.100.100 eq http  
   !  
   route-map POLICY permit 10  
    match ip address 100  
    set ip next-hop recursive 10.2.2.2  

B. access-list 100 permit tcp 10.1.1.0 0.0.0.255 host 10.100.100.100 eq http  
   !  
   route-map POLICY permit 10  
    match ip address 100  
    set ip next-hop 10.2.2.2  
   route-map POLICY permit 20  

C. access-list 100 permit tcp 10.1.1.0 0.0.0.255 host 10.100.100.100 eq http  
   !  
   route-map POLICY permit 10  
    match ip address 100  
    set ip next-hop recursive 10.2.2.2  
   route-map POLICY permit 20  

D. access-list 100 permit tcp 10.1.1.0 0.0.0.255 host 10.100.100.100 eq http  
   !  
   route-map POLICY permit 10  
    match ip address 100  
    set ip next-hop 10.2.2.2 [web:90]

- 問題文要約（日本語）:  
通常のルーティングを上書きして、10.1.1.0/24 から 10.100.100.100 への **HTTP トラフィックだけ** を、2 ホップ先のネクストホップ 10.2.2.2 に送る必要がある。  
10.2.2.2 はこのルータに直接接続されていない（2 ホップ先）ので、PBR（Policy-Based Routing）で “再帰的に” 次ホップを解決しつつ、対象トラフィックだけを振り向けたい。どの設定が要件を満たすか、という問題。 [web:90]

- 正解: **A**

- 解説（なぜ A が正しいか）

  - まず「ソースと宛先とポートで経路を変えたい」ので、通常のルーティングではなく **PBR（route-map + ACL）** が必要になる。  
    → ACL 100 で「10.1.1.0/24 → 10.100.100.100 の HTTP トラフィック」をマッチ。 [web:90]
  - 次ホップ 10.2.2.2 は **直接接続でない**（2 ホップ先）ため、PBR で `set ip next-hop` を使う場合は `recursive` キーワードが必要。  
    `set ip next-hop recursive 10.2.2.2` により、「まず 10.2.2.2 へのルートをルーティングテーブルから探して、その次ホップへ実際の転送を行う」動作になる。 [web:69]
  - 選択肢 A は:
    - ACL で対象トラフィックを正しく絞り込み  
    - route-map でそのトラフィックだけに対し  
    - `set ip next-hop recursive 10.2.2.2` を適用  
    しており、要件通り「HTTP のみを 2 ホップ先経由で送る」設定になる。 [web:90]
  - B と D は `recursive` がなく、「10.2.2.2 が直接接続されている」前提の設定になっており、この問題の前提を満たさない。  
    C は `recursive` は正しいが、`route-map POLICY permit 20` により「その他すべてのトラフィック」も PBR の対象になってしまうため、想定より広く PBR を適用してしまう。 [web:69][web:90]

***
QUESTION 411 解説

- 問題文（英語）:  
Refer to the exhibit. The company implemented uRPF to address an antispoofing attack. A network engineer received a call from the IT security department that the regional data center is under an IP spoofing attack. Which configuration must be implemented on R1 to resolve this issue?

A. interface ethernet0/1  
   ip unicast RPF check reachable-via any allow-default allow-self-ping  

B. interface ethernet0/0  
   ip verify unicast reverse-path  

C. interface ethernet0/1  
   ip verify unicast reverse-path  

D. interface ethernet0/0  
   ip unicast RPF check reachable-via any allow-default allow-self-ping [file:1]

- 問題文要約（日本語）:  
企業は IP スプーフィング対策として uRPF を導入済み。  
ログ（exhibit）から、R1 では e0/0 には uRPF チェックが有効だが、**攻撃トラフィックが入ってくる e0/1 側には uRPF が有効化されていない**ことが分かる。  
リージョナル DC に向かうスプーフィング攻撃を防ぐには、どのインターフェイスでどのコマンドを有効にすべきかを問う。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - exhibit の説明では、  
    - e0/0: uRPF（ip verify unicast）がすでに有効  
    - e0/1: uRPF が無効  
    となっており、**攻撃は e0/1 から入ってきている**前提で問題が書かれている。 [file:1]
  - Cisco IOS では、uRPF（Unicast Reverse Path Forwarding）を有効にする代表的なコマンドが  
    `ip verify unicast reverse-path`（旧書式）または `ip verify unicast source reachable-via rx/any`（新書式）で、  
    いずれもインターフェイス単位で設定する。 [file:1]
  - よって、**攻撃が入るインターフェイス e0/1 に対して `ip verify unicast reverse-path` を設定する** C が要件を満たす。  
    これにより、R1 は「その送信元 IP に対する戻り経路が FIB 上に存在しないパケット」を e0/1 で破棄するようになり、スプーフィングを防止できる。 [file:1]
  - A と D は `ip unicast RPF check reachable-via any ...` という別構文で、問題文のヒント（`ip verify unicast ?` の出力）とズレている。  
    B は e0/0 に再度 uRPF をかけるだけで、攻撃入口である e0/1 を守れない。 [file:1]

***
QUESTION 412 解説

- 問題文（英語）:  
What is a characteristic of IPv6 RA Guard?

A. RA messages are allowed from the host port to the switch.  
B. It is unable to protect tunneled traffic  
C. It filters rogue RA broadcasts from connected hosts  
D. It is supported on the egress direction of the switch. [web:78]

- 問題文要約（日本語）:  
IPv6 RA Guard の「特徴」を 1 つ選ぶ問題。  
RA Guard は、ホストから勝手に送られる不正な Router Advertisement（RA）を止めたい場面で使われる L2 スイッチの機能。 [web:78]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - IPv6 RA Guard は、スイッチのポートにポリシーを適用して、**ホストポートからの不正な RA をフィルタリングする**機能。  
    ルータ役のポートだけ RA を許可し、それ以外のホストポートからの RA はブロックすることで、偽ルータによる攻撃を防ぐ。 [web:78]
  - Cisco の仕様上の制限として、  
    - トンネルされた IPv6 トラフィック内の RA には効かない（B も事実としては正しい）  
    - 入力方向（ingress）だけサポートで、出力方向（egress）はサポートしない（D は誤り） [web:78]
  - 選択肢ごとの整理:
    - A: host-role が設定されたポートでは RA は**許可されず**、むしろ禁止されるので誤り。 [web:78]
    - B: 仕様として正しいが、「特徴」として問われたときには C の方が RA Guard の本質を直接表している。  
    - C: 「接続ホストからの rogue RA をフィルタする」という、RA Guard の主目的そのものなので正解。  
    - D: RA Guard は **ingress のみサポート** であり、egress ではないので誤り。 [web:78]

***
QUESTION 413 解説

- 問題文（英語）:  
Refer to the exhibit. An engineer applied an IPv6 traffic filter on R1. The interface flapped between R1 and R2 and clearing the BGP session did not restore the BGP session and failed. Which action must the engineer take to restore the BGP session from R2 to R1?

A. Enable the BGP session, which went down when the session was cleared.  
B. Swap the source and destination IP addresses in the IPv6 traffic filter.  
C. Apply the IPv6 traffic filter in the outbound direction on the interface.  
D. ICMPv6 must be permitted by the IPv6 traffic filter. [file:1]

- 問題文要約（日本語）:  
R1 に IPv6 トラフィックフィルタを適用したあと、R1–R2 間インターフェイスが flap し、その後 BGP セッションを clear してもセッションが復活しない。  
ACL の記述を見ると、`deny ipv6 2001:DB8::/32 any` のようなルールで **ICMPv6 を含むあらゆる IPv6 トラフィックが遮断**されており、その結果 Neighbor Discovery（NS/NA）ができず、リンクローカルの解決や隣接関係が張れない状態。  
BGP セッション復旧には、どの対応が必要かを問う。 [file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - IPv6 では ARP がなく、**Neighbor Solicitation / Neighbor Advertisement（ICMPv6）** による Neighbor Discovery が必須。  
    これがフィルタで落ちると、L3 ではお互いの MAC を解決できず、実質的に到達不能になる。 [file:1]
  - 問題の IPv6 ACL は `deny ipv6 2001:DB8::/32 any` などの行で **R2 → R1 向きの ICMPv6 も丸ごと deny** しているため、  
    - NS/NA  
    - Router Solicitation / Advertisement  
    といった制御トラフィックも含めてブロックしてしまい、BGP の TCP セッションも張り直せない。 [file:1]
  - よって、BGP セッションを復活させるには、**ACL 内で ICMPv6 を許可する行を追加**して Neighbor Discovery を通す必要がある。  
    その意図を最も端的に表している選択肢が D「ICMPv6 must be permitted by the IPv6 traffic filter」。 [file:1]
  - A は BGP プロセス側の問題を想定しているが、根本原因は L3 到達性（ICMPv6 抑止）。  
    B や C は ACL 適用向きの問題にしているが、方向や src/dst を変えても ICMPv6 を許可しない限り状況は変わらない。 [file:1]

***
QUESTION 414 解説

- 問題文（英語）:  
Refer to the exhibit. After reloading the router, an administrator discovered that the interface utilization graphs displayed inconsistencies with their previous history in the NMS. Which action prevents this issue from occurring after another router reload in the future?

A. Rediscover all the router interfaces through SNMP after the router is reloaded.  
B. Save the router configuration to startup-config before reloading the router.  
C. Configure SNMP to use static OIDs referring to individual router interfaces.  
D. Configure SNMP interface index persistence on the router. [file:1]

- 問題文要約（日本語）:  
ルータを reload したあと、NMS 上のインターフェイス利用率グラフが「過去の履歴とつながらない」状態になっている。  
原因は、reload のたびに **インターフェイスの ifIndex（SNMP のインターフェイス番号）が変わり、NMS が「別インターフェイス」と誤認識する**こと。  
次回以降の reload でもグラフが途切れないようにするにはどうするか、という問題。 [file:1]

- 正解: **D**

- 解説（なぜ D が正しいか）

  - SNMP では、インターフェイスは `ifIndex` という番号で識別され、NMS はこの番号に紐づいてトラフィックグラフを描く。  
    ルータ再起動のたびに ifIndex が再割り当てされると、「同じ Gi0/0 でも別の OID」と見なされ、グラフが新規として開始されてしまう。 [file:1]
  - Cisco ルータでは、`snmp-server ifindex persist`（機種によってはインターフェイスモードの `snmp ifindex persist`）を設定することで、再起動しても ifIndex が変わらないようにできる。  
    これにより、NMS 側の OID とインターフェイスが継続的に一致し、グラフ履歴も綺麗に連続する。 [file:1]
  - A は毎回再ディスカバリが必要になり、運用負荷も高く履歴も分断される。  
    B は設定保存の話で ifIndex 変動とは無関係。  
    C の「静的 OID」は現実的ではなく、ifIndex が変わる前提を解決できない。 [file:1]

***
QUESTION 415 解説

- 問題文（英語）:  
Refer to the exhibit. An engineer applies a prefix-list filter that filters most of the network 10 prefixes instead of allowing them. Which action resolves the issue?

A. Modify the ip prefix-list EIGRP seq 10 permit 10.0.0.0/8 le 9 command.  
B. Modify the ip prefix-list EIGRP seq 20 permit 10.0.0.0/8 ge 9 command.  
C. Modify the ip prefix-list EIGRP seq 10 permit 10.0.0.0/8 le 32 command.  
D. Modify the ip prefix-list EIGRP seq 20 permit 10.0.0.0/8 le 32 command. [file:1]

- 問題文要約（日本語）:  
EIGRP で受信するプレフィクスを prefix-list でフィルタし、「10.x.x.x（10.0.0.0/8 のサブネット）」だけを許可したかったが、実際には 10.0.0.0/8 そのものしか通らず、ほとんどの 10 プレフィクスが落ちてしまっている。  
既存の prefix-list の seq 10 が `permit 10.0.0.0/8`（プレフィクス長ちょうど /8）だけを許可しているため、10.1.0.0/16 などのサブネットがマッチせず deny されている。これをどう直せばよいか、という問題。 [file:1]

- 正解: **C**

- 解説（なぜ C が正しいか）

  - prefix-list は「ネットワークアドレス＋プレフィクス長の範囲」でマッチを決める。  
    例えば `ip prefix-list EIGRP seq 10 permit 10.0.0.0/8` だけだと、「プレフィクス長がちょうど /8」の 10.0.0.0/8 のみ許可し、/9〜/32 の 10.x.x.x は一切マッチしない。 [file:1]
  - 10.0.0.0/8 配下のあらゆるサブネット（/9〜/32）をまとめて許可したいときは、  
    `10.0.0.0/8 le 32` のように「最大プレフィクス長」を指定してやる必要がある。  
    これにより、10.0.0.0/8 自身から 10.255.255.255/32 まで、10.0.0.0/8 の範囲に入る全サブネットがマッチする。 [file:1]
  - 選択肢 C は seq 10 を `permit 10.0.0.0/8 le 32` に修正しており、  
    既存の `seq 20 deny 0.0.0.0/0 le 32` を残したまま、「10.* は全部許可、それ以外は deny」という意図を 1 行で実現できる。 [file:1]
  - A（`le 9`）だと /8 と /9 しか許可されず、ほとんどのサブネットが依然としてフィルタされる。  
    B や D は `seq 20` 側の変更で、全体のロジックが崩れたり、10.0.0.0/8 本体を許可できなかったりするため、ベターではない。 [file:1]

***
QUESTION 416 解説

- 問題文（英語）:  
Refer to the exhibit. Which action makes 10.1.3.2 the feasible successor to reach 10.200.1.0/24?

A. Increase path bandwidth higher than 10.1.1.2 and lower than 10.1.2.2 between RtrA and the destination.  
B. Increase path bandwidth higher than 10.1.2.2 and lower than 10.1.1.2 between RtrA and the destination.  
C. Increase path bandwidth lower than 10.1.1.2 and lower than 10.1.2.2 between RtrA and the destination.  
D. Increase path bandwidth higher than 10.1.1.2 and higher than 10.1.2.2 between RtrA and the destination. [file:1]

- 問題文要約（日本語）:  
RtrA から宛先 10.200.1.0/24 へは、3 本の EIGRP 経路（次ホップ 10.1.1.2, 10.1.2.2, 10.1.3.2）がある。  
現在の successor は 10.1.1.2 へのパスで、10.1.2.2 経路も metric が近く候補になっているが、10.1.3.2 を「feasible successor」としてバックアップにしたい。  
帯域（bandwidth）変更でメトリックを調整するとして、どのような関係にすればよいかを問う。 [file:1]

- 正解: **B**

- 解説（なぜ B が正しいか）

  - EIGRP の **successor** は「最も低い FD（feasible distance＝実際に使っているメトリック）」を持つ経路。  
    **feasible successor** は「RD（reported distance＝隣接ルータから見た距離）が、現 successor の FD より小さい」経路だけが候補になる（feasibility condition）。 [file:1]
  - 帯域を上げるとメトリック（遅延＋帯域ベース）が下がり、帯域を下げるとメトリックが上がる、という前提で考える。  
    「10.1.3.2 をバックアップにしたい」ので、  
    - successor（10.1.1.2）よりは遅い（メトリックが大きい）  
    - もう一方の経路（10.1.2.2）よりは速い（メトリックが小さい）  
    という“中間のコスト”にしたい。 [file:1]
  - したがって、「10.1.2.2 より帯域を高く、10.1.1.2 より帯域を低く」する、という B の条件が、  
    - successor の座は奪わず（10.1.1.2 が最小 FD のまま）  
    - もう一方より良いサブ経路として FS 候補になる  
    という問題の意図に一番合っている。 [file:1]
  - A は 10.1.3.2 の帯域が最も高くなり successor に昇格してしまう。  
    C は 3 本の中で最も悪い経路になり FS 候補としても微妙。  
    D は 10.1.3.2 が最良経路になり successor を奪ってしまう。 [file:1]

***
EIGRP の feasible successor 条件を、自分用メモとして「RD と現 successor の FD の関係」を一文でどう表現しますか？```
 






































































